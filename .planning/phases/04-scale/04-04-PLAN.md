---
phase: 04-scale
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - cogs/music.py
  - cogs/ai.py
  - cogs/imagine.py
autonomous: true
nyquist_compliant: true
requirements: [SCALE-01, SCALE-04]
must_haves:
  truths:
    - "Every cog's DB access uses self.bot.pool (asyncpg), not self.bot.db (aiosqlite) (SCALE-02 consumer)"
    - "The 3 per-/play writes route through log_track_batch in one transaction (D-06, SCALE-01)"
    - "QueueFullError from queue.add() is caught at the cog and returns a personality rejection (D-04, SCALE-01)"
    - "Every MusicQueue mutation site persists queue state via self.bot.queue_persistence (D-19, SCALE-04)"
    - "Voice-channel id is captured from guild.voice_client.channel at persist time (D-20)"
    - "Gemini limiter stays a single GLOBAL instance — not split per-guild (D-03, confirm-only)"
  artifacts:
    - path: "cogs/music.py"
      provides: "pool migration, log_track_batch call, QueueFullError handling, persist-on-mutation hooks, voice-channel-id capture"
      contains: "queue_persistence"
    - path: "cogs/ai.py"
      provides: "self.bot.pool migration; global Gemini limiter unchanged (D-03)"
      contains: "self.bot.pool"
    - path: "cogs/imagine.py"
      provides: "self.bot.pool migration"
      contains: "self.bot.pool"
  key_links:
    - from: "cogs/music.py _log_track()"
      to: "database.log_track_batch"
      via: "await log_track_batch(self.bot.pool, ...)"
      pattern: "log_track_batch"
    - from: "cogs/music.py mutation sites"
      to: "self.bot.queue_persistence.persist"
      via: "await self.bot.queue_persistence.persist(guild, queue, vc_id)"
      pattern: "queue_persistence\\.persist"
    - from: "cogs/music.py /play handler"
      to: "QueueFullError"
      via: "try: queue.add(track) except QueueFullError"
      pattern: "QueueFullError"
---

<objective>
Migrate the three database-consuming cogs from the aiosqlite `self.bot.db` accessor to the asyncpg `self.bot.pool`, route the per-/play writes through the batched `log_track_batch` transaction (D-06), enforce the queue cap by catching `QueueFullError` at the cog layer with a personality rejection (D-04), and inject persist-on-mutation hooks (with voice-channel-id capture, D-20) at every `MusicQueue` mutation site so queues survive restarts (D-19, SCALE-04). While migrating `cogs/ai.py`, confirm the global Gemini 15 RPM limiter is left intact (D-03).

Purpose: SCALE-01 (DB-write-contention batched transaction at its call site; queue-cap enforcement) and SCALE-04 (write-on-mutation persistence). This is the consumer wave — it depends on the pool (04-03), the helpers (04-02), and `QueueFullError`/`to_dict` (04-01).
Output: All cogs on the pool; `/play` logging batched; cap rejection wired; persist hooks at add/skip/advance/shuffle/clear/loop-change.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-scale/04-CONTEXT.md
@.planning/phases/04-scale/04-RESEARCH.md
@.planning/phases/04-scale/04-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created/modified by THIS plan)

- **Accessor change** (`cogs/music.py`, `cogs/ai.py`, `cogs/imagine.py`): `db` property returns `self.bot.pool`; raw `self.db.execute(...)` sites converted to `async with self.bot.pool.acquire() as conn:` with `$N` params
- **Batched call** (`cogs/music.py` `_log_track`): the 3 separate `log_song`/`update_artist_count`/`update_user_profile` calls replaced by one `log_track_batch(self.bot.pool, ...)`
- **Cap handling** (`cogs/music.py`): `try/except QueueFullError` around every `queue.add(track)` call site (single-add and playlist loop) with a personality `followup`
- **Persist hooks** (`cogs/music.py`): `await self.bot.queue_persistence.persist(guild, queue, vc_id)` after add/skip/advance/shuffle/loop-change; `await self.bot.queue_persistence.clear_persisted(guild.id)` after `queue.clear()`
- **Voice-channel-id capture** (`cogs/music.py`): `vc_id = guild.voice_client.channel.id if guild.voice_client else None` at each persist site (D-20)
- **Confirm-only (no code change)** (`cogs/ai.py`): the single global `self.bot.gemini_service` limiter is left untouched (D-03)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migrate cog DB access to asyncpg pool + batched /play logging</name>
  <files>cogs/music.py, cogs/ai.py, cogs/imagine.py</files>
  <read_first>
    - cogs/music.py (the `db` property lines 273-275; `_log_track` lines 627-720 — the 3 sequential writes + raw `self.db.execute` at ~650; the raw `self.db.execute` at ~540; all `database` imports lines 13-16)
    - cogs/ai.py (the `db` property line 89; `self.db` call sites lines 101-153; the `self.bot.gemini_service` usage — confirm the global limiter, do NOT change it, D-03)
    - cogs/imagine.py (the `db` property line 46; `self.db` call sites lines 53-82)
    - database.py (the rewritten asyncpg helpers + new `log_track_batch` from 04-02 — every helper now takes `pool: asyncpg.Pool`)
    - .planning/phases/04-scale/04-PATTERNS.md §"database.py" (lines 63-79 pool.acquire pattern), §"services/gemini.py" (lines 389-397 — limiter is already global, confirm-only), and §"Shared Patterns" → "Pool acquisition" (lines 473-480)
    - .planning/phases/04-scale/04-RESEARCH.md Pattern 3 (log_track_batch, lines 254-298), Pitfall 6 (boolean columns — any `was_skipped`/`was_auto_queued` literal must be Python bool, lines 659-663)
    - .planning/phases/04-scale/04-CONTEXT.md D-03, D-06
  </read_first>
  <action>
    In all three cogs, change the `db` property to return `self.bot.pool` (rename to `pool` is optional but if you keep the name `db` it must return `self.bot.pool`; prefer adding a `pool` property `return self.bot.pool` and updating call sites for clarity). Every helper call already passes the accessor as the first positional arg — those now pass the pool, which matches the new asyncpg signatures, so most call sites need only the accessor swap. Convert the two RAW inline queries in cogs/music.py (the `self.db.execute(...)` at ~540 and ~650) to asyncpg: `async with self.bot.pool.acquire() as conn: row = await conn.fetchrow("... $1 ...", arg)` and read `row["col"]` (asyncpg Record is dict-like); replace any `?` with `$N` and any boolean literal `1`/`0` compared against `was_skipped`/`was_auto_queued` with Python `True`/`False` (Pitfall 6). In `_log_track` (cogs/music.py), REPLACE the three separate awaits (`log_song` + `update_artist_count` + `update_user_profile`) with a single `from database import log_track_batch` + `await log_track_batch(self.bot.pool, guild_id=str(interaction.guild.id), user_id=str(interaction.user.id), username=interaction.user.display_name, title=track.title, artist=track.artist, url=track.url, duration=track.duration_seconds)` (D-06 — one transaction). Keep the subsequent `increment_daily_stat(self.bot.pool, "total_songs_played")` / `"total_commands"`, `get_repeat_song_count`, and `update_user_streak` calls but pass `self.bot.pool`. Update the `from database import ...` line to include `log_track_batch` and drop now-unused `log_song`/`update_artist_count`/`update_user_profile` if they are no longer referenced anywhere else in the file (verify with grep before removing). In cogs/ai.py, while swapping the DB accessor, leave the Gemini call path (`self.bot.gemini_service`) and its rate limiter completely unchanged — D-03 keeps ONE global 15 RPM limiter; do NOT introduce any per-guild rate-limit logic. Do NOT change personality strings, embed logic, or the parked reconnect race (~609, D-22). SECURITY: every converted query uses `$N` params — no f-string interpolation of guild/user/title (V5, T-04-04).
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['cogs/music.py','cogs/ai.py','cogs/imagine.py']]; print('syntax OK')" && python -c "src=open('cogs/music.py').read(); assert 'log_track_batch' in src; assert 'self.bot.pool' in src; assert 'self.bot.db' not in src, 'still references aiosqlite db'; print('music OK')" && python -c "ai=open('cogs/ai.py').read(); assert 'self.bot.pool' in ai and 'self.bot.db' not in ai; assert 'per_guild' not in ai.lower().replace('-','_'), 'unexpected per-guild rate logic (D-03)'; img=open('cogs/imagine.py').read(); assert 'self.bot.pool' in img and 'self.bot.db' not in img; print('ai/imagine OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "self.bot.db" cogs/music.py cogs/ai.py cogs/imagine.py` == 0 (no aiosqlite accessor remains)
    - `cogs/music.py` contains `log_track_batch` and `self.bot.pool`
    - `grep -c "pool.acquire()" cogs/music.py` >= 1 (the two raw inline queries converted)
    - `cogs/ai.py` and `cogs/imagine.py` each contain `self.bot.pool`
    - `cogs/ai.py` introduces NO per-guild rate-limit logic (D-03 confirm) — the global `self.bot.gemini_service` path is unchanged
    - `python -c "import ast; ..."` parses all three files (valid syntax)
    - The three `log_song`/`update_artist_count`/`update_user_profile` sequential awaits in `_log_track` are replaced by one `log_track_batch` call
  </acceptance_criteria>
  <done>All three cogs use `self.bot.pool`; `/play` writes are one batched transaction; raw inline queries converted to asyncpg `$N`; the global Gemini limiter is confirmed unchanged; no `self.bot.db` reference remains.</done>
</task>

<task type="auto">
  <name>Task 2: Queue-cap rejection + persist-on-mutation hooks (with voice-channel-id capture)</name>
  <files>cogs/music.py</files>
  <read_first>
    - cogs/music.py (every mutation site from the grep map: `queue.add(track)` at ~519/~790/~839; `queue.skip()` at ~317/~886; `queue.advance()` at ~387; `queue.shuffle()` at ~963; `queue.clear()` at ~932/~1156; `queue.loop_mode = ...` at ~975; the `get_queue`/`_text_channel_id` set at ~748)
    - models/queue.py (QueueFullError + cap-enforced add from 04-01)
    - services/queue_persistence.py (persist(guild, queue, voice_channel_id) + clear_persisted(guild_id) from 04-03)
    - .planning/phases/04-scale/04-PATTERNS.md §"cogs/music.py" (lines 297-331 — mutation-site grep list, QueueFullError catch shape, voice-channel-id capture)
    - .planning/phases/04-scale/04-RESEARCH.md Pattern 4 (persist on mutation), Pitfall 3 (cap caught at cog, lines 641-646)
    - .planning/phases/04-scale/04-CONTEXT.md D-04, D-18, D-19, D-20, D-21
  </read_first>
  <action>
    Add `from models.queue import QueueFullError` (alongside the existing `from models.queue import ... LoopMode`). At EVERY `queue.add(track)` call site (the single `/play` add and the playlist-loop add), wrap in `try: ... = queue.add(track) except QueueFullError: await interaction.followup.send("queue's full at {config.MAX_QUEUE_SIZE_PER_GUILD} tracks. impressive dedication, wrong bot.", ephemeral=True); return` (use `config.MAX_QUEUE_SIZE_PER_GUILD` via f-string; for the playlist loop, `break` out of the loop and inform the user rather than `return` if there is post-loop work). Define a small async helper `async def _persist_queue(self, guild, queue)` on the cog that computes `vc_id = guild.voice_client.channel.id if guild.voice_client else None` (D-20 — captured live, NOT stored on the model) and calls `await self.bot.queue_persistence.persist(guild, queue, vc_id)`, guarded with `if hasattr(self.bot, "queue_persistence")`. Call `await self._persist_queue(guild, queue)` immediately AFTER each successful mutation: after `queue.add(...)` (both sites), after `queue.skip()` (both sites), after `queue.advance()`, after `queue.shuffle()`, and after every `queue.loop_mode = LoopMode(...)` assignment. After every `queue.clear()` call (idle-leave at ~1156, `/stop` at ~932), call `await self.bot.queue_persistence.clear_persisted(guild.id)` (guarded the same way) so a cleared queue is not restored on next boot (D-19 clear semantics). Resolve `guild` from the existing `interaction.guild` (commands) or `member.guild` / vc.guild (events) already in scope at each site. Do NOT add a `voice_channel_id` field to the MusicQueue model (D-20, Anti-Pattern). Do NOT touch the parked reconnect race at ~609 (D-22). Keep all existing personality copy and embed behavior. SECURITY: persist payload is built inside the service via typed `to_dict`; this plan only passes the live `MusicQueue` + captured `vc_id` (no user-controlled keys, T-04-06).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('cogs/music.py').read()); print('syntax OK')" && python -c "src=open('cogs/music.py').read(); assert 'QueueFullError' in src; assert src.count('queue_persistence.persist') + src.count('_persist_queue') >= 6, 'expected persist hooks at >=6 mutation sites'; assert 'clear_persisted' in src; assert 'voice_client.channel.id' in src; print('hooks OK: persist='+str(src.count('_persist_queue'))+' clear='+str(src.count('clear_persisted')))"</automated>
  </verify>
  <acceptance_criteria>
    - `cogs/music.py` contains `from models.queue import QueueFullError` (or QueueFullError in an existing models.queue import) and a `try/except QueueFullError` around `queue.add`
    - `cogs/music.py` contains `voice_client.channel.id` (D-20 live capture) and does NOT add a `voice_channel_id` attribute to the MusicQueue model
    - persist hooks present at >= 6 mutation sites (add x2, skip x2, advance, shuffle, loop-change) — verified by the `_persist_queue`/`persist` count in the verify command
    - `cogs/music.py` contains `clear_persisted` called after `queue.clear()`
    - The parked reconnect race region (~609) is unmodified — no new persist hook injected there (D-22)
    - `python -c "import ast; ast.parse(open('cogs/music.py').read())"` exits 0
    - `pytest tests/test_queue.py -x` still green (model contract intact)
  </acceptance_criteria>
  <done>Over-cap adds are rejected with a personality message; every queue mutation persists state with the live voice-channel id captured; cleared queues delete their persisted row; the parked reconnect race is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord command → cog → pool | guild_id, user_id, title, url cross into asyncpg `$N` params |
| cog → queue_persistence | live MusicQueue + captured voice-channel id handed to the persistence service |
| user → queue.add | unbounded add attempts (playlist abuse) hit the cap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | SQL injection via converted inline queries | mitigate | The two raw `self.db.execute` sites converted to asyncpg `$N` params; the batched `log_track_batch` uses `$N`; no f-string interpolation of guild/user/title (V5) |
| T-04-02 | Denial of Service | unbounded queue via playlist add loop | mitigate | `QueueFullError` raised in the model is caught at BOTH the single-add and playlist-loop sites (Pitfall 3) — over-cap rejected with a personality message |
| T-04-06 | Tampering | persisted queue payload | mitigate | Cog passes only the live MusicQueue + a captured int `vc_id`; the service serializes via typed `to_dict`/`json.dumps` — no user-controlled keys |
| T-04-09 | Availability | persistence failure crashing playback | mitigate | persist/clear hooks are guarded with `hasattr(self.bot, "queue_persistence")` and the service swallows+logs its own exceptions — a DB hiccup never aborts a command |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(...)"` — all three cogs valid syntax
- `grep` gates: no `self.bot.db` in any cog; `log_track_batch`, `QueueFullError`, `clear_persisted`, `voice_client.channel.id` present in music.py; persist hooks at >= 6 sites
- `pytest tests/test_queue.py tests/test_message_buffer.py -x` — model contracts still green (per-commit unit sample)
- Manual / structural review (SCALE-04 persist): grep confirms a persist call after each mutation site (cannot fully automate without a Discord mock — structural review per VALIDATION.md)
- Confirm-only (D-03, no change): the Gemini limiter stays a single GLOBAL 15 RPM limiter — `cogs/ai.py` continues to call the one `self.bot.gemini_service` instance; this plan does NOT split the limiter per-guild and does NOT modify `services/gemini.py`. Verify by inspection that no per-guild rate-limit logic was introduced while migrating `cogs/ai.py` to the pool.
- Manual (user_setup): restart with a queued guild → queue restored; over-cap `/play` → personality rejection (boot/UAT gate)
</verification>

<success_criteria>
- All cogs use `self.bot.pool`; no `self.bot.db` remains (SCALE-02 consumer)
- `/play` writes go through `log_track_batch` in one transaction (D-06, SCALE-01)
- Over-cap adds rejected with a personality message at both add sites (D-04, SCALE-01)
- Every mutation site persists queue state with the live voice-channel id; cleared queues delete their row (D-19, D-20, SCALE-04)
- Global Gemini limiter confirmed unchanged (D-03); parked reconnect race untouched (D-22)
</success_criteria>

<output>
Create `.planning/phases/04-scale/04-04-SUMMARY.md` when done.
</output>
