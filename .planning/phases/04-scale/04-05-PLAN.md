---
phase: 04-scale
plan: 05
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - Dockerfile
  - docker-compose.yml
  - scripts/keepalive.sh
  - scripts/backup.sh
  - .env.example
  - .dockerignore
autonomous: true
nyquist_compliant: true
requirements: [SCALE-05]
user_setup:
  - service: oracle-cloud
    why: "Host = Oracle Cloud Always Free Ampere A1 ARM, pure-free (D-07); 24/7 deployment target (SCALE-05)"
    env_vars:
      - name: POSTGRES_PASSWORD
        source: "Operator-chosen strong password; set in .env (git-ignored) on the host"
      - name: HEALTHCHECK_URL
        source: "Healthchecks.io free tier — create a check, copy its ping URL (D-13)"
    dashboard_config:
      - task: "Provision an Ampere A1 ARM instance (target ~4 OCPU / 24 GB), install Docker + Docker Compose"
        location: "Oracle Cloud console — Always Free, NOT Pay-As-You-Go (D-07)"
      - task: "Create an Object Storage bucket 'dexter-backups' and configure oci-cli (API key or instance principal)"
        location: "Oracle Cloud console — Object Storage (Always Free 20 GB, D-12)"
      - task: "Create a Healthchecks.io check and a Discord/email integration; install host crontab entries for keepalive.sh (*/5) and backup.sh"
        location: "Healthchecks.io + Oracle VM host crontab (external to Docker, D-09/D-13)"
must_haves:
  truths:
    - "docker compose config validates a 2-service (postgres + bot) arm64 stack with named volumes (D-10, D-11, SCALE-05)"
    - "Dockerfile builds a python:3.11 image with ffmpeg + curl, installs requirements, runs bot.py (SCALE-05)"
    - "keepalive.sh pings HEALTHCHECK_URL (doubles as Oracle idle-nudge + dead-man switch) (D-09, D-13)"
    - "backup.sh pg_dumps the dexter DB and pushes it to Oracle Object Storage (D-12)"
    - "Postgres data, audio cache, and logs are on persistent named volumes (D-10)"
    - "No secrets are baked into the Dockerfile or committed env files (Information Disclosure)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "bot + postgres services, arm64, named volumes, healthcheck-gated startup"
      contains: "postgres:16-alpine"
    - path: "Dockerfile"
      provides: "arm64 python image with ffmpeg + curl"
      contains: "ffmpeg"
    - path: "scripts/keepalive.sh"
      provides: "Healthchecks.io ping / Oracle idle-nudge"
      contains: "hc-ping"
    - path: "scripts/backup.sh"
      provides: "pg_dump to Oracle Object Storage"
      contains: "pg_dump"
    - path: ".env.example"
      provides: "documented env template (no real secrets)"
      contains: "DATABASE_URL"
  key_links:
    - from: "docker-compose.yml bot service"
      to: "postgres service"
      via: "depends_on: condition: service_healthy"
      pattern: "service_healthy"
    - from: "docker-compose.yml bot service"
      to: "config path constants"
      via: "volume mounts /app/data/cache and /app/logs"
      pattern: "/app/data/cache"
    - from: "scripts/keepalive.sh"
      to: "Healthchecks.io"
      via: "curl hc-ping.com/<uuid>"
      pattern: "hc-ping"
---

<objective>
Resolve the OPEN hosting question (SCALE-05) and make 24/7 honest: a Docker Compose stack (bot + colocated Postgres, arm64, persistent volumes for Postgres data / audio cache / logs) that rebuilds the entire stack with one `docker compose up` on a fresh Oracle Ampere A1 VM (D-07/D-10/D-11), a unified keep-alive + dead-man cron that nudges Oracle's idle-reclaim threshold AND pings Healthchecks.io (D-09/D-13), and a `pg_dump` to Oracle Object Storage backup (D-12). Pure-free, `$0/mo` hard constraint; Hetzner is the documented fallback (D-08).

Purpose: SCALE-05 — the deployment decision is resolved here (Oracle Cloud Always Free A1 ARM, Docker Compose) and the supporting reliability scaffolding (backup + down-detection) ships with it.
Output: `Dockerfile`, `docker-compose.yml`, `scripts/keepalive.sh`, `scripts/backup.sh`, `.env.example`, `.dockerignore`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-scale/04-CONTEXT.md
@.planning/phases/04-scale/04-RESEARCH.md
@.planning/phases/04-scale/04-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created/modified by THIS plan)

- **New file** `Dockerfile`: `python:3.11-slim-bookworm` base, apt `ffmpeg` + `curl`, `pip install -r requirements.txt`, `CMD ["python", "bot.py"]`
- **New file** `docker-compose.yml`: `postgres` (postgres:16-alpine, arm64, healthcheck, `postgres_data` volume) + `bot` (build context ., arm64, `env_file: .env`, `DATABASE_URL` to the postgres service, `audio_cache`+`logs` volumes, `depends_on: service_healthy`); named volumes `postgres_data`, `audio_cache`, `logs`
- **New file** `scripts/keepalive.sh`: one `curl` to `$HEALTHCHECK_URL` (Oracle idle-nudge + Healthchecks.io dead-man, D-09/D-13)
- **New file** `scripts/backup.sh`: `pg_dump` (custom format) piped to `oci os object put` into the `dexter-backups` bucket (D-12)
- **New file** `.env.example`: documented template — `DISCORD_TOKEN`, `GEMINI_API_KEY`, `GENIUS_TOKEN`, `OWNER_ID`, `DATABASE_URL`, `POSTGRES_PASSWORD`, `HEALTHCHECK_URL` (placeholders only, no real secrets)
- **New file** `.dockerignore`: excludes `.env`, `data/`, `logs/`, `.git/`, `__pycache__/`, `.planning/`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile + .dockerignore + .env.example (arm64 image, secret hygiene)</name>
  <files>Dockerfile, .dockerignore, .env.example</files>
  <read_first>
    - requirements.txt (the asyncpg-updated deps from 04-02 — the image installs these)
    - CLAUDE.md §"Environment Variables (.env)" (DISCORD_TOKEN, GEMINI_API_KEY, GENIUS_TOKEN) and §"Project Structure" (data/cache, logs paths)
    - config.py (BASE_DIR / "data" / "cache" and BASE_DIR / "logs" path constants — volume mount targets must match `/app/data/cache` and `/app/logs`)
    - .planning/phases/04-scale/04-RESEARCH.md §"Dockerfile (arm64 production)" (lines 735-752 — exact template) and §"Security Domain" (secret-hygiene rows)
    - .planning/phases/04-scale/04-PATTERNS.md §"docker-compose.yml and Dockerfile" (lines 452-461 — conventions: CMD matches bot.py entry, volume paths match config constants)
    - .planning/phases/04-scale/04-CONTEXT.md D-07, D-10, D-15
  </read_first>
  <action>
    Create `Dockerfile` from RESEARCH.md template: `FROM python:3.11-slim-bookworm`; `RUN apt-get update && apt-get install -y --no-install-recommends ffmpeg curl && rm -rf /var/lib/apt/lists/*` (ffmpeg for audio, curl available in-container if needed; arm64-native in Debian Bookworm); `WORKDIR /app`; `COPY requirements.txt .`; `RUN pip install --no-cache-dir -r requirements.txt`; `COPY . .`; `CMD ["python", "bot.py"]`. Do NOT add any `ENV` line containing a secret value and do NOT `COPY .env` explicitly (it is excluded by .dockerignore; the compose `env_file` injects env at runtime). Create `.dockerignore` excluding `.env`, `.env.*` (but keep `.env.example` is fine to ship as docs — exclude `.env` and `.env.local` only), `data/`, `logs/`, `.git/`, `**/__pycache__/`, `*.pyc`, `.planning/`, `tests/`, `docs/` — so secrets and local DB/cache never enter the image layer (Information Disclosure). Create `.env.example` documenting every required variable with placeholder values: `DISCORD_TOKEN=`, `GEMINI_API_KEY=`, `GENIUS_TOKEN=`, `OWNER_ID=`, `DATABASE_URL=postgresql://dexter:CHANGE_ME@postgres:5432/dexter`, `POSTGRES_PASSWORD=CHANGE_ME`, `HEALTHCHECK_URL=https://hc-ping.com/your-uuid-here`, with a comment header stating "copy to .env and fill in; .env is git-ignored, never commit real secrets". SECURITY: no real token/password literal appears in any of these three files (T-04-05).
  </action>
  <verify>
    <automated>python -c "d=open('Dockerfile').read(); assert 'python:3.11' in d; assert 'ffmpeg' in d; assert 'requirements.txt' in d; assert 'bot.py' in d; import re; assert not re.search(r'ENV\\s+\\w*(TOKEN|PASSWORD|KEY)\\s*=\\s*\\S', d), 'secret baked into Dockerfile'; di=open('.dockerignore').read(); assert '.env' in di and 'data/' in di; ex=open('.env.example').read(); assert 'DATABASE_URL' in ex and 'HEALTHCHECK_URL' in ex and 'CHANGE_ME' in ex; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile` contains `python:3.11`, `ffmpeg`, `pip install`, `requirements.txt`, `CMD ["python", "bot.py"]`
    - `Dockerfile` contains NO `ENV`/`ARG` line with a literal secret value (grep for `TOKEN=`/`PASSWORD=`/`KEY=` with a non-empty value returns nothing)
    - `.dockerignore` excludes `.env`, `data/`, `logs/`, `.git/`, `__pycache__`, `.planning/`
    - `.env.example` lists `DATABASE_URL`, `POSTGRES_PASSWORD`, `HEALTHCHECK_URL`, `DISCORD_TOKEN`, `GEMINI_API_KEY`, `GENIUS_TOKEN`, `OWNER_ID` with placeholder (`CHANGE_ME` / `your-uuid-here`) values only
    - The verify command exits 0
  </acceptance_criteria>
  <done>arm64 Dockerfile builds the bot image with ffmpeg + deps; `.dockerignore` keeps secrets/local data out of the image; `.env.example` documents every var with placeholders, no real secrets anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: docker-compose.yml (bot + postgres, arm64, named volumes, healthcheck-gated)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - Dockerfile (the build target from Task 1 — compose `build.context: .` references it)
    - config.py (data/cache + logs path constants — volume targets `/app/data/cache`, `/app/logs`)
    - .planning/phases/04-scale/04-RESEARCH.md Pattern 8 (lines 420-468 — exact compose template, named volumes, depends_on service_healthy, platform arm64)
    - .planning/phases/04-scale/04-PATTERNS.md §"docker-compose.yml and Dockerfile" (lines 452-461)
    - .planning/phases/04-scale/04-CONTEXT.md D-10 (volumes for postgres data / cache / logs), D-11 (Postgres local colocated container)
  </read_first>
  <action>
    Create `docker-compose.yml` from RESEARCH.md Pattern 8. Service `postgres`: `image: postgres:16-alpine`, `platform: linux/arm64`, env `POSTGRES_DB: dexter` / `POSTGRES_USER: dexter` / `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}`, `volumes: - postgres_data:/var/lib/postgresql/data`, a `healthcheck` running `pg_isready -U dexter -d dexter` (interval 10s, timeout 5s, retries 5), `restart: unless-stopped`. Service `bot`: `build: { context: ., dockerfile: Dockerfile, platforms: [linux/arm64] }`, `env_file: .env`, `environment: DATABASE_URL: postgresql://dexter:${POSTGRES_PASSWORD}@postgres:5432/dexter` (points at the colocated postgres service — D-11; overrides the localhost default so the bot reaches the container, not the host), `depends_on: { postgres: { condition: service_healthy } }` (bot waits for a healthy DB), `volumes: - audio_cache:/app/data/cache` and `- logs:/app/logs` (match config path constants), `restart: unless-stopped`. Declare top-level `volumes: postgres_data: , audio_cache: , logs:` (D-10 persistent volumes survive container rebuild / Oracle reclaim rebuild). SECURITY: `POSTGRES_PASSWORD` is interpolated from `.env` (not a literal); the bot's `DATABASE_URL` references the same env var — no plaintext password in the committed file (T-04-05).
  </action>
  <verify>
    <automated>docker compose config >/dev/null 2>&1 && echo "compose config valid" || python -c "import sys; c=open('docker-compose.yml').read(); assert 'postgres:16-alpine' in c; assert 'service_healthy' in c; assert '/app/data/cache' in c and '/app/logs' in c; assert 'postgres_data' in c; assert 'linux/arm64' in c; assert '${POSTGRES_PASSWORD}' in c; print('compose structure OK (docker not available to run config)')"</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` defines two services (`postgres`, `bot`) and three named volumes (`postgres_data`, `audio_cache`, `logs`)
    - `docker-compose.yml` contains `postgres:16-alpine`, `linux/arm64`, `condition: service_healthy`, `pg_isready`
    - bot volume mounts are `/app/data/cache` and `/app/logs` (match `config.AUDIO_CACHE_DIR` / `config.LOG_DIR`)
    - `POSTGRES_PASSWORD` is `${POSTGRES_PASSWORD}` (interpolated, not a literal) and the bot `DATABASE_URL` uses the `postgres` service host
    - `docker compose config` validates (if Docker available); else the structural assertions in the verify command pass
  </acceptance_criteria>
  <done>One `docker compose up` brings up Postgres (healthcheck-gated) then the bot, arm64, with persistent volumes for DB/cache/logs and no plaintext secret in the file.</done>
</task>

<task type="auto">
  <name>Task 3: keepalive.sh + backup.sh (unified idle-nudge/dead-man + Object Storage backup)</name>
  <files>scripts/keepalive.sh, scripts/backup.sh</files>
  <read_first>
    - .planning/phases/04-scale/04-RESEARCH.md Pattern 9 (keepalive, lines 470-488 — curl to hc-ping, crontab cadence) and Pattern 10 (backup, lines 489-511 — pg_dump | oci os object put) and §"Decision R-04" (curl is sufficient for the network idle criterion)
    - .planning/phases/04-scale/04-PATTERNS.md §"scripts/keepalive.sh and scripts/backup.sh" (lines 463-468 — scripts live in scripts/, executable, env from shell/cron not .env)
    - .planning/phases/04-scale/04-CONTEXT.md D-09 (external cron, independent of bot process), D-12 (pg_dump to Object Storage), D-13 (Healthchecks.io dead-man)
  </read_first>
  <action>
    Create `scripts/keepalive.sh`: a bash script (`#!/bin/bash`, `set -euo pipefail` optional but the curl must not abort the cron on transient failure — use `curl -fsS --max-time 10 "$HEALTHCHECK_URL" > /dev/null 2>&1 || true`). Add a header comment: this is cron-driven and EXTERNAL to the bot process (D-09); the outbound curl both keeps Oracle's network-idle criterion above 20% AND pings the Healthchecks.io dead-man switch (D-13); intended crontab `*/5 * * * * /opt/dexter/scripts/keepalive.sh`. Read `HEALTHCHECK_URL` from the environment (set in crontab or a sourced env file — NOT from the Docker `.env`, since cron runs on the host). Create `scripts/backup.sh`: `#!/bin/bash`, header noting it requires oci-cli configured (API key or instance principal) and a `dexter-backups` bucket (D-12, Always Free 20 GB). Body: `TIMESTAMP=$(date +%Y%m%d_%H%M%S)`, `BUCKET="dexter-backups"`, then `pg_dump --host=localhost --username=dexter --no-password --format=custom dexter | oci os object put --bucket-name "$BUCKET" --name "dexter_${TIMESTAMP}.dump" --file - --force`. Note in a comment that `pg_dump` connects to the colocated Postgres (localhost from the host, or the container — document the host-run assumption); the Postgres password comes from `~/.pgpass` or `PGPASSWORD` in the cron environment, NEVER hardcoded in the script (T-04-05). Both scripts must be self-contained and reference no Python codebase. After creating, note they need `chmod +x` on the host (document in the script header; the executor cannot reliably chmod on Windows but states it for the operator).
  </action>
  <verify>
    <automated>python -c "k=open('scripts/keepalive.sh').read(); assert k.startswith('#!/bin/bash') or k.startswith('#!/usr/bin/env bash'); assert 'hc-ping' in k or 'HEALTHCHECK_URL' in k; assert 'curl' in k; import re; assert not re.search(r'(TOKEN|PASSWORD)=\\S', k), 'secret literal in keepalive'; b=open('scripts/backup.sh').read(); assert 'pg_dump' in b; assert 'oci os object put' in b; assert 'dexter-backups' in b; assert not re.search(r'PGPASSWORD=\\S+\\b(?!.*pgpass)', b) or 'pgpass' in b.lower(); print('scripts OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/keepalive.sh` is a bash script that curls `$HEALTHCHECK_URL` and tolerates transient failure (`|| true` / non-fatal), with a header noting external-cron + dual-duty (idle-nudge + dead-man)
    - `scripts/backup.sh` runs `pg_dump ... --format=custom dexter | oci os object put --bucket-name dexter-backups ...`
    - Neither script contains a hardcoded token or DB password literal (password via `~/.pgpass` / `PGPASSWORD` env / oci config, documented in comments)
    - Both scripts reference no Python files (standalone)
    - The verify command exits 0
  </acceptance_criteria>
  <done>The keep-alive cron curls Healthchecks.io (doubling as Oracle idle-nudge + dead-man switch); the backup cron pg_dumps to Oracle Object Storage; no secrets are embedded in either script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed repo → image | source + (excluded) secrets cross into the Docker image at build |
| host cron → Healthchecks.io / Oracle | outbound pings + backups leave the VM |
| host → Object Storage | pg_dump (roast-fuel DB) crosses to OCI bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | secrets baked into image / committed env | mitigate | `.dockerignore` excludes `.env`/`data/`; no `ENV`-with-secret in Dockerfile; `docker-compose.yml` interpolates `${POSTGRES_PASSWORD}` from `.env`; `.env.example` ships placeholders only; scripts read creds from cron env / `~/.pgpass` / `~/.oci`, never literals |
| T-04-10 | Information Disclosure | OCI credentials in image | mitigate | `oci-cli` config lives in `~/.oci/` on the host (excluded from image via `.dockerignore`-equivalent host separation); API keys never in `Dockerfile` or committed `.env` |
| T-04-11 | Availability | Oracle idle reclaim / undetected downtime | mitigate | keep-alive curl nudges the network idle threshold (D-09) AND pings the Healthchecks.io dead-man switch which alerts if pings stop (D-13); `restart: unless-stopped` recovers crashed containers |
| T-04-12 | Tampering | backup integrity | accept | `pg_dump` custom format to a same-account bucket; full-account-termination risk accepted per D-07/D-12 (Hetzner + off-provider backup are documented deferred hedges) |
</threat_model>

<verification>
- `docker compose config` validates (when Docker is available) — 2 services, 3 volumes, arm64, healthcheck-gated
- Structural grep gates: `postgres:16-alpine`, `service_healthy`, `/app/data/cache`, `/app/logs`, `pg_dump`, `oci os object put`, `hc-ping`/`HEALTHCHECK_URL`
- Secret-hygiene gates: no secret literal in `Dockerfile`, `docker-compose.yml` (uses `${POSTGRES_PASSWORD}`), or scripts
- Manual (user_setup): full-stack clean-volume boot on the Oracle A1 VM (`docker compose down -v && docker compose up`) — bot connects to fresh Postgres, schema created (SCALE-05 boot gate); keep-alive + backup crons installed and verified against Healthchecks.io + Object Storage
</verification>

<success_criteria>
- `docker compose config` validates an arm64 bot + colocated Postgres stack with persistent volumes for DB/cache/logs (D-10, D-11, SCALE-05)
- Dockerfile builds the bot image (ffmpeg + asyncpg deps) and runs `bot.py` (SCALE-05)
- keep-alive cron unifies Oracle idle-nudge + Healthchecks.io dead-man switch (D-09, D-13)
- backup cron pushes a `pg_dump` to Oracle Object Storage (D-12)
- No secret literal in any committed infra file (Information Disclosure mitigation)
- Hosting decision resolved: Oracle Cloud Always Free A1 ARM, Hetzner documented fallback (D-07, D-08)
</success_criteria>

<output>
Create `.planning/phases/04-scale/04-05-SUMMARY.md` when done.
</output>
