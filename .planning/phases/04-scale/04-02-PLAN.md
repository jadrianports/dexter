---
phase: 04-scale
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database.py
  - requirements.txt
  - tests/test_database_phase4.py
  - tests/conftest.py
autonomous: true
nyquist_compliant: true
requirements: [SCALE-01, SCALE-02, SCALE-04]
user_setup:
  - service: postgresql
    why: "Integration tests and runtime require a live Postgres; SQLite is fully removed (D-15)"
    env_vars:
      - name: DATABASE_URL
        source: "Local Docker Compose Postgres or local install (postgresql://dexter:dexter@localhost:5432/dexter)"
    dashboard_config:
      - task: "Create a dexter_test database for integration tests"
        location: "Local Postgres (CREATE DATABASE dexter_test) — used by tests/conftest.py asyncpg fixture"
must_haves:
  truths:
    - "database.py uses asyncpg (not aiosqlite) everywhere; SCHEMA_SQL is Postgres DDL with no SQLite-isms (D-15, D-16, SCALE-02)"
    - "init_db(pool) creates all 7 tables including guild_queues via asyncpg (SCALE-02, SCALE-04)"
    - "log_track_batch() writes song_history + user_artist_counts + user_profiles in ONE transaction (D-06, SCALE-01)"
    - "All queries use $N positional params — no string interpolation of guild/user IDs (V5 input validation)"
    - "get_local_date / compute_streak carry over unchanged (D-17)"
  artifacts:
    - path: "database.py"
      provides: "asyncpg helpers, Postgres SCHEMA_SQL incl. guild_queues, log_track_batch"
      contains: "asyncpg.Pool"
    - path: "tests/test_database_phase4.py"
      provides: "Postgres schema + batch-transaction + persist/restore integration tests"
      contains: "class TestPostgresSchema"
    - path: "tests/conftest.py"
      provides: "asyncpg pool fixture for integration tests"
      contains: "asyncpg.create_pool"
    - path: "requirements.txt"
      provides: "asyncpg dependency; aiosqlite removed"
      contains: "asyncpg"
  key_links:
    - from: "database.py log_track_batch()"
      to: "asyncpg transaction"
      via: "async with conn.transaction()"
      pattern: "async with conn\\.transaction\\(\\)"
    - from: "database.py SCHEMA_SQL"
      to: "guild_queues table"
      via: "CREATE TABLE IF NOT EXISTS guild_queues"
      pattern: "guild_queues"
---

<objective>
Rewrite `database.py` from aiosqlite to asyncpg — the foundational Wave 1 spine (D-15 Postgres-only, start fresh per D-14). Convert every SQLite-ism (`AUTOINCREMENT`→`BIGSERIAL`, `datetime('now')`→`now()`/`TIMESTAMPTZ`, `BOOLEAN DEFAULT 0`→`false`, `date(col)=date('now')`→`col::date = CURRENT_DATE`, `?`→`$N`), delete `migrate_add_streak_columns` (streak columns go straight into the fresh CREATE TABLE), add the `guild_queues` table (SCALE-04), add the batched `log_track_batch()` transaction (D-06), swap the dependency, and cover it all with asyncpg-backed integration tests.

Purpose: SCALE-02 (Postgres migration) + SCALE-01's DB-write-contention fix (D-06 batched transaction). This is the spine every other plan's data path depends on.
Output: A fully asyncpg `database.py`; `requirements.txt` with `asyncpg` (aiosqlite removed); `tests/conftest.py` asyncpg fixture; `tests/test_database_phase4.py` integration suite.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-scale/04-CONTEXT.md
@.planning/phases/04-scale/04-RESEARCH.md
@.planning/phases/04-scale/04-PATTERNS.md
@.planning/phases/04-scale/04-VALIDATION.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created/modified by THIS plan)

- **Rewritten module** (`database.py`): all helpers re-signatured `pool: asyncpg.Pool`
- **New function** (`database.py`): `log_track_batch(pool, *, guild_id, user_id, username, title, artist, url, duration)` — the D-06 batched transaction
- **New DDL** (`database.py` `SCHEMA_SQL`): Postgres types + new `guild_queues` table (`guild_id TEXT PK`, `payload JSONB`, `updated_at TIMESTAMPTZ`)
- **Deleted** (`database.py`): `migrate_add_streak_columns` (PRAGMA-based, D-16); WAL/busy_timeout PRAGMAs; `aiosqlite` import + `row_factory`
- **Unchanged carry-over** (`database.py`): `get_local_date`, `compute_streak` (D-17)
- **Dependency** (`requirements.txt`): `asyncpg==0.31.0` added; `aiosqlite` removed
- **Test fixture** (`tests/conftest.py`): `pool` asyncpg fixture pointed at `dexter_test`
- **Test file** (`tests/test_database_phase4.py`): `TestPostgresSchema`, `TestBatchTransaction`, helper-execution tests
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite database.py from aiosqlite to asyncpg (schema + all helpers)</name>
  <files>database.py, requirements.txt</files>
  <read_first>
    - database.py (FULL current file — every helper, SCHEMA_SQL lines 65-117, init_db lines 143-152, migrate_add_streak_columns lines 120-140, get_local_date/compute_streak lines 18-58)
    - requirements.txt (current deps — line 3 `aiosqlite` to remove)
    - .planning/phases/04-scale/04-PATTERNS.md §"database.py" (lines 32-138 — exact before/after for init_db, per-call commit, ON CONFLICT, SCHEMA_SQL conversion rules, unchanged functions)
    - .planning/phases/04-scale/04-RESEARCH.md Pattern 1 (lines 208-234 pool lifecycle), Pattern 2 (lines 236-252 $N params), Pattern 3 (lines 254-298 log_track_batch), Pattern 11 (lines 513-577 full Postgres DDL), the SQLite→Postgres Conversion Table (lines 579-597), Pitfall 1 (executescript, lines 629-633), Pitfall 6 (boolean columns, lines 659-663)
    - .planning/phases/04-scale/04-CONTEXT.md D-06, D-14, D-15, D-16, D-17
    - CLAUDE.md §"Database Schema" (the 6 tables being retyped) and §"Tech Stack" (Postgres is the ONE sanctioned change)
  </read_first>
  <action>
    Replace `import aiosqlite` with `import asyncpg` and `import json`. Keep `get_local_date` and `compute_streak` byte-for-byte unchanged (D-17). Replace `SCHEMA_SQL` with the Postgres DDL from RESEARCH.md Pattern 11: `INTEGER PRIMARY KEY AUTOINCREMENT`→`BIGSERIAL PRIMARY KEY`, `TEXT DEFAULT (datetime('now'))`→`TIMESTAMPTZ DEFAULT now()`, `BOOLEAN DEFAULT 0`→`BOOLEAN DEFAULT false`; put the streak columns (`current_streak`, `longest_streak`, `last_streak_date`) directly into the fresh `user_profiles CREATE TABLE`; add the new `guild_queues` table (`guild_id TEXT PRIMARY KEY`, `payload JSONB NOT NULL`, `updated_at TIMESTAMPTZ DEFAULT now()`). DELETE `migrate_add_streak_columns` entirely. Rewrite `init_db` to take `pool: asyncpg.Pool`, drop both PRAGMAs and `executescript`/`migrate`, and run `async with pool.acquire() as conn: await conn.execute(SCHEMA_SQL)` (asyncpg accepts a multi-statement DDL string only because it has no `$N` params — Pitfall 1). Re-signature EVERY helper from `db: aiosqlite.Connection` to `pool: asyncpg.Pool`, wrap each body in `async with pool.acquire() as conn:`, convert `?`→`$1,$2,...`, and remove every `await db.commit()` (asyncpg auto-commits non-transaction statements). For SELECT helpers use `await conn.fetch(...)` / `await conn.fetchrow(...)` and return `dict(row)` (asyncpg Record is dict-like). Convert `mark_song_skipped` to `SET was_skipped = true` (Python bool, not integer 1 — Pitfall 6) and `get_repeat_song_count`/`get_images_today` date filters to `col::date = CURRENT_DATE`. ON CONFLICT upserts port verbatim except qualify the post-conflict column (`play_count = user_artist_counts.play_count + 1`, `total_songs_queued = user_profiles.total_songs_queued + 1`, `username = EXCLUDED.username`, `last_active_at = now()`). Add the new `log_track_batch(pool, *, guild_id, user_id, username, title, artist, url, duration)` per Pattern 3: one `async with pool.acquire() as conn: async with conn.transaction():` wrapping the song_history insert + (conditional) artist upsert + user_profiles upsert. In `requirements.txt`, remove the `aiosqlite` line and add `asyncpg==0.31.0`. SECURITY: every guild_id/user_id/title flows through `$N` params — NO f-string/format interpolation into any SQL body except the already-allowlisted `increment_daily_stat` field name (keep its `allowed_fields` validation set intact).
  </action>
  <verify>
    <automated>python -c "import ast,sys; src=open('database.py').read(); assert 'aiosqlite' not in src, 'aiosqlite still present'; assert 'asyncpg' in src; assert 'guild_queues' in src; assert 'log_track_batch' in src; assert 'migrate_add_streak_columns' not in src; assert \"datetime('now')\" not in src; assert 'TIMESTAMPTZ' in src; ast.parse(src); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aiosqlite" database.py` == 0 and `grep -c "aiosqlite" requirements.txt` == 0
    - `database.py` contains `asyncpg.Pool`, `async with conn.transaction()`, `log_track_batch`, `guild_queues`, `BIGSERIAL`, `TIMESTAMPTZ DEFAULT now()`, `col::date = CURRENT_DATE`
    - `grep -c "datetime('now')" database.py` == 0 and `grep -c "AUTOINCREMENT" database.py` == 0 and `grep -c "PRAGMA" database.py` == 0
    - `grep -c "migrate_add_streak_columns" database.py` == 0
    - `get_local_date` and `compute_streak` bodies are unchanged from the current file
    - `requirements.txt` contains `asyncpg==0.31.0`
    - `python -c "import ast; ast.parse(open('database.py').read())"` exits 0 (valid syntax)
  </acceptance_criteria>
  <done>`database.py` is fully asyncpg with Postgres-only DDL incl. `guild_queues` and `log_track_batch`; all SQLite-isms gone; streak helpers untouched; dependency swapped.</done>
</task>

<task type="auto">
  <name>Task 2: asyncpg test fixture + Postgres integration tests</name>
  <files>tests/conftest.py, tests/test_database_phase4.py</files>
  <read_first>
    - tests/test_database_phase3.py (lines 1-55 — exact class/fixture structure to replicate; this is the closest analog)
    - database.py (the rewritten module from Task 1 — import the new helpers + log_track_batch + SCHEMA_SQL)
    - .planning/phases/04-scale/04-PATTERNS.md §"Test Patterns" (lines 521-561 — asyncpg pool fixture + TestPostgresSchema shape)
    - .planning/phases/04-scale/04-RESEARCH.md §"Validation Architecture" → "Integration-Testable Seams" (lines 918-924 — strategy: real Postgres, init_db, yield, drop tables)
    - .planning/phases/04-scale/04-VALIDATION.md §"Per-Task Verification Map" (SCALE-01 batch tx, SCALE-02 schema/helpers rows)
  </read_first>
  <action>
    Create `tests/conftest.py` with a `@pytest_asyncio.fixture async def pool()` that calls `asyncpg.create_pool(os.getenv("TEST_DATABASE_URL", "postgresql://dexter:dexter@localhost:5432/dexter_test"))`, runs `await init_db(p)`, `yield p`, then drops all 7 tables (`DROP TABLE IF EXISTS guild_queues, song_history, user_artist_counts, image_generation_log, bot_daily_stats, user_profiles CASCADE`) and `await p.close()`. Create `tests/test_database_phase4.py` mirroring test_database_phase3.py structure with: `TestPostgresSchema` — `init_db` ran (via fixture); query `pg_tables` (schemaname='public') and assert `song_history`, `user_profiles`, `user_artist_counts`, `image_generation_log`, `bot_daily_stats`, `guild_queues` all present; assert `user_profiles` has `current_streak`/`longest_streak`/`last_streak_date` columns via `information_schema.columns`. `TestBatchTransaction` — call `log_track_batch(pool, ...)` once, then assert exactly one `song_history` row, one `user_artist_counts` row with `play_count=1`, and one `user_profiles` row with `total_songs_queued=1`; call it again with the same artist and assert `play_count=2` and `total_songs_queued=2` (upsert path). Add a `TestHelpers` smoke that runs `log_song`, `update_user_streak`, `get_history_rows`, `get_repeat_song_count`, `get_images_today` without error and asserts `get_repeat_song_count` returns the expected count after two same-title logs. Mark every test `@pytest.mark.asyncio` and accept the `pool` fixture. Note in a module docstring that these require a live Postgres `dexter_test` DB (Wave-0 / user_setup dependency).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/test_database_phase4.py').read()); ast.parse(open('tests/conftest.py').read()); print('syntax OK')" && pytest tests/test_database_phase4.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` contains `asyncpg.create_pool` and the `DROP TABLE ... CASCADE` teardown
    - `tests/test_database_phase4.py` contains `class TestPostgresSchema` and `class TestBatchTransaction`
    - `pytest tests/test_database_phase4.py --collect-only` exits 0 (tests discoverable; collection does not require a running Postgres)
    - When a live `dexter_test` Postgres is available: `pytest tests/test_database_phase4.py -x` exits 0 (gated on user_setup — collection-only is the autonomous gate)
    - The fixture reads its DSN from `os.getenv("TEST_DATABASE_URL", ...)` (no hardcoded production DSN)
  </acceptance_criteria>
  <done>asyncpg fixture + integration tests created and collectible; full run passes against a live `dexter_test` Postgres.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cog/service → database.py | guild_id, user_id, title, url, prompt (Discord-sourced) become SQL parameters |
| process → Postgres | DSN credentials cross the network to the DB container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | SQL injection via guild/user IDs in every helper | mitigate | All values bound via asyncpg `$N` positional params; zero string interpolation into SQL bodies (V5). `increment_daily_stat` field name is validated against a fixed `allowed_fields` set before interpolation (carried over) |
| T-04-05 | Information Disclosure | Postgres DSN/password in logs | mitigate | DSN comes from `config.DATABASE_URL` (env, git-ignored); `database.py` never logs the DSN; errors surface as messages, not the connection string |
| T-04-06 | Tampering | `guild_queues` JSONB payload | mitigate | `log_track_batch` and queue writes use typed Python values via `$N`; jsonb payload (written in 04-04) is `json.dumps` of a typed dict — no user-controlled keys |
| T-04-SC | Tampering | `pip install asyncpg==0.31.0` | accept | RESEARCH.md Package Legitimacy Audit: asyncpg = MagicStack, ~8yr track record, [OK] disposition, pinned version. No [ASSUMED]/[SUS] — no blocking checkpoint required |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(...)"` — `database.py`, `conftest.py`, `test_database_phase4.py` all valid syntax
- `grep` gates: no `aiosqlite`, no `datetime('now')`, no `AUTOINCREMENT`, no `PRAGMA`, no `migrate_add_streak_columns` in `database.py`
- `pytest tests/test_database_phase4.py --collect-only` — tests discoverable without a live DB (autonomous gate)
- `pytest tests/test_database.py -x` — existing pure `compute_streak`/`get_local_date` tests still green (confirms D-17 carry-over)
- Full integration run (`pytest tests/test_database_phase4.py -x`) green against a live `dexter_test` Postgres (user_setup-gated)
</verification>

<success_criteria>
- `database.py` is Postgres-only via asyncpg; SCHEMA_SQL has all 7 tables incl. `guild_queues` (SCALE-02, SCALE-04)
- `log_track_batch()` writes the 3 per-/play rows in one transaction (D-06, SCALE-01)
- No SQLite-ism remains (`datetime('now')`, `AUTOINCREMENT`, `PRAGMA`, `aiosqlite`, `migrate_add_streak_columns` all gone)
- `get_local_date`/`compute_streak` unchanged (D-17); existing streak unit tests green
- Integration tests collectible autonomously and green against a live Postgres
</success_criteria>

<output>
Create `.planning/phases/04-scale/04-02-SUMMARY.md` when done.
</output>
