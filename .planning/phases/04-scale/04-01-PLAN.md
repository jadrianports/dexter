---
phase: 04-scale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - models/queue.py
  - models/message_buffer.py
  - config.py
  - tests/test_queue.py
  - tests/test_message_buffer.py
autonomous: true
nyquist_compliant: true
requirements: [SCALE-01, SCALE-04]
must_haves:
  truths:
    - "MusicQueue.add() rejects tracks past MAX_QUEUE_SIZE_PER_GUILD by raising QueueFullError (D-04, SCALE-01)"
    - "Track.to_dict()/from_dict() round-trip is lossless for jsonb persistence (D-18, SCALE-04)"
    - "MessageBuffer evicts channels not seen within MESSAGE_BUFFER_TTL_HOURS on each add (D-05, SCALE-01)"
    - "config.py exposes MAX_QUEUE_SIZE_PER_GUILD, MESSAGE_BUFFER_TTL_HOURS, DATABASE_URL, DB_POOL_MIN, DB_POOL_MAX, HEALTHCHECK_URL"
  artifacts:
    - path: "models/queue.py"
      provides: "QueueFullError, MusicQueue.add() cap enforcement, Track.to_dict/from_dict"
      contains: "class QueueFullError"
    - path: "models/message_buffer.py"
      provides: "_last_seen tracking + _evict_stale() TTL eviction"
      contains: "_evict_stale"
    - path: "config.py"
      provides: "Phase 4 config constants"
      contains: "MAX_QUEUE_SIZE_PER_GUILD"
    - path: "tests/test_queue.py"
      provides: "TestQueueCap + TestTrackSerialization unit tests"
      contains: "class TestQueueCap"
    - path: "tests/test_message_buffer.py"
      provides: "TestTTLEviction unit test"
      contains: "class TestTTLEviction"
  key_links:
    - from: "models/queue.py MusicQueue.add()"
      to: "config.MAX_QUEUE_SIZE_PER_GUILD"
      via: "len(self.tracks) >= config.MAX_QUEUE_SIZE_PER_GUILD"
      pattern: "config\\.MAX_QUEUE_SIZE_PER_GUILD"
    - from: "models/message_buffer.py add()"
      to: "config.MESSAGE_BUFFER_TTL_HOURS"
      via: "timedelta(hours=config.MESSAGE_BUFFER_TTL_HOURS)"
      pattern: "config\\.MESSAGE_BUFFER_TTL_HOURS"
---

<objective>
Establish the pure-logic seams and config constants that the rest of Phase 4 builds on: the per-guild queue cap (D-04), Track jsonb (de)serialization (D-18), message-buffer TTL eviction (D-05), and all new `config.py` constants. None of this touches the database, so it is the independent Wave 1 spine that parallelizes with the Postgres rewrite (04-02).

Purpose: Deliver the SCALE-01 anti-bloat guards (queue cap, buffer eviction) and the SCALE-04 serialization contract (`to_dict`/`from_dict`) as fully unit-tested pure logic before any DB or Discord wiring depends on them.
Output: `QueueFullError` + cap-enforced `MusicQueue.add()`, `Track.to_dict`/`from_dict`, `MessageBuffer._evict_stale()`, six new `config.py` constants, and three new unit test classes (Wave 0 test scaffolds for this plan's seams).
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-scale/04-CONTEXT.md
@.planning/phases/04-scale/04-RESEARCH.md
@.planning/phases/04-scale/04-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created/modified by THIS plan)

- **Config constants** (`config.py`): `DATABASE_URL`, `DB_POOL_MIN`, `DB_POOL_MAX`, `MAX_QUEUE_SIZE_PER_GUILD`, `MESSAGE_BUFFER_TTL_HOURS`, `HEALTHCHECK_URL`
- **Exception** (`models/queue.py`): `QueueFullError`
- **Methods** (`models/queue.py`): `Track.to_dict()`, `Track.from_dict()` (classmethod); modified `MusicQueue.add()` with cap check
- **Methods** (`models/message_buffer.py`): `MessageBuffer._evict_stale()`; `_last_seen` / `_ttl` instance attrs; modified `add()`
- **Test classes**: `tests/test_queue.py::TestQueueCap`, `tests/test_queue.py::TestTrackSerialization`, `tests/test_message_buffer.py::TestTTLEviction`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 4 config constants</name>
  <files>config.py</files>
  <read_first>
    - config.py (current state — replicate the `os.getenv(...) or "0"` env pattern exactly; `load_dotenv()` is already called)
    - .planning/phases/04-scale/04-PATTERNS.md §"config.py" (lines 269-293 — exact constant block + convention)
    - .planning/phases/04-scale/04-RESEARCH.md §"config.py additions for Phase 4" (lines 712-731 — values)
    - .planning/phases/04-scale/04-CONTEXT.md D-04, D-05 (Claude's Discretion on exact values + pool size)
  </read_first>
  <action>
    Append a Phase 4 block to config.py following the existing module-level-constant convention. Add `DATABASE_URL = os.getenv("DATABASE_URL", "postgresql://dexter:dexter@localhost:5432/dexter")` (per D-15 Postgres-only, default points at the local compose Postgres); `DB_POOL_MIN = 2` and `DB_POOL_MAX = 10` (per asyncpg pool sizing, Claude's Discretion D-01); `MAX_QUEUE_SIZE_PER_GUILD = 500` (anti-bloat per D-04, within the 500-1000 range); `MESSAGE_BUFFER_TTL_HOURS = 24` (idle-channel eviction TTL per D-05); `HEALTHCHECK_URL = os.getenv("HEALTHCHECK_URL", "")` (Healthchecks.io dead-man ping per D-13, empty default so absence is safe). Do NOT remove or alter any existing constant. SECURITY: `DATABASE_URL` and `HEALTHCHECK_URL` read from env via `os.getenv` so secrets live in `.env` (git-ignored) and never as literals in the source (Information Disclosure mitigation).
  </action>
  <verify>
    <automated>python -c "import config; assert config.MAX_QUEUE_SIZE_PER_GUILD == 500; assert config.MESSAGE_BUFFER_TTL_HOURS == 24; assert config.DB_POOL_MIN == 2 and config.DB_POOL_MAX == 10; assert isinstance(config.DATABASE_URL, str) and config.DATABASE_URL.startswith('postgresql://'); assert hasattr(config, 'HEALTHCHECK_URL')"</automated>
  </verify>
  <acceptance_criteria>
    - `config.py` contains `MAX_QUEUE_SIZE_PER_GUILD`, `MESSAGE_BUFFER_TTL_HOURS`, `DATABASE_URL`, `DB_POOL_MIN`, `DB_POOL_MAX`, `HEALTHCHECK_URL`
    - `DATABASE_URL` and `HEALTHCHECK_URL` are read via `os.getenv(...)` (not hardcoded secret literals)
    - The verify command above exits 0
    - No existing constant in `config.py` is removed or changed
  </acceptance_criteria>
  <done>All six Phase 4 constants importable from `config` with the specified values/types; env-backed values use `os.getenv`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Queue cap (QueueFullError) + Track jsonb serialization</name>
  <files>models/queue.py, tests/test_queue.py</files>
  <read_first>
    - models/queue.py (current `Track` dataclass lines 16-27; `MusicQueue.add()` lines 48-51 — preserve return-index contract)
    - tests/test_queue.py (existing `make_track()` helper lines 7-19 and `TestMusicQueueAdd` — reuse the helper; tests are sync, no asyncio mark)
    - .planning/phases/04-scale/04-PATTERNS.md §"models/queue.py" (lines 141-205 — exact QueueFullError + to_dict/from_dict shape) and §"Test Patterns" (lines 563-581)
    - .planning/phases/04-scale/04-RESEARCH.md §"Track to_dict / from_dict" (lines 669-710) and Pitfall 3 (lines 641-646 — cap must live in the model, not the cog, so playlist loop is covered)
    - .planning/phases/04-scale/04-CONTEXT.md D-04, D-18
  </read_first>
  <behavior>
    - TestQueueCap: adding exactly `MAX_QUEUE_SIZE_PER_GUILD` tracks succeeds; the next `add()` raises `QueueFullError`
    - TestQueueCap: `add()` still returns the inserted index (len-1) for non-full adds (preserve existing contract)
    - TestTrackSerialization: `Track.from_dict(t.to_dict()) == t` for a fully-populated track (lossless round-trip)
    - TestTrackSerialization: `from_dict` tolerates a missing `artist`/`thumbnail` (uses `.get(...)` with defaults) and a missing `was_auto_queued` (defaults False)
  </behavior>
  <action>
    In models/queue.py: define `class QueueFullError(Exception)` at module level with a docstring. Add a cap guard at the top of `MusicQueue.add()` — if `len(self.tracks) >= config.MAX_QUEUE_SIZE_PER_GUILD` raise `QueueFullError(f"Queue is at capacity ({config.MAX_QUEUE_SIZE_PER_GUILD} tracks).")` BEFORE appending; otherwise append and return `len(self.tracks) - 1` as today. Import `config` at module top (the playlist path loops over `add()`, so the cap must be enforced here per Pitfall 3, not at the cog). Add `Track.to_dict()` returning a dict of all eight fields (`video_id, title, artist, url, duration_seconds, requested_by, was_auto_queued, thumbnail`) and `Track.from_dict(cls, d)` (classmethod) reconstructing a Track using `d["..."]` for required fields and `d.get(...)` with defaults for `artist`, `was_auto_queued` (False), and `thumbnail`. In tests/test_queue.py: add `TestQueueCap` and `TestTrackSerialization` reusing the existing `make_track()` helper; these are pure sync tests (no `@pytest.mark.asyncio`). Track is a `@dataclass` so `==` is value equality — assert round-trip equality directly.
  </action>
  <verify>
    <automated>pytest tests/test_queue.py::TestQueueCap tests/test_queue.py::TestTrackSerialization -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class QueueFullError" models/queue.py` ≥ 1
    - `models/queue.py` contains `config.MAX_QUEUE_SIZE_PER_GUILD` inside `add()`
    - `models/queue.py` contains `def to_dict` and `def from_dict`
    - `pytest tests/test_queue.py::TestQueueCap tests/test_queue.py::TestTrackSerialization -x` exits 0
    - `pytest tests/test_queue.py -x` (whole file, incl. pre-existing tests) exits 0 — cap default 500 does not break existing add tests
  </acceptance_criteria>
  <done>`QueueFullError` raised past the cap; `add()` index contract preserved; `Track` round-trips losslessly; both new test classes green and existing queue tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MessageBuffer TTL idle-channel eviction</name>
  <files>models/message_buffer.py, tests/test_message_buffer.py</files>
  <read_first>
    - models/message_buffer.py (current `__init__` lines 12-14 and `add()` lines 16-27 — preserve `get_history`/`get_gemini_history`/`clear` unchanged)
    - tests/test_message_buffer.py (existing test style — pure sync, no fixture)
    - .planning/phases/04-scale/04-PATTERNS.md §"models/message_buffer.py" (lines 335-385 — exact _evict_stale + _last_seen shape) and §"Test Patterns" (lines 583-598)
    - .planning/phases/04-scale/04-RESEARCH.md Pattern 7 (lines 386-418)
    - .planning/phases/04-scale/04-CONTEXT.md D-05
  </read_first>
  <behavior>
    - TestTTLEviction: a channel whose `_last_seen` is older than `MESSAGE_BUFFER_TTL_HOURS` is removed from `_buffers` on the next `add()` to a different channel
    - TestTTLEviction: a fresh channel added in the same `add()` call survives; its history length is 1
    - TestTTLEviction: a recently-seen channel (within TTL) is NOT evicted
  </behavior>
  <action>
    In models/message_buffer.py: import `timedelta` (alongside `datetime`) and `import config`. In `__init__`, add `self._last_seen: dict[int, datetime] = {}` and `self._ttl = timedelta(hours=config.MESSAGE_BUFFER_TTL_HOURS)`. Add `_evict_stale(self)` that computes `cutoff = datetime.now() - self._ttl` and pops every channel from BOTH `self._buffers` and `self._last_seen` whose `_last_seen` timestamp is `< cutoff`. In `add()`, call `self._evict_stale()` first, then set `self._last_seen[channel_id] = datetime.now()`, then proceed with the existing append logic unchanged. Leave `get_history`, `get_gemini_history`, and `clear` untouched. In tests/test_message_buffer.py: add `TestTTLEviction` (pure sync) that adds to channel A, manually back-dates `buf._last_seen[A]` past the TTL via `datetime.now() - timedelta(hours=config.MESSAGE_BUFFER_TTL_HOURS + 1)`, adds to channel B, then asserts `buf.get_history(A) == []` and `len(buf.get_history(B)) == 1`.
  </action>
  <verify>
    <automated>pytest tests/test_message_buffer.py::TestTTLEviction -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_evict_stale" models/message_buffer.py` ≥ 2 (definition + call in `add`)
    - `models/message_buffer.py` contains `config.MESSAGE_BUFFER_TTL_HOURS`
    - `pytest tests/test_message_buffer.py::TestTTLEviction -x` exits 0
    - `pytest tests/test_message_buffer.py -x` (whole file) exits 0 — eviction does not break existing add/get tests
  </acceptance_criteria>
  <done>Stale channels evicted on `add()`; fresh/recent channels retained; `_evict_stale` covered by a green unit test; existing buffer tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → config | `.env` secrets (`DATABASE_URL`, `HEALTHCHECK_URL`) cross into the process via `os.getenv` |
| Discord message → MessageBuffer | user-authored message content is buffered in memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `config.DATABASE_URL` / `HEALTHCHECK_URL` | mitigate | Values read via `os.getenv` from `.env` (git-ignored); never written as literals in `config.py`; never logged in this plan |
| T-04-02 | Denial of Service | unbounded `MusicQueue.tracks` / `MessageBuffer._buffers` | mitigate | Queue cap (`QueueFullError` past `MAX_QUEUE_SIZE_PER_GUILD`) and TTL eviction (`_evict_stale`) bound memory growth across guilds (SCALE-01) |
| T-04-03 | Tampering | buffered message content (user-controlled) | accept | Content is stored verbatim for AI context and never `eval`'d or interpolated into SQL; low-value in-memory data, evicted on TTL |
</threat_model>

<verification>
- `pytest tests/test_queue.py tests/test_message_buffer.py -x` — both files green (fast unit sample, < 30s, the per-commit sampling target)
- `python -c "import config; ..."` constant assertions pass
- No existing test in either file regresses (cap default 500 + TTL eviction are additive)
</verification>

<success_criteria>
- `QueueFullError` raised by `MusicQueue.add()` past `MAX_QUEUE_SIZE_PER_GUILD` (SCALE-01)
- `Track.to_dict()`/`from_dict()` round-trip lossless (SCALE-04 serialization contract for 04-04)
- `MessageBuffer._evict_stale()` removes channels beyond TTL on `add()` (SCALE-01)
- All six Phase 4 config constants importable with correct values
- Three new unit test classes green; no regression in existing queue/buffer tests
</success_criteria>

<output>
Create `.planning/phases/04-scale/04-01-SUMMARY.md` when done.
</output>
