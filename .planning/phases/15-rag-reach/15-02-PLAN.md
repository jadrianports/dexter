---
phase: 15-rag-reach
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cogs/ai.py
  - tests/test_ambient_recall_cadence.py
autonomous: true
requirements: [RAG-01, RAG-02]

must_haves:
  truths:
    - "/ask attempts recall on EVERY invocation — the 0.35 MEMORY_CALLBACK_CHANCE gate is gone (RAG-02)"
    - "/roast attempts recall on EVERY invocation, scoped to the TARGET user (str(target.id)), never the invoker (RAG-01)"
    - "The ambient roast surfaces (cogs/events.py, cogs/music.py) STILL gate recall behind MEMORY_CALLBACK_CHANCE — D-01 regression target, byte-identical"
    - "When recall clears nothing above the floor, build_chat_prompt renders byte-identical (memories or None) — RAG-02 guarantee preserved"
  artifacts:
    - path: "cogs/ai.py"
      provides: "/ask + /roast recall with the cadence gate removed (bodies un-indented, scoping unchanged)"
      contains: "recall("
    - path: "tests/test_ambient_recall_cadence.py"
      provides: "regression lock: ambient keeps gate, explicit always recalls, roast target-scoped"
      contains: "MEMORY_CALLBACK_CHANCE"
  key_links:
    - from: "cogs/ai.py /roast"
      to: "memory_service.recall"
      via: "recall(str(target.id), ...)"
      pattern: "recall\\(\\s*str\\(target\\.id\\)"
    - from: "cogs/events.py + cogs/music.py"
      to: "config.MEMORY_CALLBACK_CHANCE"
      via: "retained random-gate on ambient surfaces"
      pattern: "config\\.MEMORY_CALLBACK_CHANCE"
---

<objective>
Apply D-01: remove the `if random.random() < config.MEMORY_CALLBACK_CHANCE:` recall gate at EXACTLY
two call sites in `cogs/ai.py` — `/ask` (~:132) and `/roast` (~:210) — so deliberate, opted-in
commands always attempt recall. Leave the two ambient/unprompted surfaces (`cogs/events.py:128`,
`cogs/music.py:1272`) byte-for-byte untouched: they KEEP the 0.35 gate ("rarity hits harder").

Then ADD `tests/test_ambient_recall_cadence.py` — there is currently NO test locking this gate at any
of the four call sites (RESEARCH Open Question 2), so this is new coverage, not an adjustment.

Purpose: RAG-01 ("grounds its roast" reads as reliable, not a dice roll) and RAG-02 (invoker memory
informs `/ask`). The single largest execution risk is accidental over-removal from the ambient
surfaces — this plan treats the four call sites as an enumerated checklist, not a find-and-replace.
Output: edited `cogs/ai.py`, new `tests/test_ambient_recall_cadence.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-rag-reach/15-RESEARCH.md
@.planning/phases/15-rag-reach/15-PATTERNS.md
@.planning/phases/15-rag-reach/15-CONTEXT.md

# Surfaces (read before editing — four near-identical recall sites)
@cogs/ai.py             # /ask ~:128-142, /roast ~:206-220 (EDIT these two)
@cogs/events.py         # _generate_ambient_roast, gate at :128 (DO NOT TOUCH)
@cogs/music.py          # _build_roast_line, gate at :1272 (DO NOT TOUCH)
@personality/prompts.py # build_chat_prompt memories fallback (RAG-02 guarantee — unchanged)
@tests/test_roast_command.py  # fake-bot/interaction + _invoke_roast helper convention
</context>

<artifacts_this_phase_produces>
| Symbol | File | Kind | Notes |
|--------|------|------|-------|
| `AICog.ask` (edited) | `cogs/ai.py` | command coroutine | gate removed, body un-indented, recall scoping unchanged (invoker) |
| `AICog.roast` (edited) | `cogs/ai.py` | command coroutine | gate removed, recall stays `str(target.id)` (RAG-01) |
| `tests/test_ambient_recall_cadence.py` | test file | new | regression lock for the D-01 four-site invariant |

No new symbols in `cogs/events.py` / `cogs/music.py` — they are regression targets (unchanged).
`import random` is REMOVED from `cogs/ai.py` (its only uses were the two deleted gate lines).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Remove the recall cadence gate from /ask and /roast in cogs/ai.py</name>
  <files>cogs/ai.py</files>
  <read_first>
    - cogs/ai.py `/ask` (:128-145) and `/roast` (:206-222) — the two gate blocks to edit (exact current source in context).
    - .planning/phases/15-rag-reach/15-PATTERNS.md lines 20-62 — the before/after transformation and the "DO NOT touch events.py/music.py" checklist.
    - .planning/phases/15-rag-reach/15-RESEARCH.md lines 223-271 (Pattern 1) — un-indent-the-body instruction + anti-pattern warning.
  </read_first>
  <action>
    In cogs/ai.py, at BOTH `/ask` (~:132) and `/roast` (~:210): delete the `if random.random() <
    config.MEMORY_CALLBACK_CHANCE:` line and un-indent its body by one level (4 spaces). Keep
    everything else byte-identical: the `_memory_svc = getattr(self.bot, "memory_service", None)`
    guard, the `if _memory_svc is not None:` check, the `try/except Exception as _mem_err: log.debug(...)`
    non-fatal degrade, and the `memories or None` / `roast_memories or None` argument into
    `build_chat_prompt`. Do NOT change the recall arguments — `/ask` stays `str(interaction.user.id)`
    (invoker), `/roast` stays `str(target.id)` (RAG-01 target-scoping, already correct).

    Update the now-stale explanatory comments above each block (currently "Cadence gate (D-04) keeps
    callbacks rare..." at ~:128-130 and ~:206-208) to reflect D-01: explicit commands always attempt
    recall; the MEMORY_SIMILARITY_FLOOR (0.70) is the real "when relevant" gate and memories remain
    candidate ammo the model may NOOP. Cite D-01 in the comment.

    `import random` becomes unused in cogs/ai.py after this edit (its ONLY uses were the two deleted
    gate lines — confirm with a grep for `random.` before removing). Remove the `import random` line.
    Do NOT remove `import config` (still used elsewhere in the file).

    DO NOT edit cogs/events.py or cogs/music.py in this task or plan — they are regression targets.
  </action>
  <verify>
    <automated>bash -c 'set -e; test $(grep -rn "config.MEMORY_CALLBACK_CHANCE" cogs/ | wc -l) -eq 2; grep -q "cogs/events.py" <(grep -rl "config.MEMORY_CALLBACK_CHANCE" cogs/); grep -q "cogs/music.py" <(grep -rl "config.MEMORY_CALLBACK_CHANCE" cogs/); ! grep -q "MEMORY_CALLBACK_CHANCE" cogs/ai.py; ! grep -qn "^import random" cogs/ai.py; python -c "import cogs.ai" ; echo GATE_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "config.MEMORY_CALLBACK_CHANCE" cogs/` returns EXACTLY 2 matches — `cogs/events.py` and `cogs/music.py` only (never 0, never 4).
    - `grep -n "MEMORY_CALLBACK_CHANCE" cogs/ai.py` returns nothing (gate and any stale reference gone).
    - `grep -n "^import random" cogs/ai.py` returns nothing (unused import removed) AND `python -c "import cogs.ai"` exits 0.
    - `inspect.getsource(cogs.ai.AICog.roast.callback)` still contains `recall(` with `str(target.id)` (target-scoping preserved) and no longer contains `random.random(`.
    - `cogs/events.py` and `cogs/music.py` are unmodified (git diff touches only `cogs/ai.py`).
  </acceptance_criteria>
  <done>Both explicit-command gates are removed and bodies un-indented, recall scoping is unchanged, `import random` is gone, and exactly two `config.MEMORY_CALLBACK_CHANCE` references remain (both ambient).</done>
</task>

<task type="auto">
  <name>Task 2: Add tests/test_ambient_recall_cadence.py regression lock</name>
  <files>tests/test_ambient_recall_cadence.py</files>
  <read_first>
    - tests/test_roast_command.py (:1-90) — `_make_bot`/`_make_interaction`/`_make_target`/`_invoke_roast` helpers and the `.callback` invocation convention (add `bot.memory_service = AsyncMock()`).
    - cogs/ai.py (post-edit) — AICog.ask / AICog.roast bodies; cogs/events.py `_generate_ambient_roast`; cogs/music.py `_build_roast_line`.
    - .planning/phases/15-rag-reach/15-RESEARCH.md lines 657-681 (Open Question 2 — no pre-existing lock) + 715-742 (recommended shape).
  </read_first>
  <action>
    Create tests/test_ambient_recall_cadence.py. Cover the D-01 four-site invariant with deterministic
    (non-statistical) assertions:

    (A) Source-inspection lock (the primary, non-flaky guarantee):
    - Assert `MEMORY_CALLBACK_CHANCE` IS present in `inspect.getsource(cogs.events.EventsCog._generate_ambient_roast)`
      and in `inspect.getsource(cogs.music.MusicCog._build_roast_line)` — ambient surfaces KEEP the gate.
    - Assert `MEMORY_CALLBACK_CHANCE` is NOT present in `inspect.getsource(cogs.ai.AICog.ask.callback)`
      nor `inspect.getsource(cogs.ai.AICog.roast.callback)` — explicit surfaces lost the gate.
      (Use `.callback` for the app_commands.Command-wrapped methods; use the bare method for the
      ambient helpers, which are plain methods. Confirm the exact qualified names when writing.)

    (B) Behavioral lock for the explicit surfaces (recall always fires):
    - Build a fake bot per test_roast_command.py with `bot.memory_service = AsyncMock()`
      (`recall` returns `[]`). Invoke `/roast` via `cog.roast.callback(cog, interaction, target)`
      with target != invoker and a mocked `bot.gemini_service.chat` (AsyncMock) + patched
      `cogs.ai.build_chat_prompt`; assert `bot.memory_service.recall.assert_awaited_once()` and that
      its first positional arg == `str(target.id)` (RAG-01 target-scoping, NOT invoker id).
    - Invoke `/ask` similarly (mock `message_buffer`, patch `get_user_summary`/`get_mood`/
      `get_seasonal_context`/`increment_daily_stat` and `build_chat_prompt` as needed per the
      test_roast_command.py mock style); assert `recall.assert_awaited_once()` with first arg ==
      `str(interaction.user.id)`.
    - Do NOT `patch("cogs.ai.random.random", ...)` — `import random` no longer exists in cogs/ai.py
      after Task 1; there is no gate to defeat. The behavioral assertion is simply "recall fires".

    Follow the existing `unittest.mock` (AsyncMock/MagicMock/patch) + `@pytest.mark.asyncio` convention.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ambient_recall_cadence.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_ambient_recall_cadence.py -x` exits 0 (no live DB / no Discord connection needed).
    - A source-inspection test asserts BOTH ambient functions contain `MEMORY_CALLBACK_CHANCE` and BOTH explicit `AICog.ask`/`AICog.roast` callbacks do NOT — this test FAILS if a future edit removes an ambient gate or re-adds an explicit one.
    - A behavioral test asserts `/roast` calls `recall` exactly once with first arg `str(target.id)` (target, not invoker).
    - A behavioral test asserts `/ask` calls `recall` exactly once with first arg `str(interaction.user.id)`.
    - No test references `cogs.ai.random` (would raise AttributeError after Task 1).
  </acceptance_criteria>
  <done>The new test file deterministically locks the D-01 invariant: ambient surfaces retain the gate, explicit surfaces always recall, and `/roast` recall stays target-scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord command invoker → recall scope | `/ask` recalls the invoker; `/roast` recalls the TARGET — a scoping error would disclose one user's memory under another's identity |
| Ambient trigger cadence → user perception | over-removing the gate on ambient surfaces makes memory feel "watching," not occasional (trust boundary between rare-callback and creepy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Information Disclosure | `/roast` recall scope | mitigate | Recall argument stays `str(target.id)` (RAG-01); behavioral test asserts the first arg is the target id, not invoker id — a regression that swapped it would fail the test. |
| T-15-06 | Tampering (behavioral regression) | accidental over-removal of ambient gate | mitigate | Four-site enumeration treated as a checklist; grep-gate asserts exactly 2 remaining `config.MEMORY_CALLBACK_CHANCE` refs; source-inspection test locks ambient retention. |
| T-15-07 | Information Disclosure | `build_chat_prompt` fallback breakage | accept/verify | RAG-02 byte-identical guarantee lives in `personality/prompts.py` (unchanged) and is already locked by `tests/test_prompts.py -k memory_block`; D-01 only changes how often `memories` is non-empty, never the prompt-builder. |
| T-15-SC | Tampering (supply chain) | pip installs | accept | Zero new pip dependencies this phase; no install step exists. |
</threat_model>

<verification>
- `grep -rn "config.MEMORY_CALLBACK_CHANCE" cogs/` == exactly 2 (events.py, music.py).
- `python -c "import cogs.ai"` clean; `import random` no longer in cogs/ai.py.
- `pytest tests/test_ambient_recall_cadence.py -x` green.
- Regression: `pytest tests/test_prompts.py -k memory_block -x` still green (RAG-02 byte-identical lock unchanged); `pytest tests/test_roast_command.py -x` still green.
- Full suite: `pytest tests/ -x` green.
</verification>

<success_criteria>
- `/ask` and `/roast` attempt recall unconditionally; scoping unchanged (invoker / target respectively).
- Ambient surfaces (events.py, music.py) retain the 0.35 gate byte-for-byte.
- RAG-02 byte-identical prompt guarantee preserved (floor returns nothing → identical prompt).
- New regression test deterministically locks all four call-site behaviors.
</success_criteria>

<output>
Create `.planning/phases/15-rag-reach/15-02-SUMMARY.md` when done.
</output>
