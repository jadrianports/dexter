---
phase: 15-rag-reach
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database.py
  - tests/test_database_phase15.py
autonomous: true
requirements: [RAG-03, RAG-04]

must_haves:
  truths:
    - "delete_all_user_memories removes ALL of a user's memory rows (and their in-row embeddings) so a later recall returns nothing (RAG-04, Success Criterion 4)"
    - "list_user_memories returns a user's full stored fact set (up to MEMORY_MAX_PER_USER), display-ordered, scoped to that one user (RAG-03 substrate)"
    - "A live-DB test proves remember -> forget -> recall == [] against a real pgvector column and its ANN search path"
    - "delete_all_user_memories takes exactly one identity argument (user_id) — no second id param can ever target another user"
  artifacts:
    - path: "database.py"
      provides: "list_user_memories(pool, *, user_id, limit) + delete_all_user_memories(pool, user_id)"
      contains: "async def list_user_memories"
    - path: "database.py"
      provides: "scoped nuke-all delete helper"
      contains: "async def delete_all_user_memories"
    - path: "tests/test_database_phase15.py"
      provides: "static signature/scoping checks + live-DB Success Criterion 4 proof"
      contains: "test_remember_forget_recall_empty"
  key_links:
    - from: "database.delete_all_user_memories"
      to: "user_memories"
      via: "DELETE ... WHERE user_id = $1"
      pattern: "DELETE FROM user_memories WHERE user_id = \\$1"
    - from: "tests/test_database_phase15.py"
      to: "database.delete_all_user_memories / database.search_memories"
      via: "remember -> forget -> recall round-trip"
      pattern: "delete_all_user_memories"
---

<objective>
Add the two new `database.py` helpers that back the `/memory` view (RAG-03) and `/memory forget`
(RAG-04) surfaces, and lock them with `tests/test_database_phase15.py` — including the load-bearing
live-DB `remember -> forget -> recall == []` integration proof (Success Criterion 4).

Both helpers are plain scoped SQL (no ANN, no embedding call, no Gemini call). `list_user_memories`
is a display-ordered SELECT of the user's full store; `delete_all_user_memories` is a single-param
nuke-all DELETE whose one-argument signature structurally forbids a future cross-user forget.

Purpose: Provide the DB substrate the new `/memory` cog (15-03) consumes, and prove — against a real
pgvector column — that forget genuinely erases rows AND embeddings. This proof is the trust escape
hatch Phase 16 hard-depends on.
Output: `database.list_user_memories`, `database.delete_all_user_memories`, `tests/test_database_phase15.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-rag-reach/15-RESEARCH.md
@.planning/phases/15-rag-reach/15-PATTERNS.md
@.planning/phases/15-rag-reach/15-CONTEXT.md

# Analog source-of-truth (read before editing)
@database.py            # Phase 11 memory helpers block, ~:899-1200
@tests/test_database_phase11.py
@tests/conftest.py
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (later plans/verify depend on these exact names):

| Symbol | File | Kind | Notes |
|--------|------|------|-------|
| `list_user_memories(pool, *, user_id, limit)` | `database.py` | async DB helper | plain SELECT, display-ordered, `WHERE user_id = $1`, `LIMIT $2` |
| `delete_all_user_memories(pool, user_id)` | `database.py` | async DB helper | single-param nuke-all DELETE, returns rows-deleted `int` |
| `tests/test_database_phase15.py` | test file | new | static-inspection class + live-DB integration class |
| `test_remember_forget_recall_empty` | test fn | new | Success Criterion 4 proof (rows AND embeddings gone) |

Reused as-is (do NOT re-create): `count_user_memories` (:1057), `insert_memory` (:958),
`search_memories` (:899), `evict_lowest_salience` (:1115, the scoped-DELETE template).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add list_user_memories + delete_all_user_memories to database.py</name>
  <files>database.py</files>
  <read_first>
    - database.py `evict_lowest_salience` (:1115-1142) — the T-11-04c scoped-DELETE template to clone (bound `$N`, `WHERE user_id = $1`, no interpolation).
    - database.py `get_user_memories_for_eviction` (:1080-1112) — docstring/shape convention. WARNING: do NOT reuse its `ORDER BY salience ASC` (worst-first, eviction ordering); the view needs best-first.
    - database.py `delete_expired_memories` (:1171-1200) — the `int(result.split()[-1])` rows-deleted parsing idiom.
    - .planning/phases/15-rag-reach/15-PATTERNS.md lines 91-158 — the exact research-provided source for both helpers.
  </read_first>
  <action>
    In database.py, near the Phase 11 memory-helper block (after `get_user_memories_for_eviction`
    / around `evict_lowest_salience`, ~:1112-1143), add TWO new async helpers exactly as specified
    in 15-PATTERNS.md (research-provided code):

    (1) `async def list_user_memories(pool: asyncpg.Pool, *, user_id: str, limit: int) -> list[asyncpg.Record]:`
    — a plain `conn.fetch` of `SELECT id, fact, kind, salience, created_at FROM user_memories WHERE
    user_id = $1 ORDER BY salience DESC, created_at DESC LIMIT $2` bound with `(user_id, limit)`.
    Docstring must state: display-ordering (best/most-recent first, NOT eviction ordering), the
    T-11-04c `WHERE user_id = $1` cross-user guard, and that the caller passes
    `config.MEMORY_MAX_PER_USER` (never `MEMORY_INJECT_CAP`) so the view can never truncate below
    what forget erases (Pitfall 2).

    (2) `async def delete_all_user_memories(pool: asyncpg.Pool, user_id: str) -> int:` — a
    `conn.execute("DELETE FROM user_memories WHERE user_id = $1", user_id)` returning
    `int(result.split()[-1])`. Signature has EXACTLY ONE identity parameter (`user_id`) after `pool`
    — no second id, ever (V4 access-control: structurally prevents a future accidental cross-user
    forget per D-03 / T-11-04c). Docstring must state that the single DELETE removes the row AND its
    in-row `embedding` (pgvector stores the vector in-row — no separate index cleanup), so "rows AND
    embeddings gone" is inherent.

    Use bound `$N` positional params only in BOTH helpers — never f-strings/`.format`/string concat
    in the SQL. Do NOT add a `deleted_at`/tombstone column or any soft-delete path (Pitfall 3).
  </action>
  <verify>
    <automated>python -c "import inspect, database; s=inspect.getsource(database.list_user_memories); assert 'WHERE user_id = \$1' in s and 'ORDER BY salience DESC' in s and 'LIMIT \$2' in s; d=inspect.getsource(database.delete_all_user_memories); assert 'DELETE FROM user_memories WHERE user_id = \$1' in d; import inspect as i; ps=list(i.signature(database.delete_all_user_memories).parameters); assert ps==['pool','user_id'], ps; print('helpers ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `database.list_user_memories` and `database.delete_all_user_memories` both exist and import cleanly (`python -c "import database"` exits 0).
    - `inspect.signature(database.delete_all_user_memories)` parameters == exactly `['pool', 'user_id']` (no second identity param).
    - `inspect.getsource(database.list_user_memories)` contains `WHERE user_id = $1`, `ORDER BY salience DESC`, `LIMIT $2`; contains no f-string/`.format`/string-concatenated SQL.
    - `inspect.getsource(database.delete_all_user_memories)` contains `DELETE FROM user_memories WHERE user_id = $1`; contains no `deleted_at`, no soft-delete flag.
    - `grep -n "deleted_at" database.py` returns nothing new (no tombstone column added).
  </acceptance_criteria>
  <done>Both helpers exist, are `user_id`-scoped with bound `$N` params, `delete_all_user_memories` has a single-identity signature, and neither uses string-interpolated SQL.</done>
</task>

<task type="auto">
  <name>Task 2: Static source-inspection tests in tests/test_database_phase15.py</name>
  <files>tests/test_database_phase15.py</files>
  <read_first>
    - tests/test_database_phase11.py (:1-133) — the `TestWriteHelpersExist` static class convention (`inspect.getsource` + substring asserts, `_SKIP_LIVE`/`_skip_reason` module guard).
    - .planning/phases/15-rag-reach/15-PATTERNS.md lines 428-434 — static source-inspection test convention for this phase.
    - database.py — the two helpers added in Task 1 (for the exact assertions to lock).
  </read_first>
  <action>
    Create tests/test_database_phase15.py. Reproduce the `_LOCAL_DEFAULT` / `_TEST_DSN` /
    `_SKIP_LIVE` / `_skip_reason` module-level skip guard exactly as test_database_phase11.py does
    (so the live class auto-skips when `TEST_DATABASE_URL` is unset). Add a static class (no live DB,
    always runs) `class TestPhase15HelpersExist:` asserting, via `inspect`:
    - `hasattr(database, "list_user_memories")` and `hasattr(database, "delete_all_user_memories")`.
    - `inspect.getsource(database.list_user_memories)` contains `"user_id"` and `"$1"` and
      `"ORDER BY salience DESC"` (display ordering, not eviction ordering).
    - `inspect.getsource(database.delete_all_user_memories)` contains `"user_id = $1"` and
      `"DELETE FROM user_memories"` and NOT `"deleted_at"` (hard delete, no tombstone).
    - `list(inspect.signature(database.delete_all_user_memories).parameters) == ["pool", "user_id"]`
      — the structural "never accepts a target" guard (V4 / T-11-04c). Assert with a message tying it
      to "no second identity parameter".
    This half of the file is the autonomous (no-DB) gate; keep it importable with `--collect-only`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_database_phase15.py -k "TestPhase15HelpersExist or exist or signature or scoped" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_database_phase15.py::TestPhase15HelpersExist -x` exits 0 (runs with no live DB).
    - A test asserts `inspect.signature(delete_all_user_memories).parameters == ['pool','user_id']` and fails if a third parameter is added.
    - A test asserts `delete_all_user_memories` source contains no `deleted_at`.
    - `pytest tests/test_database_phase15.py --collect-only` exits 0 (autonomous collection gate).
  </acceptance_criteria>
  <done>The static class passes with no live DB and structurally locks: both helpers exist, are `user_id`-scoped, and `delete_all_user_memories` cannot gain a second identity param without breaking a test.</done>
</task>

<task type="auto">
  <name>Task 3: Live-DB remember -> forget -> recall == [] proof (Success Criterion 4)</name>
  <files>tests/test_database_phase15.py</files>
  <read_first>
    - tests/test_database_phase11.py `test_insert_and_search_memories` (:179-214) — the EXACT live-DB round-trip shape to mirror (same `pool` fixture, `@pytest.mark.skipif(_SKIP_LIVE,...)` + `@pytest.mark.asyncio`, plain-list `embedding=[x]*config.EMBED_DIM`, `insert_memory`/`search_memories` call signatures).
    - tests/conftest.py — the shared `pool` fixture (creates schema via `init_db`, drops tables on teardown).
    - .planning/phases/15-rag-reach/15-RESEARCH.md lines 755-782 — the research-provided target test body.
  </read_first>
  <action>
    Append a live-DB test to tests/test_database_phase15.py, guarded with
    `@pytest.mark.skipif(_SKIP_LIVE, reason=_skip_reason)` + `@pytest.mark.asyncio`, taking the shared
    `pool` fixture, named `test_remember_forget_recall_empty`:
    1. `insert_memory(pool, user_id="test-phase15-forget", guild_id=None, kind="daily_batch",
       fact="user only listens to sad indie at 2am", embedding=[0.3]*config.EMBED_DIM, salience=0.3,
       expires_at=now+90d)` — mirror test_insert_and_search_memories's arg shapes exactly.
    2. Sanity: `search_memories(pool, user_id=..., query_embedding=embedding, k=5)` returns len==1
       BEFORE forget (proves the row is really there).
    3. `deleted = await database.delete_all_user_memories(pool, user_id)`; assert `deleted == 1`.
    4. `after = await search_memories(pool, user_id=..., query_embedding=embedding, k=5)`;
       assert `after == []` — the row AND its embedding are gone, re-queried through the REAL ANN
       search path (not a row-count assertion alone). This is the load-bearing RAG-04 proof.
    Use the same unique `user_id` so the test is self-cleaning even without the fixture drop.
  </action>
  <verify>
    <automated>python -m pytest tests/test_database_phase15.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - With `TEST_DATABASE_URL` unset: `pytest tests/test_database_phase15.py -q` runs, the live test reports SKIPPED (not error), static tests PASS.
    - With a pgvector-enabled `TEST_DATABASE_URL` set: `test_remember_forget_recall_empty` PASSES — `search_memories` returns 1 row before forget and `[]` after (proving rows AND embeddings verifiably gone).
    - The assertion is on `search_memories(...) == []` (ANN re-query), not merely on the DELETE row count.
  </acceptance_criteria>
  <done>The live-DB test proves `remember -> forget -> recall == []` against a real pgvector column via the real search path; it skips cleanly when no live DB is configured. (Phase gate: MUST be run at least once against a real pgvector Postgres before phase close — it is RAG-04's verification.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord interaction identity → DB helper | `user_id` (always `str(interaction.user.id)`, set by 15-03) crosses into a scoped SQL statement; no free-text/id args reach these helpers |
| Application → Postgres/pgvector | SQL text + bound params cross to Neon; embedding column lives in-row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `delete_all_user_memories` SQL | mitigate | Single bound `$1` param, `DELETE FROM user_memories WHERE user_id = $1`; no string interpolation. Locked by static test asserting the exact SQL substring. |
| T-15-02 | Information Disclosure / Tampering | cross-user forget/list via a second id param | mitigate | `delete_all_user_memories` signature is `(pool, user_id)` only — no second identity param; static `inspect.signature` test fails if one is added (structural enforcement of D-03 / T-11-04c). `list_user_memories` is `user_id = $1`-scoped. |
| T-15-03 | Repudiation / integrity | soft-delete leaving embeddings recallable | mitigate | Real `DELETE` (no `deleted_at`/tombstone); Success Criterion 4 live test re-queries via `search_memories` and asserts `[]` — proves the embedding is physically gone, not just filtered. |
| T-15-04 | Information Disclosure | view truncated below what forget erases | mitigate | `list_user_memories` docstring mandates `config.MEMORY_MAX_PER_USER` at the call site (Pitfall 2); enforced by the 15-03 caller + comment. |
| T-15-SC | Tampering (supply chain) | pip installs | accept | Zero new pip dependencies this phase (verified vs requirements.txt + milestone "zero new dependency" constraint); no install step exists, no `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `python -c "import database"` imports cleanly with both new helpers present.
- `pytest tests/test_database_phase15.py -x` green (static classes pass; live test skips without `TEST_DATABASE_URL`, passes with a pgvector DB).
- `grep -rn "deleted_at" database.py` shows no tombstone column was introduced.
- Full suite regression: `pytest tests/ -x` green (no existing memory-helper tests broken).
</verification>

<success_criteria>
- `list_user_memories` (display-ordered, `user_id`-scoped, `LIMIT $2`) and `delete_all_user_memories` (single-param nuke-all, rows-deleted int) exist in database.py with bound `$N` params only.
- `delete_all_user_memories` cannot gain a second identity parameter without failing a test.
- The live-DB Success Criterion 4 proof (`remember -> forget -> recall == []`) exists and passes against a real pgvector Postgres; skips cleanly otherwise.
</success_criteria>

<output>
Create `.planning/phases/15-rag-reach/15-01-SUMMARY.md` when done.
</output>
