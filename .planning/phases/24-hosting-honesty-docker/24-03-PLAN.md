---
phase: 24-hosting-honesty-docker
plan: 03
type: execute
wave: 2
depends_on: [24-01, 24-02]
files_modified:
  - tests/test_hosting_drift_guard.py
  - .planning/phases/24-hosting-honesty-docker/24-HOST-UAT.md
autonomous: true
requirements: [HOST-01, HOST-02, HOST-03, HOST-04]
must_haves:
  truths:
    - "tests/test_hosting_drift_guard.py exists and passes on the post-scrub repo: the zero-tolerance Koyeb/Oracle scan returns zero offenders, and every Render hit is allowlisted (D-12)"
    - "The guard is NON-VACUOUS: a positive control injects a synthetic 'Koyeb'/'Oracle' string through the real scan seam and asserts it IS caught (drift-guard false-negative mitigation)"
    - "The guard excludes the D-03/D-10 sealed prefixes (.planning/, milestones/, docs/superpowers/) AND its own file, with a guard-of-the-guard test proving sealed files are tracked-but-excluded (not vacuously absent)"
    - "The guard asserts docs/DEPLOY-KOYEB.md is absent and docs/DEPLOY-DOCKER.md is present (HOST-02 file-swap backstop)"
    - "24-HOST-UAT.md documents the human-run HOST-03 boot verification (docker compose up -d --build, clean startup log, /health on :8000 responds, no new silent failures in dexter.log) and the HOST-04 owner Render-dashboard-deletion step (D-08 / D-09)"
    - "The full pytest suite is green post-scrub — zero runtime-code-path regression"
  artifacts:
    - path: "tests/test_hosting_drift_guard.py"
      provides: "Permanent CI drift guard for hosting-term reintroduction (D-12)"
      min_lines: 90
      contains: "def test_no_koyeb_or_oracle_references"
    - path: ".planning/phases/24-hosting-honesty-docker/24-HOST-UAT.md"
      provides: "Parked human-run boot verification (HOST-03) + Render-dashboard deletion (HOST-04)"
      contains: "docker compose up"
  key_links:
    - from: "tests/test_hosting_drift_guard.py"
      to: "git ls-files (tracked files minus sealed prefixes and self)"
      via: "_tracked_non_archive_files scanning for Koyeb/Oracle/Render"
      pattern: "git.*ls-files"
    - from: "24-HOST-UAT.md"
      to: "docker compose + /health"
      via: "human boot checklist against Neon"
      pattern: "/health"
---

<objective>
Add the permanent regression backstop and the parked human-run verification: create
`tests/test_hosting_drift_guard.py` (D-12) — a repo-introspection drift guard mirroring
`tests/test_invite_drift_guard.py` that fails CI if any dead `Koyeb`/`Oracle` reference reappears
(Render enforced as an allowlist diff) — and write `24-HOST-UAT.md`, the owner-run checklist for the
HOST-03 Docker boot and the HOST-04 Render-dashboard deletion.

Purpose: The scrub in plans 24-01/24-02 is a one-time cleanup; without a guard, a future phase can
paste old Koyeb prose back in and nothing catches it. This plan promotes the phase's own success grep
into a permanent, non-vacuous CI test (HOST-01), and parks HOST-03/HOST-04 as a documented human UAT
because both need real secrets / owner dashboard access that must not live in this session (D-08/D-09).

Output: One new test file and one parked UAT checklist. This plan runs in Wave 2 — the guard's
zero-offender assertion and the file-swap check depend on the scrub (24-01) and doc swap (24-02) being
complete; a pre-scrub guard is expected to fail.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-hosting-honesty-docker/24-CONTEXT.md
@.planning/phases/24-hosting-honesty-docker/24-RESEARCH.md
@.planning/phases/24-hosting-honesty-docker/24-PATTERNS.md
@.planning/phases/24-hosting-honesty-docker/24-VALIDATION.md
@tests/test_invite_drift_guard.py
</context>

## Artifacts this phase produces (phase-wide)

New files created across Phase 24:
- `docs/DEPLOY-DOCKER.md` — created in plan 24-02
- `tests/test_hosting_drift_guard.py` (+ `_repo_root`, `_tracked_non_archive_files`,
  `_scan_for_zero_tolerance_terms` helpers) — **created by this plan** (Task 1)
- `.planning/phases/24-hosting-honesty-docker/24-HOST-UAT.md` — **created by this plan** (Task 2)

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_hosting_drift_guard.py — non-vacuous Koyeb/Oracle/Render drift guard (D-12)</name>
  <files>tests/test_hosting_drift_guard.py</files>
  <read_first>
    - `tests/test_invite_drift_guard.py` (full, 265 lines) — the primary analog: module-docstring
      convention, `_repo_root()` (copy verbatim), `_tracked_doc_files()` (adapt), the
      positive/negative-control shape, and the `test_planning_tree_is_excluded_from_the_scan`
      guard-of-the-guard
    - `.planning/phases/24-hosting-honesty-docker/24-PATTERNS.md` § "tests/test_hosting_drift_guard.py
      — THE HIGH-VALUE MAPPING" (the 7 copyable pieces, including the EXCLUDED_PREFIXES tuple and the
      Render allowlist skeleton)
    - `.planning/phases/24-hosting-honesty-docker/24-RESEARCH.md` § "Verification Grep Command"
      (Part-1 zero-tolerance / Part-2 allowlist) and the Render allowlist table
  </read_first>
  <action>
    Create `tests/test_hosting_drift_guard.py` mirroring `tests/test_invite_drift_guard.py`'s shape
    (D-12). Structure it as stdlib-only (`re`, `subprocess`, `pathlib`) — no project imports needed.
    Include:
    - A module docstring stating the guarantee and naming HOST-01 / D-12, listing each test function
      with a one-line purpose (mirror the invite guard's convention).
    - `_repo_root()` — copy verbatim from the invite guard.
    - `EXCLUDED_PREFIXES = (".planning/", "milestones/", "docs/superpowers/")` (D-03/D-10) plus the
      guard's OWN file path `tests/test_hosting_drift_guard.py`. CRITICAL: the guard file itself
      contains the literal strings "Koyeb", "Oracle", and "render" (in its regex/allowlist/positive
      control), so it MUST exclude itself from BOTH scans or it trips on its own fixtures — mirror the
      invite guard's `_TESTS_PREFIX` self-exclusion rationale, but scope the self-exclusion to just this
      one file path so OTHER test files stay guarded.
    - `_tracked_non_archive_files(root)` — adapt `_tracked_doc_files`: run `git ls-files`, drop any
      path under an `EXCLUDED_PREFIXES` prefix or equal to the guard's own path; DROP the
      `TEXT_EXTENSIONS` filter entirely (a Koyeb ref can live in `.py`/`.yml`/`.md`).
    - `_scan_for_zero_tolerance_terms(paths)` — compile `\b(Koyeb|Oracle)\b` case-insensitive,
      return a list of (path, matched-term) offenders. This is the seam the positive control exercises.
    - `test_no_koyeb_or_oracle_references()` — assert `_scan_for_zero_tolerance_terms(_tracked_non_archive_files(root)) == []`.
    - `test_render_hits_are_all_allowlisted()` — for each tracked non-archive file, match
      `\brender\w*\b` case-insensitive per line; any (rel-posix-path, line-number) pair NOT in a
      hardcoded `RENDER_ALLOWLIST` frozenset is an offender; assert zero. DERIVE the allowlist from the
      actual post-scrub repo state at implement time — run `git grep -niw render -- . ':!.planning'
      ':!milestones' ':!docs/superpowers'` and encode every surviving legitimate English-word hit as a
      literal (file, line) pair (the "reviewable literal list IS the control" discipline). Do NOT blindly
      copy RESEARCH.md's line numbers — the scrub may have shifted lines; the bar is that the test passes
      on the real repo. Confirm bot.py's old Render hits (formerly lines 252/254) are GONE, not
      allowlisted.
    - Positive control `test_drift_guard_actually_detects_koyeb(tmp_path)` — write a one-line comment
      containing the word Koyeb to a tmp file, run it through `_scan_for_zero_tolerance_terms`, assert
      offenders non-empty.
    - Negative control — a clean tmp file yields zero offenders.
    - Guard-of-the-guard `test_sealed_archives_are_excluded()` — assert at least one tracked file under
      each of `.planning/` and `docs/superpowers/` EXISTS (so exclusion is not vacuous) but is ABSENT
      from `_tracked_non_archive_files()`'s return (mirror `test_planning_tree_is_excluded_from_the_scan`).
    - `test_koyeb_doc_removed_and_docker_doc_present()` — assert `docs/DEPLOY-KOYEB.md` absent and
      `docs/DEPLOY-DOCKER.md` present (HOST-02 backstop).
    Write the actual test module via the Write tool.
  </action>
  <verify>
    <automated>python -m pytest tests/test_hosting_drift_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_hosting_drift_guard.py -x -q` exits 0 with all tests passing on the
      real post-scrub repo (proves the scrub reached zero — the negative-control side of D-12).
    - The positive-control test `test_drift_guard_actually_detects_koyeb` genuinely fails when the
      scanner is broken — verify by a scratch check: temporarily point it at a file containing the word
      Koyeb and confirm it asserts non-empty (do NOT leave the scratch change in).
    - `ruff check tests/test_hosting_drift_guard.py` is clean.
    - `git grep -qF 'docs/superpowers/' tests/test_hosting_drift_guard.py` and
      `git grep -qF 'milestones/' tests/test_hosting_drift_guard.py` both succeed (all three sealed
      prefixes encoded — D-03/D-10).
    - The guard file excludes itself: `test_no_koyeb_or_oracle_references` passes even though the file
      contains the literal Koyeb/Oracle pattern (self-exclusion works).
  </acceptance_criteria>
  <done>The drift guard passes on the post-scrub repo, catches a synthetic Koyeb reintroduction (non-vacuous), excludes all three sealed prefixes + its own file, and backstops the DEPLOY doc swap.</done>
</task>

<task type="auto">
  <name>Task 2: Write 24-HOST-UAT.md — parked human-run boot verification (HOST-03) + Render deletion (HOST-04)</name>
  <files>.planning/phases/24-hosting-honesty-docker/24-HOST-UAT.md</files>
  <read_first>
    - `.planning/phases/24-hosting-honesty-docker/24-RESEARCH.md` § "HOST-03 UAT Shape" (the exact
      checklist items) and § "HOST-04 Confirmation" (no repo config exists — dashboard-only)
    - `.planning/phases/24-hosting-honesty-docker/24-CONTEXT.md` D-08 (park HOST-03) + D-09 (HOST-04)
    - `docs/DEPLOY-DOCKER.md` (created in plan 24-02) — the UAT should point the owner at this run guide
    - A prior parked-UAT for shape reference: `.planning/phases/17-vision-multimodal-roasting/17-HUMAN-UAT.md`
      (or any existing `*-HUMAN-UAT.md` in a sibling phase dir) — match the acknowledged-deferred marker convention
  </read_first>
  <action>
    Write `.planning/phases/24-hosting-honesty-docker/24-HOST-UAT.md` following the established
    parked-UAT convention (Phases 03–22 `*-HUMAN-UAT.md`). Include:
    - A header noting this is a human-run, acknowledged-deferred UAT (real `DISCORD_TOKEN` + Neon
      `DATABASE_URL` must not live in the planning session — D-08), status partial/deferred until run.
    - HOST-03 checklist (owner runs on their residential PC, per docs/DEPLOY-DOCKER.md):
      1. `cp .env.example .env`; fill real `DISCORD_TOKEN` / `GEMINI_API_KEY` / `GENIUS_TOKEN` / Neon
         `DATABASE_URL`.
      2. `docker compose up -d --build` — image builds cleanly (no pip/ffmpeg install failure).
      3. `docker compose logs -f bot` — clean startup: `init_db()` succeeds, `on_ready` fires, health
         server task scheduled, no repeated tracebacks.
      4. `curl http://localhost:8000/health` — expect HTTP 200 (the ok JSON shape), or the truthful
         degraded-503 shape if `HEALTH_STRICT_STATUS` is on and something is genuinely degraded — that
         is still "no NEW silent failure".
      5. Confirm no NEW silent failures in `dexter.log` (the `logs` named volume) beyond pre-Phase-24
         background noise (e.g. yt-dlp transient errors are expected) — regression check, not a
         zero-error-forever bar.
      6. Also document the repo-side verification the owner can run to confirm the scrub held:
         `pytest tests/test_hosting_drift_guard.py` (or the two-part `git grep` — Part-1 zero-tolerance
         Koyeb/Oracle, Part-2 Render allowlist).
    - HOST-04 (blocked-on-human, owner Render dashboard): state plainly there is NO Render config in the
      repo (D-09) — the auto-deploy + CI/CD failure emails come from a dashboard-side Render service. The
      owner deletes that service in the Render dashboard so the repo stops auto-deploying and the failure
      emails stop. Mark it blocked-on-human; no repo action possible.
    - A closing acknowledged-deferred marker matching the prior-phase convention.
    Do NOT include any real secret value; use placeholders only.
  </action>
  <verify>
    <automated>test -f .planning/phases/24-hosting-honesty-docker/24-HOST-UAT.md && grep -qiF 'docker compose up' .planning/phases/24-hosting-honesty-docker/24-HOST-UAT.md && grep -qiF '/health' .planning/phases/24-hosting-honesty-docker/24-HOST-UAT.md && grep -qiF 'render' .planning/phases/24-hosting-honesty-docker/24-HOST-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/24-hosting-honesty-docker/24-HOST-UAT.md` exists.
    - It contains the HOST-03 boot steps: `cp .env.example .env`, `docker compose up -d --build`,
      `curl http://localhost:8000/health`, and the "no new silent failures in dexter.log" regression note.
    - It documents the HOST-04 owner Render-dashboard deletion as blocked-on-human with no repo action (D-09).
    - It carries an acknowledged-deferred / partial status marker matching the prior-phase `*-HUMAN-UAT.md` convention.
    - No real secret value appears (placeholders only).
  </acceptance_criteria>
  <done>24-HOST-UAT.md is the parked owner checklist for the Docker boot (HOST-03) and Render-service deletion (HOST-04), matching the established parked-UAT convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drift guard → future contributors | The guard is the only automated thing preventing hosting-term reintroduction; a vacuous guard is worse than none |
| UAT doc → owner secret handling | The checklist tells the owner where to place real tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Tampering (false-negative) | `tests/test_hosting_drift_guard.py` passing vacuously | mitigate | Mandatory positive control injects a synthetic Koyeb string through the real `_scan_for_zero_tolerance_terms` seam and asserts it IS caught; negative control + guard-of-the-guard prove exclusions are not vacuous. |
| T-24-08 | Tampering | Guard self-trip silenced by an over-broad `tests/` exclusion, hiding a real future reintroduction in another test file | mitigate | Self-exclusion is scoped to the guard's OWN file path only, not all of `tests/`; other test files stay in scope. |
| T-24-09 | Information Disclosure | `24-HOST-UAT.md` embedding a real secret value | mitigate | Doc uses placeholders only and instructs the owner to fill real values into gitignored `.env`; acceptance criteria assert no real secret present. |
| T-24-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | This plan installs zero packages; pytest already present. No `T-{phase}-SC` checkpoint needed. |
</threat_model>

<verification>
- `pytest tests/test_hosting_drift_guard.py -x -q` passes on the post-scrub repo (all tests green).
- Positive control genuinely catches a synthetic Koyeb reintroduction; guard-of-the-guard proves the
  three sealed prefixes are tracked-but-excluded.
- Full `pytest` suite green (zero runtime regression, consistent with "NO runtime code paths change").
- `ruff check .` clean.
- `24-HOST-UAT.md` exists with the HOST-03 boot steps and the HOST-04 blocked-on-human deletion note.
</verification>

<success_criteria>
A permanent, non-vacuous `tests/test_hosting_drift_guard.py` (D-12) passes on the post-scrub repo and
fails CI on any future Koyeb/Oracle reintroduction (Render allowlist-enforced), excluding the three
sealed archive prefixes and its own file; `24-HOST-UAT.md` documents the parked HOST-03 Docker boot
verification and the HOST-04 owner Render-dashboard deletion (D-08/D-09); the full pytest suite is green.
</success_criteria>

<output>
Create `.planning/phases/24-hosting-honesty-docker/24-03-SUMMARY.md` when done.
</output>
