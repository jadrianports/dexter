---
phase: 24-hosting-honesty-docker
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - CLAUDE.md
  - docs/DEPLOY-KOYEB.md
  - docs/DEPLOY-DOCKER.md
autonomous: true
requirements: [HOST-01, HOST-02]
must_haves:
  truths:
    - "git grep -niE '\\b(Koyeb|Oracle)\\b' over .env.example and CLAUDE.md returns zero hits (D-01 / D-07)"
    - ".env.example reads as 'Docker on your PC + Neon' framing: the local-Postgres-vs-Neon DATABASE_URL split is intact, and the Healthchecks.io outbound dead-man note (K-09) is preserved verbatim (D-07)"
    - "CLAUDE.md's Tech Stack Hosting/Containerization bullets and the Phase-5 build-log narrative are host-honest (Docker + residential-IP framing), with all five K-## tags in the file preserved (D-01 / D-04)"
    - "CLAUDE.md's scripts/ project-structure tree no longer lists the deleted deploy.sh/backup.sh/keepalive.sh/lifecycle-policy.json/seed_restore_test.py (stale-reference cleanup from plan 24-01's deletions)"
    - "docs/DEPLOY-KOYEB.md no longer exists; docs/DEPLOY-DOCKER.md exists (D-05)"
    - "docs/DEPLOY-DOCKER.md contains the D-06 required sections: prereqs, cp .env.example .env + fill the four secrets, docker compose up -d --build, /health-on-:8000 verify + clean dexter.log, and an honest parked-24/7 / residential-IP / single-Discord-token framing"
    - "docs/DEPLOY-DOCKER.md embeds NO real secret values — only placeholders — and tells the reader .env is gitignored and must never be committed (secret-exposure mitigation)"
  artifacts:
    - path: "docs/DEPLOY-DOCKER.md"
      provides: "Lean Docker + Neon run guide (HOST-02)"
      min_lines: 35
      contains: "docker compose up"
    - path: ".env.example"
      provides: "Docker+Neon-framed env template, K-09/K-13 tags preserved"
      contains: "K-09"
    - path: "CLAUDE.md"
      provides: "Host-honest Tech Stack + build-log narrative, five K-## tags preserved"
  key_links:
    - from: "docs/DEPLOY-DOCKER.md"
      to: "docker-compose.yml + bot.py /health"
      via: "documents the real compose up flow and the :8000 health check"
      pattern: "docker compose up"
    - from: ".env.example"
      to: "Neon DATABASE_URL"
      via: "pooled-DSN guidance + config.sanitize_database_url note"
      pattern: "DATABASE_URL"
---

<objective>
Make Dexter's developer-facing docs and env template host-honest: reframe `.env.example` from
"Koyeb secrets / production" to "Docker on your PC + Neon", rewrite CLAUDE.md's hosting narrative and
update its now-stale scripts/ tree, `git rm docs/DEPLOY-KOYEB.md` (179-line Koyeb runbook), and write
a lean `docs/DEPLOY-DOCKER.md` documenting the real `docker compose up` → Neon flow.

Purpose: The env template and spec still tell readers to configure Koyeb Secrets and UptimeRobot keep-
alive for a 24/7 cloud service that has been parked for months. This closes HOST-02 and the doc/template
half of HOST-01 with an honest, accurate run guide against what actually runs today. This plan owns the
docs/template half; plan 24-01 owns the code/infra half; both are Wave 1 with zero file overlap.

Output: A reframed `.env.example`, a host-honest CLAUDE.md, `docs/DEPLOY-KOYEB.md` removed, and a new
lean `docs/DEPLOY-DOCKER.md`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-hosting-honesty-docker/24-CONTEXT.md
@.planning/phases/24-hosting-honesty-docker/24-RESEARCH.md
@.planning/phases/24-hosting-honesty-docker/24-PATTERNS.md
@.planning/phases/24-hosting-honesty-docker/24-VALIDATION.md
</context>

## Artifacts this phase produces (phase-wide)

New files created across Phase 24:
- `docs/DEPLOY-DOCKER.md` — **created by this plan** (Task 3)
- `tests/test_hosting_drift_guard.py` (+ `_repo_root`, `_tracked_non_archive_files`,
  `_scan_for_zero_tolerance_terms` helpers) — created in plan 24-03
- `.planning/phases/24-hosting-honesty-docker/24-HOST-UAT.md` — created in plan 24-03

<tasks>

<task type="auto">
  <name>Task 1: Reframe .env.example from Koyeb-Secrets framing to Docker+Neon (keep K-09/K-13 tags + the DATABASE_URL split)</name>
  <files>.env.example</files>
  <read_first>
    - `.env.example` (full file, 50 lines) — the Koyeb references at lines 7, 10, 23, 27, 30, 38, 41
    - `.planning/phases/24-hosting-honesty-docker/24-PATTERNS.md` § ".env.example (config, template
      reframe)" — has line-by-line reframe guidance
    - `.planning/phases/24-hosting-honesty-docker/24-CONTEXT.md` D-07 (what to keep vs drop)
  </read_first>
  <action>
    Reframe `.env.example` per D-07 — a targeted relabel, not a rewrite-from-scratch. Keep the file's
    section structure and every real value; change only the Koyeb-specific framing:
    - Line 7 — reword "local .env and Koyeb secrets" to "local .env and Docker" framing; KEEP the
      `— K-13.` tag.
    - Line 10 — reword the "set as Koyeb encrypted Secrets in production (K-13)" section header to a
      "Docker on your PC + Neon" framing; KEEP `(K-13)`.
    - Lines 23–27 — relabel the "KOYEB (production)" DATABASE_URL branch to "DOCKER + NEON"; KEEP the
      pooled-Neon-DSN guidance, the `config.sanitize_database_url` note ("paste the raw string"), and
      the local-Postgres break-glass branch (lines 18–21) structurally intact. Reword line 27's
      "differs between local and Koyeb environments" to "differs between local and Docker+Neon".
    - Line 30 — reword "Not needed in Koyeb (Neon manages...)" to a host-neutral "Not needed when using
      Neon (Neon manages its own credentials via DATABASE_URL)".
    - Lines 37–38 — DROP the UptimeRobot inbound keep-alive note that references Koyeb's 1-hour
      scale-to-zero and `docs/DEPLOY-KOYEB.md` entirely (no scale-to-zero on a residential Docker run,
      and the referenced doc is being deleted). Do NOT repoint it at DEPLOY-DOCKER.md.
    - Line 34 — KEEP the "Bot-side outbound dead-man ping (K-09)" Healthchecks.io note verbatim
      (host-agnostic, still valid — do NOT drop this safeguard).
    - Line 41 — reword "set as plain env vars in Koyeb (non-sensitive)" to a host-neutral "optional
      non-sensitive env vars".
    The four secret vars stay blank placeholders; no real secret value is introduced.
  </action>
  <verify>
    <automated>git grep -niE '\b(Koyeb|Oracle)\b' -- .env.example; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -niE '\b(Koyeb|Oracle)\b' -- .env.example` returns zero lines.
    - `git grep -nE 'K-09' -- .env.example` still returns the Healthchecks.io dead-man line (safeguard
      preserved), and `git grep -nE 'K-13' -- .env.example` still returns its two tag lines (D-04).
    - `git grep -nF 'sanitize_database_url' -- .env.example` still returns the raw-DSN guidance line.
    - `git grep -niE 'UptimeRobot|scale-to-zero' -- .env.example` returns zero (D-07 drop applied).
    - `git grep -nF 'DATABASE_URL' -- .env.example` still shows both the local break-glass and the
      Neon branch (the split is intact).
  </acceptance_criteria>
  <done>.env.example is Docker+Neon-framed; K-09/K-13 tags and the DATABASE_URL split preserved; UptimeRobot/scale-to-zero language gone; no real secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CLAUDE.md hosting narrative host-honest + update the stale scripts/ tree (keep five K-## tags)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` lines 20–30 (Containerization + Hosting bullets, lines 22 + 24)
    - `CLAUDE.md` lines 82–90 (the scripts/ project-structure tree, lines 85–86 list the deleted scripts)
    - `CLAUDE.md` lines 683–690 (log-viewer note, line 687) and lines 776–825 (Phase-5 build-log:
      lines 780, 819, 820, 822)
    - `.planning/phases/24-hosting-honesty-docker/24-PATTERNS.md` § "CLAUDE.md" (tone + the K-## line map)
    - `.planning/phases/24-hosting-honesty-docker/24-RESEARCH.md` § "Project Constraints (from CLAUDE.md)"
  </read_first>
  <action>
    Reword CLAUDE.md's hosting prose host-honest while preserving its authoritative-spec structure and
    every `(K-##)` tag (D-01, D-04). Change only the hosting-narrative prose — do NOT restructure
    sections or touch any of the 20 Critical Rules:
    - Line 22 (Containerization bullet) — reword "the Oracle-era `postgres:16-alpine` service was
      dropped in Phase 6" to a host-neutral "the colocated `postgres:16-alpine` service was dropped in
      Phase 6".
    - Line 24 (Hosting bullet) — reword "re-targeted Oracle A1 → Koyeb + Neon (Phase 5), then 24/7
      deploy parked" to an honest current-state framing: the 24/7 cloud deploy is parked (YouTube blocks
      datacenter IPs), Dexter runs on-demand via Docker on the user's residential-IP PC → Neon; the code
      is substrate-agnostic (Dockerfile + `DATABASE_URL`). Keep the substance (parked, residential,
      config-only host swap) — only remove the dead provider names.
    - Line 687 — reword "Docker/Koyeb log viewers" to "Docker/container log viewers"; KEEP `(K-16)`.
    - Line 780 — reword "deploy to Oracle Cloud" (Phase-1 deferred note) to "deploy to a cloud host"
      (or equivalent host-neutral phrasing) — it is a historical deferred-item note; keep it honest.
    - Lines 819–822 (Phase-5 build-log) — reword "Oracle A1 → Koyeb WEB + Neon", "the Oracle attempt is
      archived under `.planning/...`", "de-Oracle'd Dockerfile", and "docs/DEPLOY-KOYEB.md + a 22-check
      live-UAT runbook" to host-honest phrasing. The Phase-5 archive lives on disk under
      `.planning/phases/05-ship-it-live/` — do NOT rename that directory; but the word "Oracle" must not
      remain in CLAUDE.md's tracked text, so reword the inline reference (e.g. "the archived Phase-5
      deploy attempt under `.planning/`") rather than spelling out the `oracle-attempt/` path. Repoint the
      `docs/DEPLOY-KOYEB.md` mention to `docs/DEPLOY-DOCKER.md` since the Koyeb doc is being removed.
    - Lines 85–86 (scripts/ tree) — this line lists `deploy.sh, backup.sh, keepalive.sh,
      lifecycle-policy.json, seed_restore_test.py`, all deleted in plan 24-01. Update the scripts/ tree
      entry to reflect the surviving scripts (`memory_spike.py`, `render_demo_gif.py`) so the spec is not
      stale. (No hosting word here — this is deletion-driven honesty, part of the same CLAUDE.md pass.)
    Preserve the file's terse, hyphen-scarred prose style throughout.
  </action>
  <verify>
    <automated>git grep -niE '\b(Koyeb|Oracle)\b' -- CLAUDE.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -niE '\b(Koyeb|Oracle)\b' -- CLAUDE.md` returns zero lines (including no inline
      `oracle-attempt/` path spelled out in CLAUDE.md prose).
    - `git grep -noE 'K-[0-9]{2}' -- CLAUDE.md | sort -u` still yields K-04, K-07, K-16 (the five tag
      occurrences preserved — D-04).
    - `git grep -nE 'seed_restore_test|backup\.sh|keepalive\.sh|lifecycle-policy' -- CLAUDE.md` returns
      zero (the stale scripts/ tree references are gone).
    - `git grep -nF 'DEPLOY-DOCKER.md' -- CLAUDE.md` returns the repointed build-log reference.
  </acceptance_criteria>
  <done>CLAUDE.md hosting narrative is host-honest with zero Koyeb/Oracle; five K-## tags preserved; scripts/ tree matches the post-deletion reality; the Koyeb-doc reference repointed to DEPLOY-DOCKER.md.</done>
</task>

<task type="auto">
  <name>Task 3: git rm docs/DEPLOY-KOYEB.md and write the lean docs/DEPLOY-DOCKER.md (HOST-02, D-05/D-06)</name>
  <files>docs/DEPLOY-KOYEB.md, docs/DEPLOY-DOCKER.md</files>
  <read_first>
    - `docs/DEPLOY-KOYEB.md` lines 1–40 (front-matter "Status / Environment / Cost" convention + section
      structure) — read for shape ONLY, before `git rm`; do NOT copy content wholesale (D-06)
    - `docker-compose.yml` (full) — the real flow the doc documents (env_file: .env, named volumes,
      restart: unless-stopped, bot-only → Neon)
    - `bot.py` health-server section (~210–270) — confirm the `/health` route + `0.0.0.0:8000` bind so
      the verify-alive step is accurate
    - `.env.example` (as reframed in Task 1) — the four secrets the doc tells readers to fill
    - `.planning/phases/24-hosting-honesty-docker/24-RESEARCH.md` § "Replacement docs/DEPLOY-DOCKER.md
      Content Plan" (the ~40–60-line section list + explicit what-NOT-to-carry list)
    - `.planning/phases/24-hosting-honesty-docker/24-PATTERNS.md` § "docs/DEPLOY-DOCKER.md" (structure to mirror)
  </read_first>
  <action>
    `git rm docs/DEPLOY-KOYEB.md` (D-05 — do not leave the 179-line Koyeb runbook behind), then create
    `docs/DEPLOY-DOCKER.md` as a lean ~40–60-line run guide (D-06). Mirror the sibling doc's
    "Status / Environment / Cost" front-matter convention but retarget Environment to "Docker Compose on
    your PC + Neon serverless Postgres". Include these sections, cross-checked against the real
    `docker-compose.yml` / `bot.py` so the doc does not drift:
    1. Title + status line.
    2. Prereqs — Docker + Docker Compose installed (`docker --version`, `docker compose version`).
    3. Setup — `cp .env.example .env`; fill `DISCORD_TOKEN`, `GEMINI_API_KEY`, `GENIUS_TOKEN`, and
       `DATABASE_URL` (Neon pooled DSN); note that `config.sanitize_database_url()` strips
       `channel_binding`/`sslmode` at startup so the raw Neon string can be pasted as-is; then
       `docker compose up -d --build`.
    4. Verify-alive — `curl http://localhost:8000/health` expecting HTTP 200 / the ok JSON shape (note
       the degraded-503 shape is still truthful reporting, not a failure); `docker compose logs -f bot`
       and the `logs` named volume for `dexter.log` — confirm no repeated ERROR lines.
    5. Honest framing paragraph — plainly state this is an on-demand run on a residential IP (YouTube
       blocks datacenter IPs), NOT a 24/7 cloud standup; the 24/7 deploy stays parked (DEPLOY-F1).
    6. Single-Discord-token warning — carry forward the old doc's break-glass rule reworded: do NOT run
       two bot instances on the same Discord token simultaneously (gateway conflict → both disconnect in
       a loop). This rule has no Koyeb-specific substance.
    SECURITY: the doc must use placeholder values only (e.g. `DISCORD_TOKEN=` blank, a fake `ep-<id>-
    pooler...` DSN), embed NO real secret, and state that `.env` is gitignored and must never be
    committed. Explicitly DROP (per RESEARCH.md): the Neon-account walkthrough, UptimeRobot setup, Koyeb
    secrets-UI walkthrough, the HeavenCloud/Wispbyte runner-swap contingency, and the archived-scripts
    table. Do NOT paste runbook prose that reintroduces Koyeb/Oracle wording.
  </action>
  <verify>
    <automated>test ! -f docs/DEPLOY-KOYEB.md && test -f docs/DEPLOY-DOCKER.md && git grep -niE '\b(Koyeb|Oracle)\b' -- docs/DEPLOY-DOCKER.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f docs/DEPLOY-KOYEB.md` passes (file removed) AND `test -f docs/DEPLOY-DOCKER.md` passes.
    - `git grep -niE '\b(Koyeb|Oracle)\b' -- docs/DEPLOY-DOCKER.md` returns zero lines.
    - `docs/DEPLOY-DOCKER.md` contains all D-06 required tokens: `git grep -qF 'docker compose up' docs/DEPLOY-DOCKER.md`,
      `git grep -qF 'cp .env.example .env' docs/DEPLOY-DOCKER.md`, `git grep -qF '/health' docs/DEPLOY-DOCKER.md`,
      `git grep -qiF 'residential' docs/DEPLOY-DOCKER.md`, and `git grep -qiE 'do not run two|same Discord token' docs/DEPLOY-DOCKER.md` each succeed.
    - No real secret value appears: `git grep -niE 'DISCORD_TOKEN=[A-Za-z0-9._-]{10,}' docs/DEPLOY-DOCKER.md` returns zero, and the doc states `.env` is gitignored / never commit.
    - `wc -l docs/DEPLOY-DOCKER.md` is between ~35 and ~80 lines (lean-but-complete, not the old 179).
  </acceptance_criteria>
  <done>DEPLOY-KOYEB.md removed; DEPLOY-DOCKER.md exists with all D-06 sections, zero Koyeb/Oracle, no real secrets, and the residential/parked + single-token honesty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public doc → reader's secret handling | The run guide instructs where the reader puts real tokens; bad guidance can leak secrets |
| template reframe → operational safeguards | Dropping the wrong `.env.example` note removes a real monitoring/DSN safeguard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Information Disclosure | `docs/DEPLOY-DOCKER.md` secret handling | mitigate | Doc uses placeholder values only, embeds no real secret, and states `.env` is gitignored / never commit. Acceptance criteria grep-assert no real token pattern and the never-commit line. |
| T-24-05 | Information Disclosure | `.env.example` reframe dropping the K-09 Healthchecks.io dead-man note or the `sanitize_database_url` DSN guidance | mitigate | D-07 explicitly preserves both; acceptance criteria assert `K-09` and `sanitize_database_url` still present. |
| T-24-06 | Tampering | CLAUDE.md rewrite silently deleting a Critical Rule or a K-## tag | mitigate | Only hosting-narrative prose is reworded; K-## census gate confirms K-04/K-07/K-16 preserved; Critical Rules section untouched. |
| T-24-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | This plan installs zero packages; docs/template only. No `T-{phase}-SC` checkpoint needed. |
</threat_model>

<verification>
- Part-1 grep over `.env.example`, `CLAUDE.md`, `docs/DEPLOY-DOCKER.md` returns zero Koyeb/Oracle hits.
- `docs/DEPLOY-KOYEB.md` absent; `docs/DEPLOY-DOCKER.md` present with all D-06 tokens and no real secret.
- K-## census in `.env.example` (K-09, K-13) and CLAUDE.md (K-04/K-07/K-16) unchanged.
- CLAUDE.md scripts/ tree carries no reference to the deleted scripts.
</verification>

<success_criteria>
`.env.example` and CLAUDE.md are host-honest with zero live Koyeb/Oracle references and all tags
preserved; `docs/DEPLOY-KOYEB.md` is removed and replaced by a lean, accurate, secret-safe
`docs/DEPLOY-DOCKER.md` documenting the real `docker compose up` → Neon flow (HOST-02); the DATABASE_URL
split and the K-09 dead-man safeguard survive the `.env.example` reframe (D-07).
</success_criteria>

<output>
Create `.planning/phases/24-hosting-honesty-docker/24-02-SUMMARY.md` when done.
</output>
