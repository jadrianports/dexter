---
phase: 24-hosting-honesty-docker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot.py
  - config.py
  - utils/logger.py
  - utils/embeds.py
  - Dockerfile
  - docker-compose.yml
  - scripts/__init__.py
  - scripts/archive/backup.sh
  - scripts/archive/deploy.sh
  - scripts/archive/keepalive.sh
  - scripts/archive/lifecycle-policy.json
  - scripts/seed_restore_test.py
  - tests/test_seed_restore.py
autonomous: true
requirements: [HOST-01]
must_haves:
  truths:
    - "git grep -niE '\\b(Koyeb|Oracle)\\b' over bot.py, config.py, Dockerfile, docker-compose.yml, utils/logger.py, utils/embeds.py returns zero hits (D-01 / D-11)"
    - "The bot.py $PORT read os.environ.get('PORT','8000') is byte-identical and the /health server still binds 0.0.0.0:8000 — only surrounding comment prose changed (D-01)"
    - "Every (K-##)/(D-##) tag in the six scrubbed files is byte-identical post-edit: K-02/K-04/K-05 in bot.py, K-07 in config.py, K-16 in utils/logger.py, D-19 in utils/embeds.py, K-11/K-12 in Dockerfile (D-04)"
    - "scripts/archive/{backup.sh,deploy.sh,keepalive.sh,lifecycle-policy.json} (D-02) and scripts/seed_restore_test.py (D-11) no longer exist in the repo"
    - "Security/operational rationale in scrubbed comments is preserved — the bot.py:264 SIGTERM-cleanup note and the utils/embeds.py:294 baseline note keep their meaning; nothing security-relevant is silently dropped"
    - "Full pytest suite stays green: the orphaned tests/test_seed_restore.py (sole importer of the deleted module) is removed and scripts/__init__.py no longer names the deleted module, so collection does not error"
    - "The .planning/, milestones/, and docs/superpowers/ sealed archives are untouched (D-03 / D-10)"
  artifacts:
    - path: "bot.py"
      provides: "Host-honest health-endpoint comments; $PORT read + K-02/K-04/K-05 tags intact"
      contains: "os.environ.get"
    - path: "config.py"
      provides: "512MB cap comment, host-honest, (K-07) preserved"
      contains: "(K-07)"
    - path: "utils/embeds.py"
      provides: "host-honest baseline comment, (D-19) preserved"
      contains: "(D-19)"
    - path: "Dockerfile"
      provides: "host-honest header, (K-11)/(K-12) preserved"
      contains: "K-11"
    - path: "scripts/__init__.py"
      provides: "package marker with no reference to the deleted seed_restore_test module"
  key_links:
    - from: "bot.py health server"
      to: "PORT env var"
      via: "os.environ.get('PORT', '8000')"
      pattern: "os\\.environ\\.get\\(\"PORT\""
---

<objective>
Scrub every live `Koyeb`/`Oracle` hosting-target reference out of Dexter's runtime code, config,
and infrastructure comments (`bot.py`, `config.py`, `utils/logger.py`, `utils/embeds.py`,
`Dockerfile`, `docker-compose.yml`) — rewriting only the prose while preserving every `(K-##)` /
`(D-##)` traceability tag and every runtime behavior (the `$PORT` read stays) — and `git rm` the
dead Oracle-era ops scripts plus their orphaned test.

Purpose: The 24/7 cloud deploy was parked long ago; the code comments still name Koyeb/Oracle as if
they were live hosting targets. This makes the runtime layer of the repo host-honest (HOST-01) and
removes dead scripts that only survived because the Koyeb doc referenced them (D-02 / D-11). This
plan owns the non-doc, non-template half of the scrub; plan 24-02 owns the docs/templates.

Output: Six scrubbed source/infra files (prose-only, tags preserved, zero behavior change) and five
deleted dead files + one orphaned test removed + one package comment fixed.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-hosting-honesty-docker/24-CONTEXT.md
@.planning/phases/24-hosting-honesty-docker/24-RESEARCH.md
@.planning/phases/24-hosting-honesty-docker/24-PATTERNS.md
@.planning/phases/24-hosting-honesty-docker/24-VALIDATION.md
</context>

## Artifacts this phase produces (phase-wide)

New files created across Phase 24 (this plan creates none new — it edits and deletes):
- `docs/DEPLOY-DOCKER.md` — created in plan 24-02
- `tests/test_hosting_drift_guard.py` (+ its `_repo_root`, `_tracked_non_archive_files`,
  `_scan_for_zero_tolerance_terms` helpers) — created in plan 24-03
- `.planning/phases/24-hosting-honesty-docker/24-HOST-UAT.md` — created in plan 24-03

This plan's outputs are edits and deletions only (no new artifacts).

<tasks>

<task type="auto">
  <name>Task 1: Scrub host-name prose from Python runtime/util comments (keep $PORT read + all tags)</name>
  <files>bot.py, config.py, utils/logger.py, utils/embeds.py</files>
  <read_first>
    - `bot.py` lines ~210–270 and ~570–595 (the two `/health` comment ranges) — read both before editing
    - `config.py` lines 1–30 (the `AUDIO_CACHE_MAX_MB` comment at line 22)
    - `utils/logger.py` lines 35–45 (line 41 comment)
    - `utils/embeds.py` lines 288–298 (line 294 comment)
    - `.planning/phases/24-hosting-honesty-docker/24-RESEARCH.md` § "Full Reference Inventory" (the file:line dispositions) and § "Code Examples" (the `$PORT`-survivor before/after)
    - `.planning/phases/24-hosting-honesty-docker/24-PATTERNS.md` §§ "the `$PORT`-survivor pattern" and the D-04 tag-preservation rule
  </read_first>
  <action>
    Reword the hosting-target prose in-place at these exact sites, changing ONLY the hosting words
    and never the adjacent tag or the code line (per D-01, D-04, D-11):
    - config.py:22 — the trailing comment currently names Koyeb's 2GB ephemeral disk. Reword to
      describe the 512MB cap on the ephemeral disk, keeping the `(K-07)` tag byte-identical (this is
      CONTEXT.md's own locked worked example).
    - bot.py ~216–220 — the `_run_health_server` docstring/comments that call this "health check
      endpoint for Koyeb WEB service" and reference Koyeb's free-tier WEB-vs-Worker requirement and
      Koyeb's health checker reaching 0.0.0.0. Reword to a host-agnostic framing (a minimal HTTP
      health endpoint bound to 0.0.0.0 so any host/uptime pinger can reach it). Keep the aiohttp
      route behavior and the bind address unchanged.
    - bot.py ~252–254 — the `# Render injects $PORT ...` / `# Railway / PC / local ...` /
      `# ... Render free web service from sleeping.` block. Reword host-honest per RESEARCH.md's
      illustrative after (the `$PORT` is read for host portability; default 8000 keeps local/Docker
      runs unchanged; the /health route lets an optional external uptime pinger confirm the process
      is alive). Also drop the dead "Railway" name in the same pass (RESEARCH.md A3). The literal
      `_health_port = int(os.environ.get("PORT", "8000"))` line stays byte-identical.
    - bot.py:264 — reword "harmless under Koyeb SIGTERM-exit" to a host-neutral SIGTERM-exit phrasing;
      PRESERVE the "latent leak only on in-process restart" operational meaning (do not delete the
      safeguard note).
    - bot.py ~578–589 — the `K-02:` health-endpoint comments naming "Koyeb WEB service" / "Koyeb's
      health check". Reword host-honest, keeping the `K-02` and any `K-04`/`K-05` tags byte-identical.
    - utils/logger.py:41 — reword "Docker/Koyeb log viewers" to "Docker/container log viewers" (or
      equivalent host-neutral phrasing), keeping the `(K-16)` tag.
    - utils/embeds.py:294 — reword "No Oracle/CPU label" to "No host-CPU label" (D-11 worked example),
      keeping the `(D-19)` tag and the "baselines against actual run environment" meaning.
    Do NOT touch `scripts/memory_spike.py` or `tests/test_config.py` — they carry `(K-##)` tags but no
    hosting word (confirmed by the inventory grep), so editing them risks disturbing tags for nothing.
  </action>
  <verify>
    <automated>git grep -niE '\b(Koyeb|Oracle)\b' -- bot.py config.py utils/logger.py utils/embeds.py; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -niE '\b(Koyeb|Oracle)\b' -- bot.py config.py utils/logger.py utils/embeds.py` returns
      zero lines (git grep exit code 1 = no match).
    - `git grep -nE '\(K-07\)' -- config.py`, `git grep -nE '\(K-16\)' -- utils/logger.py`,
      `git grep -nE '\(D-19\)' -- utils/embeds.py`, and `git grep -nE 'K-02' -- bot.py` each still
      return their line — tags preserved byte-identical (D-04).
    - `git grep -nF 'os.environ.get("PORT", "8000")' -- bot.py` still returns the unchanged read.
    - `python -c "import ast; [ast.parse(open(f).read()) for f in ['bot.py','config.py','utils/logger.py','utils/embeds.py']]"` exits 0 (files still parse).
    - `ruff check bot.py config.py utils/logger.py utils/embeds.py` is clean.
  </acceptance_criteria>
  <done>Zero Koyeb/Oracle in the four Python files; every `(K-##)`/`(D-19)` tag intact; the `$PORT` read and health bind are byte-identical; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Scrub host-name prose from Dockerfile + docker-compose.yml (keep K-11/K-12 tags)</name>
  <files>Dockerfile, docker-compose.yml</files>
  <read_first>
    - `Dockerfile` lines 1–10 (header comment naming Koyeb at lines 1–2)
    - `docker-compose.yml` full file (the "Oracle-era legacy" comment at line 17; the file is already
      bot-only → Neon and needs NO functional change — comment-only edit)
    - `.planning/phases/24-hosting-honesty-docker/24-PATTERNS.md` § D-04 tag-preservation rule
  </read_first>
  <action>
    Reword the two infra-file comments in-place (D-01, D-04):
    - Dockerfile:1–2 — the header comment says "multi-arch (amd64 on Koyeb/CI, arm64 on dev machines)"
      and "Koyeb builds this Dockerfile directly from git (K-11); docker-compose.yml is local-dev only
      (K-12)." Reword host-honest: keep the multi-arch note but drop the Koyeb-builds-from-git framing
      in favor of a host-neutral "built by CI / any Docker host" phrasing, and keep the
      docker-compose-is-local-dev distinction. PRESERVE the `(K-11)` and `(K-12)` tags byte-identical.
    - docker-compose.yml:17 — reword "the colocated-Postgres override was Oracle-era legacy" to a
      host-neutral phrasing (e.g. the colocated-Postgres override is retired legacy; the bot connects
      directly to Neon). Keep the surrounding env_file/DATABASE_URL comment meaning intact. No service,
      volume, or `env_file` change — this is comment-only.
  </action>
  <verify>
    <automated>git grep -niE '\b(Koyeb|Oracle)\b' -- Dockerfile docker-compose.yml; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -niE '\b(Koyeb|Oracle)\b' -- Dockerfile docker-compose.yml` returns zero lines.
    - `git grep -nE 'K-11|K-12' -- Dockerfile` still returns both tags (D-04).
    - `git diff docker-compose.yml` shows only comment-line changes; the `services:`, `bot:`,
      `env_file: .env`, `volumes:`, and `restart:` keys are untouched.
    - `docker compose config -q` exits 0 (compose file still parses) — run only if Docker is available;
      otherwise skip with a note.
  </acceptance_criteria>
  <done>Zero Koyeb/Oracle in Dockerfile and docker-compose.yml; K-11/K-12 preserved; compose service/volume definitions unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: git rm dead Oracle-era scripts + the orphaned seed-restore test; fix package comment</name>
  <files>scripts/archive/backup.sh, scripts/archive/deploy.sh, scripts/archive/keepalive.sh, scripts/archive/lifecycle-policy.json, scripts/seed_restore_test.py, tests/test_seed_restore.py, scripts/__init__.py</files>
  <read_first>
    - `scripts/seed_restore_test.py` (docstring lines 1–26 name "Oracle" and call the deleted
      `scripts/backup.sh`; the only importable symbols are `build_seed_rows` + `SEED_USER_ID`)
    - `tests/test_seed_restore.py` (line 9 imports `from scripts.seed_restore_test import SEED_USER_ID, build_seed_rows` — this is the SOLE importer of the deleted module)
    - `scripts/__init__.py` (line 2 comment names `scripts.seed_restore_test`)
    - `.planning/phases/24-hosting-honesty-docker/24-RESEARCH.md` § "New Findings" (Pitfall 2) and
      § "Open Questions" #2 (delete-vs-rewrite — D-11 locked the delete)
  </read_first>
  <action>
    Execute the D-02 + D-11 deletions and their required completion (research under-scoped D-11 — it
    did not detect the coupled test file; delete it too or the suite goes red at collection):
    - `git rm scripts/archive/backup.sh scripts/archive/deploy.sh scripts/archive/keepalive.sh scripts/archive/lifecycle-policy.json` (D-02 — dead Oracle-era ops scripts, retired by Neon PITR + UptimeRobot).
    - `git rm scripts/seed_restore_test.py` (D-11 — dead Oracle-era restore-proof helper coupled to the
      now-deleted backup.sh; its docstring says "run ONCE manually by the user on Oracle").
    - `git rm tests/test_seed_restore.py` — this file imports `build_seed_rows`/`SEED_USER_ID` from the
      module deleted above and is its ONLY consumer; without removing it, pytest collection errors and
      the whole suite goes red, violating the phase's zero-regression gate. This is the necessary
      completion of D-11, not new scope.
    - Edit `scripts/__init__.py` line 2 — it currently reads "Required so that tests can import pure
      helpers from scripts.seed_restore_test." Reword to a generic package-marker comment that no longer
      names the deleted module (keep `scripts/__init__.py` itself — it is a harmless package marker and
      other tooling may rely on `scripts/` being a package).
    Do NOT delete `scripts/memory_spike.py` or `scripts/render_demo_gif.py` — they are live tooling.
  </action>
  <verify>
    <automated>test ! -e scripts/seed_restore_test.py && test ! -e tests/test_seed_restore.py && test ! -e scripts/archive/backup.sh && git grep -nE 'seed_restore_test' -- . ':!.planning' ':!milestones' ':!docs/superpowers'; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/archive/backup.sh`, `deploy.sh`, `keepalive.sh`, `lifecycle-policy.json`,
      `scripts/seed_restore_test.py`, and `tests/test_seed_restore.py` no longer exist (`test ! -e ...`
      passes for each).
    - `git grep -nE 'seed_restore_test|backup\.sh' -- . ':!.planning' ':!milestones' ':!docs/superpowers'`
      returns zero live-reference lines (git grep exit code 1) — CLAUDE.md's scripts/ tree reference is
      handled in plan 24-02.
    - `python -m pytest --collect-only -q` exits 0 (no import/collection error from the removed module).
    - `git grep -niE '\b(Koyeb|Oracle)\b' -- scripts/` returns zero (the only remaining scripts —
      `memory_spike.py`, `render_demo_gif.py`, `__init__.py` — carry no hosting word).
  </acceptance_criteria>
  <done>All five dead files and the orphaned test are gone; scripts/__init__.py no longer names the deleted module; pytest collection succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comment prose → operational knowledge | A comment rewrite can silently delete a real safeguard's documented rationale |
| module deletion → test suite integrity | Deleting a module without its consumer breaks collection for the whole suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | scrubbed comments (bot.py:264 SIGTERM note, utils/embeds.py:294 baseline note, config.sanitize rationale nearby) | mitigate | Reword ONLY the hosting word; preserve the operational/security meaning and the adjacent `(K-##)`/`(D-##)` tag. Acceptance criteria assert each tag substring still present post-edit. |
| T-24-02 | Tampering | pytest suite integrity after deleting `scripts/seed_restore_test.py` | mitigate | Delete the sole-consumer `tests/test_seed_restore.py` and fix `scripts/__init__.py`; `pytest --collect-only` gate proves no orphaned import. |
| T-24-03 | Tampering | `$PORT` read / health bind accidentally altered during comment scrub | mitigate | Acceptance criteria assert `os.environ.get("PORT","8000")` byte-identical and 0.0.0.0:8000 bind unchanged; `ast.parse` + `ruff` gates. |
| T-24-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | This plan installs zero packages; `requirements*.txt` untouched. No `T-{phase}-SC` checkpoint needed. |
</threat_model>

<verification>
- Part-1 grep over this plan's six scrubbed files returns zero Koyeb/Oracle hits.
- K-## / D-## tag census over the touched files is unchanged (K-02/K-04/K-05 bot.py, K-07 config.py,
  K-16 utils/logger.py, D-19 utils/embeds.py, K-11/K-12 Dockerfile).
- `python -m pytest --collect-only -q` succeeds; `ruff check .` clean on touched Python files.
- Five dead files + orphaned test removed; no live reference to `seed_restore_test`/`backup.sh` remains.
</verification>

<success_criteria>
Zero live `Koyeb`/`Oracle` references in `bot.py`, `config.py`, `utils/logger.py`, `utils/embeds.py`,
`Dockerfile`, `docker-compose.yml`; every `(K-##)`/`(D-##)` tag preserved; the `$PORT` read and health
bind unchanged; the four D-02 scripts + `scripts/seed_restore_test.py` (D-11) + the orphaned
`tests/test_seed_restore.py` deleted; pytest collection green; sealed archives untouched (D-03/D-10).
</success_criteria>

<output>
Create `.planning/phases/24-hosting-honesty-docker/24-01-SUMMARY.md` when done.
</output>
