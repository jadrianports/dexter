---
phase: 26-radio-mode-skip-democracy
plan: 04
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - cogs/music.py
  - tests/test_music_wiring.py
autonomous: true
requirements: [DJ-02]
must_haves:
  truths:
    - "The /skip slash command and the now-playing Skip button both go through ONE vote gate (D-15/SC-3)"
    - "A solo listener's /skip still skips instantly with no tally message (SC-4)"
    - "With 2+ listeners, a skip needs a strict majority and Dexter narrates the running tally on each vote (SC-3)"
    - "The vote denominator is read fresh from voice on every single vote, never memoized (D-17/Pitfall 4)"
    - "A vote-skipped track records via the existing mark_song_skipped and nothing more (D-20)"
  artifacts:
    - path: "cogs/music.py"
      provides: "The single vote-gated skip choke point"
      contains: "async def _try_skip"
    - path: "tests/test_music_wiring.py"
      provides: "Structural regression guard for the D-15 unification (Pitfall 1)"
      min_lines: 80
  key_links:
    - from: "cogs/music.py::skip"
      to: "_try_skip"
      via: "slash command routes through the shared gate (Pitfall 1 fix)"
      pattern: "_try_skip\\("
    - from: "cogs/music.py::NowPlayingView.skip_button"
      to: "_try_skip"
      via: "button routes through the same shared gate"
      pattern: "_try_skip\\("
    - from: "cogs/music.py::_try_skip"
      to: "logic.skip_vote.decide_skip"
      via: "verdict dispatch, never re-derived arithmetic"
      pattern: "decide_skip\\("
---

<objective>
Close the D-15 bypass hole: route BOTH skip entry points through one vote-gated choke point
that dispatches on `SkipVerdict`.

Purpose: SC-3 and SC-4. DJ-02 is trivially defeated by a one-click bypass sitting on a
persistent message — and, per 26-RESEARCH Pitfall 1, by the `/skip` slash command's own
duplicated inline body.
Output: `_try_skip` in `cogs/music.py`, both callers rewired, `tests/test_music_wiring.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-radio-mode-skip-democracy/26-CONTEXT.md
@.planning/phases/26-radio-mode-skip-democracy/26-RESEARCH.md
@.planning/phases/26-radio-mode-skip-democracy/26-02-SUMMARY.md
@CLAUDE.md
</context>

<planner_decisions>
Locked by the planner — do NOT re-litigate or re-ask the user:

- **PITFALL 1 IS REAL AND VERIFIED — trust the code, not 26-CONTEXT.md's canonical-refs
  summary.** That summary describes `/skip` and the button as already sharing `_do_skip`. They
  do NOT. Direct read of `cogs/music.py` confirms: `MusicCog.skip` (`:1661-1695`) has a fully
  duplicated inline skip body and never calls `_do_skip`; only `NowPlayingView.skip_button`
  (`:389`) calls it. **Adding a vote check in front of the existing `_do_skip` call would leave
  `/skip` sailing through completely unvoted** — the exact hole D-15 exists to close, just on
  the other surface. This plan's Task 2 deletes that inline body. A change that touches only
  `skip_button` or only `_do_skip` has NOT closed D-15.
- **The shape: a new `_try_skip` gate wraps, `_do_skip` stays the mechanics.** `_do_skip`
  keeps doing exactly what it does today (`mark_song_skipped`, `auto_queue_results["skipped"]`,
  `queue.skip()`, `_persist_queue`, `make_task(_play_track, "play-after-skip")`,
  `_refresh_now_playing`, the exhausted-queue `voice_client.stop()`) and is called ONLY on a
  `SKIP_NOW` verdict. This preserves D-20 (a vote-skipped track records via the existing
  `mark_song_skipped` and nothing more) and Critical Rule 3 (FFmpeg cleanup) for free, because
  the skip mechanics are not rewritten — only the decision to reach them changes.
- **`/pause`, `/resume`, `/stop` have the same inline-body-vs-`_do_*`-helper duplication. Do
  NOT unify them.** It is a pre-existing, broader pattern in this file and explicitly out of
  scope; only skip is in scope. Touching them would balloon the diff on the phase's riskiest file.
- **`_try_skip` returns a small result, not a Discord response.** The two callers differ in how
  they reply (`interaction.response.send_message` vs a deferred `followup.send`, public vs
  ephemeral), so `_try_skip` must not own the reply. It returns
  `tuple[SkipVerdict, Track | None, int, int]` — `(verdict, next_track, votes, required)` —
  and each caller renders. The TALLY line itself is posted publicly by `_try_skip` (D-16: a
  vote nobody can see isn't democracy; the room must see it to join it), which is separate from
  the caller's own acknowledgement.
</planner_decisions>

<artifacts_this_phase_produces>
Created/changed by THIS plan (26-05 does not modify these — it adds separate surfaces):

| Symbol | File | Kind |
|--------|------|------|
| `MusicCog._try_skip(guild, queue, voice_client, *, voter_id) -> tuple[SkipVerdict, Track \| None, int, int]` | `cogs/music.py` | the single vote-gated choke point (D-15) |
| `tests/test_music_wiring.py` | new file | structural guard (Pitfall 1 regression lock) |

Consumed from 26-02: `logic.skip_vote.SkipVerdict/decide_skip/required_votes`,
`MusicQueue.skip_votes_for_current/record_skip_votes`, `personality.responses.SKIP_VOTE_TALLY`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The _try_skip shared vote gate</name>
  <files>cogs/music.py</files>
  <read_first>
    - `cogs/music.py:1002-1033` — the `SHARED CONTROL HELPERS` comment block (Phase 7's stated intent: "called by both slash commands AND NowPlayingView button callbacks so that logic lives in one place") and `_do_skip` in full. `_try_skip` goes directly above `_do_skip` under the same banner.
    - `cogs/music.py:842-886` — `_on_track_end`'s `decide_on_track_end` dispatch: the exact `if action == ... elif action == ...` verdict-dispatch convention to mirror (Phase 10 D-02).
    - `cogs/music.py:864` and `cogs/ai.py:335` and `bot.py:951` — the ONE canonical voice-member idiom `[m for m in vc.channel.members if not m.bot]`. Reuse it; never invent a second definition (D-09b).
    - `cogs/music.py::_get_text_channel` — how the cog resolves where to post (Critical Rule 9).
    - `logic/skip_vote.py` (26-02) — `SkipVerdict`, `decide_skip`, `required_votes` signatures.
    - `models/queue.py` (26-02) — `skip_votes_for_current()` / `record_skip_votes()`.
    - `personality/responses.py` (26-02) — `SKIP_VOTE_TALLY` and its `{votes}`/`{required}` slots; the `pick_random(POOL).format(...)` call convention.
  </read_first>
  <action>
    Add `async def _try_skip(self, guild, queue, voice_client, *, voter_id: int) -> tuple[
    SkipVerdict, Track | None, int, int]` to `cogs/music.py`, placed directly above `_do_skip`
    under the existing `SHARED CONTROL HELPERS` banner. Returns
    `(verdict, next_track, votes_now, required)`; `next_track` is None unless the verdict was
    `SKIP_NOW` and the queue had a next track.

    Body, in order:
    1. Read the current track via `queue.get_current()`. If there is no current track, return
       `(SkipVerdict.SKIP_NOW, None, 0, 0)` and let the caller's existing "nothing playing"
       guards handle it — do not add a new guard here.
    2. Compute `listener_ids` FRESH on every invocation using the canonical idiom:
       `frozenset(m.id for m in voice_client.channel.members if not m.bot)` when
       `voice_client and voice_client.channel` else `frozenset()`. **Never memoize this
       per-track** — 26-RESEARCH Pitfall 4: D-17 requires the denominator to reflect who is in
       voice at the moment of THIS vote; a snapshot captured when the vote "opened" silently
       goes wrong the moment anyone joins or leaves. It is a synchronous property read on cached
       gateway state, not a network call. Comment this explicitly.
    3. Compute `is_requester = voter_id == current.requested_by` — a plain equality, nothing
       more. **Do NOT special-case `self.bot.user.id`**: D-13b (bot-queued tracks never bypass)
       falls out for free because no human voter's id equals the bot's, and a
       `requested_by == bot.user.id` branch would at best be redundant and at worst re-open the
       hole (Pitfall 5). Comment this. Do NOT add any `manage_guild`/`is_owner()` bypass —
       D-13a rejects it outright as a scope violation (T-26-05).
    4. `existing = queue.skip_votes_for_current()`, then
       `verdict, new_votes = decide_skip(voter_id=voter_id, is_requester=is_requester,
       listener_ids=listener_ids, existing_votes=existing)`.
    5. `queue.record_skip_votes(new_votes)`.
    6. Compute `required = required_votes(listener_count=len(listener_ids))` for the tally.
       Call the pure helper — **never re-derive the arithmetic in this file** (Phase 10 D-02:
       glue dispatches on the returned value and never mirrors the branch logic back).
    7. Dispatch on the verdict with an `if/elif` mirroring `_on_track_end`'s style:
       - `SKIP_NOW` -> `next_track = await self._do_skip(guild, queue, voice_client)`; return
         `(verdict, next_track, len(new_votes), required)`. The existing `_do_skip` mechanics run
         unchanged — that is D-20 (the existing `mark_song_skipped` and nothing more) and
         Critical Rule 3 (FFmpeg cleanup) preserved by construction.
       - `VOTE_RECORDED` / `ALREADY_VOTED` -> post the public tally line and return
         `(verdict, None, len(new_votes), required)` with NO skip mechanics.
    8. The tally post (D-16/D-18): resolve the channel via the cog's existing
       `_get_text_channel(guild)` (Critical Rule 9 — designated channel only, no new
       channel-picking); if it resolves, send
       `pick_random(SKIP_VOTE_TALLY).format(votes=len(new_votes), required=required)` with
       `allowed_mentions=discord.AllowedMentions.none()`. The numbers are interpolated by CODE
       from live state — never a Gemini call (D-18 / Critical Rule 12: a vote fires several times
       per track, carries live numbers, and must work when the 15 RPM budget is exhausted).
       Post the tally for `ALREADY_VOTED` too — the tally is unchanged, but the room still learns
       a vote is open (D-16), and staying silent would make a repeat `/skip` look broken.
       Wrap the send in a `try/except Exception` that logs and continues: a failed tally post
       must never take down the vote path.

    Import `SkipVerdict`, `decide_skip`, `required_votes` from `logic.skip_vote` and
    `SKIP_VOTE_TALLY` from `personality.responses` at the top of `cogs/music.py`, alongside the
    existing `logic.playback` / `personality.responses` imports.

    Do NOT modify `_do_skip` itself. Do NOT touch `_do_pause_toggle`, `_do_stop`, or the
    `/pause`, `/resume`, `/stop` commands.
  </action>
  <verify>
    <automated>cd /c/Users/James/Desktop/Projects/dexter && python -m pytest tests/test_now_playing_refresh.py tests/test_playback_logic.py tests/test_skip_vote_logic.py tests/test_fresh_boot_regressions.py -x -q && python -m ruff check cogs/music.py && python -m ruff format --check cogs/music.py</automated>
  </verify>
  <acceptance_criteria>
    - `cogs/music.py` contains `async def _try_skip(` with a keyword-only `voter_id` param
    - `inspect.getsource(MusicCog._try_skip)` contains `decide_skip(`, `required_votes(`, `skip_votes_for_current(`, `record_skip_votes(`
    - `_try_skip` source contains `for m in voice_client.channel.members if not m.bot`
    - `_try_skip` source contains `voter_id == current.requested_by`
    - `_try_skip` source contains no `bot.user.id` comparison, no `manage_guild`, no `is_owner`
    - `_try_skip` source contains `await self._do_skip(` on the `SKIP_NOW` branch only
    - `_try_skip` source contains `SKIP_VOTE_TALLY` and `.format(votes=` and `AllowedMentions.none()`
    - `inspect.getsource(MusicCog._do_skip)` is unchanged from its pre-task content
    - `pytest tests/test_skip_vote_logic.py -x` exits 0
  </acceptance_criteria>
  <done>One vote-gated async choke point exists: it reads listeners fresh, dispatches on SkipVerdict, narrates a templated tally, and reaches the unchanged skip mechanics only on SKIP_NOW.</done>
</task>

<task type="auto">
  <name>Task 2: Route /skip and the Skip button through _try_skip (the Pitfall 1 fix)</name>
  <files>cogs/music.py</files>
  <read_first>
    - `cogs/music.py:1661-1695` — the `/skip` slash command's **fully duplicated inline body** you are deleting: its `mark_song_skipped` + `auto_queue_results["skipped"]` block, `queue.skip()`, `_persist_queue`, the `make_task(..., "play-after-skip")` call, `_refresh_now_playing`, and the exhausted-queue branch. Every one of these already exists inside `_do_skip` (`:1006-1033`) — that is the duplication.
    - `cogs/music.py:376-393` — `NowPlayingView.skip_button`: `_resolve_cog_queue_vc`, `_guard_in_voice`, the `is_playing`/`is_paused` guard, `await interaction.response.defer()`, then `cog._do_skip(...)`.
    - `cogs/music.py::_try_skip` (Task 1) — its return tuple `(verdict, next_track, votes, required)`.
    - `CLAUDE.md` § Implementation Gotchas → Phase 1 — the 3s interaction-response rule (defer or respond, then background work).
  </read_first>
  <action>
    **`/skip` slash command (`:1661-1695`) — delete the inline body, route through `_try_skip`.**
    Keep the decorator stack exactly as-is, including
    `@app_commands.checks.cooldown(1, config.SKIP_COOLDOWN_SECONDS)` — D-14 keeps the existing
    cooldown as the secondary anti-spam guard, distinct from vote idempotency. Keep the existing
    `if not voice_client or not queue.is_playing:` "Nothing is playing." guard verbatim.
    Then:
    - `await interaction.response.defer()` first (`_try_skip` awaits `_do_skip`, DB writes and a
      channel send — the 3s rule).
    - `verdict, next_track, votes, required = await self._try_skip(interaction.guild, queue,
      voice_client, voter_id=interaction.user.id)`.
    - Render on the verdict: `SKIP_NOW` with a `next_track` -> followup
      `f"Skipped to **{next_track.title}**"`; `SKIP_NOW` with no next track -> followup
      `"End of queue."`; `VOTE_RECORDED` -> an EPHEMERAL followup confirming the vote was
      counted (the public tally is already posted by `_try_skip` — do not post it twice);
      `ALREADY_VOTED` -> an EPHEMERAL followup saying they already voted (D-14: idempotent, and
      the ephemeral keeps the repeat from adding channel noise).
    - **Delete** the inline `mark_song_skipped` / `auto_queue_results["skipped"]` /
      `queue.skip()` / `_persist_queue` / `make_task(..., "play-after-skip")` /
      `_refresh_now_playing` / `queue.is_playing = False` + `voice_client.stop()` block entirely.
      All of it lives in `_do_skip`, which `_try_skip` calls on `SKIP_NOW`. If any of those tokens
      remain in the `skip` command's body, the duplication — and the D-15 hole — still exists.
      Remove the now-unused local `from models.server_state import get_server_state` import from
      the command body if nothing else in it uses it.
    - Add a comment above the command noting that its body was unified into `_try_skip` in Phase
      26 (D-15) and that this file's `/pause`/`/resume`/`/stop` retain their own inline bodies
      by design — only skip is in scope.

    **`NowPlayingView.skip_button` (`:376-393`) — swap `_do_skip` for `_try_skip`.** Keep
    `_resolve_cog_queue_vc`, `_guard_in_voice`, the `is_playing`/`is_paused` guard and the
    existing `await interaction.response.defer()` exactly as-is. Replace
    `next_track = await cog._do_skip(interaction.guild, queue, vc)` with
    `verdict, next_track, votes, required = await cog._try_skip(interaction.guild, queue, vc,
    voter_id=interaction.user.id)` and render the same four verdict cases as the slash command
    (keeping the button's existing lowercase, ephemeral followup voice: "skipped. now playing
    **{title}**." / "end of queue."). D-15: a button press is a vote exactly like `/skip` —
    the button must NOT reach `_do_skip` directly any more.

    Do NOT touch `_do_skip`, `_do_stop`, `_do_pause_toggle`, `_do_shuffle`, or any other button.
  </action>
  <verify>
    <automated>cd /c/Users/James/Desktop/Projects/dexter && python -m pytest tests/test_now_playing_refresh.py tests/test_fresh_boot_regressions.py -x -q && python -m pytest -q && python -m ruff check cogs/music.py && python -m ruff format --check cogs/music.py</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.getsource(MusicCog.skip)` contains `_try_skip(`
    - `inspect.getsource(NowPlayingView.skip_button)` contains `_try_skip(`
    - `inspect.getsource(NowPlayingView.skip_button)` does NOT contain `_do_skip(`
    - `inspect.getsource(MusicCog.skip)` does NOT contain any of: `mark_song_skipped(`, `queue.skip()`, `_persist_queue(`, `play-after-skip`, `_refresh_now_playing(`
    - `inspect.getsource(MusicCog.skip)` still contains `config.SKIP_COOLDOWN_SECONDS`
    - `inspect.getsource(MusicCog.skip)` contains `voter_id=interaction.user.id`
    - `inspect.getsource(NowPlayingView.skip_button)` contains `voter_id=interaction.user.id`
    - Across `cogs/music.py`, `_do_skip(` is called exactly once, and that call site is inside `_try_skip`
    - `pytest` (full suite) exits 0
  </acceptance_criteria>
  <done>Both skip routes go through _try_skip; the /skip slash command's duplicated inline body is gone and the button can no longer reach _do_skip directly. D-15 is closed.</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_music_wiring.py — the D-15 structural regression guard</name>
  <files>tests/test_music_wiring.py</files>
  <read_first>
    - `tests/test_autoqueue_wiring.py` (FULL) — the exact source-scan pattern to clone: module docstring explaining why source assertions exist without a live Discord path, `inspect.getsource` helpers, class-per-concern, `src.count(...)` and index-ordering assertions.
    - `cogs/music.py::_try_skip`, `MusicCog.skip`, `NowPlayingView.skip_button`, `_do_skip` (post-Tasks 1/2) — what you are locking.
    - `logic/skip_vote.py` (26-02) — confirm the arithmetic tokens that must NOT appear in the cog.
  </read_first>
  <action>
    Create `tests/test_music_wiring.py`, cloning `tests/test_autoqueue_wiring.py`'s shape. Module
    docstring: source-assertion regression tests for the Phase 26 skip-vote choke-point
    unification (DJ-02 / D-15), closing 26-RESEARCH Pitfall 1 — `/skip`'s slash body did NOT
    call `_do_skip` before this phase, so a vote check placed only in front of `_do_skip` would
    have left `/skip` entirely unvoted. Note it uses `inspect.getsource` because Discord/process
    glue is untested-by-design per `.planning/codebase/TESTING.md`.

    Helpers: `_skip_command_source()`, `_skip_button_source()`, `_try_skip_source()`,
    `_music_module_source()`.

    `class TestSkipChokePointUnification` (the binding `-k skip_choke_point` selector — name the
    tests so they match, e.g. `test_skip_choke_point_slash_routes_through_try_skip`):
    - The `/skip` slash source contains `_try_skip(`.
    - The button source contains `_try_skip(`.
    - The button source does NOT contain `_do_skip(` (it must reach the mechanics only through
      the gate).
    - The `/skip` slash source contains none of `mark_song_skipped(`, `queue.skip()`,
      `_persist_queue(`, `play-after-skip` — the Pitfall 1 duplication is gone. Add a comment on
      this test naming it as the concrete Pitfall 1 lock.
    - `_do_skip(` is called exactly once in the whole module and that call is inside `_try_skip`
      — assert `_music_module_source().count("self._do_skip(") + count("cog._do_skip(") == 1`
      and `"_do_skip(" in _try_skip_source()`. Filter comment lines before counting.
    - Both callers pass `voter_id=interaction.user.id`.

    `class TestVerdictDispatchedNotReimplemented` (Phase 10 D-02 rule):
    - `decide_skip(` and `required_votes(` appear in `_try_skip`'s source.
    - The majority arithmetic appears NOWHERE in `cogs/music.py` — assert that none of
      `"// 2 + 1"`, `"math.floor"`, `"SKIP_VOTE_MAJORITY_RATIO"` appear in the module's
      non-comment lines. The arithmetic lives only in `logic/skip_vote.py`.

    `class TestFreshListenerRead` (D-17 / Pitfall 4):
    - `_try_skip`'s source contains `for m in voice_client.channel.members if not m.bot` — the
      denominator is read inside the gate on every invocation, not passed in or cached.
    - `cogs/music.py` contains no attribute like `_cached_listener` / `_listener_ids` that would
      indicate a memoized snapshot.

    `class TestNoBypassBackdoor` (T-26-05 / D-13a):
    - `_try_skip`'s source contains `requested_by` (the only bypass) and contains neither
      `manage_guild` nor `is_owner` — an admin/owner skip bypass is an explicit scope VIOLATION,
      not a feature.

    `class TestNoNewMemoryKindInSkipPath` (the binding `-k no_new_memory_kind` selector, D-20):
    - `_try_skip`'s and `_do_skip`'s sources contain no `distill_and_remember(` and no
      `kind=` argument — a vote-skipped track records via the EXISTING `mark_song_skipped` and
      nothing more. D-20 refuses to reopen the memory subsystem that just cleared Phase 25's
      byte-identical gate.
    - `_do_skip`'s source still contains `mark_song_skipped(` (the existing recording path is
      intact, not accidentally removed during the unification).

    Every `count()`/`not in` assertion must first strip comment lines
    (`[ln for ln in src.splitlines() if not ln.strip().startswith("#")]`) so an explanatory
    comment quoting a banned token cannot silently invalidate the gate — the comments in
    `_try_skip` legitimately discuss `bot.user.id` and the majority rule in prose.
  </action>
  <verify>
    <automated>cd /c/Users/James/Desktop/Projects/dexter && python -m pytest tests/test_music_wiring.py -x -q && python -m pytest tests/test_music_wiring.py -k skip_choke_point -x -q && python -m pytest tests/test_music_wiring.py -k no_new_memory_kind -x -q && python -m pytest -q && python -m ruff check tests/test_music_wiring.py && python -m ruff format --check tests/test_music_wiring.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_music_wiring.py` exists and defines `TestSkipChokePointUnification`, `TestVerdictDispatchedNotReimplemented`, `TestFreshListenerRead`, `TestNoBypassBackdoor`, `TestNoNewMemoryKindInSkipPath`
    - `pytest tests/test_music_wiring.py -k skip_choke_point -x` exits 0 with >=1 test collected
    - `pytest tests/test_music_wiring.py -k no_new_memory_kind -x` exits 0 with >=1 test collected
    - A test asserts `_do_skip(` is called exactly once across the module and only from `_try_skip`
    - A test asserts the majority arithmetic is absent from `cogs/music.py`
    - Every `count()`/absence assertion strips comment lines first
    - `pytest` (full suite) exits 0
  </acceptance_criteria>
  <done>A structural guard fails the build if a future edit re-duplicates a skip body, lets a surface bypass the gate, re-derives the arithmetic in glue, memoizes the denominator, adds an admin bypass, or bolts a memory write onto the skip path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user -> `/skip` slash command | Untrusted identity requests playback control affecting everyone in voice |
| Discord user -> `⏭ Skip` persistent button | The same authority via a second, one-click surface on a durable message |
| Discord gateway -> `voice_client.channel.members` | The live listener set is the vote denominator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Elevation of Privilege | `MusicCog.skip`, `NowPlayingView.skip_button` | mitigate | **This is the phase's primary control.** Both surfaces route through the single `_try_skip` gate — the Phase 20 OWNER-05 precedent ("enforced at ONE choke point, never per-cog checks", Critical Rule 19) applied to skip. 26-RESEARCH Pitfall 1 proves the hole was real on BOTH surfaces, not just the button: `/skip`'s inline body never called `_do_skip`. Task 2 deletes that body; Task 3's structural test asserts `_do_skip(` is called exactly once, from inside `_try_skip`, so a future edit cannot silently re-open either route. The only bypass is `voter_id == current.requested_by` (D-13a); `manage_guild`/`is_owner` bypasses are asserted ABSENT — D-13a rejects them as a scope violation that would reinstate the unilateral power DJ-02 removes. |
| T-26-01 | Elevation of Privilege / Repudiation | vote brigading via repeated `/skip` | mitigate | Two independent layers: `decide_skip`'s D-14 idempotency (26-02) makes a repeat vote from the same identity a no-op even after the cooldown elapses, and the retained `@app_commands.checks.cooldown(1, config.SKIP_COOLDOWN_SECONDS)` decorator remains as the command-layer anti-spam guard. Task 2's acceptance criteria assert the cooldown decorator survives the rewrite. |
| T-26-03 | Tampering (stale-state) | `listener_ids` denominator | mitigate + accept | Mitigated half: `_try_skip` reads `[m for m in voice_client.channel.members if not m.bot]` FRESH on every invocation and never memoizes it (Pitfall 4 — a per-track snapshot silently breaks the threshold the moment anyone joins or leaves mid-vote); locked by `TestFreshListenerRead`. Accepted half: a departed voter's already-cast vote still counts (D-17, scoped and documented) so a walkout cannot strand an open vote below a threshold that just dropped. |
| T-26-11 | Repudiation | skip-path memory writes | mitigate | D-20 locks that a vote-skipped track records via the existing `mark_song_skipped` and NOTHING more — no new memory kind, no new weighting, into a subsystem that just cleared Phase 25's SC-3 byte-identical gate. Enforced by construction (`_do_skip` is unmodified) and asserted by `TestNoNewMemoryKindInSkipPath`. |
| T-26-12 | Denial of Service | tally post failure | mitigate | The tally send is wrapped in a logged `try/except` so a channel-permission or rate-limit failure cannot take down the vote path or leave a skip half-applied. The tally is templated (D-18) and therefore cannot fail on an exhausted Gemini budget. |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero new packages.** 26-RESEARCH.md § Package Legitimacy Audit is "Not applicable". No install task exists in this plan. |
</threat_model>

<verification>
- `pytest tests/test_music_wiring.py -x` green, including `-k skip_choke_point` and `-k no_new_memory_kind`
- Full suite `pytest` green
- `ruff check .` and `ruff format --check .` clean
- Manual structural review: `grep -n "_do_skip(" cogs/music.py` shows exactly one call site, inside `_try_skip`
</verification>

<success_criteria>
- `_try_skip` is the single vote-gated choke point; both `/skip` and the Skip button call it and neither reaches `_do_skip` directly
- `/skip`'s duplicated inline body is deleted (Pitfall 1 closed)
- The denominator is read fresh per vote; a departed voter's vote still counts
- The tally is posted publicly from templated copy with code-interpolated numbers
- No admin/owner bypass exists; the only bypass is the track's requester
- `_do_skip` and its `mark_song_skipped` call are unmodified (D-20)
- Full suite green
</success_criteria>

<output>
Create `.planning/phases/26-radio-mode-skip-democracy/26-04-SUMMARY.md` when done
</output>
</content>
