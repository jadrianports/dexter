---
phase: 26-radio-mode-skip-democracy
plan: 02
subsystem: music
tags: [skip-vote, dj-02, pure-logic, tdd, music-queue, personality]

# Dependency graph
requires:
  - phase: 26-01
    provides: "logic/radio.py as the pure-gate module template (docstring, keyword-only, cheapest-gate-first), and the Phase 26 config.py block to extend"
provides:
  - "logic/skip_vote.py — pure, mock-free, keyword-only DJ-02 verdict seam (SkipVerdict, required_votes, decide_skip)"
  - "MusicQueue.skip_votes_for_current()/record_skip_votes() — per-track in-memory vote set keyed to (current_index, video_id), auto-resetting on track change (D-17)"
  - "SKIP_VOTE_TALLY / RADIO_START / RADIO_STOP / RADIO_LOOP_CONFLICT / RADIO_NOT_ARMED response pools"
  - "config.SKIP_VOTE_MAJORITY_RATIO knob (D-09)"
affects: [26-03-skip-vote-glue, 26-04-radio-command, 26-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure decision seam (logic/skip_vote.py) cloned from logic/radio.py's / logic/proactive.py's template: no discord/asyncio/random/datetime, keyword-only params, cheapest-gate-first ordering, config defaults as kwargs"
    - "Lazy per-track state reset: MusicQueue keys ephemeral vote state to a track-identity tuple and resets on READ when the key changes, rather than hooking every current_index mutation site (mirrors radio_played's session-scoped, in-memory-only discipline from 26-01)"
    - "D-18 templated-numbers-only UI copy: {votes}/{required} interpolated by code at the cog layer, never generated by Gemini — same accuracy-firewall discipline as SKIPS_RATE_ROASTS' {pct}"

key-files:
  created:
    - logic/skip_vote.py
    - tests/test_skip_vote_logic.py
  modified:
    - config.py
    - models/queue.py
    - personality/responses.py
    - tests/test_responses.py

key-decisions:
  - "Followed the planner's locked A1 arithmetic exactly: floor(listener_count * majority_ratio) + 1, clamped to listener_count, floored at 1 — reproduces D-09c's table (1->1, 2->2, 3->2, 4->3) at ratio=0.5 while actually honouring a non-default ratio, unlike the n // 2 + 1 sketch in 26-RESEARCH/26-PATTERNS"
  - "Counting uses len(new_votes), never an intersection with listener_ids — a departed voter's vote stays counted per D-17. Locked by a static source-grep test as well as the behavioral departed_voter test, since the buggy pattern is exactly the kind of 'obvious fix' a future edit would be tempted to make"
  - "Vote state lives on MusicQueue as a single-slot cache keyed to (current_index, video_id), reset lazily on read — not four separate reset hooks at _advance/skip/previous/jump_to. A revisited track's earlier votes are NOT resurrected (proven by the reset_on_track_change tests), which is the correct reading of D-17: a track CHANGE clears votes, and returning to an old index is still a change away from whatever was last cached"

requirements-completed: [DJ-02]

# Metrics
duration: 30min
completed: 2026-07-16
---

# Phase 26 Plan 02: Skip-Vote Decision + State Core Summary

**Pure `logic/skip_vote.py` DJ-02 verdict seam (config-honouring strict-majority arithmetic, requester bypass, D-17 departed-voter counting), per-track vote state on `MusicQueue`, and a D-18 templated tally + radio lifecycle response-pool set — all mock-free tested, zero Discord glue yet.**

## Performance

- **Duration:** ~30 min
- **Tasks:** 3 completed
- **Files modified:** 5 (2 created, 3 modified)

## Accomplishments

- `logic/skip_vote.py`: `SkipVerdict` enum + two pure keyword-only functions (`required_votes`, `decide_skip`) locking every DJ-02 branch — D-09c's majority table, the config-ratio knob, the T-26-08 unanimity clamp, D-13a requester bypass (checked first, even for an already-voted requester), D-13b's free bot-id-never-bypasses guarantee, D-14 idempotency, and D-17 departed-voter counting — 20 mock-free tests, zero Discord/asyncio/random/datetime imports
- `MusicQueue` gains `skip_votes_for_current()` / `record_skip_votes()`: a single-slot vote cache keyed to `(current_index, video_id)` that resets lazily on read whenever the key changes, making D-17's "votes reset on track change" structural with zero per-mutation-site hooks; `/replay` (same key) correctly preserves votes, `/previous`/`/jump_to`/`skip()`/`clear()` all correctly clear them; never persisted (confirmed `services/queue_persistence.py`'s explicit typed payload dict has zero `_skip_votes` references)
- Five new `personality/responses.py` pools (`SKIP_VOTE_TALLY`, `RADIO_START`, `RADIO_STOP`, `RADIO_LOOP_CONFLICT`, `RADIO_NOT_ARMED`), all lowercase, format-smoke-tested over every entry — the tally's `{votes}`/`{required}` are code-interpolated only, never a Gemini call (D-18)

## Task Commits

Each task was committed atomically:

1. **Task 1: Pure skip-vote seam — SkipVerdict, required_votes, decide_skip** - `ef1d3e0` (feat)
2. **Task 2: Per-track skip-vote state on MusicQueue (D-17 reset on track change)** - `168abcf` (feat)
3. **Task 3: D-18 templated tally pool + radio lifecycle copy** - `98467cd` (feat)

**Deviation fix (Rule 1 - style):** `870ff1f` (style — ruff auto-format on `tests/test_skip_vote_logic.py`, see Deviations below)

## Files Created/Modified

- `logic/skip_vote.py` - Pure DJ-02 verdict seam: `SkipVerdict` enum, `required_votes` (D-09c arithmetic), `decide_skip` (requester -> solo -> idempotent -> tally gate order)
- `tests/test_skip_vote_logic.py` - Mock-free tests: `TestRequiredVotesMajority`, `TestDecideSkipSolo`, `TestDecideSkipRequesterBypass`, `TestDecideSkipIdempotent`, `TestDecideSkipDepartedVoter` (Task 1), `TestSkipVotesResetOnTrackChange` (Task 2) — 28 tests total
- `config.py` - `SKIP_VOTE_MAJORITY_RATIO = 0.5` (D-09/D-09c), appended to the existing Phase 26 block (not a second block)
- `models/queue.py` - `_skip_votes`/`_skip_votes_key` fields, `skip_votes_for_current()`/`record_skip_votes()` methods
- `personality/responses.py` - `SKIP_VOTE_TALLY`, `RADIO_START`, `RADIO_STOP`, `RADIO_LOOP_CONFLICT`, `RADIO_NOT_ARMED` pools, appended after `DISCOVER_NO_HISTORY`
- `tests/test_responses.py` - `TestPhase26ResponsePools`: non-empty/str checks, `pick_random` membership, per-entry format-smoke tests, lowercase assertion, no-Gemini-import guard

## Decisions Made

- Followed the plan's `<planner_decisions>` (A1 arithmetic, gate order, `len(new_votes)` counting) exactly — no re-litigation
- Rephrased two in-code comments/docstring lines that originally quoted the literal buggy pattern `new_votes & listener_ids` as illustrative prose — the plan's own acceptance criteria (`grep -c 'new_votes & listener_ids' logic/skip_vote.py` returns 0) and my own static-guard test would otherwise have failed against my own explanatory comments; reworded to describe the same warning without using the literal banned substring
- `skip_votes_for_current()`/`record_skip_votes()` implemented as a single-slot cache (one active key + one vote set) rather than a dict of all-tracks-ever-seen vote sets — matches the plan's exact spec ("if the computed key differs... reset") and keeps memory bounded to the current track only, consistent with 26-01's "session lifetime is the bound" discipline for `radio_played`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Style] ruff format reformatted tests/test_skip_vote_logic.py**
- **Found during:** Plan-level full-suite verification (after all 3 tasks committed)
- **Issue:** `python -m ruff format --check .` flagged `tests/test_skip_vote_logic.py` as needing reformatting (a missing blank line between two class blocks) — introduced by my own Task 2 edit, not caught by Task 2's narrower `ruff format --check models/queue.py` verification command.
- **Fix:** Ran `python -m ruff format tests/test_skip_vote_logic.py`; re-ran the full relevant test subset to confirm no behavior change.
- **Files modified:** `tests/test_skip_vote_logic.py`
- **Verification:** `pytest tests/test_skip_vote_logic.py tests/test_responses.py tests/test_queue.py tests/test_radio_logic.py -q` green (121 passed); full suite subsequently green (1118 passed / 129 skipped / 0 failed)
- **Committed in:** `870ff1f`

---

**Total deviations:** 1 auto-fixed (1 style, non-functional)
**Impact on plan:** Purely cosmetic (whitespace) — no logic, test, or acceptance-criteria change. No scope creep.

## Issues Encountered

One test-writing bug of my own, caught before commit: an early draft of `test_reset_on_track_change_same_video_id_different_indices_independent` asserted that jumping back to index 0 after visiting index 2 (same `video_id`) would restore index 0's earlier vote set. That is not what the single-slot-cache design does or what D-17 requires — the cache had already moved on to index 2's key, so index 0's votes are gone, not resurrected (same "no resurrection" rule already proven by the `previous()` test). Fixed the assertion to match the actual (correct) behavior before the first task-verification run passed.

## Next Phase Readiness

- `logic/skip_vote.py`, `MusicQueue` vote state, and the response pools are all in place and locked by tests — plan 26-03 can wire this directly into the one shared skip choke point (`/skip` slash command + the `⏭ Skip` button) with no further pure-logic work needed
- The static guard against the `& listener_ids` regression (both a source-grep test and a behavioral departed-voter test) should make a future accidental reintroduction of that bug fail loudly
- No blockers; full suite green (1118 passed / 129 skipped / 0 failed) at HEAD

---
*Phase: 26-radio-mode-skip-democracy*
*Completed: 2026-07-16*

## Self-Check: PASSED

- FOUND: logic/skip_vote.py
- FOUND: tests/test_skip_vote_logic.py
- FOUND: .planning/phases/26-radio-mode-skip-democracy/26-02-SUMMARY.md
- FOUND: ef1d3e0 (Task 1 commit)
- FOUND: 168abcf (Task 2 commit)
- FOUND: 98467cd (Task 3 commit)
- FOUND: 870ff1f (deviation fix commit)
