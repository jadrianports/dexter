---
phase: 11-rag-long-term-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - config.py
  - database.py
  - bot.py
  - CLAUDE.md
  - .planning/PROJECT.md
  - tests/test_memory.py
  - tests/test_database_phase11.py
autonomous: false
requirements: [MEM-01]
user_setup: []
must_haves:
  truths:
    - "Bot boots clean against Neon with the pgvector codec registered on every pooled connection (no 'unknown type: public.vector' ValueError in boot logs)"
    - "After init_db, a user_memories table with a vector(768) embedding column and idx_user_memories_user index exists"
    - "config exposes gemini-embedding-001 @ 768d and a separate EMBED_RPM_LIMIT distinct from the 15 RPM chat budget"
    - "No source or doc file still names the deprecated text-embedding-004 model"
  artifacts:
    - path: "database.py"
      provides: "CREATE EXTENSION vector + user_memories DDL at top of SCHEMA_SQL"
      contains: "CREATE EXTENSION IF NOT EXISTS vector"
    - path: "config.py"
      provides: "Phase 11 embedding + retrieval constants"
      contains: "EMBEDDING_MODEL"
    - path: "tests/test_memory.py"
      provides: "Wave-0 pure-logic test scaffold (collects clean before models/memory.py exists)"
    - path: "requirements.txt"
      provides: "pgvector pip dependency"
      contains: "pgvector"
  key_links:
    - from: "bot.py:_initialize_once"
      to: "pgvector.asyncpg.register_vector"
      via: "create_pool(init=_register_vector) after CREATE EXTENSION on a throwaway connection"
      pattern: "init=_register_vector"
    - from: "database.py:SCHEMA_SQL"
      to: "user_memories"
      via: "idempotent plain DDL applied by init_db"
      pattern: "CREATE TABLE IF NOT EXISTS user_memories"
---

<objective>
Lay the durable-memory foundation: enable `pgvector` on the existing Neon Postgres, add the
`user_memories` table (with a `vector(768)` embedding column) to `SCHEMA_SQL`, and make the boot
path codec-safe (extension-first, then `create_pool(init=_register_vector)`). Add the Phase 11
config constants (separate embedding model + RPM limit + the MEDIUM-confidence numeric retrieval
priors). Correct the deprecated `text-embedding-004` references to `gemini-embedding-001` @ 768d.
Stand up the Wave-0 test scaffold so every later plan's `pytest tests/test_memory.py` command
resolves.

Purpose: every other Phase 11 plan depends on the table, the codec, and the constants existing.
This is MEM-01 and the boot-ordering trap (Pitfall 1) — the one genuine integration risk.

Output: a bootable bot with an empty `user_memories` store, registered vector codec, Phase 11
config block, corrected model references, and a collectable `tests/test_memory.py`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-rag-long-term-memory/11-CONTEXT.md
@.planning/phases/11-rag-long-term-memory/11-RESEARCH.md
@.planning/phases/11-rag-long-term-memory/11-PATTERNS.md
@database.py
@bot.py
@config.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: pgvector package legitimacy gate (T-11-SC)</name>
  <files>requirements.txt</files>
  <read_first>
    - .planning/phases/11-rag-long-term-memory/11-RESEARCH.md (§ "Package Legitimacy Audit" — pgvector tagged [ASSUMED]; slopcheck unavailable in this env)
    - requirements.txt (current dependency list — pgvector is the ONLY new dep)
  </read_first>
  <action>Present the pgvector legitimacy evidence (PyPI listing, version history, source repo) and
    PAUSE for human approval. Do NOT run pip install or modify requirements.txt until the human
    approves — this blocking-human gate is never auto-approved.</action>
  <what-built>Nothing yet — this gate runs BEFORE any install. RESEARCH tagged the new
    `pgvector` Python package `[ASSUMED]` (slopcheck could not run); per the package-legitimacy
    protocol it must be human-verified before install.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/pgvector/ — confirm: maintained (latest ~0.4.x), version
       history back to ~0.1.0 (~3yr), source repo github.com/pgvector/pgvector-python, and that it
       is the canonical asyncpg binding referenced by official pgvector docs.
    2. Confirm the pin to add is `pgvector>=0.3.6,<0.5` (research-recommended range; 0.4.2 current).
    3. Confirm this is the ONLY new pip dependency (asyncpg / google-genai already present, no bump).
  </how-to-verify>
  <verify>
    <human-check>Human confirms pgvector on PyPI is the legitimate canonical binding and approves the pin before Task 2 installs it.</human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms pgvector on PyPI is the legitimate canonical binding (not a typosquat/slop)
    - Human approves the `pgvector>=0.3.6,<0.5` pin before Task 2 installs it
  </acceptance_criteria>
  <done>Human approved the pgvector dependency; install may proceed in Task 2.</done>
  <resume-signal>Type "approved" to proceed with the install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add pgvector dep, Phase 11 config constants, correct stale model references</name>
  <files>requirements.txt, config.py, CLAUDE.md, .planning/PROJECT.md</files>
  <read_first>
    - config.py (the existing K-04 Neon pool tuning block lines ~102-104, and the Phase 9 block ~142-150 — match the section-comment style and placement at end of file)
    - requirements.txt (append style — one dep per line)
    - CLAUDE.md (search for "text-embedding-004" — appears in the Tech Stack / embedding notes)
    - .planning/PROJECT.md (search for "text-embedding-004")
    - .planning/phases/11-rag-long-term-memory/11-RESEARCH.md (§ "Standard Stack", § "Validation Architecture" for the prior values; § "State of the Art" for the deprecation)
  </read_first>
  <action>
    Add `pgvector>=0.3.6,<0.5` to requirements.txt (the only new dependency) and install it into the
    active environment. Append a `# --- Phase 11: RAG Long-Term Memory ---` block to config.py with
    these module constants (MEDIUM-confidence priors — annotate that 11-02 spike validates/tunes them):
    `EMBEDDING_MODEL = "gemini-embedding-001"`, `EMBED_DIM = 768`, `EMBED_RPM_LIMIT = 60` (separate
    embedding quota — NEVER the shared 15 RPM `GEMINI_RPM_LIMIT`, per Critical Rule 1 + A2),
    `MEMORY_TOP_K = 8`, `MEMORY_SIMILARITY_FLOOR = 0.70`, `MEMORY_DEDUP_THRESHOLD = 0.90`,
    `MEMORY_INJECT_CAP = 3`, `MEMORY_MAX_PER_USER = 150`, `MEMORY_DECAY_DAYS = 90`,
    `MEMORY_RERANK_RELEVANCE_WEIGHT = 1.0`, `MEMORY_RERANK_RECENCY_WEIGHT = 0.5`,
    `MEMORY_RERANK_SALIENCE_WEIGHT = 0.7`, `MEMORY_RERANK_NOVELTY_WEIGHT = 0.5`,
    `MEMORY_CALLBACK_CHANCE = 0.35` (D-04 occasional-payoff cadence; 11-02 tunes), and
    `MEMORY_DISTILL_BATCH_HOUR = 3` (daily distill-batch hour). Each numeric constant gets a trailing
    comment `# prior — validate via 11-02 spike`. Correct every `text-embedding-004` reference in
    CLAUDE.md and .planning/PROJECT.md to `gemini-embedding-001` @ 768d (it was deprecated/sunset
    2026-01-14 — see STATE.md blocker). Do NOT add salience base-weights here (11-04 owns those).
  </action>
  <verify>
    <automated>python -c "import config; assert config.EMBEDDING_MODEL=='gemini-embedding-001'; assert config.EMBED_DIM==768; assert config.EMBED_RPM_LIMIT!=config.GEMINI_RPM_LIMIT; assert 0<config.MEMORY_SIMILARITY_FLOOR<1; assert config.MEMORY_INJECT_CAP<=3; print('config ok')"</automated>
    <automated>python -c "import sys; bad=[f for f in ('CLAUDE.md','.planning/PROJECT.md') if 'text-embedding-004' in open(f,encoding='utf-8').read()]; sys.exit(1 if bad else 0)"</automated>
    <automated>python -c "import pgvector.asyncpg; print('pgvector import ok')"</automated>
  </verify>
  <done>config imports with all Phase 11 constants present; EMBED_RPM_LIMIT differs from the chat
    limit; no `text-embedding-004` remains in CLAUDE.md or PROJECT.md; pgvector installed + importable.</done>
</task>

<task type="auto">
  <name>Task 3: user_memories schema, extension-first boot ordering, Wave-0 test scaffold</name>
  <files>database.py, bot.py, tests/test_memory.py, tests/test_database_phase11.py</files>
  <read_first>
    - database.py (SCHEMA_SQL lines 67-162 — plain-DDL-only rule, no $N params; init_db lines 165-171)
    - bot.py (_initialize_once lines 333-351 — the create_pool call + init_db; the K-04 ssl/statement_cache_size/max_inactive flags; config.sanitize_database_url usage)
    - .planning/phases/11-rag-long-term-memory/11-PATTERNS.md (§ "database.py (MODIFY)" boot-ordering pattern lines 254-282; § proposed schema)
    - .planning/phases/11-rag-long-term-memory/11-RESEARCH.md (Pattern 3 lines 251-281; Pitfall 1 + Pitfall 2)
    - tests/test_database_phase4.py (the opt-in live-DB integration test convention — skip without DATABASE_URL)
  </read_first>
  <action>
    In database.py SCHEMA_SQL, add at the TOP (before the first CREATE TABLE) `CREATE EXTENSION IF NOT
    EXISTS vector;` then a `CREATE TABLE IF NOT EXISTS user_memories` with columns: `id BIGSERIAL
    PRIMARY KEY, user_id TEXT NOT NULL, guild_id TEXT, kind TEXT, fact TEXT NOT NULL, embedding
    vector(768) NOT NULL, salience REAL DEFAULT 0, hit_count INTEGER DEFAULT 1, created_at TIMESTAMPTZ
    DEFAULT now(), last_seen_at TIMESTAMPTZ DEFAULT now(), last_surfaced_at TIMESTAMPTZ (D-05
    anti-repeat), surface_count INTEGER DEFAULT 0, expires_at TIMESTAMPTZ` and
    `CREATE INDEX IF NOT EXISTS idx_user_memories_user ON user_memories(user_id, created_at DESC);`.
    Keep it plain DDL — no $N params (asyncpg multi-statement constraint). Do NOT add an HNSW/IVFFlat
    index (seq scan day-one per RESEARCH). In bot.py `_initialize_once`, BEFORE the existing
    `asyncpg.create_pool(...)` call, open a throwaway `await asyncpg.connect(dsn=config.sanitize_database_url(config.DATABASE_URL), ssl="require", statement_cache_size=0)`,
    run `CREATE EXTENSION IF NOT EXISTS vector;`, then close it in a finally. Define
    `async def _register_vector(conn): await register_vector(conn)` (import `from pgvector.asyncpg
    import register_vector`) and pass `init=_register_vector` to the existing create_pool call —
    keeping ALL existing K-04 kwargs (ssl='require', statement_cache_size, max_inactive_connection_lifetime,
    command_timeout) UNCHANGED. `statement_cache_size=0` is verified-compatible (codec is a
    per-connection set_type_codec, not a prepared statement — Pitfall 2 misbelief). Create
    tests/test_memory.py as a Wave-0 scaffold: module docstring noting it will hold pure-logic tests
    for models/memory.py (added in 11-03/04/05/07), plus one trivial passing test so collection
    succeeds before models/memory.py exists. Create tests/test_database_phase11.py as an opt-in
    live-DB integration skeleton (skip when DATABASE_URL is the local default, mirroring
    test_database_phase4.py) with a placeholder for insert/search round-trips wired in 11-03/11-04.
  </action>
  <verify>
    <automated>python -c "import database; s=database.SCHEMA_SQL; assert 'CREATE EXTENSION IF NOT EXISTS vector' in s; assert 'CREATE TABLE IF NOT EXISTS user_memories' in s; assert 'vector(768)' in s; assert 'last_surfaced_at' in s; assert 'expires_at' in s; print('schema ok')"</automated>
    <automated>python -c "src=open('bot.py',encoding='utf-8').read(); assert 'register_vector' in src; assert 'CREATE EXTENSION IF NOT EXISTS vector' in src; assert 'init=_register_vector' in src; print('boot ordering present')"</automated>
    <automated>python -m pytest tests/test_memory.py -q</automated>
  </verify>
  <done>SCHEMA_SQL contains the vector extension + user_memories(vector(768)) + index; bot.py runs
    CREATE EXTENSION on a throwaway connection then create_pool(init=_register_vector) with K-04 flags
    intact; tests/test_memory.py collects and passes; tests/test_database_phase11.py exists and skips
    cleanly without a live DB.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Created across Phase 11 (the plan-review source-grounding pass should treat these as NEW, not drift):
- Classes: `MemoryService` (services/memory.py), `MemoryFact` (models/memory.py)
- Service methods: `GeminiService.embed`, `MemoryService.recall/remember/distill/distill_and_remember/sweep`; `GeminiService._embed_limiter`
- Pure fns (models/memory.py): `rerank`, `recency_score`, `novelty_score`, `apply_floor`, `dedup_decision`, `compute_salience`, `choose_eviction`, `is_sensitive`, `contains_number`, `decay_predicate`
- DB helpers (database.py): `insert_memory`, `search_memories`, `bump_memory_hit`, `bump_surfaced`, `count_user_memories`, `evict_lowest_salience`, `delete_expired_memories`
- Boot/tasks (bot.py): `_register_vector`, `memory_distill_batch`, `memory_sweep`
- Table: `user_memories` (+ `idx_user_memories_user`); columns id/user_id/guild_id/kind/fact/embedding vector(768)/salience/hit_count/created_at/last_seen_at/last_surfaced_at/surface_count/expires_at
- Config keys: `EMBEDDING_MODEL`, `EMBED_DIM`, `EMBED_RPM_LIMIT`, `MEMORY_TOP_K`, `MEMORY_SIMILARITY_FLOOR`, `MEMORY_DEDUP_THRESHOLD`, `MEMORY_INJECT_CAP`, `MEMORY_MAX_PER_USER`, `MEMORY_DECAY_DAYS`, `MEMORY_RERANK_*_WEIGHT`, `MEMORY_CALLBACK_CHANCE`, `MEMORY_DISTILL_BATCH_HOUR`, `MEMORY_SALIENCE_BASE_WEIGHTS`
- Prompt: `DISTILL_PROMPT` (personality/prompts.py); `build_chat_prompt(memories=...)` kwarg + `{memory_context}` slot
- Files: services/memory.py, models/memory.py, scripts/memory_spike.py, tests/test_memory.py, tests/test_database_phase11.py
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip registry → build | New `pgvector` dependency enters the supply chain |
| bot process → Neon Postgres | DDL + DSN cross here at boot (SSL, never-logged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Denial of Service | boot ordering (`register_vector` on pool warm) | mitigate | extension-first: `CREATE EXTENSION` on a throwaway connect() BEFORE create_pool(init=...), so no pooled connection raises "unknown type: public.vector" (Pitfall 1) |
| T-11-02 | Information Disclosure | DSN/key in boot logs | mitigate | reuse `config.sanitize_database_url`; never log the pool object or DSN (existing T-04-05 discipline) — unchanged |
| T-11-SC | Tampering | pgvector pip install (supply chain) | mitigate | Task 1 blocking-human legitimacy gate verifies pypi.org/project/pgvector before install; pin `>=0.3.6,<0.5`; never auto-approved |
</threat_model>

<verification>
- `python -c "import bot"` import smoke succeeds (boot wiring parses, imports resolve).
- Manual clean-boot check (no DB in CI): on the user's PC with the real `.env`, start the bot
  against Neon and confirm `dexter.log` shows the schema init with NO `ValueError: unknown type:
  public.vector` and NO SSL/`channel_binding` crash. This is the MEM-01 boot gate (see 11-VALIDATION
  Manual-Only).
- Full suite stays green: `python -m pytest tests/ -q`.
</verification>

<success_criteria>
- `user_memories` (vector(768)) + index exist after `init_db`; `CREATE EXTENSION` runs before pool creation.
- Phase 11 config constants present; embedding limit is separate from the 15 RPM chat budget.
- No `text-embedding-004` reference remains in CLAUDE.md or PROJECT.md.
- `tests/test_memory.py` collects and passes; full suite green; bot imports clean.
</success_criteria>

<output>
Create `.planning/phases/11-rag-long-term-memory/11-01-SUMMARY.md` when done.
</output>
