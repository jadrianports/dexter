---
phase: 11-rag-long-term-memory
plan: 06
type: execute
wave: 6
depends_on: ["11-05"]
files_modified:
  - personality/prompts.py
  - cogs/ai.py
  - cogs/events.py
  - cogs/music.py
  - tests/test_prompts.py
autonomous: true
requirements: [MEM-06]
user_setup: []
must_haves:
  truths:
    - "A callback roast can pair a live-SQL stat with a recalled pgvector episode (the stat x episode payoff) — the number comes from SQL, the episode from memory"
    - "build_chat_prompt(memories=None) renders byte-identical to today's prompt; memories=[...] adds an accuracy-safe candidate-ammo sub-block that pins all numbers to SQL"
    - "Recalled memories are optional candidate ammo the model may NOOP (D-06); they surface as an occasional payoff, not on every roast (D-04)"
    - "All four existing build_chat_prompt callers compile unchanged"
  artifacts:
    - path: "personality/prompts.py"
      provides: "build_chat_prompt(memories=...) kwarg + {memory_context} slot in DEXTER_SYSTEM_PROMPT"
      contains: "memory_context"
    - path: "cogs/ai.py"
      provides: "recall wired into /ask and /roast"
      contains: "recall("
  key_links:
    - from: "cogs/ai.py + cogs/events.py + cogs/music.py roast surfaces"
      to: "services/memory.py:recall"
      via: "occasional cadence roll then recall, passed as build_chat_prompt(memories=...)"
      pattern: "memories="
    - from: "personality/prompts.py:build_chat_prompt"
      to: "DEXTER_SYSTEM_PROMPT {memory_context}"
      via: "optional sub-block; empty string when no memories (byte-identical)"
      pattern: "memory_context"
---

<objective>
Land the capstone: inject recalled memories into the personality prompt as optional, backward-compatible
"candidate ammo," and wire `recall()` into the roast surfaces (`/ask`, `/roast`, ambient roasts, music
notable-event roasts) so Dexter can deliver the signature stat x episode callback — a true live-SQL
number paired with a recalled pgvector episode. Memories are candidate ammo the model may NOOP (D-06);
all hard numbers still come from live SQL; callbacks surface as an occasional payoff (D-04).

Purpose: MEM-06 — the reason the phase exists. Retrieval quality is judged by whether this callback
lands.

Output: `build_chat_prompt(memories=...)` (empty/None renders byte-identical), and recall wired at the
four roast surfaces behind an occasional cadence gate.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-rag-long-term-memory/11-CONTEXT.md
@.planning/phases/11-rag-long-term-memory/11-RESEARCH.md
@.planning/phases/11-rag-long-term-memory/11-PATTERNS.md
@personality/prompts.py
@cogs/ai.py
@cogs/events.py
@cogs/music.py
@tests/test_prompts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_chat_prompt(memories=...) + accuracy-safe {memory_context} slot</name>
  <files>personality/prompts.py, tests/test_prompts.py</files>
  <read_first>
    - personality/prompts.py (DEXTER_SYSTEM_PROMPT lines 3-60 — the USER CONTEXT block at 57-58 and trailing {seasonal_context} at 60; build_chat_prompt lines 91-104 — .rstrip(), seasonal="" empty fallback)
    - .planning/phases/11-rag-long-term-memory/11-PATTERNS.md (personality/prompts.py backward-compatible kwarg pattern lines 356-411 — memory_context slot placement, empty-string byte-identity, 4 callers compile)
    - .planning/phases/11-rag-long-term-memory/11-RESEARCH.md (accuracy-safe prompt-injection site lines 287-310 — the candidate-ammo sub-block wording, numbers-from-USER-CONTEXT instruction, D-06)
    - tests/test_prompts.py (existing test style incl. test_empty_seasonal_no_artifact, the placeholder-count test — extend it)
  </read_first>
  <behavior>
    - build_chat_prompt(mood, user_summary, seasonal) with NO memories arg returns a string byte-identical to the pre-change output (regression: capture today's output for fixed inputs and assert equality)
    - build_chat_prompt(..., memories=None) is byte-identical to omitting the arg
    - build_chat_prompt(..., memories=["swore he was done with the killers"]) includes the fact AND an instruction that numbers/counts come from USER CONTEXT, never from memories; and a "use at most one, only if it lands, do not invent" instruction (D-06)
    - all four existing callers (/ask, /roast, ambient, music) still compile (memories defaults to None)
  </behavior>
  <action>
    Add `memories: list[str] | None = None` as the LAST param of build_chat_prompt. Add a
    `{memory_context}` slot to DEXTER_SYSTEM_PROMPT AFTER `{user_context}` (and before
    `{seasonal_context}`), matching the existing blank-line spacing so an empty slot leaves no
    triple-newline artifact (mirror test_empty_seasonal_no_artifact). In the builder, set
    `memory_context = ""` when memories is falsy (byte-identical fallback), else build the candidate-ammo
    sub-block: a header naming these as episodes/opinions (not stats), the bulleted facts, and the
    instruction "use at most one of these, only if it genuinely lands; do not invent details beyond
    these lines; all numbers/counts come from USER CONTEXT above, never from these memories" (D-06 +
    accuracy firewall). Pass `memory_context=memory_context` to the existing `.format(...)` call; keep
    the trailing `.rstrip()`. Extend tests/test_prompts.py: test_memories_none_byte_identical (compare
    memories=None vs the same call without the arg AND vs a frozen expected string for fixed inputs) and
    test_memory_block_rendered (fact present + numbers-from-SQL instruction present).
  </action>
  <verify>
    <automated>python -m pytest tests/test_prompts.py -x -q</automated>
    <automated>python -c "from personality.prompts import build_chat_prompt as b; a=b('normal','top: drake','It is December.'); c=b('normal','top: drake','It is December.', memories=None); assert a==c; d=b('normal','top: drake','', memories=['swore he was done with the killers']); assert 'killers' in d and ('USER CONTEXT' in d) and ('never' in d.lower()); print('prompt kwarg ok')"</automated>
  </verify>
  <done>memories=None is byte-identical to today; memories=[...] renders the candidate-ammo sub-block
    with the numbers-from-SQL instruction; all four callers compile; test_prompts.py extended + green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire recall into /ask and /roast (occasional cadence)</name>
  <files>cogs/ai.py</files>
  <read_first>
    - cogs/ai.py (/ask lines 103-145 — defer, user_summary, build_chat_prompt at 120; /roast lines 152-216 — build_chat_prompt at 180, the priority-1 chat call)
    - .planning/phases/11-rag-long-term-memory/11-PATTERNS.md (recall wiring pattern lines 419-446 — recall after defer, getattr guard, pass memories=... ; graceful degrade)
    - .planning/phases/11-rag-long-term-memory/11-CONTEXT.md (D-04 occasional payoff; D-06 candidate ammo / numbers from SQL)
    - config.py (MEMORY_CALLBACK_CHANCE)
  </read_first>
  <action>
    In /ask and /roast, after defer() and after user_summary is fetched, add an occasional-cadence gate:
    `import random` roll `random.random() < config.MEMORY_CALLBACK_CHANCE` (D-04 — only attempt a
    callback occasionally, so it saves embed calls and stays rare). Only when the roll passes, get
    `memory_service = getattr(self.bot,"memory_service",None)` and, if present, `memories = await
    memory_service.recall(str(target_or_user.id), str(interaction.guild_id), <the query/scenario text>)`
    inside try/except (non-fatal -> memories=[]). For /ask use the question text as the recall anchor and
    the invoker id; for /roast use the scenario text and the TARGET id (recall the person being roasted).
    Pass `memories=memories or None` to build_chat_prompt. The hard numbers stay in user_summary/USER
    CONTEXT (live SQL) — do not derive any number from memories (D-06). recall already runs off defer
    (no 3s impact).
  </action>
  <verify>
    <automated>python -c "import inspect, cogs.ai as a; src=inspect.getsource(a.AICog); assert 'recall(' in src and 'memories=' in src and 'MEMORY_CALLBACK_CHANCE' in src and 'getattr' in src; print('ai recall wired')"</automated>
    <automated>python -c "import cogs.ai"</automated>
  </verify>
  <done>/ask and /roast attempt recall only on the cadence roll, guard for an absent memory_service,
    pass memories to build_chat_prompt, recall the correct id (invoker for /ask, target for /roast), and
    keep all numbers sourced from SQL.</done>
</task>

<task type="auto">
  <name>Task 3: Wire recall into ambient roasts and music notable-event roasts</name>
  <files>cogs/events.py, cogs/music.py</files>
  <read_first>
    - cogs/events.py (_generate_ambient_roast lines 87-156 — build_chat_prompt at 126; the scenario string after {name} substitution is the recall anchor)
    - cogs/music.py (_build_roast_line lines 1073-1134 — build_chat_prompt at 1108; scenario_content is the recall anchor; user_id available)
    - .planning/phases/11-rag-long-term-memory/11-PATTERNS.md (recall into _generate_ambient_roast line 454; recall into _build_roast_line line 494; getattr guard)
    - config.py (MEMORY_CALLBACK_CHANCE)
  </read_first>
  <action>
    In _generate_ambient_roast (cogs/events.py) and _build_roast_line (cogs/music.py), before the
    build_chat_prompt call, add the same occasional-cadence gate (`random.random() <
    config.MEMORY_CALLBACK_CHANCE`) then a guarded `memories = await memory_service.recall(user_id,
    guild_id, <scenario text>)` in try/except (non-fatal -> []), and pass `memories=memories or None`
    to build_chat_prompt. Use the member/user id whose roast is being generated and the substituted
    scenario string as the recall anchor. These are already priority-2 background paths off any
    interaction, so the extra embed is safe. Keep numbers sourced from the existing user_summary /
    top-artist SQL (D-06).
  </action>
  <verify>
    <automated>python -c "import inspect, cogs.events as e; s=inspect.getsource(e.EventsCog._generate_ambient_roast); assert 'recall(' in s and 'memories=' in s and 'MEMORY_CALLBACK_CHANCE' in s; print('ambient recall wired')"</automated>
    <automated>python -c "import inspect, cogs.music as m; s=inspect.getsource(m.MusicCog._build_roast_line); assert 'recall(' in s and 'memories=' in s; print('music recall wired')"</automated>
    <automated>python -c "import cogs.events, cogs.music"</automated>
  </verify>
  <done>ambient + music roasts attempt recall on the cadence roll, guard for absent memory_service, pass
    memories to build_chat_prompt, and keep numbers from SQL — enabling the stat x episode callback.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan adds: the `memories=` kwarg + `{memory_context}` slot to build_chat_prompt
(personality/prompts.py) and recall wiring into cogs/ai.py, cogs/events.py, cogs/music.py. No new
symbols beyond those declared in 11-01-PLAN.md § "Artifacts this phase produces".
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recalled memory → system prompt | stored episode text is injected into the roast prompt |
| roast output → channel | model output reaches users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06a | Tampering | memory text treated as an instruction (prompt injection via stored fact) | mitigate | candidate-ammo framing: "do not invent beyond these lines; numbers come from USER CONTEXT, never memories"; the model may NOOP (D-06); distiller already constrained facts to atomic third-person (11-05) |
| T-11-06b | Tampering (accuracy) | a number is taken from memory instead of SQL | mitigate | facts are number-free (11-05 firewall) + the prompt instruction pins counts to USER CONTEXT (live SQL); Critical Rule 5 preserved |
| T-11-06c | Information Disclosure | recall returns another user's memory into a roast | mitigate | recall passes a single scoped id and search_memories filters WHERE user_id (11-03 V4) |
| T-11-06d | Spoofing / regression | empty-memory path changes today's prompt | mitigate | memories=None renders byte-identical (regression test); all 4 callers compile unchanged |
</threat_model>

<verification>
- test_prompts.py byte-identity + rendering tests pass: `python -m pytest tests/test_prompts.py -x -q`.
- Static assert: recall wired with cadence gate + getattr guard at all four surfaces (automated).
- Manual (live, on the user's PC): trigger a roast for a user with stored memories and confirm an
  occasional callback that pairs a SQL stat with a recalled episode, and that empty-memory users see
  no change (11-VALIDATION Manual-Only).
- Full suite green: `python -m pytest tests/ -q`.
</verification>

<success_criteria>
- The stat x episode callback can land; numbers come from SQL, the episode from memory.
- memories=None is byte-identical to today; memories=[...] is accuracy-safe candidate ammo.
- Callbacks are occasional (D-04) and NOOP-able (D-06); recall is per-user scoped.
</success_criteria>

<output>
Create `.planning/phases/11-rag-long-term-memory/11-06-SUMMARY.md` when done.
</output>
