---
phase: 11-rag-long-term-memory
plan: 07
type: execute
wave: 6
depends_on: ["11-05"]
files_modified:
  - models/memory.py
  - database.py
  - services/memory.py
  - bot.py
  - tests/test_memory.py
autonomous: true
requirements: [MEM-07]
user_setup: []
must_haves:
  truths:
    - "A daily decay sweep deletes expired low-salience memories so the store stays bounded over time (MEM-07)"
    - "The sweep is a daily @tasks.loop mirroring cache_cleanup/ytdlp_update (before_loop wait_until_ready + .error + start guard + cleanup-list)"
    - "The expiry predicate is a deterministic, unit-tested pure function ranking on salience + age"
  artifacts:
    - path: "models/memory.py"
      provides: "pure decay_predicate (is_expired) selecting expired low-salience facts"
      contains: "def decay_predicate"
    - path: "database.py"
      provides: "delete_expired_memories sweep helper"
      contains: "DELETE FROM user_memories"
    - path: "services/memory.py"
      provides: "MemoryService.sweep()"
      contains: "async def sweep"
    - path: "bot.py"
      provides: "memory_sweep daily @tasks.loop"
      contains: "memory_sweep"
  key_links:
    - from: "bot.py:memory_sweep"
      to: "services/memory.py:sweep"
      via: "daily @tasks.loop calls MemoryService.sweep()"
      pattern: "memory_service.sweep()"
    - from: "services/memory.py:sweep"
      to: "database.py:delete_expired_memories"
      via: "delete rows past expires_at (decay horizon)"
      pattern: "delete_expired_memories"
---

<objective>
Close the loop with hygiene: a daily decay sweep that deletes expired low-salience memories so the
store stays bounded over time. Add the pure expiry predicate, the scoped delete helper, the
`MemoryService.sweep()` orchestration, and the `memory_sweep` daily background task (mirroring
`cache_cleanup` / `ytdlp_update`). Per-user cap eviction already lands at write time in 11-04; this is
the time-based decay backstop (D-08 — low salience ages out first).

Purpose: MEM-07 — memory hygiene ships in v1.2 (per-user cap in 11-04 + this decay/expiry sweep).
Unbounded growth is the #1 companion-bot failure mode (Pitfall 13/14).

Output: `sweep()` + `delete_expired_memories` + `decay_predicate` + the `memory_sweep` daily loop.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-rag-long-term-memory/11-CONTEXT.md
@.planning/phases/11-rag-long-term-memory/11-RESEARCH.md
@.planning/phases/11-rag-long-term-memory/11-PATTERNS.md
@models/memory.py
@database.py
@services/memory.py
@bot.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure decay_predicate (expiry selection on salience + age)</name>
  <files>models/memory.py, tests/test_memory.py</files>
  <read_first>
    - models/memory.py (pure-logic conventions; MemoryFact; recency_score from 11-03)
    - .planning/phases/11-rag-long-term-memory/11-CONTEXT.md (D-08 — decay sweep + cap eviction rank on salience, low salience ages out first; MEM-07 per-user cap ~150 + decay/expiry sweep)
    - .planning/phases/11-rag-long-term-memory/11-RESEARCH.md (Pitfall 13/14 unbounded growth; § Validation test map test_decay_predicate)
    - config.py (MEMORY_DECAY_DAYS — the decay window; salience constants)
  </read_first>
  <behavior>
    - decay_predicate(fact, now, decay_days) returns True when a fact is BOTH past its decay horizon (age beyond decay_days, or now beyond expires_at) AND low-salience; a high-salience fact past the window is retained; a recent low-salience fact is retained
    - the function is pure, clock-injected (now passed in), deterministic on synthetic MemoryFact inputs
  </behavior>
  <action>
    Add `decay_predicate(fact, now, decay_days, salience_floor=...)` (and/or an `is_expired` helper) to
    models/memory.py per the behavior — select facts that are both aged past the decay window and below
    a low-salience threshold so high-salience episodes survive (D-08). Keep it pure and clock-injectable.
    Write tests first (RED): assert an old low-salience fact is selected, an old high-salience fact is
    retained, and a recent low-salience fact is retained.
  </action>
  <verify>
    <automated>python -m pytest tests/test_memory.py -x -q -k "decay or expire"</automated>
    <automated>python -c "from models.memory import decay_predicate; print('decay_predicate exported')"</automated>
  </verify>
  <done>decay_predicate selects expired low-salience facts and retains high-salience/recent ones; pure +
    clock-injectable; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: delete_expired_memories helper + MemoryService.sweep()</name>
  <files>database.py, services/memory.py, tests/test_database_phase11.py</files>
  <read_first>
    - database.py (the memory helpers from 11-03/11-04 — async with pool.acquire(), $N params; resolution_cache expiry style for a time-based DELETE WHERE expires_at)
    - services/memory.py (recall/remember; constructor; graceful-degrade style)
    - .planning/phases/11-rag-long-term-memory/11-PATTERNS.md (memory_sweep template lines 320-331 — sweep() called by the loop)
    - config.py (MEMORY_DECAY_DAYS)
    - tests/test_database_phase11.py (extend the skipped integration skeleton)
  </read_first>
  <action>
    Add `delete_expired_memories(pool, *, now)` to database.py: a single DELETE removing rows whose
    decay condition holds — `DELETE FROM user_memories WHERE expires_at IS NOT NULL AND expires_at < $1`
    (and/or a low-salience clause consistent with decay_predicate) — $N parameterized, returns the
    deleted count. Add `async def sweep(self) -> int` to MemoryService that calls
    `database.delete_expired_memories(self._pool, now=datetime.now(timezone.utc))`, logs the count, and
    swallows/returns 0 on error (never raises into the loop). Extend tests/test_database_phase11.py
    (skipped without a live DB) with an insert-expired -> sweep -> count-zero round-trip.
  </action>
  <verify>
    <automated>python -c "import database, inspect; assert hasattr(database,'delete_expired_memories'); assert 'DELETE FROM user_memories' in inspect.getsource(database.delete_expired_memories) and 'expires_at' in inspect.getsource(database.delete_expired_memories); import services.memory as m; assert hasattr(m.MemoryService,'sweep'); print('sweep wired')"</automated>
    <automated>python -m pytest tests/test_database_phase11.py -q</automated>
  </verify>
  <done>delete_expired_memories is a parameterized time-based delete; sweep() calls it with an injected
    now, logs the count, and never raises; integration skeleton collects.</done>
</task>

<task type="auto">
  <name>Task 3: memory_sweep daily @tasks.loop</name>
  <files>bot.py, tests/test_memory.py</files>
  <read_first>
    - bot.py (cache_cleanup/ytdlp_update @tasks.loop pattern lines 701-752; start guards 399-407; _cleanup_partial_init loop list 273-279 — which now includes memory_distill_batch from 11-05)
    - .planning/phases/11-rag-long-term-memory/11-PATTERNS.md (memory_sweep @tasks.loop template lines 320-331; start guard + cleanup-list additions lines 334-352)
    - services/memory.py (MemoryService.sweep from Task 2)
  </read_first>
  <action>
    Add `@tasks.loop(time=datetime.time(hour=2, minute=30)) async def memory_sweep()` (a daily hour
    distinct from cache/ytdlp/distill loops) guarded on `getattr(bot,"memory_service",None)`; it calls
    `await memory_service.sweep()`. Add `memory_sweep.before_loop` (await bot.wait_until_ready()) and
    `memory_sweep.error` (-> _post_loop_error). Start it in `_initialize_once` behind `if not
    memory_sweep.is_running(): memory_sweep.start()`. Add `memory_sweep` to the loop list in
    `_cleanup_partial_init` (alongside memory_distill_batch added in 11-05). Add a test asserting
    memory_sweep is defined and registered in the cleanup list.
  </action>
  <verify>
    <automated>python -c "import bot; assert hasattr(bot,'memory_sweep'); print('sweep task defined')"</automated>
    <automated>python -c "src=open('bot.py',encoding='utf-8').read(); assert 'memory_sweep.start()' in src; tail=src.split('_cleanup_partial_init')[1][:700]; assert 'memory_sweep' in tail; print('sweep started + in cleanup list')"</automated>
    <automated>python -c "import bot"</automated>
  </verify>
  <done>memory_sweep is a daily @tasks.loop with before_loop/error, started under a guard, and added to
    _cleanup_partial_init; bot imports clean.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan adds: `decay_predicate` (models/memory.py); `delete_expired_memories` (database.py);
`MemoryService.sweep` (services/memory.py); `memory_sweep` (bot.py). Full Phase 11 inventory in
11-01-PLAN.md § "Artifacts this phase produces".
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep task → Neon | scheduled bulk delete of expired rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07a | Denial of Service | unbounded memory growth over time | mitigate | daily decay sweep deletes expired low-salience rows (MEM-07); pairs with the 11-04 write-time cap (Pitfall 13/14) |
| T-11-07b | Tampering | over-broad delete removes live high-salience memories | mitigate | decay_predicate retains high-salience/recent facts; delete is bounded by expires_at + low-salience clause; $N parameterized |
| T-11-07c | Denial of Service | sweep error kills the background loop | mitigate | sweep() swallows errors; .error handler -> _post_loop_error; before_loop wait_until_ready (REL-02 discipline) |
</threat_model>

<verification>
- decay predicate + sweep unit tests pass: `python -m pytest tests/test_memory.py -x -q`.
- Static assert: delete_expired_memories + sweep + memory_sweep defined, started, in cleanup list (automated).
- `python -c "import bot"` import smoke succeeds.
- Full suite green: `python -m pytest tests/ -q`.
</verification>

<success_criteria>
- A daily sweep removes expired low-salience memories; high-salience/recent facts survive.
- memory_sweep follows the established daily-loop discipline (before_loop/error/start guard/cleanup).
- The store is bounded by both the write-time cap (11-04) and the decay sweep (this plan).
</success_criteria>

<output>
Create `.planning/phases/11-rag-long-term-memory/11-07-SUMMARY.md` when done.
</output>
