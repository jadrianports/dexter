---
phase: 20-owner-control-plane-rate-observability
plan: 05
type: execute
wave: 2
depends_on: ["20-02", "20-03"]
files_modified:
  - cogs/events.py
  - tests/test_proactive_events.py
autonomous: true
requirements: [OWNER-06, OWNER-02, RATE-01]
must_haves:
  truths:
    - "A guild silenced DURING a proactive-callback or vision-roast Gemini round-trip does not send — a pre-send re-check re-reads the cache immediately before message.reply and bails when now silenced (SC-2, no stale in-flight response)"
    - "The two events.py Gemini call sites (ambient roast, vision roast) pass their guild_id for RATE-01 counting"
  artifacts:
    - path: "cogs/events.py"
      provides: "guild_id threading at the 2 Gemini sites + TOCTOU pre-send re-check in _maybe_fire_proactive_callback + _maybe_fire_vision_roast"
    - path: "tests/test_proactive_events.py"
      provides: "silenced-mid-flight regression: gate passes + recall non-empty but guild silenced before reply → no reply; existing tests still green with the added silenced key"
  key_links:
    - from: "_maybe_fire_proactive_callback / _maybe_fire_vision_roast"
      to: "is_ambient_channel (silence-aware via 20-02)"
      via: "re-evaluated immediately before message.reply; bail if False"
      pattern: "is_ambient_channel"
    - from: "events.py Gemini calls"
      to: "gemini_service.chat(..., guild_id=...)"
      via: "message.guild.id threaded"
      pattern: "guild_id="
---

<objective>
Close the OWNER-06 / D-14 TOCTOU window on the ambient surfaces that send via `message.reply` after seconds-long async Gemini work, and thread `guild_id` through the two `cogs/events.py` Gemini call sites (RATE-01). The proactive-callback and vision-roast paths resolve their channel-gate at `on_message` ENTRY, then do a Gemini round-trip, then `message.reply` WITHOUT re-checking — so a silence issued mid-flight would still let a stale response slip through (SC-2). The fix: re-evaluate the silence-aware `is_ambient_channel` immediately before each `message.reply` and bail if it now returns False. The voice-join/leave/move and music-path roasts already resolve `resolve_ambient_channel` at send time, so 20-02's silenced branch covers them for free — this plan handles only the two reply-after-Gemini surfaces.

Purpose: "silence takes effect on the very next event, never a stale in-flight response slipping through after the block is issued" (SC-2) becomes true for every ambient surface. events.py owns its file, so the RATE-01 threading at :185/:565 lands here too (avoids a file conflict with 20-03).
Output: pre-send re-check in `_maybe_fire_proactive_callback` + `_maybe_fire_vision_roast`; `guild_id=str(message.guild.id)` at the ambient-roast (:185) and vision (:565) chat calls; regression + new silenced-mid-flight tests.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-owner-control-plane-rate-observability/20-CONTEXT.md
@.planning/phases/20-owner-control-plane-rate-observability/20-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces (of the phase-wide artifact set):
- The TOCTOU pre-send re-check in `cogs/events.py::_maybe_fire_proactive_callback` and `::_maybe_fire_vision_roast` (D-14 / OWNER-06 / SC-2).
- `guild_id` threaded at the `cogs/events.py` ambient-roast (`_generate_ambient_roast` chat, :185) and vision (`_generate_vision_roast` chat, :565) call sites (RATE-01).
(The voice + music roast surfaces inherit silence via 20-02's decide_ambient_channel branch — no edit needed here.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: thread guild_id through the two events.py Gemini call sites (RATE-01)</name>
  <files>cogs/events.py</files>
  <read_first>
    - cogs/events.py:160-203 (_generate_ambient_roast — the chat at :185; `member.guild.id` is in scope via the member param)
    - cogs/events.py:544-589 (_generate_vision_roast — the chat at :565; it takes `member` so `member.guild.id` is available)
    - .planning/phases/20-owner-control-plane-rate-observability/20-PATTERNS.md §"Call sites to thread guild_id through" (events.py:185/565 rows) + 20-03 (the guild_id kwarg it depends on)
  </read_first>
  <action>
    At the `gemini_service.chat(...)` call in `_generate_ambient_roast` (:185), pass `guild_id=str(member.guild.id)` (member is the handler's subject; its guild is the ambient guild). At the `gemini_service.chat(...)` call in `_generate_vision_roast` (:565), pass `guild_id=str(member.guild.id)`. Both are guild-attributable ambient roasts (D-09). Verify the exact local variable exposing the guild by reading each function's signature/scope first (both take a `member: discord.Member`, so `member.guild.id` is the guild). Do not alter the surrounding fallback/return contracts.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('cogs/events.py',encoding='utf-8').read())" && pytest tests/test_proactive_events.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The `_generate_ambient_roast` chat call passes `guild_id=str(member.guild.id)`
    - The `_generate_vision_roast` chat call passes `guild_id=str(member.guild.id)`
    - `cogs/events.py` parses (ast.parse) and existing events tests stay green
  </acceptance_criteria>
  <done>Both events.py ambient Gemini calls now tag their guild for RATE-01 counting.</done>
</task>

<task type="auto">
  <name>Task 2: TOCTOU pre-send re-check in proactive-callback + vision-roast paths (D-14 / SC-2)</name>
  <files>cogs/events.py</files>
  <read_first>
    - cogs/events.py:378-430 (on_message — the entry-point roast_channel_ok / vision_channel_ok gates via is_ambient_channel, computed BEFORE the async work)
    - cogs/events.py:434-541 (_maybe_fire_proactive_callback — recall + _generate_ambient_roast, then message.reply at ~:529)
    - cogs/events.py:591-663 (_maybe_fire_vision_roast — bytes read + _generate_vision_roast, then message.reply at ~:652)
    - logic/guild_config.py — is_ambient_channel is now silence-aware (20-02 added the branch to decide_ambient_channel it dispatches on)
    - .planning/phases/20-owner-control-plane-rate-observability/20-CONTEXT.md D-14 (TOCTOU re-check is a SECOND, later read of the same cache — not a new mechanism); .planning/.../20-PATTERNS.md §"Pattern 4 TOCTOU re-check"
  </read_first>
  <action>
    In BOTH `_maybe_fire_proactive_callback` (immediately before the `message.reply(...)` at ~:529) and `_maybe_fire_vision_roast` (immediately before the `message.reply(...)` at ~:652), add a pre-send re-check: re-evaluate `is_ambient_channel(config_row=self.bot.guild_config.get(message.guild.id), channel_id=message.channel.id, surface=<AmbientSurface.ROAST for proactive / AmbientSurface.VISION for vision>)`; if it returns False, bail silently BEFORE sending — release any reserved daily-cap slot (proactive path: call the existing `_release_reserved_slot()` before returning), and do NOT mark the vision cooldown. This is the D-14 second read of the same cache (silence issued during the Gemini round-trip now takes effect). Because 20-02 made `is_ambient_channel` silence-aware (and it already covers unconfigured/toggled-off), a mid-flight silence, a mid-flight toggle-off, or a channel change all correctly suppress the send. Do not re-derive `silenced` locally — dispatch on the pure predicate (Phase 10 D-02). Keep the existing entry-point gates untouched (the re-check is additive, not a replacement).
  </action>
  <verify>
    <automated>pytest tests/test_proactive_events.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `_maybe_fire_proactive_callback` re-calls `is_ambient_channel(...)` with `surface=AmbientSurface.ROAST` immediately before `message.reply` and returns (after releasing the reserved slot) when it is False
    - `_maybe_fire_vision_roast` re-calls `is_ambient_channel(...)` with `surface=AmbientSurface.VISION` immediately before `message.reply` and returns (without marking the cooldown) when it is False
    - Neither path re-derives `silenced`/`configured` inline — both dispatch on `is_ambient_channel`
    - The proactive re-check bail path calls the existing `_release_reserved_slot()` (no daily-cap leak)
  </acceptance_criteria>
  <done>Both reply-after-Gemini ambient surfaces re-check silence at the last moment — no stale in-flight response can slip through (SC-2).</done>
</task>

<task type="auto">
  <name>Task 3: silenced-mid-flight regression + call-site inventory fix in test_proactive_events.py</name>
  <files>tests/test_proactive_events.py</files>
  <read_first>
    - tests/test_proactive_events.py (whole file) — the _make_bot fixture (:40-54, whose bot.guild_config.get returns a configured dict), _make_message (:57-77), and the existing fire/skip tests
    - RESEARCH Wave 0 "Known regression surface": every mocked guild_config.get mapping — the added silenced branch defaults False, so existing tests stay green; new tests must set silenced=True to exercise the re-check
  </read_first>
  <action>
    Add regression + new coverage. (1) Confirm the existing `_make_bot` config dict (no `silenced` key) still drives the fire tests green — the default-False silenced branch must not change any existing assertion (call-site inventory: the mapping is unchanged, so this should already hold; add an explicit comment noting the default-False contract). (2) Add a proactive silenced-mid-flight test: build a bot whose `guild_config.get` returns a configured, non-silenced row at first but a silenced row at the pre-send re-check — simplest realization: use a MagicMock `.get` whose `side_effect` returns a non-silenced dict for the entry gate call and a `{"configured": True, "ambient_channel_id": "500", "ambient_roasts_enabled": True, "silenced": True}` dict for the pre-send re-check call; assert `message.reply` is NOT awaited and the reserved daily-cap slot is released (user id absent from `_proactive_daily_counts`). (3) Add a simpler always-silenced test: `guild_config.get` returns a silenced row throughout → the entry gate (`roast_channel_ok`) is already False so `_maybe_fire_proactive_callback` is never called from on_message; assert on_message sends nothing. (Optionally mirror one vision silenced-mid-flight test using `_maybe_fire_vision_roast` directly.)
  </action>
  <verify>
    <automated>pytest tests/test_proactive_events.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_proactive_events.py -q` exits 0
    - A test drives a silenced-mid-flight scenario (non-silenced at entry, silenced at pre-send) and asserts `message.reply` is not awaited and the daily-cap slot is released
    - Existing fire tests remain green unchanged (the missing-silenced-key default-False contract holds)
  </acceptance_criteria>
  <done>The SC-2 no-stale-response behavior is locked by a regression test; existing ambient cadence tests unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner silence write → in-flight ambient send | A `/guilds silence` mutates the cache during a seconds-long Gemini round-trip; the ambient send must observe it |
| message → ambient reply | An unprompted reply to a user message; must stay silent in an unconfigured/toggled-off/silenced guild |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Tampering (authorization state / TOCTOU) | proactive + vision reply-after-Gemini paths | mitigate | Pre-send re-check re-reads the silence-aware `is_ambient_channel` immediately before `message.reply` (D-14 / SC-2); a mid-flight silence suppresses the send |
| T-20-13 | Information disclosure (unwanted ambient output) | ambient reply gating | mitigate | The re-check also catches a mid-flight toggle-off or channel change — no ambient output in a guild that just went quiet |
| T-20-SC | Tampering (supply chain) | package installs | mitigate | Zero new installs; nothing to audit |
</threat_model>

<verification>
- `pytest tests/test_proactive_events.py -q` green (including the new silenced-mid-flight tests)
- `python -c "import cogs.events"`-equivalent ast.parse clean
- Full suite `pytest tests/ -q` green (voice + music ambient tests unaffected — they resolve at send time and inherit 20-02's branch)
</verification>

<success_criteria>
- Proactive + vision reply paths re-check silence immediately before send (SC-2)
- The two events.py Gemini calls tag guild_id (RATE-01)
- Existing ambient cadence tests stay byte-behavior-identical (default-False silenced)
</success_criteria>

<output>
Create `.planning/phases/20-owner-control-plane-rate-observability/20-05-SUMMARY.md` when done
</output>
