---
phase: 20-owner-control-plane-rate-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database.py
  - tests/conftest.py
  - tests/test_database_phase20.py
  - CLAUDE.md
autonomous: true
requirements: [OWNER-04, OWNER-02]
must_haves:
  truths:
    - "A blocked guild_id persists in its OWN guild_blocklist table, independent of guild_config (Phase 21's MEM-04 purge of guild_config never touches it)"
    - "silenced can be written to a guild_config row and read back via RETURNING"
  artifacts:
    - path: "database.py"
      provides: "guild_blocklist DDL + load_blocklist/insert_blocklist/delete_blocklist + set_silenced helpers"
      contains: "CREATE TABLE IF NOT EXISTS guild_blocklist"
    - path: "tests/test_database_phase20.py"
      provides: "static shape + live-DB CRUD tests for blocklist + silenced round-trip"
    - path: "tests/conftest.py"
      provides: "guild_blocklist added to the teardown DROP so test isolation holds"
  key_links:
    - from: "database.load_blocklist/insert_blocklist/delete_blocklist"
      to: "guild_blocklist table"
      via: "asyncpg SELECT/INSERT ON CONFLICT/DELETE"
      pattern: "guild_blocklist"
    - from: "database.set_silenced"
      to: "guild_config.silenced column"
      via: "UPDATE ... RETURNING"
      pattern: "silenced = \\$2"
---

<objective>
Add the persistent `guild_blocklist` table (D-01) and the DB-tier helpers the owner kill-switch reads/writes: blocklist CRUD (OWNER-04) and a `set_silenced` writer (OWNER-02). This is the load-bearing D-01 decision — the blacklist gets its OWN table so Phase 21's `guild_config` purge can run as a clean `DELETE ... WHERE guild_id = $1` with no "except if blocked" carve-out, and a kicked abuser can never launder their block by re-inviting.

Purpose: The database seam every Wave-2 service method (20-04) and owner command (20-07) depends on. First reader/writer of Phase 18's forward-shipped `silenced` column.
Output: `guild_blocklist` DDL + `load_blocklist`/`insert_blocklist`/`delete_blocklist`/`set_silenced` in `database.py`, conftest teardown fix, and `tests/test_database_phase20.py`. CLAUDE.md schema narrative annotated (D-03: `guild_config.is_blocked` is now dead; `guild_blocklist` is authoritative).
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-owner-control-plane-rate-observability/20-CONTEXT.md
@.planning/phases/20-owner-control-plane-rate-observability/20-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces (of the phase-wide artifact set):
- The `guild_blocklist` table (`guild_id TEXT PRIMARY KEY`, `reason TEXT`, `blocked_at TIMESTAMPTZ DEFAULT now()`).
- `database.load_blocklist(pool)`, `database.insert_blocklist(pool, *, guild_id, reason)`, `database.delete_blocklist(pool, *, guild_id)`.
- `database.set_silenced(pool, *, guild_id, silenced) -> asyncpg.Record | None`.
(Downstream: 20-04 wraps these in `GuildConfigService`; 20-07 drives them from `/guilds`.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: guild_blocklist DDL + blocklist/silenced DB helpers + conftest teardown</name>
  <files>database.py, tests/conftest.py, CLAUDE.md</files>
  <read_first>
    - database.py:178-230 (the SCHEMA_SQL guild_jams/resolution_cache/guild_config blocks + init_db) — the DDL idiom to mirror
    - database.py:410-483 (load_all_guild_configs + _GUILD_CONFIG_RETURNING_COLUMNS) — the load-all + RETURNING shapes
    - database.py:350-407 (set/get_proactive_opt_out) — the upsert helper shape
    - database.py:605-644 (set_ambient_roasts_enabled/set_vision_roasts_enabled) — the UPDATE ... RETURNING helper shape set_silenced mirrors
    - tests/conftest.py (whole file) — the DROP TABLE teardown list that must gain guild_blocklist
    - .planning/phases/20-owner-control-plane-rate-observability/20-PATTERNS.md §"database.py — guild_blocklist DDL + helpers"
    - .planning/phases/20-owner-control-plane-rate-observability/20-CONTEXT.md D-01/D-03 + Discretion (DDL/helper shapes)
  </read_first>
  <action>
    Append a `CREATE TABLE IF NOT EXISTS guild_blocklist (guild_id TEXT PRIMARY KEY, reason TEXT, blocked_at TIMESTAMPTZ DEFAULT now());` block into the SAME `SCHEMA_SQL` triple-quoted string (currently ending at the `ALTER TABLE guild_config ADD COLUMN ... vision_roasts_enabled` line ~219) — asyncpg's param-free multi-statement DDL rule (Implementation Gotcha / Pitfall 1) requires it stay in the one string applied by `init_db`'s single `conn.execute(SCHEMA_SQL)`, never a separate execute. Add a short comment marking it Phase 20 / D-01 (own table so MEM-04 purge never touches the blacklist).
    Add three module-level helpers mirroring the existing shapes: `load_blocklist(pool)` = bare `SELECT guild_id, reason, blocked_at FROM guild_blocklist` returning `list[asyncpg.Record]` (boot load-all, no per-guild filter — CONFIG-03 discipline); `insert_blocklist(pool, *, guild_id: str, reason: str | None)` = `INSERT INTO guild_blocklist (guild_id, reason) VALUES ($1, $2) ON CONFLICT (guild_id) DO UPDATE SET reason = EXCLUDED.reason` (upsert so a re-block updates the reason, mirroring set_proactive_opt_out); `delete_blocklist(pool, *, guild_id: str)` = `DELETE FROM guild_blocklist WHERE guild_id = $1`.
    Add `set_silenced(pool, *, guild_id: str, silenced: bool) -> asyncpg.Record | None` = `UPDATE guild_config SET silenced = $2, updated_at = now() WHERE guild_id = $1 RETURNING ` + `_GUILD_CONFIG_RETURNING_COLUMNS` (verbatim the set_ambient_roasts_enabled shape) — returns the updated Record, or None when no row exists for guild_id (the "row existed?" contract 20-04's service method relies on).
    In tests/conftest.py, add `guild_blocklist` to the teardown `DROP TABLE IF EXISTS ...` list so a run leaves no residue and the next run starts clean.
    In CLAUDE.md §"Database Schema (PostgreSQL)": add the `guild_blocklist` table to the running narrative (Phase 20 / OWNER-04) and annotate that `guild_config.is_blocked` is now DEAD/superseded — `guild_blocklist` is the authoritative blacklist (D-03); no DROP COLUMN (additive-only DDL precedent).
  </action>
  <verify>
    <automated>python -c "import database; import inspect; s=inspect.getsource(database); assert 'CREATE TABLE IF NOT EXISTS guild_blocklist' in s; assert all(hasattr(database,n) for n in ['load_blocklist','insert_blocklist','delete_blocklist','set_silenced'])"</automated>
  </verify>
  <acceptance_criteria>
    - `database.py` SCHEMA_SQL string contains `CREATE TABLE IF NOT EXISTS guild_blocklist` with columns `guild_id TEXT PRIMARY KEY`, `reason TEXT`, `blocked_at TIMESTAMPTZ DEFAULT now()`
    - `database.load_blocklist`, `database.insert_blocklist`, `database.delete_blocklist`, `database.set_silenced` all exist and are async
    - `insert_blocklist` source contains `ON CONFLICT (guild_id) DO UPDATE`
    - `set_silenced` source contains `silenced = $2` and `RETURNING`
    - `tests/conftest.py` DROP TABLE list contains `guild_blocklist`
    - The new DDL is inside the single `SCHEMA_SQL` string (no new standalone `conn.execute` for it)
    - CLAUDE.md schema section mentions `guild_blocklist` and notes `guild_config.is_blocked` is dead (D-03)
  </acceptance_criteria>
  <done>guild_blocklist table + blocklist CRUD + set_silenced helper land in database.py following the existing idiom; conftest drops the new table; CLAUDE.md annotated.</done>
</task>

<task type="auto">
  <name>Task 2: live-DB + static-shape tests for blocklist CRUD and silenced round-trip</name>
  <files>tests/test_database_phase20.py</files>
  <read_first>
    - tests/test_database_phase16.py (whole file) — the exact skip-guard + static-shape + live-DB structure to clone (this is the named analog for a Phase-N database test file)
    - tests/conftest.py (the `pool` fixture + skip-on-connection-error behavior)
    - database.py — the four helpers written in Task 1 + set_ambient_roasts_enabled (for the silenced round-trip assertion shape)
  </read_first>
  <action>
    Create `tests/test_database_phase20.py` mirroring `tests/test_database_phase16.py`: the `_SKIP_LIVE = os.getenv("TEST_DATABASE_URL") is None` guard, a static-shape class that always runs (no DB), and `@pytest.mark.skipif(_SKIP_LIVE, ...)` + `@pytest.mark.asyncio` live-DB tests using the `pool` fixture.
    Static-shape checks (no DB): `guild_blocklist`, `load_blocklist`, `insert_blocklist`, `delete_blocklist`, `set_silenced` exist in `database`; `insert_blocklist` source contains `ON CONFLICT (guild_id) DO UPDATE`; SCHEMA_SQL contains `CREATE TABLE IF NOT EXISTS guild_blocklist`; `set_silenced` source targets the `silenced` column (`silenced = $2`).
    Live-DB tests: (a) `test_blocklist_crud_roundtrip` — insert_blocklist(guild_id="g20-1", reason="spam"), assert load_blocklist returns a row whose guild_id is in the set and reason=="spam"; re-insert with reason="abuse" and assert the ON CONFLICT updates reason (no duplicate row); delete_blocklist and assert the guild_id is gone from load_blocklist. (b) `test_set_silenced_roundtrip` — seed a guild_config row first (via seed_guild_config_if_absent or insert_guild_config_if_absent), call set_silenced(silenced=True), assert the returned Record["silenced"] is True; set_silenced(silenced=False) → Record["silenced"] False; and set_silenced on a NON-existent guild_id returns None (the no-row contract). (c) `test_blocklist_independent_of_guild_config` — insert a blocklist row for a guild_id, DELETE that guild's guild_config row directly (simulating the Phase 21 MEM-04 purge), and assert load_blocklist STILL returns the block (D-01: the blacklist survives a guild_config purge).
  </action>
  <verify>
    <automated>pytest tests/test_database_phase20.py -q --collect-only && pytest tests/test_database_phase20.py -q -k "not roundtrip and not independent"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_database_phase20.py -q --collect-only` exits 0 (autonomous discovery gate, no live DB)
    - The static-shape class runs and passes without a database
    - The live-DB tests are decorated `@pytest.mark.skipif(_SKIP_LIVE, ...)` and skip cleanly when `TEST_DATABASE_URL` is unset
    - When a live pgvector DB is present (CI service container), `test_blocklist_crud_roundtrip`, `test_set_silenced_roundtrip`, and `test_blocklist_independent_of_guild_config` all pass
    - `test_blocklist_independent_of_guild_config` deletes the guild_config row and asserts the blocklist row still exists (D-01 durability proof)
  </acceptance_criteria>
  <done>test_database_phase20.py exists with an autonomous static-shape gate and live-DB CRUD + silenced round-trip + blocklist-survives-config-purge coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner command → DB write | `/guilds block`/`silence` (20-07) reach these helpers with an owner-supplied guild_id (parsed upstream); helpers take it as a parameterized `$N` arg |
| Phase 21 purge → guild_blocklist | MEM-04 will `DELETE FROM guild_config WHERE guild_id=$1`; guild_blocklist is a SEPARATE table it must not touch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Repudiation/Persistence | guild_blocklist durability | mitigate | Own table (D-01); `test_blocklist_independent_of_guild_config` proves the block survives a guild_config-row delete, so a kicked abuser cannot launder the block via re-invite |
| T-20-08 | Tampering (SQLi) | blocklist/silenced helpers | mitigate | All values passed as asyncpg `$N` params, never string-interpolated (mirrors the existing helper discipline) |
| T-20-SC | Tampering (supply chain) | package installs | mitigate | Zero new installs this plan/phase; nothing to audit |
</threat_model>

<verification>
- `python -c "import database"` imports clean (no syntax error in the appended DDL/helpers)
- `pytest tests/test_database_phase20.py -q --collect-only` exits 0
- Full suite `pytest tests/ -q` stays green (the new DDL is additive; existing tests unaffected)
</verification>

<success_criteria>
- guild_blocklist table + blocklist CRUD + set_silenced exist and follow the guild_jams/proactive_opt_out idioms
- Live-DB tests prove blocklist CRUD, silenced round-trip, and blocklist-survives-config-purge (D-01)
- conftest teardown drops guild_blocklist; CLAUDE.md schema narrative updated (D-03)
</success_criteria>

<output>
Create `.planning/phases/20-owner-control-plane-rate-observability/20-01-SUMMARY.md` when done
</output>
