---
phase: 20-owner-control-plane-rate-observability
plan: 04
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - services/guild_config.py
  - tests/test_guild_config_service.py
autonomous: true
requirements: [OWNER-04, OWNER-02]
must_haves:
  truths:
    - "GuildConfigService loads all blocked guild_ids into a _blocked set at boot; is_blocked(guild_id) is an O(1) set test with no DB round-trip"
    - "block_guild writes the DB then adds to the set; unblock_guild deletes then discards — write-then-mutate (D-02)"
    - "silence_guild/unsilence_guild write guild_config.silenced and push-invalidate the config cache; is_silenced reads the cached row"
  artifacts:
    - path: "services/guild_config.py"
      provides: "_blocked set + block_guild/unblock_guild/is_blocked + silence_guild/unsilence_guild/is_silenced"
      contains: "self._blocked"
    - path: "tests/test_guild_config_service.py"
      provides: "blocked-set load + push-invalidate + silence write-through + independent fail-open on blocklist load error"
  key_links:
    - from: "GuildConfigService.load_all"
      to: "database.load_blocklist"
      via: "second query populating _blocked, independent try/except (fail to empty set)"
      pattern: "load_blocklist"
    - from: "silence_guild"
      to: "_refresh_cache_entry"
      via: "database.set_silenced returns the updated row, push-invalidated into the config cache"
      pattern: "_refresh_cache_entry"
---

<objective>
Extend `GuildConfigService` (D-03) with the two in-memory reads both choke points depend on: an O(1) blocked-set (`_blocked: set[str]`, D-02) and the silenced read off the existing config cache (D-14) — plus the write-then-invalidate setters `/guilds` drives. All reads are cache-only, zero Neon round-trip on the hot path (CONFIG-03 discipline extended). The service already owns load-all-at-boot + push-invalidate, so this is the same discipline applied to one more concern.

Purpose: `interaction_check` (20-06) and `on_guild_join` (20-06) call `is_blocked`; ambient resolution (20-05) and `interaction_check` call `is_silenced`; `/guilds` (20-07) calls the setters. One service owns them all; no per-cog checks (OWNER-05).
Output: `_blocked` set + `block_guild`/`unblock_guild`/`is_blocked` + `silence_guild`/`unsilence_guild`/`is_silenced` on `GuildConfigService`, with service-level tests using the established fake-pool pattern.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-owner-control-plane-rate-observability/20-CONTEXT.md
@.planning/phases/20-owner-control-plane-rate-observability/20-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces (of the phase-wide artifact set):
- `GuildConfigService._blocked: set[str]`, loaded at boot in `load_all()` (independent try/except).
- `GuildConfigService.block_guild(*, guild_id, reason)`, `unblock_guild(*, guild_id)`, `is_blocked(guild_id) -> bool`.
- `GuildConfigService.silence_guild(*, guild_id) -> bool`, `unsilence_guild(*, guild_id) -> bool`, `is_silenced(guild_id) -> bool`.
(Downstream: 20-06's interaction_check + on_guild_join and 20-07's `/guilds` drive these.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: _blocked set + block/unblock/is_blocked + silence/unsilence/is_silenced on GuildConfigService</name>
  <files>services/guild_config.py</files>
  <read_first>
    - services/guild_config.py (whole file) — __init__ (:54-62), load_all (:68-91), get/_refresh_cache_entry (:93-103), set_ambient_roasts_enabled (:208-222 — the write-then-push-invalidate + "row existed?" bool contract to mirror)
    - database.py — load_blocklist/insert_blocklist/delete_blocklist/set_silenced (added in 20-01) + load_all_guild_configs (the existing boot query)
    - .planning/phases/20-owner-control-plane-rate-observability/20-PATTERNS.md §"services/guild_config.py — _blocked set + silence/block methods" (exact method shapes)
    - .planning/phases/20-owner-control-plane-rate-observability/20-CONTEXT.md D-02/D-03/D-14
  </read_first>
  <action>
    In `__init__`, add `self._blocked: set[str] = set()`.
    In `load_all()`, AFTER the existing config-cache load, populate the blocked set with its OWN try/except so a blocklist-load failure does NOT blank the config cache and vice-versa: `try: self._blocked = {str(r["guild_id"]) for r in await database.load_blocklist(self.pool)} except Exception: self._blocked = set(); log.error(...)`. An empty `_blocked` on failure means "nothing blocked" — the safer default direction (fail-open on the blocklist read only; the config cache keeps its own fail-closed rule, D-07/D-02).
    Add `async def block_guild(self, *, guild_id: str, reason: str | None) -> None`: `await database.insert_blocklist(self.pool, guild_id=str(guild_id), reason=reason); self._blocked.add(str(guild_id))` — write DB THEN mutate set (D-02). `async def unblock_guild(self, *, guild_id: str) -> None`: `await database.delete_blocklist(...); self._blocked.discard(str(guild_id))`. `def is_blocked(self, guild_id) -> bool: return str(guild_id) in self._blocked` (O(1), no I/O).
    Add `async def silence_guild(self, *, guild_id: str) -> bool` and `unsilence_guild(self, *, guild_id: str) -> bool` mirroring set_ambient_roasts_enabled exactly: call `database.set_silenced(self.pool, guild_id=guild_id, silenced=True/False)`; if the returned row is None return False (no such guild_config row — caller must not report success, WR-02 contract); else `self._refresh_cache_entry(row); return True`. Add `def is_silenced(self, guild_id) -> bool: row = self.get(guild_id); return bool(row and row.get("silenced", False))` (reads the config cache — already hot-path-safe, D-14).
    Do NOT route block through _cache/_refresh_cache_entry — the blocklist is its own table + its own set (D-03). Do NOT add a live SELECT anywhere in these reads (CONFIG-03 / D-02).
  </action>
  <verify>
    <automated>pytest tests/test_guild_config_service.py -q -k "blocked or silence or silenced or block"</automated>
  </verify>
  <acceptance_criteria>
    - `services/guild_config.py` `__init__` initializes `self._blocked: set[str] = set()`
    - `load_all` populates `_blocked` from `database.load_blocklist` inside its OWN try/except (config-cache load and blocklist load are independently guarded)
    - `block_guild` calls `database.insert_blocklist` THEN `self._blocked.add(...)`; `unblock_guild` deletes THEN discards
    - `is_blocked(guild_id)` is a synchronous `str(guild_id) in self._blocked` (no await, no pool access)
    - `silence_guild`/`unsilence_guild` return False when `database.set_silenced` returns None, else `_refresh_cache_entry` + True
    - `is_silenced` reads via `self.get(...)` (config cache), never a pool query
  </acceptance_criteria>
  <done>GuildConfigService owns the blocked-set and the silence read/writes, all cache-only reads, write-then-invalidate writes (D-02/D-03/D-14).</done>
</task>

<task type="auto">
  <name>Task 2: service tests — blocked-set load/push-invalidate + silence write-through + fail-open</name>
  <files>tests/test_guild_config_service.py</files>
  <read_first>
    - tests/test_guild_config_service.py (whole file) — the _FakeConn/_SpyPool/_RaisingPool trio + the load_all/resolve conventions to extend
    - services/guild_config.py — the methods from Task 1
  </read_first>
  <action>
    Extend `tests/test_guild_config_service.py` (additive, same fake-pool pattern). Add a fake conn/pool variant whose `fetch` can return DIFFERENT rows depending on the query so `load_all` populates both the config cache AND `_blocked` (e.g. a fake that returns guild_config rows for the config SELECT and blocklist rows for the `guild_blocklist` SELECT — key on a substring of the SQL). Tests:
    (1) after `load_all()`, `is_blocked("g-blocked")` is True and `is_blocked("g-clean")` is False, computed with ZERO additional `acquire()` calls beyond the boot loads (assert the spy count doesn't rise on `is_blocked`).
    (2) `block_guild(guild_id="g-new", reason="spam")` results in `is_blocked("g-new") is True` and made exactly one write; `unblock_guild(guild_id="g-new")` → `is_blocked("g-new") is False`.
    (3) a blocklist-load failure (the blocklist SELECT raises) leaves `_blocked` empty AND does not blank a successfully-loaded config cache — independence proof (fail-open on blocklist, fail-closed on config).
    (4) `silence_guild` when `database.set_silenced` returns a row → returns True and the cached row now reads silenced True via `is_silenced`; when it returns None (no guild_config row) → returns False and `is_silenced` stays False. Use monkeypatch/fake to control `database.set_silenced`'s return, mirroring how the file already fakes the pool.
  </action>
  <verify>
    <automated>pytest tests/test_guild_config_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_guild_config_service.py -q` exits 0
    - A test proves `is_blocked` after load_all does zero extra pool round-trips (spy count unchanged)
    - A test proves block_guild/unblock_guild flip is_blocked
    - A test proves a blocklist-load failure empties `_blocked` without blanking the config cache
    - A test proves silence_guild returns True + flips is_silenced on a real row, False + no-op on a missing row
  </acceptance_criteria>
  <done>Service-level tests lock the blocked-set discipline and the silence write-through/no-op contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner command → service setter | `/guilds block`/`silence` reach block_guild/silence_guild with an owner-supplied guild_id (already validated upstream) |
| hot path → in-memory read | interaction_check + on_guild_join call is_blocked on every command/join — must be O(1), zero Neon round-trip (CONFIG-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Repudiation/Persistence | _blocked set (backed by guild_blocklist) | mitigate | Boot-loaded from the independent guild_blocklist table (D-01/D-02); a re-invite hits is_blocked in on_guild_join (20-06) with no round-trip |
| T-20-11 | Denial of Service (Neon latency on hot path) | is_blocked/is_silenced reads | mitigate | Both are O(1) cache/set reads; block/unblock/silence write-then-mutate — never a live SELECT per check (CONFIG-03 / D-02) |
| T-20-12 | Availability (boot load failure) | load_all blocklist step | mitigate | Independent try/except: a blocklist-load failure empties _blocked (nothing blocked) without blanking the config cache; each subsystem keeps its own fail direction |
| T-20-SC | Tampering (supply chain) | package installs | mitigate | Zero new installs; nothing to audit |
</threat_model>

<verification>
- `pytest tests/test_guild_config_service.py -q` green
- `python -c "import services.guild_config"` imports clean
- Full suite `pytest tests/ -q` green (the additions are new methods + a new set attribute — no existing behavior changes)
</verification>

<success_criteria>
- GuildConfigService owns _blocked + is_blocked/block/unblock and is_silenced/silence/unsilence
- All reads are cache-only; writes are write-then-invalidate
- Blocklist load failure is independently fail-open without harming the config cache
</success_criteria>

<output>
Create `.planning/phases/20-owner-control-plane-rate-observability/20-04-SUMMARY.md` when done
</output>
