---
phase: 20-owner-control-plane-rate-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - logic/guild_config.py
  - tests/test_guild_config_logic.py
autonomous: true
requirements: [OWNER-05, OWNER-06, OWNER-02]
must_haves:
  truths:
    - "decide_ambient_channel returns None (silence) for a configured, toggled-on guild whose config row has silenced=True"
    - "decide_interaction_allowed refuses (returns False) on blocked-or-silenced, always allows the owner, always allows a guild-less (DM) interaction"
  artifacts:
    - path: "logic/guild_config.py"
      provides: "silenced early-return branch in decide_ambient_channel + pure decide_interaction_allowed predicate"
      contains: "def decide_interaction_allowed"
    - path: "tests/test_guild_config_logic.py"
      provides: "mock-free branch coverage for the silenced branch + decide_interaction_allowed"
  key_links:
    - from: "decide_ambient_channel"
      to: "silence"
      via: "config_row.get('silenced', False) early return"
      pattern: "silenced"
    - from: "is_ambient_channel"
      to: "decide_ambient_channel"
      via: "dispatches — becomes silence-aware for free"
      pattern: "decide_ambient_channel"
---

<objective>
Make silence and block enforcement a structural property of the pure decision seam (OWNER-05), not something every glue site must remember (D-14). Two pure additions to `logic/guild_config.py`: (1) a `silenced` early-return branch in `decide_ambient_channel` so every ambient surface that resolves through it goes silent when the guild is silenced (and `is_ambient_channel`, which dispatches on it, becomes silence-aware for free); (2) `decide_interaction_allowed`, the pure predicate that backs the slash-command choke point (D-13) — testable mock-free instead of only through a live `discord.Interaction`.

Purpose: The two-choke-point enforcement logic (OWNER-05) lives in exactly one pure, mock-free-tested place; 20-06 (interaction_check) and 20-05 (ambient TOCTOU) dispatch on these return values and never re-derive the branch (Phase 10 D-02).
Output: silenced branch + `decide_interaction_allowed` in `logic/guild_config.py`, with exhaustive branch tests. No `discord`/`datetime`/`random`/`asyncio` imports enter the module.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-owner-control-plane-rate-observability/20-CONTEXT.md
@.planning/phases/20-owner-control-plane-rate-observability/20-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces (of the phase-wide artifact set):
- The new `silenced` branch inside `logic/guild_config.decide_ambient_channel` (D-14).
- `logic/guild_config.decide_interaction_allowed(*, is_owner, has_guild, blocked, silenced) -> bool` (the pure OWNER-05/D-13 slash-command predicate).
(Downstream: 20-05's ambient TOCTOU re-check and 20-06's `DexterCommandTree.interaction_check` both dispatch on these.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: silenced branch in decide_ambient_channel + decide_interaction_allowed predicate</name>
  <files>logic/guild_config.py</files>
  <behavior>
    - decide_ambient_channel: a configured, toggled-on row with silenced=True → None (new branch); silenced absent defaults False (existing rows/mocks unaffected); a configured, toggled-on, non-silenced row still returns the channel id
    - decide_interaction_allowed(is_owner=True, ...) → True regardless of blocked/silenced (owner never locked out)
    - decide_interaction_allowed(is_owner=False, has_guild=False, ...) → True (DM/guild-less always allowed)
    - decide_interaction_allowed(is_owner=False, has_guild=True, blocked=True, silenced=False) → False
    - decide_interaction_allowed(is_owner=False, has_guild=True, blocked=False, silenced=True) → False
    - decide_interaction_allowed(is_owner=False, has_guild=True, blocked=False, silenced=False) → True
    - keyword-only args on decide_interaction_allowed (omitting any raises TypeError)
  </behavior>
  <read_first>
    - logic/guild_config.py (whole file) — decide_ambient_channel:64-120, is_ambient_channel:128-151, AmbientSurface:35-56, and the module docstring's purity discipline (:1-23)
    - .planning/phases/20-owner-control-plane-rate-observability/20-PATTERNS.md §"logic/guild_config.py — silenced branch" (exact insertion point + pattern)
    - .planning/phases/20-owner-control-plane-rate-observability/20-CONTEXT.md D-13 (interaction predicate rules) + D-14 (silenced branch) + Discretion (may add a helper vs inline; must stay keyword-only, discord/datetime/random-free)
  </read_first>
  <action>
    In `decide_ambient_channel`, insert a new early-return branch `if config_row.get("silenced", False): return None` directly AFTER the `configured` check and before (or after — semantics unchanged, both early-returns) the toggle-column check. Use `config_row.get("silenced", False)` — a missing key defaults False, matching the column `DEFAULT false` and keeping every existing test/mock (which omit `silenced`) byte-identical. Cite D-14 in an inline comment. Do NOT touch is_ambient_channel — it dispatches on decide_ambient_channel and becomes silence-aware automatically.
    Add `decide_interaction_allowed(*, is_owner: bool, has_guild: bool, blocked: bool, silenced: bool) -> bool` as a new pure function (keyword-only, no default). Body, in order: `if is_owner: return True` (D-13 owner exemption — never locked out); `if not has_guild: return True` (D-13 DM/guild-less always allowed); `if blocked or silenced: return False` (D-13 refuse on either flag — block checked defensively per the block-written-while-leave-in-flight window); `return True`. Returns True = allow; the glue in 20-06 sends the D-12 ephemeral refusal and returns False when this returns False. Docstring must name that it is the OWNER-05 slash-command choke-point predicate and that the boot-race "service absent → fail open" case is a GLUE concern (20-06), NOT modeled here.
    Keep the module free of discord/datetime/random/asyncio imports (the module docstring + test_module_imports_no_discord_datetime_or_random enforce this).
  </action>
  <verify>
    <automated>pytest tests/test_guild_config_logic.py -q -k "silenced or interaction_allowed or imports_no_discord"</automated>
  </verify>
  <acceptance_criteria>
    - `logic/guild_config.py` `decide_ambient_channel` contains a `silenced` early-return using `config_row.get("silenced", False)`
    - `logic/guild_config.decide_interaction_allowed` exists, is keyword-only, and returns a bool
    - `decide_interaction_allowed(is_owner=True, has_guild=True, blocked=True, silenced=True)` returns True
    - `decide_interaction_allowed(is_owner=False, has_guild=False, blocked=True, silenced=True)` returns True
    - `decide_interaction_allowed(is_owner=False, has_guild=True, blocked=True, silenced=False)` returns False and `(..., blocked=False, silenced=True)` returns False
    - `import logic.guild_config; inspect.getsource` still contains no `import discord`, `import datetime`, `import random`
  </acceptance_criteria>
  <done>The pure seam gains a silenced branch (D-14) and decide_interaction_allowed (D-13); both keyword-only and discord-free.</done>
</task>

<task type="auto">
  <name>Task 2: mock-free tests for the silenced branch + decide_interaction_allowed</name>
  <files>tests/test_guild_config_logic.py</files>
  <read_first>
    - tests/test_guild_config_logic.py (whole file) — the mock-free convention: plain dict/None inputs, the purity self-check, the required-kwarg TypeError tests, the TestDecideAmbientChannel branch-coverage class
    - logic/guild_config.py — the two additions from Task 1
  </read_first>
  <action>
    Extend `tests/test_guild_config_logic.py` (no new file — this is additive branch coverage to the existing pure test suite, plain dicts only).
    For the silenced branch: add tests that a `{"configured": True, "ambient_channel_id": "500", "ambient_roasts_enabled": True, "silenced": True}` row → decide_ambient_channel returns None for ROAST, VISION, and PRESENCE surfaces; that the same row with `silenced` OMITTED still returns 500 (default-False backward-compat); that `silenced: False` explicitly still returns 500; and that `is_ambient_channel(config_row=<silenced row>, channel_id=500, surface=ROAST)` returns False (dispatch-through proof).
    For decide_interaction_allowed: add a class covering all six behavior rows from Task 1's <behavior> block, plus a required-keyword-only TypeError test (call it positionally / omit an arg → TypeError), plus assert the source module still imports no discord/datetime/random (extend or rely on the existing purity self-check).
  </action>
  <verify>
    <automated>pytest tests/test_guild_config_logic.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_guild_config_logic.py -q` exits 0 with the new silenced + decide_interaction_allowed cases present
    - A silenced row is asserted to return None for all three AmbientSurface members
    - A row omitting `silenced` is asserted to still resolve its channel (default-False)
    - decide_interaction_allowed has explicit owner-exempt, DM-exempt, blocked-refuse, silenced-refuse, and allow-all-clear assertions
    - No mocks, no clocks, no RNG in the added tests (plain dicts / bools only)
  </acceptance_criteria>
  <done>Exhaustive mock-free coverage locks the silenced branch and the interaction predicate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached config row → pure decision | The (untrusted-format) `silenced`/`configured`/toggle values arrive as a plain mapping; the function must degrade to silence on any uncertainty, never raise |
| authorization inputs → predicate | is_owner/has_guild/blocked/silenced are booleans computed by the glue; the predicate is the single authorization decision point for slash commands (OWNER-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Tampering (authorization state) | decide_ambient_channel silenced branch | mitigate | Silence is a structural early-return; every ambient resolve routes through it (OWNER-05). Full TOCTOU close is 20-05's pre-send re-check that calls this again |
| T-20-06 | Denial of Service (self) | decide_interaction_allowed owner exemption | mitigate | `is_owner: return True` FIRST — the owner is never locked out even by self-silencing the home guild (D-13); locked by an explicit test |
| T-20-03 | Tampering (block/leave race) | decide_interaction_allowed both-flags check | mitigate | `blocked or silenced` refuses on EITHER flag defensively (D-13), covering the block-written-while-leave-in-flight window |
| T-20-SC | Tampering (supply chain) | package installs | mitigate | Zero new installs; nothing to audit |
</threat_model>

<verification>
- `pytest tests/test_guild_config_logic.py -q` green
- The existing test `test_module_imports_no_discord_datetime_or_random` still passes (module stayed pure)
- Full suite `pytest tests/ -q` green (existing decide_ambient_channel tests unaffected by the default-False silenced branch)
</verification>

<success_criteria>
- decide_ambient_channel silences a silenced guild; is_ambient_channel inherits it
- decide_interaction_allowed encodes the full D-13 predicate, mock-free-tested
- logic/guild_config.py remains import-pure (no discord/datetime/random)
</success_criteria>

<output>
Create `.planning/phases/20-owner-control-plane-rate-observability/20-02-SUMMARY.md` when done
</output>
