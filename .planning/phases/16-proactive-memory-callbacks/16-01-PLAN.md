---
phase: 16-proactive-memory-callbacks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - logic/proactive.py
  - tests/test_proactive_logic.py
autonomous: true
requirements: [PROACT-01]
must_haves:
  truths:
    - "should_fire_proactive_callback returns False when opted_out, when chance_roll >= chance, or when daily_count >= daily_cap; True only when all three gates pass"
    - "PROACTIVE_CALLBACK_CHANCE is strictly less than both UNPROMPTED_ROAST_CHANCE (0.30) and MEMORY_CALLBACK_CHANCE (0.35)"
    - "The gate is pure: no random, datetime, asyncio, or discord import in logic/proactive.py"
  artifacts:
    - path: "logic/proactive.py"
      provides: "should_fire_proactive_callback pure decision gate"
      contains: "def should_fire_proactive_callback"
      min_lines: 25
    - path: "config.py"
      provides: "PROACTIVE_CALLBACK_CHANCE + PROACTIVE_CALLBACK_DAILY_CAP knobs"
      contains: "PROACTIVE_CALLBACK_CHANCE"
    - path: "tests/test_proactive_logic.py"
      provides: "mock-free boundary coverage + config-rarity invariant"
      contains: "should_fire_proactive_callback"
  key_links:
    - from: "logic/proactive.py"
      to: "config.PROACTIVE_CALLBACK_CHANCE"
      via: "default keyword argument"
      pattern: "config\\.PROACTIVE_CALLBACK_CHANCE"
---

<objective>
Establish the deterministic firing-decision seam for Phase 16's rarest ambient cadence:
the pure `logic/proactive.py::should_fire_proactive_callback` gate (D-02 primitives) plus the
two `config.py` knobs it defaults from. This is the "lock the gate math first" foundation —
mock-free, side-effect-free, and independently testable before any Discord glue exists.

Purpose: The anti-creepy discipline is the deliverable (CONTEXT.md domain). The gate ordering
(opt-out → chance → daily-cap) and the strict-rarity config invariant are the mechanical heart
of that discipline; extracting them into a pure module (Phase 10 convention) makes them provable
without a running bot.
Output: `logic/proactive.py`, two `config.py` knobs, `tests/test_proactive_logic.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-proactive-memory-callbacks/16-CONTEXT.md
@.planning/phases/16-proactive-memory-callbacks/16-RESEARCH.md
@.planning/phases/16-proactive-memory-callbacks/16-PATTERNS.md
</context>

<artifacts_this_phase>
Full Phase 16 symbol inventory (so any executor sees the whole surface). **Bold = produced by
THIS plan.**
- **`logic/proactive.py` — `should_fire_proactive_callback` (pure gate)**
- **`config.PROACTIVE_CALLBACK_CHANCE`, `config.PROACTIVE_CALLBACK_DAILY_CAP`**
- **`tests/test_proactive_logic.py`**
- `database.get_proactive_opt_out`, `database.set_proactive_opt_out`, `user_profiles.proactive_opt_out` column (plan 16-02)
- `tests/test_database_phase16.py` (plan 16-02)
- `cogs/events.py::_maybe_fire_proactive_callback`, `_generate_ambient_roast(pre_recalled_memories=...)`, `EventsCog._proactive_daily_counts`, `personality/roasts.py::PROACTIVE_CALLBACK_FALLBACKS` (plan 16-03)
- `tests/test_proactive_events.py`, `tests/test_ambient_recall_cadence.py` additions (plan 16-03)
- `cogs/memory.py::MemoryCog.memory_callbacks` subcommand (plan 16-04)
- `tests/test_memory_command.py` additions (plan 16-04)
</artifacts_this_phase>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config knobs + pure should_fire_proactive_callback gate</name>
  <files>config.py, logic/proactive.py</files>
  <read_first>
    - `logic/roasts.py` (full) — the exact template: module docstring "no random/datetime/discord"
      disclaimer, keyword-only signature, short-circuit gate ordering (cheapest gate first),
      `chance_roll >= chance` fail convention, Google-style Args/Returns docstring.
    - `config.py:160-193` (Phase 11 `MEMORY_*` block) — `MEMORY_CALLBACK_CHANCE = 0.35`,
      `MEMORY_SIMILARITY_FLOOR = 0.70`; `config.py:216-225` (Phase 14 block) and the
      `sanitize_database_url` definition at line 227 — the insertion point is immediately before it.
    - `config.py` §Phase 3 `UNPROMPTED_ROAST_CHANCE = 0.30` — the ambient rate the new chance
      must undercut.
    - 16-RESEARCH.md §Pattern 1 (recommended gate shape) + §Common Pitfalls; 16-PATTERNS.md
      §`logic/proactive.py` (NEW).
  </read_first>
  <action>
    In `config.py`, insert a new `# --- Phase 16: Proactive Memory Callbacks ---` section AFTER
    the Phase 14 block and BEFORE the `sanitize_database_url` function definition (~line 226).
    Add exactly two knobs: `PROACTIVE_CALLBACK_CHANCE = 0.10` (planner discretion within the
    D-02 0.08–0.12 band; comment MUST state it stays strictly < UNPROMPTED_ROAST_CHANCE (0.30)
    and < MEMORY_CALLBACK_CHANCE (0.35)) and `PROACTIVE_CALLBACK_DAILY_CAP = 1` (per-user, per
    calendar day). Do not add any per-user cooldown knob — the daily cap is the additive bound
    D-02 specifies; belt-and-suspenders cooldown is explicitly optional and omitted here.

    Create `logic/proactive.py` mirroring `logic/roasts.py` structure. Open with a module
    docstring stating all functions are deterministic and side-effect-free (no `random`,
    `datetime`, `asyncio`, `discord`); `import config` only. Define
    `should_fire_proactive_callback(*, opted_out: bool, chance_roll: float, daily_count: int,
    chance: float = config.PROACTIVE_CALLBACK_CHANCE, daily_cap: int = config.PROACTIVE_CALLBACK_DAILY_CAP) -> bool`.
    Gate order (short-circuit, cheapest first, mirroring D-02 steps 1-3 and
    `decide_ambient_roast`'s convention): return False if `opted_out`; return False if
    `chance_roll >= chance` (strict `<` to proceed — identical boundary convention to
    `decide_ambient_roast`); return False if `daily_count >= daily_cap` (inclusive ceiling —
    cap is a ceiling, so `>=` fails); else return True. Document in the docstring that D-02
    step 4 (the recall-floor silent-skip) is deliberately NOT in this function — it is async I/O
    and lives in the cog glue (plan 16-03). Write a full Google-style Args/Returns docstring
    naming where each primitive is computed in glue (`random.random()` for `chance_roll`, the
    in-memory per-user counter for `daily_count`, `database.get_proactive_opt_out` for
    `opted_out`).
  </action>
  <behavior>
    Truth table locked by Task 2 (all other keyword args at their config defaults unless noted):
    - opted_out=True (any rolls) → False
    - opted_out=False, chance_roll = chance (exactly at threshold) → False (boundary: `>=` fails)
    - opted_out=False, chance_roll = chance - 0.01, daily_count=0, daily_cap=1 → True
    - opted_out=False, chance_roll below chance, daily_count = daily_cap (at ceiling) → False
    - opted_out=False, chance_roll below chance, daily_count = daily_cap - 1 → True
    - custom chance/daily_cap overrides are honored (not hard-coded to the config constants)
    - config invariant: PROACTIVE_CALLBACK_CHANCE < UNPROMPTED_ROAST_CHANCE and < MEMORY_CALLBACK_CHANCE
  </behavior>
  <verify>
    <automated>python -c "import config, logic.proactive as p; assert config.PROACTIVE_CALLBACK_CHANCE < config.UNPROMPTED_ROAST_CHANCE < 1 and config.PROACTIVE_CALLBACK_CHANCE < config.MEMORY_CALLBACK_CHANCE; assert p.should_fire_proactive_callback(opted_out=False, chance_roll=0.0, daily_count=0) is True; assert p.should_fire_proactive_callback(opted_out=True, chance_roll=0.0, daily_count=0) is False; import inspect,re; src=inspect.getsource(p); assert not re.search(r'^\s*import\s+(random|datetime|asyncio|discord)', src, re.M), 'gate must be pure'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `logic/proactive.py` defines `should_fire_proactive_callback` with the exact keyword-only
      signature above (verify via `inspect.signature`).
    - Source contains no `import random`, `import datetime`, `import asyncio`, or `import discord`.
    - `config.PROACTIVE_CALLBACK_CHANCE` (0.10) is strictly < 0.30 and < 0.35;
      `config.PROACTIVE_CALLBACK_DAILY_CAP` (1) exists as an int.
    - The one-liner verify above prints `OK` and exits 0.
  </acceptance_criteria>
  <done>The pure gate and both config knobs exist; the gate is import-clean of nondeterminism and
  the config rarity invariant holds.</done>
</task>

<task type="auto">
  <name>Task 2: Mock-free boundary tests + config rarity invariant</name>
  <files>tests/test_proactive_logic.py</files>
  <read_first>
    - `tests/test_roast_logic.py` (full) — the exact mock-free convention: config-derived
      boundary constants (no magic numbers), class-grouped branch/boundary tests, plain
      `assert should_fire_proactive_callback(...) is True/False`, no `pytest.mark.asyncio`,
      no mocks.
    - `logic/proactive.py` (from Task 1) — the function under test.
    - 16-VALIDATION.md §Per-Task Verification Map rows for PROACT-01 (`test_proactive_logic.py`
      and the `rarer_than_ambient` selector).
  </read_first>
  <action>
    Create `tests/test_proactive_logic.py` mirroring `test_roast_logic.py`'s structure. Derive
    boundary constants from config (e.g. `CHANCE_PASS = config.PROACTIVE_CALLBACK_CHANCE - 0.01`,
    `CHANCE_FAIL = config.PROACTIVE_CALLBACK_CHANCE`) so tests track config, not literals. Cover
    every branch/boundary from the Task 1 behavior table in a `TestShouldFireProactiveCallback`
    class: opt-out short-circuit (True regardless of rolls), chance boundary (exactly-at-threshold
    fails, one-under passes), daily-cap boundary (at-cap fails, one-under passes), and
    custom-threshold overrides (pass explicit `chance=`/`daily_cap=` and assert they are honored,
    not the config defaults). Add a standalone `test_proactive_chance_is_rarer_than_ambient`
    (matched by `-k rarer_than_ambient`) asserting
    `config.PROACTIVE_CALLBACK_CHANCE < config.UNPROMPTED_ROAST_CHANCE` AND
    `config.PROACTIVE_CALLBACK_CHANCE < config.MEMORY_CALLBACK_CHANCE`. No mocks anywhere — if a
    test needs a mock, the cut-line in `logic/proactive.py` is wrong (Phase 10 D-06 discipline).
  </action>
  <verify>
    <automated>pytest tests/test_proactive_logic.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_proactive_logic.py -x` passes with the file containing zero `mock`,
      zero `patch`, zero `pytest.mark.asyncio` usages (pure unit convention).
    - A test matched by `pytest tests/test_proactive_logic.py -k rarer_than_ambient -x` exists
      and passes.
    - Both chance and daily-cap boundary tests assert the exactly-at-threshold case returns False.
  </acceptance_criteria>
  <done>The gate math and the rarity invariant are locked by a durable, mock-free test file that
  passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| glue → pure gate | The cog (plan 16-03) computes all nondeterminism (random roll, clock-dated counter, DB-sourced opt-out flag) and passes primitives across into this pure function; the gate itself trusts its inputs and performs no I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-RARITY | Tampering (cadence-discipline bypass) | `config.PROACTIVE_CALLBACK_CHANCE` | mitigate | `test_proactive_chance_is_rarer_than_ambient` fails immediately if a future edit raises the chance to/above the ambient 0.30/0.35 rates — the anti-creepy invariant is test-enforced, not comment-enforced. |
| T-16-GATE | Tampering (gate short-circuit weakened) | `should_fire_proactive_callback` | mitigate | Exhaustive mock-free boundary tests lock opt-out-first ordering and both `>=` ceilings; any accidental `<=`/`>` slip flips a boundary test red. |
| T-16-SC | Tampering | pip installs | accept | No package installs in this phase (zero new dependencies — RESEARCH.md §Package Legitimacy Audit N/A). No supply-chain surface. |
</threat_model>

<verification>
- `pytest tests/test_proactive_logic.py -x` green.
- Wave-merge gate: `pytest -x` (full suite) stays green — this plan is purely additive (new
  module + new config knobs + new test file), so no existing test should change behavior.
</verification>

<success_criteria>
- `logic/proactive.py::should_fire_proactive_callback` exists, is pure, and enforces the D-02
  opt-out → chance → daily-cap gate order with the documented boundary conventions.
- `config.PROACTIVE_CALLBACK_CHANCE` (0.10) < 0.30 and < 0.35; `config.PROACTIVE_CALLBACK_DAILY_CAP` = 1.
- The mock-free test file passes and locks both the gate math and the rarity invariant.
</success_criteria>

<output>
Create `.planning/phases/16-proactive-memory-callbacks/16-01-SUMMARY.md` when done.
</output>
