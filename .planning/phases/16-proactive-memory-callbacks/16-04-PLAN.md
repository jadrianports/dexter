---
phase: 16-proactive-memory-callbacks
plan: 04
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - cogs/memory.py
  - tests/test_memory_command.py
autonomous: true
requirements: [PROACT-02]
must_haves:
  truths:
    - "/memory callbacks off sets the opt-out flag; /memory callbacks on clears it"
    - "Both settings reply ephemerally, in-character, and touch zero memory rows"
    - "The callbacks subcommand is self-scoped: signature exactly [self, interaction, setting]; no target/user param"
    - "Input is app_commands.Choice-constrained to on/off (no free-text parsing surface)"
  artifacts:
    - path: "cogs/memory.py"
      provides: "memory_callbacks subcommand under the /memory group"
      contains: "name=\"callbacks\""
    - path: "tests/test_memory_command.py"
      provides: "off/on round-trip, self-scoped guard, ephemeral assertions for callbacks"
      contains: "callbacks"
  key_links:
    - from: "cogs/memory.py::memory_callbacks"
      to: "database.set_proactive_opt_out"
      via: "self-scoped opt-out write"
      pattern: "set_proactive_opt_out"
---

<objective>
Add the user-facing opt-out control for PROACT-02: a `callbacks on|off` subcommand under the
existing `/memory` `app_commands.Group`, indefinite toggle, self-scoped, ephemeral, in-character
(D-03). It writes the `proactive_opt_out` flag via `database.set_proactive_opt_out` (plan 16-02)
and touches ZERO memory rows — the "pause callbacks for me" promise that is distinct from
`/memory forget`.

Purpose: The opt-out must be honored instantly and completely while leaving the taste brain
intact — a user tired of being called out can silence the surface without nuking their memories.
The `/memory` group is the natural home (it is where the user already goes to view/forget), and
the self-scoping + ephemeral + Choice-constrained conventions are already proven by the two
sibling subcommands.
Output: `cogs/memory.py::MemoryCog.memory_callbacks`, `tests/test_memory_command.py` additions.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-proactive-memory-callbacks/16-CONTEXT.md
@.planning/phases/16-proactive-memory-callbacks/16-RESEARCH.md
@.planning/phases/16-proactive-memory-callbacks/16-PATTERNS.md
</context>

<artifacts_this_phase>
Full Phase 16 symbol inventory. **Bold = produced by THIS plan.** (Consumes 16-02.)
- `logic/proactive.py::should_fire_proactive_callback`, `config.PROACTIVE_CALLBACK_*` (plan 16-01)
- `database.set_proactive_opt_out(pool, *, user_id, opted_out)` (16-02) — CONSUMED here
- `database.get_proactive_opt_out`, `user_profiles.proactive_opt_out` column (plan 16-02)
- `cogs/events.py` proactive glue + `personality/roasts.py::PROACTIVE_CALLBACK_FALLBACKS` (plan 16-03)
- **`cogs/memory.py::MemoryCog.memory_callbacks` subcommand**
- **`tests/test_memory_command.py` additions (`test_memory_callbacks_*`)**
</artifacts_this_phase>

<tasks>

<task type="auto">
  <name>Task 1: /memory callbacks on|off subcommand</name>
  <files>cogs/memory.py</files>
  <read_first>
    - `cogs/memory.py:1-42` (module + Security docstring) and `cogs/memory.py:224-289`
      (`MemoryCog`, the `memory` `app_commands.Group`, `memory_view`, `memory_forget`) — the exact
      subcommand shape: `@memory.command(name=..., description=...)`, `user_id =
      str(interaction.user.id)`, no `target` param, `ephemeral=True`, short lowercase in-character
      strings.
    - `database.set_proactive_opt_out(pool, *, user_id, opted_out)` (plan 16-02) — the write.
    - 16-RESEARCH.md §Pattern 4 (`/memory callbacks` subcommand, verified shape) + §Security
      Domain (V4 self-scoping, V5 Choice-constrained input); 16-CONTEXT.md D-03.
  </read_first>
  <action>
    Add a `memory_callbacks` subcommand to `MemoryCog` under the existing `memory` group:
    `@memory.command(name="callbacks", description="Pause or resume Dexter's occasional unprompted
    callbacks")` with `@app_commands.describe(setting="on to resume, off to pause")` and
    `@app_commands.choices(setting=[app_commands.Choice(name="on", value="on"),
    app_commands.Choice(name="off", value="off")])`. Signature:
    `async def memory_callbacks(self, interaction: discord.Interaction, setting:
    app_commands.Choice[str]) -> None` — exactly `[self, interaction, setting]`, NO `target`/`user`
    param (V4 self-scoping guard, matching the sibling docstring convention). Body: `user_id =
    str(interaction.user.id)`; `opted_out = setting.value == "off"`; `await
    database.set_proactive_opt_out(self.bot.pool, user_id=user_id, opted_out=opted_out)`. Reply
    ephemerally with the D-03 in-character copy: for off, `"fine, i'll keep my mouth shut. your
    memories are still here though."`; for on, `"back on. don't say i didn't warn you."` — both
    via `interaction.response.send_message(msg, ephemeral=True)`. Update the `MemoryCog` class
    docstring / Security block to add a T-16 note that `callbacks` is self-scoped (no target param)
    and flips only the `user_profiles` opt-out flag — it touches ZERO `user_memories` rows,
    keeping it distinct from `/memory forget`.
  </action>
  <behavior>
    - `memory_callbacks(cog, interaction, Choice("off","off"))` calls
      `set_proactive_opt_out(pool, user_id=str(interaction.user.id), opted_out=True)` and replies
      ephemerally with the "keep my mouth shut" line.
    - `Choice("on","on")` calls `set_proactive_opt_out(..., opted_out=False)` and replies with the
      "back on" line.
    - `inspect.signature(MemoryCog.memory_callbacks.callback).parameters` == `["self",
      "interaction", "setting"]` (no target/user param).
    - The subcommand never calls `delete_all_user_memories`, `insert_memory`, or any
      `user_memories` helper.
  </behavior>
  <verify>
    <automated>python -c "import inspect, cogs.memory as m; params=list(inspect.signature(m.MemoryCog.memory_callbacks.callback).parameters); assert params==['self','interaction','setting'], params; src=inspect.getsource(m.MemoryCog.memory_callbacks.callback); assert 'set_proactive_opt_out' in src and 'ephemeral=True' in src and 'user_memories' not in src and 'delete_all_user_memories' not in src; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `memory_callbacks` exists under the `memory` group with `name="callbacks"` and
      `app_commands.Choice`-constrained `on`/`off` input.
    - Callback signature is exactly `["self", "interaction", "setting"]` (no target param).
    - Body writes via `database.set_proactive_opt_out(..., opted_out=...)`, replies
      `ephemeral=True` with the D-03 lines, and references no `user_memories` helper.
    - The one-liner verify prints `OK`.
  </acceptance_criteria>
  <done>`/memory callbacks on|off` exists, is self-scoped and Choice-constrained, flips only the
  opt-out flag, and confirms ephemerally in-character.</done>
</task>

<task type="auto">
  <name>Task 2: Callbacks subcommand tests + extended self-scoping guard</name>
  <files>tests/test_memory_command.py</files>
  <read_first>
    - `tests/test_memory_command.py` (full) — the `_make_bot`/`_make_interaction` helpers, the
      `.callback(cog, interaction, ...)` invocation convention, and the existing
      `test_memory_subcommands_have_no_target_param` structural guard to extend.
    - `cogs/memory.py::memory_callbacks` (from Task 1) — the code under test.
    - 16-VALIDATION.md §Per-Task Verification Map PROACT-02 (`-k callbacks`) + §Wave 0
      Requirements (the three new `test_memory_callbacks_*` cases).
  </read_first>
  <action>
    Extend `tests/test_memory_command.py` in place (mirror its mock style; patch
    `database.set_proactive_opt_out` as an `AsyncMock`; build a `Choice` via
    `discord.app_commands.Choice(name="off", value="off")`):
    - `test_memory_callbacks_off_then_on`: call the callback with the `off` choice → assert
      `set_proactive_opt_out` awaited once with `opted_out=True` and `user_id ==
      str(interaction.user.id)`; call with the `on` choice → assert awaited with `opted_out=False`.
    - `test_memory_callbacks_response_ephemeral`: assert `interaction.response.send_message` was
      awaited with `ephemeral=True` for both settings.
    - `test_memory_callbacks_is_self_scoped`: assert
      `inspect.signature(MemoryCog.memory_callbacks.callback).parameters` == `["self",
      "interaction", "setting"]` — no `target`/`user` param (extends the file's existing V4
      structural-guard style to the new subcommand).
    - Extend the existing `test_memory_subcommands_have_no_target_param` (or add a companion) so
      the no-target guarantee explicitly covers `memory_callbacks` too.
    - `test_memory_callbacks_touches_no_memories`: source-inspection asserting the callback source
      contains neither `delete_all_user_memories` nor `user_memories` (structural distinctness
      from `/memory forget`).
  </action>
  <verify>
    <automated>pytest tests/test_memory_command.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_memory_command.py -x` passes, including all prior tests.
    - `pytest tests/test_memory_command.py -k callbacks -x` matches and passes the new cases.
    - A self-scoping guard asserts `memory_callbacks.callback` has exactly `["self",
      "interaction", "setting"]`.
    - The off/on round-trip asserts the correct `opted_out` boolean and `user_id` scoping on each.
  </acceptance_criteria>
  <done>The opt-out command's toggle behavior, ephemeral replies, self-scoping, and
  distinctness-from-forget are locked by passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord interaction → command | The only identity used is `str(interaction.user.id)`; no cross-user identity is accepted. |
| opt-out control ↔ memory store | `callbacks` must flip only the `user_profiles` flag and never reach `user_memories` — structurally distinct from `/memory forget`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Tampering / Elevation of Privilege | `memory_callbacks` signature | mitigate | Structural self-scoping guard test asserts the callback signature is exactly `["self","interaction","setting"]` — an accidental `target`/`user` param (letting one user toggle another's opt-out) fails immediately (mirrors `test_memory_subcommands_have_no_target_param`). |
| T-16-03 | Input Validation | `setting` argument | mitigate | `app_commands.Choice`-constrained to `on`/`off` — no free-text parsing surface; Discord rejects any other value before the handler runs. |
| T-16-08 | Tampering (control confusion with forget) | `memory_callbacks` body | mitigate | Source test asserts the callback references no `user_memories`/`delete_all_user_memories` — opting out provably touches zero memory rows, preserving the D-03 "pause, not delete" distinction. |
| T-16-SC | Tampering | pip installs | accept | Zero new dependencies this phase. |
</threat_model>

<verification>
- `pytest tests/test_memory_command.py -x` green.
- Wave-merge gate: `pytest -x` full suite green — additive subcommand, no change to `view`/`forget`.
</verification>

<success_criteria>
- `/memory callbacks on|off` exists, is self-scoped, Choice-constrained, and ephemeral.
- `off` sets `opted_out=True`, `on` sets `opted_out=False`, each via
  `database.set_proactive_opt_out` with `user_id == str(interaction.user.id)`.
- The subcommand touches zero memory rows (provably distinct from `/memory forget`).
</success_criteria>

<output>
Create `.planning/phases/16-proactive-memory-callbacks/16-04-SUMMARY.md` when done.
</output>
