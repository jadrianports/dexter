---
phase: 16-proactive-memory-callbacks
plan: 03
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - personality/roasts.py
  - cogs/events.py
  - tests/test_proactive_events.py
  - tests/test_ambient_recall_cadence.py
autonomous: true
requirements: [PROACT-01, PROACT-02]
must_haves:
  truths:
    - "A non-bot chat message in the designated channel evaluates the proactive gate; a DM or a message in any other channel never does"
    - "When the gate passes but recall() returns [], no Discord message is sent (silent skip — no memory beats a wrong memory)"
    - "When it fires, the callback is a message.reply with AllowedMentions.none() and mention_author=False; the per-user daily counter increments ONLY on an actual fire"
    - "_generate_ambient_roast with pre_recalled_memories provided bypasses the internal MEMORY_CALLBACK_CHANCE recall roll; the two voice call sites remain byte-identical (default None)"
    - "The proactive glue never pipes a live-SQL numeric stat into reply text outside the firewalled _generate_ambient_roast path (accuracy firewall)"
  artifacts:
    - path: "cogs/events.py"
      provides: "_maybe_fire_proactive_callback glue + pre_recalled_memories param + _proactive_daily_counts dict"
      contains: "_maybe_fire_proactive_callback"
    - path: "personality/roasts.py"
      provides: "PROACTIVE_CALLBACK_FALLBACKS template pool"
      contains: "PROACTIVE_CALLBACK_FALLBACKS"
    - path: "tests/test_proactive_events.py"
      provides: "behavioral glue proofs (channel gate, silent-skip, reply-anchor, counter)"
      contains: "_maybe_fire_proactive_callback"
  key_links:
    - from: "cogs/events.py::on_message"
      to: "_maybe_fire_proactive_callback"
      via: "guild + DEXTER_CHANNEL_ID gate after _handle_message_reactions"
      pattern: "_maybe_fire_proactive_callback"
    - from: "cogs/events.py::_maybe_fire_proactive_callback"
      to: "logic.proactive.should_fire_proactive_callback"
      via: "pure gate dispatch"
      pattern: "should_fire_proactive_callback"
    - from: "cogs/events.py::_maybe_fire_proactive_callback"
      to: "database.get_proactive_opt_out"
      via: "opt-out read"
      pattern: "get_proactive_opt_out"
    - from: "cogs/events.py::_maybe_fire_proactive_callback"
      to: "message.reply"
      via: "AllowedMentions.none() reply-anchor"
      pattern: "message\\.reply"
---

<objective>
Wire Phase 16's third, rarest ambient cadence into `cogs/events.py`: a chat-anchored proactive
memory callback that fires off a real active moment in the designated channel (D-01), gated by
opt-out → chance → daily-cap (D-02, dispatching on the pure `should_fire_proactive_callback`) →
recall-floor, then generated by the reused `_generate_ambient_roast` pipeline (D-04) and posted
as a reply with mentions suppressed. Includes the Pitfall-1 fix (a `pre_recalled_memories`
parameter that lets the proactive surface bypass the helper's internal chance roll without
disturbing the two voice-event call sites) and a new template fallback pool.

Purpose: This is where the anti-creepy discipline becomes observable behavior — rare,
reply-anchored, Gemini-framed, mention-suppressed, opt-out-honoring, and silent when there is no
real memory to surface. All machinery already exists (Phases 3/10/11/15); this is composition,
not new capability.
Output: `personality/roasts.py::PROACTIVE_CALLBACK_FALLBACKS`, `cogs/events.py`
(`pre_recalled_memories` param + `_proactive_daily_counts` + `_maybe_fire_proactive_callback` +
`on_message` gate), `tests/test_proactive_events.py`, extended `tests/test_ambient_recall_cadence.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-proactive-memory-callbacks/16-CONTEXT.md
@.planning/phases/16-proactive-memory-callbacks/16-RESEARCH.md
@.planning/phases/16-proactive-memory-callbacks/16-PATTERNS.md
@logic/proactive.py
@services/memory.py
</context>

<artifacts_this_phase>
Full Phase 16 symbol inventory. **Bold = produced by THIS plan.** (Consumes 16-01 + 16-02.)
- `logic/proactive.py::should_fire_proactive_callback` (16-01) — CONSUMED here
- `config.PROACTIVE_CALLBACK_CHANCE`, `config.PROACTIVE_CALLBACK_DAILY_CAP` (16-01) — CONSUMED here
- `database.get_proactive_opt_out(pool, user_id)` (16-02) — CONSUMED here
- **`personality/roasts.py::PROACTIVE_CALLBACK_FALLBACKS`**
- **`cogs/events.py::EventsCog._generate_ambient_roast(..., *, pre_recalled_memories=None)`**
- **`cogs/events.py::EventsCog._proactive_daily_counts: dict[str, tuple[str, int]]`**
- **`cogs/events.py::EventsCog._maybe_fire_proactive_callback(message)`**
- **`cogs/events.py::on_message` proactive-gate call**
- **`tests/test_proactive_events.py`; additions to `tests/test_ambient_recall_cadence.py`**
- `cogs/memory.py::MemoryCog.memory_callbacks` subcommand (plan 16-04)
</artifacts_this_phase>

<tasks>

<task type="auto">
  <name>Task 1: Fallback pool + pre_recalled_memories bypass on _generate_ambient_roast (Pitfall 1)</name>
  <files>personality/roasts.py, cogs/events.py</files>
  <read_first>
    - `personality/roasts.py:1-92` — the voice-register LOCKED constraints docstring, the
      `__all__` list (lines 20-38), and `BOT_MOVED_COMPLAINTS` (lines 80-85) as the exact
      shape/register for a new lowercase, contempt-outward, ≤120-char template pool.
    - `cogs/events.py:87-172` (`_generate_ambient_roast`) — the current signature and the
      internal `if random.random() < config.MEMORY_CALLBACK_CHANCE:` recall block (lines 127-138)
      that Pitfall 1 targets.
    - 16-RESEARCH.md §Pitfall 1 (Option B, locked) + §Pattern 2; 16-PATTERNS.md
      §`cogs/events.py` (MODIFIED) — the `pre_recalled_memories` bypass description.
    - `tests/test_ambient_recall_cadence.py::test_ambient_surfaces_retain_gate` — the regression
      lock asserting `"MEMORY_CALLBACK_CHANCE" in` the `_generate_ambient_roast` source; the edit
      must keep that literal present.
  </read_first>
  <action>
    In `personality/roasts.py`, add a new `PROACTIVE_CALLBACK_FALLBACKS: list[str]` pool near
    `BOT_MOVED_COMPLAINTS`, and add `"PROACTIVE_CALLBACK_FALLBACKS"` to `__all__`. Write 4-6
    lowercase, dry, contempt-outward one-liners under 120 chars, following the LOCKED voice rules
    (no self-deprecation, no bot-meta, one emoji max, mild swearing only). These fire only when
    Gemini is unavailable, so they must read as Dex dryly bringing up that he remembers something
    — e.g. referencing "your taste" / "your history" generically (no `{count}` or SQL numbers —
    accuracy firewall). Include an optional `{name}` placeholder in some lines (the helper already
    `.format(name=...)`s fallback lines that contain `{name}`).

    In `cogs/events.py::_generate_ambient_roast`, add a keyword-only parameter
    `pre_recalled_memories: list[str] | None = None` at the end of the signature. Restructure the
    recall step (current lines 127-138): when `pre_recalled_memories is not None`, set
    `amb_memories = pre_recalled_memories` and skip the internal `if random.random() <
    config.MEMORY_CALLBACK_CHANCE:` block and the internal `_memory_svc.recall(...)` call entirely;
    when it is `None`, run the existing block UNCHANGED (byte-identical behavior for the two
    voice-event call sites). The `config.MEMORY_CALLBACK_CHANCE` reference MUST remain literally
    present in the source (do not delete the gate — only make it conditionally bypassable) so
    `test_ambient_surfaces_retain_gate` stays green. Everything downstream
    (`build_chat_prompt(..., memories=amb_memories or None)`, the priority-2 Gemini call,
    lowercase/length enforcement, template fallback) is untouched.
  </action>
  <behavior>
    - Calling `_generate_ambient_roast(member, scenario, pool, pre_recalled_memories=["fact"])`
      does NOT call `memory_service.recall` (internal recall bypassed) and passes the supplied
      memories through to `build_chat_prompt(memories=...)`.
    - Calling `_generate_ambient_roast(member, scenario, pool)` (default None) behaves exactly as
      before — the internal `MEMORY_CALLBACK_CHANCE` roll still gates the internal recall.
    - `PROACTIVE_CALLBACK_FALLBACKS` is a non-empty `list[str]`, all lowercase-leading, each ≤120
      chars, no numeric/{count} placeholders.
  </behavior>
  <verify>
    <automated>python -c "import inspect, personality.roasts as r, cogs.events as e; assert isinstance(r.PROACTIVE_CALLBACK_FALLBACKS, list) and r.PROACTIVE_CALLBACK_FALLBACKS and 'PROACTIVE_CALLBACK_FALLBACKS' in r.__all__; assert all(len(x)<=120 for x in r.PROACTIVE_CALLBACK_FALLBACKS); p=inspect.signature(e.EventsCog._generate_ambient_roast).parameters; assert 'pre_recalled_memories' in p and p['pre_recalled_memories'].default is None; src=inspect.getsource(e.EventsCog._generate_ambient_roast); assert 'MEMORY_CALLBACK_CHANCE' in src and 'pre_recalled_memories' in src; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `personality.roasts.PROACTIVE_CALLBACK_FALLBACKS` is a non-empty `list[str]` in `__all__`,
      every line ≤120 chars with no numeric placeholders.
    - `_generate_ambient_roast` has a keyword-only `pre_recalled_memories: list[str] | None = None`
      parameter defaulting to `None`.
    - The source still contains `MEMORY_CALLBACK_CHANCE` (gate retained, made bypassable).
    - The one-liner verify prints `OK`.
  </acceptance_criteria>
  <done>The reusable pieces exist: a proactive fallback pool and a backward-compatible bypass on
  the shared roast generator that does not disturb the voice-event call sites.</done>
</task>

<task type="auto">
  <name>Task 2: _maybe_fire_proactive_callback glue + on_message gate + daily-counter dict</name>
  <files>cogs/events.py</files>
  <read_first>
    - `cogs/events.py:27-40` (`EventsCog.__init__` + `self._ambient_roast_times: dict[int, float]`
      at line 33) — the per-cog in-memory dict convention to mirror for `_proactive_daily_counts`.
    - `cogs/events.py:347-364` (`on_message`) — the insertion anchor: add the gate AFTER
      `_handle_message_reactions(message)`, guarded on `message.guild is not None` and
      `config.DEXTER_CHANNEL_ID` channel-id equality (Pitfall 2 — never let a DM/no-guild message
      reach the gate; a Member with `.guild` is required by `_generate_ambient_roast`).
    - `cogs/events.py:44-83` (`_get_ambient_channel`) — how `config.DEXTER_CHANNEL_ID` is used;
      the proactive gate confirms the channel by id equality, it does not need the fallback chain.
    - `cogs/events.py:124-172` (recall + reply conventions) and `cogs/events.py:193-196`
      (`AllowedMentions.none()` on an ambient send) — the mention-suppression convention; the new
      code uses `message.reply(..., allowed_mentions=discord.AllowedMentions.none(),
      mention_author=False)` instead of `channel.send`.
    - `services/memory.py::recall(user_id, guild_id, query_text, kind=None) -> list[str]` — the
      author-scoped read that returns `[]` below `MEMORY_SIMILARITY_FLOOR` (the silent-skip path).
    - 16-RESEARCH.md §Code Examples (full `on_message` gate glue, recommended shape) + §Pitfalls
      1/2/4; 16-CONTEXT.md D-01/D-02/D-04.
  </read_first>
  <action>
    In `EventsCog.__init__`, add `self._proactive_daily_counts: dict[str, tuple[str, int]] = {}`
    (str-keyed per D-02 / RESEARCH Open Question 2 — matches the `str(user.id)` convention used
    by recall/database everywhere else, distinct from the int-keyed `_ambient_roast_times`).

    Add an `import config` usage plus module-level imports for
    `from logic.proactive import should_fire_proactive_callback` and `import database` (confirm
    whether `database` is already imported; add if missing). `personality.roasts` (`roasts`) and
    `random` are already imported.

    In `on_message`, after `await self._handle_message_reactions(message)`, add:
    `if message.guild is not None and config.DEXTER_CHANNEL_ID and message.channel.id ==
    config.DEXTER_CHANNEL_ID:` then `await self._maybe_fire_proactive_callback(message)`. Do NOT
    duplicate the top-of-function `if message.author.bot: return` guard (it already ran).

    Add `async def _maybe_fire_proactive_callback(self, message: discord.Message) -> None`
    implementing the D-02 firing order exactly, short-circuit cheapest-first:
    (1) `user_id = str(message.author.id)`; read `opted_out = await
    database.get_proactive_opt_out(self.bot.pool, user_id)`.
    (2) Compute `today` = `datetime.now(tz=ZoneInfo(config.STREAK_TIMEZONE)).date().isoformat()`
    (the documented community-time convention — reuse the same `import datetime as _dt` /
    `from zoneinfo import ZoneInfo` pattern already used in `on_voice_state_update` at
    cogs/events.py:206-208; never naive `datetime.now()`). Read the stored
    `(last_date, count)` from `self._proactive_daily_counts.get(user_id, (today, 0))`; set
    `daily_count = count if last_date == today else 0` (implicit day-rollover by date comparison).
    (3) Call `should_fire_proactive_callback(opted_out=opted_out, chance_roll=random.random(),
    daily_count=daily_count)` (config defaults for chance/cap). Return early if it is False.
    (4) Resolve `memory_service = getattr(self.bot, "memory_service", None)`; return if None.
    Call `memories = await memory_service.recall(user_id, str(message.guild.id), "a proactive
    callback moment")` inside a try/except that degrades to `[]` on error (non-fatal, mirrors the
    existing recall try/except). If `not memories`: return (D-02 step 4 / Pitfall 8 silent-skip —
    NO send, NO counter increment).
    (5) Call `line = await self._generate_ambient_roast(message.author, "{name} is here and
    dexter has a thought", roasts.PROACTIVE_CALLBACK_FALLBACKS, pre_recalled_memories=memories)`
    (Pitfall 1 bypass — the glue already did the floor-checked recall).
    (6) In a `try: ... except discord.HTTPException: pass`, `await message.reply(line,
    allowed_mentions=discord.AllowedMentions.none(), mention_author=False)` and, ONLY after a
    successful send, set `self._proactive_daily_counts[user_id] = (today, daily_count + 1)` (D-02:
    the counter increments only on an actual fire).

    Accuracy firewall: the glue must NOT read any live-SQL numeric-stat helper
    (`get_user_summary` count fields, streaks, play counts) into the reply text — the only number
    source stays inside the already-firewalled `_generate_ambient_roast`/`build_chat_prompt` path.
    Do not import or call `get_user_summary` / `get_user_top_artist` in this method.
  </action>
  <verify>
    <automated>python -c "import inspect, cogs.events as e; src=inspect.getsource(e.EventsCog._maybe_fire_proactive_callback); assert 'get_proactive_opt_out' in src and 'should_fire_proactive_callback' in src and 'recall' in src and 'message.reply' in src and 'AllowedMentions.none()' in src and 'STREAK_TIMEZONE' in src; assert 'get_user_summary' not in src and 'get_user_top_artist' not in src; om=inspect.getsource(e.EventsCog.on_message); assert 'DEXTER_CHANNEL_ID' in om and 'message.guild is not None' in om and '_maybe_fire_proactive_callback' in om; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `EventsCog.__init__` defines `self._proactive_daily_counts: dict[str, tuple[str, int]] = {}`.
    - `on_message` calls `_maybe_fire_proactive_callback` only inside a `message.guild is not None`
      + `config.DEXTER_CHANNEL_ID` equality guard placed after `_handle_message_reactions`.
    - `_maybe_fire_proactive_callback` executes the D-02 order (opt-out → chance/cap gate → recall
      → generate → reply), uses `ZoneInfo(config.STREAK_TIMEZONE)` for the day key, replies with
      `AllowedMentions.none()` + `mention_author=False`, and increments the counter only after a
      successful send.
    - The method references no live-SQL numeric-stat helper (accuracy firewall).
    - The one-liner verify prints `OK`.
  </acceptance_criteria>
  <done>The proactive surface is wired: a designated-channel chat message runs the full gate chain
  and, when everything passes, produces exactly one mention-suppressed reply anchored to that
  message, counting only real fires.</done>
</task>

<task type="auto">
  <name>Task 3: Behavioral glue tests + Pitfall-1 regression lock</name>
  <files>tests/test_proactive_events.py, tests/test_ambient_recall_cadence.py</files>
  <read_first>
    - `tests/test_ambient_recall_cadence.py` (full) — the `_make_bot`/`_make_interaction` mock
      helpers, `AsyncMock` recall, `spec=discord.*` mock style, and the existing
      `test_ambient_surfaces_retain_gate` (which must stay green unmodified).
    - `cogs/events.py::_maybe_fire_proactive_callback` and `_generate_ambient_roast` (from Tasks
      1-2) — the code under test.
    - 16-VALIDATION.md §Per-Task Verification Map (the `recall_floor`, `reply_anchor`,
      `accuracy_firewall`, and `pre_recalled` selectors) + §Wave 0 Requirements.
  </read_first>
  <action>
    Create `tests/test_proactive_events.py` mirroring `test_ambient_recall_cadence.py`'s mock
    style (fake bot with `pool`, `memory_service.recall` as `AsyncMock`, `gemini_service.chat` as
    `AsyncMock`; a fake `discord.Message` with `.reply` as `AsyncMock`, `.guild`, `.author`
    (a `discord.Member` spec with `.id`, `.display_name`, `.bot=False`), `.channel.id`). Patch
    `database.get_proactive_opt_out` (AsyncMock). Build an `EventsCog` and call
    `_maybe_fire_proactive_callback` directly. Tests:
    - `test_recall_floor_silent_skip` (`-k recall_floor`): `should_fire_proactive_callback` forced
      to pass (patch `random.random` low / opt-out False / empty counter) but `recall` returns
      `[]` → assert `message.reply` is never awaited and no exception is raised, and the daily
      counter for that user is NOT incremented.
    - `test_reply_anchor` (`-k reply_anchor`): gate passes, `recall` returns `["fact"]`,
      `_generate_ambient_roast` patched/allowed to return a line → assert `message.reply` awaited
      once with `allowed_mentions == discord.AllowedMentions.none()` (or an AllowedMentions with
      everything suppressed) and `mention_author=False`; assert `channel.send` is NOT used.
    - `test_daily_counter_increments_only_on_fire`: after a successful fire the user's
      `_proactive_daily_counts` entry count is 1; a gate-skip (opt-out True) leaves it absent/0.
    - `test_opted_out_short_circuits`: `get_proactive_opt_out` returns True → `recall` never
      awaited, `message.reply` never awaited.
    - `test_non_designated_channel_skips`: drive `on_message` (or assert the guard) so a message
      whose `channel.id != config.DEXTER_CHANNEL_ID` never calls `_maybe_fire_proactive_callback`.
    - `test_accuracy_firewall` (`-k accuracy_firewall`): source-inspection asserting
      `inspect.getsource(EventsCog._maybe_fire_proactive_callback)` contains no `get_user_summary`
      / `get_user_top_artist` reference (numbers stay inside the firewalled generator).

    Extend `tests/test_ambient_recall_cadence.py` in place: keep `test_ambient_surfaces_retain_gate`
    unmodified and passing; add `test_pre_recalled_bypasses_internal_recall` (`-k pre_recalled`)
    proving that calling `_generate_ambient_roast(..., pre_recalled_memories=["fact"])` does NOT
    await `memory_service.recall` (internal gate bypassed) while still producing output, whereas
    the default `pre_recalled_memories=None` path leaves the internal `MEMORY_CALLBACK_CHANCE`
    recall gate in effect.
  </action>
  <verify>
    <automated>pytest tests/test_proactive_events.py tests/test_ambient_recall_cadence.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_proactive_events.py tests/test_ambient_recall_cadence.py -x` passes.
    - Tests matched by `-k recall_floor`, `-k reply_anchor`, `-k accuracy_firewall`, and
      `-k pre_recalled` all exist and pass.
    - `test_ambient_surfaces_retain_gate` remains present and green (voice-event gate intact).
    - The reply-anchor test asserts mention suppression AND `message.reply` (never `channel.send`).
  </acceptance_criteria>
  <done>The glue's channel gate, silent-skip, reply-anchor + mention-suppression, fire-only counter
  increment, opt-out short-circuit, accuracy firewall, and the Pitfall-1 bypass are all locked by
  passing behavioral tests; the voice-event regression lock still holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord Gateway → on_message | Untrusted per-message input crosses here; a DM, webhook, or app-integration "user" could reach the handler. The proactive gate must run only for a real guild member in the designated channel. |
| bot → designated channel | The callback is posted into a semi-public channel (not a DM); every fact reaching it already passed the upstream distill-time sensitivity gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Denial of Service (crash of `on_message` stalls all Gateway dispatch) | `_maybe_fire_proactive_callback` entry | mitigate | Gate is entered only inside `message.guild is not None` + `config.DEXTER_CHANNEL_ID` id-equality (Pitfall 2), so a no-`.guild` User never reaches `_generate_ambient_roast`'s `member.guild.id`; recall is wrapped in a degrade-to-`[]` try/except; the send is wrapped in `except discord.HTTPException`. |
| T-16-04 | Information Disclosure (surfacing a sensitive fact in a semi-public channel) | recalled fact → reply | accept (inherited) | Sensitive/PII facts were filtered at distill/write time by `models/memory.py::is_sensitive()` (Phase 11 unconditional backstop). No re-verification here; the accuracy-firewall source test additionally proves no live-SQL number leaks into reply text. |
| T-16-07 | Spoofing / mention-injection via fact text | `message.reply` payload | mitigate | `allowed_mentions=discord.AllowedMentions.none()` + `mention_author=False` on the reply — reference the user by display name, never ping; locked by the `reply_anchor` test. |
| T-16-02b | Tampering (cadence regression) | `_generate_ambient_roast` bypass | mitigate | `test_ambient_surfaces_retain_gate` + the new `pre_recalled` test ensure the voice-event `MEMORY_CALLBACK_CHANCE` gate stays intact while only the proactive caller bypasses it. |
| T-16-SC | Tampering | pip installs | accept | Zero new dependencies this phase. |
</threat_model>

<verification>
- `pytest tests/test_proactive_events.py tests/test_ambient_recall_cadence.py -x` green.
- Wave-merge gate: `pytest -x` full suite green — proves the two voice-event call sites of
  `_generate_ambient_roast` remain byte-identical (the `pre_recalled_memories=None` default) and
  no prior ambient/recall test regressed.
</verification>

<success_criteria>
- A non-bot message in the designated channel runs the D-02 gate chain; a DM or other channel
  never does.
- An empty recall produces zero sends and zero counter increments (silent skip).
- A fire produces exactly one `message.reply` with mentions suppressed and increments the per-user
  daily counter once.
- The voice-event `MEMORY_CALLBACK_CHANCE` gate is untouched; only the proactive caller bypasses
  it via `pre_recalled_memories`.
</success_criteria>

<output>
Create `.planning/phases/16-proactive-memory-callbacks/16-03-SUMMARY.md` when done.
</output>
