---
phase: 08-social-ops
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - cogs/ai.py
  - personality/roasts.py
  - tests/test_roast_command.py
autonomous: true
requirements: [SOCIAL-01]
must_haves:
  truths:
    - "User can /roast @user and get a Gemini-personalized roast referencing the target's music history"
    - "A template fallback line is sent when Gemini is rate-limited or errors — the roast never fails"
    - "Self-roast, bot-roast, and zero-history targets each return a special line (no error, no decline)"
    - "/roast respects the mood + seasonal system and runs the Gemini call at priority=1"
    - "Public roast sends use AllowedMentions.none() — no mass-mention via the target string"
  artifacts:
    - path: "cogs/ai.py"
      provides: "/roast app_command"
      contains: "name=\"roast\""
    - path: "personality/roasts.py"
      provides: "4 new roast pools (command/self/bot/no-history)"
      contains: "ROAST_COMMAND_LINES"
    - path: "tests/test_roast_command.py"
      provides: "/roast unit tests (fallback, edge cases, priority, no-mention)"
      contains: "test_roast_template_fallback"
  key_links:
    - from: "cogs/ai.py roast"
      to: "GeminiService.chat"
      via: "priority=1 call wrapped in try/except (GeminiRateLimitError, GeminiAPIError)"
      pattern: "priority=1"
    - from: "cogs/ai.py roast"
      to: "get_user_summary"
      via: "target taste summary lookup"
      pattern: "get_user_summary"
    - from: "cogs/ai.py roast public send"
      to: "discord.AllowedMentions.none"
      via: "allowed_mentions guard"
      pattern: "AllowedMentions.none"
---

<objective>
Add `/roast @user` to `cogs/ai.py` (SOCIAL-01): a Gemini-personalized roast of a target user built from their global taste summary, running at priority-1 with mood + seasonal injection and a guaranteed template fallback. Anyone is targetable — self, the bot, and zero-history users each get a special on-brand line. Add the four new roast template pools to `personality/roasts.py` and the unit tests to `tests/test_roast_command.py`.

Purpose: `/roast` is the headline social feature. It deliberately mirrors the `cogs/events.py` ambient-roast machinery (system prompt + scenario + get_user_summary + template fallback), just user-invoked at priority-1 with a 30s/invoker cooldown (D-04/D-05/D-07/D-09).

Decisions implemented in this plan: D-01 (roast output public), D-02 (anyone targetable — self/bot/zero-history special cases), D-03 (global per-user data via get_user_summary), D-04 (30s per-invoker cooldown), D-05 (priority-1 Gemini + guaranteed template fallback), D-06 (harsher-with-guardrails, music-behavior only), D-07 (reuse DEXTER_SYSTEM_PROMPT + scenario, no dedicated prompt), D-08 (respects mood system), D-09 (lives in cogs/ai.py).
Output: `/roast` command in `cogs/ai.py`, four pools in `personality/roasts.py`, `tests/test_roast_command.py` (new).
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-social-ops/08-RESEARCH.md
@.planning/phases/08-social-ops/08-PATTERNS.md
@.planning/phases/08-social-ops/08-VALIDATION.md
@.planning/phases/08-social-ops/08-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New symbols introduced by this plan — excluded from drift verification:

- `/roast` slash command (`cogs/ai.py` `AICog.roast`)
- `personality/roasts.py` pools: `ROAST_COMMAND_LINES`, `ROAST_SELF_LINES`, `ROAST_BOT_LINES`, `ROAST_NO_HISTORY_LINES` (added to `__all__`)
- `tests/test_roast_command.py` (new file): `test_roast_template_fallback`, `test_roast_edge_cases`, `test_roast_uses_priority_1`, `test_roast_no_mass_mention`

Consumes from Plan 01: `config.ROAST_COOLDOWN_SECONDS`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: personality/roasts.py — four /roast template pools</name>
  <files>personality/roasts.py</files>
  <read_first>
    - `personality/roasts.py` lines 1-60 — the module voice-register constraints docstring (LOCKED: contempt outward/down, mild swearing only, lowercase, ≤500 chars, one-emoji-max, {name} via .format()), the `__all__` list (lines 20-34), and the `VOICE_JOIN_ROASTS` pool structure to copy
    - 08-RESEARCH.md § Implementation Patterns #1 (the four pool specs + line counts) and D-06 (harsher than ambient, music-behavior only, no slurs/protected-class)
    - 08-PATTERNS.md § personality/roasts.py (MODIFY)
  </read_first>
  <action>
    Add four list[str] pools after the existing pools, following the VOICE_JOIN_ROASTS structure and the module's locked voice constraints:
    - `ROAST_COMMAND_LINES` — 4-6 lines, harsher than VOICE_JOIN_ROASTS but strictly about the target's music behavior (D-06); use the `{name}` placeholder for `.format()` at the call site.
    - `ROAST_SELF_LINES` — 3-4 lines, "roasting yourself, bleak" tone (no {name} needed; the invoker is the target).
    - `ROAST_BOT_LINES` — 3-4 lines, Dex turns it back on the invoker (no {name} needed).
    - `ROAST_NO_HISTORY_LINES` — 3-4 lines, "who even are you" no-data tone; use the `{name}` placeholder.
    Add all four names to the `__all__` list. Keep every line lowercase, ≤500 chars, ≤1 emoji, no slurs / protected-class content / genuine cruelty. Do NOT write any BANNED inward mode (BOT-META / POP-PSYCH / SELF-DEPRECATION per the docstring) in ROAST_COMMAND_LINES/ROAST_NO_HISTORY_LINES (those aim outward); ROAST_SELF_LINES/ROAST_BOT_LINES may reference the invoker's choice to roast, which is still outward at the invoker.
  </action>
  <verify>
    <automated>python -c "from personality.roasts import ROAST_COMMAND_LINES, ROAST_SELF_LINES, ROAST_BOT_LINES, ROAST_NO_HISTORY_LINES; assert all(len(p) >= 3 for p in (ROAST_COMMAND_LINES, ROAST_SELF_LINES, ROAST_BOT_LINES, ROAST_NO_HISTORY_LINES)); assert all(l == l.lower() for p in (ROAST_COMMAND_LINES, ROAST_SELF_LINES, ROAST_BOT_LINES, ROAST_NO_HISTORY_LINES) for l in p)"</automated>
  </verify>
  <acceptance_criteria>
    - `personality/roasts.py` defines `ROAST_COMMAND_LINES`, `ROAST_SELF_LINES`, `ROAST_BOT_LINES`, `ROAST_NO_HISTORY_LINES`, each a non-empty `list[str]` (≥3 entries).
    - All four pool names appear in `__all__`.
    - Every line is lowercase (the import-time assertion above passes).
    - `ROAST_COMMAND_LINES` and `ROAST_NO_HISTORY_LINES` contain `{name}` placeholders.
    - Roast tone/voice quality (harsher-with-guardrails, on-brand, no cruelty) — MANUAL verification per 08-VALIDATION.md (not automatable).
  </acceptance_criteria>
  <done>Four importable, lowercase, on-brand roast pools exist and are exported via __all__.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cogs/ai.py — /roast command (mood + seasonal + priority-1 + guaranteed fallback)</name>
  <files>cogs/ai.py, tests/test_roast_command.py</files>
  <read_first>
    - `cogs/ai.py` lines 1-138 — imports block, `AICog` class + `gemini`/`pool` properties, and the `/ask` command (lines 93-138) as the exact mood + seasonal + priority-1 + stat-increment analog
    - `cogs/events.py` `_generate_ambient_roast` (≈ lines 92-161) — the scenario + get_user_summary + guaranteed-fallback + personality-voice-enforcement (lowercase first char, 500-char truncation) pattern
    - `models/user_profile.py` `get_user_summary()` (line 8) — returns the target's taste summary string (or None for zero-history)
    - `personality/prompts.py` `build_chat_prompt(mood, user_context, seasonal)` (handles None user_context as "no data" at line ~91)
    - 08-RESEARCH.md § Implementation Patterns #1 (the full /roast flow, edge-case branching, priority=1, Pitfall 3) + § discord.py 2.x Specifics (discord.Member param, per-user cooldown key, defer)
    - 08-PATTERNS.md § cogs/ai.py — add /roast (MODIFY), § Shared Patterns (Gemini Priority + Fallback, allowed_mentions Guard, Personality Voice Enforcement)
    - 08-VALIDATION.md Per-Task Verification Map (the four `tests/test_roast_command.py` test names)
  </read_first>
  <behavior>
    - Test: `test_roast_template_fallback` — when `GeminiService.chat` raises `GeminiRateLimitError`, the followup send receives a fallback line from the appropriate pool (roast never fails).
    - Test: `test_roast_edge_cases` — self-target → ROAST_SELF_LINES scenario; bot-target → ROAST_BOT_LINES; zero-history (get_user_summary None) → ROAST_NO_HISTORY_LINES; none raise/decline.
    - Test: `test_roast_uses_priority_1` — `gemini.chat` is called with `priority=1`.
    - Test: `test_roast_no_mass_mention` — the public followup send passes `allowed_mentions=discord.AllowedMentions.none()`.
  </behavior>
  <action>
    Add a `/roast` app_command to `AICog` in `cogs/ai.py`, modeled on `/ask` + `_generate_ambient_roast`:
    - Decorators: `@app_commands.command(name="roast", description="Roast a user based on their music history")`, `@app_commands.describe(target="The user to roast")`, `@app_commands.checks.cooldown(1, config.ROAST_COOLDOWN_SECONDS, key=lambda i: i.user.id)` (per-invoker, D-04).
    - Signature: `async def roast(self, interaction, target: discord.Member)`. Start with `await interaction.response.defer()` (public — D-01).
    - Resolve edge cases FIRST (D-02), before mood/Gemini setup: bot/other-bot → scenario "someone tried to roast the bot itself" + `ROAST_BOT_LINES`; self (`target.id == interaction.user.id`) → "someone tried to roast themselves" + `ROAST_SELF_LINES`; else look up `user_summary = await get_user_summary(self.bot.pool, str(target.id))` — if None → no-history scenario + `ROAST_NO_HISTORY_LINES`; else → `roast {display_name}: {summary}` scenario + `ROAST_COMMAND_LINES`. In the self/bot/no-history branches `user_summary` is None (build_chat_prompt handles None).
    - Build fallback: `fallback_line = pick_random(fallback_pool)`; if `{name}` present, `.format(name=target.display_name)`.
    - Inject mood + seasonal exactly like `/ask`: `mood = await get_mood(self.bot.pool)`, `seasonal = get_seasonal_context()`, `system_prompt = build_chat_prompt(mood, user_summary, seasonal)` (D-08).
    - Build a one-message conversation instructing exactly one roast line in Dex's voice, under ~200 chars, lowercase, harsher than usual, no preamble (per RESEARCH #1 scenario text).
    - Call `result = await self.gemini.chat(system_prompt, conversation, priority=1)` (D-05 / Pitfall 3 — priority=1, NOT 2). On success apply personality-voice enforcement (strip, truncate >500 → `[:497]+"..."`, lowercase first char), then `await interaction.followup.send(result, allowed_mentions=discord.AllowedMentions.none())`. On empty result OR `except (GeminiRateLimitError, GeminiAPIError)` → `await interaction.followup.send(fallback_line, allowed_mentions=discord.AllowedMentions.none())` (guaranteed fallback, D-05).
    - After sending: `await increment_daily_stat(self.bot.pool, "total_commands")` and `"total_ai_queries"`.
    - Add the four pool imports from `personality.roasts` at the top of `cogs/ai.py`.
    Then create `tests/test_roast_command.py` with the four tests above, mocking `GeminiService.chat` (return / raise) and a fake interaction (mock `response.defer`, `followup.send`, `user`, `bot`, `guild`); mirror the unit-mock style of `tests/test_rate_limiter.py`. Mark async tests `@pytest.mark.asyncio`. No watch-mode flags.
    Do NOT add a per-target cooldown (deferred). Do NOT use a separate dedicated prompt — reuse build_chat_prompt (D-07).
  </action>
  <verify>
    <automated>python -m pytest tests/test_roast_command.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cogs/ai.py` defines the `roast` command with `name="roast"` and a `target: discord.Member` parameter.
    - The Gemini call passes `priority=1` (verifiable: `test_roast_uses_priority_1` passes; `grep -c "priority=1" cogs/ai.py` includes the roast call site).
    - The public followup send(s) pass `allowed_mentions=discord.AllowedMentions.none()` (T-mention-spoof mitigation; `test_roast_no_mass_mention` passes).
    - The Gemini call is wrapped in `try/except (GeminiRateLimitError, GeminiAPIError)` with a `pick_random(fallback_pool)` fallback (`test_roast_template_fallback` passes).
    - Self / bot / zero-history branches each select the right pool (`test_roast_edge_cases` passes).
    - `python -m pytest tests/test_roast_command.py -x` exits 0.
    - Roast tone in live Discord — MANUAL verification (08-VALIDATION.md), not automated.
  </acceptance_criteria>
  <done>/roast works for all target types, never fails on rate-limit, runs at priority-1 with mood/seasonal injection, and sends with AllowedMentions.none(); all four unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user → /roast | invoker selects a target Member; target.display_name flows into a public message |
| /roast → Gemini | target taste summary flows into the LLM prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Spoofing | public roast send (target string) | mitigate | `allowed_mentions=discord.AllowedMentions.none()` on every public followup; `target.display_name` is a plain string, never a raw mention tag. Acceptance criterion + `test_roast_no_mass_mention` enforce it. |
| T-08-05 | Denial of Service | shared 15-RPM Gemini budget | mitigate | 30s per-invoker cooldown (D-04) + priority=1 (waits, does not spin) + guaranteed template fallback so a saturated limiter never blocks the command. |
| T-08-06 | Information Disclosure | roast content (no slurs / protected-class) | accept | D-06 guardrails encoded in the system prompt + template pools; content quality is verified by human UAT, not automatable. No PII beyond the user's own tracked music data (already global per D-03). |
| T-08-07 | Tampering | npm/pip installs | accept | No new packages this phase (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `python -m pytest tests/test_roast_command.py -x` green.
- `python -m pytest tests/ -x` green at wave merge (no regression in /ask or other AI commands).
- Manual: `/roast @someone` in live Discord renders a public, lowercase, on-brand roast; rate-limited path falls back cleanly.
</verification>

<success_criteria>
- /roast generates a Gemini-personalized roast from the target's tracked history with a guaranteed template fallback (SOCIAL-01, ROADMAP criterion #1).
- All target edge cases (self/bot/zero-history) handled with special lines, no errors.
- priority-1 + 30s per-invoker cooldown + AllowedMentions.none() enforced.
</success_criteria>

<output>
Create `.planning/phases/08-social-ops/08-02-SUMMARY.md` when done.
</output>
