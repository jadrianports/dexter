---
phase: 08-social-ops
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - cogs/ops.py
  - utils/embeds.py
  - personality/responses.py
  - bot.py
  - utils/logger.py
  - tests/test_health_endpoint.py
autonomous: true
requirements: [SOCIAL-02, OPS-01, OPS-02, OPS-03]
must_haves:
  truths:
    - "User can /leaderboard and see a single public embed with three sections (most queued, longest streak, most-skipped), top 5 each + a dry commentary line"
    - "Empty/new server renders a personality empty-state line, not an empty embed"
    - "Owner can /stats (ephemeral) and see today's commands/songs/AI queries/images/errors + Gemini RPM headroom + image-cap usage + bot-state"
    - "Non-owner invoking /stats gets an ephemeral refusal"
    - "GET /health returns {\"status\":\"ok\"} when DB+gateway are healthy, {\"status\":\"degraded\",\"reasons\":[...]} otherwise, always HTTP 200"
    - "total_errors increments once per error surfaced to the Discord error-log channel, with a recursion guard"
  artifacts:
    - path: "cogs/ops.py"
      provides: "OpsCog (/leaderboard + /stats) + gather_bot_metrics helper"
      contains: "async def gather_bot_metrics"
    - path: "utils/embeds.py"
      provides: "leaderboard_embed + stats_embed + COLOR_LEADERBOARD/COLOR_STATS"
      contains: "def leaderboard_embed"
    - path: "bot.py"
      provides: "degraded /health body + cogs.ops registration + _start_monotonic"
      contains: "degraded"
    - path: "tests/test_health_endpoint.py"
      provides: "health ok/degraded/always-200 integration tests"
      contains: "test_health_always_200"
  key_links:
    - from: "cogs/ops.py /leaderboard"
      to: "database.get_leaderboard_songs/skips/streaks"
      via: "guild-scoped aggregate calls"
      pattern: "get_leaderboard_"
    - from: "cogs/ops.py /stats"
      to: "bot.is_owner"
      via: "inline owner check (no decorator)"
      pattern: "is_owner"
    - from: "bot.py health handler"
      to: "cogs.ops.gather_bot_metrics"
      via: "function-scope import at request time"
      pattern: "from cogs.ops import gather_bot_metrics"
    - from: "utils/logger.py log_to_discord"
      to: "database.increment_daily_stat"
      via: "total_errors increment guarded by inner try/except"
      pattern: "increment_daily_stat\\(pool, \"total_errors\"\\)"
---

<objective>
Build the ops surface (OPS-01/02/03 + SOCIAL-02): the new `cogs/ops.py` cog hosting `/leaderboard` (public, 3-section embed) and `/stats` (owner-only, ephemeral) plus the shared `gather_bot_metrics` helper; the `leaderboard_embed` + `stats_embed` builders and commentary/empty-state pools; the degraded-but-always-200 `/health` body in `bot.py` (sharing the metrics helper); `cogs.ops` registration + `_start_monotonic`; and the `total_errors` increment at the central Discord error-log site in `utils/logger.py`.

Purpose: This plan delivers three of the five requirements by wiring existing infrastructure to the Plan-01 query/getter foundation. The rich health requirement (OPS-02) is satisfied via the owner-only `/stats` embed while the public `/health` stays minimal+degraded — a deliberate continuation of the Phase-5 K-02 security decision (D-27).

Decisions implemented in this plan: D-11 (one embed, three sections), D-13 (top-5 per section + dry commentary), D-17 (empty-state personality line), D-19 (/leaderboard output public), D-20 (/leaderboard lives in cogs/ops.py), D-21 (/stats owner-only inline is_owner), D-22 (today-only window rendered), D-24 (Gemini RPM headroom + image-cap usage folded into the /stats embed), D-25 (/stats bot-wide global), D-26 (/stats lives in cogs/ops.py), D-27 (rich metrics only in /stats, never on public /health), D-28 (/health degraded body but always HTTP 200), D-29 (state-available-today + Phase-6 hooks), D-30 (Koyeb/Neon dashboard link, no psutil), D-31 (shared gather_bot_metrics helper feeds both /stats and /health), D-32 (Dexter voice on all new text), D-33 (errors/empties ephemeral, public actions public), D-23 (total_errors increment at the central error-log site).
Output: `cogs/ops.py` (new), `utils/embeds.py` (2 builders + 2 colors), `personality/responses.py` (4 pools), `bot.py` (health + registration + monotonic), `utils/logger.py` (increment), `tests/test_health_endpoint.py` (new).
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-social-ops/08-RESEARCH.md
@.planning/phases/08-social-ops/08-PATTERNS.md
@.planning/phases/08-social-ops/08-VALIDATION.md
@.planning/phases/08-social-ops/08-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

New symbols introduced by this plan — excluded from drift verification:

- `cogs/ops.py` (new file): `OpsCog` (with `/leaderboard` + `/stats` commands), module-level `async def gather_bot_metrics(bot) -> dict`, `setup(bot)`
- `utils/embeds.py`: `leaderboard_embed`, `stats_embed`, `COLOR_LEADERBOARD`, `COLOR_STATS`
- `personality/responses.py` pools: `LEADERBOARD_SONGS_COMMENTARY`, `LEADERBOARD_STREAK_COMMENTARY`, `LEADERBOARD_SKIPS_COMMENTARY`, `LEADERBOARD_EMPTY`
- `bot.py`: degraded `/health` body, `cogs.ops` in the cog-load tuple, `bot._start_monotonic`
- `utils/logger.py`: `total_errors` increment inside `log_to_discord`
- `tests/test_health_endpoint.py` (new file): `test_health_ok`, `test_health_degraded_db`, `test_health_always_200`

Consumes from Plan 01: `get_leaderboard_songs/skips/streaks`, `get_daily_stats_row`, `get_images_today_global`, `increment_daily_stat("total_errors")`, `GeminiService.rpm_usage`, `config.LEADERBOARD_TOP_N`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: utils/embeds.py + personality/responses.py — leaderboard/stats embeds + commentary pools</name>
  <read_first>
    - `utils/embeds.py` lines 1-16 (COLOR_* constants block), `queue_list()` (≈75-111, multi-section conditional add_field pattern), `now_playing()` (≈43-45, inline=True grid pattern), and the existing import block
    - `personality/responses.py` lines 1-40 (`pick_random` at line 9, `AUTO_QUEUE_ANNOUNCE`/`AUTO_QUEUE_IGNORED` pool structure to copy)
    - 08-RESEARCH.md § Implementation Patterns #2 (leaderboard_embed layout + commentary pools + empty-state D-17) and #6 (stats_embed 14-field layout, Koyeb/Neon footer D-30, Phase-6 hook comments D-29) and § discord.py embed limits (25 fields / 1024 / 6000)
    - 08-PATTERNS.md § utils/embeds.py (MODIFY) + § personality/responses.py (MODIFY)
  </read_first>
  <action>
    In `personality/responses.py`: add four list[str] pools following the AUTO_QUEUE_* structure — `LEADERBOARD_SONGS_COMMENTARY` (dry roast of the leader, 3-4 lines, D-13), `LEADERBOARD_STREAK_COMMENTARY` (3-4 lines), `LEADERBOARD_SKIPS_COMMENTARY` (3-4 lines), `LEADERBOARD_EMPTY` (≥2 lines, e.g. "nobody's done anything worth ranking yet.", D-17). All lowercase, dry, one-emoji-max (D-32).
    In `utils/embeds.py`:
    - Add `COLOR_LEADERBOARD = 0xFFD700` and `COLOR_STATS = 0x7289DA` after the existing COLOR_* block.
    - Add `leaderboard_embed(songs_rows, skips_rows, streaks_rows) -> discord.Embed`: title "leaderboard", color COLOR_LEADERBOARD, ONE embed with THREE sections (D-11). For each section, if rows present render top-5 `"{i+1}. {label} — {value}"` lines (most queued: `username — N songs`; streak: `username — N days`; most-skipped: `title — N skips`) plus a `pick_random(<commentary pool>)` line (D-13), via `add_field(..., inline=False)`; if a section's rows are empty, render a single empty-state line for that section (D-17). Section order: most queued · longest streak · most-skipped (D-11).
    - Add `stats_embed(daily, rpm_usage, rpm_max, images_today_global, metrics) -> discord.Embed`: title "dexter system status", color COLOR_STATS, the 14 inline fields from RESEARCH #6 (today's commands/songs/ai/images/errors — the today-only window per D-22; gemini rpm `X/15` + images-today-all-users — the folded quota panel per D-24; uptime minutes; guilds; voice connections; shards; database ok/unreachable; gateway ready/not). Leave the two Phase-6 hook fields as commented-out lines (D-29). Set footer `"host metrics: koyeb dashboard | neon console"` (D-30, no in-process psutil).
    - Add the `pick_random` + three commentary pool imports from `personality.responses` at the top of `utils/embeds.py`.
    Keep field counts within Discord limits (14 for stats, 3 for leaderboard — both well under 25).
  </action>
  <verify>
    <automated>python -c "import utils.embeds as e; assert hasattr(e,'leaderboard_embed') and hasattr(e,'stats_embed') and hasattr(e,'COLOR_LEADERBOARD') and hasattr(e,'COLOR_STATS'); from personality.responses import LEADERBOARD_SONGS_COMMENTARY, LEADERBOARD_STREAK_COMMENTARY, LEADERBOARD_SKIPS_COMMENTARY, LEADERBOARD_EMPTY; assert all(len(p)>=2 for p in (LEADERBOARD_SONGS_COMMENTARY,LEADERBOARD_STREAK_COMMENTARY,LEADERBOARD_SKIPS_COMMENTARY,LEADERBOARD_EMPTY))"</automated>
  </verify>
  <acceptance_criteria>
    - `utils/embeds.py` defines `leaderboard_embed`, `stats_embed`, `COLOR_LEADERBOARD`, `COLOR_STATS` (import-time assertion above passes).
    - `personality/responses.py` defines the four leaderboard pools, all non-empty and lowercase.
    - `leaderboard_embed` renders an empty-state line when a section's rows list is empty (verifiable by calling it with `([], [], [])` — embed has 3 fields with empty-state text, no crash).
    - `stats_embed` produces ≤25 fields and sets the Koyeb/Neon footer.
    - Embed visual layout — MANUAL verification in live Discord (08-VALIDATION.md).
  </acceptance_criteria>
  <done>Both embed builders + two colors exist; four commentary/empty pools exist; embeds render without crashing on empty and populated inputs.</done>
</task>

<task type="auto">
  <name>Task 2: cogs/ops.py — /leaderboard + /stats + gather_bot_metrics + bot.py registration</name>
  <read_first>
    - `cogs/library.py` — cog skeleton (imports, `__init__`, `setup()`), the established Phase-7 cog conventions
    - `cogs/ai.py` lines 77-97 — `__init__` + `pool`/`gemini` property accessors + `defer()` pattern
    - `bot.py` lines 437-438 (inline owner check `await bot.is_owner(interaction.user)` + ephemeral refusal), line 324 (the cog-load tuple `("cogs.music", "cogs.help", "cogs.events", "cogs.library")` — add `"cogs.ops"`), 269-349 (`_initialize_once` — where to set `_start_monotonic` and where `bot.guilds`/`bot.voice_clients` reads live), and the `MusicCog` `get_queue` accessor used by gather_bot_metrics
    - `database.py` (Plan 01 output) — `get_leaderboard_songs/skips/streaks`, `get_daily_stats_row`, `get_images_today_global`, `increment_daily_stat`
    - `services/gemini.py` (Plan 01 output) — `GeminiService.rpm_usage` property
    - 08-RESEARCH.md § Implementation Patterns #5 (gather_bot_metrics full spec + db probe + degraded_reasons) and #6 (/stats owner check + defer ephemeral + read calls) and § Pitfalls 6/7
    - 08-PATTERNS.md § cogs/ops.py (NEW) + § Shared Patterns (Owner Check, 3s-Defer, asyncpg query)
  </read_first>
  <action>
    Create `cogs/ops.py` (the home for both /leaderboard, D-20, and /stats, D-26):
    - Module-level `async def gather_bot_metrics(bot) -> dict` exactly per RESEARCH #5 — the single shared bot-state source of truth (D-31) used by both /stats and /health: keys guild_count, voice_count, queue_count (count guilds whose MusicCog queue has tracks), uptime_seconds (`time.monotonic() - bot._start_monotonic` if set, else 0.0), db_ok (probe via `async with pool.acquire(): await conn.execute("SELECT 1")` — set db_ok + append "database unreachable" to degraded_reasons on failure; "database pool not initialized" if pool is None), gateway_ready (`bot.is_ready()`; append "discord gateway not ready" if False), shard_count (`bot.shard_count or 1`), degraded_reasons (list — empty = healthy).
    - `OpsCog(commands.Cog)` with `__init__(self, bot)` + `pool` property (per library.py/ai.py).
    - `/leaderboard` command: `await interaction.response.defer()` (public output, D-19); call the three Plan-01 leaderboard helpers with `str(interaction.guild_id)` (per-guild scope); pass rows to `leaderboard_embed`; `await interaction.followup.send(embed=...)`. If all three return empty, the embed's per-section empty-state already covers it (D-17) — still send the public embed (a dry board), not an error.
    - `/stats` command (owner-only, bot-wide global per D-25): FIRST `if not await self.bot.is_owner(interaction.user): await interaction.response.send_message("not authorized.", ephemeral=True); return` (D-21, inline, no decorator; the ephemeral refusal honors D-33). Then `await interaction.response.defer(ephemeral=True)` (D-27/D-33/Pitfall 7 — owner output must not leak publicly). Gather `daily = await get_daily_stats_row(self.pool)` (today-only, D-22), `images = await get_images_today_global(self.pool)`, `rpm = self.bot.gemini_service.rpm_usage` (D-24), `metrics = await gather_bot_metrics(self.bot)`; build `stats_embed(daily, rpm, config.GEMINI_RPM_LIMIT, images, metrics)` (the quota panel folds into this one embed, D-24); `await interaction.followup.send(embed=..., ephemeral=True)`.
    - `async def setup(bot): await bot.add_cog(OpsCog(bot))`.
    In `bot.py`:
    - Add `"cogs.ops"` to the cog-load tuple at line 324 (alongside `"cogs.library"` — ops.py has no Gemini dependency at load, so it belongs in the first/always-loaded group, not the ai/imagine group).
    - At the end of `_initialize_once` (after restore_queues), set `bot._start_monotonic = time.monotonic()` (import `time` at module scope if not present — verify in source).
    Do NOT register a persistent view (ops has none — RESEARCH State of the Art). Do NOT expose any rich metric on the public /health endpoint (D-27 — that is Task 3's degraded body, generic reasons only).
  </action>
  <verify>
    <automated>python -c "import cogs.ops as o; assert hasattr(o,'gather_bot_metrics') and hasattr(o,'OpsCog') and hasattr(o,'setup')"</automated>
  </verify>
  <acceptance_criteria>
    - `cogs/ops.py` exists and defines `gather_bot_metrics`, `OpsCog`, `setup` (import-time assertion passes).
    - `gather_bot_metrics` returns a dict containing `degraded_reasons` (list) and the bot-state keys; on a None pool it appends a pool-not-initialized reason (no crash).
    - `/stats` uses the inline `await self.bot.is_owner(interaction.user)` check and `defer(ephemeral=True)` — verifiable: `grep -c "is_owner" cogs/ops.py` >= 1 and `grep -c "ephemeral=True" cogs/ops.py` >= 1 (T-owner-bypass / Pitfall 7).
    - `/leaderboard` calls all three `get_leaderboard_*` helpers with `str(interaction.guild_id)` (per-guild, T-state-leak-safe; verifiable: `grep -c "get_leaderboard_" cogs/ops.py` >= 3).
    - `bot.py` cog-load tuple at line ~324 includes `"cogs.ops"`; `bot._start_monotonic` is set in `_initialize_once`.
    - `/stats` owner-only + ephemeral and leaderboard layout — MANUAL verification in live Discord (08-VALIDATION.md).
  </acceptance_criteria>
  <done>OpsCog with both commands + gather_bot_metrics exists, is registered in bot.py, and uptime tracking is wired; owner check + ephemeral defer + guild-scoped leaderboard calls in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: bot.py degraded /health + utils/logger.py total_errors increment + test_health_endpoint.py</name>
  <read_first>
    - `bot.py` lines 197-229 (`_run_health_server` + the inner `health` coroutine returning `{"status":"ok"}`) and line 369 (`from services.queue_persistence import restore_queues` — the function-scope import pattern to mirror for gather_bot_metrics)
    - `cogs/ops.py` `gather_bot_metrics` (Task 2 output — the degraded_reasons source)
    - `utils/logger.py` lines 52-65 (`log_to_discord` body) and the top-of-file imports
    - `database.py` `increment_daily_stat` (Plan 01 — allowlist now includes total_errors)
    - 08-RESEARCH.md § Implementation Patterns #5 (degraded health handler, function-scope import, always-200) and #4 (total_errors increment site + Pitfall 5 recursion guard) and § Pitfall 6 (health before cogs load → broad try/except fallback)
    - 08-PATTERNS.md § bot.py (MODIFY) + § utils/logger.py (MODIFY)
    - 08-VALIDATION.md Per-Task Verification Map (the three `tests/test_health_endpoint.py` test names)
  </read_first>
  <behavior>
    - Test: `test_health_ok` — when gather_bot_metrics yields no degraded_reasons (mock bot: pool returns on execute, is_ready True), the handler body is `{"status":"ok"}`.
    - Test: `test_health_degraded_db` — when the pool raises on execute, the body is `{"status":"degraded","reasons":["database unreachable"]}` (generic reason only — no internal state).
    - Test: `test_health_always_200` — with the DB down, the HTTP status is still 200 (no kill-loop).
  </behavior>
  <action>
    In `bot.py`: replace the inner `health` coroutine body in `_run_health_server` with the degraded-aware version (RESEARCH #5): inside the handler, `try: from cogs.ops import gather_bot_metrics; metrics = await gather_bot_metrics(bot); reasons = metrics.get("degraded_reasons", [])` and `except Exception: reasons = ["metrics gatherer unavailable"]` (Pitfall 6 — health may fire before cogs load). If `reasons`, body = `json.dumps({"status":"degraded","reasons":reasons})`; else body = `'{"status":"ok"}'`. ALWAYS return `_aio_web.Response(text=body, content_type='application/json')` — HTTP 200 in every branch (D-28, no kill-loop). The body exposes ONLY status + generic reason strings — no guild/shard/pool internals (D-27). The `from cogs.ops import gather_bot_metrics` MUST live inside the handler function body (function-scope, fires at request time) to avoid a circular import (Pitfall 7-circular). Ensure `json` is importable in the handler (module-level `import json` exists, or `import json as _json` inside the handler).
    In `utils/logger.py`: in `log_to_discord`, after the successful `await channel.send(embed=embed)`, add a guarded increment (D-23): `pool = getattr(bot, "pool", None); if pool is not None:` then an INNER `try: from database import increment_daily_stat; await increment_daily_stat(pool, "total_errors") except Exception: pass`. The inner try/except is the recursion guard (Pitfall 5) — a DB-down increment failure must NOT re-enter log_to_discord. Keep the increment inside the outer `try` that wraps `channel.send` so it only counts errors actually surfaced to the channel. (No change needed at `bot.py on_app_command_error` — it routes through log_to_discord, the single increment site.)
    Create `tests/test_health_endpoint.py` with the three tests above. Build the health handler's behavior under test by constructing a fake `bot` (object with `guilds`, `voice_clients`, `is_ready()`, `shard_count`, `cogs`, and a `pool` whose `acquire()` context yields a conn whose `execute` either returns or raises) and asserting on the body string + status; mirror the async-unit style of `tests/test_rate_limiter.py`. Mark async tests `@pytest.mark.asyncio`. No watch-mode flags.
  </action>
  <verify>
    <automated>python -m pytest tests/test_health_endpoint.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `bot.py` `_run_health_server`'s handler returns a degraded body when `degraded_reasons` is non-empty and `{"status":"ok"}` otherwise, ALWAYS HTTP 200 (`test_health_ok`, `test_health_degraded_db`, `test_health_always_200` pass).
    - The `from cogs.ops import gather_bot_metrics` is inside the handler function body, not at bot.py module level (T-state-leak / circular-import guard; verifiable by line position relative to `async def health`).
    - The public health body exposes ONLY `status` + generic `reasons` strings — no guild_count/shard/pool internals (T-state-leak; D-27).
    - `utils/logger.py` `log_to_discord` calls `increment_daily_stat(pool, "total_errors")` inside an inner `try/except Exception: pass` (recursion guard, Pitfall 5; verifiable: `grep -c 'increment_daily_stat(pool, "total_errors")' utils/logger.py` >= 1).
    - `python -m pytest tests/test_health_endpoint.py -x` exits 0.
    - Healthchecks.io green / degraded alert — MANUAL verification on the live dead-man switch (08-VALIDATION.md).
  </acceptance_criteria>
  <done>/health returns a degraded-but-200 body sourced from the shared metrics helper without leaking internal state; total_errors increments at the central error-log site with a recursion guard; all three health tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → GET /health | unauthenticated HTTP request from Koyeb/UptimeRobot/anyone |
| Discord user → /stats | only the bot owner may see rich bot-state |
| Discord user → /leaderboard | guild_id flows into per-guild aggregate queries |
| error path → log_to_discord | a stat-field write happens on the error path (recursion risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information Disclosure | public `/health` body | mitigate | Rich metrics live ONLY in owner `/stats` (ephemeral); `/health` body is `status` + generic `reasons` strings only (D-27/K-02). Acceptance criterion forbids internal-state keys in the body. |
| T-08-09 | Elevation of Privilege | `/stats` owner gate | mitigate | Authoritative inline `await bot.is_owner(interaction.user)` (bound to config.OWNER_ID at create_bot); non-owner gets an ephemeral refusal before any data is gathered. No decorator (project pattern). |
| T-08-10 | Tampering | `/leaderboard` SQL (guild_id) | mitigate | Plan-01 helpers use `$N` params; cog passes `str(interaction.guild_id)` — never interpolated. Leaderboard is guild-scoped (no cross-guild leak). |
| T-08-11 | Denial of Service | non-200 health kill-loop | mitigate | `/health` returns HTTP 200 in every branch (D-28) so Koyeb never kill-loops and Neon's 5-min scale-to-zero can't trip a restart cascade. `test_health_always_200` enforces it. |
| T-08-12 | Denial of Service | total_errors increment recursion | mitigate | Inner `try/except Exception: pass` around the increment (Pitfall 5) — a DB-down failure never re-enters log_to_discord. |
| T-08-13 | Tampering | npm/pip installs | accept | No new packages this phase (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `python -m pytest tests/test_health_endpoint.py -x` green.
- `python -m pytest tests/ -x` green at wave merge (no regression).
- Manual: `/leaderboard` renders 3 sections; owner `/stats` shows the embed ephemerally, non-owner refused; `/health` reachable + flips to degraded body when DB is down.
</verification>

<success_criteria>
- /leaderboard shows a per-server 3-section ranking from live Postgres with empty-state handling (SOCIAL-02, ROADMAP criterion #2).
- /stats (owner-only, ephemeral) shows today's stats + Gemini RPM headroom + image-cap usage + bot-state (OPS-01 + OPS-03, ROADMAP criteria #3/#5 re-interpreted per D-30).
- /health returns ok/degraded, always 200, reachable by the dead-man switch (OPS-02, ROADMAP criterion #4 with state available today per D-29).
- total_errors is tracked and surfaced in /stats (OPS-01).
</success_criteria>

<output>
Create `.planning/phases/08-social-ops/08-03-SUMMARY.md` when done.
</output>
