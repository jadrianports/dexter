---
phase: 08-social-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database.py
  - services/gemini.py
  - config.py
  - tests/test_database_phase8.py
  - tests/test_rate_limiter.py
autonomous: true
requirements: [SOCIAL-02, OPS-01, OPS-03]
must_haves:
  truths:
    - "init_db() creates the total_errors column on bot_daily_stats (idempotent, existing rows default 0)"
    - "increment_daily_stat(pool, 'total_errors') upserts without raising (allowlist gated)"
    - "get_leaderboard_songs ranks users by per-guild song count (D-10), excludes other guilds, ties broken by oldest first_seen_at"
    - "get_leaderboard_skips ranks song titles (D-12) by was_skipped count, per-guild, excludes zero-skip titles (D-18)"
    - "get_leaderboard_streaks ranks guild-active users by global longest_streak"
    - "get_daily_stats_row returns today-only, bot-wide global stats (D-22/D-25); a dict of 0s when no row exists"
    - "GeminiService.rpm_usage and rpm_headroom expose current sliding-window RPM for the /stats quota panel (D-24)"
  artifacts:
    - path: "database.py"
      provides: "total_errors column + 4 new query helpers"
      contains: "total_errors INTEGER DEFAULT 0"
    - path: "services/gemini.py"
      provides: "rpm_usage/rpm_headroom getters on _RateLimiter + GeminiService"
      contains: "def rpm_usage"
    - path: "config.py"
      provides: "ROAST_COOLDOWN_SECONDS + LEADERBOARD_TOP_N"
      contains: "ROAST_COOLDOWN_SECONDS"
    - path: "tests/test_database_phase8.py"
      provides: "leaderboard + total_errors + daily-stats integration tests"
      contains: "test_leaderboard_songs_guild_scoped"
  key_links:
    - from: "increment_daily_stat allowlist"
      to: "total_errors column"
      via: "allowed_fields set membership"
      pattern: "total_errors"
    - from: "get_leaderboard_songs"
      to: "song_history.guild_id"
      via: "parameterized WHERE guild_id = $1"
      pattern: "guild_id = \\$1"
---

<objective>
Build the data + service foundation every Phase 8 feature reads from: the `total_errors` column on `bot_daily_stats`, the per-guild leaderboard aggregate query helpers, the daily-stats and global-image-count read helpers, and the public Gemini RPM getters — plus the Wave-0 test scaffolding (`tests/test_database_phase8.py` + two new methods in `tests/test_rate_limiter.py`) and the two new `config.py` constants. Everything else in this phase consumes symbols defined here.

Purpose: Centralize the only net-new persistence (`total_errors`, D-23) and the genuinely-new SQL (per-guild leaderboard aggregates, D-14/D-15) so the `/roast`, `/leaderboard`, and `/stats` plans wire against ready contracts instead of inventing queries.

Decisions implemented in this plan: D-10 (leaderboard per-server scope, encoded in the guild-scoped queries), D-12 (most-skipped ranks song titles), D-14 (per-guild aggregates from song_history, NOT the global counter), D-15 (streak board ranks guild-active users by global streak), D-16 (tie-break by oldest first_seen_at), D-18 (exclude zero-activity / zero-skip rows), D-22 (today-only stats window), D-23 (total_errors column — the only net-new persistence), D-24 (Gemini RPM getter for the quota panel), D-25 (/stats stats are bot-wide global), D-13 (LEADERBOARD_TOP_N = 5), D-04 (ROAST_COOLDOWN_SECONDS = 30). The query helpers feed the cog commands the Plan-03 ops cog (D-20/D-26) and Plan-02 /roast cooldown consume.
Output: `database.py` (column + 5 helpers), `services/gemini.py` (RPM getters), `config.py` (2 constants), `tests/test_database_phase8.py` (new), `tests/test_rate_limiter.py` (extended).
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-social-ops/08-RESEARCH.md
@.planning/phases/08-social-ops/08-PATTERNS.md
@.planning/phases/08-social-ops/08-VALIDATION.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New symbols introduced by this plan — excluded from drift verification:

- `database.py` column: `bot_daily_stats.total_errors`
- `database.py` functions: `get_leaderboard_songs`, `get_leaderboard_skips`, `get_leaderboard_streaks`, `get_daily_stats_row`, `get_images_today_global`
- `database.py` allowlist extension: `"total_errors"` added to `increment_daily_stat` `allowed_fields`
- `services/gemini.py` methods: `_RateLimiter.rpm_usage`, `_RateLimiter.rpm_headroom`
- `services/gemini.py` properties: `GeminiService.rpm_usage`, `GeminiService.rpm_headroom`
- `config.py` constants: `ROAST_COOLDOWN_SECONDS = 30`, `LEADERBOARD_TOP_N = 5`
- `tests/test_database_phase8.py` (new file)
- `tests/test_rate_limiter.py` new methods: `test_rpm_usage_getter`, `test_rpm_headroom_getter`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 test scaffolding — test_database_phase8.py + rate-limiter getter tests</name>
  <files>tests/test_database_phase8.py, tests/test_rate_limiter.py</files>
  <read_first>
    - `tests/conftest.py` — the `pool` fixture (creates schema via init_db, drops all tables incl. bot_daily_stats in teardown; live DB at TEST_DATABASE_URL, skips on connection error)
    - `tests/test_database_phase4.py` — integration test style (insert rows, call helper, assert) to mirror
    - `tests/test_rate_limiter.py` — existing `TestRateLimiter` class + `_RateLimiter` usage to extend in place
    - 08-VALIDATION.md § Per-Task Verification Map + Wave 0 Requirements — the exact test names this plan must create
    - 08-RESEARCH.md § Validation Architecture (Phase Requirements → Test Map) for each test's assertion intent
  </read_first>
  <action>
    Create `tests/test_database_phase8.py` with these live-DB integration tests (each uses the `pool` fixture, marked `@pytest.mark.asyncio`), written RED first against the not-yet-existing helpers (Task 2 makes them green):
    - `test_total_errors_column_exists` — query `information_schema.columns` for `bot_daily_stats.total_errors`; assert present (per D-23).
    - `test_total_errors_increment` — call `increment_daily_stat(pool, "total_errors")` twice, then `get_daily_stats_row(pool)`; assert `total_errors == 2`.
    - `test_get_daily_stats_row_empty` — on a clean DB call `get_daily_stats_row(pool)`; assert dict with all five keys == 0.
    - `test_leaderboard_songs_guild_scoped` — insert song_history rows for two guild_ids + matching user_profiles; assert `get_leaderboard_songs(pool, "G1")` counts only G1 rows (D-10/D-14).
    - `test_leaderboard_skips_filter` — insert a mix of `was_skipped=true/false` for one title; assert `get_leaderboard_skips(pool, guild)` counts only the skipped rows and excludes zero-skip titles (D-12/D-18).
    - `test_leaderboard_streak_guild_scoped` — insert history for a user in guild A only; assert that user appears for guild A and NOT for guild B in `get_leaderboard_streaks` (D-15).
    - `test_leaderboard_tie_break` — two users, equal song count, different `first_seen_at`; assert the older `first_seen_at` ranks first (D-16).
    - `test_leaderboard_empty_guild` — query a guild with no history; assert all three helpers return empty lists.
    Extend `tests/test_rate_limiter.py` `TestRateLimiter` with: `test_rpm_usage_getter` (after N `acquire(priority=1)` calls, `limiter.rpm_usage() == N`) and `test_rpm_headroom_getter` (`limiter.rpm_headroom() == max_requests - rpm_usage()`).
    Do NOT add watch-mode flags. Mirror the conftest `pool` fixture import style. These tests reference symbols Task 2/Task 3 create — they are expected RED until those tasks land.
  </action>
  <verify>
    <automated>python -m pytest tests/test_database_phase8.py tests/test_rate_limiter.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_database_phase8.py` exists and defines all 8 named test functions above (verifiable: `grep -c "def test_" tests/test_database_phase8.py` >= 8).
    - `tests/test_rate_limiter.py` contains `test_rpm_usage_getter` and `test_rpm_headroom_getter`.
    - `python -m pytest tests/test_database_phase8.py tests/test_rate_limiter.py --collect-only -q` exits 0 (all tests discoverable — import-clean even before helpers exist, since helpers are imported at module top and Task 2 lands them; if collection requires the helper symbols, Task 2 may be done first in the same wave — coordinate via commit order).
    - No `-f`/`--looponfail`/watch flags present.
  </acceptance_criteria>
  <done>The three Wave-0 test artifacts named in 08-VALIDATION.md exist and are collectable; behavior tasks in this and later plans have their automated verify available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: database.py — total_errors column, allowlist, leaderboard + daily-stats helpers</name>
  <files>database.py</files>
  <read_first>
    - `database.py` lines 66-149 (SCHEMA_SQL — add ALTER after bot_daily_stats CREATE), 152-158 (init_db applies SCHEMA_SQL as one no-$N execute), 269-288 (`increment_daily_stat` + `allowed_fields`), the existing `get_daily_command_count` / `get_images_today` fetch helpers, and `log_track_batch`/`update_user_profile` (username is the stored display name — use it, do not resolve via Discord)
    - 08-RESEARCH.md § Implementation Patterns #2 (the three exact leaderboard SQL queries A/B/C) and #4 (total_errors column + allowlist + get_daily_stats_row) and #6 (get_images_today_global)
    - 08-PATTERNS.md § database.py (MODIFY) — the copy-from analogs + exact helper signatures
  </read_first>
  <behavior>
    - Test: `test_total_errors_column_exists` — column present after init_db.
    - Test: `test_total_errors_increment` — increment("total_errors") works (allowlist accepts it).
    - Test: `test_get_daily_stats_row_empty` — returns all-zero dict when no today row.
    - Test: `test_leaderboard_songs_guild_scoped` — per-guild count, other guilds excluded.
    - Test: `test_leaderboard_skips_filter` — only was_skipped=true rows counted, zeros excluded.
    - Test: `test_leaderboard_streak_guild_scoped` — only guild-active users returned.
    - Test: `test_leaderboard_tie_break` — equal counts → oldest first_seen_at first.
    - Test: `test_leaderboard_empty_guild` — empty guild → empty lists.
  </behavior>
  <action>
    In `database.py`:
    1. SCHEMA_SQL: append `ALTER TABLE bot_daily_stats ADD COLUMN IF NOT EXISTS total_errors INTEGER DEFAULT 0;` after the `bot_daily_stats` CREATE TABLE block. Idempotent; existing rows take DEFAULT 0. Stays inside the single no-$N `conn.execute(SCHEMA_SQL)` in init_db (Pitfall 1).
    2. Add `"total_errors"` to the `allowed_fields` set in `increment_daily_stat` (D-23 / Pitfall 2) — do this BEFORE any caller exists or it raises ValueError.
    3. Add `get_daily_stats_row(pool)` → returns today's row (date.today().isoformat() as $1) as a dict with keys total_commands/total_songs_played/total_ai_queries/total_images_generated/total_errors; when no row, return all-zero dict (per RESEARCH #4). This is the today-only, bot-wide global window for `/stats` (D-22/D-25).
    4. Add `get_leaderboard_songs(pool, guild_id)` — per-server scope (D-10/D-14), Query A: `SELECT sh.user_id, up.username, COUNT(*) AS songs_queued FROM song_history sh JOIN user_profiles up USING (user_id) WHERE sh.guild_id = $1 GROUP BY sh.user_id, up.username HAVING COUNT(*) >= 1 ORDER BY songs_queued DESC, up.first_seen_at ASC LIMIT $2` with $2 = config.LEADERBOARD_TOP_N. The `HAVING COUNT(*) >= 1` excludes zero-activity users (D-18). asyncpg `$N` params ONLY — never interpolate guild_id (T-sql-inj).
    5. Add `get_leaderboard_skips(pool, guild_id)` — most-skipped ranks by song TITLE (D-12), Query B: `SELECT title, COUNT(*) AS skip_count FROM song_history WHERE guild_id = $1 AND was_skipped = true GROUP BY title HAVING COUNT(*) >= 1 ORDER BY skip_count DESC LIMIT $2`. The `HAVING COUNT(*) >= 1` on skipped rows excludes zero-skip titles (D-18).
    6. Add `get_leaderboard_streaks(pool, guild_id)` — Query C: `SELECT up.user_id, up.username, up.longest_streak FROM user_profiles up WHERE up.user_id IN (SELECT DISTINCT user_id FROM song_history WHERE guild_id = $1) AND up.longest_streak >= 1 ORDER BY up.longest_streak DESC, up.first_seen_at ASC LIMIT $2` (D-15/D-16).
    7. Add `get_images_today_global(pool)` — `SELECT COUNT(*) AS cnt FROM image_generation_log WHERE generated_at::date = CURRENT_DATE`; return int (RESEARCH #6).
    All helpers use `async with pool.acquire() as conn:` + `conn.fetch`/`conn.fetchrow`. No new index needed (existing idx_history_guild covers the guild_id prefix — RESEARCH #2).
  </action>
  <verify>
    <automated>python -m pytest tests/test_database_phase8.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `database.py` SCHEMA_SQL contains `total_errors INTEGER DEFAULT 0` (verifiable: `grep -v '^#' database.py | grep -c "total_errors INTEGER DEFAULT 0"` >= 1).
    - `increment_daily_stat` `allowed_fields` includes `"total_errors"`.
    - `database.py` defines `get_leaderboard_songs`, `get_leaderboard_skips`, `get_leaderboard_streaks`, `get_daily_stats_row`, `get_images_today_global`.
    - All leaderboard SQL uses `$1`/`$2` positional params — no f-string/`%`/`.format` interpolation of guild_id or title (T-sql-inj mitigation; verifiable: no `guild_id` inside an f-string in these helpers).
    - `python -m pytest tests/test_database_phase8.py -x` exits 0 against a live test DB. If no Postgres is available locally, `python -m pytest tests/test_database_phase8.py --collect-only -q` exits 0 and the live run is deferred to the phase gate.
  </acceptance_criteria>
  <done>All 8 database integration tests pass (or collect clean if no live DB); the total_errors column, allowlist entry, and 5 read helpers exist with parameterized SQL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: services/gemini.py RPM getters + config.py constants</name>
  <files>services/gemini.py, config.py</files>
  <read_first>
    - `services/gemini.py` lines 34-86 (`_RateLimiter` — `_timestamps` deque, `_max_requests`, `_clean()`, `_lock`) and the `GeminiService` class header (where to add the properties, after `__init__`, before `chat()`)
    - `config.py` lines ~32-40 (Cooldowns + AI constants blocks — where ASK_COOLDOWN_SECONDS lives)
    - 08-RESEARCH.md § Implementation Patterns #3 (getter signatures, Pitfall 4: must be synchronous — never add a second asyncio.Lock acquire) and § config.py
    - 08-PATTERNS.md § services/gemini.py (MODIFY) + § config.py (MODIFY)
  </read_first>
  <behavior>
    - Test: `test_rpm_usage_getter` — after N `acquire(priority=1)` calls, `rpm_usage() == N`.
    - Test: `test_rpm_headroom_getter` — `rpm_headroom() == max_requests - rpm_usage()`, floored at 0.
  </behavior>
  <action>
    In `services/gemini.py`:
    1. Add SYNCHRONOUS method `_RateLimiter.rpm_usage(self) -> int`: call `self._clean()` then `return len(self._timestamps)`. Do NOT make it async; do NOT acquire `self._lock` (Pitfall 4 — the benign read race is acceptable for a dashboard). This getter backs the /stats Gemini-quota panel (D-24).
    2. Add SYNCHRONOUS method `_RateLimiter.rpm_headroom(self) -> int`: `return max(0, self._max_requests - self.rpm_usage())`.
    3. On `GeminiService`, add `@property rpm_usage` → `self._rate_limiter.rpm_usage()` and `@property rpm_headroom` → `self._rate_limiter.rpm_headroom()` (placed after `__init__`, before `chat()`). Confirm the limiter attribute name by reading the class (`self._rate_limiter` per RESEARCH; verify in source).
    In `config.py`: add `ROAST_COOLDOWN_SECONDS = 30` (D-04) and `LEADERBOARD_TOP_N = 5` (D-13) in the Cooldowns/AI constants region adjacent to `ASK_COOLDOWN_SECONDS`. Both constants are consumed cross-plan (roast cooldown by Plan 02, top-N by Plan 01/03) — defining both here keeps config.py out of the parallel feature plans' file sets.
  </action>
  <verify>
    <automated>python -m pytest tests/test_rate_limiter.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `services/gemini.py` defines `rpm_usage` and `rpm_headroom` (verifiable: `grep -c "def rpm_usage\|def rpm_headroom" services/gemini.py` == 2).
    - `rpm_usage`/`rpm_headroom` on `_RateLimiter` are plain `def` (not `async def`) and contain no `self._lock` acquire (Pitfall 4).
    - `config.py` defines `ROAST_COOLDOWN_SECONDS` and `LEADERBOARD_TOP_N` (verifiable: `grep -c "ROAST_COOLDOWN_SECONDS\|LEADERBOARD_TOP_N" config.py` >= 2).
    - `python -m pytest tests/test_rate_limiter.py -x` exits 0 (existing tests still green + 2 new getter tests pass).
  </acceptance_criteria>
  <done>RPM getters exist and are synchronous; both new config constants exist; full rate-limiter test file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → database helpers | guild_id (Discord snowflake string) and field names cross into SQL |
| caller → increment_daily_stat | a stat-field name string crosses into a column update |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `get_leaderboard_*` SQL (guild_id, title) | mitigate | asyncpg `$1`/`$2` parameterized queries ONLY; `title` is read back from rows, never interpolated. Acceptance criterion forbids f-string interpolation of guild_id. |
| T-08-02 | Tampering | `increment_daily_stat` field-name | mitigate | The existing `allowed_fields` allowlist gates the column name; adding `"total_errors"` to the set is the correct extension. A non-allowlisted field still raises ValueError. |
| T-08-03 | Tampering | npm/pip installs | accept | No new packages this phase (RESEARCH § Package Legitimacy Audit: N/A). No install tasks. |
</threat_model>

<verification>
- `python -m pytest tests/test_database_phase8.py tests/test_rate_limiter.py -x` green (live DB), or `--collect-only -q` clean where no Postgres is available + live run deferred to phase gate.
- `database.py` SCHEMA_SQL idempotent (re-running init_db does not error — ADD COLUMN IF NOT EXISTS).
- No new external dependencies added (requirements.txt unchanged).
</verification>

<success_criteria>
- total_errors column + allowlist entry exist and round-trip via increment_daily_stat (OPS-01 foundation).
- Three guild-scoped leaderboard helpers return correct, parameterized aggregates (SOCIAL-02 foundation).
- get_daily_stats_row + get_images_today_global read helpers exist (OPS-01/OPS-03 foundation).
- rpm_usage/rpm_headroom expose current RPM synchronously (OPS-03 foundation).
- Wave-0 test files exist so Plan 02/03 behavior tasks have automated verifies.
</success_criteria>

<output>
Create `.planning/phases/08-social-ops/08-01-SUMMARY.md` when done.
</output>
