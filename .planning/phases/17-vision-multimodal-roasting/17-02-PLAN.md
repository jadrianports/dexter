---
phase: 17-vision-multimodal-roasting
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - personality/prompts.py
  - personality/roasts.py
  - cogs/events.py
  - tests/test_vision_events.py
autonomous: true
requirements: [VIS-01, VIS-02]
must_haves:
  truths:
    - "An oversized (> MAX_VISION_IMAGE_BYTES) or non-allowlisted (e.g. image/gif) attachment is rejected on metadata alone — attachment.read() and Gemini are never called"
    - "A safety-blocked or empty vision response produces NO reply (silent skip) and does NOT mark the cooldown"
    - "A transport failure (rate-limit / API-down) on the vision call produces a template-fallback reply from VISION_ROAST_FALLBACKS"
    - "A vision roast is posted as a reply with AllowedMentions.none() + mention_author=False, only in the designated channel"
    - "A user opted out via proactive_opt_out receives no vision roast (shared Phase 16 control, no new flag)"
    - "The vision prompt instructs Dex to roast the image's content/vibe, never a real person's face/body/weight/identity"
  artifacts:
    - path: "cogs/events.py"
      provides: "_first_valid_image_attachment gate + _generate_vision_roast (str|None dispatch) + _maybe_fire_vision_roast + on_message dispatch + _vision_roast_cooldowns"
      contains: "_maybe_fire_vision_roast"
    - path: "personality/prompts.py"
      provides: "build_vision_prompt conduct-clause system prompt"
      contains: "def build_vision_prompt"
    - path: "personality/roasts.py"
      provides: "VISION_ROAST_FALLBACKS transport-failure-only pool"
      contains: "VISION_ROAST_FALLBACKS"
    - path: "tests/test_vision_events.py"
      provides: "behavioral lock — structural gate reject, silent-skip vs template-fallback, reply-anchor, opt-out"
      contains: "_maybe_fire_vision_roast"
  key_links:
    - from: "cogs/events.py on_message"
      to: "_maybe_fire_vision_roast"
      via: "designated-channel + attachments guard dispatch"
      pattern: "_maybe_fire_vision_roast"
    - from: "cogs/events.py _maybe_fire_vision_roast"
      to: "database.get_proactive_opt_out"
      via: "shared Phase 16 opt-out read"
      pattern: "get_proactive_opt_out"
    - from: "cogs/events.py _maybe_fire_vision_roast"
      to: "logic.vision.should_fire_vision_roast"
      via: "primitives (opted_out, cooldown_elapsed, chance_roll)"
      pattern: "should_fire_vision_roast"
    - from: "cogs/events.py _generate_vision_roast"
      to: "gemini_service.chat(image_bytes=..., priority=2)"
      via: "str|None dispatch (None->silent skip, exception->fallback)"
      pattern: "image_bytes="
    - from: "cogs/events.py _maybe_fire_vision_roast"
      to: "message.reply"
      via: "AllowedMentions.none() reply-anchored send"
      pattern: "AllowedMentions.none"
---

<objective>
Wire the vision-roast trigger surface end-to-end: the conduct-clause vision prompt (D-03 step 2),
the transport-failure-only fallback pool, and the `cogs/events.py` glue — a before-download
structural mime/size gate (D-02 / VIS-01), a dedicated `str | None` generator that silently skips
on a safety block and template-falls-back only on transport failure (D-04 / VIS-02), and the
`on_message` dispatch that makes this a fourth independent unprompted cadence reusing the Phase 16
opt-out and reply-anchored send.

Purpose: This is the surface that actually reacts to user images. It depends on 17-01 (the config
knobs, the pure gate, and the image-carrying `chat()`), so it runs in Wave 2. The safety-critical
piece is the silent-skip-vs-fallback dispatch: reusing `_generate_ambient_roast` unmodified would
make a safety-blocked image emit the same visible template as a rate-limited one (RESEARCH Pitfall 1),
so this plan introduces a dedicated generator and locks the distinction with a behavioral test.

Output: `build_vision_prompt`, `VISION_ROAST_FALLBACKS`, the four `cogs/events.py` additions
(`_first_valid_image_attachment`, `_generate_vision_roast`, `_maybe_fire_vision_roast`, the
`on_message` dispatch + `_vision_roast_cooldowns`), and `tests/test_vision_events.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-vision-multimodal-roasting/17-CONTEXT.md
@.planning/phases/17-vision-multimodal-roasting/17-RESEARCH.md
@.planning/phases/17-vision-multimodal-roasting/17-PATTERNS.md
@.planning/phases/17-vision-multimodal-roasting/17-01-PLAN.md
@cogs/events.py
@personality/prompts.py
@personality/roasts.py
</context>

<artifacts_this_phase>
This plan (17-02) produces: `personality/prompts.py::build_vision_prompt`,
`personality/roasts.py::VISION_ROAST_FALLBACKS`, `cogs/events.py::_first_valid_image_attachment`,
`cogs/events.py::EventsCog._generate_vision_roast`, `cogs/events.py::EventsCog._maybe_fire_vision_roast`,
the `EventsCog.on_message` vision dispatch block, `cogs/events.py::EventsCog._vision_roast_cooldowns`,
and `tests/test_vision_events.py`.

Consumed from 17-01 (Wave 1): `config.VISION_ROAST_CHANCE`, `config.VISION_ROAST_COOLDOWN_SECONDS`,
`config.MAX_VISION_IMAGE_BYTES`, `config.VISION_MIME_ALLOWLIST`, `logic.vision.should_fire_vision_roast`,
and `GeminiService.chat(..., image_bytes=..., image_mime_type=...)`.
</artifacts_this_phase>

<tasks>

<task type="auto">
  <name>Task 1: Vision conduct-clause prompt builder + transport-failure-only fallback pool (VIS-02 / D-03 step 2)</name>
  <files>personality/prompts.py, personality/roasts.py</files>
  <read_first>
    @personality/prompts.py (build_discover_commentary_prompt / build_jam_suggestion_prompt, ~lines 238-283 — the small-dedicated-builder shape to mirror, NOT the full few-shot build_chat_prompt)
    @personality/roasts.py (PROACTIVE_CALLBACK_FALLBACKS ~lines 215-222 + the __all__ list ~lines 20-39 + the LOCKED voice-register header)
    @.planning/phases/17-vision-multimodal-roasting/17-PATTERNS.md (the personality/prompts.py and personality/roasts.py sections)
  </read_first>
  <action>
    In personality/prompts.py add a module-level `VISION_ROAST_PROMPT` string and a
    `build_vision_prompt() -> str` builder modeled on build_discover_commentary_prompt's small
    dedicated shape (NOT build_chat_prompt's full few-shot DEXTER block — keeping the inline request
    small matters under Gemini's 20MB combined inline-data cap, RESEARCH Pitfall 4). The prompt MUST
    instruct Dex to (a) react to / roast the image's content, vibe, or subject matter in his dry
    lowercase voice, one short line under ~120 characters, no preamble; and (b) carry the D-03 step 2
    conduct clause verbatim in intent — NEVER comment on a real person's face, body, weight, or
    perceived identity, and if the image is primarily a person, keep the roast about the scene or
    context, not their appearance.

    In personality/roasts.py add `VISION_ROAST_FALLBACKS: list[str]` — at least four dry, lowercase,
    self-contained lines (NO {name}/{title}/{count} placeholders, so the generator can pick_random
    them without .format()). These fire ONLY on a transport failure (rate-limit / API-down), NEVER on
    a safety block (D-04 — the safety block is a silent skip handled in Task 2). Keep the LOCKED
    voice-register constraints (lowercase, <=500 chars, one emoji max, contempt aimed outward). Add
    the string "VISION_ROAST_FALLBACKS" to the module `__all__`.
  </action>
  <verify>
    <automated>python -c "from personality.prompts import build_vision_prompt; p=build_vision_prompt().lower(); assert 'face' in p and ('body' in p or 'appearance' in p), p; from personality.roasts import VISION_ROAST_FALLBACKS; assert len(VISION_ROAST_FALLBACKS)>=4 and all('{' not in l for l in VISION_ROAST_FALLBACKS)"</automated>
  </verify>
  <acceptance_criteria>
    - personality/prompts.py contains `def build_vision_prompt(`
    - build_vision_prompt() output (lowercased) contains "face" AND ("body" OR "appearance") — the conduct clause is present, not buried
    - personality/roasts.py defines `VISION_ROAST_FALLBACKS` with >= 4 placeholder-free lines and lists "VISION_ROAST_FALLBACKS" in __all__
    - the single python -c verify command exits 0
  </acceptance_criteria>
  <done>The conduct-clause vision prompt builder and the transport-only fallback pool exist, are
  importable, and the conduct clause is explicit in the prompt text.</done>
</task>

<task type="auto">
  <name>Task 2: cogs/events.py vision glue — structural gate, str|None generator, dispatch (VIS-01/02 / D-02/D-03/D-04)</name>
  <files>cogs/events.py</files>
  <read_first>
    @cogs/events.py (imports ~1-21; __init__ ~32-42 incl. _ambient_roast_times; _generate_ambient_roast ~95-201 — the generator to NOT reuse unmodified; on_message ~376-402; _maybe_fire_proactive_callback ~406-514 — the structural template)
    @logic/vision.py (should_fire_vision_roast — from 17-01)
    @logic/roasts.py (cooldown_elapsed — reuse, do not reimplement)
    @database.py (get_proactive_opt_out(pool, user_id) ~356-376 — shared opt-out, no new column)
    @.planning/phases/17-vision-multimodal-roasting/17-PATTERNS.md (the cogs/events.py section + Shared Patterns: silent-skip-vs-fallback, AllowedMentions.none, opt-out reuse, cooldown reuse)
    @.planning/phases/17-vision-multimodal-roasting/17-RESEARCH.md (Pitfall 1, Pitfall 3 content_type normalization, Pattern 3 Option A, the attachment before-download guard)
  </read_first>
  <action>
    Extend cogs/events.py. Imports: add `from logic.vision import should_fire_vision_roast`; add
    `cooldown_elapsed` to the existing `from logic.roasts import ...`; add `from personality.prompts
    import build_vision_prompt`; add `GeminiAPIError` to the existing `from services.gemini import
    GeminiRateLimitError`. In `EventsCog.__init__` add `self._vision_roast_cooldowns: dict[int, float]
    = {}` (int-keyed, mirrors _ambient_roast_times).

    Add a module-level helper `_first_valid_image_attachment(message: discord.Message) ->
    discord.Attachment | None` (D-02 structural gate, VIS-01): iterate `message.attachments`; for each,
    normalize `(attachment.content_type or "").split(";")[0].strip().lower()` (handles content_type
    None -> reject and "; charset=" params -> normalize, RESEARCH Pitfall 3); `continue` if the
    normalized type is not in `config.VISION_MIME_ALLOWLIST` or if `attachment.size >
    config.MAX_VISION_IMAGE_BYTES`; return the FIRST passing attachment (roast the first valid image,
    D-02) with ZERO bytes fetched; else return None.

    Add async `_generate_vision_roast(self, member: discord.Member, image_bytes: bytes, mime_type: str)
    -> str | None` per Shared-Patterns Option A (a DEDICATED generator — do NOT branch or reuse
    _generate_ambient_roast, whose always-returns-str contract collapses safety-block and transport-
    failure, RESEARCH Pitfall 1): getattr `gemini_service` (None -> return None, silent skip); call
    `gemini_service.chat(build_vision_prompt(), [{"role": "user", "content": "react to this image in
    one line."}], priority=2, image_bytes=image_bytes, image_mime_type=mime_type)` inside a try; on
    `except (GeminiRateLimitError, GeminiAPIError)` return `pick_random(roasts.VISION_ROAST_FALLBACKS)`
    (transport -> template, D-04); if the result is falsy return None (safety-blocked OR genuinely
    empty -> silent skip, VIS-02 — MUST NOT return a fallback line here); otherwise strip, lowercase
    the first char if uppercase, and cap to <=500 chars (mirroring _generate_ambient_roast's
    enforcement).

    Add async `_maybe_fire_vision_roast(self, message: discord.Message) -> None` (structure copied
    from _maybe_fire_proactive_callback): read `opted_out = await
    database.get_proactive_opt_out(self.bot.pool, str(message.author.id))` inside try/except -> on
    exception log.debug + return (fail closed); `attachment = _first_valid_image_attachment(message)`
    (None -> return, before any I/O); compute `seconds_since_last =
    asyncio.get_event_loop().time() - self._vision_roast_cooldowns.get(message.author.id, 0.0)`; gate
    on `should_fire_vision_roast(opted_out=opted_out, cooldown_elapsed=cooldown_elapsed(seconds_since_last,
    config.VISION_ROAST_COOLDOWN_SECONDS), chance_roll=random.random())` (False -> return, no I/O,
    no cooldown mark); only now `image_bytes = await attachment.read()` (the single network fetch);
    `line = await self._generate_vision_roast(message.author, image_bytes, attachment.content_type)`;
    if line is None return (VIS-02 silent skip — no send, no cooldown mark); else
    `await message.reply(line, allowed_mentions=discord.AllowedMentions.none(), mention_author=False)`
    inside try/except discord.HTTPException (on HTTPException return without marking cooldown); on a
    successful send set `self._vision_roast_cooldowns[message.author.id] =
    asyncio.get_event_loop().time()`.

    In on_message, add a dispatch block BESIDE the existing proactive-callback block (after ~line 402),
    as a FOURTH independent cadence (do NOT merge with the proactive gate): guard
    `message.guild is not None and config.DEXTER_CHANNEL_ID and message.channel.id ==
    config.DEXTER_CHANNEL_ID and message.attachments` -> `await self._maybe_fire_vision_roast(message)`.
  </action>
  <verify>
    <automated>python -c "import cogs.events, inspect; s=inspect.getsource(cogs.events); assert all(k in s for k in ['_maybe_fire_vision_roast','_generate_vision_roast','_first_valid_image_attachment','_vision_roast_cooldowns','should_fire_vision_roast','AllowedMentions.none'])" && pytest tests/test_gemini.py tests/test_vision_logic.py tests/test_proactive_events.py -q</automated>
  </verify>
  <acceptance_criteria>
    - cogs/events.py contains `_first_valid_image_attachment`, `_generate_vision_roast`, `_maybe_fire_vision_roast`, and `self._vision_roast_cooldowns`
    - cogs/events.py imports `should_fire_vision_roast`, `cooldown_elapsed`, `build_vision_prompt`, and `GeminiAPIError`
    - `grep -n "_maybe_fire_vision_roast" cogs/events.py` shows the on_message dispatch under the DEXTER_CHANNEL_ID + `message.attachments` guard
    - `_generate_vision_roast` calls `chat(` with `image_bytes=` and `priority=2`, returns None on a falsy result, and returns `pick_random(roasts.VISION_ROAST_FALLBACKS)` in the `except (GeminiRateLimitError, GeminiAPIError)` clause
    - `_maybe_fire_vision_roast` calls `attachment.read()` only AFTER `should_fire_vision_roast` returns True
    - the python -c symbol/smoke check exits 0 AND the targeted regression suite (`tests/test_gemini.py tests/test_vision_logic.py tests/test_proactive_events.py`) stays green (events.py imports cleanly, prior surfaces intact)
  </acceptance_criteria>
  <done>The vision trigger is wired: a before-download mime/size gate, a dedicated str|None generator
  that silently skips on safety/empty and template-falls-back only on transport failure, the pure-gate
  cadence check reusing the shared opt-out + cooldown helper, and the reply-anchored AllowedMentions.none()
  send dispatched from on_message as a fourth independent cadence.</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_vision_events.py — behavioral lock for the VIS-01/VIS-02 dispatch contract</name>
  <files>tests/test_vision_events.py</files>
  <read_first>
    @tests/test_proactive_events.py (the _make_bot / _make_message mock helpers + the spec=discord.* + patch("cogs.events.random.random") style to mirror)
    @cogs/events.py (the _maybe_fire_vision_roast / _generate_vision_roast / _first_valid_image_attachment written in Task 2)
    @.planning/phases/17-vision-multimodal-roasting/17-VALIDATION.md (the VIS-01/VIS-02 rows in the Per-Task Verification Map — the exact behaviors to lock)
    @.planning/phases/17-vision-multimodal-roasting/17-RESEARCH.md (Validation Architecture §Phase Requirements -> Test Map — structural gate, safety-block silent skip, transport fallback, vis02 regression)
  </read_first>
  <action>
    Create tests/test_vision_events.py mirroring tests/test_proactive_events.py's mock scaffold. Add a
    `_make_attachment(content_type, size)` helper (MagicMock with `content_type`, `size`, and an
    AsyncMock `read`) and extend `_make_message` to carry an `attachments` list. Lock these behaviors,
    each as its own test (mock Gemini via patching `_generate_vision_roast` where the dispatch decision
    is under test, and patch `cogs.events.database.get_proactive_opt_out` + `cogs.events.random.random`
    as in the proactive tests):

    1. structural_gate: a message whose only attachment is oversized (size > MAX_VISION_IMAGE_BYTES)
       OR a non-allowlisted type (e.g. "image/gif") -> `_first_valid_image_attachment` returns None ->
       `_maybe_fire_vision_roast` awaits neither `attachment.read()` nor `message.reply` (no bytes
       fetched, no Gemini). Also assert a "image/jpeg; charset=utf-8" content_type IS accepted
       (Pitfall 3 normalization).
    2. safety_block_silent_skip (VIS-02): gate forced to pass (opt-out False, random.random -> 0.0,
       cooldown fresh), `_generate_vision_roast` patched to return None -> `message.reply` NOT awaited
       AND `_vision_roast_cooldowns` is not marked for the author.
    3. transport_fallback (VIS-02): gate forced to pass, `_generate_vision_roast` patched to return a
       VISION_ROAST_FALLBACKS-style string -> `message.reply` IS awaited once with that string.
    4. vis02_regression: a direct unit test of `_generate_vision_roast` — with a fake gemini_service
       whose `chat` is an AsyncMock returning None -> result is None (silent skip); with `chat`
       side_effect=GeminiRateLimitError (and separately GeminiAPIError) -> result is a member of
       roasts.VISION_ROAST_FALLBACKS. This is the safety-block-vs-transport distinction VIS-02 requires.
    5. reply_anchor: on a real fire, `message.reply` is called with
       `allowed_mentions=discord.AllowedMentions.none()` and `mention_author=False`, and the cooldown
       is marked.
    6. opt_out: `get_proactive_opt_out` -> True -> no read, no reply (shared Phase 16 control).
  </action>
  <verify>
    <automated>pytest tests/test_vision_events.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_vision_events.py exists with tests covering: structural gate reject (size + mime + content_type normalization), safety-block silent skip, transport-failure template fallback, the direct _generate_vision_roast None-vs-fallback regression, reply-anchor + AllowedMentions.none(), and opt-out
    - The safety-block test asserts `message.reply` is NOT awaited and the cooldown is NOT marked
    - The transport-failure test asserts `message.reply` IS awaited with a fallback line
    - `pytest tests/test_vision_events.py -q` exits 0
  </acceptance_criteria>
  <done>The behavioral contract that distinguishes silent-skip (safety/empty) from template-fallback
  (transport) is locked by an automated test, along with the before-download structural gate and the
  reply-anchored, opt-out-respecting send.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user upload -> bot | Arbitrary user-uploaded image bytes + attachment metadata enter via `on_message` |
| Image content -> Gemini vision prompt | Adversarial text/imagery inside the image can attempt to override the conduct clause |
| Vision response -> Discord channel | A safety-blocked reaction must leave zero visible trace (VIS-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Denial of Service / Tampering | oversized / wrong-mime attachment | mitigate | `_first_valid_image_attachment` before-download gate rejects on `attachment.size > MAX_VISION_IMAGE_BYTES` or a type not in `VISION_MIME_ALLOWLIST` using metadata only — zero bytes fetched for a rejected attachment; locked by the structural_gate test |
| T-17-02 | Information Disclosure / EoP | image byte source (SSRF) | mitigate | Attachments-only trigger (D-02) — bytes come solely from `attachment.read()` (Discord CDN), never an arbitrary user-supplied URL/embed; the on_message guard keys on `message.attachments`, never message content URLs |
| T-17-03 | Tampering | content-type spoofing (metadata says image/png, bytes differ) | accept | Metadata gate is the cheap pre-download reject; Gemini's server-side decode is the authoritative backstop — a spoofed-but-undecodable file 400s -> `GeminiAPIError` -> the defined transport-fallback path. No hand-rolled magic-byte sniffing (no new dependency; RESEARCH Don't Hand-Roll) |
| T-17-04 | Tampering (of intended behavior) / reputational harm | roasting a real person's appearance | mitigate | `build_vision_prompt` conduct clause instructs Dex to roast content/vibe, NEVER a real person's face/body/weight/identity (D-03 step 2); the prompt-text assertion in Task 1 verifies the clause is present |
| T-17-05 | Tampering / Elevation of Privilege | prompt injection via adversarial text embedded in the image | mitigate | Defense in depth: the app-level conduct clause (this plan) PLUS the model-level real-block `safety_settings` (17-01); neither layer is relied on alone (VIS-02 mandates structural + model) |
| T-17-06 | Information Disclosure | safety-block leaking as a visible template reply | mitigate | Dedicated `_generate_vision_roast` returns None on a safety-blocked/empty response (silent skip) and a fallback ONLY on `GeminiRateLimitError`/`GeminiAPIError`; the vis02_regression + safety_block_silent_skip tests lock the two paths apart (RESEARCH Pitfall 1) |
| T-17-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Phase installs ZERO new packages (RESEARCH Package Legitimacy Audit: N/A). No slopcheck / human checkpoint required |
</threat_model>

<verification>
- `pytest tests/test_vision_events.py -q` green (structural gate, silent-skip vs template-fallback, reply-anchor, opt-out)
- `python -c` symbol/smoke import of cogs.events succeeds (glue imports cleanly)
- Wave merge gate: `pytest -q` full suite green (the fourth cadence must not regress ambient/proactive surfaces or /ask)
</verification>

<success_criteria>
- Dexter occasionally reacts to an image posted in the designated channel, gated by chance + per-user cooldown + priority-2 — not every image, not every channel (VIS-01)
- Oversized or wrong-mime images are rejected before download, never reaching the Gemini call (VIS-01)
- A safety-blocked image reaction is silently skipped — no visible refusal, never routed through the generic transport-fallback template (VIS-02)
- The vision roast is reply-anchored with AllowedMentions.none(), respects the shared proactive opt-out, and never comments on a real person's appearance (D-02/D-03/D-04)
</success_criteria>

<output>
Create `.planning/phases/17-vision-multimodal-roasting/17-02-SUMMARY.md` when done.

Note (parked for phase close, per Phase 11/13/14/15/16 precedent): the live "feel"/cadence of the
roast on a real posted image, a genuine Gemini vision round-trip, a real safety-block producing zero
visible trace, and /ask + /imagine being unregressed to a human ear are all live-Discord manual
checks — record them in `17-HUMAN-UAT.md` at phase close (see 17-VALIDATION.md §Manual-Only).
</output>
