---
phase: 17-vision-multimodal-roasting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - services/gemini.py
  - logic/vision.py
  - tests/test_vision_logic.py
  - tests/test_gemini.py
autonomous: true
requirements: [VIS-01, VIS-03]
must_haves:
  truths:
    - "Every user-influenced Gemini call (chat/ambient/proactive via chat(), /imagine via generate_image(), and the new vision call) sends explicit safety_settings"
    - "The vision path uses a real block threshold; /ask + /imagine + all non-image chat() calls use a permissive-but-explicit threshold (no personality regression)"
    - "chat() carrying image bytes returns None on a safety-blocked/empty response and raises only on transport failure (429/API/network)"
    - "should_fire_vision_roast fires only when not opted out, cooldown elapsed, and chance_roll strictly below chance"
    - "VISION_ROAST_CHANCE is strictly rarer than UNPROMPTED_ROAST_CHANCE (0.30) and MEMORY_CALLBACK_CHANCE (0.35)"
  artifacts:
    - path: "logic/vision.py"
      provides: "should_fire_vision_roast pure cadence gate"
      contains: "def should_fire_vision_roast"
    - path: "services/gemini.py"
      provides: "_build_safety_settings helper + image-carrying chat() path + safety_settings threaded into all generate_content configs"
      contains: "def _build_safety_settings"
    - path: "config.py"
      provides: "Phase 17 vision knobs (cadence, guards, per-surface safety thresholds)"
      contains: "VISION_SAFETY_THRESHOLD"
    - path: "tests/test_vision_logic.py"
      provides: "mock-free boundary + rarity truth table for the pure gate"
      contains: "should_fire_vision_roast"
    - path: "tests/test_gemini.py"
      provides: "safety_settings threading + threshold differentiation + None-on-block contract tests"
      contains: "safety_settings"
  key_links:
    - from: "services/gemini.py chat() / generate_image()"
      to: "_build_safety_settings"
      via: "GenerateContentConfig(safety_settings=...)"
      pattern: "safety_settings=_build_safety_settings"
    - from: "logic/vision.py"
      to: "config.VISION_ROAST_CHANCE"
      via: "keyword-only default arg"
      pattern: "config\\.VISION_ROAST_CHANCE"
---

<objective>
Establish the config-dependent leaf foundations for vision/multimodal roasting: the explicit
`safety_settings` retrofit across all three user-influenced Gemini `generate_content` sites
(D-01 / VIS-03), the image-carrying extension of `GeminiService.chat()` that preserves the
`None`-on-block / raise-on-transport contract VIS-02 depends on, and the pure `logic/vision.py`
cadence gate (D-04 / VIS-01) plus every Phase 17 config knob.

Purpose: This is the "everything the events glue consumes" wave. It owns `config.py` in full
(cadence + guard + per-surface safety-threshold constants) so the Wave-2 glue plan can read
those symbols without a config-file merge conflict, and it lands the safety hardening as an
auditable, tested change independent of the trigger surface.

Output: `logic/vision.py`, the extended/retrofitted `services/gemini.py`, six new `config.py`
constants, and the two unit-test locks (`tests/test_vision_logic.py`, `tests/test_gemini.py`
additions).
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-vision-multimodal-roasting/17-CONTEXT.md
@.planning/phases/17-vision-multimodal-roasting/17-RESEARCH.md
@.planning/phases/17-vision-multimodal-roasting/17-PATTERNS.md
@logic/proactive.py
@logic/roasts.py
@services/gemini.py
@config.py
</context>

<artifacts_this_phase>
Full symbol inventory for Phase 17 (both plans). This plan (17-01) produces the ★-marked items;
the rest are produced by 17-02 and listed here so executors see the complete contract.

Config knobs (config.py — ★ this plan):
- ★ `VISION_ROAST_CHANCE = 0.12`
- ★ `VISION_ROAST_COOLDOWN_SECONDS = 600`
- ★ `MAX_VISION_IMAGE_BYTES = 8 * 1024 * 1024`
- ★ `VISION_MIME_ALLOWLIST = frozenset({"image/png", "image/jpeg", "image/webp"})`
- ★ `VISION_SAFETY_THRESHOLD = "BLOCK_MEDIUM_AND_ABOVE"`
- ★ `TEXT_SAFETY_THRESHOLD = "BLOCK_ONLY_HIGH"`

Functions / methods:
- ★ `logic/vision.py::should_fire_vision_roast` (NEW pure gate)
- ★ `services/gemini.py::_build_safety_settings` (NEW module helper)
- ★ `services/gemini.py::_SAFETY_CATEGORIES` (NEW module tuple)
- ★ `services/gemini.py::GeminiService.chat` (MODIFIED — optional `image_bytes`/`image_mime_type` kwargs + safety_settings)
- ★ `services/gemini.py::GeminiService.generate_image` (MODIFIED — safety_settings threaded)
- `personality/prompts.py::build_vision_prompt` (17-02)
- `personality/roasts.py::VISION_ROAST_FALLBACKS` (17-02)
- `cogs/events.py::_first_valid_image_attachment` (17-02)
- `cogs/events.py::EventsCog._generate_vision_roast` (17-02)
- `cogs/events.py::EventsCog._maybe_fire_vision_roast` (17-02)
- `cogs/events.py::EventsCog._vision_roast_cooldowns` (17-02)

Test files:
- ★ `tests/test_vision_logic.py` (NEW)
- ★ `tests/test_gemini.py` (MODIFIED)
- `tests/test_vision_events.py` (17-02)
</artifacts_this_phase>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase 17 config knobs + pure logic/vision.py cadence gate (VIS-01 / D-04)</name>
  <files>config.py, logic/vision.py, tests/test_vision_logic.py</files>
  <read_first>
    @config.py (the Phase 16 proactive block, ~lines 227-232, for placement + the rarity-comparison constants UNPROMPTED_ROAST_CHANCE / MEMORY_CALLBACK_CHANCE)
    @logic/proactive.py (should_fire_proactive_callback — the exact keyword-only, cheapest-gate-first shape to mirror)
    @logic/roasts.py (cooldown_elapsed + the `>=`/strictly-less-than boundary convention)
    @tests/test_proactive_logic.py (the mock-free boundary-test convention to mirror)
    @.planning/phases/17-vision-multimodal-roasting/17-PATTERNS.md (the `logic/vision.py` and `config.py` sections — copy those shapes exactly)
  </read_first>
  <behavior>
    - opted_out=True with cooldown_elapsed=True and chance_roll=0.0 -> False (opt-out wins, cheapest gate)
    - cooldown_elapsed=False with a passing chance_roll -> False
    - chance_roll exactly == VISION_ROAST_CHANCE (at threshold) -> False (strictly-less-than passes)
    - chance_roll == VISION_ROAST_CHANCE - 0.01, cooldown_elapsed=True, opted_out=False -> True
    - Rarity invariant: config.VISION_ROAST_CHANCE < config.UNPROMPTED_ROAST_CHANCE AND < config.MEMORY_CALLBACK_CHANCE
  </behavior>
  <action>
    Append a `# --- Phase 17: Vision / Multimodal Roasting ---` block to config.py directly after
    the Phase 16 proactive block. Add exactly six constants with these names and values:
    `VISION_ROAST_CHANCE = 0.12` (D-04 rarity — strictly below UNPROMPTED_ROAST_CHANCE 0.30 and
    MEMORY_CALLBACK_CHANCE 0.35); `VISION_ROAST_COOLDOWN_SECONDS = 600` (per-user cooldown ceiling
    passed to logic.roasts.cooldown_elapsed by the Wave-2 glue); `MAX_VISION_IMAGE_BYTES = 8 * 1024 * 1024`
    (8MB raw — headroom below Gemini's 20MB combined inline-request cap after ~33% base64 inflation,
    RESEARCH Pitfall 4); `VISION_MIME_ALLOWLIST = frozenset({"image/png", "image/jpeg", "image/webp"})`
    (image/gif DELIBERATELY EXCLUDED — Gemini image-understanding 400s on GIF, RESEARCH Pitfall 2 /
    D-03 "planner may trim"); `VISION_SAFETY_THRESHOLD = "BLOCK_MEDIUM_AND_ABOVE"` (D-01 real block —
    vision only); `TEXT_SAFETY_THRESHOLD = "BLOCK_ONLY_HIGH"` (D-01 permissive-but-explicit for /ask +
    /imagine + every non-image chat() call — must NOT regress edgy output). Add a one-line comment on
    VISION_ROAST_CHANCE restating the strict-rarity constraint (mirrors the Phase 16 comment).

    Then create logic/vision.py mirroring logic/proactive.py: a module docstring stating the module
    is deterministic and side-effect-free (no random, no asyncio, no datetime, no discord — glue
    computes rolls/cooldowns/opt-out and passes primitives, per D-04 and the Phase 10 seam
    convention), `from __future__ import annotations`, `import config`, and a single keyword-only
    function `should_fire_vision_roast(*, opted_out: bool, cooldown_elapsed: bool, chance_roll: float,
    chance: float = config.VISION_ROAST_CHANCE) -> bool`. Gate order (cheapest first, matching
    should_fire_proactive_callback): if opted_out return False; if not cooldown_elapsed return False;
    if chance_roll >= chance return False; else return True. Do NOT reimplement cooldown math here —
    the glue reuses logic.roasts.cooldown_elapsed.

    Create tests/test_vision_logic.py as a mock-free class TestShouldFireVisionRoast mirroring
    tests/test_proactive_logic.py: assert each boundary row in <behavior>, plus a standalone
    rarity-invariant test asserting VISION_ROAST_CHANCE is strictly below both ambient cadences.
  </action>
  <verify>
    <automated>pytest tests/test_vision_logic.py -q</automated>
  </verify>
  <acceptance_criteria>
    - config.py defines all six constants with the exact names above
    - `python -c "import config; assert config.VISION_ROAST_CHANCE < config.UNPROMPTED_ROAST_CHANCE and config.VISION_ROAST_CHANCE < config.MEMORY_CALLBACK_CHANCE"` exits 0
    - `python -c "import config; assert 'image/gif' not in config.VISION_MIME_ALLOWLIST"` exits 0
    - `python -c "import config; assert config.VISION_SAFETY_THRESHOLD != config.TEXT_SAFETY_THRESHOLD"` exits 0
    - logic/vision.py contains `def should_fire_vision_roast(`
    - `grep -nE "^import (random|discord|datetime)|^from (random|discord|datetime)" logic/vision.py` returns nothing (pure module — no nondeterministic imports)
    - tests/test_vision_logic.py exists and `pytest tests/test_vision_logic.py -q` exits 0
  </acceptance_criteria>
  <done>All six config knobs exist with the specified names/values; logic/vision.py exposes the
  keyword-only pure gate with the exact cheapest-gate-first semantics; the mock-free truth table +
  rarity invariant are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared safety_settings helper + retrofit + image-carrying chat() (VIS-03 / VIS-02 plumbing / D-01)</name>
  <files>services/gemini.py, tests/test_gemini.py</files>
  <read_first>
    @services/gemini.py (chat() ~146-207, generate_image() ~209-248, the errors.APIError try/except contract ~197-204, _RateLimiter ~34-103)
    @tests/test_gemini.py (the existing TestGeminiChat `patch("services.gemini.genai")` + AsyncMock scaffold to reuse)
    @.planning/phases/17-vision-multimodal-roasting/17-PATTERNS.md (the `services/gemini.py` section — helper shape, call-site edits, and the CRITICAL contract-preservation note)
    @.planning/phases/17-vision-multimodal-roasting/17-RESEARCH.md (Pattern 1/2, the block-detection recipe, and Assumption A2 on the adjustable HarmCategory set)
  </read_first>
  <behavior>
    - Plain chat() (no image) passes a GenerateContentConfig whose safety_settings is non-None and whose threshold == config.TEXT_SAFETY_THRESHOLD
    - chat(image_bytes=b"...", image_mime_type="image/png") passes threshold == config.VISION_SAFETY_THRESHOLD and appends a from_bytes image part to the last Content
    - generate_image() passes a GenerateContentConfig whose safety_settings is non-None and threshold == config.TEXT_SAFETY_THRESHOLD
    - VISION_SAFETY_THRESHOLD and TEXT_SAFETY_THRESHOLD are two distinct values, asserted by equality/identity across the two call shapes
    - chat() with a mocked response.text of None returns None (no exception) — the safety-blocked/empty contract
    - chat() where generate_content raises errors.APIError(code=429) raises GeminiRateLimitError; a non-429 APIError raises GeminiAPIError
  </behavior>
  <action>
    In services/gemini.py add a module-level `_SAFETY_CATEGORIES` tuple of the four adjustable
    HarmCategory strings: "HARM_CATEGORY_HARASSMENT", "HARM_CATEGORY_HATE_SPEECH",
    "HARM_CATEGORY_SEXUALLY_EXPLICIT", "HARM_CATEGORY_DANGEROUS_CONTENT" (place near _RateLimiter),
    and a helper `_build_safety_settings(threshold: str) -> list[types.SafetySetting]` returning one
    `types.SafetySetting(category=cat, threshold=threshold)` per category. Per RESEARCH Assumption A2,
    before locking the tuple verify `list(types.HarmCategory)` at implementation time and include any
    additional adjustable, non-deprecated, non-UNSPECIFIED category the installed SDK exposes (do not
    hardcode past what the enum actually offers).

    Thread `safety_settings=_build_safety_settings(...)` into BOTH existing GenerateContentConfig
    construction sites: generate_image() (~line 227) uses `config.TEXT_SAFETY_THRESHOLD`; chat()
    (~line 193) selects the threshold by input — `config.VISION_SAFETY_THRESHOLD if image_bytes is not
    None else config.TEXT_SAFETY_THRESHOLD`.

    Extend chat()'s signature with two keyword-only optional params after `priority`:
    `*, image_bytes: bytes | None = None, image_mime_type: str | None = None`. After the existing
    contents-building loop, when image_bytes is not None and contents is a non-empty list, append
    `types.Part.from_bytes(data=image_bytes, mime_type=image_mime_type)` to the last Content's parts
    (compose the image onto the final user turn); the `mime_type` is the attachment's already-gate-
    validated content_type — do not re-derive it from bytes.

    PRESERVE the return/raise contract exactly (D-01 / VIS-02 hinge): return
    `response.text if response.text else None` (None on empty/blocked — NEVER raise for a safety
    block); only errors.APIError (429 -> GeminiRateLimitError, else GeminiAPIError) and a generic
    Exception (-> GeminiAPIError) raise. Do NOT add prompt_feedback.block_reason / finish_reason
    branching that changes the return/raise contract — such detection is permitted for `log.debug`
    observability ONLY, because the silent-skip-vs-fallback decision lives one layer up in
    cogs/events.py (17-02). The image_bytes call still goes through `self._rate_limiter.acquire(priority)`
    unchanged (priority-2 from the vision glue rides the existing 15 RPM limiter — VIS-01, no new limiter).

    Add tests to tests/test_gemini.py reusing the existing genai-patch scaffold: assert each row in
    <behavior> by inspecting the `config=` kwarg captured from the mocked generate_content call_args
    (`kwargs["config"].safety_settings` non-None and `.safety_settings[0].threshold`), and add the
    None-on-empty + 429->GeminiRateLimitError variants.
  </action>
  <verify>
    <automated>pytest tests/test_gemini.py -q</automated>
  </verify>
  <acceptance_criteria>
    - services/gemini.py contains `def _build_safety_settings(` and a `_SAFETY_CATEGORIES` tuple
    - `grep -c "safety_settings=_build_safety_settings" services/gemini.py` returns >= 2 (both chat() and generate_image() sites; chat threads via the threshold-selecting expression)
    - chat()'s signature includes `image_bytes` and `image_mime_type` keyword-only params
    - `grep -n "Part.from_bytes" services/gemini.py` shows the image part is appended in chat()
    - A test asserts the vision call uses config.VISION_SAFETY_THRESHOLD and a plain chat()/generate_image() uses config.TEXT_SAFETY_THRESHOLD (two distinct values)
    - A test asserts chat() returns None (no raise) on a mocked empty/blocked response
    - `pytest tests/test_gemini.py -q` exits 0
  </acceptance_criteria>
  <done>All three generate_content configs carry explicit safety_settings; vision uses the real-block
  threshold and /ask + /imagine + non-image chat() stay permissive-but-explicit; chat() can carry an
  image without breaking its None-on-empty / raise-on-transport contract; test_gemini.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user -> Gemini prompt | User-influenced text/image content crosses into `generate_content` (chat/ambient/proactive/imagine/vision) |
| Bot -> Gemini API | Outbound call on the shared 15 RPM budget; a mis-threaded safety config would leave user-influenced input unfiltered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06a | Information Disclosure | safety_settings threading across `chat()` / `generate_image()` / vision config | mitigate | Shared `_build_safety_settings` threaded into every `generate_content` config; a unit test asserts all three configs carry non-None safety_settings and that vision uses the real-block threshold while text stays permissive (VIS-03) |
| T-17-06b | Tampering | `chat()` return/raise contract (silent-skip hinge for VIS-02) | mitigate | Preserve `None`-on-empty/blocked, raise-only-on-transport; a unit test locks None-on-block so the Wave-2 glue can distinguish silent-skip from template-fallback |
| T-17-07 | Denial of Service | shared 15 RPM `_RateLimiter` under a fourth background consumer | mitigate | Vision rides the existing limiter at priority-2 (yields to user priority-1); rarity is enforced by VISION_ROAST_CHANCE (0.12, < ambient/callback) + the Wave-2 per-user cooldown — no new limiter |
| T-17-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Phase installs ZERO new packages (RESEARCH Package Legitimacy Audit: N/A — `google-genai` 2.8.0 + `discord.py` 2.7.1 already pinned). No slopcheck / human checkpoint required; any new-package need is a constraint violation to escalate, not add |
</threat_model>

<verification>
- `pytest tests/test_vision_logic.py -q` green (pure gate boundary + rarity invariant)
- `pytest tests/test_gemini.py -q` green (safety threading, threshold differentiation, None-on-block, 429->GeminiRateLimitError)
- Wave merge gate: `pytest -q` full suite green (no regression from the safety_settings retrofit — the permissive TEXT threshold must not newly block existing /ask / ambient behavior)
</verification>

<success_criteria>
- Explicit `safety_settings` are applied to every user-influenced Gemini call (VIS-03): vision real-block, /ask + /imagine permissive-but-explicit
- `logic/vision.py::should_fire_vision_roast` exists as a keyword-only, discord/random/datetime-free pure gate (VIS-01 / D-04)
- All six Phase 17 config knobs exist with the exact names/values; VISION_ROAST_CHANCE is strictly rarer than both ambient cadences
- `chat()` can carry an image and still returns None on a safety-blocked/empty response (the VIS-02 hinge is preserved for the Wave-2 glue)
</success_criteria>

<output>
Create `.planning/phases/17-vision-multimodal-roasting/17-01-SUMMARY.md` when done.
</output>
