---
phase: 13-semantic-music-memory
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03"]
files_modified:
  - bot.py
autonomous: true
requirements: [TASTE-01, TASTE-03]
must_haves:
  truths:
    - "A daily @tasks.loop taste_distill_batch runs at a UTC hour distinct from the existing 02:30/03:00/04:00 loops"
    - "The loop reads structured song_history only (never the message buffer), bands counts number-free, and writes kind=taste_episode facts"
    - "Users below TASTE_MIN_ACTIVITY_TRACKS in the window are skipped"
    - "The loop is registered at all three sites: start-guard, _cleanup_partial_init stop-list, and docstrings"
    - "One user's distill failure does not abort the batch (per-user try/except)"
  artifacts:
    - path: "bot.py"
      provides: "taste_distill_batch @tasks.loop + before_loop/error pair + 3-site registration"
      contains: "taste_distill_batch"
  key_links:
    - from: "bot.py::taste_distill_batch"
      to: "database.get_active_taste_users / get_user_artist_activity"
      via: "structured song_history reads over the lookback window"
      pattern: "get_active_taste_users"
    - from: "bot.py::taste_distill_batch"
      to: "logic.taste.summarize_taste / has_min_activity"
      via: "number-free pre-bucketing before any Gemini call (D-02)"
      pattern: "summarize_taste"
    - from: "bot.py::taste_distill_batch"
      to: "memory_service.distill_and_remember"
      via: "kind='taste_episode', base_salience from MEMORY_SALIENCE_BASE_WEIGHTS"
      pattern: "kind=\"taste_episode\""
    - from: "_cleanup_partial_init"
      to: "taste_distill_batch"
      via: "stop-list tuple"
      pattern: "taste_distill_batch"
---

<objective>
Wire the foundation together: a new daily module-scope `@tasks.loop` `taste_distill_batch` (sibling of
`memory_distill_batch`) that, for each user active in the rolling `song_history` window, reads the
plan 13-02 aggregate helpers, pre-buckets the raw counts to number-free phrases via plan 13-01's
`logic.taste`, and writes them to the existing pgvector store through
`memory_service.distill_and_remember(kind="taste_episode", ...)` — on its own distinct UTC schedule,
with no thundering-herd on the Neon pool (TASTE-01 / TASTE-03, D-06/D-07/D-08).

Scope guard: the loop writes exactly ONE kind, `taste_episode` (D-09) — `taste_shift` stays deferred,
so this phase adds no second kind, salience tier, or write path.

Purpose: This is the phase's deliverable — Dexter's listening history becomes a retrievable,
number-free taste memory that every downstream v1.3 consumer reads. The loop is glue; correctness
lives in the tested contracts it composes (config, logic/taste, DB helpers, the kind-aware
remember()), and in getting the three registration sites right so a botched boot never leaves it
firing against a torn-down pool.
Output: one new `@tasks.loop` + `.before_loop`/`.error` pair in `bot.py`; registration at the
start-guard, the `_cleanup_partial_init` stop-list, and the boot docstrings.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-semantic-music-memory/13-CONTEXT.md
@.planning/phases/13-semantic-music-memory/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the taste_distill_batch @tasks.loop (read → band → write) + before_loop/error pair</name>
  <files>bot.py</files>
  <read_first>
    - bot.py lines 808-900 (memory_distill_batch) — the EXACT loop template to clone: `@tasks.loop(time=datetime.time(hour=..., minute=0))`, the getattr(bot, "memory_service", None) no-op guard, the per-user try/except swallow, and the CR-02 comment (key writes on the real snowflake user_id, never a display name).
    - bot.py lines 903-932 (memory_sweep + before_loop/error) — the identical `.before_loop`→wait_until_ready and `.error`→_post_loop_error shape.
    - bot.py lines 637-664 (_post_loop_error) — confirm signature for the .error handler.
    - database.get_active_taste_users / get_user_artist_activity (plan 13-02) — return columns.
    - logic.taste.has_min_activity / summarize_taste (plan 13-01) — the pure gate + number-free phrase builder.
    - config: TASTE_DISTILL_BATCH_HOUR, TASTE_LOOKBACK_DAYS, TASTE_BASELINE_DAYS, TASTE_MIN_ACTIVITY_TRACKS, TASTE_OBSESSION_MIN_PLAYS/NEW_ARRIVAL_MIN_PLAYS/STEADY_MIN_BASELINE, MEMORY_SALIENCE_BASE_WEIGHTS["taste_episode"].
    - CLAUDE.md Implementation Gotchas — restore_queues per-guild `continue` discipline (one failure must not abort the batch).
  </read_first>
  <action>
    Add `taste_distill_batch` at module scope in bot.py near memory_distill_batch, declared
    `@tasks.loop(time=datetime.time(hour=config.TASTE_DISTILL_BATCH_HOUR, minute=0))`. Body: no-op guard
    `memory_service = getattr(bot, "memory_service", None)` and `pool = getattr(bot, "pool", None)` —
    return if either is None (partial init / GEMINI_API_KEY unset). Compute
    `since = datetime.now(timezone.utc) - timedelta(days=config.TASTE_LOOKBACK_DAYS)` and
    `baseline_since = datetime.now(timezone.utc) - timedelta(days=config.TASTE_BASELINE_DAYS)`. Fetch
    candidates via `await database.get_active_taste_users(pool, since=since)`; if empty, log and return.
    For each candidate row: read guild_id, user_id, tracks_in_window (all native song_history columns —
    user_id is already the real snowflake, satisfying CR-02; never derive an owner key from a display
    name). Apply the D-08 gate: `if not logic_taste.has_min_activity(tracks_in_window,
    min_tracks=config.TASTE_MIN_ACTIVITY_TRACKS): continue`. Fetch per-artist activity via
    `get_user_artist_activity(pool, guild_id=..., user_id=..., since=since, baseline_since=baseline_since)`,
    then `phrases = logic_taste.summarize_taste(rows, obsession_min=config.TASTE_OBSESSION_MIN_PLAYS,
    new_arrival_min=config.TASTE_NEW_ARRIVAL_MIN_PLAYS, steady_min_baseline=config.TASTE_STEADY_MIN_BASELINE)`
    — the number-free pre-bucketing (D-02); if `not phrases: continue`. Build a number-free
    `raw_text` string from the phrases (join them; NEVER interpolate a raw count — phrases are already
    digit-free). Write exactly ONE kind, `taste_episode` (D-09 — no `taste_shift`). Wrap the write in a
    per-user try/except (swallow + log.debug on failure, mirroring memory_distill_batch) calling
    `await memory_service.distill_and_remember(user_id=user_id, guild_id=guild_id, raw_text=raw_text,
    kind="taste_episode", base_salience=config.MEMORY_SALIENCE_BASE_WEIGHTS["taste_episode"])` — note
    guild_id is carried through (unlike daily_batch's None) since taste is guild-scoped listening
    (13-PATTERNS.md). Add the paired `@taste_distill_batch.before_loop` (await bot.wait_until_ready())
    and `@taste_distill_batch.error` (log.error + `await _post_loop_error("taste_distill_batch",
    error)`), cloning memory_distill_batch's handlers with the renamed strings. Import logic.taste (e.g.
    `from logic import taste as logic_taste`) and confirm `database`, `datetime`, `timedelta`,
    `timezone` are already imported in bot.py.
  </action>
  <verify>
    <automated>python -m py_compile bot.py && grep -nE "def taste_distill_batch|kind=\"taste_episode\"|summarize_taste|get_active_taste_users" bot.py</automated>
  </verify>
  <acceptance_criteria>
    - `bot.py` defines `@tasks.loop(...TASTE_DISTILL_BATCH_HOUR...)` `async def taste_distill_batch()` with a `getattr(bot, "memory_service", None)` no-op guard.
    - The loop calls `logic.taste.has_min_activity` (D-08 gate) before the second query and `logic.taste.summarize_taste` (D-02 pre-bucketing) before any Gemini call.
    - The write is `memory_service.distill_and_remember(..., kind="taste_episode", base_salience=config.MEMORY_SALIENCE_BASE_WEIGHTS["taste_episode"])` with `guild_id` carried through (not None), inside a per-user try/except; only this one kind is written (D-09).
    - The loop body contains NO digit-interpolation of a count into raw_text (raw_text is built only from summarize_taste phrases).
    - Paired `.before_loop` (wait_until_ready) and `.error` (_post_loop_error) handlers exist. `python -m py_compile bot.py` exits 0.
  </acceptance_criteria>
  <done>taste_distill_batch exists as a memory_distill_batch-shaped daily loop that reads structured history, bands it number-free, and writes taste_episode facts; bot.py compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Register taste_distill_batch at all three boot sites</name>
  <files>bot.py</files>
  <read_first>
    - bot.py lines 447-460 (the start-guard block: `if not memory_distill_batch.is_running(): memory_distill_batch.start()` and siblings) — add the taste loop's start-guard here.
    - bot.py lines 277-286 (_cleanup_partial_init stop-list tuple `for _loop in (idle_check, cache_cleanup, ytdlp_update, status_rotation, memory_distill_batch, memory_sweep):`) — the MOST consequential site; the loop MUST be added here or a botched boot leaves it firing against a torn-down pool (WR-04).
    - bot.py lines 265-266 and 278-279 (the two docstring/comment "Loops stopped:" lists) — update for future maintainers (documentation hygiene, matching existing care).
  </read_first>
  <action>
    Add three registrations for taste_distill_batch, cloning the memory_distill_batch treatment
    exactly. (1) In the start-guard block (~lines 447-460), add `if not
    taste_distill_batch.is_running(): taste_distill_batch.start()` alongside the other loop starts,
    with a comment tagging it Phase 13 / TASTE-03. (2) In `_cleanup_partial_init`'s stop-list tuple
    (~lines 280-281), append `taste_distill_batch` to the tuple so it is cancelled on a partial-init
    teardown — this is the load-bearing registration (WR-04). (3) Update both "Loops stopped:"
    docstring/comment lists (~lines 265-266 and 278-279) to include taste_distill_batch (Phase 13).
    Do not reorder or alter the existing loop registrations.
  </action>
  <verify>
    <automated>python -m py_compile bot.py && test $(grep -vE '^\s*#' bot.py | grep -c 'taste_distill_batch') -ge 4 && grep -n 'idle_check, cache_cleanup, ytdlp_update, status_rotation' bot.py</automated>
  </verify>
  <acceptance_criteria>
    - `taste_distill_batch` appears in the `_cleanup_partial_init` stop-list tuple alongside `memory_distill_batch, memory_sweep` (source assertion — the WR-04 site).
    - `taste_distill_batch.start()` appears in the boot start-guard block, guarded by `if not taste_distill_batch.is_running()`.
    - Both "Loops stopped:" docstring/comment lists mention taste_distill_batch.
    - Non-comment references to `taste_distill_batch` across bot.py number at least 4 (loop def, start-guard, stop-list, before_loop/error); `python -m py_compile bot.py` exits 0.
  </acceptance_criteria>
  <done>The loop is registered at the start-guard, the _cleanup_partial_init stop-list, and both docstrings; the grep gate confirms ≥4 non-comment references and bot.py compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| song_history (user-queued titles/artists) → raw_text → Gemini distiller → user_memories | User-controlled content flows into an embedded, recallable memory fact; numbers and sensitive content must not cross. |
| new daily loop → Neon pool | An extra daily batch adds load; a bad schedule or unbounded fan-out could contend with the other loops. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Denial of service | taste_distill_batch schedule | mitigate | Runs once daily at TASTE_DISTILL_BATCH_HOUR (05:00 UTC), distinct from cache_cleanup(hourly)/memory_sweep(02:30)/memory_distill_batch(03:00)/ytdlp_update(04:00); D-08 min-activity gate skips light users so fan-out is bounded (no thundering-herd, D-06). |
| T-13-06 | Information disclosure | raw_text builder | mitigate | raw_text is assembled only from logic.taste.summarize_taste's digit-free phrases (D-02 pre-bucketing); distill()'s contains_number()/is_sensitive() backstops remain the belt-and-suspenders second gate. No SQL-known count reaches the prompt (Critical Rule 12). |
| T-13-07 | Spoofing | per-user owner key | mitigate | Loop keys writes on the native song_history.user_id snowflake, never a display name (CR-02); get_user_artist_activity is guild+user scoped so no cross-user taste bleeds into a fact. |
| T-13-08 | Repudiation / silent failure | fire-and-forget loop | mitigate | Per-user try/except swallow keeps one failure from aborting the batch; the `.error` handler routes loop-level failures to _post_loop_error (Phase 9 make_task/error-surfacing convention). |
| T-13-SC | Tampering (supply chain) | package installs | accept | N/A — zero new package installs this phase. |
</threat_model>

<verification>
- `python -m py_compile bot.py` exits 0.
- `taste_distill_batch` present in the _cleanup_partial_init stop-list tuple and the start-guard block (grep source assertions).
- Non-comment `taste_distill_batch` references ≥ 4.
- `python -m pytest -q` full suite green (no import/boot regression introduced by the new loop).
</verification>

<success_criteria>
- A daily taste_distill_batch loop reads structured history, pre-buckets number-free, and writes kind=taste_episode facts on a distinct UTC schedule.
- The loop is registered at all three boot sites (start-guard, stop-list, docstrings).
- No raw count reaches the distiller; users below the min-activity floor are skipped.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces
- `bot.py::taste_distill_batch` (`@tasks.loop`) + `before_taste_distill_batch` (`.before_loop`) + `on_taste_distill_batch_error` (`.error`)
- taste_distill_batch registered at: the boot start-guard, the `_cleanup_partial_init` stop-list tuple, and both "Loops stopped:" docstrings
</artifacts_produced>

<output>
Create `.planning/phases/13-semantic-music-memory/13-04-SUMMARY.md` when done.
</output>
