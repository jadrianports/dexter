---
phase: 13-semantic-music-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - logic/taste.py
  - tests/test_taste_logic.py
autonomous: true
requirements: [TASTE-01, TASTE-02]
must_haves:
  truths:
    - "taste_episode carries a distinct salience base weight below the sweep floor (0.5)"
    - "A distinct TASTE_DECAY_DAYS horizon exists, shorter than MEMORY_DECAY_DAYS (90)"
    - "Raw listening counts are banded to number-free qualitative phrases before any prompt sees them"
    - "Users below the min-activity threshold produce no taste facts"
  artifacts:
    - path: "config.py"
      provides: "TASTE_* knobs, taste_episode salience weight, MEMORY_DECAY_DAYS_BY_KIND mapping"
      contains: "TASTE_DECAY_DAYS"
    - path: "logic/taste.py"
      provides: "pure banding, classification, min-activity gate, decay-horizon resolver"
      exports: ["has_min_activity", "classify_artist", "summarize_taste", "resolve_decay_days"]
    - path: "tests/test_taste_logic.py"
      provides: "mock-free unit tests locking the pure logic + number-free firewall"
  key_links:
    - from: "logic/taste.py::summarize_taste"
      to: "number-free output"
      via: "no digit ever appears in a produced phrase"
      pattern: "summarize_taste"
    - from: "config.MEMORY_SALIENCE_BASE_WEIGHTS"
      to: "taste_episode"
      via: "new dict key below MEMORY_DECAY_SALIENCE_FLOOR"
      pattern: "taste_episode"
---

<objective>
Establish the two contracts every later Phase 13 plan consumes: (1) the taste tuning config
(salience weight, decay tier, cadence, lookback, thresholds — TASTE-02, D-03/D-04/D-06/D-07/D-08)
and (2) the pure `logic/taste.py` decision seam that turns raw play/skip counts into number-free
qualitative bands (TASTE-01, D-01/D-02), applies the min-activity gate (D-08), and resolves the
per-kind decay horizon (D-03) consumed by the memory-service self-refresh in plan 13-03.

Purpose: The accuracy firewall (Critical Rule 12) is a hard constraint — pre-bucketing raw counts
to number-free phrases in a pure, unit-tested module keeps numbers structurally out of the distiller
pipe, rather than trusting a single downstream gate (D-02).
Output: New `# --- Phase 13: Semantic Music Memory ---` config block; new `logic/taste.py`; new
mock-free `tests/test_taste_logic.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-semantic-music-memory/13-CONTEXT.md
@.planning/phases/13-semantic-music-memory/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 13 taste config knobs, salience weight, and decay-by-kind mapping</name>
  <files>config.py</files>
  <read_first>
    - config.py lines 160-187 (the `# --- Phase 11: RAG Long-Term Memory ---` block: MEMORY_DECAY_DAYS,
      MEMORY_DECAY_SALIENCE_FLOOR, MEMORY_DISTILL_BATCH_HOUR, MEMORY_SALIENCE_BASE_WEIGHTS) — the exact
      shape to mirror.
    - 13-PATTERNS.md §"config.py (new taste knobs)" — the confirmed knob list and the D-04 below-floor constraint.
  </read_first>
  <action>
    Add a new `# --- Phase 13: Semantic Music Memory ---` block placed directly after the Phase 11
    block (after line ~187). Add module-level constants with directional-prior values (all
    Claude's-discretion per CONTEXT, tunable later): TASTE_DECAY_DAYS = 30 (D-03; shorter half-life
    than MEMORY_DECAY_DAYS=90, Pitfall 5), TASTE_DISTILL_BATCH_HOUR = 5 (D-06; distinct UTC slot clear
    of the 02:30/03:00/04:00 loops), TASTE_LOOKBACK_DAYS = 7 (D-07 rolling recent window),
    TASTE_BASELINE_DAYS = 90 (bounded "before-window" span for steady/new-arrival detection —
    keeps the aggregate query index-friendly), TASTE_MIN_ACTIVITY_TRACKS = 5 (D-08 gate),
    TASTE_OBSESSION_MIN_PLAYS = 5, TASTE_NEW_ARRIVAL_MIN_PLAYS = 3, TASTE_STEADY_MIN_BASELINE = 5,
    TASTE_BAND_HEAVY_PLAYS = 5, TASTE_BAND_FEW_PLAYS = 2 (band/classification thresholds passed into
    the pure logic in Task 2). Extend the EXISTING MEMORY_SALIENCE_BASE_WEIGHTS dict (do NOT create a
    second dict) with a new key `"taste_episode": 0.4` — D-04 requires this to be strictly less than
    MEMORY_DECAY_SALIENCE_FLOOR (0.5) so taste rows are sweep-eligible; add an inline comment stating
    that constraint. Add a NEW module-level mapping MEMORY_DECAY_DAYS_BY_KIND: dict[str, int] =
    {"taste_episode": TASTE_DECAY_DAYS} — this is the per-kind decay-horizon override consumed by
    logic.taste.resolve_decay_days (Task 2) and the memory-service self-refresh (plan 13-03); document
    that kinds absent from this map fall back to MEMORY_DECAY_DAYS, preserving Phase 11 semantics.
  </action>
  <verify>
    <automated>python -c "import config; assert config.TASTE_DECAY_DAYS < config.MEMORY_DECAY_DAYS; assert config.MEMORY_SALIENCE_BASE_WEIGHTS['taste_episode'] < config.MEMORY_DECAY_SALIENCE_FLOOR; assert config.MEMORY_DECAY_DAYS_BY_KIND['taste_episode'] == config.TASTE_DECAY_DAYS; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `config.py` defines TASTE_DECAY_DAYS, TASTE_DISTILL_BATCH_HOUR, TASTE_LOOKBACK_DAYS,
      TASTE_BASELINE_DAYS, TASTE_MIN_ACTIVITY_TRACKS, and the classification threshold constants.
    - `config.MEMORY_SALIENCE_BASE_WEIGHTS['taste_episode']` exists and is `< config.MEMORY_DECAY_SALIENCE_FLOOR` (D-04).
    - `config.MEMORY_DECAY_DAYS_BY_KIND['taste_episode'] == config.TASTE_DECAY_DAYS`, and `.get('milestone', config.MEMORY_DECAY_DAYS)` returns `config.MEMORY_DECAY_DAYS` (Phase 11 unchanged).
    - The verify one-liner exits 0.
  </acceptance_criteria>
  <done>All taste knobs, the below-floor taste_episode salience weight, and the decay-by-kind mapping exist; the assertion one-liner passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create logic/taste.py pure banding + gate + decay-resolver, locked by mock-free tests</name>
  <files>logic/taste.py, tests/test_taste_logic.py</files>
  <read_first>
    - logic/skip_stats.py (the pure-logic seam convention: no Discord/asyncio/DB/random/datetime.now;
      all nondeterministic values passed in as primitives; the module docstring format).
    - tests/test_skip_stats.py (the mock-free unit-test convention: plain-primitive inputs, branch coverage, a never-raises test).
    - 13-PATTERNS.md §"Accuracy firewall / number-free distillation" (bands: "played heavily" / "a few times" / "dropped off" / "new this week").
    - 13-CONTEXT.md D-01 (obsession / new-arrival / steady patterns), D-02 (pre-bucket in the caller), D-08 (min-activity gate).
  </read_first>
  <behavior>
    - has_min_activity(tracks_in_window, min_tracks=5) → False when tracks_in_window < min_tracks, True at/above (D-08 gate; mirrors skip_stats floor).
    - classify_artist(plays_in_window, plays_before_window, skips_in_window, *, thresholds...) → a small enum/str label:
      OBSESSION when plays_in_window >= obsession_min; NEW_ARRIVAL when plays_before_window == 0 and plays_in_window >= new_arrival_min;
      STEADY when plays_before_window >= steady_min_baseline and plays_in_window > 0; DROPPED_OFF when plays_before_window >= steady_min_baseline and plays_in_window == 0; else NONE.
    - summarize_taste(artist_rows, *, thresholds...) → list[str] of number-free descriptor phrases, one per notable artist (e.g. "played the killers heavily this week", "phonk is new for them this week", "keeps coming back to mac demarco"). NONE-classified artists are omitted.
    - FIREWALL: no string returned by summarize_taste contains any digit character (assert re.search(r"\d", phrase) is None for every phrase) — this is the number-free guarantee (D-02 / Critical Rule 12).
    - Empty artist_rows → empty list. Function never raises for any list of dict/records with the expected integer count fields.
  </behavior>
  <action>
    Create `logic/taste.py` following the `logic/skip_stats.py` header/discipline exactly (pure,
    deterministic, no imports beyond `from __future__ import annotations`, `re`, `enum`/dataclass as
    needed). Implement: has_min_activity(tracks_in_window, *, min_tracks) -> bool (D-08); a TastePattern
    enum (OBSESSION, NEW_ARRIVAL, STEADY, DROPPED_OFF, NONE); classify_artist(plays_in_window,
    plays_before_window, skips_in_window, *, obsession_min, new_arrival_min, steady_min_baseline) ->
    TastePattern with the precedence in <behavior>; summarize_taste(artist_rows, *, obsession_min,
    new_arrival_min, steady_min_baseline) -> list[str] that reads integer count fields
    (plays_in_window, plays_before_window, skips_in_window) off each row (dict-or-Record access via
    row["..."]), classifies, and emits a fixed number-free template phrase per non-NONE pattern —
    phrases must never interpolate a count; and resolve_decay_days(kind, *, default_days,
    kind_overrides) -> int returning kind_overrides.get(kind, default_days) (D-03; consumed by
    services/memory.py in plan 13-03). Create `tests/test_taste_logic.py` with mock-free tests covering
    every classify_artist branch, the has_min_activity floor (below / at / above), a resolve_decay_days
    test asserting "taste_episode"→30 and "milestone"→90 given the real config values, a digit-free
    firewall test that iterates summarize_taste output and asserts `re.search(r"\d", phrase) is None`,
    an empty-input test, and a never-raises test. Do NOT put any threshold literal inside logic/taste.py
    defaults that duplicates config — pass config.TASTE_* values in from the caller (tests may pass
    literals directly, matching test_skip_stats.py).
  </action>
  <verify>
    <automated>python -m pytest tests/test_taste_logic.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_taste_logic.py` exits 0 with no skips and no live-DB/mock dependency.
    - The digit-free firewall test proves every summarize_taste phrase matches `re.search(r"\d", phrase) is None` for a fixture spanning all patterns.
    - resolve_decay_days test asserts `resolve_decay_days("taste_episode", default_days=90, kind_overrides={"taste_episode":30}) == 30` and `resolve_decay_days("milestone", ...) == 90`.
    - `logic/taste.py` imports nothing from discord/asyncio/asyncpg/random and calls no `datetime.now()` (grep: `grep -nE "import (discord|asyncio|asyncpg|random)|datetime\.now" logic/taste.py` returns no matches).
  </acceptance_criteria>
  <done>logic/taste.py exists as a pure seam; test_taste_logic.py is green and locks classification, the min-activity gate, the decay resolver, and the number-free firewall.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-queued song titles/artists → taste phrases | Users control what they queue; artist strings flow (in a later plan) from song_history into raw_text. Numbers must never cross into the phrase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information disclosure | logic/taste.py::summarize_taste | mitigate | Number-free template phrases only; unit test asserts `re.search(r"\d", phrase) is None` for all output (D-02 / Critical Rule 12). Downstream is_sensitive/contains_number backstop remains belt-and-suspenders. |
| T-13-02 | Tampering | config.MEMORY_DECAY_DAYS_BY_KIND | mitigate | Map contains only taste_episode; `.get(kind, MEMORY_DECAY_DAYS)` fallback keeps Phase 11 kinds byte-identical (verified in Task 1 assertion). |
| T-13-SC | Tampering (supply chain) | package installs | accept | N/A — zero new package installs this phase (pure config + pure-Python logic). |
</threat_model>

<verification>
- `python -c "import config"` succeeds and the Task 1 assertion one-liner exits 0.
- `python -m pytest tests/test_taste_logic.py -q` is green.
- `python -m pytest -q` (full suite) remains green — no Phase 11 regression from the config additions.
</verification>

<success_criteria>
- TASTE_* knobs + taste_episode salience (below floor) + MEMORY_DECAY_DAYS_BY_KIND present in config.py.
- logic/taste.py provides has_min_activity, classify_artist, summarize_taste, resolve_decay_days — all pure.
- tests/test_taste_logic.py green, including the digit-free firewall assertion.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (consolidated — full phase list)

This plan (13-01):
- `config.TASTE_DECAY_DAYS`, `config.TASTE_DISTILL_BATCH_HOUR`, `config.TASTE_LOOKBACK_DAYS`,
  `config.TASTE_BASELINE_DAYS`, `config.TASTE_MIN_ACTIVITY_TRACKS`, `config.TASTE_OBSESSION_MIN_PLAYS`,
  `config.TASTE_NEW_ARRIVAL_MIN_PLAYS`, `config.TASTE_STEADY_MIN_BASELINE`, `config.TASTE_BAND_HEAVY_PLAYS`,
  `config.TASTE_BAND_FEW_PLAYS`
- `config.MEMORY_SALIENCE_BASE_WEIGHTS["taste_episode"]` (0.4)
- `config.MEMORY_DECAY_DAYS_BY_KIND` (new mapping)
- `logic/taste.py`: `has_min_activity`, `TastePattern` (enum), `classify_artist`, `summarize_taste`, `resolve_decay_days`
- `tests/test_taste_logic.py`

Downstream plans produce:
- 13-02: `database.get_active_taste_users`, `database.get_user_artist_activity`, `database.refresh_memory_expiry`, `tests/test_database_phase13.py`
- 13-03: kind-aware decay horizon + self-refresh-on-dedup wiring in `services/memory.py`, `tests/test_memory_taste.py`
- 13-04: `bot.py::taste_distill_batch` (`@tasks.loop`) + its `.before_loop`/`.error` pair + 3-site registration
</artifacts_produced>

<output>
Create `.planning/phases/13-semantic-music-memory/13-01-SUMMARY.md` when done.
</output>
