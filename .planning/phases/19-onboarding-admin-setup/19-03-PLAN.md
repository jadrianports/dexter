---
phase: 19-onboarding-admin-setup
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:
  - bot.py
  - personality/roasts.py
  - cogs/music.py
autonomous: true
requirements: [ONBOARD-01, ONBOARD-05]
must_haves:
  truths:
    - "Joining a guild inserts a configured=false row, attempts an in-persona welcome that never crashes the join, and notifies the owner"
    - "A guild invited while Dexter was offline is welcomed exactly once on the next boot, keyed on the DB insert result — never on a cache miss"
    - "The startup message fires in the home guild only; idle-loneliness stays per-guild"
    - "Repeat-song and milestone roasts route through the config seam, silent in an unconfigured guild"
    - "Owner receives a join and a remove notice in ERROR_LOG_CHANNEL_ID; on_guild_remove touches no DB rows"
  artifacts:
    - path: "bot.py"
      provides: "on_guild_join, on_guild_remove, boot backfill loop, home-guild-only startup"
      contains: "async def on_guild_join"
    - path: "personality/roasts.py"
      provides: "welcome copy (in-persona line pool + plain /setup channel hint)"
      contains: "setup channel"
  key_links:
    - from: "bot.py::_initialize_once boot backfill"
      to: "database.insert_guild_config_if_absent + logic.should_welcome_guild"
      via: "welcome decision keyed on the insert Record, never bot.guild_config.get()"
      pattern: "should_welcome_guild\\(inserted_row="
    - from: "cogs/music.py::_post_music_roast"
      to: "resolve_ambient_channel(surface=AmbientSurface.ROAST)"
      via: "config-seam resolver replacing _get_text_channel"
      pattern: "resolve_ambient_channel\\([^)]*surface=AmbientSurface\\.ROAST"
---

<objective>
Wire the guild-lifecycle glue on top of the 19-01 DB helpers and the 19-02 seam: `on_guild_join`
(insert row -> in-persona welcome via `resolve_announce_channel`, wrapped so a send failure never
crashes the join -> owner notice, ONBOARD-01/05), `on_guild_remove` (owner notice + cache evict, no DB
writes, D-12), the boot backfill that welcomes guilds invited while Dexter was offline exactly once
(D-14/D-15), the home-guild-only startup narrowing (D-23), and the live CONFIG-04 music-roast hole
fix (Pitfall 1). Also completes the deferred `resolve_ambient_channel` call-site migration for bot.py
and music.py that 19-02's required-`surface` change left pending.

Purpose: this is the ONBOARD-01/05 user journey — a fresh server gets greeted (even under the
on-demand hosting model where the gateway join event never fired) and the owner gets a
kill-switch-ready notice for every join/leave. All work here is Discord/process glue: untested-by-design
per D-26, verified by structural review + a green full suite + clean boot.

Output: two lifecycle handlers, a boot backfill loop, a home-guild-only startup message, welcome copy,
the `cogs.admin` cog registration, and the `_post_music_roast` seam fix.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-onboarding-admin-setup/19-CONTEXT.md
@.planning/phases/19-onboarding-admin-setup/19-RESEARCH.md
@.planning/phases/19-onboarding-admin-setup/19-PATTERNS.md
@bot.py
@cogs/music.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: on_guild_join / on_guild_remove handlers + welcome copy + owner notice (D-10/12/13/16)</name>
  <files>bot.py, personality/roasts.py</files>
  <read_first>
    - bot.py lines 270-308 (the `on_ready`/`_initialize_once` guard) and 402-441 (log_to_discord wiring,
      GuildConfigService construction, home-guild seed) — so the handlers reuse `bot.pool`,
      `bot.guild_config`, `bot.log_to_discord` correctly.
    - bot.py lines 514-536 — `_post_startup_messages`'s per-guild try/except loop (the notify-and-continue
      shape to mirror).
    - personality/roasts.py — `STARTUP_MESSAGES` and `pick_random` (the copy-pool + picker idiom the
      welcome copy mirrors; lowercase, one emoji max, under 500 chars — CLAUDE.md Critical Rules 6/7/8).
    - .planning/phases/19-onboarding-admin-setup/19-RESEARCH.md §"Code Examples" (the on_guild_join
      skeleton + owner-embed field list) and §"Security / Threat Model Inputs" (T-19-02 embed injection).
    - .planning/phases/19-onboarding-admin-setup/19-PATTERNS.md §"bot.py" (on_guild_join/on_guild_remove).
  </read_first>
  <action>
    In personality/roasts.py add a `WELCOME_MESSAGES` list of in-persona one-liners (savage, lowercase,
    one emoji max) and a single plain `WELCOME_SETUP_HINT` string that names the exact next command,
    e.g. "run `/setup channel` to pick where i talk. i'll stay quiet until you do." — the
    sarcasm-dialed-back functional line per D-11 / Critical Rule 6.
    In bot.py add a reusable async helper `_post_guild_welcome(guild) -> bool` that: resolves
    `bot.guild_config.resolve_announce_channel(guild)`; on `None` logs a WARNING and returns False; else
    sends `pick_random(WELCOME_MESSAGES) + "\n" + WELCOME_SETUP_HINT` with
    `allowed_mentions=discord.AllowedMentions.none()` inside a `try/except discord.HTTPException` that logs
    a WARNING and returns False; returns True on a successful send (D-13). Add a reusable
    `_build_guild_notice_embed(guild, *, joined, welcome_posted)` that constructs the D-16 embed: title
    "joined: {name}" / "removed: {name}", and fields for member count, guild owner tag + id, guild
    created-at (`discord.utils.format_dt(..., "R")`), and Dexter's total guild count (`len(bot.guilds)`);
    render `guild.name` and the owner tag as PLAIN field values (never wrapped in markdown, never used as
    a hyperlink label) and put the numeric `guild.id`/`owner_id` inside backtick inline-code spans — the
    T-19-02 embed-injection mitigation; include a "welcome posted: yes/no" field only when `joined` and
    `welcome_posted is not None`.
    Add `@bot.event async def on_guild_join(guild)`: defensively return if `bot` lacks `pool`/`guild_config`
    (a join before init is picked up by the boot backfill); call
    `database.insert_guild_config_if_absent(bot.pool, guild_id=str(guild.id))`; if
    `should_welcome_guild(inserted_row=row)` (imported from logic.guild_config), call
    `bot.guild_config._refresh_cache_entry(row)` and set `welcome_posted = await _post_guild_welcome(guild)`
    (else `welcome_posted = False` and skip the welcome — the row already existed); always send
    `bot.log_to_discord(_build_guild_notice_embed(guild, joined=True, welcome_posted=welcome_posted))`.
    Add `@bot.event async def on_guild_remove(guild)` (D-12): evict via
    `bot.guild_config._cache.pop(str(guild.id), None)` and send
    `bot.log_to_discord(_build_guild_notice_embed(guild, joined=False, welcome_posted=None))` — touch NO
    DB rows (the MEM-04 purge is Phase 21; and Phase 21 must preserve a blocked guild's row / move the
    blacklist to its own table — carry this constraint forward in a comment, do not solve it here).
    Import `should_welcome_guild` and `AmbientSurface` from logic.guild_config, and
    `WELCOME_MESSAGES`/`WELCOME_SETUP_HINT`/`pick_random` from personality.roasts.
  </action>
  <acceptance_criteria>
    - `bot.py` defines `async def on_guild_join` and `async def on_guild_remove`, both decorated `@bot.event`.
    - `on_guild_join`'s welcome decision uses `should_welcome_guild(inserted_row=` and NOT `bot.guild_config.get(`.
    - `_build_guild_notice_embed` renders `guild.id`/`owner_id` inside backtick inline-code spans and never builds a markdown link from `guild.name`.
    - `on_guild_remove` body contains `_cache.pop(`, contains no `DELETE` or `database.` write call, and carries a comment naming the Phase 21 blocked-guild-row constraint (D-12).
    - `personality/roasts.py` exposes `WELCOME_MESSAGES` and `WELCOME_SETUP_HINT`, and the hint contains `/setup channel`.
    - `ruff check bot.py personality/roasts.py` exits 0 and the files parse.
  </acceptance_criteria>
  <verify>
    <automated>ruff check bot.py personality/roasts.py && python -c "import ast; ast.parse(open('bot.py').read()); ast.parse(open('personality/roasts.py').read())" && python -c "s=open('bot.py').read(); assert 'async def on_guild_join' in s and 'async def on_guild_remove' in s and 'should_welcome_guild(inserted_row=' in s and '_cache.pop(' in s" && python -c "import personality.roasts as r; assert r.WELCOME_MESSAGES and '/setup channel' in r.WELCOME_SETUP_HINT"</automated>
  </verify>
  <done>on_guild_join inserts + welcomes (keyed on the insert result) + notifies; on_guild_remove notifies + evicts cache with zero DB writes; welcome copy lands the persona + a plain /setup channel instruction; embeds render attacker-influenceable strings safely.</done>
</task>

<task type="auto">
  <name>Task 2: Boot backfill after seed_home_guild + home-guild-only startup + idle PRESENCE + cog registration</name>
  <files>bot.py</files>
  <read_first>
    - bot.py lines 407-455 — the GuildConfigService construction, `load_all()`, the home-guild seed
      try/except closing at line 440, the `# Phase 4: Queue persistence service` comment at 442, and the
      cog-load tuple at 449.
    - bot.py lines 514-536 — `_post_startup_messages` (the `for guild in bot.guilds:` loop that D-23
      narrows to the home guild) and line 529's `resolve_ambient_channel(guild)` call.
    - bot.py lines 737-756 — the idle-loneliness `resolve_ambient_channel(guild)` call at line 747
      (stays per-guild, only gains `surface=PRESENCE`).
    - .planning/phases/19-onboarding-admin-setup/19-RESEARCH.md §"Additional Research Findings" #6
      (exact backfill insertion point, the fail-closed-vs-cache Pitfall 3) and Pitfall 2 (startup narrowing
      is a behavior change, not just a kwarg add).
    - .planning/phases/19-onboarding-admin-setup/19-PATTERNS.md §"bot.py" (backfill + startup snippets).
  </read_first>
  <action>
    Insert a boot backfill loop in `_initialize_once` immediately AFTER the home-guild seed try/except
    closes (after line ~440) and BEFORE the `# Phase 4: Queue persistence service` block at ~442 — this
    ordering is a hard constraint (D-14 constraint 1: running before `seed_home_guild` would backfill and
    welcome the home guild as configured=false, breaking CONFIG-05). For each `guild in bot.guilds`: in a
    per-guild `try/except` (log a WARNING + `continue` on failure), call
    `database.insert_guild_config_if_absent(bot.pool, guild_id=str(guild.id))`; if
    `should_welcome_guild(inserted_row=row)` is False, `continue` (already had a row — not a new join, D-14
    constraint 2: the decision reads the INSERT's own return value, NEVER `bot.guild_config.get()`, so a
    fail-closed empty cache cannot welcome-spam); else `bot.guild_config._refresh_cache_entry(row)`, await
    `_post_guild_welcome(guild)`, and log one INFO line (D-15: no cap, sequential, one line each). After
    the loop, send ONE owner summary embed via `bot.log_to_discord` naming the count of guilds welcomed and
    a compact list of their `name` + backtick-wrapped `id` (skip the summary entirely when the count is 0).
    Narrow `_post_startup_messages` (D-23): replace `for guild in bot.guilds:` with a single home-guild
    lookup — `home_id = bot.guild_config.home_guild_id`; if `home_id` is falsy, return (fresh clone: nobody
    gets a startup message); else `home_guild = bot.get_guild(int(home_id))`, and if present resolve
    `bot.guild_config.resolve_ambient_channel(home_guild, surface=AmbientSurface.PRESENCE)` inside the same
    try/except and send one `STARTUP_MESSAGES` line. Add `surface=AmbientSurface.PRESENCE` to the
    idle-loneliness `resolve_ambient_channel(guild)` call at line 747 (no loop change — idle stays
    per-guild). Add `"cogs.admin"` to the cog-load tuple at line 449 so the /setup cog (19-04) loads.
  </action>
  <acceptance_criteria>
    - The backfill loop appears in `_initialize_once` after the `seed_home_guild` call and before the queue-persistence wiring; it calls `insert_guild_config_if_absent` and gates the welcome on `should_welcome_guild(inserted_row=`.
    - No `bot.guild_config.get(` appears anywhere in the backfill welcome decision path.
    - `_post_startup_messages` no longer iterates `bot.guilds`; it resolves a single `home_guild` via `bot.guild_config.home_guild_id` and passes `surface=AmbientSurface.PRESENCE`.
    - The idle-loneliness `resolve_ambient_channel` call (line ~747) passes `surface=AmbientSurface.PRESENCE`.
    - `"cogs.admin"` is present in the cog-load tuple.
    - `ruff check bot.py` exits 0; `pytest -q` still green.
  </acceptance_criteria>
  <verify>
    <automated>ruff check bot.py && python -c "s=open('bot.py').read(); assert 'insert_guild_config_if_absent' in s and 'should_welcome_guild(inserted_row=' in s and 'surface=AmbientSurface.PRESENCE' in s and '\"cogs.admin\"' in s and 'for guild in bot.guilds' not in s.split('_post_startup_messages')[1].split('async def')[0]" && pytest -q</automated>
  </verify>
  <done>Backfill welcomes only genuinely-new guilds (keyed on the insert result) after the home seed; startup is home-guild-only; idle-loneliness declares PRESENCE; the admin cog is registered; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Close the live CONFIG-04 music-roast hole in _post_music_roast (Pitfall 1)</name>
  <files>cogs/music.py</files>
  <read_first>
    - cogs/music.py lines 1168-1176 — `_post_music_roast` (the ONLY site to change) and its current
      `self._get_text_channel(guild)` resolve.
    - cogs/music.py lines 973-986 — `_get_text_channel` (the pre-Phase-18 command-response resolver that
      STAYS for command responses; it must remain used at 672/679/823/934/2191 — do NOT touch those).
    - .planning/phases/19-onboarding-admin-setup/19-RESEARCH.md §"Summary" finding #1 + §"Common Pitfalls"
      Pitfall 1 (why this is a live gap, not a kwarg add) + §"Test regression surface" #9
      (`_post_music_roast` is untested-by-design; do NOT conflate with the `_get_text_channel` mocks in
      test_autoqueue_playback.py / test_now_playing_refresh.py).
  </read_first>
  <action>
    Add `from logic.guild_config import AmbientSurface` to cogs/music.py's imports. In `_post_music_roast`
    (line ~1170) replace `channel = self._get_text_channel(guild)` with
    `channel = self.bot.guild_config.resolve_ambient_channel(guild, surface=AmbientSurface.ROAST)` (D-18:
    repeat-song + both milestone roasts all funnel through this one method, so this single swap gates all
    three; an unconfigured guild now stays silent instead of roasting in whatever channel /play last ran).
    Leave the `if channel is None: return` guard and the send/allowed_mentions body unchanged. Do NOT
    change any other `_get_text_channel` call site — those are legitimate command-response resolvers, not
    ambient roasts (RESEARCH Pitfall 1 warning-sign).
  </action>
  <acceptance_criteria>
    - `_post_music_roast`'s body calls `self.bot.guild_config.resolve_ambient_channel(guild, surface=AmbientSurface.ROAST)` and no longer calls `self._get_text_channel(guild)`.
    - `_get_text_channel` still exists and is still referenced elsewhere in cogs/music.py (the command-response sites are untouched).
    - `cogs/music.py` imports `AmbientSurface`.
    - `ruff check cogs/music.py` exits 0; `pytest -q` still green (test_autoqueue_playback.py / test_now_playing_refresh.py unaffected).
  </acceptance_criteria>
  <verify>
    <automated>ruff check cogs/music.py && python -c "import re; s=open('cogs/music.py').read(); i=s.index('async def _post_music_roast'); body=s[i:i+400]; assert 'resolve_ambient_channel(guild, surface=AmbientSurface.ROAST)' in body and '_get_text_channel' not in body; assert s.count('_get_text_channel') >= 5" && pytest -q</automated>
  </verify>
  <done>Repeat-song and milestone roasts route through the surface-keyed ambient resolver (silent in unconfigured guilds); every command-response `_get_text_channel` site is untouched; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new/departing guild → owner notice | attacker-chosen `guild.name`/owner tag crosses into the owner's ERROR_LOG embed |
| gateway join event → welcome send | untrusted third-party guild receives an unprompted send; a permission failure must never crash the join |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Tampering / Spoofing | `_build_guild_notice_embed` | mitigate | `guild.name` + owner tag rendered as PLAIN embed field values (no markdown wrap, no hyperlink label); numeric `guild.id`/`owner_id` inside backtick inline-code spans (snowflakes cannot contain a backtick). Embeds do not parse mentions from field text; `allowed_mentions=none()` on the welcome content send |
| T-19-04 | Denial of Service | repeated join/leave notice + welcome on kick-and-reinvite | accept (known limitation) | Out of Phase 19 scope by design — the blacklist + re-invite refusal is Phase 20 OWNER-04 (D-12). Record explicitly for PORT-04 disclosure; do NOT build an abuse heuristic here (REQUIREMENTS Out of Scope). `insert_guild_config_if_absent` idempotency bounds state damage |
| T-19-05 | Tampering | welcome send failure path | accept (self-healing) | The welcome is wrapped in try/except (D-13): a `Forbidden`/HTTPException logs a WARNING, annotates the owner notice ("welcome posted: no"), and never crashes the join (ONBOARD-01) |
| T-19-SC | Tampering | package installs | mitigate | N/A — zero new dependencies this phase |
</threat_model>

<verification>
- `pytest -q` fully green (glue is untested-by-design per D-26; the suite proves no import/collection regression and that the deferred 19-02 call-site migration for bot.py/music.py is complete).
- `ruff check .` + `ruff format --check .` pass for bot.py / personality/roasts.py / cogs/music.py.
- Clean local boot (manual/structural): no TypeError from a missing `surface=` at any `resolve_ambient_channel` site (bot.py + music.py migrations complete this phase's surface rollout).
- Structural review of the untested handlers per the Manual-Only checks in 19-VALIDATION.md.
</verification>

<success_criteria>
- `on_guild_join` inserts a row, welcomes only when the insert actually inserted, never crashes on a send failure, and notifies the owner (ONBOARD-01/05).
- `on_guild_remove` notifies the owner and evicts the cache entry with zero DB writes (D-12).
- Boot backfill welcomes each guild invited-while-offline exactly once, keyed on the insert result, after `seed_home_guild` (D-14/D-15).
- Startup message is home-guild-only (D-23); idle-loneliness stays per-guild; `_post_music_roast` routes through the seam (Pitfall 1).
</success_criteria>

## Artifacts this phase produces (this plan)

- `bot.py::on_guild_join`, `bot.py::on_guild_remove` (top-level `@bot.event` handlers)
- `bot.py::_post_guild_welcome`, `bot.py::_build_guild_notice_embed` (reusable helpers)
- bot.py boot-backfill loop in `_initialize_once` (after `seed_home_guild`)
- home-guild-only `_post_startup_messages`; `surface=AmbientSurface.PRESENCE` on the idle-loneliness resolve
- `"cogs.admin"` added to the cog-load tuple
- `personality/roasts.py::WELCOME_MESSAGES`, `personality/roasts.py::WELCOME_SETUP_HINT`
- `cogs/music.py::_post_music_roast` routed through `resolve_ambient_channel(surface=ROAST)`

<output>
Create `.planning/phases/19-onboarding-admin-setup/19-03-SUMMARY.md` when done.
</output>
