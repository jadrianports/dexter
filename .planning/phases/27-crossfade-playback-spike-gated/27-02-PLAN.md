---
phase: 27-crossfade-playback-spike-gated
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - models/queue.py
  - personality/responses.py
  - tests/test_queue.py
  - tests/test_queue_persistence.py
  - tests/test_responses.py
autonomous: true
requirements: [DJ-03]

must_haves:
  truths:
    - "A room's 'we like smooth transitions here' preference is not silently revoked by /stop (D-12)"
    - "Fade scratch state never outlives the queue it belonged to — a stale handoff cannot make the next session's first track fade in from a track that is no longer playing"
    - "The toggle dies on restart and is never written to Neon (D-12, mirroring Phase 26 D-08)"
    - "The /crossfade copy is lowercase with at most one emoji (Critical Rules 7/8)"
  artifacts:
    - path: "models/queue.py"
      provides: "crossfade_enabled preference (survives clear) + _xf_pending/_xf_truncator playback state (reset by clear)"
      contains: "crossfade_enabled"
    - path: "personality/responses.py"
      provides: "CROSSFADE_ON / CROSSFADE_OFF templated copy pools"
      contains: "CROSSFADE_ON"
    - path: "tests/test_queue_persistence.py"
      provides: "Guard that the toggle is unpersisted by construction"
      min_lines: 40
  key_links:
    - from: "models/queue.py::MusicQueue.clear"
      to: "_xf_pending / _xf_truncator"
      via: "explicit null in the playback-state reset block"
      pattern: "_xf_pending"
    - from: "services/queue_persistence.py::persist"
      to: "the typed-key payload dict"
      via: "six fixed keys, never __dict__ — crossfade is unpersisted by construction"
      pattern: "payload = \\{"
---

<objective>
Land the D-12 state split on `MusicQueue` — the `crossfade_enabled` **server preference** that
survives `clear()`, and the `_xf_pending` / `_xf_truncator` **playback state** that does not — plus
the `/crossfade` copy pools and the guards that lock both rules.

Purpose: D-12 is a two-sided rule and the sides are easy to conflate. `crossfade_enabled` follows
`auto_lyrics` (a stated preference; `/stop` must not revoke it). The two `_xf_*` fields follow
`radio_armed` (playback state; a stale `_xf_pending` surviving a `/stop` would make the next
session's first track try to fade in from a track that is no longer playing — RESEARCH §7).
Because every teardown site already calls `clear()`, this needs **zero** `bot.py` edits — the same
property Phase 26's radio disarm inherited.
Output: three `MusicQueue` fields, two copy pools, three test guards.

Covers VALIDATION.md Behavior Map rows **9, 10, 15**.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-crossfade-playback-spike-gated/27-RESEARCH.md
@.planning/phases/27-crossfade-playback-spike-gated/27-PATTERNS.md
@.planning/phases/27-crossfade-playback-spike-gated/27-CONTEXT.md
</context>

## Artifacts this phase produces (this plan's share)

| Symbol | Kind | Path |
|--------|------|------|
| `MusicQueue.crossfade_enabled` | attribute (`bool`, default `False`) — **NOT** reset by `clear()` | `models/queue.py` |
| `MusicQueue._xf_pending` | attribute (`tuple[Track, float] \| None`) — **IS** reset by `clear()` | `models/queue.py` |
| `MusicQueue._xf_truncator` | attribute (`TruncatingSource \| None`) — **IS** reset by `clear()` | `models/queue.py` |
| `CROSSFADE_ON` | copy pool (`list[str]`) | `personality/responses.py` |
| `CROSSFADE_OFF` | copy pool (`list[str]`) | `personality/responses.py` |

> **Note on `/crossfade` copy placement.** D-09b says "the `/autolyrics` shape" — that governs the
> COMMAND mechanics (one choice arg, no `defer()`, no cooldown, `AllowedMentions.none()`), which
> plan 27-05 implements. It does **not** govern where the copy string lives. `/autolyrics` inlines
> its copy in the cog, but VALIDATION Behavior Map row 15 mandates the test at
> `tests/test_responses.py::test_crossfade_copy_style` — which requires the copy to be importable
> from `personality/responses.py`. So: **`/autolyrics` mechanics + Phase 26 `RADIO_START`/`RADIO_STOP`
> pool placement.** Both precedents are honored; neither is violated.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MusicQueue — the crossfade_enabled preference + the two _xf_* playback fields</name>
  <read_first>
    - `models/queue.py` lines 65-115 — `MusicQueue.__init__`. Note the **two distinct comment
      blocks**: `auto_lyrics` at :78-80 (preference, "Deliberately NOT reset by clear()") and
      `radio_armed` at :90-108 (playback state, with the "unpersisted by construction" note).
      This is the one file where two different analogs apply to two different fields.
    - `models/queue.py` lines 247-273 — `clear()`. Grouped by phase, one comment per group.
      `auto_lyrics` is **absent** from this method by design.
    - `tests/test_queue.py` line 24 (`test_auto_lyrics_defaults_off_and_survives_clear`) and
      line 307 (`test_clear_resets_filter_and_elapsed`) — the two existing analog tests.
    - `27-RESEARCH.md` §Findings 7 — the exact field shapes and the "stale `_xf_pending` after a
      `/stop`" failure this prevents.
  </read_first>
  <behavior>
    - A fresh `MusicQueue` has `crossfade_enabled is False`, `_xf_pending is None`,
      `_xf_truncator is None`.
    - `crossfade_enabled = True` → `clear()` → still `True` (the D-12 preference rule).
    - `_xf_pending = (track, 196.0)` and `_xf_truncator = object()` → `clear()` → both `None`.
  </behavior>
  <action>
    In `models/queue.py::MusicQueue.__init__`, add three fields in **two different places**:

    1. **Beside `auto_lyrics` (~:78-80), above the playback-state block** —
       `self.crossfade_enabled: bool = False`. Comment must mirror the `auto_lyrics` comment's
       shape and state: per-guild, in-memory, deliberately NOT reset by `clear()` because it is a
       server preference ("we like smooth transitions here"), not playback state — D-12. Resets
       only on restart.

    2. **In the playback-state region (near `radio_armed`, ~:90-108)** —
       `self._xf_pending: tuple[Track, float] | None = None` and
       `self._xf_truncator = None`. Comment must state: these ARE playback state (like `radio_armed`
       / `loop_mode`, unlike `crossfade_enabled` above), which is why `clear()` nulls them — a stale
       `_xf_pending` surviving a `/stop` would make the next session's first track try to fade in
       from a track that is no longer playing (RESEARCH §7). Also note they are unpersisted by
       construction: `services/queue_persistence.py::persist` builds an explicit typed key dict,
       never `__dict__`.

    In `models/queue.py::MusicQueue.clear()`, add a Phase 27 group that nulls **only**
    `_xf_pending` and `_xf_truncator`. **`crossfade_enabled` MUST NOT appear in `clear()`** — its
    absence is the D-12 rule. Comment must name D-12 and state that every existing teardown site
    (`/stop`, the stop button's `_do_stop`, `bot.py::idle_check`'s idle-leave, reconnect-failure)
    already calls `clear()`, so this needs no per-site edit — the same property radio inherited.

    Do NOT touch `_play_generation`, `disarm_radio()`, the skip-vote cache, or any other existing
    line in `clear()`.
  </action>
  <verify>
    <automated>pytest tests/test_queue.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from models.queue import MusicQueue; q=MusicQueue(1); assert q.crossfade_enabled is False and q._xf_pending is None and q._xf_truncator is None"` passes.
    - `grep -n "crossfade_enabled" models/queue.py` shows it in `__init__` and **NOT** inside `clear()`.
    - `grep -n "_xf_pending\|_xf_truncator" models/queue.py` shows both in `__init__` **and** in `clear()`.
    - `git diff models/queue.py` shows zero changes to `_play_generation`, `disarm_radio()`, or the skip-vote cache.
  </acceptance_criteria>
  <done>The D-12 split exists: one field survives clear(), two are reset by it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The D-12 clear() guard + the not-persisted guard (Behavior Map rows 9, 10)</name>
  <read_first>
    - `tests/test_queue.py` lines 24-35 and 307-323 — the `auto_lyrics`-survives-clear test shape,
      the direct model for row 9.
    - `tests/test_radio_logic.py` lines 315-327 — `test_disarm_never_persisted_in_queue_persistence_payload`.
      This is the repo's **actual** not-persisted precedent: a source scan asserting the string
      never appears in `services/queue_persistence.py`.
    - `services/queue_persistence.py` lines 40-56 — the typed-key payload dict (six fixed keys:
      tracks / current_index / loop_mode / text_channel_id / voice_channel_id / active_filter).
      **This is the thing the test asserts against. No production change is needed to make
      crossfade unpersisted — the test locks the existing construction.**
  </read_first>
  <behavior>
    - `tests/test_queue.py::test_crossfade_toggle_survives_clear` — toggle True → `clear()` → still
      True; `_xf_*` set → `clear()` → both None.
    - `tests/test_queue_persistence.py::test_crossfade_not_persisted` — a persisted payload built
      from a queue with `crossfade_enabled=True` carries no crossfade key.
  </behavior>
  <action>
    **Row 9** — add `test_crossfade_toggle_survives_clear` to `tests/test_queue.py`, modeled on
    `test_auto_lyrics_defaults_off_and_survives_clear`. It must assert **both halves of D-12 in one
    test**: (a) `crossfade_enabled=True` survives `clear()`, and (b) `_xf_pending` and
    `_xf_truncator`, both set to non-None sentinels beforehand, are `None` after. A test that only
    checked the surviving half would not lock the rule that actually prevents the stale-handoff bug.

    **Row 10** — create `tests/test_queue_persistence.py`. **This is a NEW file.** Both
    `27-VALIDATION.md` §Wave 0 and the phase brief claim it "exists (extend)" — it does not; the
    repo's only persistence guard lives in `tests/test_radio_logic.py`. Create it rather than
    hunting for a file to extend. Add `test_crossfade_not_persisted` asserting **two independent
    ways**:
      1. **Behavioral (primary):** drive `QueuePersistence.persist()` with a `MusicQueue` that has
         `crossfade_enabled=True` and `_xf_pending` set, capturing the JSON payload via a stubbed
         pool (mirror however `tests/test_database_phase21.py` or `tests/test_discover.py` stub the
         asyncpg pool — reuse the existing convention, do not invent a new one). Assert the decoded
         payload's key set is **exactly** the six known keys — an equality assertion, not a
         `not in` check, so any future field added via `__dict__` fails this test.
      2. **Structural (secondary, the radio precedent):** assert the literal strings `"crossfade"`
         and `"_xf_"` do not appear in `services/queue_persistence.py`'s source.

    Do not modify `services/queue_persistence.py` — the point of this task is that it needs no
    change.
  </action>
  <verify>
    <automated>pytest tests/test_queue.py tests/test_queue_persistence.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_queue.py::test_crossfade_toggle_survives_clear` passes and asserts both halves of D-12.
    - `tests/test_queue_persistence.py::test_crossfade_not_persisted` passes.
    - The payload assertion is an **exact key-set equality**, not a `not in` membership check.
    - `git diff --stat services/queue_persistence.py` is empty (zero production changes).
  </acceptance_criteria>
  <done>Rows 9 and 10 are locked; the toggle provably never reaches Neon.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CROSSFADE_ON / CROSSFADE_OFF copy pools + the style guard (Behavior Map row 15)</name>
  <read_first>
    - `personality/responses.py` lines 207-236 — the Phase 26 pools (`SKIP_VOTE_TALLY`,
      `RADIO_START`, `RADIO_STOP`, `RADIO_LOOP_CONFLICT`, `RADIO_NOT_ARMED`) and `pick_random`.
    - `tests/test_responses.py` lines 79-130 — `TestPhase26ResponsePools`: the pool-list
      registry constant, `test_pools_are_non_empty_lists_of_str` (≥3 entries), the
      `pick_random`-returns-a-member test, and `test_all_phase26_pool_entries_are_lowercase`.
    - `cogs/music.py` lines 1973-1995 — `/autolyrics`'s copy tone (dry, lowercase, no emoji).
    - CLAUDE.md §Critical Rules 7/8 — ≤1 emoji, lowercase, no caps lock.
  </read_first>
  <behavior>
    - `CROSSFADE_ON` and `CROSSFADE_OFF` are each `list[str]` with ≥3 entries.
    - Every entry is lowercase and contains at most one emoji.
    - `pick_random(CROSSFADE_ON)` returns a member.
    - Neither pool contains a format placeholder (both are zero-arg — there is no `{seconds}` slot,
      because D-12b rejected a user-settable fade length).
  </behavior>
  <action>
    Add a `# --- Phase 27: Crossfade (DJ-03) ---` section to `personality/responses.py` with two
    zero-arg pools in the Phase 26 style, each ≥3 entries:

    - `CROSSFADE_ON: list[str]` — confirms the toggle is on.
    - `CROSSFADE_OFF: list[str]` — confirms the toggle is off.

    Tone: lowercase, dry, sarcastic, accurate-first, ≤1 emoji (Critical Rules 7/8) — match
    `/autolyrics`'s register ("fine. i'll narrate your questionable taste in a thread. enjoy." /
    "auto-lyrics off. blessed silence."). Write your own lines; do not copy the autolyrics strings.

    **Zero-arg pools — no `{}` placeholders.** There is no fade-length slot to interpolate (D-12b:
    the length is a fixed constant, not a user-facing arg), and the copy must not name the number —
    a hardcoded "4 seconds" in a string would silently drift the moment `CROSSFADE_SECONDS` changes.

    Then extend `tests/test_responses.py` with a `TestPhase27ResponsePools` class following
    `TestPhase26ResponsePools`, including a `test_crossfade_copy_style` (the VALIDATION row-15
    name — do not rename) that asserts, for every entry in both pools: the entry equals its own
    `.lower()`, and its emoji count is ≤1. Count emoji by scanning for codepoints in the
    `unicodedata` "So" (Symbol, other) category or a Misc-Symbols/Emoticons/Transport/Supplemental
    codepoint-range check — pick one and state the choice in the test's docstring. Also assert
    `"{" not in entry` for both pools (locks the zero-arg rule).
  </action>
  <verify>
    <automated>pytest tests/test_responses.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from personality import responses as r; assert len(r.CROSSFADE_ON)>=3 and len(r.CROSSFADE_OFF)>=3"` passes.
    - `tests/test_responses.py::TestPhase27ResponsePools::test_crossfade_copy_style` exists and passes.
    - Every entry in both pools satisfies `entry == entry.lower()` and has ≤1 emoji.
    - No entry in either pool contains `{` (zero-arg guard).
    - No entry contains the literal `"4"` or the word "second" (no hardcoded fade length).
  </acceptance_criteria>
  <done>Row 15 is locked; the copy is importable from responses.py for plan 27-05 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MusicQueue` in-memory state → `services/queue_persistence.py` → Neon | The only boundary where queue state leaves the process. The payload is a typed dict of six fixed keys flowing through `$N` params (existing T-04-06 mitigation). |
| Discord user → `/crossfade` → `crossfade_enabled` | Bounded: the only reachable values are the two `app_commands.Choice` literals. No free text, no int, reaches this field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Information Disclosure | `_xf_pending` holding a `Track` past its session | mitigate | `clear()` nulls it at every teardown site. Locked by row 9's second half. A stale handoff is both a correctness bug and a cross-session state leak; one null closes both. |
| T-27-06 | Tampering | Queue state reaching the Neon payload | mitigate | `persist()`'s typed six-key dict is never `__dict__`. Row 10 asserts **exact key-set equality**, so a future field cannot silently ride along into the DB. |
| T-27-07 | Denial of Service | Unbounded growth of `_xf_pending` | accept | Single-slot `tuple \| None`, overwritten per transition, nulled on `clear()`. No accumulation is possible — unlike `radio_played`, this is not a collection. |
| T-27-08 | Spoofing | `/crossfade` copy carrying a mention | mitigate | Plan 27-05 sends with `AllowedMentions.none()` (the `/autolyrics` precedent). Pools are static author-written literals with no interpolation slot (row 15 asserts `"{" not in entry`), so no user string can reach the reply. |
| T-27-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero installs this plan.** No `requirements.txt` change; no package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `pytest tests/test_queue.py tests/test_queue_persistence.py tests/test_responses.py -x -q` — rows 9, 10, 15 green.
- `pytest -q` — full suite ≥ 1175 passed, 0 failed.
- `ruff check . && ruff format --check .` — clean.
- `grep -n "crossfade_enabled" models/queue.py` proves absence from `clear()`.
</verification>

<success_criteria>
- `crossfade_enabled` survives `clear()`; `_xf_pending` / `_xf_truncator` are nulled by it.
- The toggle is provably unpersisted, asserted by exact key-set equality.
- Two lowercase, ≤1-emoji, zero-arg copy pools exist in `personality/responses.py`.
- Zero production changes to `services/queue_persistence.py`.
- Full suite green at or above the 1175 baseline.
</success_criteria>

<output>
Create `.planning/phases/27-crossfade-playback-spike-gated/27-02-SUMMARY.md` when done.
</output>
