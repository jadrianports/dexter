---
phase: 27-crossfade-playback-spike-gated
plan: 05
type: execute
wave: 3
depends_on: ["27-01", "27-02", "27-03", "27-04"]
files_modified:
  - cogs/music.py
  - tests/test_music_wiring.py
autonomous: true
requirements: [DJ-03]

must_haves:
  truths:
    - "A user can turn crossfade on with /crossfade on and off with /crossfade off"
    - "With crossfade on and both tracks cached, the outgoing tail blends into the incoming head"
    - "A non-fadeable transition silently hard-cuts — the room sees nothing, only a log line (D-10b)"
    - "The per-track voice_client.play() + generation-counter model is untouched (D-01)"
    - "Phase 26's _try_skip → _do_skip single choke point and its vote cache are untouched (D-12c/D-12d)"
    - "A skip cuts the fade dead — both sources torn down, no partially-faded state (D-12c)"
  artifacts:
    - path: "cogs/music.py"
      provides: "/crossfade on|off command + the two _play_track integration points + the _on_track_end handoff"
      contains: "name=\"crossfade\""
    - path: "tests/test_music_wiring.py"
      provides: "Structural guards: D-01 engine block intact, single _do_skip call site preserved"
  key_links:
    - from: "cogs/music.py::_play_track"
      to: "logic.crossfade.decide_crossfade"
      via: "consulted after get_source, BEFORE the _play_generation increment; glue dispatches on the verdict"
      pattern: "decide_crossfade"
    - from: "cogs/music.py::_on_track_end"
      to: "queue._xf_pending"
      via: "set from truncator.position_seconds when cut_short, BEFORE the decide_on_track_end dispatch"
      pattern: "_xf_pending"
    - from: "cogs/music.py::_play_track"
      to: "audio.get_source(crossfade_from=...)"
      via: "the pending handoff, consumed and cleared at source-acquisition time"
      pattern: "crossfade_from"
---

<objective>
Wire the feature: the `/crossfade on|off` command, the two `_play_track` integration points, and the
`_on_track_end` handoff. This is the plan that touches the scar surface, and it is **additive or it
does not ship** (D-01).

Purpose: everything the prior four plans built is inert until the glue consults `decide_crossfade`
and dispatches on the verdict. RESEARCH §5 names both insertion points precisely and states that
**everything between them stays verbatim** — the generation block, `after_callback` and its guard,
the `stop()`/`play()` ordering, both `source.cleanup()` failure paths, and the prefetch dispatch are
all byte-identical. Per `TESTING.md` this glue is **untested-by-design**; its safety evidence is the
spike's D-11 attack artifacts (ffmpeg counts, counter values, stale-callback log lines) recorded in
RESEARCH §Evidence, **not** unit tests. Do not invent mock-heavy tests over `_play_track`.
Output: one command, three glue insertions, two structural guards.

Completes the phase. Behavior Map rows 1-18 are all covered once this lands.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-crossfade-playback-spike-gated/27-RESEARCH.md
@.planning/phases/27-crossfade-playback-spike-gated/27-PATTERNS.md
@.planning/phases/27-crossfade-playback-spike-gated/27-CONTEXT.md
@.planning/phases/27-crossfade-playback-spike-gated/27-01-SUMMARY.md
@.planning/phases/27-crossfade-playback-spike-gated/27-02-SUMMARY.md
@.planning/phases/27-crossfade-playback-spike-gated/27-03-SUMMARY.md
@.planning/phases/27-crossfade-playback-spike-gated/27-04-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

| Symbol | Kind | Path |
|--------|------|------|
| `/crossfade on\|off` | slash command (`MusicCog.crossfade`) | `cogs/music.py` |

<tasks>

<task type="auto">
  <name>Task 1: The /crossfade on|off command — the /autolyrics shape</name>
  <read_first>
    - `cogs/music.py` lines 1973-1995 — `/autolyrics`. **This is the shape D-09b names.** Copy it
      and rename: one `mode` choice arg, `app_commands.Choice` on/off, no `defer()`, **no cooldown
      decorator**, `AllowedMentions.none()` on both branches.
    - `personality/responses.py` — the `CROSSFADE_ON` / `CROSSFADE_OFF` pools from plan 27-02, and
      `pick_random`.
    - `cogs/music.py` — how the Phase 26 `/radio` command reads `responses.RADIO_START` via
      `pick_random`, for the pool-consumption convention.
    - `27-CONTEXT.md` D-09b and D-08b.
  </read_first>
  <action>
    Add a `/crossfade` command to `MusicCog`, structurally mirroring `/autolyrics`:

    - `@app_commands.command(name="crossfade", description=...)` with
      `@app_commands.choices(mode=[Choice(name="on", value="on"), Choice(name="off", value="off")])`.
    - **No `defer()`** — a pure in-memory toggle responds immediately, well inside the 3s rule.
    - **No cooldown decorator.** `/autolyrics` has none; `/filter` has `FILTER_COOLDOWN_SECONDS`.
      D-09b says the `/autolyrics` shape — follow it, do not add a cooldown.
    - Body: `queue = self.get_queue(interaction.guild.id)`; set `queue.crossfade_enabled` to
      `mode.value == "on"`; reply with `pick_random(responses.CROSSFADE_ON)` or
      `pick_random(responses.CROSSFADE_OFF)`, `allowed_mentions=discord.AllowedMentions.none()`.
    - Docstring: "Toggle crossfade for this server (in-memory; survives /stop, resets on restart)."
      — the D-12 distinction, stated where a reader will find it.

    Do NOT add a `seconds` argument (D-12b — rejected), a `/setup` toggle or a `guild_config` column
    (Phase 26 D-21 — rejected), or a `/filter` value (D-09b — rejected: crossfade is a transition
    behavior, not an `-af` chain over one source; putting it in `/filter` would make it mutually
    exclusive with `bassboost`, which is arbitrary and wrong).
  </action>
  <verify>
    <automated>pytest tests/test_music_wiring.py -x -q && python -c "import ast; src=open('cogs/music.py').read(); ast.parse(src); assert 'name=\"crossfade\"' in src; assert 'CROSSFADE_ON' in src and 'CROSSFADE_OFF' in src; print('command ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `/crossfade` is registered with exactly two choices (on/off).
    - The command body contains **no** `defer()` call and **no** `checks.cooldown` decorator.
    - Both reply branches pass `allowed_mentions=discord.AllowedMentions.none()`.
    - Copy is sourced from `responses.CROSSFADE_ON` / `CROSSFADE_OFF` via `pick_random` — no inline string literals.
    - `grep -c "seconds" ` over the new command body returns 0 (no user-settable fade length).
  </acceptance_criteria>
  <done>The toggle is reachable from Discord and defaults off.</done>
</task>

<task type="auto">
  <name>Task 2: _play_track — the two integration points (arm the truncator; consume the handoff)</name>
  <read_first>
    - `cogs/music.py` lines 640-760 — `_play_track` in full. **Read the whole function before
      editing.** Note the exact order: resolve filter (:664-667) → `get_source()` (:673) →
      `_play_generation += 1` (:702) → capture `current_gen` (:703) → `after_callback` guarded on
      `queue._play_generation == current_gen` (:709) → `voice_client.stop()` (:718) →
      `voice_client.play(source, after=...)` (:728) → `_prefetch_next_track` with the in-scope
      `current_gen` (:748). Also `source.cleanup()` on both failure paths (:721, :734).
    - `27-RESEARCH.md` §Findings 5 — the two insertion points, stated precisely.
    - `27-CONTEXT.md` D-01 — the engine model is **inviolable**; a rewrite proposal IS the no-go.
    - `27-PATTERNS.md` §"The block that must stay verbatim" (`cogs/music.py:700-712`).
    - CLAUDE.md §"Implementation Gotchas → Phase 1" (never call `voice_client.stop()` before
      `_play_track`) and §"Phases 9-12" (Pitfall 4: `_play_track`'s create_task calls stay bare
      `asyncio.create_task` — a `make_task` callback there would double-log).
  </read_first>
  <action>
    Two additions to `_play_track`. **Everything between them stays verbatim** — do not touch the
    generation block, `after_callback`, the `stop()`/`play()` ordering, either `source.cleanup()`
    failure path, or the prefetch dispatch.

    **Integration point 1 — consume a pending handoff at `get_source` call time.** Before the
    `await self.audio.get_source(...)` call at :673, read `queue._xf_pending`. If set, pass it as
    `crossfade_from=` and **clear the handoff immediately** (`queue._xf_pending = None`) so it can
    never be consumed twice. If unset, call `get_source` exactly as today — the kwarg defaults to
    None and the call is byte-identical.

    **Integration point 2 — arm this track's own fade, immediately after `get_source` returns and
    BEFORE the `queue._play_generation += 1` at :702.** Consult `decide_crossfade`, feeding:
    `enabled=queue.crossfade_enabled`, `has_next=bool(queue.upcoming())`,
    `loop_single=(queue.loop_mode is LoopMode.SINGLE)`, `filter_active=(ffmpeg_filter is not None)`
    — **reuse the already-resolved `ffmpeg_filter` local from :666, do not re-derive
    `queue.active_filter != "off"`** — `outgoing_cached` / `incoming_cached` via
    `self.audio.is_cached(...)`, the two durations from the `Track`s, `seek_offset=offset_seconds`.

    Then dispatch on the returned member, and **never re-derive a rung** (Phase 10 D-02):
    - `FadeVerdict.FADE` → wrap: `source = TruncatingSource(source, cut_frame(outgoing_duration=...,
      fade_seconds=config.CROSSFADE_SECONDS))`, stash the truncator on `queue._xf_truncator`, and
      log the mix at info (mirror the spike's `crossfade: mixing tail=... into head=... over Ns`).
    - anything else → `log.info("crossfade: hard cut (%s)", verdict.value)` and pass `source`
      through **untouched**. **D-10b: log only — the room sees nothing.** Do not send a message, do
      not add a Dexter line, do not set a flag the embed reads. The fallback IS current behavior;
      there is nothing to apologize for. `services/audio.py:121`'s
      `log.warning("Download failed ... falling back to stream")` is the tone precedent.

    **D-01 compliance is the bar for this task.** One `play()` per track, one source per `play()`.
    The wrap replaces the local `source` variable *before* the generation block; the voice client
    still receives exactly one object. Nothing in `logic/playback.py` changes. No session-long
    mixer, no ownership transfer across the player boundary, no keeping an FFmpeg process alive
    across `play()` calls.

    **Nesting is expected and correct.** The fade for the *next* transition arms while the *current*
    fade is still mixing, so `source` becomes `TruncatingSource(CrossfadeSource(...))` — RESEARCH
    attack 1 observed exactly this and it is load-bearing. Any instrumentation or `isinstance` check
    on `vc.source` must expect the wrapper, not the mixer (landmine #2).
  </action>
  <verify>
    <automated>pytest tests/test_music_wiring.py -x -q && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `git diff cogs/music.py` shows **zero** changes to the block from `queue._play_generation += 1` through `voice_client.play(source, after=after_callback)` — the D-01 subject, verbatim.
    - `decide_crossfade` is called exactly once in `_play_track`, before the generation increment.
    - `filter_active` is fed from the resolved `ffmpeg_filter` local, not a re-derived `queue.active_filter != "off"`.
    - The non-FADE branch contains **no** `channel.send`, `interaction.response`, or `followup` call — log only.
    - `queue._xf_pending` is set to `None` in the same block that reads it.
    - `_play_track`'s existing `asyncio.create_task` / `make_task` calls are unchanged (Pitfall 4).
    - `pytest -q` full suite ≥ 1175 passed, 0 failed.
  </acceptance_criteria>
  <done>A fade arms when eligible and silently hard-cuts otherwise, with the engine model untouched.</done>
</task>

<task type="auto">
  <name>Task 3: _on_track_end handoff + the D-01/D-12c structural guards</name>
  <read_first>
    - `cogs/music.py` lines 858-930 — `_on_track_end`. It dispatches on `decide_on_track_end`'s
      returned `TrackEndAction` and **must keep doing so** (Phase 10 D-02). Phase 26's D-10 radio
      lookahead also lives here as an *independent additional gate* alongside the dispatch — never
      a new `TrackEndAction` member. The crossfade handoff follows that same "additional gate, not
      a new member" precedent.
    - `cogs/music.py` `_try_skip` (:1043) → `_do_skip` (:1122) — **Phase 26's D-15 single vote-gated
      choke point: `_do_skip` is called EXACTLY ONCE, from inside `_try_skip`.** Phase 26 left
      `_do_skip` completely unmodified, preserving Critical Rule 3 for free.
    - `tests/test_music_wiring.py` — the Phase 26 structural-guard file; mirror its conventions.
    - `27-RESEARCH.md` §Findings 5 (last paragraph — the `_on_track_end` addition) and landmine #5.
    - `27-CONTEXT.md` D-12c and D-12d.
  </read_first>
  <action>
    **(a) The handoff.** In `_on_track_end`, **before** the existing `decide_on_track_end` dispatch:
    if `queue._xf_truncator` is not None and its `cut_short` is True and `queue.is_playing`, set
    `queue._xf_pending = (outgoing_track, queue._xf_truncator.position_seconds)`. Then clear
    `queue._xf_truncator` **unconditionally** (whether or not it cut short) — a truncator that
    reached a natural EOF has nothing to hand off and must not linger into the next track.

    ⚠ **Read the cut position from `truncator.position_seconds` (i.e. `frames_read`), NEVER from
    the planned number or from `Track.duration_seconds`.** A file shorter than its YouTube metadata
    otherwise produces an `-ss` past EOF (landmine #5). `CrossfadeSource` tolerates an empty tail
    anyway (plan 27-03 / row 14), but that is the safety net, not the design.

    The outgoing track is the one that just finished — capture it **before** `queue.advance()` is
    called, since `advance()` moves `current_index`. Do not reorder or otherwise disturb the
    existing `advance()` / `_persist_queue` / `decide_on_track_end` sequence; this is an addition
    ahead of it, not a rewrite of it.

    **(b) D-12c / D-12d — verify by reading, change nothing.** A skip already cuts the fade dead for
    free: `_do_skip` → `_play_track` → `voice_client.stop()` tears down the current source, and
    `CrossfadeSource.cleanup()` owns both decoders (plan 27-03), so both die together with no
    partially-faded state. **Phase 26's `_try_skip` → `_do_skip` choke point and the
    `(current_index, video_id)` vote cache are UNTOUCHED** (D-12c/D-12d, Phase 26 D-15/D-17). The
    vote targets the incoming track because `queue.current_index` has already advanced — the fade is
    a presentation detail; the queue is the only truth. **Make no edit to `_try_skip`, `_do_skip`,
    or the vote cache in this plan.** If you believe one is needed, stop and surface it rather than
    editing — that would be a scope breach of the phase's central constraint.

    Note for reviewers, from RESEARCH: `_do_skip`'s `make_task(self._play_track(...))` is async, so
    the fade audibly continues until the next source is acquired. This is **pre-existing behavior,
    unchanged**, merely more visible with crossfade on. **Do not "fix" it in this phase.**

    **(c) The structural guards.** Extend `tests/test_music_wiring.py` with source-introspection
    tests (the file's existing convention — these are structural review encoded as tests, not mocks
    over glue):
    - `test_do_skip_has_exactly_one_call_site` — if the Phase 26 file already has an equivalent,
      assert it still passes rather than duplicating it. Phase 26's D-15 invariant must survive
      this phase.
    - `test_play_track_generation_block_intact` — assert `_play_track`'s source contains the
      `queue._play_generation += 1` → `current_gen = queue._play_generation` →
      `after_callback` → `queue._play_generation == current_gen` sequence, in that order. This is
      the D-01 tripwire: it turns a future edit to the engine block into a red build.
    - `test_crossfade_hard_cut_is_log_only` — assert the non-FADE branch in `_play_track`'s source
      reaches no user-facing send. Scope the scan to the function's source via `inspect.getsource`
      and search the crossfade block for `channel.send` / `followup` / `response.send_message`
      (D-10b).
  </action>
  <verify>
    <automated>pytest tests/test_music_wiring.py -x -q && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `git diff cogs/music.py` shows **zero** changes to `_try_skip`, `_do_skip`, and the skip-vote cache.
    - `_on_track_end` still dispatches on `decide_on_track_end`'s `TrackEndAction`; the handoff is an addition ahead of it, not a new enum member.
    - The cut position is read from `truncator.position_seconds`; `grep` shows no `duration_seconds` feeding `_xf_pending`.
    - `queue._xf_truncator` is cleared on **every** `_on_track_end` path, not only the cut-short one.
    - All three structural guards exist and pass.
    - `pytest -q` full suite ≥ 1175 passed, 0 failed.
  </acceptance_criteria>
  <done>The handoff works; D-01 and Phase 26's choke point are locked by tripwire tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user → `/crossfade` → `queue.crossfade_enabled` | Bounded to two `app_commands.Choice` literals. No free text reaches the field. |
| **Untrusted track metadata → the mixing path** | `Track.duration_seconds` (YouTube-controlled) and `Track.video_id` flow into `decide_crossfade` and, if FADE, into the `-ss` cut arithmetic. |
| `queue._xf_pending` → the next `_play_track` invocation | Cross-invocation state carrying a `Track` reference and a float. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-21 | **Tampering** | **Untrusted track metadata reaching the mixing path** | mitigate | Two independent controls: (1) the cut position is read from `TruncatingSource.position_seconds` — an in-process frame count — **never** from `Track.duration_seconds`, so hostile metadata cannot produce an `-ss` past EOF (landmine #5); (2) `cut_frame` floors at 0 (plan 27-01 / T-27-01). `duration_seconds` still feeds `decide_crossfade`'s `TOO_SHORT` rung, where the worst case of a lie is a hard cut — i.e. current behavior. |
| T-27-22 | **Tampering** | **A skip mid-fade double-playing or desyncing the generation counter** | mitigate | The generation block is untouched (asserted by `test_play_track_generation_block_intact`). RESEARCH attack 1 observed `STALE after_callback suppressed: gen=3 current_gen=2` **while two decoders were live** — the guard working during the exact race. Verified with the D-17 patch installed too. |
| T-27-23 | **Tampering** | **Orphaned FFmpeg from a skip mid-fade (Critical Rule 3)** | mitigate | Both decoders are owned by one `CrossfadeSource.cleanup()` (plan 27-03), so `voice_client.stop()` tears down both. RESEARCH measured `ffmpeg` 0→2→1→**0** across attack 1 and 2→**0** at both attack-2 teardown sites, with **no per-site edit** — the DEPLOY-06 scar class does not reopen. |
| T-27-24 | Elevation of Privilege | `/crossfade` invoked by any member | accept | Matches `/autolyrics`, `/filter`, `/loop` — every music command is unrestricted by design. Phase 26 D-21 explicitly ruled that music commands do not get per-guild permission surfaces; the `guild_config`/`AmbientSurface` machinery gates what Dexter does **unprompted at strangers**, which this is not. Worst case is a room member toggling a transition style. |
| T-27-25 | Spoofing | `/crossfade` reply carrying a mention | mitigate | `AllowedMentions.none()` on both branches; pools are static author-written literals with no interpolation slot (plan 27-02 / row 15). |
| T-27-26 | Denial of Service | A stale `_xf_pending` faded from a track that is gone | mitigate | Cleared at consumption (task 2), cleared unconditionally in `_on_track_end` (task 3), and nulled by `clear()` at every teardown site (plan 27-02). Three independent controls; `get_source` additionally falls through to the ordinary ladder when the outgoing file is missing (plan 27-03). |
| T-27-27 | Denial of Service | Vote-gating bypassed via a new skip path | mitigate | **No edit to `_try_skip` / `_do_skip` is permitted in this plan**, asserted by an empty diff over both. `test_do_skip_has_exactly_one_call_site` keeps Phase 26's D-15 invariant red-on-break — the guard that caught a real bypass (`/seek` past-end) during Phase 26's execution. |
| T-27-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero installs this plan.** No `requirements.txt` change → no package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `pytest tests/test_music_wiring.py -x -q` — the D-01 and D-15 tripwires green.
- `pytest -q` — full suite ≥ 1175 passed, 0 failed.
- `ruff check . && ruff format --check .` — clean.
- **Clean-boot check** (`TESTING.md`'s standard for untested-by-design glue): the bot imports and
  `setup_hook` runs without error; `dexter.log` shows the patch-install line.
- `git diff cogs/music.py` shows an empty diff over `_try_skip`, `_do_skip`, the skip-vote cache,
  and `_play_track`'s generation block.

**Glue is untested-by-design** per `.planning/codebase/TESTING.md` — structural review + clean boot.
Do **not** add mock-heavy unit tests over `_play_track` / `_on_track_end`. Their safety evidence is
the spike's D-11 attack artifacts in `27-RESEARCH.md` §Evidence. That is by design, not a gap.
</verification>

<success_criteria>
- `/crossfade on|off` exists in the `/autolyrics` shape, off by default.
- A fade arms when `decide_crossfade` returns FADE; every other verdict silently hard-cuts with a
  log line and no user-facing message.
- The D-01 engine block, Phase 26's `_do_skip` choke point, and the vote cache are provably untouched.
- Full suite green at or above the 1175 baseline.
- **Before `/gsd-verify-work`:** the user re-listens to the shipped implementation's render (D-08/
  D-10 — an agent cannot hear; the user's ear IS the SC-2 bar). The two parked live-Discord items
  (D-09 confirmation; D-17.5 decoder tolerance of the suppressed marker) go to `27-HUMAN-UAT.md`.
</success_criteria>

<output>
Create `.planning/phases/27-crossfade-playback-spike-gated/27-05-SUMMARY.md` when done.
</output>
