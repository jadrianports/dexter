---
phase: 03-alive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - personality/prompts.py
  - personality/roasts.py
  - tests/test_roasts.py
  - tests/test_prompts.py
autonomous: false
requirements: [PERS-01, PERS-02, PERS-03, PERS-04, PERS-07, PERS-08, PERS-09]
must_haves:
  truths:
    - "All Phase 3 config constants exist in config.py and import cleanly"
    - "personality/roasts.py exposes named template pools for every unprompted moment"
    - "DEXTER_SYSTEM_PROMPT contains few-shot USER/DEXTER exemplars and the three banned-mode rules"
    - "A reviewer (the user) approves the voice register of the written roast pools and rewritten system prompt"
  artifacts:
    - path: "config.py"
      provides: "Phase 3 constants (DEXTER_CHANNEL_ID, STREAK_TIMEZONE, roast/streak/status/lyrics constants)"
      contains: "DEXTER_CHANNEL_ID"
    - path: "personality/roasts.py"
      provides: "Template pools for join/leave/late-night/move/idle/startup/status/milestone/streak/no-lyrics"
      contains: "VOICE_JOIN_ROASTS"
    - path: "personality/prompts.py"
      provides: "Rewritten DEXTER_SYSTEM_PROMPT with few-shot exemplars + arrogant/outward-only rules"
      contains: "DEXTER_SYSTEM_PROMPT"
    - path: "tests/test_roasts.py"
      provides: "Pool non-empty assertions, pick_random behavior, is_late_night unit tests"
  key_links:
    - from: "personality/roasts.py"
      to: "personality.responses.pick_random"
      via: "import"
      pattern: "from personality.responses import pick_random"
    - from: "personality/prompts.py build_chat_prompt"
      to: "DEXTER_SYSTEM_PROMPT"
      via: ".format placeholders {max_length}/{mood_context}/{user_context}/{seasonal_context}"
      pattern: "DEXTER_SYSTEM_PROMPT.format"
---

<objective>
Lay the personality + configuration foundation for Phase 3. This plan owns ALL new `config.py` constants (so downstream plans only READ config, never write it — keeping waves parallel), creates the new `personality/roasts.py` template-pool module, and rewrites `DEXTER_SYSTEM_PROMPT` with few-shot exemplars per D-06.

Purpose: Per the sequencing guidance, the system-prompt rewrite (D-06) and the roast template-pool module (D-07) plus new config constants are foundational — most roast features depend on them. This is the highest-leverage work in Phase 3: the root-cause fix for the voice that "didn't translate."

Output: `config.py` (Phase 3 constants), `personality/roasts.py` (template pools), rewritten `DEXTER_SYSTEM_PROMPT`, and tests covering the pure/testable seams.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-alive/03-CONTEXT.md
@.planning/phases/03-alive/03-PATTERNS.md
@.planning/phases/03-alive/03-DISCUSSION-LOG.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (exclude from drift verification):

**New module `personality/roasts.py`** with module-level `list[str]` pools:
- `VOICE_JOIN_ROASTS`, `VOICE_LEAVE_ROASTS`, `LATE_NIGHT_ROASTS`, `BOT_MOVED_COMPLAINTS`,
  `IDLE_LONELINESS_MESSAGES`, `STARTUP_MESSAGES`, `STATUS_LINES`,
  `REPEAT_SONG_ROAST_TEMPLATES`, `MILESTONE_SONG_TEMPLATES`, `MILESTONE_STREAK_TEMPLATES`, `NO_LYRICS_FOUND`
- Pure helper `is_late_night(hour: int) -> bool`

**New `config.py` constants:** `DEXTER_CHANNEL_ID`, `STREAK_TIMEZONE`, `UNPROMPTED_ROAST_CHANCE`,
`LATE_NIGHT_ROAST_CHANCE`, `AMBIENT_ROAST_CEILING_SECONDS`, `ROAST_COOLDOWN_SECONDS`,
`REPEAT_SONG_ROAST_THRESHOLD`, `LATE_NIGHT_HOURS`, `MILESTONE_SONG_THRESHOLDS`,
`MILESTONE_STREAK_THRESHOLDS`, `STATUS_ROTATION_INTERVAL_SECONDS`,
`IDLE_LONELINESS_THRESHOLD_SECONDS`, `LYRICS_COOLDOWN_SECONDS`, `LYRICS_PAGE_SIZE`,
`HISTORY_PAGE_SIZE`, `HISTORY_FETCH_LIMIT`

**Rewritten symbol:** `DEXTER_SYSTEM_PROMPT` (same name, new content + preserved format placeholders).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add all Phase 3 constants to config.py</name>
  <files>config.py</files>
  <read_first>
    - config.py (read in full — current state ends at OWNER_ID line 53; see the ERROR_LOG_CHANNEL_ID int-or-None pattern at line 50 and the plain-numeric-constant style at lines 12-47)
    - .planning/phases/03-alive/03-PATTERNS.md (config.py section, lines 516-557 — exact constant names, values, and placement)
    - .planning/phases/03-alive/03-CONTEXT.md (D-09, D-12, D-13, D-17, D-19, D-21 — values these constants encode)
  </read_first>
  <action>
    Add a Phase 3 block to config.py after the `# --- Auto-Queue ---` block (line 47) and before `# --- Error Logging ---` (line 49). Define exactly these constants with these values:
    - `DEXTER_CHANNEL_ID = int(os.getenv("DEXTER_CHANNEL_ID", "0")) or None` (D-09; mirror the ERROR_LOG_CHANNEL_ID env pattern at line 50).
    - `STREAK_TIMEZONE = os.getenv("STREAK_TIMEZONE", "America/New_York")` (D-17; IANA default, env-overridable — add an inline comment noting it can be overridden via env).
    - `UNPROMPTED_ROAST_CHANCE = 0.30` (D-12, join/leave chance).
    - `LATE_NIGHT_ROAST_CHANCE = 0.50` (D-12, 1-5am chance).
    - `AMBIENT_ROAST_CEILING_SECONDS = 300` (D-13, unified per-user ambient ceiling).
    - `ROAST_COOLDOWN_SECONDS = 300` (alias of the ceiling for clarity).
    - `REPEAT_SONG_ROAST_THRESHOLD = 3` (D-12, plays/day to trigger).
    - `LATE_NIGHT_HOURS = (1, 5)` (tuple of inclusive hour bounds).
    - `MILESTONE_SONG_THRESHOLDS = [100, 250, 500, 1000]` (D-21).
    - `MILESTONE_STREAK_THRESHOLDS = [7, 14, 30, 60, 100]` (D-21).
    - `STATUS_ROTATION_INTERVAL_SECONDS = 300` (PERS-07, 5 min).
    - `IDLE_LONELINESS_THRESHOLD_SECONDS = 1800` (PERS-08, 30 min).
    - `LYRICS_COOLDOWN_SECONDS = 10`, `LYRICS_PAGE_SIZE = 1500`, `HISTORY_PAGE_SIZE = 10`, `HISTORY_FETCH_LIMIT = 50` (LYRIC-01 / HIST-01 tuning).
    Use the existing `# --- Phase 3: ... ---` section-comment style. Do NOT modify any existing constant. `os` is already imported at the top of config.py.
  </action>
  <verify>
    <automated>python -c "import config; assert config.DEXTER_CHANNEL_ID is None or isinstance(config.DEXTER_CHANNEL_ID, int); assert config.STREAK_TIMEZONE == 'America/New_York'; assert config.MILESTONE_SONG_THRESHOLDS == [100,250,500,1000]; assert config.MILESTONE_STREAK_THRESHOLDS == [7,14,30,60,100]; assert config.LATE_NIGHT_HOURS == (1,5); assert config.REPEAT_SONG_ROAST_THRESHOLD == 3; print('config ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import config"` succeeds (clean import, no syntax error)
    - config.py contains `DEXTER_CHANNEL_ID`, `STREAK_TIMEZONE`, `UNPROMPTED_ROAST_CHANCE`, `LATE_NIGHT_ROAST_CHANCE`, `AMBIENT_ROAST_CEILING_SECONDS`, `REPEAT_SONG_ROAST_THRESHOLD`, `LATE_NIGHT_HOURS`, `MILESTONE_SONG_THRESHOLDS`, `MILESTONE_STREAK_THRESHOLDS`, `STATUS_ROTATION_INTERVAL_SECONDS`, `IDLE_LONELINESS_THRESHOLD_SECONDS`, `LYRICS_PAGE_SIZE`, `HISTORY_PAGE_SIZE`, `HISTORY_FETCH_LIMIT`
    - No existing constant (lines 12-53 of original) is altered or removed
    - The verify command prints `config ok`
  </acceptance_criteria>
  <done>All Phase 3 config constants exist, import cleanly, and carry the specified values; no existing constant changed.</done>
</task>

<task type="auto">
  <name>Task 2: Create personality/roasts.py template-pool module + is_late_night helper + tests</name>
  <files>personality/roasts.py, tests/test_roasts.py</files>
  <read_first>
    - personality/responses.py (read in full — copy the module docstring style, the `pick_random()` signature at lines 9-11, and the `POOL_NAME: list[str] = [...]` convention used throughout)
    - .planning/phases/03-alive/03-PATTERNS.md (personality/roasts.py section, lines 29-85 — the full list of pools to create and the voice-register hard constraints to write into the docstring)
    - .planning/phases/03-alive/03-DISCUSSION-LOG.md ("Voice samples" section, lines 33-63 — the CANONICAL locked register; seed each pool from the matching scenario; reproduce the banned-line examples in a comment so they are never written)
    - .planning/phases/03-alive/03-CONTEXT.md (D-01 through D-05 voice rules; D-07 hybrid backbone; specifics lines 126-134 — startup must be arrogant not self-deprecating)
    - tests/test_responses.py (read in full — copy the pool-non-empty / pick_random test style for tests/test_roasts.py)
  </read_first>
  <action>
    Create personality/roasts.py following the personality/responses.py pattern exactly. Import `pick_random` from personality.responses (do NOT redeclare it): `from personality.responses import pick_random`. Write a module docstring that states the voice-register hard constraints verbatim: contempt aimed OUTWARD at user data never inward at self (D-02), mild swearing only — damn/hell/crap/ass/screw, no f-bombs (D-03), lowercase, ≤500 chars, one emoji max (D-05), humor from specific recall of tracked behavior not generic quips (D-01). Include a comment block listing the three BANNED inward modes with the canonical banned-line examples from DISCUSSION-LOG.md line 63 so they are never accidentally written.

    Define these module-level `list[str]` pools, each with 4-8 lines seeded from the matching DISCUSSION-LOG.md "Voice samples" scenario and written to the locked register (D-04 is the ceiling, most lines a notch below):
    - `VOICE_JOIN_ROASTS` — join-with-data + new-face lines; may contain a `{name}` placeholder for `.format()` at call site.
    - `VOICE_LEAVE_ROASTS` — "last to leave" register.
    - `LATE_NIGHT_ROASTS` — 1-5am time-related lines.
    - `BOT_MOVED_COMPLAINTS` — moved-channel complaint (always fires).
    - `IDLE_LONELINESS_MESSAGES` — 30-min-silence lines (idle sample).
    - `STARTUP_MESSAGES` — arrogant boot lines; seed with "i'm back. the queue fell apart without me, obviously. let's see what damage you did." — NEVER the self-deprecating "did you miss me. probably not." (D-02).
    - `STATUS_LINES` — static personality status strings (seed from status pool line 57); may contain `{n}` (server count) or `{current_song}` placeholders.
    - `REPEAT_SONG_ROAST_TEMPLATES` — fallback for the Gemini-backed repeat-song roast (D-08); may take `{name}`/`{count}`/`{title}` placeholders.
    - `MILESTONE_SONG_TEMPLATES` — fallback for song-count milestones; may take `{count}` placeholder.
    - `MILESTONE_STREAK_TEMPLATES` — fallback for streak-day milestones; may take `{days}`/`{record}` placeholders (D-20 — reference longest_streak).
    - `NO_LYRICS_FOUND` — personality error when neither Genius nor AZLyrics returns lyrics.

    Also add the pure helper `is_late_night(hour: int) -> bool` that returns True when `config.LATE_NIGHT_HOURS[0] <= hour <= config.LATE_NIGHT_HOURS[1]` (import config). This is the unit-testable seam for PERS-03.

    Create tests/test_roasts.py: assert every pool above is a non-empty `list[str]`; assert `pick_random(pool)` returns a member of the pool; assert `is_late_night(3) is True`, `is_late_night(1) is True`, `is_late_night(5) is True`, `is_late_night(0) is False`, `is_late_night(6) is False`, `is_late_night(12) is False`; assert no pool line contains an f-bomb (case-insensitive substring scan for the four-letter root and its common censored forms); assert STARTUP_MESSAGES contains no self-deprecation marker (scan that no line contains the phrase "miss me").
  </action>
  <verify>
    <automated>pytest tests/test_roasts.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - personality/roasts.py defines all 11 named pools listed in the action, each a non-empty `list[str]`
    - personality/roasts.py imports `pick_random` from personality.responses (does not redeclare it)
    - `is_late_night(3) is True` and `is_late_night(6) is False` (PERS-03 seam)
    - tests/test_roasts.py exits 0 under `pytest tests/test_roasts.py -x`
    - The f-bomb scan test passes (no banned profanity in any pool — D-03)
    - STARTUP_MESSAGES contains no line with the substring "miss me" (D-02 self-deprecation guard)
  </acceptance_criteria>
  <done>personality/roasts.py exists with all named pools and is_late_night; tests/test_roasts.py is green; D-02/D-03 voice guards are enforced by automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite DEXTER_SYSTEM_PROMPT with few-shot exemplars (D-06)</name>
  <files>personality/prompts.py, tests/test_prompts.py</files>
  <behavior>
    - build_chat_prompt("normal", "top artist: drake", "It's December.") still returns a non-empty string with no unfilled `{...}` placeholders
    - The rewritten DEXTER_SYSTEM_PROMPT still contains the four format placeholders `{max_length}`, `{mood_context}`, `{user_context}`, `{seasonal_context}` so build_chat_prompt() does not raise KeyError
    - The prompt body contains at least 4 few-shot exemplar pairs (USER:/DEXTER: markers) and an explicit statement of the three banned modes
  </behavior>
  <read_first>
    - personality/prompts.py (read in full — ONLY DEXTER_SYSTEM_PROMPT at lines 3-36 changes; MUSIC_RECOMMENDATION_PROMPT, MOOD_CONTEXTS, build_chat_prompt at lines 67-80, and build_recommendation_prompt MUST be left untouched. Note build_chat_prompt.format() depends on the four placeholders.)
    - .planning/phases/03-alive/03-PATTERNS.md (personality/prompts.py section, lines 155-181 — required rewrite pattern: identity statement, three banned modes, 4-6 few-shot USER/DEXTER pairs, preserved placeholders)
    - .planning/phases/03-alive/03-DISCUSSION-LOG.md ("Voice samples", lines 33-63 — source the few-shot exemplars from these locked lines; the canonical formula line is at line 35/176)
    - .planning/phases/03-alive/03-CONTEXT.md (D-01, D-02, D-05, D-06; specifics lines 126-134 — "Squidward-meets-Dexter-Morgan WITHOUT fourth-wall self-reference")
    - tests/test_prompts.py (read in full — extend it; keep existing assertions passing)
  </read_first>
  <action>
    Rewrite ONLY the `DEXTER_SYSTEM_PROMPT` string literal (lines 3-36) in personality/prompts.py. Structure the new prompt as:
    1. Identity statement: arrogant, superior, dry, contemptuous; humor comes from specific recall of the user's tracked behavior (real song title / artist / play-count) — the "Squidward-meets-Dexter-Morgan without fourth-wall self-reference" register (D-01).
    2. An explicit BANNED list (D-02): never reference being a bot / fourth wall; never pop-psych or diagnose the user; never self-deprecate, sound lonely, or fish for thanks. Contempt aims OUTWARD and DOWN at users only.
    3. Language rule (D-03): mild swearing only (damn/hell/crap/ass/screw), no f-bombs.
    4. Preserve the existing RESPONSE RULES intent (D-05): lowercase, ≤{max_length} chars, one emoji max, accurate-first-sarcastic-second, dial back sarcasm for genuinely serious/emotional questions.
    5. A FEW-SHOT EXEMPLARS section with at least 4 (target 4-6) `USER:` / `DEXTER:` pairs whose DEXTER lines are drawn from / written to the DISCUSSION-LOG.md "Voice samples" register, including the canonical formula line "marcus. back with the drake. forty-seven plays last week. one artist, one emotion, zero growth. impressive commitment to being boring."
    6. Keep the trailing MOOD / USER CONTEXT / seasonal sections so the four format placeholders `{max_length}`, `{mood_context}`, `{user_context}`, `{seasonal_context}` all remain present in the string. Escape any literal `{`/`}` that are NOT format placeholders by doubling them (`{{`/`}}`), since `.format()` is applied to this string.

    Extend tests/test_prompts.py: assert build_chat_prompt("normal", "top artist: drake", "It's December.") returns a non-empty string with no remaining `{` placeholder tokens for the four known keys; assert DEXTER_SYSTEM_PROMPT contains all four placeholder tokens; assert it contains at least 4 occurrences of a "DEXTER:" exemplar marker; assert the banned-mode language is present (e.g. the string contains a phrase indicating no fourth-wall / no self-deprecation).
  </action>
  <verify>
    <automated>pytest tests/test_prompts.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from personality.prompts import build_chat_prompt; s=build_chat_prompt('normal','x','y'); assert s and '{max_length}' not in s and '{mood_context}' not in s"` succeeds (no KeyError, all placeholders filled)
    - DEXTER_SYSTEM_PROMPT contains all four tokens: `{max_length}`, `{mood_context}`, `{user_context}`, `{seasonal_context}`
    - DEXTER_SYSTEM_PROMPT contains ≥4 `DEXTER:` exemplar markers and the canonical "impressive commitment to being boring" line
    - MUSIC_RECOMMENDATION_PROMPT, MOOD_CONTEXTS, build_chat_prompt, and build_recommendation_prompt are byte-for-byte unchanged from the original
    - tests/test_prompts.py exits 0 under `pytest tests/test_prompts.py -x`
  </acceptance_criteria>
  <done>DEXTER_SYSTEM_PROMPT is rewritten with few-shot exemplars and banned-mode rules; build_chat_prompt still formats cleanly; only the prompt literal changed; tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 3 config constants (config.py), the new personality/roasts.py template pools, and the rewritten DEXTER_SYSTEM_PROMPT few-shot prompt. Per CONTEXT.md, this phase locks the VOICE STANDARD — the exact wording of every roast line and the system-prompt exemplars is explicitly the user's to review during execution (Claude's Discretion: "written + reviewed by user during execution").
  </what-built>
  <how-to-verify>
    1. Open personality/roasts.py and read every pool (VOICE_JOIN_ROASTS, VOICE_LEAVE_ROASTS, LATE_NIGHT_ROASTS, BOT_MOVED_COMPLAINTS, IDLE_LONELINESS_MESSAGES, STARTUP_MESSAGES, STATUS_LINES, REPEAT_SONG_ROAST_TEMPLATES, MILESTONE_SONG_TEMPLATES, MILESTONE_STREAK_TEMPLATES, NO_LYRICS_FOUND). Confirm the voice matches the locked register in 03-DISCUSSION-LOG.md "Voice samples": arrogant, outward contempt, mild swearing, lowercase, no bot-self-awareness, no pop-psych, no self-deprecation.
    2. Open personality/prompts.py and read the rewritten DEXTER_SYSTEM_PROMPT. Confirm the few-shot exemplars read in the voice you want and the banned modes are stated.
    3. Run `pytest tests/test_roasts.py tests/test_prompts.py -v` and confirm green.
    Edit any wording you want changed directly, or describe the changes you want.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the voice standard, or list the specific lines/exemplars to rewrite.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan produces only static content (config constants, template strings, a prompt string) and pure functions. No new untrusted input crosses any boundary; the system prompt is sent to Gemini exactly as the existing build_chat_prompt path already does. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | DEXTER_SYSTEM_PROMPT format string | mitigate | Double-escape literal braces in exemplars so `.format()` cannot raise/misformat; covered by the build_chat_prompt test asserting no KeyError |
| T-03-02 | Information Disclosure | config.py env reads | accept | DEXTER_CHANNEL_ID/STREAK_TIMEZONE are non-secret config; no secret is read or logged here. No external attack surface beyond existing Discord/Gemini access. |
</threat_model>

<verification>
- `pytest tests/test_roasts.py tests/test_prompts.py -x --tb=short` is green
- `python -c "import config, personality.roasts, personality.prompts"` imports cleanly
- Existing suite unaffected: `pytest tests/ -x --tb=short` stays green
- User has approved the voice register (checkpoint)
</verification>

<success_criteria>
- All Phase 3 config constants exist and import cleanly (PERS-02/03/04/07/08/09 tuning available to downstream plans)
- personality/roasts.py exposes all 11 named pools + is_late_night, all unit-tested
- DEXTER_SYSTEM_PROMPT rewritten with ≥4 few-shot exemplars and the three banned-mode rules; build_chat_prompt still formats without error (D-06)
- Voice standard approved by the user
</success_criteria>

<output>
Create `.planning/phases/03-alive/03-01-SUMMARY.md` when done.
</output>
