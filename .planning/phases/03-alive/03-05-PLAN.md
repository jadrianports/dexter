---
phase: 03-alive
plan: 05
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - cogs/music.py
autonomous: true
requirements: [LYRIC-01, HIST-01, PERS-04, PERS-09]
must_haves:
  truths:
    - "User can run /lyrics for the current song with paginated Genius/AZLyrics results and a personality error when nothing is playing or no lyrics are found"
    - "User can run /history to see recently queued server-wide songs (title/artist/who/when), paginated"
    - "Bot always roasts when a user plays the same song 3+ times in a day"
    - "Bot updates the user's streak and roasts on song-count (100/250/500/1000) and streak-day (7/14/30/60/100) milestone hits"
  artifacts:
    - path: "cogs/music.py"
      provides: "/lyrics + /history slash commands, LyricsPageView + HistoryPageView, repeat-song roast hook, streak+milestone roast hook, chunk_lyrics consumer"
      contains: "name=\"lyrics\""
  key_links:
    - from: "cogs/music.py /lyrics"
      to: "self.bot.lyrics_service.get_lyrics"
      via: "await with defer"
      pattern: "lyrics_service"
    - from: "cogs/music.py /history"
      to: "database.get_history_rows"
      via: "guild-scoped query"
      pattern: "get_history_rows"
    - from: "cogs/music.py _log_track post-queue path"
      to: "database.get_repeat_song_count / update_user_streak"
      via: "threshold + milestone checks"
      pattern: "get_repeat_song_count|update_user_streak"
    - from: "cogs/music.py _log_track repeat-song + milestone roasts"
      to: "self.bot.gemini_service.chat (priority 2)"
      via: "D-08 personalized line, template fallback on GeminiRateLimitError"
      pattern: "gemini_service.*priority"
---

<objective>
Add the user-facing `/lyrics` and `/history` commands plus the in-music roasts (repeat-song and streak/milestone) to `cogs/music.py`. This plan owns `cogs/music.py` exclusively, so all hooks that fire from the post-queue logging path (`MusicCog._log_track`, lines 310-326) live here.

Purpose: `/lyrics` (LYRIC-01) consumes the LyricsService from plan 03-03; `/history` (HIST-01) consumes get_history_rows from plan 03-02; the repeat-song roast (PERS-04) and the streak/song-count milestone roast (PERS-09) consume get_repeat_song_count / update_user_streak from plan 03-02 and the roast pools from plan 03-01. Both PERS-04 and PERS-09 are the two D-08 "special low-frequency moments" that attempt a priority-2 Gemini personalized line (riffing on the user's real tracked data) with the template pool as the guaranteed fallback. All in-music roasts post to the music channel (queue._text_channel_id) per D-11.

Output: extended `cogs/music.py` with two slash commands, two page views, and two roast hooks in `_log_track`.

Depends on plan 03-01 (roasts pools, config), 03-02 (get_history_rows, get_repeat_song_count, update_user_streak, update_user_profile returning the new total, top-artist count helper), 03-03 (LyricsService — wired onto self.bot by plan 03-06; consuming code here references self.bot.lyrics_service which 03-06 sets up in on_ready). Consumes the existing Phase-2 self.bot.gemini_service for the D-08 personalized roast path.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-alive/03-CONTEXT.md
@.planning/phases/03-alive/03-PATTERNS.md
@.planning/phases/03-alive/03-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan adds the following NEW symbols to `cogs/music.py` (exclude from drift verification):
- `class LyricsPageView(discord.ui.View)`, `class HistoryPageView(discord.ui.View)`
- `MusicCog.lyrics` (/lyrics command), `MusicCog.history` (/history command)
- Repeat-song roast + streak/milestone roast calls inside `MusicCog._log_track` (each attempts a priority-2 Gemini line with template fallback per D-08)
- Consumes (created elsewhere this phase): `services.lyrics.chunk_lyrics`/`LyricsService`, `database.get_history_rows`/`get_repeat_song_count`/`update_user_streak`/top-artist count helper, `personality.roasts.NO_LYRICS_FOUND`/`REPEAT_SONG_ROAST_TEMPLATES`/`MILESTONE_SONG_TEMPLATES`/`MILESTONE_STREAK_TEMPLATES`, `config.LYRICS_*`/`HISTORY_*`/`REPEAT_SONG_ROAST_THRESHOLD`/`MILESTONE_*`/`STREAK_TIMEZONE`
- Consumes (existing Phase-2): `self.bot.gemini_service.chat(..., priority=2)` + `GeminiRateLimitError` from `services.gemini`, `personality.prompts` few-shot voice builder
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add LyricsPageView + HistoryPageView and the /lyrics + /history commands</name>
  <files>cogs/music.py</files>
  <read_first>
    - cogs/music.py (read the QueuePageView class lines 71-94 — copy the Previous/Next button + on_timeout structure; the queue_cmd cooldown pattern lines 533-545; the replay defer pattern lines ~590-601; get_queue lines 116-120; get_current/is_playing usage; the imports block lines 1-15)
    - .planning/phases/03-alive/03-PATTERNS.md (cogs/music.py section lines 268-358 — LyricsPageView/HistoryPageView signatures, /lyrics and /history command bodies, defer pattern, imports to add; Shared Patterns lines 579-597 cooldown + defer)
    - .planning/phases/03-alive/03-RESEARCH.md (Pattern 8 chunk_lyrics lines 487-509; Pitfall 8 lines 598-601 QueuePageView is queue-coupled so new views take list inputs; Open Question 3 lines 713-716 on_timeout should edit the message)
    - services/lyrics.py (from plan 03-03 — chunk_lyrics signature, LyricsService.get_lyrics signature; import chunk_lyrics from services.lyrics)
    - database.py (from plan 03-02 — get_history_rows signature returning title/artist/url/duration_seconds/user_id/queued_at)
    - personality/roasts.py (NO_LYRICS_FOUND pool from plan 03-01)
    - config.py (LYRICS_COOLDOWN_SECONDS, LYRICS_PAGE_SIZE, HISTORY_PAGE_SIZE, HISTORY_FETCH_LIMIT from plan 03-01)
    - utils/embeds.py (read the error() and any embed builders used by existing commands, to match the embed style for lyrics/history)
  </read_first>
  <action>
    In cogs/music.py, add imports: `from services.lyrics import chunk_lyrics`, `from database import get_history_rows, get_repeat_song_count, update_user_streak`, `from personality.roasts import pick_random, NO_LYRICS_FOUND, REPEAT_SONG_ROAST_TEMPLATES, MILESTONE_SONG_TEMPLATES, MILESTONE_STREAK_TEMPLATES`.

    Add `class LyricsPageView(discord.ui.View)` after QueuePageView (line 94), following the same Previous/Next button structure but taking `pages: list[str]` + `title: str` (not a MusicQueue) per Pitfall 8. Build the embed from `self.pages[self.page]` with a footer like "page X/Y". Override on_timeout to disable buttons AND edit the stored message (store `self.message` after sending) so the disabled state shows (Open Question 3). Send all messages/edits with allowed_mentions=discord.AllowedMentions.none() (defense-in-depth against mention injection from scraped lyrics — pairs with sanitize_lyrics in plan 03-03).

    Add `class HistoryPageView(discord.ui.View)` taking `rows: list[dict]` + guild for username resolution, HISTORY_PAGE_SIZE rows per page; each line shows title / artist / who-requested (resolve user_id to a display name via guild.get_member, fall back to the id) / when (format queued_at) per D-16.

    Add the `/lyrics` command (cooldown config.LYRICS_COOLDOWN_SECONDS): get the current track from get_queue(interaction.guild.id); if no current track or not is_playing -> respond with embeds.error using a personality "nothing is playing" line, ephemeral. Else `await interaction.response.defer()` (Genius/AZLyrics can take seconds — Shared Pattern defer), call `await self.bot.lyrics_service.get_lyrics(track.title, track.artist)` (guard hasattr(self.bot, "lyrics_service") — degrade to NO_LYRICS_FOUND if the service is absent). If None -> followup with pick_random(NO_LYRICS_FOUND). Else chunk via chunk_lyrics(text, config.LYRICS_PAGE_SIZE), build LyricsPageView, followup.send the first page + view, store the message on the view.

    Add the `/history` command (cooldown 5s): call `await get_history_rows(self.db, guild_id=str(interaction.guild.id), limit=config.HISTORY_FETCH_LIMIT)`; if empty -> embeds.error personality "no history yet" line, ephemeral. Else build HistoryPageView and send the first page + view. Bind LIMIT as int (config.HISTORY_FETCH_LIMIT is already int — matches OBS-05).
  </action>
  <verify>
    <automated>python -c "import cogs.music; print('music import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - cogs/music.py defines class LyricsPageView and class HistoryPageView (both discord.ui.View, taking list inputs not a MusicQueue)
    - A /lyrics app command exists (name="lyrics") that defers, calls self.bot.lyrics_service.get_lyrics, and paginates via chunk_lyrics + LyricsPageView; returns a NO_LYRICS_FOUND personality line when nothing plays or no lyrics found (D-15)
    - A /history app command exists (name="history") that calls get_history_rows guild-scoped and paginates via HistoryPageView showing title/artist/who/when (D-16)
    - LyricsPageView and HistoryPageView sends use allowed_mentions=discord.AllowedMentions.none()
    - LyricsPageView.on_timeout disables buttons and edits the message
    - `python -c "import cogs.music"` succeeds
  </acceptance_criteria>
  <done>/lyrics and /history exist with pagination and personality fallbacks; scraped lyrics cannot ping via allowed_mentions; module imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Hook the repeat-song roast into _log_track (PERS-04, D-08 Gemini path + template fallback)</name>
  <files>cogs/music.py</files>
  <read_first>
    - cogs/music.py (the `_log_track` method lines 310-326: imports log_song/update_artist_count/update_user_profile/increment_daily_stat and the call sequence; line 343 queue._text_channel_id; _get_text_channel helper used at lines 163-172/205)
    - .planning/phases/03-alive/03-PATTERNS.md (cogs/music.py "Repeat-song detection" lines 344-347 — call get_repeat_song_count after log_song; post to queue._text_channel_id, NOT EventsCog; D-11)
    - .planning/phases/03-alive/03-RESEARCH.md (PERS-04 row line 60; Architectural Responsibility Map line 90 — repeat-song roast fires from MusicCog post-queue log path)
    - .planning/phases/03-alive/03-DISCUSSION-LOG.md ("Same song 3+/day" line 45 — register for the roast)
    - database.py (get_repeat_song_count signature from plan 03-02; the top-artist lookup helper — e.g. get_top_artist / get_user_artist_counts — used to feed the Gemini personalization, per D-01 "riff on the user's actual top artist")
    - personality/roasts.py (REPEAT_SONG_ROAST_TEMPLATES from plan 03-01)
    - config.py (REPEAT_SONG_ROAST_THRESHOLD from plan 03-01)
    - services/gemini.py (the existing Phase-2 Gemini service + shared 15 RPM `_RateLimiter`: `chat(prompt, ..., priority=2)` raises GeminiRateLimitError when the priority-2 background tier would wait >10s — the SAME tier the AI auto-queue uses; read the exact call signature + import GeminiRateLimitError here)
    - personality/prompts.py (the few-shot DEXTER voice builder so the Gemini-generated line matches the locked arrogant/outward-only register — D-01/D-06)
  </read_first>
  <action>
    In `_log_track` (cogs/music.py lines 310-326), after the log_song(...) call, add a repeat-song check: `count = await get_repeat_song_count(self.db, guild_id=str(interaction.guild.id), user_id=str(interaction.user.id), title=track.title)`. If `count >= config.REPEAT_SONG_ROAST_THRESHOLD`, always produce and post a roast (D-14 earned roast bypasses any ambient ceiling — there is no ceiling check here).

    Per D-08, this repeat-song callback is one of the two reserved "special low-frequency moments" — attempt a personalized Gemini line FIRST, then fall back to the template (do NOT make Gemini merely optional):
    1. If self.bot has a gemini_service (guard hasattr), attempt a priority-2 Gemini line via `self.bot.gemini_service.chat(prompt, priority=2)` — the same background tier the auto-queue uses (NOT priority 1; never let an earned roast contend with user /ask requests). Build the prompt from the few-shot DEXTER voice builder (personality/prompts.py) and the user's actual tracked data: their top artist (from the plan-03-02 artist-count helper), the repeated title, and count.
    2. If the call raises GeminiRateLimitError (the limiter would wait >10s) or any other exception, fall back to the template: pick a line from REPEAT_SONG_ROAST_TEMPLATES and, if it has placeholders, format with name=interaction.user.display_name / count=count / title=track.title. The template path is the GUARANTEED always-fires implementation so PERS-04 holds with or without Gemini.

    Post the resulting line to the music channel via self._get_text_channel(interaction.guild) (where music is happening, per D-11) with allowed_mentions=discord.AllowedMentions.none(). None-guard the channel. Wrap both the Gemini attempt and the send in try/except so neither a rate-limit, a Gemini failure, nor a post failure ever blocks the queueing flow.
  </action>
  <verify>
    <automated>python -c "import cogs.music; print('repeat-song hook import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `_log_track` calls get_repeat_song_count after log_song
    - When count >= config.REPEAT_SONG_ROAST_THRESHOLD the roast ALWAYS posts (no chance roll, no ambient-ceiling check — D-14)
    - Per D-08: the hook attempts a priority-2 Gemini personalized line (riffing on the user's actual top artist / title / count) and falls back to REPEAT_SONG_ROAST_TEMPLATES on GeminiRateLimitError (>10s wait) or any failure; the template path always fires so PERS-04 holds with or without Gemini
    - The Gemini call uses priority=2 (background tier), never priority 1
    - The roast posts to the music channel via _get_text_channel (D-11), not via EventsCog, with allowed_mentions=none and a None-channel guard
    - The Gemini attempt and the send are wrapped so a rate-limit or failure does not break queueing
    - `python -c "import cogs.music"` succeeds
  </acceptance_criteria>
  <done>Playing the same song 3+ times in a day always triggers a roast posted to the music channel: a priority-2 Gemini line riffing on the user's real top artist when the limiter allows, falling back to MILESTONE/REPEAT templates on rate-limit or failure; the queueing flow is unaffected on any failure.</done>
</task>

<task type="auto">
  <name>Task 3: Hook streak update + song-count/streak-day milestone roasts into _log_track (PERS-09, D-08 Gemini path + template fallback)</name>
  <files>cogs/music.py</files>
  <read_first>
    - cogs/music.py (_log_track lines 310-326 — the update_user_profile call at line 324 is the per-command activity path where streak updates belong; _get_text_channel helper)
    - .planning/phases/03-alive/03-PATTERNS.md (database.py update_user_profile note lines 457 — milestone check on total_songs_queued; cogs/music.py post-queue hook; "Pure Functions" + update_user_streak signature lines 429-438)
    - .planning/phases/03-alive/03-RESEARCH.md (PERS-09 row line 65; Architectural Responsibility Map lines 90-92 — milestone DB update is the hook, cog dispatches post-update; System Architecture lines 191-194 milestone crossing logic; D-21 always-fire-once-per-threshold)
    - .planning/phases/03-alive/03-DISCUSSION-LOG.md ("Milestone (songs)" + "Streak" lines 47-49 — register, including the longest_streak knife-twist D-20)
    - database.py (from plan 03-02 — update_user_streak returns (new_streak, longest_streak, milestone_or_None); whatever update_user_profile returns for the new total_songs_queued count; the top-artist lookup helper used to feed the Gemini personalization per D-01)
    - personality/roasts.py (MILESTONE_SONG_TEMPLATES, MILESTONE_STREAK_TEMPLATES from plan 03-01)
    - config.py (MILESTONE_SONG_THRESHOLDS, MILESTONE_STREAK_THRESHOLDS, STREAK_TIMEZONE from plan 03-01)
    - services/gemini.py (the existing Phase-2 Gemini service + shared 15 RPM `_RateLimiter`: `chat(prompt, ..., priority=2)` raises GeminiRateLimitError when the priority-2 background tier would wait >10s — the SAME tier the AI auto-queue uses; read the exact call signature + import GeminiRateLimitError here)
    - personality/prompts.py (the few-shot DEXTER voice builder so the Gemini-generated milestone line matches the locked arrogant/outward-only register — D-01/D-06)
  </read_first>
  <action>
    In `_log_track`, after update_user_profile (line 324). Each milestone is one of the two D-08 reserved "special low-frequency moments," so each attempts a priority-2 Gemini personalized line FIRST and falls back to its template pool — mirroring Task 2's pattern:

    1. Song-count milestone (PERS-09): determine the user's new total_songs_queued (use the value returned by the plan-03-02 update_user_profile if it returns the count, otherwise SELECT it). If the new total exactly equals a value in config.MILESTONE_SONG_THRESHOLDS (D-21 fires once per threshold via exact-equality): per D-08, attempt a priority-2 Gemini milestone line via `self.bot.gemini_service.chat(prompt, priority=2)` built from the few-shot DEXTER voice builder and the user's real data (their top artist from the plan-03-02 artist-count helper + count=total). On GeminiRateLimitError (>10s wait) or any failure, fall back to a line from MILESTONE_SONG_TEMPLATES formatted with count=total. The template path is the GUARANTEED always-fires implementation so PERS-09 holds with or without Gemini. Post to _get_text_channel(interaction.guild), allowed_mentions=none.

    2. Streak update + streak-day milestone (PERS-09): call `new_streak, longest, streak_milestone = await update_user_streak(self.db, user_id=str(interaction.user.id), tz_name=config.STREAK_TIMEZONE)`. If streak_milestone is not None (current_streak hit a config.MILESTONE_STREAK_THRESHOLDS boundary): per D-08, attempt a priority-2 Gemini streak-milestone line built from the few-shot voice builder and the user's real data — their top artist, days=new_streak, and their `longest_streak` record (D-20 knife-twist). On GeminiRateLimitError (>10s wait) or any failure, fall back to a line from MILESTONE_STREAK_TEMPLATES formatted with days=new_streak / record=longest (still referencing longest_streak per D-20). The template path is the GUARANTEED always-fires implementation. Post to _get_text_channel, allowed_mentions=none.

    Both milestone roasts are earned (D-14) and bypass any ambient ceiling. All Gemini calls use priority=2 (background tier), never priority 1 — never let an earned roast contend with user /ask requests (D-08). Wrap each Gemini attempt and each send in try/except so neither a rate-limit, a Gemini failure, nor a post failure ever blocks queueing. Run streak update once per queued song (the same per-command path as last_active_at) so same-day repeats are a no-op per compute_streak (D-18).
  </action>
  <verify>
    <automated>python -c "import cogs.music; print('streak/milestone hook import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `_log_track` calls update_user_streak with tz_name=config.STREAK_TIMEZONE after update_user_profile
    - A song-count milestone fires when the new total exactly equals a MILESTONE_SONG_THRESHOLDS value (100/250/500/1000), once per threshold (D-21)
    - A streak-day milestone fires when update_user_streak reports a MILESTONE_STREAK_THRESHOLDS crossing (7/14/30/60/100), referencing longest_streak (D-20)
    - Per D-08: each milestone attempts a priority-2 Gemini personalized line (riffing on the user's real top artist; the streak line also riffs on their longest_streak record per D-20) and falls back to MILESTONE_SONG_TEMPLATES / MILESTONE_STREAK_TEMPLATES on GeminiRateLimitError (>10s wait) or any failure; the template path always fires so PERS-09 holds with or without Gemini
    - All Gemini calls use priority=2 (background tier), never priority 1
    - Both roasts post to the music channel with allowed_mentions=none; each Gemini attempt and each send is try/except-guarded
    - `python -c "import cogs.music"` succeeds
  </acceptance_criteria>
  <done>Queuing a song updates the user's streak and fires song-count and streak-day milestone roasts on exact-threshold crossings: a priority-2 Gemini line riffing on the user's real data (top artist, and longest_streak record for streaks) when the limiter allows, falling back to MILESTONE_*_TEMPLATES on rate-limit or failure; queueing is unaffected on any failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped lyrics -> Discord embed | Lyrics text (sanitized by plan 03-03) is rendered in an embed. |
| song title / user id -> DB queries | User-influenced track title and user id flow into parameterized helpers from plan 03-02. |
| user tracked data -> Gemini prompt -> Discord | Repeat-song/milestone roasts feed the user's title + top-artist into a priority-2 Gemini call; the model output is posted back to Discord. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Spoofing / mention injection | /lyrics embed, repeat-song + milestone roasts | mitigate | LyricsPageView/HistoryPageView and all roast sends (template AND Gemini output) pass allowed_mentions=discord.AllowedMentions.none(); lyrics pre-sanitized in plan 03-03 |
| T-03-15 | Tampering (SQL) | /history, repeat-song count, streak update | mitigate | All consume parameterized helpers from plan 03-02; LIMIT bound as int (OBS-05) |
| T-03-16 | DoS | /lyrics outbound fetch | mitigate | Timeouts/size caps live in plan 03-03's LyricsService; /lyrics defers and has a per-user cooldown |
| T-03-17 | (DoS) | per-song streak write | accept | One single-row UPSERT per queued song on the existing activity path; bounded. No external attack surface beyond existing DB/Discord access. |
| T-03-18 | DoS / quota exhaustion | D-08 priority-2 Gemini roast calls (repeat-song + milestones) | mitigate | Reuse the existing Phase-2 shared 15 RPM `_RateLimiter` at priority 2 (reject/template-fallback if wait >10s) — the same internal-API surface already covered by Phase 2; earned roasts use priority 2 only, never contending with user /ask (priority 1). Mention-injection on the model output is handled by allowed_mentions=none under T-03-14. |
</threat_model>

<verification>
- `python -c "import cogs.music"` imports cleanly
- Structural review confirms /lyrics defers before the network call, /history is guild-scoped, repeat-song + milestones always fire at threshold (template path guaranteed), the D-08 Gemini path uses priority=2 with a GeminiRateLimitError/exception fallback to the templates, streak uses config.STREAK_TIMEZONE, and all sends use allowed_mentions=none
- Existing suite stays green: `pytest tests/ -x --tb=short`
- Clean-boot check happens in plan 03-06 once lyrics_service is registered
</verification>

<success_criteria>
- /lyrics returns paginated current-song lyrics or a personality error (LYRIC-01, D-15)
- /history shows server-wide recent songs with title/artist/who/when, paginated (HIST-01, D-16)
- Same song 3+ times/day always roasts to the music channel — priority-2 Gemini personalized line when available, template fallback otherwise (PERS-04, D-08/D-11/D-14)
- Streak updates per song and song-count/streak-day milestones roast on exact crossings — priority-2 Gemini personalized line when available (referencing top artist + longest_streak record), template fallback otherwise (PERS-09, D-08/D-20/D-21)
- No command path can ping via mentions (template or Gemini output); queries are parameterized; Gemini roasts never use priority 1
</success_criteria>

<output>
Create `.planning/phases/03-alive/03-05-SUMMARY.md` when done.
</output>
