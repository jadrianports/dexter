---
phase: 03-alive
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - database.py
  - tests/test_streak.py
  - tests/test_database_phase3.py
autonomous: true
requirements: [PERS-04, PERS-09]
must_haves:
  truths:
    - "compute_streak() implements consecutive +1 / same-day no-op / missed-day reset using the configured timezone"
    - "user_profiles gains current_streak, longest_streak, last_streak_date via an idempotent additive migration"
    - "get_repeat_song_count() returns same-song-today plays for a user in a guild"
    - "update_user_streak() persists streak and reports milestone crossings"
  artifacts:
    - path: "database.py"
      provides: "compute_streak, get_local_date, migrate_add_streak_columns, get_repeat_song_count, update_user_streak, get_history_rows; streak columns in SCHEMA_SQL"
      contains: "def compute_streak"
    - path: "tests/test_streak.py"
      provides: "compute_streak all-cases + get_local_date timezone tests"
    - path: "tests/test_database_phase3.py"
      provides: "migration idempotency, repeat-song COUNT, streak/milestone crossing tests"
  key_links:
    - from: "database.init_db"
      to: "migrate_add_streak_columns"
      via: "call after executescript(SCHEMA_SQL)"
      pattern: "migrate_add_streak_columns"
    - from: "database.compute_streak"
      to: "zoneinfo.ZoneInfo(config.STREAK_TIMEZONE)"
      via: "get_local_date"
      pattern: "ZoneInfo"
---

<objective>
Add streak persistence and the pure streak/repeat-song logic that PERS-09 and PERS-04 depend on. This plan owns `database.py` exclusively: the additive `user_profiles` streak migration (D-19), the pure `compute_streak()` date-math seam (D-17/D-18), the milestone-aware `update_user_streak()`, the `get_repeat_song_count()` helper (PERS-04), and a `get_history_rows()` helper that returns the fields `/history` needs (HIST-01 consumer is plan 03-05).

Purpose: The streak migration gates streak features and the pure logic must be unit-testable in isolation per the testing convention. Built TDD because the seams (compute_streak, repeat-song COUNT, migration) have defined inputs/outputs.

Output: `database.py` streak/repeat-song/history helpers + `tests/test_streak.py` + `tests/test_database_phase3.py`.

This plan reads `config.STREAK_TIMEZONE` and `config.MILESTONE_*` constants defined in plan 03-01 but does NOT write config.py.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-alive/03-CONTEXT.md
@.planning/phases/03-alive/03-PATTERNS.md
@.planning/phases/03-alive/03-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan adds the following NEW symbols to `database.py` (exclude from drift verification):
- Pure functions: `compute_streak(current_streak: int, last_streak_date: str | None, tz_name: str) -> tuple[int, str]`, `get_local_date(tz_name: str) -> date`
- Async helpers: `migrate_add_streak_columns(db)`, `get_repeat_song_count(db, *, guild_id, user_id, title) -> int`, `update_user_streak(db, *, user_id, tz_name) -> tuple[int, int, int | None]`, `get_history_rows(db, *, guild_id, limit=50) -> list[dict]`
- New `user_profiles` columns in SCHEMA_SQL and via migration: `current_streak INTEGER`, `longest_streak INTEGER`, `last_streak_date TEXT`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure streak math — compute_streak + get_local_date + tests (RED→GREEN)</name>
  <files>database.py, tests/test_streak.py</files>
  <behavior>
    - compute_streak(0, None, "America/New_York") == (1, <today-in-tz isoformat>) — first ever activity
    - compute_streak(5, <yesterday-in-tz>, tz) == (6, <today>) — consecutive day increments
    - compute_streak(5, <today-in-tz>, tz) == (5, <today>) — same day is a no-op
    - compute_streak(5, <2-days-ago-in-tz>, tz) == (1, <today>) — a fully missed day resets to 1 (D-18 strict)
    - get_local_date("America/New_York") returns a date object equal to datetime.now(ZoneInfo(...)).date()
  </behavior>
  <read_first>
    - database.py (read in full — note `from datetime import date` at line 5; add `from zoneinfo import ZoneInfo` and import config where needed. Match the module's existing style.)
    - .planning/phases/03-alive/03-RESEARCH.md (Pattern 6, lines 416-459 — exact compute_streak reference implementation and the tz pitfall; Pitfall 3 lines 571-575)
    - .planning/phases/03-alive/03-PATTERNS.md ("Pure Functions to Unit-Test" table lines 657-668 — compute_streak/get_local_date proposed location is database.py)
    - tests/test_database.py (read in full — copy the test style and any fixtures; tests/test_streak.py is new but should follow the same pytest conventions)
  </read_first>
  <action>
    RED: Create tests/test_streak.py first with the behavior cases above. Use a fixed tz_name "America/New_York". For the consecutive/same/missed cases, compute the reference dates dynamically from get_local_date(tz) +/- timedelta so the test is timezone-correct and not date-of-run-dependent. Run pytest — it must fail (functions not yet defined).
    GREEN: Add to database.py: `from zoneinfo import ZoneInfo` import; a pure `get_local_date(tz_name: str) -> date` returning `datetime.now(tz=ZoneInfo(tz_name)).date()` (add `datetime` to the existing datetime import); and a pure `compute_streak(current_streak, last_streak_date, tz_name) -> tuple[int, str]` implementing D-18: last_streak_date None → (1, today_iso); delta == 0 → (current_streak, last_streak_date) no-op; delta == 1 → (current_streak+1, today_iso); delta >= 2 → (1, today_iso). today computed via get_local_date(tz_name).isoformat(); last parsed via date.fromisoformat. These functions take NO db and NO Discord objects (the testable seam). Re-run pytest — must pass.
  </action>
  <verify>
    <automated>pytest tests/test_streak.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - database.py defines pure `compute_streak(current_streak, last_streak_date, tz_name)` and `get_local_date(tz_name)`
    - compute_streak uses ZoneInfo(tz_name) (via get_local_date), NOT raw UTC or naive datetime.now() (D-17, Pitfall 3)
    - All four compute_streak branches (first / consecutive / same-day / missed) pass in tests/test_streak.py
    - tests/test_streak.py exits 0 under `pytest tests/test_streak.py -x`
  </acceptance_criteria>
  <done>Pure streak math exists in database.py, uses the configured timezone, and all four cases are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Streak migration + SCHEMA_SQL columns + repeat-song/streak/history DB helpers + tests</name>
  <files>database.py, tests/test_database_phase3.py</files>
  <behavior>
    - After init_db on a fresh in-memory db, PRAGMA table_info(user_profiles) includes current_streak, longest_streak, last_streak_date
    - migrate_add_streak_columns is idempotent: running it twice does not raise
    - get_repeat_song_count returns the number of song_history rows for (guild_id, user_id, title) dated today; >=3 after three inserts of the same title
    - update_user_streak persists current_streak/longest_streak/last_streak_date and returns a milestone value when current_streak hits a MILESTONE_STREAK_THRESHOLDS boundary, else None
    - get_history_rows returns dicts containing title, artist, user_id, queued_at ordered newest-first
  </behavior>
  <read_first>
    - database.py (read in full — SCHEMA_SQL user_profiles block lines 12-18, init_db lines 63-71, get_recent_songs lines 159-172 for the async-helper style, update_user_profile lines 108-122)
    - .planning/phases/03-alive/03-RESEARCH.md (Pattern 7 lines 461-485, Code Examples "Idempotent streak migration" lines 650-667, Pitfall 4 lines 577-580 — call migration AFTER executescript)
    - .planning/phases/03-alive/03-PATTERNS.md (database.py section lines 362-457 — exact SCHEMA_SQL addition, migrate_add_streak_columns, get_repeat_song_count, update_user_streak, get_history_rows signatures)
    - tests/test_database_hardening.py and tests/test_database_phase2.py (read both — copy the in-memory aiosqlite fixture + pytest-asyncio pattern used for DB tests)
    - config.py (read the MILESTONE_STREAK_THRESHOLDS / REPEAT_SONG_ROAST_THRESHOLD constants added by plan 03-01)
  </read_first>
  <action>
    RED: Create tests/test_database_phase3.py with the behaviors above, using the same in-memory aiosqlite + pytest-asyncio fixture as tests/test_database_hardening.py. Run pytest — must fail.
    GREEN: In database.py:
    1. Add `current_streak INTEGER DEFAULT 0`, `longest_streak INTEGER DEFAULT 0`, `last_streak_date TEXT` to the SCHEMA_SQL `user_profiles` CREATE TABLE (for fresh installs).
    2. Add async `migrate_add_streak_columns(db)` (place before init_db): PRAGMA table_info(user_profiles) → existing column set → ALTER TABLE ADD COLUMN for each of the three only if absent → commit. Idempotent.
    3. In `init_db`, call `await migrate_add_streak_columns(db)` AFTER `await db.executescript(SCHEMA_SQL)` and before the final commit (Pitfall 4).
    4. Add async `get_repeat_song_count(db, *, guild_id, user_id, title) -> int` — SELECT COUNT(*) FROM song_history WHERE guild_id=? AND user_id=? AND title=? AND date(queued_at)=date('now'); return the count (PERS-04).
    5. Add async `update_user_streak(db, *, user_id, tz_name) -> tuple[int, int, int | None]` — read current current_streak/last_streak_date for the user, call compute_streak, compute new longest_streak = max(old_longest, new_streak), UPDATE user_profiles SET current_streak/longest_streak/last_streak_date, commit, and return (new_streak, new_longest, milestone) where milestone is the threshold value from config.MILESTONE_STREAK_THRESHOLDS if new_streak exactly equals one of them and it increased this call, else None (D-21 always-fire-once-per-threshold; relies on exact-equality so no extra bookkeeping needed).
    6. Add async `get_history_rows(db, *, guild_id, limit=50) -> list[dict]` returning title, artist, url, duration_seconds, user_id, queued_at ordered by queued_at DESC, id DESC, LIMIT bound as int (HIST-01 data source for plan 03-05).
    Re-run pytest — must pass.
  </action>
  <verify>
    <automated>pytest tests/test_database_phase3.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - SCHEMA_SQL user_profiles block contains current_streak, longest_streak, last_streak_date
    - init_db calls migrate_add_streak_columns AFTER executescript(SCHEMA_SQL)
    - migrate_add_streak_columns runs twice without raising (idempotent — asserted in test)
    - get_repeat_song_count returns 3 after three same-title inserts dated today (PERS-04 seam)
    - update_user_streak returns a non-None milestone when current_streak crosses to exactly 7 (or another threshold) and None otherwise
    - get_history_rows returns dicts with keys including title, artist, user_id, queued_at
    - tests/test_database_phase3.py exits 0; existing `pytest tests/test_database.py -x` stays green
  </acceptance_criteria>
  <done>Streak columns + migration + repeat-song/streak/history helpers exist in database.py; migration is idempotent and ordered correctly; all phase3 DB tests green; existing DB tests unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| song title → SQL | Song titles (user-influenced via /play) flow into parameterized SQL in get_repeat_song_count. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering (SQL injection) | get_repeat_song_count / get_history_rows | mitigate | All queries use parameterized placeholders (`?`) with bound tuples — never string-interpolated; LIMIT bound as int (matches OBS-05 convention) |
| T-03-04 | Tampering | migrate_add_streak_columns ALTER TABLE | mitigate | Column names are hard-coded literals, not derived from input; PRAGMA-guarded idempotency prevents partial-migration corruption |
| T-03-05 | (DoS) | update_user_streak per-command write | accept | Single-row UPSERT on the existing per-command activity path; no new unbounded growth. No external attack surface beyond existing DB access. |
</threat_model>

<verification>
- `pytest tests/test_streak.py tests/test_database_phase3.py -x --tb=short` is green
- `pytest tests/test_database.py -x` (existing) stays green
- `python -c "import database"` imports cleanly (zoneinfo + config imports resolve)
</verification>

<success_criteria>
- compute_streak implements D-18 strict reset using the configured timezone, fully unit-tested (PERS-09)
- user_profiles streak columns added via idempotent additive migration ordered after schema creation (D-19)
- get_repeat_song_count powers PERS-04; update_user_streak reports milestone crossings for PERS-09
- get_history_rows provides the HIST-01 data source for plan 03-05
</success_criteria>

<output>
Create `.planning/phases/03-alive/03-02-SUMMARY.md` when done.
</output>
