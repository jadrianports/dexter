---
phase: 03-alive
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-03", "03-04", "03-05"]
files_modified:
  - bot.py
autonomous: false
requirements: [PERS-07, PERS-08]
must_haves:
  truths:
    - "Bot rotates its presence every 5 min through a pool (current song / server count / personality line / seasonal)"
    - "Bot posts a startup message after all cogs load, to the resolved Dexter channel"
    - "Bot posts a lonely idle message once after 30+ min of silence while humans are in voice"
    - "LyricsService is wired onto self.bot so /lyrics works at runtime"
    - "The bot boots cleanly with all Phase 3 wiring in place"
  artifacts:
    - path: "bot.py"
      provides: "status_rotation tasks.loop, startup message, idle-loneliness extension of idle_check, LyricsService wiring, Dexter-channel resolver"
      contains: "status_rotation"
  key_links:
    - from: "bot.py on_ready"
      to: "services.lyrics.LyricsService"
      via: "conditional init with GENIUS_TOKEN"
      pattern: "LyricsService"
    - from: "bot.py status_rotation"
      to: "bot.change_presence"
      via: "tasks.loop every STATUS_ROTATION_INTERVAL_SECONDS"
      pattern: "change_presence"
    - from: "bot.py startup message"
      to: "personality.roasts.STARTUP_MESSAGES"
      via: "pick_random after cog load"
      pattern: "STARTUP_MESSAGES"
---

<objective>
Wire the background-task and startup behaviors into `bot.py`. This plan owns `bot.py` exclusively: the `status_rotation` loop (PERS-07), the startup message posted after all cogs load (PERS-08), the lonely-idle extension of the existing `idle_check` loop (PERS-08), and the `LyricsService` runtime wiring (so plan 03-05's `/lyrics` works). It closes Phase 3 with a clean-boot human-verify checkpoint.

Purpose: These behaviors mirror the existing `idle_check` / `cache_cleanup` / `ytdlp_update` loop registration pattern and the existing Gemini conditional-init pattern. They are the last wiring step and depend on the personality pools (03-01), the LyricsService (03-03), and the EventsCog channel-resolver (03-04 — bot.py reuses the same D-09/D-10 fallback for the startup/idle posts).

Output: extended `bot.py` (status rotation, startup, idle-loneliness, lyrics wiring), validated by a clean boot.

This is the final plan; it runs after Wave 2 so the cogs that register the new commands already exist.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-alive/03-CONTEXT.md
@.planning/phases/03-alive/03-PATTERNS.md
@.planning/phases/03-alive/03-RESEARCH.md
@bot.py
</context>

<artifacts_this_phase_produces>
This plan adds the following NEW symbols to `bot.py` (exclude from drift verification):
- `status_rotation` (`@tasks.loop`) + `before_status_rotation` + a `_pick_next_status()` helper
- `_resolve_dexter_channel(guild)` helper (D-09/D-10 fallback for ambient bot.py posts)
- Startup-message post block at the end of `on_ready`
- Idle-loneliness post inside the existing `idle_check` else-branch
- `bot.lyrics_service` wiring in `on_ready`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire LyricsService + status_rotation loop + Dexter-channel resolver into bot.py</name>
  <files>bot.py</files>
  <read_first>
    - bot.py (read the on_ready service-wiring lines 59-106 — the Gemini conditional-init pattern lines 78-84, the cog-load lines 91-97, the background-task start guards lines 99-105; the create_bot activity lines 45-47 using discord.ActivityType.listening; the @tasks.loop definitions lines 180-241 and their before_loop + start-guard pattern)
    - .planning/phases/03-alive/03-PATTERNS.md (bot.py section lines 461-513 — status_rotation body, before_loop, start guard, lyrics service wiring, _resolve_dexter_channel; Shared Patterns lines 562-616)
    - .planning/phases/03-alive/03-RESEARCH.md (Pattern 3 status rotation lines 289-313; Code Examples background-loop registration lines 634-648; Pitfall 5 startup-after-cogs lines 582-585)
    - personality/roasts.py (STATUS_LINES, STARTUP_MESSAGES from plan 03-01)
    - personality/seasonal.py (get_seasonal_context — feeds a seasonal status line)
    - cogs/events.py (the _get_ambient_channel fallback chain from plan 03-04 — mirror the same D-09/D-10 order in _resolve_dexter_channel; do NOT modify cogs/events.py)
    - config.py (DEXTER_CHANNEL_ID, STATUS_ROTATION_INTERVAL_SECONDS from plan 03-01)
  </read_first>
  <action>
    In bot.py on_ready, after the Gemini conditional-init block (line 84) and before the cog-load block, add LyricsService wiring mirroring the Gemini pattern: read `os.getenv("GENIUS_TOKEN")`, `from services.lyrics import LyricsService`, `bot.lyrics_service = LyricsService(genius_token)` and log.info("Lyrics service initialized") (LyricsService itself logs a warning when the token is missing — graceful degradation, so wire it unconditionally).

    Add a module-level `_resolve_dexter_channel(guild)` helper implementing the D-09/D-10 fallback: config.DEXTER_CHANNEL_ID -> the MusicCog queue._text_channel_id for that guild -> guild.system_channel -> first writable text channel. None-safe. (Mirror cogs/events.py._get_ambient_channel order; keep it bot.py-local — small duplication is acceptable to preserve file ownership.)

    Add a module-level `_status_index` accumulator and `_pick_next_status(bot)` that cycles through a pool combining: a current-song line (from any active queue's now-playing track, formatted into a STATUS_LINES `{current_song}` template if present), a server-count line ("{n} servers" template with len(bot.guilds)), a static personality line from personality.roasts.STATUS_LINES, and a seasonal line from get_seasonal_context() when non-empty. Return a plain string.

    Add `@tasks.loop(seconds=config.STATUS_ROTATION_INTERVAL_SECONDS) async def status_rotation()` that calls `await bot.change_presence(activity=discord.Activity(type=discord.ActivityType.listening, name=_pick_next_status(bot)))`, plus `@status_rotation.before_loop async def before_status_rotation(): await bot.wait_until_ready()`. In on_ready's background-task start block (lines 99-105), add `if not status_rotation.is_running(): status_rotation.start()`.
  </action>
  <verify>
    <automated>python -c "import bot; assert hasattr(bot, 'status_rotation'); print('bot wiring import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - bot.py on_ready wires bot.lyrics_service = LyricsService(os.getenv("GENIUS_TOKEN")) (PERS dependency for /lyrics)
    - bot.py defines status_rotation (@tasks.loop), before_status_rotation, _pick_next_status, and starts the loop in on_ready with an is_running guard
    - status_rotation calls bot.change_presence with discord.ActivityType.listening
    - _resolve_dexter_channel implements the D-09/D-10 four-step fallback and is None-safe
    - `python -c "import bot"` succeeds and bot.status_rotation exists (clean import)
  </acceptance_criteria>
  <done>LyricsService is wired onto the bot, status rotation cycles the pool every 5 min, and the Dexter-channel resolver is available; bot.py imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Post startup message after cogs load + add lonely-idle message to idle_check</name>
  <files>bot.py</files>
  <read_first>
    - bot.py (the END of on_ready lines 98-106 — the startup message must be the LAST statement after all load_extension calls and the background-task starts, per Pitfall 5; the idle_check loop lines 180-215, especially the else-branch lines 208-210 where humans ARE present)
    - .planning/phases/03-alive/03-PATTERNS.md (bot.py "Startup message" lines 503-510 and "Idle loneliness" lines 512-513 — extend idle_check else-branch; mirror the vc._idle_seconds accumulator pattern)
    - .planning/phases/03-alive/03-RESEARCH.md (PERS-08 row line 64; Pitfall 5 lines 582-585; System Architecture idle_check extension lines 184-186)
    - .planning/phases/03-alive/03-DISCUSSION-LOG.md ("Idle" line 51, "Startup" line 53 — register; startup is arrogant NOT self-deprecating)
    - personality/roasts.py (STARTUP_MESSAGES, IDLE_LONELINESS_MESSAGES from plan 03-01)
    - config.py (IDLE_LONELINESS_THRESHOLD_SECONDS from plan 03-01)
  </read_first>
  <action>
    Startup message: as the LAST statement in on_ready (after line 106 "Dexter is ready."), iterate the guilds the bot is in (or post to the single configured DEXTER_CHANNEL_ID guild) and post pick_random(personality.roasts.STARTUP_MESSAGES) via _resolve_dexter_channel(guild), allowed_mentions=discord.AllowedMentions.none(). Must run AFTER all load_extension calls so commands are registered first (Pitfall 5). Use the arrogant STARTUP_MESSAGES pool (NOT a self-deprecating line — D-02). Wrap in try/except so a post failure does not abort on_ready.

    Idle loneliness: in idle_check's else-branch (lines 208-210, where human_members > 0), add a separate "silent since last command" accumulator on the vc (e.g. vc._idle_loneliness_seconds, distinct from vc._idle_seconds so it does NOT interfere with the auto-leave timer — Anti-Pattern: do not fight the idle-leave timer). Increment by 60 each tick; when it reaches config.IDLE_LONELINESS_THRESHOLD_SECONDS AND a loneliness message has not already been posted this silence window, post pick_random(IDLE_LONELINESS_MESSAGES) to the music/_resolve_dexter_channel and set a per-vc "posted" flag. Reset both the accumulator and the posted flag whenever a command runs in that guild (hook the reset where queue activity occurs is out of scope for bot.py; instead reset on any new track/play — for this plan, reset the accumulator and flag when human_members transitions or when the now-playing track changes, whichever is observable in idle_check). Keep it to "once per silence window" per PERS-08. allowed_mentions=none on the post.
  </action>
  <verify>
    <automated>python -c "import bot; print('startup+idle import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The startup-message post is the last action in on_ready, after all load_extension calls and the background-task starts (Pitfall 5)
    - The startup post uses STARTUP_MESSAGES (arrogant) and allowed_mentions=none, wrapped in try/except
    - idle_check posts a lonely message once after config.IDLE_LONELINESS_THRESHOLD_SECONDS of silence with humans present, using a SEPARATE accumulator that does not modify vc._idle_seconds (the auto-leave timer is untouched)
    - The loneliness message is gated to once-per-silence-window
    - `python -c "import bot"` succeeds
  </acceptance_criteria>
  <done>Startup message posts after cogs load; idle-loneliness fires once per silence window without disturbing the auto-leave timer; bot.py imports cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 3 is wired: personality prompt + roast pools (03-01), streak DB (03-02), lyrics service (03-03), voice/reaction/seasonal events (03-04), /lyrics + /history + repeat-song + milestone roasts (03-05), and bot.py status rotation + startup + idle-loneliness + lyrics wiring (this plan). Per PROJECT.md, Discord/cog/bot.py wiring is verified by structural review + clean boot, not unit tests.
  </what-built>
  <how-to-verify>
    1. Run the full unit suite: `pytest tests/ -x --tb=short` — confirm green (covers streak math, lyrics helpers, roast pools, seasonal, DB phase3).
    2. Clean-boot check: `python -c "import bot, cogs.music, cogs.events, services.lyrics, personality.roasts, personality.prompts, database, config"` — confirm no import error.
    3. If you have a test Discord server, run the bot (`python bot.py --first-run --guild <id>` to sync, then `python bot.py`) and verify live: the startup message posts to your DEXTER_CHANNEL (or the fallback channel); the status rotates every 5 min; joining voice sometimes triggers a join roast; /lyrics on a playing song returns paginated lyrics (or a personality line if GENIUS_TOKEN is unset and AZLyrics misses); /history shows recent songs; reacting behaviors (eyes on a YouTube link, salute on "gn") fire.
    4. Confirm GENIUS_TOKEN status: if set, /lyrics uses Genius; if unset, the bot logs the warning and /lyrics degrades to AZLyrics-only or the NO_LYRICS_FOUND line — both are acceptable.
  </how-to-verify>
  <resume-signal>Type "approved" if the full suite is green and the bot boots and behaves correctly, or describe any failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GENIUS_TOKEN secret | Read from env in on_ready and passed to LyricsService. |
| roast/status text -> Discord | Template-pool lines posted to a resolved channel / set as presence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Information Disclosure | GENIUS_TOKEN read in on_ready | mitigate | Token passed straight to LyricsService(); never logged in bot.py (log.info says "Lyrics service initialized", not the token value) |
| T-03-19 | Tampering (mention injection) | startup / idle-loneliness posts | mitigate | All channel.send calls pass allowed_mentions=discord.AllowedMentions.none() |
| T-03-20 | (DoS) | status_rotation loop | accept | A single change_presence every 300s; tasks.loop handles exceptions/reconnect. No external attack surface beyond existing Discord access. |
</threat_model>

<verification>
- `pytest tests/ -x --tb=short` is green (full suite)
- `python -c "import bot, cogs.music, cogs.events, services.lyrics, personality.roasts, personality.prompts, database, config"` imports cleanly (clean boot)
- Structural review confirms: startup post is last in on_ready, idle-loneliness uses a separate accumulator from the auto-leave timer, all ambient sends use allowed_mentions=none, GENIUS_TOKEN is never logged
- User has approved the clean boot + live behavior (checkpoint)
</verification>

<success_criteria>
- Status rotates every 5 min through the pool (PERS-07)
- Startup message posts after cogs load; idle-loneliness fires once per silence window without disturbing auto-leave (PERS-08)
- LyricsService is wired so /lyrics works at runtime
- Full unit suite green and bot boots cleanly; user approves
</success_criteria>

<output>
Create `.planning/phases/03-alive/03-06-SUMMARY.md` when done.
</output>
