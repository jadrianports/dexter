---
phase: 03-alive
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - cogs/events.py
  - personality/seasonal.py
  - tests/test_seasonal.py
autonomous: true
requirements: [PERS-02, PERS-03, PERS-05, PERS-06]
must_haves:
  truths:
    - "Bot roasts users on voice join (30%) and leave (30%) within a unified per-user ambient ceiling, and always complains when moved between channels"
    - "Bot delivers late-night (1-5am) roasts at 50% chance, sharing the ambient ceiling"
    - "Join/leave/late-night roasts attempt a priority-2 Gemini-personalized line first (riffing on the member's tracked taste) and fall back to the template pools on rate-limit or any failure (maximize-AI decision 2026-06-11; supersedes D-08 ambient-static for these three triggers)"
    - "Bot reacts to messages: eyes on YT/Spotify links, salute on goodnight, neutral on bare mention, deflecting-warmth text on thanks"
    - "Expanded seasonal awareness adds new date branches beyond the existing 5"
  artifacts:
    - path: "cogs/events.py"
      provides: "on_voice_state_update join/leave/move roasts, reaction listeners, ambient-channel resolver, ambient cooldown"
      contains: "on_voice_state_update"
    - path: "personality/seasonal.py"
      provides: "Expanded get_seasonal_context branches"
      contains: "def get_seasonal_context"
  key_links:
    - from: "cogs/events.py on_voice_state_update"
      to: "personality.roasts pools"
      via: "pick_random(roasts.VOICE_JOIN_ROASTS) etc."
      pattern: "roasts\\.VOICE_(JOIN|LEAVE)_ROASTS"
    - from: "cogs/events.py _get_ambient_channel"
      to: "config.DEXTER_CHANNEL_ID / queue._text_channel_id / guild.system_channel"
      via: "D-09/D-10 fallback chain"
      pattern: "DEXTER_CHANNEL_ID"
    - from: "cogs/events.py _generate_ambient_roast"
      to: "self.bot.gemini_service.chat (priority=2) with template fallback"
      via: "maximize-AI: Gemini-first ambient roast, GeminiRateLimitError/exception -> pick_random fallback"
      pattern: "gemini_service"
---

<objective>
Wire the ambient personality into Discord events. This plan owns `cogs/events.py` (voice join/leave/move roasts, message reactions, ambient cooldown, the D-09/D-10 channel-resolution fallback) and the additive expansion of `personality/seasonal.py` (PERS-06).

Purpose: These are the always-listening behaviors that make Dexter feel present. They consume the template pools and config constants from plan 03-01 (Wave 1) and must coexist with — never fight — the existing music idle-leave timer in bot.py.

Output: extended `cogs/events.py`, expanded `personality/seasonal.py`, extended `tests/test_seasonal.py`.

Depends on plan 03-01 (personality/roasts.py pools, config.py constants). Reads `queue._text_channel_id` from the MusicCog but does not modify cogs/music.py.

**MID-FLIGHT DECISION (2026-06-11 — "maximize AI"):** Voice JOIN, LEAVE, and LATE-NIGHT roasts are generated **Gemini-first** (priority-2, personalized to the member's tracked taste via the few-shot DEXTER voice) and fall back to the `personality/roasts.py` pools only on rate-limit/failure. This **supersedes D-08's** "never let ambient roasts touch the live API" for these three triggers. Channel-move stays template-only (no per-user data to personalize). This plan therefore ALSO consumes the existing Phase-2 `self.bot.gemini_service` + `GeminiRateLimitError` and the user-taste-summary path that `/ask` uses — mirroring plan 03-05's repeat-song/milestone Gemini pattern.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-alive/03-CONTEXT.md
@.planning/phases/03-alive/03-PATTERNS.md
@.planning/phases/03-alive/03-RESEARCH.md
@cogs/events.py
@personality/seasonal.py
</context>

<artifacts_this_phase_produces>
This plan adds the following NEW symbols (exclude from drift verification):
- `EventsCog.on_voice_state_update`, `EventsCog._handle_voice_join`, `_handle_voice_leave`, `_handle_message_reactions`, `_get_ambient_channel`, `_check_ambient_cooldown`, `_mark_ambient_roast`
- `EventsCog.__init__` new attrs: `_ambient_roast_times: dict[int, float]`, `_idle_loneliness_posted: bool`
- Expanded branches inside `personality/seasonal.py::get_seasonal_context`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add on_voice_state_update join/leave/move roasts + ambient cooldown + channel resolver to cogs/events.py</name>
  <files>cogs/events.py</files>
  <read_first>
    - cogs/events.py (read in full — current 33-line stub: __init__ at lines 14-15, on_message at lines 17-29, setup at lines 32-33. Extend, do not replace.)
    - .planning/phases/03-alive/03-PATTERNS.md (cogs/events.py section lines 209-264 — __init__ additions, listener decorator pattern, channel-resolution helper signature, imports to add)
    - .planning/phases/03-alive/03-RESEARCH.md (Pattern 1 lines 225-267 join/leave/move discrimination; Pattern 2 lines 269-287 per-user ambient cooldown; Pitfall 1 lines 559-564 ignore bot's own voice events; Anti-Patterns lines 525-533 do NOT touch music idle timer, None-check queue._text_channel_id)
    - personality/roasts.py (the pools created by plan 03-01: VOICE_JOIN_ROASTS, VOICE_LEAVE_ROASTS, LATE_NIGHT_ROASTS, BOT_MOVED_COMPLAINTS, and is_late_night)
    - cogs/music.py (lines 116-120 get_queue, line 343 queue._text_channel_id assignment — to read the last-active music channel for the fallback chain; do NOT modify this file)
  </read_first>
  <action>
    Extend cogs/events.py. Add imports: `import asyncio`, `import config`, `from personality.roasts import pick_random`, `from personality import roasts`, plus (for the maximize-AI path) the few-shot voice builder from `personality.prompts` and `GeminiRateLimitError` from `services.gemini`. In __init__ add `self._ambient_roast_times: dict[int, float] = {}` and `self._idle_loneliness_posted: bool = False`.

    Add `_check_ambient_cooldown(self, user_id, ceiling_seconds) -> bool` and `_mark_ambient_roast(self, user_id)` using `asyncio.get_event_loop().time()` (RESEARCH Pattern 2). The unified ceiling (config.AMBIENT_ROAST_CEILING_SECONDS) covers join + leave + late-night COMBINED per D-13.

    Add `async def _get_ambient_channel(self, guild) -> discord.TextChannel | None` implementing the D-09/D-10 fallback chain in order: (1) config.DEXTER_CHANNEL_ID via guild.get_channel, (2) the MusicCog queue._text_channel_id (resolve MusicCog via self.bot.cogs.get("MusicCog"), get_queue(guild.id), None-check _text_channel_id per anti-pattern), (3) guild.system_channel, (4) first text channel where guild.me has send permission. Never return a channel the bot cannot post in.

    MAXIMIZE-AI generator (mid-flight decision 2026-06-11 — supersedes D-08 ambient-static for join/leave/late-night). Add `async def _generate_ambient_roast(self, member, scenario: str, fallback_pool: list[str]) -> str`:
    1. Resolve the member's tracked taste for personalization by REUSING the existing Phase-2 user-taste-summary path that `/ask` injects (find it in the AI cog / models.user_profile / database — the SAME summary fed to personality.prompts.build_chat_prompt as user_summary). If unavailable/no data, taste = None.
    2. If `getattr(self.bot, "gemini_service", None)`: attempt a priority-2 Gemini line. Build the roast prompt from the few-shot DEXTER voice (personality.prompts) + the member's taste + `scenario` (e.g. "{name} just joined the voice channel"), MIRRORING how plan 03-05 builds its repeat-song/milestone Gemini prompts — read services/gemini.py for the exact `chat(...)` signature and the priority-2 background tier. Enforce the voice rules on the result (lowercase, <=500 chars). Return it.
    3. On `GeminiRateLimitError` (limiter would wait >10s) OR any other exception, fall back: `line = pick_random(fallback_pool)`, format with `name=member.display_name` if it contains `{name}` (tolerate lines without the placeholder). Return the template line — this path is the GUARANTEED fire so PERS-02/03 hold with or without Gemini.
    4. This helper MUST NOT raise (always return a usable string) and MUST use priority=2 only (never priority 1 — ambient roasts never contend with user `/ask`).

    Add `@commands.Cog.listener() async def on_voice_state_update(self, member, before, after)`. Logic per RESEARCH Pattern 1:
    - Bot-move complaint FIRST: if `member == self.bot.user and before.channel is not None and after.channel is not None and before.channel != after.channel` -> always post pick_random(roasts.BOT_MOVED_COMPLAINTS) to _get_ambient_channel (D-12 always-fires, bypasses ambient ceiling; template-only — it is about the bot, no per-user personalization).
    - Then `if member.bot: return` (ignore all other bot events — Pitfall 1).
    - JOIN (before.channel is None and after.channel is not None): roll config.UNPROMPTED_ROAST_CHANCE; if it passes AND _check_ambient_cooldown -> if is_late_night(current local hour): roll config.LATE_NIGHT_ROAST_CHANCE, set scenario = "it's late night (1-5am) and {name} just joined voice", fallback_pool = roasts.LATE_NIGHT_ROASTS; else scenario = "{name} just joined the voice channel", fallback_pool = roasts.VOICE_JOIN_ROASTS. Post `await self._generate_ambient_roast(member, scenario, fallback_pool)` to _get_ambient_channel; then _mark_ambient_roast.
    - LEAVE (before.channel is not None and after.channel is None): roll config.UNPROMPTED_ROAST_CHANCE AND _check_ambient_cooldown -> post `await self._generate_ambient_roast(member, "{name} just left the voice channel", roasts.VOICE_LEAVE_ROASTS)`, then _mark_ambient_roast.
    - Channel switch (both non-None, different, not bot) is NOT a roast trigger (D-12) — do nothing.
    Send all posts with `allowed_mentions=discord.AllowedMentions.none()` to prevent any accidental ping (covers both Gemini-generated and template text). Do NOT read or reset the music idle timer (Anti-Pattern).
  </action>
  <verify>
    <automated>python -c "import cogs.events; print('events import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - cogs/events.py defines on_voice_state_update, _handle... helpers, _get_ambient_channel, _check_ambient_cooldown, _mark_ambient_roast
    - The bot-move branch checks `member == self.bot.user` and fires before the `if member.bot: return` guard (so the bot's own move is detected but user-join self-events are ignored)
    - Join/leave use config.UNPROMPTED_ROAST_CHANCE and the unified ambient ceiling (config.AMBIENT_ROAST_CEILING_SECONDS); late-night uses is_late_night + config.LATE_NIGHT_ROAST_CHANCE
    - `_generate_ambient_roast` attempts a priority-2 Gemini line (personalized via the member's taste summary + few-shot DEXTER voice) and falls back to pick_random(fallback_pool) on GeminiRateLimitError or any exception; it NEVER raises and uses priority=2 only (never priority 1)
    - The template pools remain referenced as the guaranteed fallback so join (30%) / leave (30%) / late-night (50%) still fire with or without Gemini
    - _get_ambient_channel implements the four-step D-09/D-10 fallback and None-checks queue._text_channel_id
    - All sends pass allowed_mentions=discord.AllowedMentions.none()
    - No code in on_voice_state_update references vc._idle_seconds or the music idle timer
    - `python -c "import cogs.events"` succeeds (clean import)
  </acceptance_criteria>
  <done>cogs/events.py handles join/leave/move roasts under a unified ambient ceiling, resolves the post channel via the D-09/D-10 chain, and never fights the music idle timer; module imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add message reactions to on_message + expand seasonal awareness with tests</name>
  <files>cogs/events.py, personality/seasonal.py, tests/test_seasonal.py</files>
  <read_first>
    - cogs/events.py (the on_message listener lines 17-29 — extend AFTER the existing buffer-feeding block; do not break buffer feeding)
    - .planning/phases/03-alive/03-PATTERNS.md (cogs/events.py reaction note lines 239-244; personality/seasonal.py section lines 185-206 — add elif branches before final return "")
    - .planning/phases/03-alive/03-RESEARCH.md (PERS-05 row line 61 reaction wiring; PERS-06 row line 62 seasonal expansion)
    - .planning/phases/03-alive/03-DISCUSSION-LOG.md ("Thanks -> deflect" line 55, "Seasonal" line 59 — canonical register for the deflect text and seasonal lines)
    - personality/seasonal.py (read in full — the if-month chain lines 15-28; add new branches in the same string-returning format)
    - tests/test_seasonal.py (read in full — extend it; keep existing assertions passing)
    - personality/roasts.py (NO_LYRICS_FOUND and any THANKS/deflect content; if a deflect pool is needed, the deflect line lives in roasts.py from plan 03-01 — otherwise add a small THANKS_DEFLECT pool here is out of scope; use pick_random on an existing pool or inline the canonical deflect line)
  </read_first>
  <action>
    In cogs/events.py on_message, after the existing buffer-feeding block, call a new `await self._handle_message_reactions(message)`. Implement `_handle_message_reactions`:
    - If the message content contains a YouTube or Spotify URL (youtube.com / youtu.be / spotify.com / open.spotify) -> `await message.add_reaction("\N{EYES}")` (the eyes emoji).
    - If the lowercased content is exactly or starts with "goodnight" or "gn" (word-boundary, not substring of another word) -> add the salute emoji reaction.
    - If the bot is mentioned (self.bot.user in message.mentions) AND the message is a BARE mention (no other command/text of substance) -> add the neutral-face emoji reaction.
    - If the message expresses thanks directed at the bot ("thanks"/"thank you"/"ty" with the bot mentioned or in a DM-like reply context) -> send the deflecting-warmth line (canonical "...you're welcome. don't get used to it." register from DISCUSSION-LOG line 55) with allowed_mentions=none. Wrap add_reaction calls in try/except discord.HTTPException so a failed reaction never crashes the handler.

    In personality/seasonal.py, add new elif branches before the final `return ""`, in the same `if month == X (and day == Y):` -> return short-string format. Add at least 3 new dates beyond the existing 5 (Claude's discretion per D-discretion). Suggested: Thanksgiving week (month==11 and day>=22), St. Patrick's (month==3 and day==17), Fourth of July (month==7 and day==4), and a generic summer line (month in (6,7,8)). Keep each return a short sentence fragment matching the existing tone; do not return JSON.

    Extend tests/test_seasonal.py: assert get_seasonal_context returns the expected non-empty string for each NEW date by monkeypatching/freezing datetime.now (follow the existing test's date-injection approach); assert it still returns "" for an ordinary non-seasonal date.
  </action>
  <verify>
    <automated>pytest tests/test_seasonal.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - cogs/events.py on_message still feeds the message buffer AND calls _handle_message_reactions
    - _handle_message_reactions adds the eyes reaction on YT/Spotify links, salute on goodnight/gn, neutral on bare mention, and sends a deflect line on thanks
    - add_reaction calls are wrapped so an HTTPException does not crash on_message
    - personality/seasonal.py get_seasonal_context has at least 3 new date branches beyond the original 5, each returning a non-empty string
    - tests/test_seasonal.py exits 0 under `pytest tests/test_seasonal.py -x` and covers at least one new seasonal date
    - `python -c "import cogs.events, personality.seasonal"` succeeds
  </acceptance_criteria>
  <done>Message reactions and the thanks-deflect are wired into on_message without breaking buffer feeding; seasonal awareness has new dated branches covered by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message content -> reaction/post decision | User message content (links, keywords, mentions) drives reaction and deflect-text behavior. |
| roast text -> Discord channel | Template-pool lines (+ member.display_name) are posted to a resolved channel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering (mention injection) | ambient roast posts containing member.display_name | mitigate | All channel.send calls pass allowed_mentions=discord.AllowedMentions.none(), so a crafted display name containing @everyone cannot ping |
| T-03-12 | DoS | reaction handler on every message | accept | Reactions are O(1) string checks on existing on_message; add_reaction wrapped in try/except so a rate-limit/HTTP error degrades silently. No external attack surface beyond existing Discord access. |
| T-03-13 | (Spoofing) | bot-move self-detection | mitigate | Bot-move branch is gated on `member == self.bot.user`; all other bot voice events are dropped by `if member.bot: return`, preventing self-roasting (Pitfall 1) |
</threat_model>

<verification>
- `pytest tests/test_seasonal.py -x --tb=short` is green
- `python -c "import cogs.events, personality.seasonal"` imports cleanly
- Existing suite stays green: `pytest tests/ -x --tb=short`
- Structural review confirms on_voice_state_update does not touch the music idle timer and all sends use allowed_mentions=none
</verification>

<success_criteria>
- Voice join/leave/move roasts fire per D-12 odds under the unified D-13 ambient ceiling (PERS-02, PERS-03)
- Message reactions and thanks-deflect work without breaking the message buffer (PERS-05)
- Seasonal awareness expanded with new dated branches, unit-tested (PERS-06)
- Ambient posts resolve via the D-09/D-10 channel fallback and never ping via allowed_mentions
</success_criteria>

<output>
Create `.planning/phases/03-alive/03-04-SUMMARY.md` when done.
</output>
