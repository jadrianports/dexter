---
phase: 05-ship-it-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cogs/music.py
  - bot.py
  - cogs/events.py
  - services/queue_persistence.py
  - tests/test_streak.py
autonomous: true
requirements: [DEPLOY-04, DEPLOY-06]
must_haves:
  truths:
    - "clear_persisted() fires on the idle-leave path (bot.py) so a ghost queue cannot resurrect on next boot"
    - "clear_persisted() fires on the reconnect-failure path (cogs/music.py) so a dead session does not persist"
    - "Smart-rejoin guards is_connected() after connect() before calling _play_track (WR-03 defensive fix)"
    - "Reconnect/rejoin path emits diagnostic logs (generation transitions, connection state) at INFO level so a future /gsd:debug session has a trail"
    - "Late-night roast hour is computed in STREAK_TIMEZONE, not naive host-local time"
    - "The _play_generation stale-callback invariant is unchanged (no regression to playback)"
  artifacts:
    - path: "cogs/music.py"
      provides: "clear_persisted on reconnect-failure + reconnect/play diagnostic logging + unchanged _play_track stop order"
      contains: "clear_persisted"
    - path: "bot.py"
      provides: "clear_persisted on idle-leave"
      contains: "clear_persisted"
    - path: "cogs/events.py"
      provides: "TZ-explicit late-night hour via ZoneInfo(STREAK_TIMEZONE)"
      contains: "ZoneInfo"
    - path: "services/queue_persistence.py"
      provides: "is_connected() guard after connect() in smart-rejoin"
      contains: "is_connected"
    - path: "tests/test_streak.py"
      provides: "test_tz_aware_hour_is_integer covering the D-06 TZ pattern"
      contains: "test_tz_aware_hour_is_integer"
  key_links:
    - from: "bot.py idle-leave (~399)"
      to: "bot.queue_persistence.clear_persisted"
      via: "await after queue.clear()"
      pattern: "clear_persisted"
    - from: "cogs/music.py reconnect-failure (~1206)"
      to: "self.bot.queue_persistence.clear_persisted"
      via: "await after queue.clear()"
      pattern: "clear_persisted"
    - from: "services/queue_persistence.py smart-rejoin (~147)"
      to: "music_cog._play_track"
      via: "guarded by vc.is_connected() after connect()"
      pattern: "is_connected"
---

<objective>
Apply the three net-new code fixes that block a clean, safe first boot: the DEPLOY-06 `clear_persisted()` gap closure on the idle-leave and reconnect-failure paths, the DEPLOY-04 defensive reconnect-race hardening plus diagnostic instrumentation, and the D-06 timezone-correctness fix for the late-night roast. Add the one Wave-0 TZ smoke test.

These are the only blockers to the bot booting cleanly on Oracle without a ghost-queue resurrection bug or a wrong-hour personality behavior. Every change is provable from code inspection and cannot regress current behavior (D-02). The reconnect race itself is live-UAT-only by nature — this plan ships the provable invariants + instrumentation so a live `/gsd:debug` session has a trail if it still races (D-04).

Purpose: Resolve IN-02 (DEPLOY-06) and WR-03 (DEPLOY-04) before the bot is deployed, and align wall-clock personality behavior to the community timezone (supports DEPLOY-02 C2 late-night check).
Output: Modified `cogs/music.py`, `bot.py`, `cogs/events.py`, `services/queue_persistence.py`; one new test in `tests/test_streak.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ship-it-live/05-CONTEXT.md
@.planning/phases/05-ship-it-live/05-PATTERNS.md
@.planning/phases/05-ship-it-live/05-RESEARCH.md
@.planning/phases/05-ship-it-live/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close the clear_persisted() gaps (DEPLOY-06 / IN-02)</name>
  <files>bot.py, cogs/music.py</files>
  <read_first>
    - cogs/music.py:974-985 — the AUTHORITATIVE `/stop` template (D-05): `queue._play_generation += 1` → `queue.clear()` → `if hasattr(self.bot, "queue_persistence"): await self.bot.queue_persistence.clear_persisted(...)`. Mirror this exactly.
    - bot.py:390-403 — the idle-leave gap site. NOTE: in the `idle_check` task `bot` is the top-level `commands.Bot` (use `bot`, NOT `self.bot`); `queue_persistence` is set on `bot` in `_initialize_once()` before any task fires; `clear_persisted` is `async` — must be `await`ed.
    - cogs/music.py:1188-1210 — the reconnect-failure gap site inside `on_voice_state_update` (async method — `await` is valid). The bare `queue.clear()` at ~1206 is the gap.
    - 05-PATTERNS.md "clear_persisted template" + "bot.py — clear_persisted gap at line 399" + "cogs/music.py — reconnect path" sections.
    - 05-RESEARCH.md "Focus Area 2: clear_persisted Fix" + "Pitfall 6" (ghost queue on restart).
    - services/queue_persistence.py:64-82 — `clear_persisted` swallows failures by design (persistence issues must never crash playback); do not add try/except around the call.
  </read_first>
  <action>
    Mirror the `/stop` template (cogs/music.py:977-980) at the two gap sites per D-05.

    Site A — bot.py idle-leave (~395-399, verify current line): immediately before the existing `queue.clear()` at ~399, add `queue._play_generation += 1` (invalidate stale after-callbacks, matching the template). After `queue.clear()`, add `if hasattr(bot, "queue_persistence"): await bot.queue_persistence.clear_persisted(guild.id)`. Use `bot` (top-level instance), NOT `self.bot` — this is a module-level task, not a cog method. Keep the existing `vc.stop()` / `await vc.disconnect()` order; insert the generation increment before `vc.stop()` to match the template ordering.

    Site B — cogs/music.py reconnect-failure (~1206, verify current line): the bare `queue.clear()` after the `for attempt in range(3)` loop. Before it add `queue._play_generation += 1`; after it add `if hasattr(self.bot, "queue_persistence"): await self.bot.queue_persistence.clear_persisted(member.guild.id)`. Here use `self.bot` (cog method) and `member.guild.id`.

    Do NOT wrap either call in try/except — `clear_persisted` already swallows its own failures (queue_persistence.py:64-82). Do NOT change the existing `vc.stop()`/`disconnect()` calls beyond inserting the generation increment.
  </action>
  <verify>
    <automated>python -m py_compile bot.py cogs/music.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile bot.py cogs/music.py` exits 0.
    - bot.py: `grep -n "clear_persisted" bot.py` returns a line inside the idle-leave block (after `queue.clear()` at ~399), and a `queue._play_generation += 1` appears before `vc.stop()` in that block.
    - cogs/music.py: `grep -c "clear_persisted" cogs/music.py` returns >= 2 (the existing `/stop` site at 977-980 plus the new reconnect-failure site at ~1206).
    - The reconnect-failure site uses `self.bot.queue_persistence.clear_persisted(member.guild.id)`; the idle-leave site uses `bot.queue_persistence.clear_persisted(guild.id)` (NOT `self.bot` in bot.py).
    - No try/except introduced around either `clear_persisted` call.
    - Live confirmation (NOT run here — captured in the runbook check B2 / DEPLOY-06): bot idle-leaves, restart, `/queue` shows empty.
  </acceptance_criteria>
  <done>Both clear_persisted() gap sites mirror the /stop template; py_compile passes; the ghost-queue-on-restart bug (Pitfall 6) is structurally closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconnect-race defensive guard + diagnostic instrumentation (DEPLOY-04)</name>
  <files>services/queue_persistence.py, cogs/music.py</files>
  <read_first>
    - services/queue_persistence.py:138-153 — the smart-rejoin block (WR-03). `await vc_channel.connect()` immediately followed by `await music_cog._play_track(guild, current)` with no post-connect guard.
    - cogs/music.py:305-375 — `_play_track`: confirm the two existing `is_connected()` guards (top at 311, pre-`play()` at 365) and that `queue._play_generation += 1` (346) happens BEFORE `voice_client.stop()` (363). This invariant must remain unchanged.
    - cogs/music.py:1188-1210 — the reconnect loop where INFO-level diagnostic logs are added.
    - 05-PATTERNS.md "services/queue_persistence.py — smart-rejoin voice race" + "cogs/music.py — _play_track diagnostic logging" + "is_connected guard" shared pattern.
    - 05-RESEARCH.md "Focus Area 1: Reconnect Race" — esp. the log-level table and the "What the race looks like if it still fires live" diagnostic note.
    - CLAUDE.md Implementation Gotchas: "never `voice_client.stop()` before `_play_track()`" — _play_track handles its own stop internally; the fix must NOT change the call order (callers call `_play_track` only, never `stop()` then `_play_track()`).
  </read_first>
  <action>
    Apply the D-02 defensive guard and the D-03 instrumentation. Three edits, all non-regressive.

    Edit 1 — services/queue_persistence.py smart-rejoin (~147): capture the connect result as `vc = await vc_channel.connect()` (currently discarded). Immediately after, add `log.info("smart-rejoin: connected=%s guild=%s", vc.is_connected(), guild_id)`, then a paranoia guard `if not vc.is_connected(): log.warning("Smart rejoin: vc not connected post-connect() guild=%s", guild_id); return` BEFORE calling `await music_cog._play_track(guild, current)`. This mirrors the `is_connected()` guard idiom from cogs/music.py:311/365.

    Edit 2 — cogs/music.py reconnect loop (~1194-1204): add `log.info("reconnect attempt %d/3 in guild %d", attempt + 1, member.guild.id)` at the top of the try (before `await before.channel.connect()`); capture `vc = await before.channel.connect()` and add `log.info("reconnect: vc.is_connected()=%s gen=%d guild=%d", vc.is_connected(), queue._play_generation, member.guild.id)` immediately after. Keep the existing `_play_track` call order unchanged. These are INFO level (low-frequency path).

    Edit 3 — cogs/music.py _play_track (~346-370): add DEBUG-level (NOT INFO — this is the hot per-play path) logs: before `queue._play_generation += 1` add `log.debug("gen=%d → %d in guild %d", queue._play_generation, queue._play_generation + 1, guild.id)`; before `voice_client.stop()` (inside the `is_playing/is_paused` branch) add `log.debug("stopping current playback gen=%d in guild %d", queue._play_generation, guild.id)`; after `voice_client.play(...)` add `log.debug("play() called gen=%d connected=%s guild=%d", current_gen, voice_client.is_connected(), guild.id)`.

    Do NOT add INFO-level logs inside `_play_track` (would flood per-track). Do NOT call `voice_client.stop()` from any new code. Do NOT reorder the existing generation-increment / stop / play sequence.
  </action>
  <verify>
    <automated>python -m py_compile services/queue_persistence.py cogs/music.py && python -m pytest tests/test_queue.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile services/queue_persistence.py cogs/music.py` exits 0.
    - `python -m pytest tests/test_queue.py -q` exits 0 (the _play_generation stale-callback invariant is unregressed — DEPLOY-04 structural proof).
    - `grep -n "is_connected" services/queue_persistence.py` shows the new post-connect guard inside the smart-rejoin try block; `connect()` result is captured as `vc` (not discarded).
    - The reconnect loop in cogs/music.py contains two new `log.info(` calls referencing `attempt` and `vc.is_connected()`.
    - `_play_track` contains three new `log.debug(` calls (gen transition, stopping, play() called) and zero new `log.info` calls.
    - The order `queue._play_generation += 1` (346) → `voice_client.stop()` (363) → `voice_client.play()` (370) is unchanged; no new `voice_client.stop()` call was introduced.
    - Live confirmation (NOT run here — runbook DEPLOY-04 step): trigger a live reconnect; logs show the generation/connection-state trail; escalate to /gsd:debug only if it still races.
  </acceptance_criteria>
  <done>Smart-rejoin guards is_connected() before _play_track; reconnect path logs at INFO and _play_track at DEBUG; test_queue.py green confirms no playback regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Timezone-correct late-night hour + Wave-0 TZ smoke test (D-06)</name>
  <files>cogs/events.py, tests/test_streak.py</files>
  <read_first>
    - cogs/events.py:192-205 — the late-night block. Line ~197 `local_hour = _dt.datetime.now().hour` is naive host-local (the bug). `_dt` alias is already imported at ~196; the result feeds `roasts.is_late_night(local_hour)` at ~205.
    - database.py:6,19-26 — the AUTHORITATIVE ZoneInfo pattern: `from zoneinfo import ZoneInfo` + `datetime.now(tz=ZoneInfo(tz_name))`. tzdata is already in requirements.txt (required for ZoneInfo on Linux).
    - config.py:58 — `STREAK_TIMEZONE = os.getenv("STREAK_TIMEZONE") or "America/New_York"`. Do NOT hardcode the string; use `config.STREAK_TIMEZONE`.
    - tests/test_streak.py:1-45 — the pure-test convention (class-based, no DB/asyncpg, "These tests do NOT use..." docstring). `TZ = "America/New_York"` already defined.
    - 05-PATTERNS.md "cogs/events.py — naive late-night time at line 197" + "Pytest pure-function test class".
    - 05-RESEARCH.md "Focus Area 3: Timezone Correctness" + "Pitfall 2" (naive datetime.now is host-local, diverges from tasks.loop UTC).
    - 05-VALIDATION.md Wave-0 gap: `test_tz_aware_hour_is_integer()`.
  </read_first>
  <action>
    Edit 1 — cogs/events.py (~196-197): replace `local_hour = _dt.datetime.now().hour` with a TZ-aware read. Add `from zoneinfo import ZoneInfo as _ZoneInfo` alongside the existing `import datetime as _dt` (match the local-alias style already in the file), then set `local_hour = _dt.datetime.now(tz=_ZoneInfo(config.STREAK_TIMEZONE)).hour`. Confirm `config` is imported at module top (it is — events.py references `config.UNPROMPTED_ROAST_CHANCE` etc.). Do not touch the `hour = discord.utils.utcnow().hour` line above it (that is a separate UTC read used elsewhere).

    Edit 2 — tests/test_streak.py: add the Wave-0 smoke test as a module-level function (or a small class consistent with the file). Implement `test_tz_aware_hour_is_integer()` that computes `datetime.now(tz=ZoneInfo("America/New_York")).hour` and asserts it is an `int` in `[0, 23]`. Import `datetime` and `ZoneInfo` locally inside the test (matching the existing `test_matches_datetime_now_tz` style). This covers the D-06 pattern correctness.

    Scope note (D-06 Claude's discretion): bot.py:467 yt-dlp loop tzinfo is OUT OF SCOPE for this plan — it is low-stakes and deferred (CONTEXT.md Deferred Ideas / Claude's discretion). The one that matters (events.py:197, a DEPLOY-02 behavioral check) is fixed here. Do NOT modify bot.py:467 in this plan.
  </action>
  <verify>
    <automated>python -m py_compile cogs/events.py && python -m pytest tests/test_streak.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile cogs/events.py` exits 0.
    - `python -m pytest tests/test_streak.py -q` exits 0 AND includes a passing `test_tz_aware_hour_is_integer`.
    - `grep -n "ZoneInfo" cogs/events.py` shows the new import and the `datetime.now(tz=...)` usage; `grep -n "datetime.now().hour" cogs/events.py` returns nothing (naive read removed).
    - events.py uses `config.STREAK_TIMEZONE` (not a hardcoded "America/New_York" literal).
    - bot.py:467 is unchanged (deferred per D-06 discretion).
    - Live confirmation (NOT run here — runbook DEPLOY-02 check C2): late-night roast fires at the correct Eastern hour.
  </acceptance_criteria>
  <done>events.py late-night hour is TZ-explicit via config.STREAK_TIMEZONE; the Wave-0 TZ smoke test passes; bot.py:467 deliberately left as-is.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord gateway → bot voice state events | Untrusted timing of voice connect/disconnect events crosses into the reconnect/rejoin code paths being modified |
| bot process → Postgres (guild_queues) | clear_persisted() issues a DELETE; the only state mutation in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Reconnect-race fix reordering the generation/stop/play sequence | mitigate | Acceptance criteria assert the existing `_play_generation += 1` → `stop()` → `play()` order is unchanged; CLAUDE.md gotcha enforced ("never stop() before _play_track") |
| T-05-02 | Denial of Service | DEBUG logging on the hot `_play_track` path flooding logs | mitigate | New per-play logs are DEBUG level (not INFO); reconnect-path logs are INFO (low frequency) per D-03 log-level rule |
| T-05-03 | Information Disclosure | Diagnostic logs leaking secrets | accept | New logs emit only guild ids, generation counters, and boolean connection state — no tokens, no PII; consistent with existing log calls in the same methods |
| T-05-04 | Denial of Service | clear_persisted() failure crashing the idle-leave / reconnect path | mitigate | clear_persisted swallows its own exceptions by design (queue_persistence.py:64-82); no try/except added; persistence failure cannot crash playback |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to verify |
</threat_model>

<verification>
- `python -m py_compile bot.py cogs/music.py cogs/events.py services/queue_persistence.py` exits 0.
- `python -m pytest tests/test_queue.py tests/test_streak.py -q` exits 0 (invariant + new TZ smoke test).
- Full pure-suite sanity: `python -m pytest tests/ -q --ignore=tests/test_database_phase4.py` stays green (no regression).
- Source assertions per task acceptance_criteria (clear_persisted count >= 2, is_connected guard present, naive datetime.now removed).
- All live behaviors (DEPLOY-04 reconnect under concurrency, DEPLOY-06 idle-leave clear) are deferred to the runbook (Plan 03) and the user's /gsd-verify-work step — NOT executed on the dev machine.
</verification>

<success_criteria>
- DEPLOY-06: clear_persisted() present and mirroring the /stop template at both the idle-leave (bot.py) and reconnect-failure (cogs/music.py) sites; py_compile passes.
- DEPLOY-04: is_connected() guard added after connect() in smart-rejoin; reconnect/rejoin diagnostic logging in place; test_queue.py confirms the _play_generation invariant is unregressed.
- D-06: events.py:197 late-night hour computed via ZoneInfo(config.STREAK_TIMEZONE); Wave-0 TZ smoke test passes.
- No regression in the pure-unit suite.
</success_criteria>

<output>
Create `.planning/phases/05-ship-it-live/05-01-SUMMARY.md` when done.
</output>
