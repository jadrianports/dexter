---
phase: 05-ship-it-live
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - .planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md
  - .planning/phases/04-scale/04-HUMAN-UAT.md
  - .planning/phases/04-scale/04-VERIFICATION.md
  - .planning/phases/03-alive/03-VERIFICATION.md
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-05, DEPLOY-08]
user_setup:
  - service: Healthchecks.io
    why: "Dead-man switch monitoring for the keepalive cron (DEPLOY-08)"
    env_vars:
      - name: HEALTHCHECK_URL
        source: "Healthchecks.io → create check 'Dexter Keepalive' → copy ping URL https://hc-ping.com/<uuid>"
    dashboard_config:
      - task: "Add Discord webhook integration (error-log channel) + email integration to the check"
        location: "Healthchecks.io → Integrations → Discord (webhook URL) and Email"
  - service: Oracle A1 VM
    why: "All live UAT execution (Docker boot, crons, Discord behavior, restore round-trip)"
    dashboard_config:
      - task: "Set VM timezone (timedatectl set-timezone America/New_York), enable docker (systemctl enable docker), create .env/.pgpass/.oci/config, install crontab entries"
        location: "Oracle A1 VM via SSH — per runbook prerequisites checklist"
must_haves:
  truths:
    - "One ordered master runbook consolidates all 21 standing checks in the locked order: A boot/infra → B queue persistence → C behavioral/Discord → D destructive/backup-restore LAST"
    - "Every check has a command/action, an expected output, and a pass/fail capture field"
    - "The runbook carries the prominent 'never docker compose down -v in production' warning and the D-11 troubleshooting table"
    - "The runbook has a prerequisites checklist (VM tz, docker enabled — D-09 systemctl is-enabled docker for reboot survival, .env, ~/.pgpass, ~/.oci/config, lifecycle policy applied, crontab, per-guild command sync — D-08 --first-run --guild / owner /sync)"
    - "Dead-man alert routing is documented (D-12): Healthchecks.io alerts → Discord webhook into the error-log channel + email backup"
    - "The three source verification docs are updated by reference to point at the consolidated runbook (not maintained in parallel)"
  artifacts:
    - path: ".planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md"
      provides: "Consolidated ordered live-UAT runbook (21 checks + prereqs + troubleshooting + warnings)"
      min_lines: 120
      contains: "down -v"
  key_links:
    - from: "05-UAT-RUNBOOK.md group A1 (boot)"
      to: "scripts/deploy.sh + docker-compose.yml"
      via: "deploy/boot commands reference the Plan-02 deploy.sh and the compose stack"
      pattern: "docker compose"
    - from: "05-UAT-RUNBOOK.md group D1 (restore)"
      to: "scripts/seed_restore_test.py"
      via: "the destructive check runs the Plan-02 restore-verify script"
      pattern: "seed_restore_test"
    - from: "04-HUMAN-UAT.md / 04-VERIFICATION.md / 03-VERIFICATION.md"
      to: "05-UAT-RUNBOOK.md"
      via: "by-reference pointer (D-07)"
      pattern: "05-UAT-RUNBOOK"
---

<objective>
Produce the single consolidated, ordered live-UAT runbook (D-07) that carries all 21 standing checks plus every deploy decision, and update the three source verification docs to point at it by reference. This is the validation deliverable for the phase: the phase is verified when this checklist passes live on Oracle — not when the code lands (D-01).

The runbook is a DOCUMENT Claude authors and structurally verifies on the dev machine. Claude does NOT execute any live step — every check is written as a user-executed instruction (command/action + expected output + pass/fail capture). The live run happens in the user's `/gsd-verify-work` session on Oracle.

Purpose: Give the user one ordered SSH-session checklist covering DEPLOY-01/02/03/05/07/08, with prerequisites, troubleshooting, and the down -v landmine warning, so the live validation is turnkey.
Output: `.planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md` (new) + by-reference updates to the three source docs.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ship-it-live/05-CONTEXT.md
@.planning/phases/05-ship-it-live/05-RESEARCH.md
@.planning/phases/05-ship-it-live/05-PATTERNS.md
@.planning/phases/05-ship-it-live/05-01-PLAN.md
@.planning/phases/05-ship-it-live/05-02-PLAN.md
@.planning/phases/04-scale/04-HUMAN-UAT.md
@.planning/phases/04-scale/04-VERIFICATION.md
@.planning/phases/03-alive/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the consolidated ordered live-UAT runbook (D-07)</name>
  <files>.planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md</files>
  <read_first>
    - 05-RESEARCH.md "Focus Area 7: Live-UAT Runbook Consolidation" — the AUTHORITATIVE enumeration of all 21 checks in the consolidated A/B/C/D groups, each with source / command-action / expected. Use this table verbatim as the runbook body content.
    - 05-RESEARCH.md "Focus Area 6: Deploy Mechanics" — the first-deploy sequence (cp .env / chmod 600 / ~/.pgpass / oci setup config / docker compose up -d / --first-run --guild / crontab) and the D-11 troubleshooting table (9 rows). Both go into the runbook prerequisites + troubleshooting sections.
    - 05-RESEARCH.md "Focus Area 5: Dead-Man Alert Routing" — the Healthchecks.io check creation + Discord webhook + email integration steps (DEPLOY-08, group A4).
    - 05-RESEARCH.md "Focus Area 4" — the restore round-trip step (group D1) wraps the Plan-02 `scripts/seed_restore_test.py`; reference that script, do not duplicate its internals.
    - 05-PATTERNS.md "Live-UAT Runbook (new markdown file)" — the frontmatter pattern (status/phase/source/started/updated), the per-check format (`### N. Name` / `expected:` / `result: [pending]`), the required sections (red-box warning, troubleshooting table, prereqs checklist), and the suggested path `.planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md`.
    - .planning/phases/04-scale/04-HUMAN-UAT.md (frontmatter + check format) and 04-VERIFICATION.md / 03-VERIFICATION.md (the source checks being consolidated) — for exact wording fidelity.
    - 05-01-PLAN.md and 05-02-PLAN.md (in context) — so the runbook references the ACTUAL fix sites and script names produced in Wave 1 (clear_persisted B2 check, reconnect DEPLOY-04 log-trail check, deploy.sh, seed_restore_test.py).
  </read_first>
  <action>
    Create `.planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md` with frontmatter (`status: partial`, `phase: 05-ship-it-live`, `source: [05-CONTEXT.md, 05-RESEARCH.md, 04-VERIFICATION.md, 03-VERIFICATION.md, 04-HUMAN-UAT.md]`, `started: [fill on execution]`, `updated: [fill on execution]`).

    Top of document (before any check): a prominent red-box-style WARNING block stating NEVER run `docker compose down -v` in production (wipes postgres_data / audio_cache / logs volumes; use `docker compose restart bot` or `docker compose up -d --build bot`).

    Prerequisites checklist section (D-10/D-11/D-13/D-14 setup, all user-executed on the VM): set VM timezone `timedatectl set-timezone America/New_York`; `systemctl is-enabled docker` (enable if not); `cp .env.example .env` + fill + `chmod 600`; create `~/.pgpass` (chmod 600); `oci setup config`; apply the lifecycle policy `oci os object-lifecycle-policy put --items file://scripts/lifecycle-policy.json`; install crontab entries (`*/5` keepalive + `0 */6` backup with `HEALTHCHECK_URL` in the env block); run command sync `python bot.py --first-run --guild <GUILD_ID>`; create the Healthchecks.io check + Discord webhook + email integrations.

    Checks section in the LOCKED order (D-07): A → B → C → D.
    - Group A: Boot + Infra (6 checks A1–A6) — Docker clean boot, reboot survival (systemctl is-enabled docker + sudo reboot), over-cap rejection, keepalive cron (Healthchecks.io green), backup cron (manual `bash scripts/backup.sh`), Postgres integration tests (`pytest tests/test_database_phase4.py`). A1's boot step references `scripts/deploy.sh` for the ongoing-update path.
    - Group B: Queue persistence (2 checks B1–B2) — round-trip across `docker compose restart bot`; B2 = clear_persisted on idle-leave (DEPLOY-06): idle 10 min alone → bot leaves → restart → `/queue` empty.
    - Group C: Behavioral Discord (9 checks C1–C9) — voice join roast, late-night roast (notes the events.py TZ fix from Plan 01), startup message, status rotation, /lyrics, /history, message reactions, repeat-song roast, idle loneliness. Add a DEPLOY-04 check in this group: trigger a live voice reconnect, inspect the new diagnostic log trail (generation/connection-state), and note escalation to /gsd:debug only if it still races.
    - Group D: Destructive (1 check D1, ALWAYS LAST) — `python scripts/seed_restore_test.py` (the Plan-02 script): seed → backup → restore into `dexter_restore_test` → assert row counts → drop throwaway. Restate that this never touches the live DB.

    Each check uses the format: `### <ID>. <Name>` then `expected: <command/action> → <observable outcome>` then `result: [pending]`.

    Troubleshooting table section: the D-11 9-row table from RESEARCH Focus Area 6 (arm64 manifest fail, container exits, healthcheck never healthy, pool-acquire timeout, volume perms, commands not appearing, invalid token, pg_dump auth, oci NotAuthenticated → fixes).

    Use lowercase prose where it is bot-facing copy, but the runbook itself is operator documentation — clarity over personality. Do NOT include fenced implementation code beyond the literal shell commands a user types.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md && grep -c "result:" .planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - The file `.planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md` exists.
    - `grep -c "result:" 05-UAT-RUNBOOK.md` returns >= 21 (one capture field per check) — confirms all 21 checks present.
    - The checks appear in A→B→C→D order: the first A-group heading precedes the first B heading, which precedes the first C heading, which precedes the D heading (destructive restore is LAST).
    - `grep -c "down -v" 05-UAT-RUNBOOK.md` returns >= 1 (the warning) and the warning appears before the first check.
    - The runbook contains a prerequisites checklist mentioning `timedatectl`, `~/.pgpass`, `lifecycle-policy.json`, `--first-run --guild`, and `HEALTHCHECK_URL`.
    - The runbook contains the troubleshooting table (>= 8 rows; grep for `arm64`, `pool-acquire`, `pg_dump`).
    - Group D references `scripts/seed_restore_test.py`; group A references `scripts/deploy.sh` and `docker compose`; the DEPLOY-04 behavioral check references the diagnostic log trail / `/gsd:debug` escalation.
    - No live step is written as Claude-executed (all are user actions with `result: [pending]`).
  </acceptance_criteria>
  <done>05-UAT-RUNBOOK.md exists with all 21 checks in the locked A→B→C→D order, the down -v warning, prerequisites checklist, and troubleshooting table; every check has a pending capture field.</done>
</task>

<task type="auto">
  <name>Task 2: Update the three source verification docs by reference (D-07)</name>
  <files>.planning/phases/04-scale/04-HUMAN-UAT.md, .planning/phases/04-scale/04-VERIFICATION.md, .planning/phases/03-alive/03-VERIFICATION.md</files>
  <read_first>
    - 05-CONTEXT.md D-07 — "The 3 source docs (03-VERIFICATION, 04-VERIFICATION, 04-HUMAN-UAT) are updated BY REFERENCE, not maintained in parallel." This is the exact contract for this task.
    - .planning/phases/04-scale/04-HUMAN-UAT.md (full) — its frontmatter + check list; the consolidation target.
    - .planning/phases/04-scale/04-VERIFICATION.md — the 6 Phase-4 deploy checks + IN-02 / WR-03 notes; do not delete the historical content, just add the pointer.
    - .planning/phases/03-alive/03-VERIFICATION.md — the 9 Phase-3 behavioral checks; same treatment.
  </read_first>
  <action>
    In each of the three source docs, add a short by-reference banner near the top (just under the frontmatter / first heading) stating that these live checks have been consolidated into the Phase 5 master runbook and that live execution + result capture now happens there: point to `.planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md`. Do NOT delete or rewrite the existing check bodies (they remain the historical source of truth for wording) and do NOT change their `result:` / status fields — the runbook is the live-execution surface, these stay as the provenance record. Keep each banner to 1–3 lines. Do not touch any other content in these files.
  </action>
  <verify>
    <automated>grep -l "05-UAT-RUNBOOK" .planning/phases/04-scale/04-HUMAN-UAT.md .planning/phases/04-scale/04-VERIFICATION.md .planning/phases/03-alive/03-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - All three files contain a reference to `05-UAT-RUNBOOK` (the grep returns all three paths).
    - The reference banner appears near the top of each file (within the first ~15 lines after frontmatter), not buried at the end.
    - The original check bodies and their `result:`/status fields are unchanged (the by-reference edit is additive only — confirm by diff that only a banner was inserted).
    - No check content was deleted from any of the three files.
  </acceptance_criteria>
  <done>Each of the three source verification docs carries a by-reference banner pointing at 05-UAT-RUNBOOK.md; their original check content is preserved unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (user) → Oracle VM | The runbook drives privileged host operations; its correctness/ordering is the safety control |
| destructive restore step → live DB | The runbook's D-group ordering + warnings prevent a bad restore from corrupting earlier UAT state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Denial of Service | Operator runs `docker compose down -v` following the runbook | mitigate | Prominent down -v warning at the top of the runbook before any check; deploy.sh (Plan 02) also warns |
| T-05-11 | Tampering | Destructive restore check corrupting earlier UAT state | mitigate | D-07 locked ordering places the destructive backup/restore check LAST (group D); restore targets the throwaway DB only (Plan 02) |
| T-05-12 | Information Disclosure | Runbook embedding real secrets (tokens, ping UUID, pg password) | mitigate | Runbook uses placeholders (`<GUILD_ID>`, `<your-uuid>`, `YOUR_POSTGRES_PASSWORD`); no real secret literals; chmod 600 instructed for .env/.pgpass |
| T-05-13 | Spoofing | Owner `/sync` runnable by non-owners | accept | Existing `interaction.user.id != bot.owner_id` guard (bot.py:356) already in place; runbook documents owner-only usage; no code change needed |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; documentation only |
</threat_model>

<verification>
- `05-UAT-RUNBOOK.md` exists; `grep -c "result:"` >= 21; A→B→C→D ordering holds; down -v warning + prereqs + troubleshooting present.
- All three source docs reference `05-UAT-RUNBOOK` with original content preserved.
- All live checks in the runbook are user-executed instructions with `result: [pending]` — none authored as Claude-run.
- The actual live execution of the 21 checks is the user's /gsd-verify-work step on Oracle — NOT part of this plan's dev-machine verification.
</verification>

<success_criteria>
- DEPLOY-02/03/05: the 9 Phase-3 behavioral + 6 Phase-4 deploy + queue-persistence checks are consolidated, ordered, and capture-ready in one runbook.
- DEPLOY-01: boot + reboot-survival checks (A1, A2) reference deploy.sh and systemctl/restart-policy.
- DEPLOY-08: keepalive/Healthchecks.io check (A4) plus the Discord-webhook + email alert setup are documented in the prereqs.
- DEPLOY-07 round-trip (D1) references the Plan-02 restore script and is ordered LAST.
- The three source docs are updated by reference, not in parallel (D-07).
</success_criteria>

<output>
Create `.planning/phases/05-ship-it-live/05-03-SUMMARY.md` when done.
</output>
