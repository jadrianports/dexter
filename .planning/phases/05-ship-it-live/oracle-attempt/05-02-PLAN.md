---
phase: 05-ship-it-live
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy.sh
  - scripts/backup.sh
  - scripts/lifecycle-policy.json
  - scripts/seed_restore_test.py
  - tests/test_seed_restore.py
autonomous: true
requirements: [DEPLOY-01, DEPLOY-07]
user_setup:
  - service: Oracle Cloud Infrastructure (OCI Object Storage)
    why: "Backup lifecycle policy + dump upload/download target (bucket dexter-backups)"
    env_vars:
      - name: HEALTHCHECK_URL
        source: "Healthchecks.io check ping URL — set in the Oracle VM crontab environment (used by deploy.sh success ping)"
    dashboard_config:
      - task: "Apply the 14-day lifecycle policy to the dexter-backups bucket via oci-cli (runbook prereq step, user-executed on the VM)"
        location: "Oracle VM: oci os object-lifecycle-policy put --items file://scripts/lifecycle-policy.json"
must_haves:
  truths:
    - "deploy.sh performs git pull → docker compose up -d --build bot → tail logs → healthcheck ping, and never runs docker compose down -v"
    - "backup.sh documents the 6-hour cadence (0 */6 * * *) instead of */30"
    - "lifecycle-policy.json encodes a 14-day DELETE rule scoped to the dexter_ prefix"
    - "seed_restore_test.py seeds known rows, backs up, restores into a throwaway dexter_restore_test DB via docker compose exec, verifies row counts, and drops the throwaway DB — never touching the live DB during restore"
    - "test_seed_restore.py asserts the pure seed-row shape with no DB connection"
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "D-13 manual git-pull + rebuild update workflow with down -v warning"
      contains: "up -d --build bot"
    - path: "scripts/backup.sh"
      provides: "6-hour cadence comment (cron expression 0 */6 * * *)"
      contains: "0 */6 * * *"
    - path: "scripts/lifecycle-policy.json"
      provides: "OCI 14-day auto-delete lifecycle rule for dexter_ prefix"
      contains: "timeAmount"
    - path: "scripts/seed_restore_test.py"
      provides: "D-15 non-destructive seed → backup → restore-verify → teardown"
      contains: "dexter_restore_test"
    - path: "tests/test_seed_restore.py"
      provides: "Wave-0 pure tests for seed-row data shape"
      min_lines: 25
  key_links:
    - from: "scripts/seed_restore_test.py"
      to: "scripts/backup.sh"
      via: "subprocess invocation"
      pattern: "backup.sh"
    - from: "scripts/seed_restore_test.py"
      to: "postgres container"
      via: "docker compose exec postgres pg_restore --no-owner --no-acl into dexter_restore_test"
      pattern: "pg_restore"
    - from: "tests/test_seed_restore.py"
      to: "scripts/seed_restore_test.py"
      via: "imports the pure seed-row builder"
      pattern: "import"
---

<objective>
Write the helper scripts the user needs for one ready-to-run SSH session: the D-13 `deploy.sh` update workflow, the D-14 backup-cadence change + OCI lifecycle policy, and the D-15 non-destructive seed → backup → restore-verify script. Add the Wave-0 pure test that validates the seed-row data shape with no DB.

Claude can write and structurally verify all of these on the dev machine (shell syntax-check, py_compile, pure pytest); none of them are EXECUTED here — the user runs them live on Oracle per the labor asymmetry (D-01). The restore-verify script is explicitly non-destructive: it restores into a throwaway `dexter_restore_test` DB inside the Postgres container (avoiding the pg_restore version-mismatch landmine) and never touches the live DB during restore.

Purpose: Make the DEPLOY-01 (deploy workflow) and DEPLOY-07 (backup/restore round-trip) live steps turnkey for the user.
Output: `scripts/deploy.sh`, edited `scripts/backup.sh`, `scripts/lifecycle-policy.json`, `scripts/seed_restore_test.py`, `tests/test_seed_restore.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ship-it-live/05-CONTEXT.md
@.planning/phases/05-ship-it-live/05-PATTERNS.md
@.planning/phases/05-ship-it-live/05-RESEARCH.md
@scripts/backup.sh
@scripts/keepalive.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: deploy.sh + backup.sh cadence + lifecycle-policy.json (D-13 / D-14)</name>
  <files>scripts/deploy.sh, scripts/backup.sh, scripts/lifecycle-policy.json</files>
  <read_first>
    - scripts/backup.sh:1-20 — the existing header/cadence comment (line ~12 documents `*/30 * * * *`); the shell conventions (`set -euo pipefail`, header block with PURPOSE/DEPLOYMENT/PREREQUISITES), and the SECURITY no-hardcoded-secrets note. This is the analog for deploy.sh.
    - scripts/keepalive.sh — the `curl -fsS --max-time 10 "${HEALTHCHECK_URL}" ... || true` non-fatal healthcheck ping idiom (line ~31) and `set -uo pipefail` style.
    - 05-PATTERNS.md "scripts/deploy.sh (new file)" + "scripts/backup.sh — cadence change only" + "Shell script conventions" shared pattern.
    - 05-RESEARCH.md "Focus Area 6: Deploy Mechanics" (the deploy.sh reference script + the `--build bot` rebuilds-only-bot constraint) and "OCI Object Storage lifecycle policy" (the lifecycle JSON structure + apply command) and "Focus Area 4" cadence-change note.
    - docker-compose.yml — confirm `postgres:16-alpine` is pinned and the three named volumes (postgres_data, audio_cache, logs) so the "never down -v" warning names the right stakes.
  </read_first>
  <action>
    deploy.sh (new): follow the backup.sh header convention — `#!/bin/bash`, header block `# scripts/deploy.sh — update Dexter on Oracle (D-13)` with PURPOSE/DEPLOYMENT/ENVIRONMENT/SECURITY sections, then `set -euo pipefail`. Body: `cd` into `REPO_DIR="/opt/dexter"`; `git pull`; `docker compose up -d --build bot` (--build bot ONLY — never `--build` alone, so Postgres is never rebuilt); tail bot logs briefly (`docker compose logs -f bot --tail=50 &` then `sleep 15` then kill the tail PID with `|| true`); if `HEALTHCHECK_URL` is set, ping it with `curl -fsS --max-time 10 "${HEALTHCHECK_URL}" > /dev/null 2>&1 || true` (the keepalive.sh idiom). End with a loud multi-line `echo` WARNING that NEVER run `docker compose down -v` in production (it wipes postgres_data, audio_cache, logs volumes). No secrets hardcoded.

    backup.sh (edit only): change the cadence comment at line ~12 from `*/30 * * * *` to `0 */6 * * *` (every 6 hours, D-14). Update any surrounding comment prose that says "every 30 minutes" to "every 6 hours". Make NO functional code change to the pg_dump/upload pipeline.

    lifecycle-policy.json (new): the OCI Object Storage lifecycle rule array per RESEARCH Focus Area 4 — a single rule `name: "delete-old-backups"`, `action: "DELETE"`, `isEnabled: true`, `timeAmount: 14`, `timeUnit: "DAYS"`, `objectNameFilter.inclusionPrefixes: ["dexter_"]`. This is applied by the user via `oci os object-lifecycle-policy put --items file://scripts/lifecycle-policy.json` (runbook prereq) — Claude only writes the file.
  </action>
  <verify>
    <automated>bash -n scripts/deploy.sh && bash -n scripts/backup.sh && python -c "import json; json.load(open('scripts/lifecycle-policy.json'))"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/deploy.sh` and `bash -n scripts/backup.sh` both exit 0 (syntax check).
    - `python -c "import json; json.load(open('scripts/lifecycle-policy.json'))"` exits 0 (valid JSON).
    - deploy.sh contains `up -d --build bot` (literal "bot" present — not a bare `--build`), contains a `down -v` warning string, and starts with `set -euo pipefail`.
    - deploy.sh pings `${HEALTHCHECK_URL}` with the `|| true` non-fatal idiom and contains no hardcoded token/password literals.
    - backup.sh: `grep -c "0 \*/6 \* \* \*" scripts/backup.sh` returns >= 1 and `grep -c "\*/30" scripts/backup.sh` returns 0 (cadence updated, old removed).
    - lifecycle-policy.json contains `"timeAmount": 14`, `"timeUnit": "DAYS"`, and `"inclusionPrefixes": ["dexter_"]`.
    - Live confirmation (NOT run here — runbook DEPLOY-07 prereq + DEPLOY-01 deploy step): user applies the policy and runs deploy.sh on Oracle.
  </acceptance_criteria>
  <done>deploy.sh implements the D-13 workflow with the down -v guard; backup.sh documents the 6h cadence; lifecycle-policy.json is valid OCI lifecycle JSON; all syntax-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: seed_restore_test.py + pure seed-shape test (D-15, Wave-0 gap)</name>
  <files>scripts/seed_restore_test.py, tests/test_seed_restore.py</files>
  <read_first>
    - scripts/backup.sh — the pg/docker invocation style (BUCKET literal, timestamp naming, `docker compose exec postgres` for in-container DB ops) and the OCI `oci os object` command shape. This is the invocation analog.
    - tests/test_streak.py:1-45 — the pure-test convention: class-based, NO asyncpg/DB fixture, the "These tests do NOT use..." docstring. This is the analog for test_seed_restore.py.
    - CLAUDE.md Database Schema — the exact columns for `user_profiles` (user_id, username, total_songs_queued), `song_history` (guild_id, user_id, title, artist, url, duration_seconds), `user_artist_counts` (user_id, artist, play_count). Note: streak columns live on the schema as extended in Phase 3 — read database.py for the actual streak column names before seeding (do not invent column names).
    - 05-PATTERNS.md "scripts/seed_restore_test.py (new file)" + "tests/test_seed_restore.py (new file)" — the design sketch and the test-class pattern.
    - 05-RESEARCH.md "Focus Area 4" — the full restore round-trip sequence (createdb -T template0 → pg_restore --no-owner --no-acl via `docker compose exec -T postgres` → row-count verify → dropdb), the Option B docker-exec recommendation (avoids version mismatch), and Pitfall 4/5 (version mismatch, pipe masks failure → check min dump size).
  </read_first>
  <action>
    seed_restore_test.py (new): a Python std-lib + asyncpg script (no new packages). Structure it so the PURE row-construction logic is importable and DB-free:
    - A module-level constant `THROWAWAY_DB = "dexter_restore_test"` and an obviously-fake `SEED_USER_ID = "999999999999999999"`.
    - A PURE function (e.g. `build_seed_rows()`) returning the dicts/tuples for the known rows: 1 `user_profiles` row (with a streak value — use the ACTUAL streak column names found in database.py), 3 `song_history` rows for that user, 2 `user_artist_counts` rows. This function does NO IO and is what the test imports.
    - An async `main()` flow that: connects to the live `dexter` DB via asyncpg and inserts the seed rows; runs `subprocess.run(["bash", "scripts/backup.sh"])`; downloads the newest `dexter_*` dump from OCI (`oci os object list` → pick newest → `oci os object get`); checks the dump file is above a minimum size (guards Pitfall 5 pipe-masks-failure); creates the throwaway DB with `docker compose exec postgres createdb -U dexter -T template0 dexter_restore_test`; restores with `docker compose exec -T postgres pg_restore -U dexter -d dexter_restore_test --no-owner --no-acl` reading the dump from stdin (Option B — version-matched, avoids Pitfall 4); connects to `dexter_restore_test` and asserts row counts equal the seeded counts; drops the throwaway DB with `docker compose exec postgres dropdb -U dexter dexter_restore_test`.
    - NEVER run pg_restore/dropdb/createdb against the live `dexter` DB — only `dexter_restore_test`. Guard the dropdb so it can only target the throwaway name.
    This script is run manually by the user on Oracle (runbook check D1) — it is NOT a cron job and is NOT executed on the dev machine.

    test_seed_restore.py (new): pure pytest mirroring test_streak.py. Docstring "These tests do NOT use asyncpg — build_seed_rows() is a pure function and must stay that way." Import `build_seed_rows` (and `SEED_USER_ID`) from scripts.seed_restore_test. A `TestSeedData` class asserting: the returned structure has exactly 1 user_profiles row, 3 song_history rows, 2 user_artist_counts rows; the user_id fields are strings matching SEED_USER_ID; the song_history rows carry the required title/url fields; counts are positive integers. No DB connection, no asyncpg import, no `pytest.mark.asyncio`.
  </action>
  <verify>
    <automated>python -m py_compile scripts/seed_restore_test.py && python -m pytest tests/test_seed_restore.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile scripts/seed_restore_test.py` exits 0.
    - `python -m pytest tests/test_seed_restore.py -q` exits 0 with the seed-shape tests passing.
    - seed_restore_test.py `grep -c "dexter_restore_test" scripts/seed_restore_test.py` >= 2 (createdb + restore + dropdb all target the throwaway); `grep -c "pg_restore" scripts/seed_restore_test.py` >= 1 and the pg_restore invocation includes `--no-owner` and `--no-acl` and is wrapped by `docker compose exec`.
    - No code path passes the live database name `dexter` (not `dexter_restore_test`) to `pg_restore`, `dropdb`, or `createdb`.
    - test_seed_restore.py imports `build_seed_rows` from the script, has NO `import asyncpg` and NO `pytest.mark.asyncio`, and asserts the 1/3/2 row counts.
    - The seed rows use real database.py / CLAUDE.md column names (no invented columns).
    - Live confirmation (NOT run here — runbook check D1 / DEPLOY-07): user runs `python scripts/seed_restore_test.py` on Oracle; row counts match.
  </acceptance_criteria>
  <done>seed_restore_test.py performs the non-destructive D-15 round-trip via docker exec against a throwaway DB only; the pure build_seed_rows() is importable and tested DB-free; both verifications green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI repo → Oracle VM host | Scripts authored here run as host-level shell with DB + OCI credentials; no secrets may be embedded |
| seed_restore_test.py → live Postgres | The script connects to the live DB to seed and must never restore over it |
| backup dump → OCI Object Storage | Dumps contain full DB data; bucket must be private |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | seed_restore_test.py restoring over the live `dexter` DB | mitigate | Restore/createdb/dropdb target only `dexter_restore_test`; acceptance asserts the live name is never passed to pg_restore/dropdb; dropdb guarded to the throwaway name |
| T-05-06 | Information Disclosure | Secrets hardcoded in deploy.sh / seed script | mitigate | Scripts read `HEALTHCHECK_URL` / DB creds from environment / ~/.pgpass; no token/password literals; acceptance greps confirm none present |
| T-05-07 | Information Disclosure | OCI backup dump readable without auth | accept | OCI Object Storage bucket is private by default; auth via ~/.oci/config (D-10); no code change here |
| T-05-08 | Tampering | pg_restore version mismatch corrupting the verify | mitigate | Restore runs inside the postgres container via `docker compose exec` (Option B) — version-matched with pg_dump (Pitfall 4) |
| T-05-09 | Denial of Service | `docker compose down -v` wiping postgres_data during a deploy | mitigate | deploy.sh never calls down -v; loud warning echoed; runbook (Plan 03) carries the red-box warning |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs — std lib + already-present asyncpg only; nothing to verify |
</threat_model>

<verification>
- `bash -n scripts/deploy.sh scripts/backup.sh` exits 0.
- `python -c "import json; json.load(open('scripts/lifecycle-policy.json'))"` exits 0.
- `python -m py_compile scripts/seed_restore_test.py` exits 0.
- `python -m pytest tests/test_seed_restore.py -q` exits 0.
- Source assertions per acceptance_criteria (down -v warning, --build bot, 6h cadence, throwaway-DB-only restore, --no-owner/--no-acl, no live-DB target).
- All live execution (applying the lifecycle policy, running deploy.sh, running the restore round-trip on Oracle) is deferred to the runbook (Plan 03) and the user's /gsd-verify-work step — NOT executed on the dev machine.
</verification>

<success_criteria>
- DEPLOY-01: deploy.sh implements the D-13 git-pull + `--build bot` rebuild workflow with the down -v guard; syntax-clean.
- DEPLOY-07: backup.sh documents the 6h cadence; lifecycle-policy.json is valid 14-day OCI JSON; seed_restore_test.py performs the non-destructive D-15 round-trip via docker exec into a throwaway DB; pure seed-shape test green.
- The two Wave-0 commitments for this plan (test_seed_restore.py new file, importable pure build_seed_rows) are satisfied.
</success_criteria>

<output>
Create `.planning/phases/05-ship-it-live/05-02-SUMMARY.md` when done.
</output>
