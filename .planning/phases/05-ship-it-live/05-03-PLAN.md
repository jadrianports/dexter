---
phase: 05-ship-it-live
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - .planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-04, DEPLOY-05, DEPLOY-06, DEPLOY-07, DEPLOY-08]
user_setup: []

must_haves:
  truths:
    - "The runbook no longer instructs any Oracle/OCI/host-cron/systemctl/pg_dump-cron step (K-18 DROP)"
    - "Every Postgres/Docker-Postgres reference is re-worded to Neon serverless Postgres (K-18 SWAP)"
    - "The runbook adds Koyeb deploy-healthy, git-auto-deploy, health-endpoint-alive, UptimeRobot-active, Neon scale-to-zero-reconnect, and Neon PITR-restore checks (K-18 ADD)"
    - "The proven A(deploy/boot)→B(infra/queue)→C(behavioral)→D(destructive-LAST) ordering and all platform-agnostic behavioral checks are preserved (K-18 KEEP)"
    - "Host-reboot survival becomes Koyeb-restart + queue-restore-from-Neon survival; the runbook is the doc carrying live verification for DEPLOY-01/02/03/04/05/06/07/08 and P-01…P-04"
  artifacts:
    - path: ".planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md"
      provides: "Re-targeted Koyeb+Neon live-UAT runbook"
      contains: "Neon PITR"
  key_links:
    - from: "05-UAT-RUNBOOK.md health-endpoint check"
      to: "bot.py /health endpoint (Plan 01)"
      via: "curl https://<service>.koyeb.app/health"
      pattern: "/health"
    - from: "05-UAT-RUNBOOK.md deploy section"
      to: "docs/DEPLOY-KOYEB.md (Plan 02)"
      via: "prerequisites reference the deploy contract doc"
      pattern: "DEPLOY-KOYEB"
---

<objective>
Surgically re-target `05-UAT-RUNBOOK.md` from Oracle A1 to Koyeb + Neon in place (K-18), preserving the proven A→B→C→D ordered structure and every platform-agnostic behavioral check while swapping the deploy/infra/destructive sections to the new substrate. This is the doc that carries live verification for the whole phase — DEPLOY-01/02/03/05/07/08 plus the already-committed P-01 (DEPLOY-04) and P-02 (DEPLOY-06) code fixes are confirmed by executing it.

Purpose: The phase is "verified-live" (K-17), not "code-complete." The runbook is the single artifact the user runs against the live Koyeb+Neon deployment and reports via /gsd-verify-work. It must reference the real net-new deploy surface from Plans 01-02 (the `/health` endpoint, the DEPLOY-KOYEB.md contract) and must not send the user down dead Oracle paths (OCI buckets, host cron, systemctl, pg_dump).

Output: an updated `05-UAT-RUNBOOK.md` whose prerequisites, Group A (boot/infra), Group B (queue persistence), and Group D (destructive restore) are Koyeb+Neon, whose Group C behavioral checks are unchanged, and whose new checks cover Koyeb deploy/git-deploy/health/UptimeRobot + Neon scale-to-zero + Neon PITR.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ship-it-live/05-CONTEXT.md
@.planning/phases/05-ship-it-live/05-RESEARCH.md
@.planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md
@.planning/phases/05-ship-it-live/05-HUMAN-UAT.md
@docs/DEPLOY-KOYEB.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

| Symbol / File | Kind | Decision |
|---------------|------|----------|
| `05-UAT-RUNBOOK.md` (re-targeted in place) | edited doc | K-18 / K-17 |
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Re-target Group A + prerequisites + the destructive-warning to Koyeb + Neon</name>
  <files>.planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md</files>
  <read_first>
    - 05-UAT-RUNBOOK.md (full file in context): frontmatter + "Run on: Oracle A1 ARM VM" (lines 1-16); the `down -v` WARNING block (lines 19-32); Prerequisites Checklist (lines 34-63); Group A A1-A6 (lines 65-112); Troubleshooting table (lines 235-249); Session Summary (lines 251-263)
    - 05-RESEARCH.md "Runbook Re-Target Plan (K-18)" (lines 550-583) — the authoritative DROP / SWAP / ADD / KEEP lists
    - 05-RESEARCH.md Research Directive 2 (Koyeb deploy, git auto-deploy, health check, UptimeRobot) + Directive 3 (Neon scale-to-zero, PITR restore procedure lines 355-370)
    - docs/DEPLOY-KOYEB.md (Plan 02 output) — the deploy contract the prerequisites should point at
    - 05-CONTEXT.md K-02/K-08/K-09/K-11/K-17/K-18
  </read_first>
  <action>
    Header + frontmatter: change "Run on: Oracle A1 ARM VM via SSH" to "Run on: Koyeb WEB service (git-auto-built) + Neon serverless Postgres"; update frontmatter `source` to keep the same sources but note the Koyeb+Neon re-target; bump the runbook version note at the bottom to 2.0 (Koyeb+Neon) dated 2026-06-15.
    WARNING block: DROP the `docker compose down -v` / named-volume landmine (Oracle-specific — Neon is managed, no local volumes in prod). Replace with a Koyeb/Neon-appropriate caution: the destructive test is the Neon PITR restore (Group D) — it temporarily interrupts connections; never point pg_restore/dropdb at the live Neon branch; run Group D last. Keep the "break-glass: never run PC compose + Koyeb on the same token" caution (K-14).
    Prerequisites: DROP VM timezone/systemctl/`.pgpass`/`oci setup config`/OCI lifecycle/crontab/host pg_dump-version/`docker compose` host steps. REPLACE with Koyeb+Neon prereqs that point at docs/DEPLOY-KOYEB.md: (a) Neon project created in us-east-2, pooled DATABASE_URL copied; (b) Koyeb WEB service created from the GitHub repo, Dockerfile build, branch gsd/phase-5-ship-it-live, region wdc1, health check HTTP /health:8000; (c) Koyeb encrypted secrets + plain env vars set per the K-13 table; (d) UptimeRobot HTTP monitor on https://<service>.koyeb.app/health at 5-min interval; (e) Healthchecks.io check + Discord-webhook + email integration kept (K-09 — keep this existing prereq, it is platform-agnostic); (f) per-guild command sync via `python bot.py --first-run --guild <GUILD_ID>` or owner `/sync` (P-04 — KEEP).
    Group A — re-target the six checks:
    - A1 (Docker clean boot) → "Koyeb deploy healthy": after first deploy, Koyeb dashboard shows the service "Healthy"; Koyeb logs show "Dexter is ready." and the startup personality message posts to DEXTER_CHANNEL_ID. View logs via the Koyeb console or `koyeb services logs <service>`.
    - A2 (reboot survival) → "Koyeb restart + queue-restore-from-Neon survival": stop+restart the Koyeb service (or trigger a redeploy); the bot reconnects automatically and posts its startup message with no host intervention; `/queue` shows the queue restored from Neon guild_queues.
    - A3 (over-cap rejection) → KEEP behaviorally; set MAX_QUEUE_SIZE_PER_GUILD=1 via a Koyeb env var + redeploy instead of editing `.env` on a VM; restore to 500 after.
    - A4 (keepalive cron + Healthchecks.io) → SWAP to two checks: UptimeRobot monitor shows green and has pinged /health within the last interval; Healthchecks.io dashboard shows the bot-side dead-man ping within its window (DEPLOY-08).
    - A5 (backup cron) → DROP the OCI pg_dump cron entirely; replaced by Neon-managed PITR (the actual restore proof moves to Group D / D1).
    - A6 (Postgres integration tests) → SWAP to run against Neon: `pytest tests/test_database_phase4.py -x` pointed at a throwaway Neon database/branch (or the live test DB), DATABASE_URL set to the Neon pooled string; still expects the schema/transaction/helper tests green.
    Also ADD two new Group A checks: "Health endpoint alive" — `curl https://<service>.koyeb.app/health` returns `{"status":"ok"}` (the Plan-01 endpoint); and "Git auto-deploy" — push a trivial commit to gsd/phase-5-ship-it-live, Koyeb auto-builds, service returns Healthy (K-11).
    Troubleshooting table: DROP Oracle/arm64/OCI/.pgpass rows; ADD Koyeb-specific rows: service stuck "Unhealthy"/restart-loop → health endpoint not binding 0.0.0.0:8000 (Pitfall 5); bot offline after ~1h → UptimeRobot not pinging (Pitfall 4); asyncpg `unrecognized configuration parameter "channel_binding"` → sanitizer not applied (Pitfall 1); intermittent SSL-EOF after quiet periods → max_inactive_connection_lifetime not <300 (Pitfall 2); `InvalidSQLStatementNameError` → statement_cache_size not 0 (Pitfall 3); new commits to main not deploying → Koyeb still tracking the feature branch (Pitfall 8). Keep platform-agnostic rows (DISCORD_TOKEN invalid, slash commands missing).
    Use the existing runbook's exact section style (`### A1.` headings, `expected:` / `result: [pending]` lines). This is a doc edit — no fenced code beyond the existing command-line examples already in the runbook style.
  </action>
  <verify>
    <automated>python -c "p='.planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md'; s=open(p,encoding='utf-8').read(); banned=['OCI','oci os','systemctl','.pgpass','docker compose down -v','Oracle A1']; bad=[b for b in banned if b in s]; assert not bad, f'Oracle remnants: {bad}'; need=['Koyeb','Neon','/health','UptimeRobot','statement_cache_size','channel_binding']; miss=[n for n in need if n not in s]; assert not miss, f'missing Koyeb/Neon content: {miss}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 05-UAT-RUNBOOK.md no longer contains "OCI", "systemctl", ".pgpass", "docker compose down -v", or "Oracle A1"
    - 05-UAT-RUNBOOK.md contains "Koyeb", "Neon", "/health", "UptimeRobot"
    - Group A contains a Koyeb deploy-healthy check, a Koyeb-restart + queue-restore-from-Neon check, a health-endpoint curl check, a git-auto-deploy check, and an UptimeRobot/Healthchecks.io check
    - Troubleshooting table references the channel_binding sanitizer, SSL-EOF lifetime, statement_cache_size, and the 0.0.0.0:8000 binding pitfalls
    - "Run on:" line names Koyeb + Neon (not Oracle A1)
  </acceptance_criteria>
  <done>Prerequisites, the warning block, Group A, and the troubleshooting table are fully Koyeb+Neon; no Oracle/OCI/host-cron path remains.</done>
</task>

<task type="auto">
  <name>Task 2: Re-target Group B (queue persistence), keep Group C, rebuild Group D as Neon PITR, fix the summary count</name>
  <files>.planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md</files>
  <read_first>
    - 05-UAT-RUNBOOK.md Group B B1-B2 (lines 115-131); Group C C1-C11 (lines 133-220); Group D D1 (lines 223-231); Session Summary (lines 251-263) — same file as Task 1, edit after Task 1's changes
    - 05-RESEARCH.md "Runbook Re-Target Plan" KEEP list (Group C is platform-agnostic, ADD/SWAP only B and D) + Neon PITR restore procedure (lines 355-370) + Pitfall 7 (PITR window / seed-rows roast-fuel, lines 631-634)
    - scripts/seed_restore_test.py (kept by Plan 02) — the optional seed helper referenced for PITR roast-fuel
    - 05-CONTEXT.md K-17 #2/#3/#5 (queue restore, scale-to-zero, Neon restore) and the P-01/P-02 live-verify mapping
  </read_first>
  <action>
    Group B — re-target both checks to Koyeb+Neon (KEEP the behaviors):
    - B1 (queue persistence round-trip): `/play` a song → confirm in `/queue` → trigger a Koyeb redeploy/restart (NOT `docker compose restart bot`) → after the service comes back, `/queue` shows the queue restored from Neon guild_queues; smart-rejoin reconnects voice only if a non-bot human remains (DEPLOY-05, K-17 #2).
    - B2 (clear_persisted on idle-leave, DEPLOY-06 / P-02): join voice with the bot, let it sit alone 10+ min → bot auto-leaves + posts idle-leave message → trigger a Koyeb restart → `/queue` is EMPTY (not restored), confirming clear_persisted() fired; if it restores, inspect Koyeb logs for the clear_persisted call. Re-word the log-inspection step from `docker compose logs bot` to the Koyeb log viewer / `koyeb services logs <service>`.
    ADD a new Group B (or end-of-B) check "Neon scale-to-zero reconnect" (K-17 #3 / DEPLOY-01): leave the bot idle 6+ minutes with no Discord commands (so Neon suspends), then run `/history` or `/play` → the DB query succeeds with no crash and no SSL-EOF / 08006 / channel_binding error in the Koyeb logs (proves max_inactive_connection_lifetime=240 + statement_cache_size=0 + sanitizer all work live).
    Group C (C1-C11) — KEEP every behavioral check unchanged EXCEPT replace any `docker compose restart bot` / `docker compose logs bot` mechanics with the Koyeb equivalent (restart via Koyeb dashboard; logs via Koyeb console / `koyeb services logs`). The Discord-side behavior, decisions referenced (C2 late-night TZ → P-03; C11 reconnect-race diagnostic → P-01/DEPLOY-04), and `/gsd:debug` escalation language stay intact. Do not renumber C1-C11.
    Group D — rebuild D1 as the Neon PITR restore proof (DEPLOY-07 / K-08 / K-17 #5), DESTRUCTIVE-LAST: drop the OCI/pg_dump/`seed_restore_test.py`-against-OCI flow. New D1: (1) optionally run `python scripts/seed_restore_test.py` or queue real songs so there are known recent song_history rows (roast-fuel, Pitfall 7); (2) note a recent timestamp; (3) in the Neon console → Branches → Backup & Restore, use Time-Travel Assist to confirm the restore point contains expected data, then restore "From history" to a timestamp inside the 6-hour PITR window; (4) Neon auto-creates a `{branch}_old_{ts}` backup branch before overwriting — verify it appears; (5) connections briefly interrupt and the asyncpg pool reconnects automatically (same hostname); (6) verify post-restore via `/history` returning the expected data. Keep the "Group D only after A/B/C pass" guard and the destructive-last rationale.
    Session Summary: update the table — Group A check IDs (now incl. health-endpoint + git-deploy), Group B (B1, B2, + scale-to-zero), Group C (C1-C11 unchanged), Group D (D1 Neon PITR). Recompute the Total and change the closing line from "verified when all 21 checks pass on Oracle A1" to "verified-live when all checks pass on Koyeb+Neon per K-17; report via /gsd-verify-work."
  </action>
  <verify>
    <automated>python -c "p='.planning/phases/05-ship-it-live/05-UAT-RUNBOOK.md'; s=open(p,encoding='utf-8').read(); assert 'Neon PITR' in s, 'Neon PITR restore missing'; assert 'scale-to-zero' in s.lower(), 'scale-to-zero reconnect check missing'; assert 'pg_dump' not in s and 'dexter-backups' not in s, 'OCI backup flow still present'; assert s.count('### C')>=11, 'Group C behavioral checks reduced'; assert 'Koyeb+Neon' in s or ('Koyeb' in s and 'Neon' in s), 'verified-live wording missing'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Group B uses Koyeb redeploy/restart (not docker compose) and includes a Neon scale-to-zero reconnect check
    - Group C still has 11 behavioral checks (C1-C11), with Koyeb log/restart mechanics substituted but behaviors intact
    - Group D D1 is the Neon PITR branch-restore proof (contains "Neon PITR"); no pg_dump / OCI bucket references remain
    - Session Summary total recomputed and the closing line says verified-live on Koyeb+Neon (K-17), not Oracle A1
    - All 8 DEPLOY req IDs are traceable to runbook checks (frontmatter requirements list)
  </acceptance_criteria>
  <done>Group B/C/D are Koyeb+Neon, Group C behaviors preserved, the destructive Neon PITR restore is D1-last, and the summary reflects the new check set and the K-17 verified-live bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runbook → human operator | Instructions drive live actions against production Koyeb+Neon; a wrong instruction can cause data loss or downtime |
| Destructive restore → live Neon branch | The PITR restore in Group D overwrites the live branch history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering / data loss | Neon PITR restore on live branch | mitigate | Group D runs LAST; Neon auto-creates a `{branch}_old_{ts}` backup branch before overwrite; runbook instructs restoring only within the 6-hour window and verifying via Time-Travel Assist first. |
| T-05-11 | Denial of Service | dual instance during runbook execution | mitigate | Runbook keeps the K-14 break-glass caution; operator must not run PC compose while testing Koyeb on the same token. |
| T-05-12 | Information Disclosure | runbook embedding real secrets | mitigate | Runbook uses `<service>` / `<GUILD_ID>` / `<your-uuid>` placeholders only; secrets live in Koyeb's secret store, never in the doc. |
| T-05-SC | Tampering | pip/package steps in runbook | accept | The runbook only runs the existing pinned pytest suite against Neon; introduces no new installs. No legitimacy gate needed. |
</threat_model>

<verification>
- Automated string gates pass: no Oracle/OCI/pg_dump remnants; Koyeb/Neon/health/UptimeRobot/PITR/scale-to-zero present; Group C intact (>=11 checks); verified-live wording updated.
- Manual readability pass: a user can follow prerequisites → A → B → C → D end-to-end against a live Koyeb+Neon stack without hitting a dead Oracle instruction.
- The runbook is itself the live-verification instrument for DEPLOY-01..08 and P-01..P-04; execution by the user (reported via /gsd-verify-work) is the phase gate (K-17).
</verification>

<success_criteria>
- 05-UAT-RUNBOOK.md is fully Koyeb+Neon: prerequisites, Group A, Group B, Group D re-targeted; Group C behaviors preserved with Koyeb mechanics.
- New checks present: Koyeb deploy-healthy, git-auto-deploy, /health curl, UptimeRobot, Neon scale-to-zero reconnect, Neon PITR restore.
- A→B→C→D ordering and destructive-last preserved; host-reboot → Koyeb-restart + queue-restore-from-Neon.
- No OCI/host-cron/systemctl/pg_dump/Oracle path remains; the summary states the K-17 verified-live bar.
</success_criteria>

<output>
Create `.planning/phases/05-ship-it-live/05-03-SUMMARY.md` when done.
</output>
