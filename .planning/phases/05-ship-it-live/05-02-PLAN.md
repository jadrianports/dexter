---
phase: 05-ship-it-live
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - Dockerfile
  - utils/logger.py
  - .env.example
  - scripts/archive/backup.sh
  - scripts/archive/keepalive.sh
  - scripts/archive/deploy.sh
  - scripts/archive/lifecycle-policy.json
  - docs/DEPLOY-KOYEB.md
autonomous: true
requirements: [DEPLOY-01, DEPLOY-08]
user_setup:
  - service: koyeb
    why: "Host the bot 24/7 as a free WEB service, git-auto-built from the Dockerfile"
    env_vars:
      - name: DISCORD_TOKEN
        source: "Koyeb encrypted Secret (paste from Discord Developer Portal)"
      - name: GEMINI_API_KEY
        source: "Koyeb encrypted Secret"
      - name: GENIUS_TOKEN
        source: "Koyeb encrypted Secret"
      - name: DATABASE_URL
        source: "Koyeb encrypted Secret — Neon pooled connection string (with -pooler host)"
    dashboard_config:
      - task: "Create a free WEB service, connect the GitHub repo, track branch gsd/phase-5-ship-it-live, build method Dockerfile, region wdc1, health-check HTTP path /health port 8000"
        location: "Koyeb console → Create Service → GitHub"
  - service: neon
    why: "Free serverless Postgres (pooled endpoint) for all bot data; starts fresh (K-06)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon console → Connection Details → copy the POOLED string (host contains -pooler), region us-east-2"
    dashboard_config:
      - task: "Create a free project in us-east-2; copy the pooled connection string into the Koyeb DATABASE_URL secret"
        location: "Neon console → Project → Connection Details"
  - service: uptimerobot
    why: "External 5-min HTTP ping to https://<service>.koyeb.app/health to defeat Koyeb free's 1-hour scale-to-zero (K-02 amended)"
    dashboard_config:
      - task: "Add an HTTP(s) monitor for https://<service>.koyeb.app/health at a 5-minute interval"
        location: "UptimeRobot console → Add New Monitor"

must_haves:
  truths:
    - "yt-dlp is floor-pinned to a recent stable in requirements.txt and aiohttp is an explicit dep, so each Koyeb build starts from a solid base while runtime self-heal still updates yt-dlp (K-15)"
    - "The Dockerfile header no longer references Oracle/arm64 and documents that Koyeb builds it directly while compose is local-only (K-11/K-12)"
    - "Logs stream to stdout so the Koyeb log viewer captures them (K-16)"
    - "The four Oracle ops scripts are archived (not deleted) under scripts/archive/, retired by Neon-managed backups + UptimeRobot + Koyeb git-deploy (K-08/K-09/K-11)"
    - "A deploy doc captures the full Koyeb+Neon+UptimeRobot deploy contract (WEB service, Dockerfile git-build on the feature branch then main, encrypted secrets vs plain env vars, region, /health:8000, keep-alive) and .env.example reflects the K-13 secrets contract (Neon DATABASE_URL, dead-man HEALTHCHECK_URL kept)"
  artifacts:
    - path: "requirements.txt"
      provides: "yt-dlp floor pin + explicit aiohttp"
      contains: "yt-dlp>=2026.06.09"
    - path: "Dockerfile"
      provides: "de-Oracle'd, Koyeb-targeted header; unchanged build steps"
      contains: "Koyeb builds this Dockerfile directly"
    - path: "utils/logger.py"
      provides: "stdout StreamHandler"
      contains: "StreamHandler(sys.stdout)"
    - path: "docs/DEPLOY-KOYEB.md"
      provides: "Koyeb+Neon+UptimeRobot deploy contract"
      contains: "Neon"
    - path: ".env.example"
      provides: "K-13 secrets contract for both environments"
      contains: "DATABASE_URL"
    - path: "scripts/archive/deploy.sh"
      provides: "retired Oracle deploy script preserved in git"
  key_links:
    - from: "docs/DEPLOY-KOYEB.md"
      to: "bot.py /health endpoint (Plan 01)"
      via: "Koyeb health-check config references path /health port 8000"
      pattern: "/health"
    - from: "Dockerfile"
      to: "requirements.txt"
      via: "pip install -r requirements.txt"
      pattern: "pip install"
---

<objective>
Package Dexter for Koyeb + Neon without touching the application logic that Plan 01 owns. Floor-pin yt-dlp and make aiohttp explicit (K-15), de-Oracle the Dockerfile header (K-11/K-12), point logging at stdout for the Koyeb viewer (K-16), retire the four Oracle ops scripts to `scripts/archive/` (K-08/K-09/K-11), and write the deploy contract doc + refresh `.env.example` for the two-environment secrets model (K-13).

Purpose: The bot image already builds and runs locally (Dexter#2172 break-glass baseline). What is missing is the deploy packaging: a reproducible build base, stdout logs Koyeb can read, removal of the now-dead Oracle cron/OCI scripts, and a single authoritative doc the user follows to stand up the Koyeb WEB service + Neon DB + UptimeRobot keep-alive. This plan produces everything ready-to-deploy; the live deploy itself is a user UAT step (runbook, Plan 03).

Output: updated `requirements.txt`, `Dockerfile`, `utils/logger.py`, `.env.example`; `scripts/archive/{backup.sh,keepalive.sh,deploy.sh,lifecycle-policy.json}`; new `docs/DEPLOY-KOYEB.md`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ship-it-live/05-CONTEXT.md
@.planning/phases/05-ship-it-live/05-RESEARCH.md
@.planning/phases/05-ship-it-live/05-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

| Symbol / File | Kind | Decision |
|---------------|------|----------|
| `requirements.txt` yt-dlp>=2026.06.09 + aiohttp>=3.9.0 | pinned deps | K-15 |
| `Dockerfile` de-Oracle'd header | infra edit | K-11 / K-12 |
| `utils/logger.py` stdout StreamHandler + `import sys` | utility edit | K-16 |
| `.env.example` two-environment secrets contract | infra edit | K-13 |
| `scripts/archive/backup.sh` | moved file (retired) | K-08 |
| `scripts/archive/keepalive.sh` | moved file (retired) | K-09 |
| `scripts/archive/deploy.sh` | moved file (retired) | K-11 |
| `scripts/archive/lifecycle-policy.json` | moved file (retired) | K-08 |
| `docs/DEPLOY-KOYEB.md` | new deploy contract doc | K-02/K-11/K-12/K-13/K-09 |
| `scripts/seed_restore_test.py` | kept in place (optional PITR roast-fuel) | K-08 |
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Pin deps, de-Oracle the Dockerfile, and stream logs to stdout</name>
  <files>requirements.txt, Dockerfile, utils/logger.py</files>
  <read_first>
    - requirements.txt (full file in context: yt-dlp at line 2, asyncpg==0.31.0 at line 3 is the pin model, aiohttp at line 13)
    - Dockerfile (full file in context: Oracle/arm64 header at lines 1-4 and 8; FROM at line 5; build steps 9-21 unchanged)
    - utils/logger.py:1-48 (StreamHandler() with no arg at line 41 defaults to stderr; no `import sys` currently)
    - 05-PATTERNS.md "requirements.txt" / "Dockerfile" / "utils/logger.py" sections (lines 255-339) — exact edits and pin model
    - 05-RESEARCH.md Net-New Code Integration Map items 4, 5, 6 (lines 500-528); K-15 says floor-pin (>=), not exact, because the bot self-updates yt-dlp at 4am
  </read_first>
  <action>
    requirements.txt: change `yt-dlp` (line 2) to `yt-dlp>=2026.06.09` — the latest stable verified on GitHub releases at plan time (2026-06-15); use floor pin `>=` not exact `==` because the daily 4am ytdlp_update task self-heals beyond it (K-15). Change `aiohttp` (line 13) to `aiohttp>=3.9.0` — make the health-endpoint dependency explicit so it survives a future discord.py change (K-15 / Research open question 1). Leave asyncpg==0.31.0 and all other lines unchanged.
    Dockerfile: replace the Oracle/arm64 header (lines 1-4) and the arm64 comment (line 8) with a multi-arch / Koyeb-targeted header per 05-PATTERNS.md: note the image is multi-arch (amd64 on Koyeb/CI, arm64 on dev), that Koyeb builds this Dockerfile directly from git (K-11) while docker-compose.yml is local-dev only (K-12), and that secrets are injected at runtime via env vars never baked into layers (T-04-05). Keep `FROM python:3.11-slim-bookworm` and every functional line (apt-get ffmpeg/curl, WORKDIR, COPY, pip install, CMD) byte-for-byte unchanged — comment-only change.
    utils/logger.py: add `import sys` near the top (after `from __future__ import annotations`, before `import logging`), and change `logging.StreamHandler()` at line 41 to `logging.StreamHandler(sys.stdout)` so Docker/Koyeb log convention is followed and the Koyeb log viewer reliably captures bot output (K-16). The TimedRotatingFileHandler (ephemeral /app/logs) stays unchanged.
  </action>
  <verify>
    <automated>python -c "import sys; req=open('requirements.txt',encoding='utf-8').read(); assert 'yt-dlp>=2026.06.09' in req, 'yt-dlp not floor-pinned'; assert 'aiohttp>=3.9.0' in req, 'aiohttp not pinned'; df=open('Dockerfile',encoding='utf-8').read(); assert 'arm64v8' not in df and 'Oracle A1 ARM' not in df, 'Oracle/arm64 reference still present'; assert 'Koyeb builds this Dockerfile directly' in df, 'Koyeb header missing'; assert df.count('FROM python:3.11-slim-bookworm')==1, 'FROM changed'; lg=open('utils/logger.py',encoding='utf-8').read(); assert 'import sys' in lg and 'StreamHandler(sys.stdout)' in lg, 'stdout handler missing'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains `yt-dlp>=2026.06.09` and `aiohttp>=3.9.0`; `asyncpg==0.31.0` still present
    - Dockerfile contains "Koyeb builds this Dockerfile directly" and no longer contains `arm64v8` or `Oracle A1 ARM`; `FROM python:3.11-slim-bookworm` unchanged (exactly one occurrence)
    - utils/logger.py contains `import sys` and `StreamHandler(sys.stdout)`
    - `python -c "import utils.logger"` exits 0 (logger module still imports/initializes)
  </acceptance_criteria>
  <done>Build base is reproducible and self-healing, the Dockerfile is Koyeb-targeted with no functional change, and logs stream to stdout for the Koyeb viewer.</done>
</task>

<task type="auto">
  <name>Task 2: Retire Oracle ops scripts to scripts/archive/ and write the Koyeb deploy contract + .env.example</name>
  <files>scripts/archive/backup.sh, scripts/archive/keepalive.sh, scripts/archive/deploy.sh, scripts/archive/lifecycle-policy.json, docs/DEPLOY-KOYEB.md, .env.example</files>
  <read_first>
    - scripts/ directory listing (backup.sh, keepalive.sh, deploy.sh, lifecycle-policy.json, seed_restore_test.py, __init__.py present)
    - 05-RESEARCH.md "Oracle Script Disposition" (lines 536-547) — retire 4, keep seed_restore_test.py; move not delete
    - 05-RESEARCH.md Research Directive 2 (lines 264-328) — Koyeb WEB service pattern, build-from-Dockerfile + git auto-deploy (K-11), secrets vs plain env vars table (lines 292-306), region pick wdc1 + Neon us-east-2 (lines 308-312), runner-swap K-10 (lines 314-328)
    - 05-RESEARCH.md Research Directive 3 (lines 332-382) — Neon scale-to-zero, pooled vs direct endpoint (use the -pooler string), PITR restore procedure
    - 05-CONTEXT.md K-02/K-08/K-09/K-11/K-12/K-13/K-14 + Claude's Discretion (region, cache value)
    - .env.example (full file in context) — current Oracle-flavored DATABASE_URL/POSTGRES_PASSWORD/keepalive comments
  </read_first>
  <action>
    Create scripts/archive/ then move the four retired scripts with `git mv` to preserve history: `git mv scripts/backup.sh scripts/archive/backup.sh`, `git mv scripts/keepalive.sh scripts/archive/keepalive.sh`, `git mv scripts/deploy.sh scripts/archive/deploy.sh`, `git mv scripts/lifecycle-policy.json scripts/archive/lifecycle-policy.json`. Keep scripts/seed_restore_test.py and scripts/__init__.py in place (seed script kept as optional Neon-PITR roast-fuel per K-08). Add a short scripts/archive/README.md is optional — instead put a one-line note at the top of DEPLOY-KOYEB.md pointing at the archive. (If `git mv` is unavailable in the sandbox, use a plain move; the file must end up under scripts/archive/ and absent from scripts/.)
    Create docs/DEPLOY-KOYEB.md — the authoritative deploy contract. Sections: (1) Substrate overview (Koyeb free WEB service + Neon serverless Postgres + UptimeRobot, $0/mo no-credit-card — K-01); (2) Neon setup — create free project in us-east-2, copy the POOLED connection string (host contains `-pooler`), note schema auto-creates on first connect (K-03/K-06), DATABASE_URL is the only value differing between envs; (3) Koyeb service — Create WEB service (NOT Worker: free tier has no Worker — K-02 amended), connect GitHub repo, build method = Dockerfile, track branch `gsd/phase-5-ship-it-live` then re-point to `main` after the user merges (K-11), region wdc1, health check HTTP path `/health` port 8000 (the endpoint from Plan 01), free instance specs (512MB RAM / 0.1 vCPU / 2GB disk); (4) Secrets vs env vars per the K-13 table — `DISCORD_TOKEN`/`GEMINI_API_KEY`/`GENIUS_TOKEN`/`DATABASE_URL` as encrypted Koyeb secrets, `DEXTER_CHANNEL_ID`/`ERROR_LOG_CHANNEL_ID`/`OWNER_ID`/`HEALTHCHECK_URL`/`STREAK_TIMEZONE` as plain env vars; never bake secrets into the image (T-05-06); (5) UptimeRobot keep-alive — add an HTTP monitor on `https://<service>.koyeb.app/health` at 5-min interval to defeat the 1-hour scale-to-zero (K-02); (6) Dead-man monitoring kept — Healthchecks.io outbound HEALTHCHECK_URL ping (K-09); (7) K-14 break-glass rule — never run the PC compose and Koyeb on the same Discord token simultaneously; (8) K-10 runner-swap contingency — if music stutters on 0.1 vCPU or Koyeb sleeps despite pings, swap the runner to HeavenCloud/Wispbyte pointed at the same Neon DB (only the runner changes); (9) Archived-scripts note — the Oracle backup/keepalive/deploy/lifecycle scripts now live in scripts/archive/, retired by Neon-managed PITR + UptimeRobot + Koyeb git-deploy. Use lowercase-friendly prose consistent with project tone; this is a doc, not code — no fenced implementation needed beyond illustrative env-var lists.
    Update .env.example for the K-13 contract: keep all existing var names (identical across both environments). Re-comment the DATABASE base — local/break-glass uses the local-Postgres URL; in Koyeb DATABASE_URL is the Neon POOLED string set as an encrypted secret. Drop/replace Oracle-specific framing: the keepalive cron comment for HEALTHCHECK_URL should note it is the bot-side outbound dead-man ping (kept under K-09) plus a pointer that UptimeRobot now provides the inbound keep-alive. Keep POSTGRES_PASSWORD for the local compose stack (K-12). Do not commit any real secret values.
  </action>
  <verify>
    <automated>python -c "import os; arch='scripts/archive/'; need=['backup.sh','keepalive.sh','deploy.sh','lifecycle-policy.json']; [open(arch+f,encoding='utf-8').close() for f in need]; assert not os.path.exists('scripts/backup.sh') and not os.path.exists('scripts/deploy.sh'), 'old scripts still in scripts/'; assert os.path.exists('scripts/seed_restore_test.py'), 'seed script should be kept'; d=open('docs/DEPLOY-KOYEB.md',encoding='utf-8').read(); assert 'Neon' in d and 'WEB service' in d and '/health' in d and 'UptimeRobot' in d and 'us-east-2' in d, 'deploy doc missing required content'; e=open('.env.example',encoding='utf-8').read(); assert 'DATABASE_URL' in e, '.env.example missing DATABASE_URL'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/archive/ contains backup.sh, keepalive.sh, deploy.sh, lifecycle-policy.json
    - scripts/ no longer contains backup.sh / keepalive.sh / deploy.sh / lifecycle-policy.json
    - scripts/seed_restore_test.py still exists (kept)
    - docs/DEPLOY-KOYEB.md exists and contains "Neon", "WEB service", "/health", "UptimeRobot", "us-east-2"
    - docs/DEPLOY-KOYEB.md mentions the K-14 break-glass rule and the K-10 runner-swap contingency
    - .env.example contains DATABASE_URL and references Neon for the Koyeb environment; keeps identical var names (K-13)
  </acceptance_criteria>
  <done>The Oracle scripts are archived (history preserved), the seed script is kept, and a single deploy doc + refreshed .env.example fully capture the Koyeb+Neon+UptimeRobot contract a user can follow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git repo → Koyeb build | Koyeb pulls and builds the Dockerfile from the tracked branch; build logs are visible in the Koyeb console |
| Koyeb secret store → container env | Encrypted secrets are injected at runtime; the image must never contain them |
| Docs/.env.example → user | Templates must not leak any real credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | secrets in image layers / build logs | mitigate | Dockerfile takes no ARG/ENV secrets; all of DISCORD_TOKEN/GEMINI_API_KEY/GENIUS_TOKEN/DATABASE_URL are Koyeb encrypted secrets injected at runtime. DEPLOY-KOYEB.md states this explicitly; comment-only Dockerfile change adds no ENV. |
| T-05-07 | Information Disclosure | .env.example / docs committed with real values | mitigate | .env.example keeps placeholders only (no real tokens/URLs); DEPLOY-KOYEB.md uses `<service>` / placeholder examples. `.env` stays git-ignored. |
| T-05-08 | Denial of Service | two bot instances on one token (PC compose + Koyeb) | mitigate | DEPLOY-KOYEB.md codifies the K-14 break-glass rule: never run both simultaneously; PC stays on local Postgres. |
| T-05-09 | Tampering | supply-chain via unpinned yt-dlp build | mitigate | yt-dlp floor-pinned to a verified-legitimate recent release (github.com/yt-dlp/yt-dlp, RESEARCH Package Legitimacy Audit); aiohttp pinned; no new packages introduced. |
| T-05-SC | Tampering | pip installs | mitigate | No net-new packages; only version pins on already-vetted yt-dlp + aiohttp. RESEARCH "## Package Legitimacy Audit" confirms zero new deps — no [ASSUMED]/[SUS]/[SLOP] entries. |
</threat_model>

<verification>
- Automated string checks pass (deps pinned, Dockerfile de-Oracle'd, stdout handler, scripts archived, deploy doc + .env.example content present)
- `python -c "import utils.logger"` exits 0
- `docker build -t dexter:koyeb .` succeeds locally (optional human-check — confirms the comment-only Dockerfile change did not break the build); otherwise the live Koyeb build in the runbook (Plan 03) is the deploy-time verification
- The live Koyeb deploy + UptimeRobot + Neon connection is a user UAT step captured in 05-UAT-RUNBOOK.md (Plan 03), not an autonomous task here.
</verification>

<success_criteria>
- yt-dlp floor-pinned to 2026.06.09 and aiohttp made explicit; asyncpg pin intact.
- Dockerfile header is Koyeb-targeted with zero functional change.
- Logs stream to stdout.
- Four Oracle scripts archived under scripts/archive/ with history preserved; seed script kept.
- docs/DEPLOY-KOYEB.md captures the full Koyeb+Neon+UptimeRobot deploy contract incl. secrets model, region, /health:8000, branch flow, break-glass, runner-swap.
- .env.example reflects the K-13 two-environment secrets contract with placeholders only.
</success_criteria>

<output>
Create `.planning/phases/05-ship-it-live/05-02-SUMMARY.md` when done.
</output>
