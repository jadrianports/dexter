---
phase: 23-portfolio-surface-ci-cd
plan: 07
type: execute
wave: 7
depends_on: ["23-04", "23-06"]
files_modified:
  - scripts/render_demo_gif.py
  - docs/demo.gif
  - README.md
  - .planning/phases/23-portfolio-surface-ci-cd/23-HUMAN-UAT.md
autonomous: true
requirements: [PORT-02, PORT-03, PORT-04, CICD-03]
user_setup:
  - service: ghcr
    why: "CICD-03 / D-17 — GHCR packages are private by default; visibility is a UI setting on the package page and genuinely cannot be set from the publishing workflow without a new PAT"
    dashboard_config:
      - task: "Change package visibility to Public, then verify a logged-out `docker pull` succeeds"
        location: "github.com/users/jadrianports/packages/container/dexter/settings"

must_haves:
  truths:
    - "PORT-03: the README reads as an architecture case study — tagline, five badges, demo GIF, feature list, a mermaid cog-service-logic diagram, 3-4 hard-problem callouts, honest boundaries, and a working invite link."
    - "D-15: exactly five badges — CI status (pointing at the real ci.yml workflow), Python, discord.py, Postgres/pgvector, Gemini. No license badge, because there is no LICENSE file and the badge would imply one."
    - "D-09: the README is professional engineering prose. Dexter is quoted only where he is the subject. PORT-04's disclosure section reads as disclosure, not as a joke."
    - "D-08: mid-depth — 3-4 hard-problem callouts of 2-3 sentences each, not a per-subsystem deep dive nobody reads."
    - "D-07: docs/demo.gif is rendered from the D-06 mock via Playwright — one source, two derivatives, so the two demos cannot tell different stories."
    - "D-07: Playwright enters neither requirements.txt nor requirements-dev.txt, and the GIF is never regenerated in CI — it is a committed, generated-once asset."
    - "Phase 22 D-07 / IN-01: the README's invite link makes tests/test_invite_drift_guard.py NON-VACUOUS for the first time — a tracked doc now carries the canonical URL and the guard enforces it."
    - "D-17: the GHCR public flip and its logged-out docker pull proof are recorded as human items — the workflow ships correct; the toggle is a UI setting no workflow can reach."
  artifacts:
    - path: "scripts/render_demo_gif.py"
      provides: "Playwright capture + ffmpeg palette conversion deriving docs/demo.gif from the landing page's mock"
      min_lines: 30
    - path: "docs/demo.gif"
      provides: "PORT-02 / PORT-03 — the README's embedded demo"
    - path: "README.md"
      provides: "PORT-03 + the README half of PORT-04 — the architecture case study"
      contains: "badge.svg"
      min_lines: 80
    - path: ".planning/phases/23-portfolio-surface-ci-cd/23-HUMAN-UAT.md"
      provides: "The acknowledged-deferred human items (D-17 GHCR flip, README/mermaid render check, landing-page review)"
  key_links:
    - from: "README.md"
      to: "logic/invite.py build_invite_url"
      via: "the canonical invite URL, now enforced by tests/test_invite_drift_guard.py"
      pattern: "oauth2/authorize"
    - from: "docs/demo.gif"
      to: "site/src/components/DemoMock.astro"
      via: "scripts/render_demo_gif.py Playwright video capture"
      pattern: "one source, two derivatives"
    - from: "README.md CI badge"
      to: ".github/workflows/ci.yml"
      via: "the badge SVG, reporting the real last run at HEAD"
      pattern: "workflows/ci.yml/badge.svg"
---

<objective>
Write the surface most engineers actually land on first — the README, rewritten from two lines into an architecture case study — and derive its demo GIF from the mock that already exists.

Purpose: a recruiter skims the landing page; a senior engineer opens the README. D-09 splits the voice accordingly. The landing page is Dexter's: dark, lowercase, dry. The README is **the engineer's**: clear technical prose, no sarcasm in the analysis, with Dexter quoted only where he is the subject (the demo, example outputs, maybe the tagline). A README that roasts the reader shows a bit and costs a lot, on the one document where a hiring engineer is looking for signal about judgment.

D-08 sets the depth at mid. Nobody reads a 900-line README and the good parts get buried; a shallow product README fails PORT-03's explicit "rewritten as an architecture case study" ask. Tagline → badges → demo → features → a mermaid diagram of the layering → 3–4 hard-problem callouts → honest boundaries → invite link.

One structural payoff worth naming: the moment the README carries the canonical invite URL, Phase 22's `test_invite_drift_guard.py` — which has passed **vacuously** since the day it was written, because no tracked doc contained an OAuth2 URL — begins enforcing something real. Verify that explicitly rather than assuming it.

Output: PORT-03, the README half of PORT-04, PORT-02's README derivative, and an honest ledger of what remains human.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-portfolio-surface-ci-cd/23-CONTEXT.md
@.planning/phases/23-portfolio-surface-ci-cd/23-RESEARCH.md
@.planning/phases/23-portfolio-surface-ci-cd/23-UI-SPEC.md
@.planning/PROJECT.md
@CLAUDE.md
@tests/test_invite_drift_guard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render docs/demo.gif from the landing page's mock (D-07 — one source, two derivatives)</name>

  <read_first>
    - `.planning/phases/23-portfolio-surface-ci-cd/23-RESEARCH.md` §Finding 7 — the full pipeline: Playwright **video recording**, not a screenshot loop (`page.screenshot()` disables CSS animations by default, so a screenshot approach fights the tool instead of using it), then a two-pass `palettegen`/`paletteuse` ffmpeg conversion, the established technique for small, crisp-text GIFs. Also read its dependency-placement rule and the three reasons the GIF is a committed asset rather than a CI output
    - `.planning/phases/23-portfolio-surface-ci-cd/23-PATTERNS.md` §`scripts/render_demo_gif.py` — the in-repo analogs (`scripts/seed_restore_test.py`, `scripts/backup.sh`): standalone, dev-machine-run, never imported by app code, never in a requirements file
    - `.planning/phases/23-portfolio-surface-ci-cd/23-CONTEXT.md` §D-07 — why GitHub's markdown renderer forces a `.gif` rather than reusing the page's animated mock directly
    - `site/src/components/DemoMock.astro` — the 16s loop being recorded
  </read_first>

  <files>scripts/render_demo_gif.py, docs/demo.gif</files>

  <action>
    Derive the README's demo from the mock that already exists. One source, two derivatives — no second authoring pass, and therefore no way for the landing page and the README to drift into telling different stories.

    `scripts/render_demo_gif.py`: launch Chromium via `sync_playwright()`, open a browser context configured with `record_video_dir` and a `record_video_size` matched to the mock's desktop geometry, navigate to the built `site/dist/index.html` (or a local preview server), bring the demo section into view, wait out one full 16s loop, then call `context.close()` — the video only finalizes on close, which is the single easiest step to get wrong. Then convert the resulting `.webm` to `docs/demo.gif` with the two-pass ffmpeg palette approach. ffmpeg is already a project dependency, and this conversion runs on a dev machine, never in CI and never in the image.

    Starting parameters: 12 fps, 640px wide, lanczos scaling, `palettegen` with `stats_mode=diff`, `paletteuse` with bayer dithering. Target under 2MB with legible text. If a full 16s loop overshoots the budget, tune **fps first, then width**, and record what you landed on. Do not silently ship a 6MB GIF — a bloated hero asset on a portfolio page is its own bad signal.

    **Dependency placement is a rule here, not a preference.** Playwright goes in neither `requirements.txt` (baked into the Docker image, where a headless browser has zero runtime relevance to a Discord bot) nor `requirements-dev.txt` (installed on every CI run, and deliberately minimal — currently just `ruff`). Document the one-time local setup (`pip install playwright` followed by `playwright install chromium`) in the script's own header comment, the same way `ci.yml` documents conditional manual steps without baking them into the default path. The GIF is a committed, generated-once asset: it changes only when the mock's copy changes, and adding a ~300MB headless-browser download to CI to regenerate presentation content would trade away exactly the fast, minimal-privilege job posture D-03 is built around.

    **Do not put an invite URL anywhere in this file.** `test_logic_invite_is_the_only_url_constructor` scans every tracked non-test `.py` file for the literal OAuth2 authorize URL or an `oauth_url(` call and permits exactly one file (`logic/invite.py`). A stray URL in a comment here turns CI red — and the Phase 21/22 precedent is that even *prose* mentions in docstrings have tripped these source-inspection guards before.
  </action>

  <verify>
    <automated>python -c "import pathlib; p=pathlib.Path('docs/demo.gif'); assert p.exists(), 'docs/demo.gif missing'; mb=p.stat().st_size/1048576; assert mb<=2.0, f'GIF is {mb:.2f}MB (over 2MB budget)'; s=pathlib.Path('scripts/render_demo_gif.py').read_text(encoding='utf-8'); assert 'oauth2/authorize' not in s, 'invite URL literal in a .py file breaks the single-constructor guard'; r=pathlib.Path('requirements.txt').read_text()+pathlib.Path('requirements-dev.txt').read_text(); assert 'playwright' not in r.lower(), 'playwright must not enter a requirements file'; print(f'ok {mb:.2f}MB')"</automated>
  </verify>

  <acceptance_criteria>
    - `docs/demo.gif` exists and is 2MB or smaller. The final fps/width are recorded in the SUMMARY.
    - The GIF shows the same conversation as the landing page's mock — the same four messages, the same verbatim Dexter lines. It was rendered from the mock, not authored separately.
    - `scripts/render_demo_gif.py` contains a header comment documenting the one-time `pip install playwright` + `playwright install chromium` setup.
    - `playwright` appears in **neither** `requirements.txt` nor `requirements-dev.txt`.
    - No CI workflow references Playwright or regenerates the GIF (`grep -ric playwright .github/workflows/` returns 0).
    - `scripts/render_demo_gif.py` contains no OAuth2 authorize URL and no `oauth_url(` call — `pytest tests/test_invite_drift_guard.py -q` stays green.
    - `ruff check . && ruff format --check .` exit 0 (the new script is linted like any other Python in this repo).
  </acceptance_criteria>

  <done>The README's demo is the landing page's demo, mechanically derived — not a second telling of the same story.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md as an architecture case study (PORT-03, PORT-04 / D-08, D-09, D-10, D-15)</name>

  <read_first>
    - `.planning/phases/23-portfolio-surface-ci-cd/23-CONTEXT.md` §D-08 (the structure and the four candidate callouts), §D-09 (professional prose; Dexter quoted only where he is the subject; the corollary that PORT-04 must read as disclosure), §D-10 (the four boundaries, constraint → decision), §D-15 (exactly five badges; **no license badge**)
    - `.planning/phases/23-portfolio-surface-ci-cd/23-RESEARCH.md` §Finding 8 — the exact badge markdown and the CI badge's exact URL form (it must reference `ci.yml` **by filename**), plus the sequencing constraint: the badge only renders real status after a real run of that workflow file on the default branch
    - `.planning/PROJECT.md` §"Key Decisions" — **the authoritative ledger of what shipped.** The source for boundary 4's memory-scoping wording and the richest source for the hard-problem callouts
    - `CLAUDE.md` — the tech stack, the project structure, the Critical Rules, and the Implementation Gotchas (the callouts are drawn from real scars recorded here)
    - `.planning/phases/23-portfolio-surface-ci-cd/23-UI-SPEC.md` §"Copywriting Contract" — the landing page's four boundary rows; the README expands these per D-10 rather than contradicting them
    - `tests/test_invite_drift_guard.py` — the guard that is about to stop being vacuous. Note `_tracked_doc_files`'s `.md` extension allowlist: `README.md` is in scope, so the URL pasted here **must** be byte-identical to `build_invite_url()`'s output
    - The 23-01 SUMMARY — the green CI run's URL and head SHA, which is what the badge will report on
  </read_first>

  <files>README.md</files>

  <action>
    Replace the two-line README with a mid-depth case study. Structure, in order:

    **Tagline** — one line. Dexter may speak here (D-09 permits it where he is the subject), but the sentence after it is the engineer's.

    **Five badges, exactly** (D-15): CI status, Python, discord.py, Postgres/pgvector, Gemini. Use RESEARCH Finding 8's exact markdown. The CI badge must reference `ci.yml` by filename and link to the workflow's runs page. **No license badge** — it would imply a `LICENSE` file that does not exist, and adding one is deliberately out of this phase's scope. Five reads as considered; twelve reads as padding.

    **The demo GIF** — embed `docs/demo.gif`. Caption it honestly. It is a reconstruction of the UI carrying real, unedited Dexter output; it is not a screen recording and must not be described as one.

    **Feature list** — concise. This is where the five-button now-playing panel gets its due: the demo cannot show it, and the landing page's Feature 3 already says so out loud. Do not quietly drop it here.

    **A mermaid diagram of the cog → service → logic layering.** A `graph TD` with four groupings: the **cogs** layer (the Discord surface: music, ai, imagine, library, memory, admin, ops, invite, events), the **services** layer (the I/O boundary: youtube/yt-dlp, gemini with its two separate rate limiters, memory/pgvector RAG, guild_config cache, lyrics, audio, metrics, queue_persistence), the **logic** layer (the pure, mock-free decision seam: playback, roasts, proactive, vision, taste, autoqueue, guild_config, invite), and **persistence** (Neon Postgres + pgvector). Edges: cogs into services, cogs into logic, services into the database. The point the diagram must land is the one that actually distinguishes this codebase — decision logic is pure and tested; Discord and process glue stay thin and untested-by-design.

    **3–4 hard-problem callouts, 2–3 sentences each** (D-08 offers four candidates; take all four — each is a real scar with a real fix, and together they cover four different kinds of engineering problem):
    1. **pgvector RAG memory on zero new infrastructure** — a new table on the Postgres already in use, rather than Redis or a knowledge graph, with embeddings on a *separate* 60 RPM limiter so background memory writes can never starve the user-facing 15 RPM chat budget.
    2. **The `_play_generation` counter** — how a monotonically-incrementing generation guard kills stale playback after-callbacks that would otherwise double-play tracks on skip/stop/replay races.
    3. **The accuracy firewall** — memory supplies the *episode*, live SQL supplies the *number*. Embedded text never carries a count or a streak, because a stale embedded number would make a roast confidently wrong. This is the rule that keeps a personality feature from corroding factual accuracy.
    4. **Two-choke-point kill-switch enforcement** — the owner control plane refuses a blocked or silenced guild at exactly two enforced choke points (the command-tree `interaction_check` and a pre-send re-check on ambient surfaces) instead of scattering per-command checks that a future feature would inevitably forget.

    Write these **as the engineer**: what the problem was, what breaks if you get it wrong, what the decision was. No sarcasm in the analysis — that is D-09's whole point, and it is the section a hiring engineer reads most carefully.

    **Honest boundaries** (PORT-04, the fuller README version per D-10) — the same four boundaries as the landing page, expanded: the 100-guild verification wall, the on-demand hosting caveat (the bot is offline unless the owner is running it — the YouTube datacenter-IP block killed the free-cloud deploy), the full-savage personality mitigated by a reactive kill-switch, and the hybrid memory scoping **exactly as Phase 21 shipped it**. For that last one, work from `.planning/PROJECT.md` §Key Decisions, not from memory: guild-scoped recall on `/roast @user`, ambient roasts, proactive callbacks, the music-command callback, and the auto-queue taste blend (an explicit per-call-site opt-in); `/ask` deliberately global but **self-scoped**, so no cross-user exposure is possible; the legacy NULL-guild corpus grandfathered; guild data purged on removal. Each boundary states the constraint, then the deliberate decision made in response. **This section reads as disclosure, not as a joke** (D-09's corollary).

    **The invite link** — paste the canonical URL. It must be byte-identical to `build_invite_url()`'s output, because `README.md` is a tracked `.md` file and Phase 22's drift guard scans it. This is the moment that guard stops being vacuous. Do not shorten it, do not wrap it in a redirect, do not "clean up" the query string (Phase 22 D-07).

    Link out to the live landing page. Do **not** duplicate the landing page's architecture story anywhere else, and do not add a second copy of the architecture summary to the site — one story, one surface (D-05).
  </action>

  <verify>
    <automated>pytest tests/test_invite_drift_guard.py -q && python -c "import pathlib,re; r=pathlib.Path('README.md').read_text(encoding='utf-8'); import config; from logic.invite import build_invite_url; canon=build_invite_url(client_id=config.DISCORD_CLIENT_ID, permissions_value=config.INVITE_PERMISSIONS_VALUE, scopes=config.INVITE_SCOPES); assert canon in r, 'canonical invite URL missing from README'; assert r.count('img.shields.io')==4, f'expected 4 shields badges, found {r.count(chr(34)) and r.count(\"img.shields.io\")}'; assert 'workflows/ci.yml/badge.svg' in r, 'CI badge missing'; assert 'licen' not in r.lower().split('## ')[0], 'no license badge permitted (D-15)'; assert 'mermaid' in r, 'mermaid diagram missing'; assert 'demo.gif' in r, 'demo GIF not embedded'; print('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - `README.md` contains all eight D-08 sections in order: tagline, badges, demo GIF, feature list, mermaid diagram, hard-problem callouts, honest boundaries, invite link.
    - **Exactly five badges**: one CI status badge referencing `ci.yml` by filename, plus four shields.io tech badges. **Zero license badges** (D-15).
    - The CI badge points at the real workflow and renders green — cross-check against the run recorded in the 23-01 SUMMARY (`gh run list --branch main --workflow CI --limit 1` is success at HEAD). Do not finalize copy claiming green CI before that is true.
    - `docs/demo.gif` is embedded, and its caption does not describe it as a screen recording.
    - The mermaid block is a `graph TD` naming the real modules across cogs / services / logic / persistence.
    - Three to four hard-problem callouts are present, each 2–3 sentences, written in analytical prose with **no sarcasm**.
    - All four PORT-04 boundaries are present, each in constraint → decision form. Boundary 4 names `/ask` explicitly and matches `.planning/PROJECT.md` §Key Decisions — diff it and record the check.
    - **`pytest tests/test_invite_drift_guard.py -q` is green AND now non-vacuous**: assert that `_collect_offenders(_tracked_doc_files(root), _canonical_url())` runs over a set that genuinely includes `README.md`, and that the canonical URL is found in it. Record in the SUMMARY that the guard has, for the first time since Phase 22, something real to guard. If the pasted URL had drifted by a single byte, this test would be red — that is the proof it is live.
    - `grep -c 'bit.ly\|tinyurl\|redirect' README.md` returns 0 — no shortener, no vanity redirect (Phase 22 D-07).
    - The README links to the live landing page and does not duplicate its content.
  </acceptance_criteria>

  <done>The README is a case study a senior engineer would respect, the badges are real, and the invite link is the same one the bot itself hands out — enforced, not merely intended.</done>
</task>

<task type="auto">
  <name>Task 3: Write 23-HUMAN-UAT.md and close the phase honestly (D-17, CICD-03)</name>

  <read_first>
    - `.planning/phases/22-invite-plumbing/22-HUMAN-UAT.md` — the established format (frontmatter, numbered tests with expected/result, a summary block). Every phase since 11 uses it
    - `.planning/phases/23-portfolio-surface-ci-cd/23-VALIDATION.md` §"Manual-Only Verifications" — the five rows that must each become an item here
    - `.planning/phases/23-portfolio-surface-ci-cd/23-CONTEXT.md` §D-17 — the GHCR public flip, and why it genuinely cannot be automated without a PAT this project deliberately does not have
    - `.planning/phases/23-portfolio-surface-ci-cd/23-RESEARCH.md` §Pitfall 6 — the symptom (a green workflow, a failing logged-out pull) and why it is expected rather than a bug
    - The 23-02, 23-04, and 23-06 SUMMARYs — several of these items may already be **done** (the Pages toggle, the demo lines); record them as done-and-verified rather than pending
  </read_first>

  <files>.planning/phases/23-portfolio-surface-ci-cd/23-HUMAN-UAT.md</files>

  <action>
    Write the phase's human ledger, following the established format. Carry every item from 23-VALIDATION's Manual-Only table, and mark each one honestly as done (with evidence) or pending:

    1. **GHCR public flip + logged-out pull (D-17, the CICD-03 half no workflow can prove).** Package settings → Change visibility → Public, then `docker logout ghcr.io` followed by `docker pull ghcr.io/jadrianports/dexter:v1.4.0-rc1` (or the milestone tag once it is cut). This is the acknowledged-deferred pattern, identical to Phase 22's Dev Portal step. State plainly that until this is done, `docker pull` fails for everyone except the owner — that is expected, not a bug, and CICD-03's "pullable with zero build step" claim is not yet true.
    2. **GitHub Pages source toggle** — likely already done in plan 23-02; if so, record it as verified with the deploy run URL as evidence rather than leaving it open.
    3. **README renders correctly on github.com** — the five badges resolve and the mermaid diagram renders. GitHub's markdown/mermaid pipeline is not reproducible locally, so this genuinely needs a human eye on the rendered page.
    4. **Landing page visual and copy review** — load the live page. Confirm the four boundaries read as *disclosure, not apology*, and confirm the hosting caveat sits **before** the closing CTA (D-05's whole reason for that ordering).
    5. **The verbatim demo lines** — if plan 23-02 supplied them, record it as done with the provenance. If it was blocked, this stays open and **PORT-02 is reported incomplete**, not quietly passed.

    Then close out: run the full suite, confirm CI is green at HEAD, confirm the Pages deploy is green, and confirm the landing page and README tell the same story. Update `.planning/REQUIREMENTS.md`'s traceability rows for PORT-01…04 and CICD-02/03 to reflect their true state — including any that are honestly still blocked on a human. **Do not mark a requirement complete because the code is written; mark it complete when the thing it promises is true.** That distinction is, more or less, the entire subject of this phase.
  </action>

  <verify>
    <automated>pytest -q && python -c "import pathlib; t=pathlib.Path('.planning/phases/23-portfolio-surface-ci-cd/23-HUMAN-UAT.md').read_text(encoding='utf-8').lower(); [t.index(k) for k in ['ghcr','pages','mermaid','boundar']]; print('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/23-portfolio-surface-ci-cd/23-HUMAN-UAT.md` exists in the established format and carries every row from 23-VALIDATION's Manual-Only table.
    - The D-17 GHCR item names the exact steps (visibility flip, then `docker logout ghcr.io` + `docker pull`) and states plainly that CICD-03's "pullable with zero build step" claim is **not yet true** until it is done.
    - Items already completed during the phase (the Pages toggle, the demo lines) are recorded as **done with evidence** (run URLs, provenance), not left pending out of habit.
    - If the verbatim demo lines were never supplied, PORT-02 is recorded as **incomplete** — not passed.
    - `.planning/REQUIREMENTS.md` traceability rows for PORT-01…04 and CICD-02/03 reflect their true state, including anything honestly still blocked on a human.
    - Final gates all green: `ruff check . && ruff format --check .`, `pytest -q` (full suite), `gh run list --branch main --workflow CI --limit 1` success at HEAD, and `gh run list --workflow "Deploy Pages" --limit 1` success.
    - The live landing page and the README tell the same story, with the architecture detail living in exactly one of them (D-05).
  </acceptance_criteria>

  <done>Every claim this phase makes is either proven or honestly marked as not-yet-true. Nothing is marked complete merely because the code was written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `README.md` (tracked doc) → the drift guard | For the first time, a tracked doc carries the canonical OAuth2 URL. Phase 22's guard stops being vacuous and starts enforcing |
| `docs/demo.gif` → a public, permanently-cached README asset | A binary artifact derived from user-supplied verbatim output, published where it cannot easily be retracted |
| `.planning/PROJECT.md` (internal ledger) → the public README's privacy disclosure | The README's boundary 4 publishes Dexter's memory-scoping behavior. Drift from the ledger makes a public privacy claim false |
| GHCR package visibility | The gap between "the workflow succeeded" and "a stranger can actually pull it" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-DRIFT | Tampering / Repudiation | The README's invite link | mitigate | `README.md` is a tracked `.md` file, so `tests/test_invite_drift_guard.py` scans it. The acceptance criteria require proving the guard is now **non-vacuous** — that it genuinely finds and matches the URL — rather than merely observing it stay green (which it did for free when it scanned nothing) |
| T-23-17 | Tampering | A second URL constructor sneaking into `scripts/render_demo_gif.py` | mitigate | `test_logic_invite_is_the_only_url_constructor` scans every tracked non-test `.py` file. The plan explicitly forbids the URL literal in the new script — including in a comment, following the Phase 21/22 precedent where even docstring prose tripped these source-inspection guards |
| T-23-HONEST | Spoofing / Repudiation | The README's demo GIF and its caption | mitigate | Mechanically derived from the D-06 mock (one source, two derivatives), so it cannot drift from the landing page's story. Captioned as a reconstruction, never as a screen recording |
| T-23-14 | Repudiation | A false privacy disclosure in the README's boundary 4 | mitigate | Cross-checked line-by-line against `.planning/PROJECT.md` §Key Decisions and required to name `/ask` explicitly as global-but-self-scoped — Phase 21's own SUMMARY flagged that an imprecise row omitting `/ask` would make this public disclosure false |
| T-23-GHCRVIS | Information Disclosure / Availability | GHCR package visibility (D-17) | accept / transfer | Deliberately a human step. Automating it needs a PAT with package-admin scope — a broad new secret in a CI posture whose defining property is that it has none. Transferred to `23-HUMAN-UAT.md` with the honest statement that CICD-03's pull claim is untrue until it happens |
| T-23-18 | Repudiation | Marking a requirement complete because code exists | mitigate | Task 3's closing rule: a requirement is complete when the thing it promises is **true**, not when it is implemented. Anything blocked on a human is recorded as blocked |
| T-23-SC | Tampering | `pip install playwright` (dev machine only) | mitigate | Human-verified in plan 23-02 Task 1 against `pypi.org/project/playwright` (`[ASSUMED]` disposition — slopcheck did not run). Never enters `requirements.txt`, `requirements-dev.txt`, CI, or the Docker image; asserted by grep |
</threat_model>

<artifacts_this_phase_produces>
**Created by this plan:**
- `scripts/render_demo_gif.py` — Playwright video capture of the built mock + two-pass ffmpeg palette conversion; dev-machine only, documented one-time setup, never in CI
- `docs/demo.gif` — the committed, generated-once README demo (≤2MB), derived from `DemoMock.astro` (D-07)
- `.planning/phases/23-portfolio-surface-ci-cd/23-HUMAN-UAT.md` — the acknowledged-deferred ledger (D-17 GHCR flip, Pages toggle, README/mermaid render, landing-page review, demo-line provenance)

**Modified by this plan:**
- `README.md` — full rewrite, 2 lines → architecture case study (PORT-03 + the README half of PORT-04)
- `.planning/REQUIREMENTS.md` — traceability rows updated to their true state

**External state changed:**
- Phase 22's `test_invite_drift_guard.py` becomes **non-vacuous** — a tracked doc now carries the canonical URL and the guard enforces it. The IN-01 concern is closed from both directions: the source-doc guard finally has something to guard, and the D-02 artifact scan covers the built site that the source guard structurally cannot see
</artifacts_this_phase_produces>

<verification>
- `docs/demo.gif` exists, ≤2MB, shows the same conversation as the landing page's mock
- `playwright` in neither requirements file; no CI workflow references it
- `README.md`: 5 badges (no license badge), mermaid diagram, 3–4 callouts, 4 boundaries, canonical invite link
- `pytest tests/test_invite_drift_guard.py -q` green **and proven non-vacuous** (README.md is in the scanned set and the URL is found there)
- `ruff check . && ruff format --check .` exit 0; `pytest -q` full suite green
- CI green at HEAD; Pages deploy green; live page and README consistent
- `23-HUMAN-UAT.md` written; REQUIREMENTS.md traceability honest
</verification>

<success_criteria>
The README is an architecture case study a senior engineer would respect — real badges, a real diagram, four real hard problems, four honestly-disclosed boundaries. The demo GIF is the landing page's demo, mechanically derived. The invite link in the README is byte-identical to the one the bot hands out, and a test now proves it. Everything still blocked on a human is written down as blocked, not quietly marked done.
</success_criteria>

<output>
Create `.planning/phases/23-portfolio-surface-ci-cd/23-07-SUMMARY.md` when done. Record: the GIF's final fps/width/size, the drift-guard non-vacuity proof, the boundary-4-vs-PROJECT.md cross-check, and the true state of every one of the six phase requirements.
</output>
