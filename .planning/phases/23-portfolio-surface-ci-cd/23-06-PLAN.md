---
phase: 23-portfolio-surface-ci-cd
plan: 06
type: execute
wave: 6
depends_on: ["23-02", "23-05"]
files_modified:
  - site/src/data/demo-transcript.ts
  - site/src/components/DemoMock.astro
  - site/src/components/DemoMessage.astro
  - site/src/components/Features.astro
  - site/src/components/FeatureCard.astro
  - site/src/components/Boundaries.astro
  - site/src/components/BoundaryItem.astro
  - site/src/pages/index.astro
autonomous: true
requirements: [PORT-01, PORT-02, PORT-04]
user_setup: []

must_haves:
  truths:
    - "D-06: the demo is an HTML/CSS mock of a Discord conversation whose Dexter lines are VERBATIM, UNEDITED real output — copied byte-for-byte from 23-DEMO-TRANSCRIPT.md, never authored, never 'improved'."
    - "D-06: the page never claims or implies the demo is a screen recording. The caption 'real words. reconstructed pixels.' is the one required acknowledgment."
    - "D-05: section order is hero → demo → features → boundaries → CTA → footer. The demo lands HIGH (show, then tell), and PORT-04's boundaries sit immediately BEFORE the closing CTA — so a visitor reads 'this bot is offline unless the owner is running it' before clicking Add-to-Discord, not after."
    - "D-10/PORT-04: all four boundaries appear on the landing page as a real section, each framed as constraint → deliberate decision. Disclosure that reads as judgment, not apology."
    - "PORT-04: boundary 4's memory-scoping copy describes what Phase 21 ACTUALLY SHIPPED (PROJECT.md §Key Decisions), not the hypothesis."
    - "UI-SPEC: the demo mock honors prefers-reduced-motion by rendering all four messages fully revealed and static — losing the timing, never the content."
    - "D-06 honest cost, disclosed rather than hidden: the mock cannot show the five-button now-playing panel, so the feature list names that gap out loud."
  artifacts:
    - path: "site/src/data/demo-transcript.ts"
      provides: "The four-message transcript; Dexter's two lines verbatim from the user"
      contains: "demoTranscript"
    - path: "site/src/components/DemoMock.astro"
      provides: "The CSS-animated Discord-conversation reproduction + the honesty caption + the sr-only transcript"
      contains: "reconstructed pixels"
    - path: "site/src/components/Boundaries.astro"
      provides: "PORT-04's four boundaries on the landing page, positioned before the CTA"
      min_lines: 20
    - path: "site/src/pages/index.astro"
      provides: "The locked single-scroll composition (D-05)"
      contains: "Boundaries"
  key_links:
    - from: "site/src/data/demo-transcript.ts"
      to: ".planning/phases/23-portfolio-surface-ci-cd/23-DEMO-TRANSCRIPT.md"
      via: "byte-for-byte copy of the user-supplied lines"
      pattern: "verbatim"
    - from: "site/src/components/Boundaries.astro"
      to: ".planning/PROJECT.md Key Decisions"
      via: "boundary 4 describes the shipped hybrid memory scoping"
      pattern: "ask"
---

<objective>
Land the three content sections — the demo mock (PORT-02), the feature showcase (PORT-01), and the honest boundaries (PORT-04) — and compose the single-scroll page in its locked order (D-05).

Purpose: two things in this plan carry the whole phase's credibility.

**The demo's honesty rests entirely on the words being real.** D-06's mock is a legitimate convention — documentation sites reproduce UIs in HTML constantly — *because and only because* the transcript is verbatim Dexter output. Reconstructing the pixels is honest. Writing the roast lines ourselves would make the "personality demo" a showcase of an executor's copywriting while implying it is Gemini's generation, on a page whose entire thesis is honest disclosure. If a line seems flat, that is what the bot actually said. Ship it flat.

**PORT-04 is the page's spine, not its apology.** The four boundaries are framed constraint → deliberate decision, and they sit immediately before the closing CTA on purpose: the hosting caveat has a real user-facing consequence, and a visitor should never invite a bot that is usually offline without having been told first.

Output: the page a recruiter actually reads.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-portfolio-surface-ci-cd/23-UI-SPEC.md
@.planning/phases/23-portfolio-surface-ci-cd/23-CONTEXT.md
@.planning/phases/23-portfolio-surface-ci-cd/23-DEMO-TRANSCRIPT.md
@.planning/PROJECT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The demo mock (PORT-02 / D-06) — verbatim words, reconstructed pixels</name>

  <read_first>
    - `.planning/phases/23-portfolio-surface-ci-cd/23-DEMO-TRANSCRIPT.md` — **the source of truth.** The user's real Dexter lines, supplied in plan 23-02. Copy byte-for-byte. If this file still carries `{{DEXTER_DEMO_LINE_*}}` tokens or a `BLOCKED` status, apply the stop-rule below
    - `.planning/phases/23-portfolio-surface-ci-cd/23-UI-SPEC.md` §"Demo Mock Spec" — **in full.** Structure, message-row anatomy, the placeholder contract, the 16s CSS keyframe timeline with its per-element delay offsets, and the mandatory `prefers-reduced-motion` block
    - `.planning/phases/23-portfolio-surface-ci-cd/23-UI-SPEC.md` §"Honesty & Branding Guardrails" — never imply a screen recording; never reproduce Discord's actual logo/mascot/badge assets; favor the generic chat-UI convention over a pixel-accurate copy when in doubt
    - `.planning/phases/23-portfolio-surface-ci-cd/23-UI-SPEC.md` §"Accessibility Contract" → the decorative-fake-UI pattern — `role="img"` + `aria-labelledby` on the wrapper, `aria-hidden="true"` on the visual rows, and an `.sr-only` plain-text transcript so a screen-reader user gets **the joke**, not merely the news that a demo exists
    - `.planning/phases/23-portfolio-surface-ci-cd/23-CONTEXT.md` §D-06 in full — including its two plainly-stated costs
    - `.planning/phases/23-portfolio-surface-ci-cd/23-RESEARCH.md` §Finding 1 — why `logs/dexter.log` could not supply these lines, and why the `personality/` template pools must not be silently substituted for them
  </read_first>

  <files>site/src/data/demo-transcript.ts, site/src/components/DemoMock.astro, site/src/components/DemoMessage.astro</files>

  <action>
    **The hard rule, before anything else: do not write, edit, tighten, punch up, re-punctuate, or "improve" either Dexter line. Not one character.** Copy them byte-for-byte out of `23-DEMO-TRANSCRIPT.md`, preserving the lowercase, the punctuation, any emoji, any awkwardness. A slightly flat real line is the honest artifact; a perfect invented line is a fabrication on the page that promises honest disclosure. This is not a style preference — it is the entire legitimacy of PORT-02.

    **Stop-rule.** If `23-DEMO-TRANSCRIPT.md` is missing, still tokenized, or marked `BLOCKED`: build the component fully with the `{{DEXTER_DEMO_LINE_1}}` / `{{DEXTER_DEMO_LINE_2}}` tokens left **visibly intact**, and report **PORT-02 as incomplete** — not "complete with placeholder styling." Do **not** fill them with plausible roasts, and do **not** substitute strings from `personality/responses.py` or `personality/roasts.py`. RESEARCH Finding 1 warns against that substitution explicitly: those are real shipped strings, but they are *template fallbacks*, not Gemini generations, so using them silently reopens the exact provenance question D-06 settled. Carry it as a `23-HUMAN-UAT.md` item instead.

    **`site/src/data/demo-transcript.ts`** — the four-message array in the shape UI-SPEC's placeholder contract defines, with its `DO NOT AUTHOR` header comment carried over verbatim. The transcript lives in its own data file rather than inline in the component so that a future line-swap is a single-file edit with zero markup risk. The human handle (`wrenlow`) and the two human setup lines **are** ours to author — they are not attributed to Dexter and claim to be nothing. If the user's real lines fit a different setup better, **change the setup lines**; never bend a real Dexter line to fit a fake human one. If a supplied line contains a real Discord handle, replace that handle with `wrenlow` and note the substitution in the SUMMARY.

    **`DemoMessage.astro`** — one message row: a CSS-circle avatar (no image asset), username plus a generic "BOT" pill on Dexter's rows only, a timestamp (hidden below 480px), and a flat message bubble set in **sans**. Sans is deliberate: Discord's own UI is sans-serif, so it reads as an authentic UI reproduction rather than a stylized illustration — which is exactly what D-06's honesty framing needs. No Discord logo, no mascot, no reproduction of Discord's actual badge asset.

    **`DemoMock.astro`** — the window-chrome frame, four `DemoMessage` rows, two typing indicators, and the caption **"real words. reconstructed pixels."** below the frame. That caption is the one required on-page acknowledgment. The page must never call this a recording, a video, or footage.

    Animation: a single 16s infinite CSS keyframe timeline with per-element `animation-delay` offsets, staged exactly as UI-SPEC specifies. Zero JS. Under `prefers-reduced-motion`, all four messages render immediately in their final revealed state and the typing indicators do not render — a reduced-motion visitor loses the timing, never a word of the content.

    Accessibility: the wrapper is announced once as an image-like unit (`role="img"` + `aria-labelledby`); the visual rows are `aria-hidden="true"`; an `.sr-only` transcript carries the actual dialogue. Reuse the `.sr-only` utility from `global.css` — do not hand-roll a second variant.
  </action>

  <verify>
    <automated>cd site && npm run build && node -e "const h=require('fs').readFileSync('dist/index.html','utf8'); if(/DEXTER_DEMO_LINE/.test(h)) throw new Error('PORT-02 INCOMPLETE: placeholder tokens in built output'); if(!/reconstructed pixels/.test(h)) throw new Error('missing honesty caption'); if(/screen.?record|screencast|footage/i.test(h)) throw new Error('page implies a recording'); if(!/role=.img./.test(h)) throw new Error('demo mock not announced as an image-like unit'); console.log('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - `site/src/data/demo-transcript.ts`'s two Dexter strings are **byte-identical** to the lines in `23-DEMO-TRANSCRIPT.md` (diff them explicitly; do not eyeball).
    - No `{{DEXTER_DEMO_LINE_*}}` token survives into `site/dist/index.html`. If one does, PORT-02 is reported **incomplete** and the stop-rule was applied.
    - No string from `personality/responses.py` or `personality/roasts.py` appears in `demo-transcript.ts` (cross-check the two Dexter lines against those pools).
    - `site/dist/index.html` contains the caption "real words. reconstructed pixels." and contains **no** occurrence of "recording", "screencast", "footage", or "video" describing the demo.
    - `grep -ic 'discord' site/src/components/DemoMock.astro site/src/components/DemoMessage.astro` shows Discord referenced only in prose/labels — no logo, mascot, SVG, or brand asset is imported.
    - The demo mock wrapper carries `role="img"` + `aria-labelledby`; the visual rows carry `aria-hidden="true"`; an `.sr-only` transcript containing both Dexter lines is present in the built HTML.
    - A `prefers-reduced-motion` rule disables the mock's animation and reveals all four messages.
    - `cd site && npm run build` exits 0.
  </acceptance_criteria>

  <done>The demo shows Dexter being Dexter, in his own actual words, with a caption that tells the truth about how the pixels were made.</done>
</task>

<task type="auto">
  <name>Task 2: Features + Boundaries (PORT-01, PORT-04 / D-10) — the showcase, and the disclosure that earns it</name>

  <read_first>
    - `.planning/phases/23-portfolio-surface-ci-cd/23-UI-SPEC.md` §"Copywriting Contract" — **the exact copy for all six features and all four boundaries is already written and voice-checked.** Use it verbatim
    - `.planning/phases/23-portfolio-surface-ci-cd/23-UI-SPEC.md` §"Color" (the disclosure marker hue and why it is deliberately not an alarm-red) and §"Responsive Behavior" (features grid 1/2/3-up; boundaries **always single-column** — a grid would visually trivialize content D-10 wants read one at a time)
    - `.planning/PROJECT.md` §"Key Decisions" — **the authoritative ledger of what actually shipped.** Read the Phase 21 hybrid-memory-scoping row and the Phase 21 guild-purge row in full, plus the parked-24/7-deploy row. Boundary 4's copy must describe the shipped reality, not the hypothesis
    - `.planning/phases/23-portfolio-surface-ci-cd/23-CONTEXT.md` §D-10 — the four boundaries with their actual shipped substance, and the constraint → decision framing
    - `CLAUDE.md` §"Critical Rules" 6 — **dial back sarcasm for serious content.** The boundaries section is serious content. UI-SPEC already wrote it straight; keep it that way
  </read_first>

  <files>site/src/components/Features.astro, site/src/components/FeatureCard.astro, site/src/components/Boundaries.astro, site/src/components/BoundaryItem.astro</files>

  <action>
    **Features** (`Features.astro` + `FeatureCard.astro`): the section shell maps over the six feature objects from UI-SPEC's Copywriting Contract, rendering each through `FeatureCard.astro` (an h3 title + a body blurb). Copy verbatim — it is already in voice.

    Note that **Feature 3 deliberately carries a cost D-06 named out loud**: the mock cannot convincingly show the five-button now-playing panel (the Phase 7 player UI), so the feature list names that gap directly — *"the demo above can't show it — this is the part you're missing."* That is the honest handling of a real limitation, and it must not be quietly softened or dropped. Removing it would hide a gap the phase explicitly chose to disclose.

    **Boundaries** (`Boundaries.astro` + `BoundaryItem.astro`): the PORT-04 section. Four stacked, full-width items at every breakpoint. Each renders a disclosure marker, an h3 constraint label, and body copy in the **constraint → deliberate decision** shape (D-10). This section is visually distinct from Features on purpose — a plain heading with no accent glyph, and the calmer disclosure hue rather than the amber action accent, because disclosure is not a call to action.

    **Voice, and this is load-bearing:** the boundaries section is **not sarcastic**. CLAUDE.md's Critical Rule 6 says dial back the sarcasm for serious content, and D-09's corollary is explicit — *PORT-04's disclosure must read as disclosure, not as a joke.* UI-SPEC's boundary copy is already written straight; ship it straight. A visitor should come away thinking "this engineer knows exactly what they built and what they didn't," not "cute."

    **Boundary 4 (memory scoping) is the one to get factually right**, and it is the one most likely to drift into describing a hypothesis. `.planning/PROJECT.md` §Key Decisions records what Phase 21 actually shipped: guild-scoped recall on `/roast @user`, ambient roasts, proactive callbacks, the music-command callback, and the auto-queue taste blend (all via an explicit per-call-site `guild_scoped=True` opt-in); `/ask` deliberately staying **global but self-scoped** (it recalls only the invoker's own memory, so no cross-user exposure is possible); the legacy `guild_id IS NULL` corpus grandfathered as globally recallable; and guild data purged on removal. UI-SPEC's boundary-4 copy already reflects this — cross-check it against PROJECT.md line by line and fix any drift **toward PROJECT.md**, which is the ledger of record. Getting `/ask` wrong here would make Dexter's public privacy disclosure false, which is precisely the failure PORT-04 exists to prevent.
  </action>

  <verify>
    <automated>cd site && npm run build && node -e "const h=require('fs').readFileSync('dist/index.html','utf8'); const b=['verification','offline unless','kill-switch','memory'].filter(k=>!new RegExp(k,'i').test(h)); if(b.length) throw new Error('missing PORT-04 boundary topic(s): '+b.join(', ')); if(!/\/ask/.test(h)) throw new Error('boundary 4 must name /ask explicitly (MEM-02 shipped reality)'); const cards=(h.match(/feature-card/g)||[]).length; if(cards<6) throw new Error('expected 6 feature cards, found '+cards); console.log('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - Six feature cards render, with copy matching UI-SPEC's Copywriting Contract verbatim.
    - Feature 3 still names the now-playing-panel gap the demo cannot show (the disclosed cost from D-06). It was not softened or removed.
    - Four boundary items render, each with a constraint and a decision — all four topics present in the built HTML: the 100-guild verification wall, the on-demand hosting caveat, the full-savage + kill-switch tradeoff, and the hybrid memory scoping.
    - **Boundary 2 states plainly that the bot is offline unless the owner is running it** — the caveat with a real user-facing consequence.
    - **Boundary 4 names `/ask` explicitly** and describes it as global-but-self-scoped, matching `.planning/PROJECT.md` §Key Decisions. Diff the claim against PROJECT.md's Phase 21 row and record the check in the SUMMARY.
    - The boundaries section contains no joke, no dry one-liner, and no sarcasm (Critical Rule 6). Read it back and confirm it reads as an engineer's disclosure.
    - Boundaries render single-column at every breakpoint; features render 1/2/3-up per UI-SPEC's breakpoints.
    - `cd site && npm run build` exits 0.
  </acceptance_criteria>

  <done>The page shows what Dexter does — and tells the truth about what it doesn't, in a voice that reads as judgment rather than apology.</done>
</task>

<task type="auto">
  <name>Task 3: Compose index.astro in the locked order (D-05) and verify the whole page end to end</name>

  <read_first>
    - `.planning/phases/23-portfolio-surface-ci-cd/23-CONTEXT.md` §D-05 — the section order and, specifically, **why** the boundaries sit immediately before the closing CTA: a visitor must read the hosting caveat *before* clicking Add-to-Discord, not after being disappointed
    - `.planning/phases/23-portfolio-surface-ci-cd/23-UI-SPEC.md` §Layout ("Section order (locked, D-05, do not reorder)") and §Accessibility Contract (landmarks, `aria-labelledby` per section, heading hierarchy with no skipped levels)
    - `site/src/pages/index.astro` — the minimal version from plan 23-03, which this task replaces
  </read_first>

  <files>site/src/pages/index.astro</files>

  <action>
    Compose the single scroll: **Hero → DemoMock → Features → Boundaries → Cta → Footer**, inside `Layout.astro`. That order is locked (D-05) — do not reorder, and do not add sections.

    In particular: **no on-page architecture section.** D-05 rules it out deliberately, because the architecture story lives in the README case study (PORT-03) and two copies of it on two surfaces is a drift waiting to happen. The page shows and links; the README explains. Every duplication this phase avoids is a drift it cannot suffer.

    Give the demo section the `id="demo"` the hero's "see it in action ↓" micro-link anchors to. Each major section is a `<section>` with `aria-labelledby` pointing at its own heading's `id`. Section backgrounds alternate per UI-SPEC (Hero dominant, Demo secondary, Features dominant, Boundaries secondary, CTA dominant, Footer secondary) so the scroll separates without a divider on every seam.

    Then verify the assembled page as a whole, not just as parts: the drift scan, the heading hierarchy, the subpath-correctness of every asset, and the total page weight against UI-SPEC's budget.
  </action>

  <verify>
    <automated>cd site && npm run build && cd .. && SITE_DIST_REQUIRED=1 pytest tests/test_site_drift_guard.py tests/test_invite_drift_guard.py -q && node -e "const h=require('fs').readFileSync('site/dist/index.html','utf8'); const order=['id=\"demo\"','feature','boundar','cta'].map(k=>h.toLowerCase().indexOf(k.toLowerCase())); if(order.some(i=>i<0)) throw new Error('a required section is missing'); for(let i=1;i<order.length;i++) if(order[i]<order[i-1]) throw new Error('D-05 section order violated'); if((h.match(/<h1/g)||[]).length!==1) throw new Error('exactly one h1 required'); console.log('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - The built page's section order is Hero → Demo → Features → Boundaries → CTA → Footer. **The boundaries section appears before the closing CTA in document order** (D-05's whole point — asserted, not assumed).
    - Exactly one `<h1>`; `<h2>` for each remaining section heading; `<h3>` for feature/boundary titles; no heading level skipped.
    - The demo section has `id="demo"` and the hero's micro-link resolves to it.
    - No architecture section exists on the page (D-05 — it lives in the README only).
    - The canonical invite URL still appears exactly twice in the built HTML; `SITE_DIST_REQUIRED=1 pytest tests/test_site_drift_guard.py tests/test_invite_drift_guard.py -q` is green.
    - No root-absolute path escapes the `/dexter/` base in the built HTML (re-assert plan 23-05's Pitfall-1 gate against the now-complete page).
    - Total HTML + CSS weight (excluding fonts) is under UI-SPEC's ~50KB budget, or the overage is recorded with a reason.
    - `pytest -q` full suite green; commit and push to `main`; `gh run list --workflow "Deploy Pages" --limit 1` reports success and the live page at `https://jadrianports.github.io/dexter` renders all six sections.
  </acceptance_criteria>

  <done>The landing page is complete, live, in the right order, and still byte-verified against the one legal invite URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied Dexter lines → public landing-page copy | Text the user pasted becomes a public claim about what the product says. Its provenance is the demo's only warrant |
| `.planning/PROJECT.md` (internal ledger) → public privacy disclosure | Boundary 4 publishes the shipped memory-scoping behavior. If the copy drifts from the ledger, Dexter's public privacy claim becomes false |
| built `dist/*.html` → GitHub Pages → recruiter's browser | The complete page, with both invite anchors, now carries real content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-HONEST | Spoofing / Repudiation | The demo transcript (PORT-02) | mitigate | Byte-for-byte copy from `23-DEMO-TRANSCRIPT.md`, diffed not eyeballed; an explicit stop-rule forbids authoring lines or substituting `personality/` template strings; the built HTML is asserted to contain no surviving placeholder token and no claim of being a recording |
| T-23-13 | Spoofing | Implying the mock is a screen recording | mitigate | The required caption "real words. reconstructed pixels." is asserted present, and the built HTML is asserted to contain no "recording"/"screencast"/"footage" claim. No Discord brand asset is used anywhere |
| T-23-14 | Repudiation | A **false privacy disclosure** in boundary 4 | mitigate | Boundary 4's copy is cross-checked line-by-line against `.planning/PROJECT.md` §Key Decisions (the shipped-reality ledger), and `/ask` is asserted present by name — Phase 21's own SUMMARY warned that an imprecise row omitting `/ask` would make this public disclosure false |
| T-23-15 | Repudiation | Quietly dropping a disclosed limitation | mitigate | Feature 3's explicit naming of the now-playing-panel gap (a cost D-06 recorded) is an acceptance criterion — it cannot be silently softened away, because the honest handling of that gap is part of what makes the mock legitimate |
| T-23-DRIFT | Tampering | The invite URL, now surrounded by real content | mitigate | The D-02 scan runs in this plan's verify and in CI on every push. The URL still appears exactly twice, still byte-matched to `build_invite_url()` |
| T-23-16 | Information Disclosure | A real Discord handle leaking into the public demo | mitigate | The human speaker is the fictional `wrenlow`; any real handle in a user-supplied line is replaced at copy time and the substitution recorded |
| T-23-SC | Tampering | npm/pip installs | n/a | This plan installs nothing new — it authors `.astro`/`.ts`/CSS inside the already-verified toolchain from plan 23-03 |
</threat_model>

<artifacts_this_phase_produces>
**Created by this plan:**
- `site/src/data/demo-transcript.ts` — the four-message array; Dexter's two lines verbatim from the user (D-06)
- `site/src/components/DemoMock.astro` — window chrome, 16s CSS timeline, typing indicators, the honesty caption, the `.sr-only` transcript
- `site/src/components/DemoMessage.astro` — one message row (CSS avatar, BOT pill, timestamp, flat bubble)
- `site/src/components/Features.astro`, `site/src/components/FeatureCard.astro` — the six-feature showcase (PORT-01)
- `site/src/components/Boundaries.astro`, `site/src/components/BoundaryItem.astro` — PORT-04's four boundaries, constraint → decision

**Modified by this plan:**
- `site/src/pages/index.astro` — the locked single-scroll composition (D-05)

**External state changed:**
- The complete landing page is live at `https://jadrianports.github.io/dexter` (PORT-01, PORT-02, PORT-04)

**Consumed downstream:**
- The built `DemoMock` → `scripts/render_demo_gif.py` records it into `docs/demo.gif` (plan 23-07, D-07 — one source, two derivatives)
- The boundary copy → expanded into the README's fuller PORT-04 section (plan 23-07, D-10)
</artifacts_this_phase_produces>

<verification>
- `cd site && npm run build` exits 0; no `{{DEXTER_DEMO_LINE_*}}` token survives
- The two Dexter lines are byte-identical to `23-DEMO-TRANSCRIPT.md` (diffed)
- All four PORT-04 boundaries present; boundary 4 names `/ask` and matches PROJECT.md
- Section order asserted: demo → features → boundaries → CTA
- `SITE_DIST_REQUIRED=1 pytest tests/test_site_drift_guard.py tests/test_invite_drift_guard.py -q` green; canonical URL appears exactly twice
- `pytest -q` full suite green; Pages deploy green; live page renders all six sections
</verification>

<success_criteria>
The landing page is complete and live. The demo shows real Dexter output and says so. The four boundaries read as deliberate engineering judgment, sit before the CTA, and describe what actually shipped. Nothing on the page claims more than it can back.
</success_criteria>

<output>
Create `.planning/phases/23-portfolio-surface-ci-cd/23-06-SUMMARY.md` when done. Record: the verbatim-lines diff result, the boundary-4-vs-PROJECT.md cross-check, and the live Pages URL.
</output>
