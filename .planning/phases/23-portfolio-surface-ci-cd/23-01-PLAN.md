---
phase: 23-portfolio-surface-ci-cd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cogs/events.py
  - tests/test_guild_config_logic.py
  - tests/test_memory.py
  - .github/workflows/ci.yml
autonomous: true
requirements: [PORT-03, CICD-02, CICD-03]
user_setup: []

must_haves:
  truths:
    - "D-13: origin/main is no longer parked at Phase 18 — every commit of Phases 19, 20, 21, 22 is on GitHub."
    - "D-13: a CI workflow run whose head SHA equals local HEAD has concluded `success` on `main` — the badge PORT-03 will publish reports on the code actually being promoted, not on four-phase-old code."
    - "D-13 (corrected premise): the retired davey/PyNaCl Pitfall-7 warning no longer appears in ci.yml — the risk it describes did not materialize on the real run."
    - "The ~124 DB-gated tests (incl. Phase 21's memory-scoping surgery) have executed for the first time against a real pgvector container and passed."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "The existing Phase 18 gate, with the stale Pitfall-7 comment retired"
      contains: "permissions:"
  key_links:
    - from: "local HEAD"
      to: "origin/main"
      via: "git push"
      pattern: "git rev-list --count origin/main..HEAD == 0"
    - from: "origin/main HEAD commit"
      to: "a green GitHub Actions CI run"
      via: "push-triggered ci.yml"
      pattern: "gh run list --branch main --workflow CI"
---

<objective>
Discharge D-13: get 147 unpushed commits (Phases 19–22) onto `origin/main`, through the existing CI gate, and green — **before a single line of Phase 23 code is written**.

Purpose: every recruiter-facing claim this phase makes is a claim about GitHub. A CI badge, a Pages deploy, and a GHCR image are all worthless if `origin/main` is parked at Phase 18's tip (2026-07-09). More practically: four unexercised phases going through a gate for the first time is a realistic red. D-13 sequences that red **here**, where it is an isolated failure — not tangled with a brand-new Astro build job, a Pages deploy, and a multi-arch Docker publish.

Output: `origin/main == HEAD`, a green CI run recorded at that SHA, and a stale warning comment removed from `ci.yml`.

**Blocking:** every other plan in Phase 23 depends on this. Do not start plan 23-02 until the CI run at HEAD is green.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-portfolio-surface-ci-cd/23-CONTEXT.md
@.planning/phases/23-portfolio-surface-ci-cd/23-RESEARCH.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear the two known-red pre-push conditions (ruff format drift + the retired Pitfall-7 comment)</name>

  <read_first>
    - `.planning/phases/23-portfolio-surface-ci-cd/23-CONTEXT.md` — D-13 in full, **including the CORRECTION block** (CI HAS run and gone green at Phase 18; the "never run" claim was retracted; the davey/PyNaCl risk is RETIRED)
    - `.planning/phases/23-portfolio-surface-ci-cd/23-RESEARCH.md` — Finding 6 (the exact three files `ruff format --check` fails on) and Pitfall 7 (why this must be fixed *before* the push, not diagnosed after it)
    - `.github/workflows/ci.yml` — read the whole file; the Pitfall-7 comment block sits between the `setup-python` step and the `pip install` steps
  </read_first>

  <files>cogs/events.py, tests/test_guild_config_logic.py, tests/test_memory.py, .github/workflows/ci.yml</files>

  <action>
    Two mechanical, pre-push repairs. Both exist so the first real CI run of Phases 19–22 fails (if it fails at all) for a *real* reason, not a known one.

    1. Run `ruff format .` at the repo root. RESEARCH Finding 6 verified — and this session re-confirmed — that exactly three files are reformatted: `cogs/events.py`, `tests/test_guild_config_logic.py`, `tests/test_memory.py`. The diff is purely mechanical line-wrapping. **Review the diff and confirm it is whitespace-only** — if `ruff format` changes anything that is not whitespace/line-wrapping in those three files, STOP and surface it; that would mean the formatter version drifted, which is a different problem.

    2. In `.github/workflows/ci.yml`, delete the `NOTE (Pitfall 7)` comment block (the six-line comment warning that the native `davey`/PyNaCl install may fail on the first real run and suggesting a conditional `build-essential`/`libsodium-dev` step). Per D-13's CORRECTION block, that risk is **retired** — the install worked on the GitHub runner across three real runs. Leaving a stale warning in the gate file implies a live risk that already didn't materialize. Delete the comment only; **do not touch the `pip install` steps themselves, the service container, the `TEST_DATABASE_URL` env, the triggers, or the `permissions:` ceiling** — this file is the gate that the rest of the phase hangs off, and it stays otherwise byte-identical in this task.

    Commit both changes together as the last pre-push commit.
  </action>

  <verify>
    <automated>ruff format --check . && ruff check . && grep -c "Pitfall 7" .github/workflows/ci.yml; test $? -ne 0</automated>
  </verify>

  <acceptance_criteria>
    - `ruff format --check .` exits 0 (was: "3 files would be reformatted").
    - `ruff check .` exits 0 (was already clean — must stay clean).
    - `grep -c "Pitfall 7" .github/workflows/ci.yml` finds zero matches.
    - `grep -c "libsodium-dev" .github/workflows/ci.yml` finds zero matches.
    - `git diff HEAD~1 --stat` shows exactly 4 files changed.
    - `.github/workflows/ci.yml` still contains `permissions:` with `contents: read`, still contains `pull_request:` and **not** `pull_request_target`, and still contains `pgvector/pgvector:pg16` — the gate's safety properties are unchanged.
  </acceptance_criteria>

  <done>The repo is lint-clean and format-clean at HEAD, and ci.yml carries no retracted warning.</done>
</task>

<task type="auto">
  <name>Task 2: Push Phases 19–22 to origin/main, watch the run, and repair to green (D-13)</name>

  <read_first>
    - `.planning/phases/23-portfolio-surface-ci-cd/23-RESEARCH.md` — Finding 6 (full local suite is **1036 passed / 124 skipped / 0 failed** at HEAD; the 124 skips are the DB-unavailable fixture path and are exactly the slice CI will execute for the first time) and Open Question 6 (the named residual: Phase 21's memory-scoping surgery lives in that DB-gated slice)
    - `tests/conftest.py` — the `pool` fixture's skip-on-connection-error behavior, so a CI failure in that slice can be read correctly
    - `.github/workflows/ci.yml` — the `TEST_DATABASE_URL` env and the pgvector service container that unskip those tests in CI
  </read_first>

  <files>(no repo files modified unless a CI repair is required)</files>

  <action>
    Push, watch, repair. This is the phase's hard prerequisite.

    Push `main` to `origin` (147 commits as of planning; re-check with `git rev-list --count origin/main..HEAD`). Then watch the resulting CI run with `gh run watch`, or poll `gh run list --branch main --workflow CI --limit 1`.

    **CI-repair contingency (D-13's "plan a real repair path" — a red run is a realistic outcome, not a hypothetical).** If the run is red, triage the failure into exactly one of three buckets and act accordingly:

    - **Bucket A — lint/format/tooling drift.** A `ruff check` or `ruff format --check` failure, or a dependency-install failure. Fix mechanically, commit, push, re-watch. Task 1 should have eliminated the known instance of this.
    - **Bucket B — CI-environment-only failure.** A DB-gated test failing against the `pgvector/pgvector:pg16` service container that passes locally, a service-container startup/timing problem, or a missing env var. This is the *expected* risk surface: those ~124 tests have never run against a real Postgres with Phases 19–22's code. Fix the workflow or the test's environment assumption — **never** by weakening or skipping the test. Commit, push, re-watch.
    - **Bucket C — a genuine logic regression in Phases 19–22.** A test failing for a real behavioral reason (highest prior: `services/memory.py::search_memories`/`recall()` — Phase 21's guild-scoping work, the subsystem that produced the Phase 13 CR-01 blocker). **STOP. Do not fix it inline, and do not proceed to plan 23-02.** Surface the failing test, its output, and your diagnosis to the user, and wait. A real regression in shipped code is a bigger event than Phase 23 and must not be repaired inside a portfolio phase's first plan.

    Bound the loop: **at most 3 repair-and-repush iterations.** If the run is still not green after the third, stop and escalate regardless of bucket — something structural is wrong and a fourth blind attempt is not diagnosis.

    Finally, record the green run's head SHA and run URL in the plan SUMMARY. Plan 23-07's CI badge is a claim about *this run*; if the SHA does not equal HEAD, the badge is reporting on something other than the code being promoted, and PORT-03's success criterion is not met.
  </action>

  <verify>
    <automated>test "$(git rev-list --count origin/main..HEAD)" -eq 0 && gh run list --branch main --workflow CI --limit 1 --json conclusion,headSha --jq '.[0] | select(.conclusion=="success" and .headSha=="'"$(git rev-parse HEAD)"'") | .conclusion'</automated>
  </verify>

  <acceptance_criteria>
    - `git rev-list --count origin/main..HEAD` returns `0` — nothing left unpushed.
    - `gh run list --branch main --workflow CI --limit 1 --json conclusion,headSha` reports `conclusion == "success"` **and** `headSha == $(git rev-parse HEAD)`. Both halves matter: a green run at an older SHA does not satisfy D-13.
    - The green run's job log shows the pytest step executing (not skipping) the DB-gated tests — i.e. the run's passed-test count is materially higher than the local 1036, confirming the pgvector service container unskipped the ~124.
    - If any repair commits were made, each is listed in the SUMMARY with its bucket (A/B/C) and a one-line cause.
    - If the run went to Bucket C, this task ends with an escalation to the user and **plan 23-02 has not been started**.
  </acceptance_criteria>

  <done>`origin/main == HEAD`, and the CI run at that exact SHA is green. The Phase 23 baseline is real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → `origin/main` (public repo) | 147 commits, never before seen by GitHub, become public and become the code every downstream Phase 23 artifact describes |
| pushed commit → GitHub Actions runner | The existing `ci.yml` gate executes this code with the default `GITHUB_TOKEN` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Repudiation | The PORT-03 CI badge | mitigate | Task 2 asserts `headSha == HEAD` on the green run, not merely "a green run exists" — a badge reporting on Phase 18 while the page describes Phase 22 would be a false claim on the phase's own honesty-themed surface |
| T-23-02 | Tampering | The `ci.yml` gate itself | mitigate | Task 1 deletes a comment only; acceptance criteria positively re-assert that `permissions: contents: read`, the `pull_request` (never `_target`) trigger, and the pgvector service container all survive unchanged |
| T-23-03 | Information Disclosure | The 147 commits becoming public | accept | The repo is already public and every commit was authored under the project's existing no-secrets-in-git discipline (`.env` is gitignored; `config.py`'s `DISCORD_CLIENT_ID` is a deliberately public constant per Phase 22 D-04). No new exposure is created by pushing — the exposure decision was made when the repo was made public |
| T-23-04 | Denial of Service | The repair loop | mitigate | Bounded at 3 iterations, with Bucket C (real regression) escalating immediately rather than being patched under portfolio-phase pressure |
| T-23-SC | Tampering | npm/pip installs | n/a | This plan installs nothing. The package-legitimacy gate for `astro` and `playwright` lives in plan 23-02, before the first `npm install`/`pip install` of the phase |
</threat_model>

<artifacts_this_phase_produces>
**Created by this plan:**
- (no new files)

**Modified by this plan:**
- `cogs/events.py`, `tests/test_guild_config_logic.py`, `tests/test_memory.py` — `ruff format` whitespace only
- `.github/workflows/ci.yml` — retired Pitfall-7 comment block deleted

**External state changed (the real deliverable):**
- `origin/main` advanced from Phase 18's tip to HEAD (Phases 19–22 published)
- A green GitHub Actions CI run exists at `main`'s HEAD SHA — the run the PORT-03 badge will point at, and the run `pages.yml` (plan 23-04) will gate on via `workflow_run`
</artifacts_this_phase_produces>

<verification>
- `git rev-list --count origin/main..HEAD` == 0
- `gh run list --branch main --workflow CI --limit 1` → success at HEAD's SHA
- `ruff check . && ruff format --check .` exit 0
- `grep "Pitfall 7" .github/workflows/ci.yml` → no matches
</verification>

<success_criteria>
Phases 19–22 are on GitHub, the existing gate has run against them, and it is green at exactly the commit the rest of Phase 23 will build on. The stale davey/PyNaCl warning is gone. Nothing else in the repo changed.
</success_criteria>

<output>
Create `.planning/phases/23-portfolio-surface-ci-cd/23-01-SUMMARY.md` when done. Record the green run URL and head SHA verbatim — plan 23-07 cites them.
</output>

