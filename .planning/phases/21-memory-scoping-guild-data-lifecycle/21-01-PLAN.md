---
phase: 21-memory-scoping-guild-data-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database.py
  - services/memory.py
  - tests/test_memory.py
autonomous: true
requirements: [MEM-02, MEM-03, MEM-05]
must_haves:
  truths:
    - "database.search_memories(guild_id='G') narrows the ANN read to that guild's rows PLUS the grandfathered guild_id IS NULL corpus (D-01)"
    - "database.search_memories(guild_id=None) emits SQL byte-identical to pre-Phase-21 — no clause, no param, no numbering shift"
    - "MemoryService.recall() defaults to global recall; a caller must explicitly opt in via guild_scoped=True (MEM-02: /ask stays global)"
    - "remember()'s k=1 dedup search never receives a guild kwarg — the Phase 13 CR-01 scarred path is byte-identical (MEM-05, D-02)"
    - "WHERE user_id = $1 survives verbatim in every guild-scoped SQL output — the guild clause narrows within it, never replaces or OR-widens it"
  artifacts:
    - path: "database.py"
      provides: "search_memories() optional guild_id kwarg + dynamic $N placeholder numbering"
      contains: "OR guild_id IS NULL"
    - path: "services/memory.py"
      provides: "MemoryService.recall() keyword-only guild_scoped: bool = False opt-in"
      contains: "guild_scoped"
    - path: "tests/test_memory.py"
      provides: "TestSearchMemoriesGuildFilter (4 kind x guild combos) + TestRecallGuildScoped + TestRememberDedupCallShapeUnchanged"
      contains: "class TestSearchMemoriesGuildFilter"
  key_links:
    - from: "services/memory.py::recall"
      to: "database.search_memories"
      via: "guild_id kwarg forwarded ONLY when guild_scoped is True"
      pattern: "guild_scoped"
    - from: "services/memory.py::remember"
      to: "database.search_memories"
      via: "k=1 dedup search — UNCHANGED, never passes guild_id or kind"
      pattern: "search_memories\\(\\s*self\\._pool,\\s*user_id=user_id,\\s*query_embedding=fact_vec,\\s*k=1,?\\s*\\)"
---

<objective>
Build the read-path scoping substrate: `database.search_memories()` gains an optional guild filter
emitting `(guild_id = $N OR guild_id IS NULL)` (D-01 grandfather rule), and
`MemoryService.recall()` gains an explicit keyword-only `guild_scoped` opt-in (D-02, read path
ONLY). No call site changes behavior yet — plan 21-03 wires the opt-ins.

Purpose: MEM-03's tested backward-compat rule and the MEM-02 "byte-identical for non-opting
callers" guarantee both live here. This is also where MEM-05's regression lock lands: the plan
proves, structurally, that `remember()`'s CR-13-01-scarred k=1 dedup search never sees the new
parameter.

Output: the guild clause, the `guild_scoped` kwarg, and three new test classes in
`tests/test_memory.py`.

Per D-04: the descope gate does NOT fire — research verified all three tripwires against literal
code. Build the full hybrid scoping; do not build the "keep memory global + disclose" fallback.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-memory-scoping-guild-data-lifecycle/21-CONTEXT.md
@.planning/phases/21-memory-scoping-guild-data-lifecycle/21-RESEARCH.md
@.planning/phases/21-memory-scoping-guild-data-lifecycle/21-PATTERNS.md
@.planning/phases/21-memory-scoping-guild-data-lifecycle/21-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan produces (later plans consume these — do not rename):

| Symbol | Location | Signature / Shape |
|---|---|---|
| `search_memories` guild filter | `database.py` | new keyword-only `guild_id: str \| None = None`; emits `AND (guild_id = $N OR guild_id IS NULL)` only when not-None |
| `recall` guild opt-in | `services/memory.py::MemoryService.recall` | new keyword-only `guild_scoped: bool = False` (appended after `kind`, behind a bare `*`) |
| `TestSearchMemoriesGuildFilter` | `tests/test_memory.py` | fake-pool SQL-string assertions, all 4 kind x guild combinations |
| `TestRecallGuildScoped` | `tests/test_memory.py` | asserts `guild_id` kwarg is FORWARDED when opted in and ABSENT when not |
| `TestRememberDedupCallShapeUnchanged` | `tests/test_memory.py` | MEM-05 structural lock — strict stub with no `guild_id`/`kind` params |

Later plans depend on: the exact kwarg names `guild_id=` (on `search_memories`) and
`guild_scoped=` (on `recall`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: search_memories gains the optional guild clause with dynamic $N numbering (MEM-03 / D-01)</name>

  <files>database.py, tests/test_memory.py</files>

  <read_first>
    - `database.py:1333-1391` — the CURRENT `search_memories`. Note `kind_clause = " AND kind = $3"`
      HARDCODES `$3`, and `params` is built with a list-concat expression. Both must change.
    - `21-PATTERNS.md` § "`database.py::search_memories` (:1333) — gains second optional clause" —
      the exact correct dynamic-numbering form and the four combinations to test.
    - `21-RESEARCH.md` § "Pitfall 1" — why the hardcoded `$3` breaks once a second optional clause
      exists.
    - `tests/test_memory.py:544-608` — `_FakeConn` / `_FakePoolCM` / `_FakePool` helpers and the
      existing `TestSearchMemoriesKindFilter` class. This class is the structural template for the
      new one and MUST keep passing UNMODIFIED (it hardcodes the literal string `"AND kind = $3"`).
  </read_first>

  <behavior>
    - `search_memories(user_id, query_embedding, k)` → no optional clause; params `(u, emb, k)`.
      SQL byte-identical to pre-Phase-21.
    - `search_memories(..., kind="taste_episode")` → `AND kind = $3`; params `(u, emb, "taste_episode", k)`.
      Byte-identical to pre-Phase-21 (existing test locks this).
    - `search_memories(..., guild_id="G")` → `AND (guild_id = $3 OR guild_id IS NULL)`;
      params `(u, emb, "G", k)`.
    - `search_memories(..., kind="taste_episode", guild_id="G")` →
      `AND kind = $3 AND (guild_id = $4 OR guild_id IS NULL)`;
      params `(u, emb, "taste_episode", "G", k)`. This is the auto-queue taste-blend call shape.
    - In ALL of the above, `WHERE user_id = $1` is present verbatim (T-21-01).
    - `guild_id IS NULL` never appears WITHOUT the `guild_id = $N OR` half (T-21-02).
  </behavior>

  <action>
    Add a keyword-only `guild_id: str | None = None` parameter to `database.search_memories`,
    appended AFTER the existing `kind` parameter in the signature.

    Replace the hardcoded-`$3` clause construction with append-time dynamic numbering: start
    `params` as `[user_id, query_embedding]` and a `clauses` string as `""`; then, in this exact
    order — `kind` FIRST, `guild_id` SECOND — append the value to `params` and concatenate the
    clause using `${len(params)}` computed at that moment. Order matters: appending `kind` first is
    what keeps the kind-only case binding at `$3`, which the pre-existing
    `test_kind_taste_episode_appends_clause_and_binds_positionally` asserts as a literal string.

    The guild clause text is exactly one parenthesized OR-group:
    `" AND (guild_id = ${n} OR guild_id IS NULL)"`. It is AND-appended INSIDE the existing
    `WHERE user_id = $1` guard — never OR-appended to it, never emitted as a bare
    `guild_id IS NULL`. The `LIMIT $` expression already computes `str(len(params) + 1)` and
    continues to work unchanged because `k` is still the last positional bound.

    Do NOT change the SELECT column list, the `ORDER BY embedding <=> $2`, or the return type.

    Update the docstring: add a `guild_id` entry to Args describing the D-01 grandfather rule
    (guild-stamped rows for this guild PLUS the legacy `guild_id IS NULL` corpus), and note the
    Phase 21 dynamic-numbering requirement so a future third optional clause does not regress it.

    In `tests/test_memory.py`, add a `TestSearchMemoriesGuildFilter` class immediately after the
    existing `TestSearchMemoriesKindFilter`, reusing the existing `_FakePool` helper. Cover all
    four combinations above, asserting on `pool.conn.last_sql` and `pool.conn.last_params`. Include
    an explicit assertion in the guild-scoped cases that `"WHERE user_id = $1"` is still in the SQL
    (T-21-01) and that the SQL contains both `"guild_id = $"` and `"OR guild_id IS NULL"` (T-21-02).
    Do NOT modify `TestSearchMemoriesKindFilter` — its passing unmodified is the regression proof.
  </action>

  <verify>
    <automated>pytest tests/test_memory.py -k "SearchMemories" -x</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_memory.py -k "SearchMemories" -x` exits 0.
    - `git diff tests/test_memory.py` shows ZERO changes inside `class TestSearchMemoriesKindFilter`
      (both its tests still pass byte-identical assertions, incl. the literal `"AND kind = $3"`).
    - `database.py::search_memories` source contains the substring `OR guild_id IS NULL`.
    - `database.py::search_memories` source does NOT contain the substring `" AND kind = $3"`
      (the hardcoded literal is gone — replaced by `${len(params)}` interpolation).
    - `database.py::search_memories` source contains `WHERE user_id = $1` and the guild clause is
      introduced by ` AND (` — never by ` OR `.
    - `tests/test_memory.py` contains `class TestSearchMemoriesGuildFilter` with at least 4 tests.
    - `ruff check database.py tests/test_memory.py` exits 0.
  </acceptance_criteria>

  <done>
    All four kind x guild combinations emit the correct SQL and bind the correct positional params;
    the pre-Phase-21 no-clause and kind-only shapes are provably unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MemoryService.recall gains the keyword-only guild_scoped opt-in (MEM-02 / D-02)</name>

  <files>services/memory.py, tests/test_memory.py</files>

  <read_first>
    - `services/memory.py:60-129` — the CURRENT `recall()` signature, its docstring (the stale
      "reserved for future per-guild memory scoping" text at `:88-91`), and its call to
      `database.search_memories` at `:122-128` which today always passes `kind=kind`.
    - `tests/test_memory.py:471`, `:518`, `:631`, `:654` — the FOUR existing `fake_search` stubs on
      the recall path. Their signature is `async def fake_search(pool, *, user_id, query_embedding,
      k, kind=None)` — note there is NO `guild_id` parameter and NO `**kwargs`.
    - `21-RESEARCH.md` § "Pitfall 2" and § "Summary" (the sentence "never as a bare `None`-defaulted
      kwarg unconditionally forwarded").
    - `tests/test_memory.py:611-680` — `TestRecallKindParam`, the structural template for the new
      `TestRecallGuildScoped` class.
  </read_first>

  <behavior>
    - `recall(user_id, guild_id, query_text)` (default) → `database.search_memories` is called with
      NO `guild_id` keyword at all. Global recall, byte-identical to pre-Phase-21. This is MEM-02.
    - `recall(user_id, guild_id, query_text, guild_scoped=True)` → `database.search_memories`
      receives `guild_id=<that guild_id>`.
    - `recall(user_id, guild_id, query_text, kind="taste_episode", guild_scoped=True)` → both
      `kind=` and `guild_id=` reach `search_memories`.
    - `guild_scoped` is keyword-only: `recall(u, g, q, None, True)` positionally raises TypeError.
  </behavior>

  <action>
    HAZARD — READ THIS BEFORE WRITING CODE. The code samples in `21-RESEARCH.md` (Pattern 2) and
    `21-PATTERNS.md` show `guild_id=guild_id if guild_scoped else None` as an UNCONDITIONAL keyword
    on the `search_memories` call. That sample is wrong and contradicts the RESEARCH Summary's own
    rule ("never as a bare `None`-defaulted kwarg unconditionally forwarded"). Passing
    `guild_id=None` unconditionally raises `TypeError: unexpected keyword argument 'guild_id'` in
    all four existing recall-path `fake_search` stubs listed in the read_first block above, because they do not
    declare a `guild_id` parameter. Do NOT "fix" that by editing those four stubs — enlarging the
    dedup/recall test-double surface is exactly what MEM-05 forbids.

    The correct implementation: forward the `guild_id` keyword to `database.search_memories` ONLY
    when `guild_scoped` is truthy. Build a small kwargs mapping (e.g. a dict holding `guild_id`
    only when opted in) and splat it into the existing `search_memories` call alongside the
    unchanged `user_id`/`query_embedding`/`k`/`kind` keywords. When not opted in, the call site
    must produce a keyword set identical to today's — no `guild_id` key present.

    Add `guild_scoped: bool = False` as a KEYWORD-ONLY parameter (behind a bare `*`) after the
    existing `kind` parameter on `MemoryService.recall`. Do not reorder or repurpose the existing
    positional `guild_id` parameter — `/ask` passes a real non-null guild_id today and must stay
    global, so guild_id presence can never be the scoping signal (D-02 seam consequence).

    Rewrite the now-stale docstring block at `services/memory.py:88-91`: `guild_id` is no longer
    "reserved for future scoping" — it is the value used as the ANN guild filter WHEN `guild_scoped`
    is True, and ignored otherwise. Document `guild_scoped` explicitly: default `False` = global
    recall (MEM-02, byte-identical); `True` = narrow to this guild's rows plus the grandfathered
    `guild_id IS NULL` corpus (D-01). Also update step 2 of the numbered pipeline in the docstring
    ("scoped cosine ANN ... (WHERE user_id only)") to reflect the optional guild narrowing.

    DO NOT TOUCH `services/memory.py:248-254` — `remember()`'s k=1 dedup `search_memories` call.
    It must remain exactly `database.search_memories(self._pool, user_id=user_id,
    query_embedding=fact_vec, k=1)`. Any edit there is a direct D-02 / MEM-05 violation (this is the
    Phase 13 CR-01 scarred path). Task 3 locks that.

    In `tests/test_memory.py`, add a `TestRecallGuildScoped` class after `TestRecallKindParam`. Its
    stubs are NEW, so give them a `**extra` catch-all: `async def fake_search(pool, *, user_id,
    query_embedding, k, kind=None, **extra)`. Assert: (a) default `recall(...)` → `"guild_id" not
    in extra`; (b) `recall(..., guild_scoped=True)` → `extra["guild_id"] == "guild1"`;
    (c) `recall(..., kind="taste_episode", guild_scoped=True)` → both `kind` and `extra["guild_id"]`
    arrive; (d) `inspect.signature(MemoryService.recall)` shows `guild_scoped` with
    `kind == KEYWORD_ONLY` and `default is False`.
  </action>

  <verify>
    <automated>pytest tests/test_memory.py -x</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_memory.py -x` exits 0 with ZERO edits to the four pre-existing
      `fake_search` stubs at `tests/test_memory.py:471`, `:518`, `:631`, `:654` (verify via
      `git diff tests/test_memory.py` — those four `def fake_search` lines must be untouched).
    - `services/memory.py::MemoryService.recall` signature contains `guild_scoped: bool = False`
      and it is KEYWORD-ONLY (`inspect.signature(...).parameters["guild_scoped"].kind` is
      `KEYWORD_ONLY`).
    - `services/memory.py` no longer contains the string `reserved for future per-guild memory scoping`.
    - `git diff services/memory.py` shows NO change to the `remember()` dedup call region
      (`search_memories(self._pool, user_id=user_id, query_embedding=fact_vec, k=1)` intact).
    - `tests/test_memory.py` contains `class TestRecallGuildScoped`.
    - `ruff check services/memory.py tests/test_memory.py` exits 0.
  </acceptance_criteria>

  <done>
    `recall()` defaults to global and forwards no guild kwarg; `guild_scoped=True` narrows the ANN.
    Every pre-existing recall test passes unmodified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MEM-05 regression lock — remember()'s dedup search call shape is byte-identical</name>

  <files>tests/test_memory.py</files>

  <read_first>
    - `services/memory.py:248-285` — `remember()`'s k=1 dedup `search_memories` call AND the D-05
      `expires_at` self-refresh branch gated on `nearest_kind` (the matched row's kind, NOT the
      incoming write's kind). The inline comment at `:265-274` is the CR-13-01 scar's own
      description — read it.
    - `tests/test_memory_taste.py:140-240` — the existing dedup/refresh monkeypatch tests, including
      the `async def fake_search(pool, *, user_id, query_embedding, k)` stub at `:156` whose
      NARROW signature (no `kind`, no `guild_id`) is precisely the structural lock this task
      formalizes.
    - `tests/test_memory.py:851-1110` — `TestRememberService` and its existing `fake_search` stubs
      at `:933`, `:993`, `:1043` (also narrow: `(pool, *, user_id, query_embedding, k)`).
  </read_first>

  <behavior>
    - Running `MemoryService.remember(...)` against a STRICT stub declaring exactly
      `(pool, *, user_id, query_embedding, k)` — no `kind`, no `guild_id`, no `**kwargs` —
      completes without TypeError. If a future edit ever threads a guild or kind kwarg into the
      dedup search, this test fails loudly at the call boundary.
    - The D-05 self-refresh still fires on a near-dup whose MATCHED row kind is in
      `MEMORY_DECAY_DAYS_BY_KIND`, and still does NOT fire when the matched row's kind is a Phase 11
      kind — unchanged by the guild-scoping work.
  </behavior>

  <action>
    Add a `TestRememberDedupCallShapeUnchanged` class to `tests/test_memory.py` (name it so
    `pytest -k dedup` selects it).

    Test A — structural call-shape lock: monkeypatch `database.search_memories` with a strict stub
    whose signature is exactly `async def strict_search(pool, *, user_id, query_embedding, k)` —
    deliberately NO `kind` parameter, NO `guild_id` parameter, NO `**kwargs` escape hatch. Record
    the received kwargs. Run `svc.remember(...)` (mirror the service-construction and
    monkeypatch/restore idiom already used in `TestRememberService`, including restoring the
    original `database.search_memories` in a `finally`). Assert the call completed and the recorded
    kwarg keys are exactly `{"user_id", "query_embedding", "k"}`. Assert `k == 1`. A TypeError here
    IS the failure mode being locked — do not add `**kwargs` to the stub to make it pass.

    Test B — source-level guard: `inspect.getsource(MemoryService.remember)` must NOT contain the
    substring `guild_scoped`, and must NOT contain `guild_id=` anywhere inside the
    `search_memories(` call region (note: `remember()` legitimately has its own `guild_id` parameter
    that it passes to `insert_memory` — assert narrowly on the dedup search call region, e.g. by
    slicing the source between `search_memories(` and the following `)` and asserting `guild_id`
    is absent from that slice).

    Test C — D-05 semantics regression: assert `config.MEMORY_DECAY_DAYS_BY_KIND` still gates the
    refresh on the MATCHED row's kind. Rather than duplicating `tests/test_memory_taste.py`'s
    coverage, assert that `refresh_memory_expiry` is invoked when the stubbed near-dup row's `kind`
    is `"taste_episode"` and NOT invoked when it is `"daily_batch"`, using the same
    monkeypatch-`database.refresh_memory_expiry` idiom.

    Do not modify `tests/test_memory_taste.py` — its unmodified passing is itself part of the
    regression proof (see acceptance criteria).
  </action>

  <verify>
    <automated>pytest tests/test_memory.py tests/test_memory_taste.py -x</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_memory.py -k dedup -x` exits 0.
    - `pytest tests/test_memory_taste.py -x` exits 0 with `git diff --stat tests/test_memory_taste.py`
      showing ZERO changed lines (the file is untouched by this phase).
    - `tests/test_memory.py` contains a stub whose literal signature is
      `async def strict_search(pool, *, user_id, query_embedding, k)` — grep confirms no `guild_id`,
      no `kind`, no `**` on that line.
    - `inspect.getsource(services.memory.MemoryService.remember)` contains no `guild_scoped`.
    - `ruff check tests/test_memory.py` exits 0.
  </acceptance_criteria>

  <done>
    The CR-13-01 scarred dedup path is locked by a test that fails structurally if any future edit
    threads a scoping kwarg into it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord guild → memory ANN read | A user's memories formed in Guild A must not surface in Guild B via any unprompted surface. The `user_memories` table is multi-tenant by `user_id` AND (from this phase) by `guild_id`. |
| Caller → `database.search_memories` | The SQL WHERE clause is the ONLY isolation control. Any regression here is a silent data-disclosure bug with no runtime error. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | `database.search_memories` WHERE clause | mitigate | The guild clause is AND-appended INSIDE the existing `WHERE user_id = $1` guard (T-11-03a), never OR-appended and never replacing it. Task 1 asserts `"WHERE user_id = $1"` remains present verbatim in every guild-scoped SQL output. A guild clause that dropped the user guard would be a cross-USER leak — strictly worse than the cross-guild leak being fixed. |
| T-21-02 | Information Disclosure | `database.search_memories` guild clause text | mitigate | Emit exactly one parenthesized OR-group `(guild_id = $N OR guild_id IS NULL)`. A degenerate `guild_id IS NULL`-only clause would silently blind ALL guild-scoped recall to the legacy corpus only, defeating MEM-01 with no error. Task 1 asserts BOTH branches are present in the SQL string, not just one. |
| T-21-04 | Information Disclosure / functional regression | `MemoryService.recall` opt-in default | mitigate | `guild_scoped` is keyword-only and defaults `False`; the `guild_id` kwarg is forwarded to `search_memories` ONLY when opted in. Task 2 asserts `"guild_id" not in extra` on the default path — a copy-paste that always scoped (or never scoped) fails this. Scoping can never be inferred from `guild_id` presence (both `/ask` and `/roast` pass a real guild_id). |
| T-21-05 | Tampering | `services/memory.py::remember` dedup search (CR-13-01 scar) | mitigate | D-02 confines the change to the read path. Task 3's strict stub (no `guild_id`, no `kind`, no `**kwargs`) makes any future kwarg leak into the dedup search a hard TypeError, protecting the `expires_at` self-refresh semantics from cross-kind corruption. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (21-RESEARCH § Package Legitimacy Audit: N/A). No supply-chain surface. |
</threat_model>

<verification>
- `pytest tests/test_memory.py tests/test_memory_taste.py -x` — green.
- `pytest tests/test_memory.py tests/test_ambient_recall_cadence.py tests/test_autoqueue_wiring.py tests/test_memory_taste.py -x` — the phase quick command, green (no call sites changed yet, so all pre-existing behavior holds).
- `ruff check database.py services/memory.py tests/test_memory.py` — clean.
- `git diff` touches ONLY `database.py::search_memories`, `MemoryService.recall`, and additive test classes. No cog files. No `remember()` body changes.
</verification>

<success_criteria>
- `search_memories` supports 4 kind x guild combinations with correct `$N` binding; the no-clause and kind-only shapes are byte-identical to pre-Phase-21.
- `recall(guild_scoped=False)` (the default) forwards NO guild kwarg — MEM-02's guarantee holds at the call boundary, not just in prose.
- The four pre-existing recall-path `fake_search` stubs and all of `tests/test_memory_taste.py` pass UNMODIFIED.
- `remember()`'s dedup search is provably unreachable by the new parameter (MEM-05).
</success_criteria>

<output>
Create `.planning/phases/21-memory-scoping-guild-data-lifecycle/21-01-SUMMARY.md` when done.
</output>
