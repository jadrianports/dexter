---
phase: 21-memory-scoping-guild-data-lifecycle
plan: 04
type: execute
wave: 3
depends_on: ["21-02", "21-03"]
files_modified:
  - bot.py
  - .planning/PROJECT.md
  - CLAUDE.md
autonomous: true
requirements: [MEM-04, MEM-01, MEM-03, MEM-05]
must_haves:
  truths:
    - "When Dexter leaves or is force-left from a guild, on_guild_remove purges that guild's data across the four tables"
    - "A purge failure logs a warning and is swallowed — it can NEVER crash guild removal (WR-04 discipline)"
    - "There is exactly ONE purge site — cogs/ops.py is NOT a second one (guild.leave() already fires on_guild_remove)"
    - "PROJECT.md Key Decisions records the memory-scoping path that actually shipped, so Phase 23's PORT-04 can disclose it honestly (ROADMAP SC-4)"
    - "CLAUDE.md's schema + gotchas narrative reflects the shipped guild-scoping and purge behavior"
  artifacts:
    - path: "bot.py"
      provides: "on_guild_remove calls database.purge_guild_data, wrapped in try/except"
      contains: "purge_guild_data"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions rows for hybrid memory scoping (shipped) + guild-data purge"
      contains: "guild_scoped"
    - path: "CLAUDE.md"
      provides: "updated memory/schema/gotchas narrative for Phase 21"
      contains: "purge_guild_data"
  key_links:
    - from: "bot.py::on_guild_remove"
      to: "database.purge_guild_data"
      via: "awaited inside try/except, after cache-evict, before the owner notice"
      pattern: "purge_guild_data\\(bot\\.pool"
---

<objective>
Close the phase: wire the MEM-04 purge into the single existing `bot.py::on_guild_remove` hook
(D-03), then record the shipped memory-scoping decision in PROJECT.md Key Decisions — ROADMAP
success criterion 4 and a hard dependency for Phase 23's PORT-04 disclosure.

Purpose: the purge helper from 21-02 is dead code until it is called. And the decision record is
not paperwork — Phase 23 has to tell strangers, in public, exactly what Dexter remembers and where
it travels. It can only be honest if this phase writes down what actually shipped.

Output: one wrapped call in `bot.py`, two Key Decisions rows, and a CLAUDE.md narrative update.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-memory-scoping-guild-data-lifecycle/21-CONTEXT.md
@.planning/phases/21-memory-scoping-guild-data-lifecycle/21-RESEARCH.md
@.planning/phases/21-memory-scoping-guild-data-lifecycle/21-PATTERNS.md
@.planning/phases/21-memory-scoping-guild-data-lifecycle/21-02-SUMMARY.md
@.planning/phases/21-memory-scoping-guild-data-lifecycle/21-03-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
| Symbol | Location | Shape |
|---|---|---|
| purge hook call | `bot.py::on_guild_remove` | `await database.purge_guild_data(bot.pool, guild_id=str(guild.id))` inside try/except |
| Key Decisions rows | `.planning/PROJECT.md` § Key Decisions | 2 new table rows (hybrid scoping shipped; guild-data purge) |
| CLAUDE.md narrative | `CLAUDE.md` | schema/critical-rules/gotchas updates for Phase 21 |

This is the terminal plan of Phase 21. Phase 23 (PORT-04) consumes the PROJECT.md decision rows.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: bot.py::on_guild_remove calls the purge, wrapped (MEM-04 / D-03)</name>

  <files>bot.py</files>

  <read_first>
    - `bot.py:761-773` — the CURRENT `on_guild_remove`. Note its docstring explicitly names this
      phase's job ("The MEM-04 guild-data purge is Phase 21's job") and carries a constraint about
      preserving a blocked guild's row — Phase 20 D-01 already resolved that by moving the blocklist
      to its own table, so the docstring is now stale and must be rewritten.
    - `bot.py:707-758` — `on_guild_join`, specifically its `try: ... except Exception as exc:
      log.warning("on_guild_join: DB write/welcome chain failed for guild %s: %s", guild.id, exc)`
      block. This is the WR-04 best-effort discipline to mirror exactly (same log level, same
      message shape).
    - `database.py::purge_guild_data` — the helper from plan 21-02 (keyword-only `guild_id=`,
      raises on failure by design).
    - `cogs/ops.py::_force_leave_teardown` (`:520-541`) — read ONLY to confirm it calls
      `guild.leave()`. Do NOT edit this file.
  </read_first>

  <action>
    In `bot.py::on_guild_remove`, add a purge call positioned AFTER the existing
    `bot.guild_config._cache.pop(...)` cache-evict and BEFORE the existing
    `bot.log_to_discord(...)` owner notice.

    Guard it on `hasattr(bot, "pool")` (mirroring the existing `hasattr` guards in this handler —
    the pool may not exist in partial-init/test contexts), and wrap the await in `try/except
    Exception as exc` with a `log.warning` that names the handler, the guild id, and the exception —
    same shape as `on_guild_join`'s WR-04 warning. The purge is BEST-EFFORT: a DB failure must log
    and be swallowed, never propagate. A crash here would break guild removal itself.

    Call it as `await database.purge_guild_data(bot.pool, guild_id=str(guild.id))` — `guild_id` is
    keyword-only and the table stores guild ids as TEXT, so the `str()` cast is required.

    Rewrite the docstring. It currently says "NO DB write (D-12)" and forward-declares the Phase 21
    constraint. It must now state: the handler evicts the config cache, purges the guild's data
    across four tables (MEM-04, best-effort), and notifies the owner. Explicitly note that
    `guild_blocklist` is NOT purged (Phase 20 D-01 / OWNER-04) — a kicked abuser's block survives
    their data, so a re-invite is still refused. Also note that this is the SINGLE purge site:
    `/guilds leave` and `/guilds block` both call `guild.leave()`, which fires this gateway event,
    so `cogs/ops.py` must never grow its own purge call (D-03) — a second site risks a double-delete
    or a missed path.

    Do NOT touch `cogs/ops.py`. Do NOT add a purge call anywhere else in the codebase.

    Note on ordering (already verified safe by research, no synchronization needed): in the
    `/guilds block` flow the blocklist INSERT happens after `guild.leave()`, so it can run
    concurrently with this purge. Because the purge touches four tables and the blocklist insert
    touches only `guild_blocklist` — completely disjoint — there is no race and no ordering
    requirement. Do not add locks, sleeps, or sequencing.
  </action>

  <verify>
    <automated>python -c "import inspect, bot; s = inspect.getsource(bot.on_guild_remove); assert 'purge_guild_data' in s; assert 'try' in s and 'except' in s; assert 'log.warning' in s; print('ok')" && pytest -x</automated>
  </verify>

  <acceptance_criteria>
    - `inspect.getsource(bot.on_guild_remove)` contains `purge_guild_data(bot.pool`, and contains
      `guild_id=str(guild.id)`.
    - The purge call is inside a `try:` / `except Exception` block whose handler calls `log.warning`
      (not `log.error`, not a bare `raise`) — grep the function source.
    - `inspect.getsource(bot.on_guild_remove)` no longer contains the stale strings `NO DB write`
      or `is Phase 21's job`.
    - `grep -rn "purge_guild_data" cogs/` returns ZERO matches — there is exactly one purge call
      site, in `bot.py` (D-03).
    - `grep -c "purge_guild_data" bot.py` returns 1 (the single call; the import is via the existing
      `database` module reference).
    - `git diff --stat cogs/ops.py` shows ZERO changes.
    - Full suite `pytest` exits 0.
    - `ruff check bot.py` exits 0.
  </acceptance_criteria>

  <done>
    Every guild departure path — natural kick/leave, `/guilds leave`, `/guilds block` — purges that
    guild's data exactly once, and a purge failure can never crash the removal.
  </done>
</task>

<task type="auto">
  <name>Task 2: record the shipped scoping decision in PROJECT.md + sync CLAUDE.md (ROADMAP SC-4 / PORT-04)</name>

  <files>.planning/PROJECT.md, CLAUDE.md</files>

  <read_first>
    - `.planning/PROJECT.md:177-200` — the `## Key Decisions` table. Columns are
      `| Decision | Rationale | Outcome |`. Read several existing rows to match the voice: terse,
      phase-tagged, outcome-marked (`✓ Good`, `✓ Resolved`, `⚠️ Revisit`, `— Pending`).
    - `.planning/ROADMAP.md` § "Phase 21" success criterion 4 — the exact obligation this task
      discharges ("Whichever path is taken, the decision and its rationale are recorded in
      PROJECT.md Key Decisions before the phase closes, so PORT-04 can disclose it honestly").
    - `21-CONTEXT.md` § D-01/D-02/D-03/D-04 — the four locked decisions whose SHIPPED outcome is
      being recorded.
    - `CLAUDE.md` § "Database Schema" (the running schema narrative), § "Critical Rules" (11–16 are
      the memory rules), and § "Implementation Gotchas → Phases 13–17" (where the `search_memories`
      / `recall` kind-clause discipline and the CR-01 scar are documented).
    - The 21-01/21-02/21-03 SUMMARYs — record what ACTUALLY shipped, not what was planned. If any
      requirement was descoped during execution, THAT is what goes in the table.
  </read_first>

  <action>
    PROJECT.md — append two rows to the `## Key Decisions` table, matching the existing column
    format and terse voice:

    Row 1 (the SC-4 obligation — the memory-scoping path that shipped): record that hybrid
    read-path guild scoping SHIPPED (D-04's descope gate did not fire; none of the three
    REQUIREMENTS.md tripwires hit). State the shape precisely enough for Phase 23 to disclose it
    without re-reading the code: ambient/unprompted surfaces (`/roast @user`, ambient voice roasts,
    proactive callbacks, the music-command callback, and the auto-queue taste blend) recall only
    memories stamped with the current guild PLUS the legacy `guild_id IS NULL` corpus (grandfathered
    per D-01); `/ask` continues to recall the invoker's OWN memory globally (MEM-02 — self-scoped,
    no cross-user exposure possible). Rationale: the discriminator is "explicit synchronous
    self-pull stays global; unprompted ambient broadcast to a channel scopes." Note the read/write
    asymmetry (D-02): `remember()` / dedup / eviction stay `user_id`-scoped and byte-identical,
    deliberately keeping the Phase 13 CR-01 scarred path untouched. Outcome: `✓ Good`.

    Row 2 (MEM-04): guild-data purge on `on_guild_remove` — four tables
    (`guild_config`, `guild_queues`, `guild_jams`, guild-stamped `user_memories`), one transaction,
    single hook. Rationale: stale context must not resurface on re-invite. Note the hard invariant:
    `guild_blocklist` is NEVER purged (Phase 20 D-01 / OWNER-04), so a kicked abuser's block
    outlives their data. Outcome: `✓ Good`.

    Be precise about `/ask` in Row 1. Phase 23 will publish this. An imprecise row that says "memory
    is now guild-scoped" full stop would make PORT-04's public disclosure WRONG — `/ask` is still
    global and users deserve to know that.

    CLAUDE.md — update the running narrative in the three places this phase changes documented
    behavior:
    1. § "Database Schema" — the `user_memories` narrative: note that as of Phase 21 the ANN read
       path optionally narrows to `(guild_id = $N OR guild_id IS NULL)` and that a guild's rows are
       hard-deleted on removal via `purge_guild_data`; `guild_blocklist` is excluded from that purge
       by design (it is why Phase 20 gave it its own table).
    2. § "Critical Rules" — add a rule capturing the load-bearing subtlety: guild-scoping is an
       EXPLICIT per-call-site opt-in (`guild_scoped=True`), never inferred from `guild_id` presence,
       because `/ask` also passes a real guild_id and must stay global.
    3. § "Implementation Gotchas → Phases 13–17" (or a new Phases 18–21 subsection, whichever
       matches the file's existing structure) — record: (a) `search_memories`' placeholder numbers
       are now computed from `len(params)`, not hardcoded, because two optional clauses exist —
       a third clause must follow the same discipline; (b) `recall()` forwards `guild_id=` to
       `search_memories` ONLY when opted in — passing it unconditionally (even as `None`) breaks
       the existing narrow-signature test doubles; (c) `remember()`'s k=1 dedup search is
       untouchable (CR-13-01 scar).

    Do not restructure CLAUDE.md. Additive edits only, matching the surrounding prose style.
  </action>

  <verify>
    <automated>grep -q "guild_scoped" .planning/PROJECT.md && grep -q "purge_guild_data" CLAUDE.md && grep -q "guild_blocklist" .planning/PROJECT.md && echo ok</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/PROJECT.md` § Key Decisions contains 2 new rows, both matching the existing
      `| Decision | Rationale | Outcome |` 3-column format (no broken table rendering).
    - The scoping row explicitly states that `/ask` remains GLOBAL — `grep -i "ask" .planning/PROJECT.md`
      shows it named in the new row. (Phase 23 publishes this; an imprecise row makes PORT-04 lie.)
    - The scoping row names the grandfathered `guild_id IS NULL` corpus (D-01).
    - The purge row explicitly states `guild_blocklist` is NOT purged.
    - `CLAUDE.md` contains `purge_guild_data` and `guild_scoped`.
    - `CLAUDE.md` § Critical Rules gained a rule stating guild-scoping is an explicit opt-in, never
      inferred from `guild_id` presence.
    - Both files' claims match the 21-01/21-02/21-03 SUMMARYs (what shipped, not what was planned).
  </acceptance_criteria>

  <done>
    ROADMAP SC-4 is discharged: the shipped decision is written down, precisely enough that Phase 23
    can disclose it publicly without re-deriving it from code.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord gateway event → destructive DB write | `on_guild_remove` is fired by Discord, not by an authenticated command. It must be safe to run at any time, and safe to FAIL at any time. |
| Shipped behavior → public disclosure (Phase 23) | PROJECT.md Key Decisions is the source of truth PORT-04 will publish. A wrong row here becomes a public false statement about what the bot remembers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Denial of Service | `on_guild_remove` purge call | mitigate | The purge is wrapped in try/except with `log.warning` (WR-04 discipline, mirroring `on_guild_join`). A DB outage during a guild removal must not raise out of a gateway event handler. Guarded on `hasattr(bot, "pool")` for partial-init contexts. |
| T-21-03 | Tampering / Denial of an abuse-mitigation control | Purge call sites | mitigate | Exactly ONE purge site (`bot.py::on_guild_remove`). Acceptance criteria assert `grep -rn "purge_guild_data" cogs/` returns zero matches — a second site in `cogs/ops.py` would double-delete and, worse, invites a future "purge everything for this guild" that sweeps `guild_blocklist`. `guild.leave()` already fires this event on both force-leave paths. |
| T-21-11 | Repudiation / Information Disclosure (false disclosure) | PROJECT.md Key Decisions row | mitigate | The scoping row MUST state that `/ask` stays global. Phase 23 publishes this verbatim; a row that claims blanket guild-scoping would make Dexter's public privacy statement false. Acceptance criteria require `/ask` to be named explicitly in the row. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase. No supply-chain surface. |
</threat_model>

<verification>
- Full suite `pytest` — green (all waves complete at this point).
- `pytest tests/test_database_phase21.py -x` — green (live tests run in CI).
- `ruff check .` — clean.
- `python -c "import bot"` — imports cleanly (no syntax/name errors in the edited handler).
- `grep -rn "purge_guild_data" .` — exactly 3 kinds of hit: the definition in `database.py`, the
  single call in `bot.py`, and the tests. ZERO hits in `cogs/`.
- `.planning/PROJECT.md` Key Decisions renders as a valid markdown table with the 2 new rows.
</verification>

<success_criteria>
- `on_guild_remove` purges the departing guild's data, best-effort, from the single hook (D-03).
- A purge failure logs and is swallowed — guild removal never crashes.
- `cogs/ops.py` is untouched; there is no second purge site.
- PROJECT.md Key Decisions records the ACTUAL shipped scoping (hybrid read-path scoping, `/ask`
  global, NULL corpus grandfathered) and the purge invariant (`guild_blocklist` survives) —
  discharging ROADMAP SC-4 and unblocking Phase 23's PORT-04.
- CLAUDE.md's schema / critical-rules / gotchas narrative matches shipped behavior.
</success_criteria>

<output>
Create `.planning/phases/21-memory-scoping-guild-data-lifecycle/21-04-SUMMARY.md` when done.
</output>
