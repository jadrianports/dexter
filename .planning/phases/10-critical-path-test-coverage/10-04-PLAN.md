---
phase: 10-critical-path-test-coverage
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02", "10-03"]
files_modified: []
autonomous: false
requirements: [TEST-04]

must_haves:
  truths:
    - "The full pytest suite runs green — no failures; live-DB integration tests skip cleanly when Postgres is unavailable"
    - "The three new logic test files (playback, health, roasts) are collected and pass as part of the full suite"
    - "The bot boots via `python bot.py`, comes online, and logs/dexter.log shows no new ERROR or silent-failure lines introduced by the Phase 10 refactor"
  artifacts:
    - path: "tests/test_playback_logic.py"
      provides: "Playback logic regression lock (must pass in the full suite)"
    - path: "tests/test_health_logic.py"
      provides: "Health logic regression lock (must pass in the full suite)"
    - path: "tests/test_roast_logic.py"
      provides: "Roast logic regression lock (must pass in the full suite)"
  key_links:
    - from: "tests/"
      to: "logic/"
      via: "full pytest suite imports and exercises every extracted pure function"
      pattern: "pytest"
---

<objective>
The TEST-04 regression gate: prove the Phase 10 extraction introduced no behavioral
regression. Two-part gate (D-04): (1) the full pytest suite is green — automated; and (2) a
manual bot boot where the user runs `python bot.py`, confirms the bot comes online, and
eyeballs `logs/dexter.log` for new ERROR / silent-failure lines.

The boot check cannot be fully automated here — it needs the real Discord token + Neon, and
the bot runs on the user's PC on demand — so the boot is a blocking human-verify checkpoint
(this plan is `autonomous: false`). No new silent failures = gate passes.

Purpose: confirm that carving decision logic out of the shipped MusicCog/AICog/OpsCog/
EventsCog/restore glue (10-01..10-03) preserved behavior end-to-end.
Output: a green full-suite run + a confirmed clean boot recorded in the SUMMARY.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-critical-path-test-coverage/10-CONTEXT.md
@.planning/codebase/TESTING.md
</context>

<artifacts_produced>
## Artifacts this phase produces (10-04)

This plan produces no new source — it is the regression gate. It verifies, collectively,
the artifacts produced by 10-01..10-03:
- `logic/playback.py`, `logic/health.py`, `logic/roasts.py` (+ `logic/__init__.py`)
- `tests/test_playback_logic.py`, `tests/test_health_logic.py`, `tests/test_roast_logic.py`
- the behavior-preserving wiring in `cogs/music.py`, `cogs/ai.py`, `cogs/ops.py`,
  `cogs/events.py`, `bot.py`, `services/queue_persistence.py`

Gate evidence recorded in the SUMMARY: full-suite pass/fail counts (+ skip count) and the
manual-boot result (online + clean `logs/dexter.log`).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Full pytest suite green (automated regression gate)</name>
  <files></files>
  <read_first>
    - .planning/codebase/TESTING.md (run commands; live-DB tests skip when Postgres unavailable — skips are NOT failures)
    - tests/conftest.py (the live-DB `pool` fixture skips, not errors, when Postgres is absent)
  </read_first>
  <action>
    Run the complete test suite from the project root and confirm it is green. Run `pytest -q`
    (optionally `pytest -v` for the per-test view). The pass bar is ZERO failures and ZERO
    errors; live-DB integration tests that `pytest.skip` because Postgres is unavailable are
    expected and acceptable. Confirm the three new Phase 10 logic test files are collected
    (they appear in the run). If anything fails, the failure is a real regression from the
    10-01..10-03 wiring — stop and report it (do not paper over it); it must be fixed in the
    owning plan, not masked here. Record the final summary line (passed / skipped / failed)
    for the SUMMARY.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` exits 0 (no failures, no errors; skips allowed)
    - `pytest tests/test_playback_logic.py tests/test_health_logic.py tests/test_roast_logic.py -q` exits 0 (the three new logic suites are present and pass)
    - The recorded summary line shows 0 failed / 0 errored
  </acceptance_criteria>
  <done>The full suite runs with zero failures and zero errors (live-DB tests may skip), and the three new logic test files are collected and pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual clean-boot check (real token + Neon)</name>
  <files></files>
  <read_first>
    - .planning/phases/10-critical-path-test-coverage/10-01-SUMMARY.md (what the playback/restore wiring touched — where to look in the boot log)
    - .planning/phases/10-critical-path-test-coverage/10-02-SUMMARY.md (the /health metrics wiring)
    - .planning/phases/10-critical-path-test-coverage/10-03-SUMMARY.md (the voice-state roast wiring)
    - logs/dexter.log + logs/error.log (the boot-window evidence to eyeball)
  </read_first>
  <what-built>
    Phases 10-01..10-03 extracted playback, health, and ambient-roast decision logic into
    `logic/` pure functions and rewired the live cogs/services to call them (D-02). The full
    automated suite is green (Task 1). This checkpoint confirms the refactor did not break
    runtime boot or introduce silent failures that unit tests cannot see (Discord/DB glue is
    untested-by-design).
  </what-built>
  <how-to-verify>
    1. From the project root, run `python bot.py` with the real `.env` (DISCORD_TOKEN,
       GEMINI_API_KEY, GENIUS_TOKEN, DATABASE_URL pointed at Neon).
    2. Confirm the bot logs in and comes online — watch for the startup line and shard READY;
       it should reach "i'm back. did you miss me. probably not." in the designated channel
       (or at minimum log a successful on_ready / command sync).
    3. Open `logs/dexter.log` (and `logs/error.log`) and eyeball the boot window for any NEW
       ERROR / traceback / silent-failure lines that were not present before Phase 10 —
       especially around queue restore (`restore_queues` / smart-rejoin), `/health` metrics,
       and voice-state handling (the three rewired paths).
    4. (Optional, if a voice channel + audio are handy) queue a song and let it finish to
       exercise the rewired `_on_track_end` / auto-queue path once.
    5. Stop the bot (Ctrl+C) and confirm a clean shutdown with no orphaned errors.
  </how-to-verify>
  <action>
    MANUAL (not auto-executable — needs the real Discord token + Neon, runs on the user's PC
    on demand): the user runs `python bot.py`, confirms the bot comes online, and eyeballs
    `logs/dexter.log` / `logs/error.log` for new ERROR / silent-failure lines around the
    three rewired paths (queue restore/smart-rejoin, `/health` metrics, voice-state roasts),
    then stops the bot cleanly. Claude does not perform this step; it pauses at this blocking
    checkpoint and resumes on the user's signal.
  </action>
  <verify>
    <human-check>User confirms: bot booted, came online, and `logs/dexter.log` shows no new ERROR / silent failures attributable to the Phase 10 refactor.</human-check>
  </verify>
  <acceptance_criteria>
    - User-confirmed: `python bot.py` reaches online/READY (startup message or successful on_ready logged)
    - User-confirmed: `logs/dexter.log` and `logs/error.log` show no NEW ERROR / traceback / silent-failure lines from the three rewired paths
    - User-confirmed: clean shutdown on Ctrl+C with no orphaned-process errors
    - Resume signal "approved" received (or specific offending log lines reported for fix in the owning Wave-1 plan)
  </acceptance_criteria>
  <done>The bot boots clean via `python bot.py` and `logs/dexter.log` shows no new ERROR / silent failures from the refactor (user-approved).</done>
  <resume-signal>
    Type "approved" if the bot booted, came online, and `logs/dexter.log` shows no new ERROR
    / silent failures from the refactor. Otherwise paste the offending log line(s) and which
    rewired path they point to so the owning Wave-1 plan can be corrected.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This is a verification-only plan; it modifies no source and adds no boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | regression confirmation of the Phase 10 refactor | accept | No new attack surface — this gate's entire purpose is to confirm the extract-and-unit-test refactor introduced no behavioral regression. It adds no inputs, network calls, auth, or persisted data; it runs the existing test suite and a manual boot. A green suite + clean boot with no new silent failures in `logs/dexter.log` is the disposition evidence. |
</threat_model>

<verification>
- `pytest -q` exits 0 (full suite green; live-DB tests may skip)
- The three new logic suites pass within the full run
- Manual boot: `python bot.py` comes online and `logs/dexter.log` shows no new ERROR / silent failures from the refactor (human-approved)
</verification>

<success_criteria>
The full test suite stays green and the bot boots clean with no new silent failures in
`dexter.log` — the Phase 10 extraction is confirmed behavior-preserving (TEST-04).
</success_criteria>

<output>
Create `.planning/phases/10-critical-path-test-coverage/10-04-SUMMARY.md` when done,
recording the full-suite summary line (passed/skipped/failed) and the manual-boot result.
</output>
