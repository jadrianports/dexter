---
phase: 28-portfolio-finish-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md]
autonomous: false
requirements: [PORT-02, CICD-02, CICD-03]

must_haves:
  truths:
    - "A single owner-action runbook (28-HUMAN-UAT.md) exists, cloning the 23-HUMAN-UAT.md structure"
    - "The runbook documents CICD-02 (Settings → Pages → Source = GitHub Actions) as host-independent/do-now, WITH the explicit push-to-origin/main + CI-success dependency (the toggle alone does not publish)"
    - "The runbook documents CICD-03 as strictly post-tag (the v1.5 tag is cut by /gsd:complete-milestone, not this phase); GHCR visibility can only be flipped after release.yml first runs"
    - "The runbook documents PORT-02 as verbatim-only capture of two real Dexter lines replacing the {{DEXTER_DEMO_LINE_*}} tokens — never authored/improved (D-06), needs a live bot"
    - "The runbook documents the D-01.3 parked local visual pass (npm run dev) as host-INDEPENDENT and owner-closable at will"
    - "The owner is prompted during execution to perform the CICD-02 Pages toggle (attempt-now, D-03)"
  artifacts:
    - path: ".planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md"
      provides: "The D-04 owner-action release runbook for the three blocked-on-human items + the parked local visual pass"
      contains: "## Summary"
      min_lines: 40
  key_links:
    - from: ".planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md"
      to: ".github/workflows/pages.yml"
      via: "cites the workflow_run/CI-success-on-main trigger condition for CICD-02"
      pattern: "pages.yml"
    - from: ".planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md"
      to: "site/src/data/demo-transcript.ts"
      via: "PORT-02 step points at the {{DEXTER_DEMO_LINE_*}} tokens to replace"
      pattern: "DEXTER_DEMO_LINE"
---

<objective>
Produce the single owner-action release runbook (`28-HUMAN-UAT.md`) that hands off the three
genuinely blocked-on-human items — PORT-02 (verbatim demo lines), CICD-02 (GitHub Pages toggle),
CICD-03 (GHCR visibility flip) — plus the one parked-but-host-independent local visual pass (D-01.3).
Then, per D-03, prompt the owner to perform the CICD-02 Pages toggle now, since it is host-independent
and has no tag dependency (it is the only one of the three actionable during the phase).

Purpose: This phase closes green while accurately parking what needs the owner or the live bot,
matching the Phase 23/24 precedent (D-04). CICD-02 gets an attempt-now nudge because it is a single
GitHub-UI toggle with zero host dependency — an asymmetry that justifies not leaving it purely deferred.
Output: `.planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md`; an owner prompt for the toggle.

This plan writes NO feature code. It writes one runbook and pauses once for an owner action.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/28-portfolio-finish-release/28-CONTEXT.md
@.planning/phases/28-portfolio-finish-release/28-RESEARCH.md
@.planning/phases/28-portfolio-finish-release/28-PATTERNS.md

@.planning/phases/23-portfolio-surface-ci-cd/23-HUMAN-UAT.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- **File** `.planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md` — the D-04 owner runbook,
  frontmatter + numbered tests (`expected:`/`result:`) + a `## Summary` counts block + a `## Gaps`
  section, cloning `23-HUMAN-UAT.md`.

No code is created or modified. `site/src/data/demo-transcript.ts` (D-06), `.github/workflows/pages.yml`
and `.github/workflows/release.yml` (already wired) are READ ONLY — cited, never edited.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write 28-HUMAN-UAT.md — the owner-action release runbook</name>
  <files>.planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/phases/23-portfolio-surface-ci-cd/23-HUMAN-UAT.md` — the exact structural analog:
      frontmatter (`status`/`phase`/`source`/`started`/`updated`), numbered `### N.` tests with
      `expected:`/`result:` pairs, a `## Summary` counts block, a `## Gaps` section
    - `.planning/phases/28-portfolio-finish-release/28-CONTEXT.md` — D-01 (the parked visual pass),
      D-04 (the three items + verbatim-only PORT-02 instruction), D-05 (the tag/CICD-03 sequencing),
      D-06 (why demo-transcript.ts scaffolding stays untouched)
    - `.planning/phases/28-portfolio-finish-release/28-RESEARCH.md` — Pitfall 2 (the push-to-main gap:
      local main is ~92 commits ahead of origin, the toggle alone does not publish), Pitfall 3 (no
      v1.5 tag exists yet → CICD-03 literally cannot run in-phase), and the verbatim `pages.yml` /
      `release.yml` trigger YAML in Code Examples
  </read_first>
  <action>
    Create `.planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md` cloning the `23-HUMAN-UAT.md`
    shape. Frontmatter: `status: partial`, `phase: 28-portfolio-finish-release`, `source:
    [28-CONTEXT.md D-01/D-04/D-05/D-06, 28-RESEARCH.md, 23-HUMAN-UAT.md]`, `started`/`updated` set to
    today (2026-07-18). Then numbered `### N.` tests, each with a falsifiable `expected:` citing the
    exact UI path/command and an evidence-grounded `result: **BLOCKED-ON-HUMAN.**` explaining what
    unblocks it. Document exactly these four items:

    1. **CICD-02 — enable GitHub Pages (do-now, host-independent).** expected: `Repo Settings → Pages →
       Build and deployment → Source: GitHub Actions`. State plainly that this is a manual once-only
       toggle that cannot be automated (enabling Pages via API needs an elevated `administration:write`/
       `pages:write` PAT that the zero-secrets-in-CI posture deliberately does not carry — RESEARCH
       "Don't Hand-Roll"). Add a SEPARATE explicit step/note (RESEARCH Pitfall 2): the toggle alone
       publishes nothing — `pages.yml` only fires on `workflow_run: ["CI"] completed` +
       `conclusion == success` + `head_branch == main`, so the site goes live only after a push to
       `origin/main` triggers a successful `ci.yml` run (local `main` is ~92 commits ahead of origin at
       research time). Cite the `pages.yml` trigger condition.
    2. **CICD-03 — flip GHCR package visibility (post-tag, sequenced).** expected: after the `v1.5` tag
       (cut by `/gsd:complete-milestone`, NOT this phase — D-02/D-05) fires `release.yml`
       (`push: tags: ["v*"]`) and creates the package, go to the package settings page → Change
       visibility → Public, then verify a logged-out `docker pull ghcr.io/jadrianports/dexter:<tag>`
       succeeds. State the ordering explicitly: no `v1.5` tag exists yet (RESEARCH Pitfall 3), so there
       is nothing to flip until that first release run — do this immediately AFTER the milestone tag.
       Cite the `release.yml` trigger.
    3. **PORT-02 — supply two verbatim real Dexter lines (parked, needs live bot).** expected: run the
       bot, capture two real Dexter outputs (one `/ask` or `/roast`, one ambient/roast line), paste
       them byte-for-byte into the `text` fields of `site/src/data/demo-transcript.ts`, replacing
       `{{DEXTER_DEMO_LINE_1}}` / `{{DEXTER_DEMO_LINE_2}}`, then rebuild. State the D-06 rule verbatim in
       spirit: **do NOT author or "improve" lines** — the demo's legitimacy rests on the words being
       real; the `previewSample` scaffolding stays untouched until the real capture (DEPLOY-F1-gated).
    4. **PORT-05 local visual pass (parked, host-INDEPENDENT, owner-closable at will).** expected:
       `cd site && npm run dev`, open the local URL, eyeball the proper-case copy, confirm the staged
       demo animation CYCLES rather than sits frozen, and confirm the dark "after hours" identity reads
       right. Note this is the one parked check that needs NO residential Discord host (a static site) —
       so unlike the 33-item live-Discord tail, the owner can close it whenever.

    In `## Summary` reuse the exact counts-block shape (`total`/`passed`/`issues`/`pending`/`skipped`/
    `blocked`). In `## Gaps`, state explicitly that PORT-05's automated confirmation half (D-01.1/D-01.2)
    is NOT in this runbook — it is the committed guard `tests/test_demo_transcript_guard.py` (plan 28-01)
    — and that this file covers only the genuinely manual items. Do NOT modify `demo-transcript.ts`, any
    `.github/workflows/*` file, or any Dexter-voice copy — this task authors an owner checklist only.
  </action>
  <verify>
    <automated>test -f .planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md && grep -q "Source: GitHub Actions" .planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md && grep -q "release.yml" .planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md && grep -q "DEXTER_DEMO_LINE" .planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md && grep -q "npm run dev" .planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md && grep -q "## Summary" .planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists at `.planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md` with frontmatter
      (`status`/`phase`/`source`/`started`/`updated`) matching the `23-HUMAN-UAT.md` shape.
    - It contains distinct numbered sections for CICD-02, CICD-03, PORT-02, and the PORT-05 local visual
      pass, each with an `expected:` and a `result:` line.
    - The CICD-02 section contains the explicit push-dependency note (the toggle alone does not publish;
      a push to `origin/main` + a successful `ci.yml` run is additionally required — Pitfall 2).
    - The CICD-03 section states it is sequenced strictly AFTER the `v1.5` tag (cut by
      `/gsd:complete-milestone`), not an in-phase action.
    - The PORT-02 section contains the verbatim-only instruction (do NOT author/improve lines) and points
      at `{{DEXTER_DEMO_LINE_1}}` / `{{DEXTER_DEMO_LINE_2}}`.
    - It contains a `## Summary` counts block and a `## Gaps` section noting PORT-05's automated half is
      the committed guard test, not this runbook.
    - `git status --short site/ .github/` shows no modified tracked files (no code/workflow touched).
  </acceptance_criteria>
  <done>The owner runbook exists, clones the 23-HUMAN-UAT.md structure, and documents all four items
  (CICD-02 do-now with push caveat, CICD-03 post-tag, PORT-02 verbatim-only, PORT-05 local visual pass)
  with falsifiable owner steps — no code, workflow, or Dexter-voice copy touched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Owner performs the CICD-02 GitHub Pages toggle (attempt-now)</name>
  <files>(none — owner GitHub-UI action; no repo file changes)</files>
  <action>
    Present the owner with the CICD-02 Pages toggle documented in `28-HUMAN-UAT.md` and pause for
    their action (D-03 "attempt what's doable now"). This is a genuine human-only step: automation was
    ruled out at research time — enabling GitHub Pages via API requires an elevated
    `administration:write`/`pages:write` PAT that this project's zero-secrets-in-CI posture (Phase 22
    D-04) deliberately does not carry, so Claude cannot perform it via CLI/API. Surface the steps in
    `<how-to-verify>` and accept either a "toggled" or a "deferred" response so the phase closes green.
    Do NOT push commits or cut any tag as part of this step (the consolidated push and the `v1.5` tag
    are `/gsd:complete-milestone`'s job, per D-02/D-05).
  </action>
  <how-to-verify>
    This is the D-03 attempt-now step — CICD-02 is host-independent with no tag dependency:
    1. Open the repo on GitHub → `Settings → Pages → Build and deployment → Source: GitHub Actions`.
    2. Understand the caveat (RESEARCH Pitfall 2): this toggle alone publishes NOTHING. `pages.yml`
       fires only on a successful `ci.yml` run on `main`, and local `main` is ~92 commits ahead of
       `origin/main` — so the site goes live only after those commits are pushed and CI passes on main
       (that push is a repo-wide event sequenced by `/gsd:complete-milestone`, not this phase).
    3. Once both the toggle and a push-triggered CI-success-on-main have happened, verify
       `jadrianports.github.io/dexter` renders and matches the local build.
    If you would rather defer, that is fine — the step is fully captured in `28-HUMAN-UAT.md` and the
    phase still closes green (Phase 23/24 precedent).
  </how-to-verify>
  <resume-signal>Type "toggled" (Pages source set) or "deferred — tracked in 28-HUMAN-UAT.md". Either
  answer lets the phase close green per D-03/D-04.</resume-signal>
  <done>The owner has been prompted for the CICD-02 Pages toggle and responded "toggled" or "deferred —
  tracked in 28-HUMAN-UAT.md"; no commit/tag/push was performed by this step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan writes one markdown runbook and pauses for one owner GitHub-UI action. No code, no dependency, no runtime surface, no user input reaches any executable path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Spoofing | PORT-02 execution invents a plausible-sounding Dexter line instead of capturing a real one (violates the D-06 authenticity contract) | mitigate | Not machine-enforceable — mitigated by the runbook's explicit "do NOT author or improve lines" instruction (D-04) + the documented D-06 rationale; the `{{DEXTER_DEMO_LINE_*}}` tokens stay intact until a real capture |
| T-28-SC | Tampering | npm/pip installs | accept | Zero new installs / zero new runtime surface — this plan is documentation + one human GitHub-UI action; RESEARCH Package Legitimacy Audit: not applicable |
</threat_model>

<verification>
- `.planning/phases/28-portfolio-finish-release/28-HUMAN-UAT.md` exists and clones the 23-HUMAN-UAT.md
  structure (frontmatter, numbered `expected:`/`result:` tests, `## Summary` counts, `## Gaps`).
- The runbook documents CICD-02 (do-now + push caveat), CICD-03 (post-tag), PORT-02 (verbatim-only),
  and the PORT-05 local visual pass.
- No tracked file under `site/` or `.github/` is modified (D-06 + already-wired workflows untouched).
- The owner has been prompted for the CICD-02 Pages toggle (checkpoint reached and answered).
</verification>

<success_criteria>
- One owner-action runbook (`28-HUMAN-UAT.md`) hands off PORT-02, CICD-02, CICD-03, and the parked
  local visual pass with falsifiable, evidence-grounded steps.
- CICD-02 is attempted now via an owner prompt; CICD-03 is correctly sequenced post-tag; PORT-02 stays
  parked with the verbatim-only contract intact.
- No feature code, workflow file, or Dexter-voice copy is touched.
</success_criteria>

<output>
Create `.planning/phases/28-portfolio-finish-release/28-02-SUMMARY.md` when done.
</output>
