---
phase: 28-portfolio-finish-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_demo_transcript_guard.py]
autonomous: true
requirements: [PORT-05]

must_haves:
  truths:
    - "npm run build in site/ boots clean and produces a fresh site/dist/"
    - "The existing tests/test_site_drift_guard.py still passes 3/3 against the fresh build (PORT-05 invite drift guard still holds at c7fd22e)"
    - "No raw {{DEXTER_DEMO_LINE token or any {{...}} placeholder reaches the shipped site/dist/ HTML"
    - "Both previewSample strings are present in shipped dist/ HTML (proving resolveLine() resolved the token→preview fallback at build time)"
    - "Proper-case hero/feature copy and the after-hours identity hex values (0a0c11 / ffb454) survive into the shipped dist/ artifact"
    - "A positive control proves the guard would fail if a raw token ever leaked (non-vacuous)"
  artifacts:
    - path: "tests/test_demo_transcript_guard.py"
      provides: "Durable D-01.1 + D-01.2 committed guard locking the resolveLine() token→previewSample invariant over the built site/dist/, plus a build-independent source-level structural guard"
      contains: "def test_"
      min_lines: 60
  key_links:
    - from: "tests/test_demo_transcript_guard.py"
      to: "site/dist/"
      via: "Path.rglob over the built tree (never git ls-files — dist/ is gitignored)"
      pattern: "rglob"
    - from: "tests/test_demo_transcript_guard.py"
      to: "site/src/data/demo-transcript.ts"
      via: "source-level structural read (build-independent guard)"
      pattern: "demo-transcript"
---

<objective>
Confirm the already-shipped `/site` portfolio redesign (PORT-05, `c7fd22e`) is still true, and lock
that confirmation as a durable, committed regression guard so it can never silently rot on a future
edit. This is the automated half of D-01: a fresh clean build, the existing site guard re-run green,
and a NEW committed guard test that asserts — over the shipped `site/dist/` bytes — that the demo
token→previewSample contract resolved correctly and no raw placeholder leaked, with a mandatory
positive control proving the guard is non-vacuous.

Purpose: PORT-05 is a "confirm it's still true" requirement, not a rebuild. A one-time manual grep
has zero durability; a committed guard makes the token/preview contract permanently enforced (D-01.2).
Output: `tests/test_demo_transcript_guard.py` — a new drift guard mirroring `test_site_drift_guard.py`.

This plan writes NO feature code and touches NO `/site` source. It adds one test file only.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/28-portfolio-finish-release/28-CONTEXT.md
@.planning/phases/28-portfolio-finish-release/28-RESEARCH.md
@.planning/phases/28-portfolio-finish-release/28-PATTERNS.md

@tests/test_site_drift_guard.py
@tests/test_invite_drift_guard.py
@site/src/data/demo-transcript.ts
</context>

<artifacts_this_phase_produces>
This plan creates:
- **File** `tests/test_demo_transcript_guard.py` — the new committed drift guard.
- **Helpers inside it** (import-or-duplicate from the analogs, planner's discretion per RESEARCH A2):
  `_site_dist_dir()`, `_dist_html_files()`, and a new `_dist_css_files()` (rglob `*.css`); a pure
  detector `_raw_tokens_in(text) -> list[str]` that the positive/negative controls feed synthetic
  `tmp_path` fixtures through.
- **Tests** (names may vary slightly; behavior is fixed): `test_no_raw_token_in_built_demo`
  (dist-scan, the D-01.1/D-01.2 guard), `test_dist_scan_detects_a_leaked_token` (mandatory positive
  control), `test_dist_scan_accepts_resolved_preview_samples` (negative control),
  `test_every_unfilled_token_entry_has_a_preview_sample` (build-independent structural guard).

No other files are created or modified. `site/dist/**` is produced by the build but is gitignored
(not a committed change). `site/src/data/demo-transcript.ts` is READ ONLY (D-06 — never modified).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build the site clean and confirm the existing PORT-05 guard still holds</name>
  <files>(no committed file change — produces the gitignored site/dist/ the new guard reads)</files>
  <read_first>
    - `site/package.json` — confirm the `build` script and Astro version before running it
    - `tests/test_site_drift_guard.py` — the existing guard being re-run (and the analog Task 2 mirrors)
    - `.planning/phases/28-portfolio-finish-release/28-RESEARCH.md` — Summary + Code Examples (the
      exact strings a correct build produces) + Pitfall 1 (why "after hours" the literal is NOT a valid
      assertion target)
  </read_first>
  <action>
    Run `npm run build` inside `site/` (Astro ^7.0.9; Node ≥22.12.0 is already present per RESEARCH).
    This regenerates `site/dist/` — the fresh shipped artifact the new guard in Task 2 scans, and the
    artifact that lets the dist-scan test actually RUN rather than skip. Then re-run the existing
    `pytest tests/test_site_drift_guard.py -q` and confirm it passes 3/3 against that fresh build —
    this is the PORT-05 "still true at milestone close" confirmation for the invite-URL surface.
    Do NOT modify any `site/` source, any `.astro`/`.ts`/`.css` file, or
    `site/src/data/demo-transcript.ts` (D-06 — the placeholder contract is untouchable). This task
    produces only the gitignored `site/dist/` output plus green-test evidence; it stages no committed
    change.
  </action>
  <verify>
    <automated>cd site && npm run build && cd .. && pytest tests/test_site_drift_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd site && npm run build` exits 0 with no errors.
    - `site/dist/index.html` exists after the build.
    - `pytest tests/test_site_drift_guard.py -q` reports 3 passed, 0 failed.
    - `git status --short site/` shows no modified tracked files under `site/` (dist/ is gitignored).
  </acceptance_criteria>
  <done>Fresh `site/dist/` present; the existing site drift guard is green 3/3 against it.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_demo_transcript_guard.py — the durable token→preview guard</name>
  <files>tests/test_demo_transcript_guard.py</files>
  <read_first>
    - `tests/test_site_drift_guard.py` — the exact analog: module-docstring shape, `_site_dist_dir()`/
      `_dist_html_files()` helpers, the `SITE_DIST_REQUIRED` skip/fail split, positive+negative control
      shape
    - `tests/test_invite_drift_guard.py` — source of the importable `_repo_root()` helper (lines 64-71)
    - `site/src/data/demo-transcript.ts` — the subject: the two `{{DEXTER_DEMO_LINE_*}}` token entries,
      their `previewSample` fallbacks, and `resolveLine()` (lines 41-44). READ ONLY — never edit (D-06)
    - `.planning/phases/28-portfolio-finish-release/28-RESEARCH.md` — Code Examples (the verbatim
      strings to assert: the two previewSample lines, the proper-case copy, the hex `0a0c11`/`ffb454`)
      and Pitfall 1 (do NOT assert the literal "after hours")
    - `.planning/phases/28-portfolio-finish-release/28-PATTERNS.md` — the per-line pattern assignments
  </read_first>
  <action>
    Create `tests/test_demo_transcript_guard.py` as a new sibling drift guard (RESEARCH Open Question 1
    recommendation — a new file, not an extension of `test_site_drift_guard.py`, to keep that file's
    docstring accurate). Open with a module docstring in the repo convention explaining why this guard
    exists as its own file, the dist-scan collection strategy, the `SITE_DIST_REQUIRED` skip/fail
    rationale, and a Tests list — framed around D-01.2 and the token-leak threat T-28-01. Get
    `_repo_root` by importing it from `tests.test_invite_drift_guard`; add `_site_dist_dir()`,
    `_dist_html_files()` (rglob `*.html`) and a new `_dist_css_files()` (rglob `*.css`) — import the
    first two from `tests.test_site_drift_guard` or duplicate the ~6-line helpers (RESEARCH A2 says
    either is safe). Add a pure detector `_raw_tokens_in(text)` returning every `{{...}}` match via
    regex `\{\{[A-Z0-9_]+\}\}`, so the controls can feed it synthetic strings.

    Write four tests. (1) The dist-scan guard: mirror the `SITE_DIST_REQUIRED` skip/fail split exactly
    (reuse the env-var name, do not invent a new one); over `_dist_html_files(_site_dist_dir())` assert
    `_raw_tokens_in` finds ZERO tokens in every shipped HTML file, assert BOTH previewSample strings
    (`"Seventeen songs and four of them are the same sad boy. Bold curatorial vision."` and
    `"Third time today. I'm keeping notes. For later."`) are present (proving `resolveLine()` resolved
    the fallback at build time), assert a representative proper-case copy subset is present (e.g.
    `"An AI with opinions"`, `"What it actually does"`, `"Watch it work"`, `"Known limits"`), and assert
    the after-hours identity survives by checking the hex substrings `0a0c11` and `ffb454` are present
    in `_dist_css_files(...)` — NEVER the literal phrase "after hours" (Pitfall 1: it is a stripped CSS
    comment and would fail permanently on a correct build). (2) The mandatory positive control: write a
    `tmp_path` HTML fixture containing a literal `{{DEXTER_DEMO_LINE_1}}` and assert `_raw_tokens_in`
    flags it non-empty. (3) The negative control: write a `tmp_path` fixture containing both real
    previewSample strings and assert `_raw_tokens_in` returns empty (no false-positive on legitimate
    resolved output). (4) The build-independent structural guard: read
    `site/src/data/demo-transcript.ts` as text and assert every entry whose `text` still holds a
    `{{...}}` token has a NON-EMPTY `previewSample` — a hardcoded-pair assertion over the two known
    dexter entries is proportionate (RESEARCH Open Question 2); this guard asserts the pairing invariant
    only and must NOT assert token absence in the source (tokens legitimately live in the source — that
    is the D-06 contract). Use descriptive f-string assertion messages naming what leaked, never a bare
    `assert`. No `<action>` code is copied verbatim into the file blindly — read the analog and follow
    its structure.
  </action>
  <verify>
    <automated>pytest tests/test_demo_transcript_guard.py -q && SITE_DIST_REQUIRED=1 pytest tests/test_demo_transcript_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_demo_transcript_guard.py -q` reports at least 4 passed, 0 failed (dist present
      from Task 1, so the dist-scan runs, not skips).
    - `SITE_DIST_REQUIRED=1 pytest tests/test_demo_transcript_guard.py -q` also passes (the hard-fail
      path is exercised against a real dist/, proving the guard is not silently skipping in CI shape).
    - The file contains a positive-control test that writes a literal `{{DEXTER_DEMO_LINE_1}}` fixture
      and asserts the detector flags it.
    - `grep -n "0a0c11" tests/test_demo_transcript_guard.py` and `grep -n "ffb454"` both match; the
      literal string `"after hours"` does NOT appear as a dist assertion target (Pitfall 1).
    - `git status --short site/src/data/demo-transcript.ts` shows no modification (D-06 respected).
  </acceptance_criteria>
  <done>The new guard is green and non-vacuous: it catches a leaked token via the positive control, it
  proves the shipped bytes carry resolved previews + proper-case copy + the after-hours hex identity,
  and it locks the source-level token/preview pairing — all without touching demo-transcript.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a read-only test over local, developer-controlled build artifacts. No network, no user input, no runtime surface, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | A future edit drops `previewSample` while `text` still holds a `{{...}}` token, so a raw placeholder ships as visible page content | mitigate | This plan's entire deliverable — `tests/test_demo_transcript_guard.py` dist-scan + structural guard, with a positive control proving it is non-vacuous |
| T-28-SC | Tampering | npm/pip installs | accept | Zero new installs / zero new runtime surface — the guard uses only stdlib (`os`, `pathlib`, `re`) + already-installed `pytest`; RESEARCH Package Legitimacy Audit: not applicable |
</threat_model>

<verification>
- `cd site && npm run build` boots clean; `site/dist/index.html` exists.
- `pytest tests/test_site_drift_guard.py -q` → 3 passed (PORT-05 invite surface still holds).
- `pytest tests/test_demo_transcript_guard.py -q` → ≥4 passed; positive control present and green.
- `SITE_DIST_REQUIRED=1 pytest tests/test_demo_transcript_guard.py -q` → passes against a real dist/.
- `pytest -q` full suite green (no regression — this plan only adds a test file).
</verification>

<success_criteria>
- A committed `tests/test_demo_transcript_guard.py` durably asserts: no raw `{{...}}` token in shipped
  `dist/`, both previewSample strings present, proper-case copy present, `0a0c11`/`ffb454` identity
  present — with a mandatory positive control.
- The build-independent structural guard locks the source-level token→previewSample pairing.
- `site/src/data/demo-transcript.ts` is unmodified (D-06).
</success_criteria>

<output>
Create `.planning/phases/28-portfolio-finish-release/28-01-SUMMARY.md` when done.
</output>
