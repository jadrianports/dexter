---
phase: 06-speed-caching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - database.py
  - models/queue.py
  - services/metrics.py
  - tests/conftest.py
  - tests/test_phase6_perf.py
autonomous: true
requirements: [PERF-03, PERF-06]
must_haves:
  truths:
    - "A repeated non-URL search query resolves from Postgres without re-searching YouTube (resolution cache hit)"
    - "Pipeline timing (cache-hit rate, avg time-to-first-audio, avg download) is observable via an in-memory rolling aggregate (baselined against the user's PC run env, NOT Oracle — D-19)"
  artifacts:
    - path: "database.py"
      provides: "resolution_cache table in SCHEMA_SQL + normalize_search_query / get_resolution_cache / set_resolution_cache helpers"
      contains: "resolution_cache"
    - path: "services/metrics.py"
      provides: "PerfMetrics rolling-aggregate class (deque-backed)"
      contains: "class PerfMetrics"
    - path: "config.py"
      provides: "Phase 6 constants: PREFETCH_TIMEOUT_SECONDS, RES_CACHE_TTL_DAYS, SPONSORBLOCK_CATEGORIES, PERF_ROLLING_WINDOW"
      contains: "PREFETCH_TIMEOUT_SECONDS"
    - path: "models/queue.py"
      provides: "_prefetch_video_id / _prefetch_task state fields, reset in clear()"
      contains: "_prefetch_video_id"
    - path: "tests/test_phase6_perf.py"
      provides: "Wave-0 failing-test scaffold for normalization, resolution cache, and PerfMetrics"
      contains: "class TestNormalizeQuery"
  key_links:
    - from: "database.py set_resolution_cache"
      to: "resolution_cache table"
      via: "INSERT ... ON CONFLICT (query_key) DO UPDATE SET ... expires_at"
      pattern: "ON CONFLICT \\(query_key\\)"
    - from: "tests/conftest.py teardown"
      to: "resolution_cache table"
      via: "DROP TABLE ... resolution_cache CASCADE"
      pattern: "resolution_cache"
---

<objective>
Lay the Phase 6 foundation: the config constants, the Postgres `resolution_cache` table + asyncpg helpers, the pure `normalize_search_query` function, the `PerfMetrics` rolling-aggregate class, the `MusicQueue` prefetch state fields, the conftest teardown, and the Wave-0 failing-test scaffold (`tests/test_phase6_perf.py`). The cache is Postgres-backed rather than in-memory specifically so it survives the bot's frequent restarts (on-demand local run, deploy parked) — an in-memory cache would be cold most sessions (D-08).

Every downstream plan (02 youtube pipeline, 03 audio pipeline, 04 wiring) consumes contracts defined here: the config constants, the DB helpers, and the `PerfMetrics` class. This is the interface-first plan — define contracts before implementations. It also satisfies the Wave-0 obligation from 06-VALIDATION.md so the rest of the phase has automated verification to run against.

Purpose: Establish the data layer + measurement layer + test scaffold so the pipeline plans have stable, tested contracts to build on. Satisfies PERF-03 (resolution cache) and PERF-06 (instrumentation aggregate); seeds the test files PERF-01/04/05/07 will fill.
Output: `config.py` constants, `database.py` table + helpers, `services/metrics.py` PerfMetrics, `models/queue.py` prefetch fields, `tests/conftest.py` teardown, `tests/test_phase6_perf.py`.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-speed-caching/06-CONTEXT.md
@.planning/phases/06-speed-caching/06-RESEARCH.md
@.planning/phases/06-speed-caching/06-PATTERNS.md
@.planning/phases/06-speed-caching/06-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (defined in THIS plan)

**Config constants (`config.py`, new `# --- Phase 6: Speed & Caching ---` block):**
- `PREFETCH_TIMEOUT_SECONDS = 45` (D-10; consumed by Plan 04 prefetch)
- `RES_CACHE_TTL_DAYS = 14` (D-07; consumed by `set_resolution_cache`)
- `SPONSORBLOCK_CATEGORIES = frozenset({"sponsor","selfpromo","intro","outro","interaction","music_offtopic"})` (D-14; consumed by Plan 02)
- `PERF_ROLLING_WINDOW = 50` (D-18; consumed by `PerfMetrics`)

**Database (`database.py`):**
- `resolution_cache` table (columns: `query_key` TEXT PK, `video_id` TEXT NOT NULL, `title` TEXT, `created_at` TIMESTAMPTZ DEFAULT now(), `expires_at` TIMESTAMPTZ NOT NULL) + `idx_rescache_expires` index
- `normalize_search_query(q: str) -> str` (pure)
- `get_resolution_cache(pool, *, query_key: str) -> dict | None`
- `set_resolution_cache(pool, *, query_key: str, video_id: str, title: str | None, ttl_days: int) -> None`

**Metrics (`services/metrics.py`):**
- `PerfMetrics` class with `record_cache_result(hit)`, `record_download(elapsed)`, `record_search(elapsed)`, `record_ttfa(elapsed)`, `summary() -> dict`

**Queue (`models/queue.py`):**
- `_prefetch_video_id: str | None`, `_prefetch_task: asyncio.Task | None` (reset in `clear()`)

**Tests:** `tests/test_phase6_perf.py` (new) with `TestNormalizeQuery`, `TestPerfMetrics`, `TestResolutionCache`, `test_url_bypasses_cache`, `test_prefetch_task_spawned`, `test_prefetch_skips_cached`, `test_prefetch_stale_gen`, `test_timing_logged` (the prefetch + timing tests start as expected-fail placeholders Plan 04 satisfies).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config constants, resolution_cache schema + helpers, PerfMetrics, queue fields</name>
  <files>config.py, database.py, services/metrics.py, models/queue.py</files>
  <read_first>
    - config.py (the full file — locate the end of the Phase 5 block, ~line 104, and the Phase 7 block at lines 107-126 as the formatting analog per 06-PATTERNS.md)
    - database.py SCHEMA_SQL block (06-PATTERNS.md cites guild_queues/user_favorites at lines 119-148) AND the `update_artist_count` helper (lines 239-252) as the ON CONFLICT asyncpg analog
    - models/queue.py `__init__` Phase 7 fields (06-PATTERNS.md lines 83-85) and `clear()` Phase 7 resets (lines 221-225)
    - 06-RESEARCH.md Pattern 6 (resolution cache table + helpers, lines 396-447) and Pattern 7 (PerfMetrics, lines 464-508)
    - CLAUDE.md "Implementation Gotchas" — asyncpg SCHEMA_SQL is plain DDL with NO `$N` params (Pitfall 1)
  </read_first>
  <behavior>
    - normalize_search_query("  Lo-Fi   Beats  ") returns "lo-fi beats" (strip + lower + collapse internal whitespace to single spaces)
    - PerfMetrics(window=10) with one True + one False cache result → summary()["cache_hit_rate"] == 50.0
    - PerfMetrics.summary() on an empty instance returns 0.0 for all averages and "samples": 0 (no ZeroDivisionError)
    - PerfMetrics(window=2) keeps only the last 2 samples (deque maxlen behavior)
  </behavior>
  <action>
    Add a `# --- Phase 6: Speed & Caching ---` block to config.py AFTER the Phase 5 block (after ~line 104), defining exactly: PREFETCH_TIMEOUT_SECONDS = 45, RES_CACHE_TTL_DAYS = 14, SPONSORBLOCK_CATEGORIES = frozenset({"sponsor","selfpromo","intro","outro","interaction","music_offtopic"}), PERF_ROLLING_WINDOW = 50. Annotate each with its D-NN source (D-10, D-07, D-14, D-18).

    In database.py, append to SCHEMA_SQL (before its closing triple-quote) the resolution_cache CREATE TABLE IF NOT EXISTS with columns query_key TEXT PRIMARY KEY, video_id TEXT NOT NULL, title TEXT, created_at TIMESTAMPTZ DEFAULT now(), expires_at TIMESTAMPTZ NOT NULL, plus CREATE INDEX IF NOT EXISTS idx_rescache_expires ON resolution_cache(expires_at). DDL ONLY — no `$N` params in the schema string (CLAUDE.md Pitfall 1). Add the pure function normalize_search_query(q) that returns re.sub(r"\\s+", " ", q.strip().lower()). Add async get_resolution_cache(pool, *, query_key) using conn.fetchrow with "SELECT video_id, title FROM resolution_cache WHERE query_key = $1 AND expires_at > now()" returning dict(row) or None. Add async set_resolution_cache(pool, *, query_key, video_id, title, ttl_days) computing expires = datetime.now(timezone.utc) + timedelta(days=ttl_days) in Python and INSERT ... ON CONFLICT (query_key) DO UPDATE SET video_id=EXCLUDED.video_id, title=EXCLUDED.title, expires_at=EXCLUDED.expires_at (Pitfall 5: TTL must refresh on hit). All values via positional $N params (V5 / SQL-injection mitigation T-06-01) — never string-interpolated.

    Create services/metrics.py with the PerfMetrics class verbatim from 06-RESEARCH.md Pattern 7: four collections.deque(maxlen=window) fields (cache_hits bool, download_times, search_times, ttfa_times), the four record_* methods, and summary() returning {cache_hit_rate, avg_download_s, avg_ttfa_s, avg_search_s, samples} with guarded division (0.0 when empty).

    In models/queue.py __init__, after the Phase 7 fields, add self._prefetch_video_id: str | None = None and self._prefetch_task: asyncio.Task | None = None. In clear(), alongside the Phase 7 resets, add self._prefetch_video_id = None and self._prefetch_task = None. (asyncio is already imported in queue.py per 06-PATTERNS.md; confirm in read_first.)
  </action>
  <verify>
    <automated>python -c "import config; assert config.PREFETCH_TIMEOUT_SECONDS==45 and config.RES_CACHE_TTL_DAYS==14 and 'music_offtopic' in config.SPONSORBLOCK_CATEGORIES and config.PERF_ROLLING_WINDOW==50; import database; assert database.normalize_search_query('  Lo-Fi   Beats ')=='lo-fi beats'; from services.metrics import PerfMetrics; m=PerfMetrics(window=10); m.record_cache_result(True); m.record_cache_result(False); assert m.summary()['cache_hit_rate']==50.0; assert PerfMetrics(window=2).summary()['samples']==0; from models.queue import MusicQueue; q=MusicQueue(1); assert q._prefetch_video_id is None; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `config.SPONSORBLOCK_CATEGORIES` is a frozenset containing exactly the 6 D-14 categories.
    - `database.SCHEMA_SQL` contains the substring `resolution_cache` and `idx_rescache_expires`; contains NO `$` character inside the schema string (Pitfall 1).
    - `database.set_resolution_cache` source contains `ON CONFLICT (query_key) DO UPDATE SET` and updates `expires_at` (Pitfall 5).
    - `services/metrics.py` defines `class PerfMetrics` using `collections.deque` with `maxlen`.
    - `models/queue.py` `clear()` resets both `_prefetch_video_id` and `_prefetch_task` to None.
    - The verify command exits 0 and prints OK.
  </acceptance_criteria>
  <done>Config constants present and correct; resolution_cache table + 3 helpers in database.py; PerfMetrics class in services/metrics.py; queue prefetch fields added and reset on clear; one-liner verify prints OK.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 test scaffold — tests/test_phase6_perf.py + conftest teardown</name>
  <files>tests/test_phase6_perf.py, tests/conftest.py</files>
  <read_first>
    - tests/conftest.py lines 42-45 (the DROP TABLE teardown listing guild_queues, song_history, user_favorites, user_playlists, user_playlist_tracks CASCADE — resolution_cache must be added to this list)
    - tests/test_queue.py (pure unit-test structure analog per 06-PATTERNS.md) and tests/test_database_phase4.py (live-DB integration analog)
    - 06-VALIDATION.md "Per-Task Verification Map" (lines 41-57) — the exact test names this file must define
    - database.py (the helpers under test from Task 1) and services/metrics.py
  </read_first>
  <action>
    Create tests/test_phase6_perf.py covering PERF-03 and PERF-06 with PASSING tests, plus expected-fail placeholders for PERF-01 prefetch tests that Plan 04 will satisfy. Define:
    (1) TestNormalizeQuery (pure): test_strips_whitespace, test_lowercases, test_collapses_internal_whitespace — assert normalize_search_query behavior.
    (2) TestPerfMetrics (pure): test_cache_hit_rate (50.0 from one True/one False), test_empty_summary_no_divzero, test_window_maxlen.
    (3) TestResolutionCache: a live-DB integration test_hit and test_expired_ttl_miss using the live `pool` fixture from conftest (insert via set_resolution_cache with ttl_days=14 then get_resolution_cache returns the row; insert an already-expired row by writing then asserting a get with a past expiry misses — for expiry, write with set_resolution_cache then manually UPDATE expires_at to a past timestamp via the pool and assert get returns None). Mark these `@pytest.mark.asyncio` and gate on the live `pool` fixture the existing phase-4 tests use.
    (4) test_url_bypasses_cache (unit): assert YouTubeService().is_url() correctly classifies a youtube.com URL as True and a plain query as False (the guard Plan 04 relies on to bypass cache for URLs — D-09).
    (5) Prefetch placeholders for Plan 04 — test_prefetch_task_spawned, test_prefetch_skips_cached, test_prefetch_stale_gen — mark each with `@pytest.mark.xfail(reason="implemented in Plan 04-04", strict=False)` so the file is green now and Plan 04 removes the xfail when wiring lands.
    (6) test_timing_logged placeholder — same xfail treatment, satisfied when Plan 04 records timing.

    In tests/conftest.py, add `resolution_cache` to the DROP TABLE teardown list (line ~42-45) so live-DB tests start clean. Keep the CASCADE.
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase6_perf.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_phase6_perf.py` defines classes `TestNormalizeQuery`, `TestPerfMetrics`, `TestResolutionCache` and functions `test_url_bypasses_cache`, `test_prefetch_task_spawned`, `test_prefetch_skips_cached`, `test_prefetch_stale_gen`, `test_timing_logged`.
    - `tests/conftest.py` DROP TABLE teardown string contains `resolution_cache`.
    - `pytest tests/test_phase6_perf.py -x -q` exits 0 (pure + live-DB tests pass; prefetch/timing placeholders xfail, not error). If the live `pool` fixture is unavailable in the runner, the resolution-cache integration tests must skip (not error) — guard with the same skip pattern test_database_phase4.py uses.
  </acceptance_criteria>
  <done>test_phase6_perf.py exists with all named tests; conftest teardown drops resolution_cache; the file runs green (pure + live pass, prefetch/timing xfail).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user → `/play` query → resolution_cache write | User-controlled search text becomes a DB cache key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `resolution_cache` query_key (ASVS V5) | mitigate | `normalize_search_query` sanitizes (strip/lower/collapse) before write; ALL writes/reads use asyncpg positional `$N` params — no string interpolation in `get_resolution_cache`/`set_resolution_cache` |
| T-06-02 | Tampering | Cache poisoning (query→wrong video_id) | accept | Per-user query input only; normalized keys cannot collide cross-user maliciously; 14-day TTL bounds staleness (D-07) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs NO new packages (06-RESEARCH.md Package Legitimacy Audit: none) — no install task, no slopcheck checkpoint required |
</threat_model>

<verification>
- `python -c "import config, database; from services.metrics import PerfMetrics; from models.queue import MusicQueue"` imports cleanly.
- `python -m pytest tests/test_phase6_perf.py -x -q` exits 0.
- `grep -v '^#' database.py | grep -c resolution_cache` returns ≥ 3 (table + 2 helpers reference it).
</verification>

<success_criteria>
- All four Phase 6 config constants defined with correct values.
- resolution_cache table in SCHEMA_SQL (DDL-only, no `$N`); normalize/get/set helpers present and parameterized.
- PerfMetrics class returns correct hit rate and survives empty state.
- Queue prefetch fields present and reset on clear().
- tests/test_phase6_perf.py green; conftest drops resolution_cache.
</success_criteria>

<output>
Create `.planning/phases/06-speed-caching/06-01-SUMMARY.md` when done.
</output>
