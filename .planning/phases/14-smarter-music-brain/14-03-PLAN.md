---
phase: 14-smarter-music-brain
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - cogs/ai.py
  - tests/test_autoqueue_wiring.py
autonomous: true
requirements: [BRAIN-01]
must_haves:
  truths:
    - "try_auto_queue injects a guild-scoped 'recently skipped' negative block into the recommendation prompt (D-01)"
    - "try_auto_queue injects an unattributed collective 'the room tends to like' positive block recalled from in-voice members' taste_episode memory (D-03)"
    - "a candidate whose artist matches a recently-skipped artist is rejected by the hard post-filter, independently of validate_youtube_match (D-02)"
    - "when both hint sources are empty, the recommendation prompt is byte-identical to today (no memory beats a wrong memory)"
    - "should_start_playback (scar #2) is untouched — playback still gates on voice_client.is_playing(), never queue.is_playing"
  artifacts:
    - path: "cogs/ai.py"
      provides: "try_auto_queue extended with negative + positive hint fetch and D-02 hard filter"
      contains: "get_recently_skipped"
    - path: "tests/test_autoqueue_wiring.py"
      provides: "source-assertion regression that the wiring + scar guard are present"
      contains: "is_recently_skipped_artist"
  key_links:
    - from: "cogs/ai.py::try_auto_queue"
      to: "database.get_recently_skipped"
      via: "guild-scoped fetch feeding build_recommendation_prompt(recently_skipped=...)"
      pattern: "get_recently_skipped"
    - from: "cogs/ai.py::try_auto_queue"
      to: "memory_service.recall(kind='taste_episode')"
      via: "per in-voice member recall → select_positive_taste_context"
      pattern: "kind=\"taste_episode\""
    - from: "cogs/ai.py per-suggestion loop"
      to: "logic.autoqueue.is_recently_skipped_artist"
      via: "independent second gate on the validated candidate"
      pattern: "is_recently_skipped_artist"
---

<objective>
Make auto-queue taste-aware (BRAIN-01) by wiring the plan 14-01/14-02 substrate into
`cogs/ai.py::try_auto_queue`, without touching the scar-#2 playback gate:

- **D-01 negative hint:** fetch `database.get_recently_skipped(guild_id, since=now-AUTO_QUEUE_SKIP_LOOKBACK_DAYS,
  limit=AUTO_QUEUE_SKIP_HINT_CAP)` and pass it as `recently_skipped=` to `build_recommendation_prompt`.
- **D-03 positive hint:** for each non-bot member in the voice channel (REUSE the exact member set already
  computed for the `auto_queue_ignored` write at ~line 410), call `memory_service.recall(user_id, guild_id,
  <anchor>, kind="taste_episode")`, blend via `select_positive_taste_context(cap=AUTO_QUEUE_POSITIVE_TASTE_CAP)`,
  and pass it as `positive_taste=`. Omit the block (byte-identical) when nothing clears the recall floor.
- **D-02 hard post-filter:** inside the existing per-suggestion validation loop, after a candidate passes
  `validate_youtube_match`, ALSO reject it if `is_recently_skipped_artist(candidate_artist, skipped_artists)`
  — an INDEPENDENT second gate (validate_youtube_match stays unchanged).

Purpose: auto-queue stops re-suggesting what the room keeps skipping and leans toward what it likes.
Output: extended try_auto_queue + a source-assertion regression test.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-smarter-music-brain/14-CONTEXT.md
@.planning/phases/14-smarter-music-brain/14-RESEARCH.md
@.planning/phases/14-smarter-music-brain/14-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (full Phase 14 symbol list — for the source-grounding review pass)

- `database.py`: `get_recently_skipped`, `get_user_top_artist`, `get_artist_cooccurrence`; `search_memories` `kind` param
- `services/memory.py`: `recall` `kind` param
- `logic/autoqueue.py`: `is_recently_skipped_artist`
- `logic/taste.py`: `select_positive_taste_context`
- `personality/prompts.py`: `build_recommendation_prompt` kwargs; `build_discover_commentary_prompt`; `build_jam_suggestion_prompt`
- `personality/responses.py`: `DISCOVER_NO_HISTORY`
- `cogs/music.py`: `/discover` command + confirm-to-queue View
- `cogs/library.py`: `/jam suggest` subcommand + propose-and-confirm View
- `config.py`: 6 Phase 14 knobs

**This plan (14-03) consumes** the above (does not create new module-level symbols); it extends `cogs/ai.py::try_auto_queue` and adds imports of `get_recently_skipped`, `is_recently_skipped_artist`, `select_positive_taste_context`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Wire the negative + positive taste hints into try_auto_queue (D-01/D-03)</name>
  <files>cogs/ai.py, tests/test_autoqueue_wiring.py</files>
  <read_first>
    - cogs/ai.py lines 1-41 (imports — add `get_recently_skipped` from database, `is_recently_skipped_artist` from logic.autoqueue, `select_positive_taste_context` from logic.taste; `build_recommendation_prompt` already imported)
    - cogs/ai.py lines 255-448 (`try_auto_queue` full — the outer try at 269, the `cleaned` recent-songs build at 278-286, the `prompt = build_recommendation_prompt(cleaned)` at 288, the in-voice member set at 409-413, the scar-#2 `should_start_playback` block at 372-391)
    - database.py `get_recently_skipped` (created in 14-01) + services/memory.py `recall` kind param (14-01)
    - .planning/phases/14-smarter-music-brain/14-PATTERNS.md § "cogs/ai.py — try_auto_queue extended" (exact insertion points + the member-set reuse note)
    - .planning/phases/14-smarter-music-brain/14-RESEARCH.md § "Open Questions" #3 (the recall anchor query_text choice)
  </read_first>
  <action>
    Import `get_recently_skipped` from database, `is_recently_skipped_artist` from logic.autoqueue,
    `select_positive_taste_context` from logic.taste at the top of cogs/ai.py.

    Inside `try_auto_queue`, INSIDE the existing outer try block, immediately before line 288
    (`prompt = build_recommendation_prompt(cleaned)`), fetch both hints (each degrading to an empty
    list on failure, never raising — matching the surrounding degrade-to-empty contract):
    (a) Negative: compute `since = datetime.now(timezone.utc) - timedelta(days=config.AUTO_QUEUE_SKIP_LOOKBACK_DAYS)`
    and call `await get_recently_skipped(self.bot.pool, guild_id=str(guild.id), since=since,
    limit=config.AUTO_QUEUE_SKIP_HINT_CAP)`; map rows to `[{"title": r["title"], "artist": r["artist"]}]`.
    Also derive `skipped_artists = [r["artist"] for r in rows if r["artist"]]` for the Task 2 hard filter.
    (b) Positive: compute the in-voice non-bot member list by REUSING the same enumeration already used
    for the auto_queue_ignored write (`vc = guild.voice_client; voice_members = [m for m in vc.channel.members
    if not m.bot] if vc and vc.channel else []`) — do NOT compute a second, different member set. Only if
    `self.bot.memory_service` exists, for each member call `await memory_service.recall(str(member.id),
    str(guild.id), <anchor>, kind="taste_episode")` (collect each member's list[str]), where `<anchor>` is a
    fixed module-level anchor phrase such as `"music taste and listening preferences"` (Claude's discretion
    per RESEARCH OQ#3 — any stable anchor works since recall is already scoped to user_id + kind). Blend via
    `select_positive_taste_context(member_facts, cap=config.AUTO_QUEUE_POSITIVE_TASTE_CAP)`.

    Pass both into the prompt: `build_recommendation_prompt(cleaned, recently_skipped=<neg or None>,
    positive_taste=<pos or None>)`. When both are empty the call is byte-identical to today (Pattern 1).
    Keep `skipped_artists` in scope for Task 2. Do NOT alter the `should_start_playback` block (scar #2).

    Add tests/test_autoqueue_wiring.py: a source-assertion module (no live Discord needed) using
    `inspect.getsource(AICog.try_auto_queue)` that asserts the wiring exists — references to
    `get_recently_skipped`, `recall(` with `kind="taste_episode"`, `select_positive_taste_context`,
    `build_recommendation_prompt(` with `recently_skipped=` and `positive_taste=` — and a scar guard
    asserting the source still contains `should_start_playback(` and NOT a `not queue.is_playing` gate.
  </action>
  <verify>
    <automated>python -m pytest tests/test_autoqueue_wiring.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cogs/ai.py` imports `get_recently_skipped`, `is_recently_skipped_artist`, `select_positive_taste_context`
    - `try_auto_queue` source contains `get_recently_skipped(`, `kind="taste_episode"`, `select_positive_taste_context(`, and `build_recommendation_prompt(` called with `recently_skipped=` and `positive_taste=`
    - The in-voice member set is the SAME enumeration reused (source contains a single `for m in vc.channel.members if not m.bot`-style comprehension feeding both the recall fan-out and the existing ignored-write)
    - `try_auto_queue` source still contains `should_start_playback(` and does NOT contain `not queue.is_playing` (scar #2 intact)
    - `pytest tests/test_autoqueue_wiring.py -q` exits 0
  </acceptance_criteria>
  <done>Negative + positive hints wired; byte-identical when empty; member set reused; scar #2 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the D-02 hard post-filter to the per-suggestion validation loop</name>
  <files>cogs/ai.py, tests/test_autoqueue_wiring.py</files>
  <read_first>
    - cogs/ai.py lines 311-364 (the per-suggestion loop: `validate_youtube_match` gate at 324-341, `validated = result`, the `async_extract` + duration check + `Track(...)` + `queue.add` at 343-364)
    - logic/autoqueue.py `is_recently_skipped_artist` (created in 14-02)
    - .planning/phases/14-smarter-music-brain/14-PATTERNS.md § "cogs/ai.py — D-02 hard post-filter insertion point"
  </read_first>
  <action>
    Inside the per-suggestion loop of `try_auto_queue`, after a candidate has passed `validate_youtube_match`
    (i.e. `validated is not None`) and before it is extracted/queued, add an INDEPENDENT second gate: if
    `is_recently_skipped_artist(suggestion["artist"], skipped_artists)` returns True, `continue` to the next
    suggestion (log at info that the candidate was dropped as a recently-skipped artist). `skipped_artists`
    is the list derived in Task 1. This runs independently of and after `validate_youtube_match` (D-02 —
    the hallucination guard is unchanged; this is the belt-and-suspenders skip filter). Do not modify
    `validate_youtube_match`'s own call.
    Extend tests/test_autoqueue_wiring.py with an assertion that the loop source calls
    `is_recently_skipped_artist(` and that it is a separate statement from the `validate_youtube_match(` call
    (both present, distinct). Also add a direct behavioral unit assertion importing
    `logic.autoqueue.is_recently_skipped_artist` and confirming a skipped-artist candidate is rejected
    (reinforces the gate semantics without needing a live Discord/Gemini path).
  </action>
  <verify>
    <automated>python -m pytest tests/test_autoqueue_wiring.py tests/test_autoqueue_validate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `try_auto_queue` source contains BOTH `validate_youtube_match(` and `is_recently_skipped_artist(` as distinct calls
    - The hard filter appears after the `validated is not None` branch and short-circuits via `continue`
    - `validate_youtube_match`'s call signature/body is unchanged from pre-Phase-14
    - `pytest tests/test_autoqueue_wiring.py tests/test_autoqueue_validate.py -q` exits 0
  </acceptance_criteria>
  <done>D-02 hard filter runs as an independent second gate after validation; validate_youtube_match unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-voice members' taste_episode memory → shared auto-queue prompt | per-member recalled facts are blended into ONE guild-shared recommendation prompt |
| Gemini-suggested tracks → YouTube validation → queue | untrusted {title, artist} suggestions before validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Information Disclosure | positive-taste blend in try_auto_queue | mitigate | Facts pass through `select_positive_taste_context` which returns an UNATTRIBUTED collective list; the prompt block is labelled "THE ROOM TENDS TO LIKE" (never "member X likes Y"). recall() itself is scoped `WHERE user_id = $1`. Per-member fan-out never re-associates a fact with a member in the emitted prompt (Pitfall 4) |
| T-14-01 | Tampering | `get_recently_skipped` call | mitigate | Uses the plan-14-01 bound-`$N` helper (no interpolation); `since` is a Python datetime bound as a param |
| T-14-03 | Spoofing (content) | recommended tracks | mitigate | Every suggestion still passes `validate_youtube_match` (unchanged) AND the new `is_recently_skipped_artist` gate before queueing — no unvalidated track reaches the queue |
| T-14-04 | Tampering | recommendation prompt text | accept | Song-metadata prompt-injection is an existing surface; recent songs already cleaned via `build_genius_search_query` upstream — unchanged |
| T-14-SC | Tampering | package installs | accept | Zero new package installs this phase |
</threat_model>

<verification>
- `python -m pytest tests/test_autoqueue_wiring.py tests/test_autoqueue_validate.py -q` green.
- `python -m pytest tests/ -q` remains green (no auto-queue/playback regression; byte-identical prompt when hints empty).
- Manual (deferred to parked live-Discord UAT): confirm a repeatedly-skipped artist stops reappearing in auto-queue rounds.
</verification>

<success_criteria>
- Auto-queue prompt carries a guild-scoped skip-avoidance block (D-01) and an unattributed room-taste block (D-03).
- A recently-skipped artist is rejected by an independent hard filter (D-02), validate_youtube_match unchanged.
- Byte-identical behavior when both hints are empty; scar #2 playback gate intact.
</success_criteria>

<output>
Create `.planning/phases/14-smarter-music-brain/14-03-SUMMARY.md` when done.
</output>
