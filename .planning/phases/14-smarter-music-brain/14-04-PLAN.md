---
phase: 14-smarter-music-brain
plan: 04
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - cogs/music.py
  - personality/responses.py
  - tests/test_discover.py
autonomous: true
requirements: [BRAIN-02]
must_haves:
  truths:
    - "/discover reads the invoker's guild-scoped top artist and surfaces SQL co-occurrence adjacency — the picks are 100% SQL-derived, never Gemini-invented (D-04)"
    - "Gemini output for /discover is commentary-only wrapping the SQL artist names, never the recommendation itself (D-04 firewall)"
    - "cold-start (no listening history / no adjacency) returns an in-character 'not enough listening yet' message, never an error (D-05)"
    - "/discover offers to queue an adjacent artist confirm-first (button/follow-up), never silently auto-queues (D-05)"
    - "the co-occurrence query is a guild-wide aggregate exposing no per-user rows (Criterion 4)"
  artifacts:
    - path: "cogs/music.py"
      provides: "/discover slash command + one-shot confirm-to-queue view"
      contains: "name=\"discover\""
    - path: "personality/responses.py"
      provides: "DISCOVER_NO_HISTORY in-character cold-start message list"
      contains: "DISCOVER_NO_HISTORY"
    - path: "tests/test_discover.py"
      provides: "source-assertion + cold-start / firewall regression"
      contains: "get_artist_cooccurrence"
  key_links:
    - from: "cogs/music.py::discover"
      to: "database.get_user_top_artist + database.get_artist_cooccurrence"
      via: "invoker-anchored top artist → guild-wide adjacency"
      pattern: "get_artist_cooccurrence"
    - from: "cogs/music.py::discover"
      to: "personality.prompts.build_discover_commentary_prompt"
      via: "Gemini voice-only wrapping of SQL picks"
      pattern: "build_discover_commentary_prompt"
---

<objective>
Add the `/discover` slash command (BRAIN-02) to `cogs/music.py` — invoker-anchored,
server-adjacency-grounded, 100% SQL-derived picks with Gemini supplying only Dex's voice:

- **D-04 anchor + adjacency:** read the invoker's guild-scoped top artist via
  `database.get_user_top_artist(guild_id, user_id, limit=1)` (Option B from plan 14-01), then compute
  `database.get_artist_cooccurrence(guild_id, anchor_artist, since=now-DISCOVER_COOCCURRENCE_WINDOW_DAYS,
  limit=DISCOVER_ADJACENT_COUNT)`. The adjacent artists ARE the recommendation (SQL-derived, zero
  hallucination). Gemini (`build_discover_commentary_prompt`) only wraps those exact names in voice.
- **D-05 cold-start + confirm-first:** empty anchor or empty adjacency → an in-character
  `pick_random(DISCOVER_NO_HISTORY)` message, never an error. On a hit, present the adjacent artist(s)
  with Dex commentary and OFFER to queue one via a one-shot confirm view — never silent auto-queue.

Purpose: a personal-feeling discovery surface grounded entirely in real server data.
Output: `/discover` command + confirm-to-queue view + cold-start response list + tests.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-smarter-music-brain/14-CONTEXT.md
@.planning/phases/14-smarter-music-brain/14-RESEARCH.md
@.planning/phases/14-smarter-music-brain/14-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (full Phase 14 symbol list — for the source-grounding review pass)

- `database.py`: `get_recently_skipped`, `get_user_top_artist`, `get_artist_cooccurrence`; `search_memories` `kind` param
- `services/memory.py`: `recall` `kind` param
- `logic/autoqueue.py`: `is_recently_skipped_artist`
- `logic/taste.py`: `select_positive_taste_context`
- `personality/prompts.py`: `build_recommendation_prompt` kwargs; `build_discover_commentary_prompt`; `build_jam_suggestion_prompt`
- `personality/responses.py`: `DISCOVER_NO_HISTORY`
- `cogs/music.py`: `/discover` command + confirm-to-queue View
- `cogs/library.py`: `/jam suggest` subcommand + propose-and-confirm View
- `config.py`: 6 Phase 14 knobs

**This plan (14-04) creates:** the `/discover` command (`discover` method), a one-shot confirm-to-queue `discord.ui.View`, and `personality/responses.py::DISCOVER_NO_HISTORY`. It consumes `get_user_top_artist`, `get_artist_cooccurrence`, `build_discover_commentary_prompt` from plans 14-01/14-02.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the /discover command core (anchor → adjacency → commentary → cold-start)</name>
  <files>cogs/music.py, personality/responses.py, tests/test_discover.py</files>
  <read_first>
    - cogs/music.py lines 484-530 (MusicCog class header, `youtube_service` property ~493, `get_queue` ~503, `_play_track` ~522) and one existing `@app_commands.command` (e.g. `history` ~1779 or `nowplaying` ~1666) for the decorator + defer + `_get_text_channel` conventions
    - cogs/music.py lines ~350-363 of cogs/ai.py's `try_auto_queue` Track-building block (the `Track(...)` field shape + `async_extract` + duration cap) — the confirm-to-queue action mirrors it in Task 2
    - database.py `get_user_top_artist` + `get_artist_cooccurrence` (created in 14-01)
    - personality/prompts.py `build_discover_commentary_prompt` (created in 14-02)
    - cogs/library.py lines 786-822 (`jam_add` guild-only + cold-state guard shape) and personality/responses.py (existing message-list + `pick_random` convention, e.g. NOTHING_PLAYING)
    - .planning/phases/14-smarter-music-brain/14-PATTERNS.md § "cogs/music.py — /discover command" (firewall + cold-start + confirm-to-queue notes)
  </read_first>
  <action>
    Add a `DISCOVER_NO_HISTORY` list of 3-5 in-character lowercase "not enough listening yet" lines to
    personality/responses.py (mirror the existing list-of-strings + module-level constant convention used
    by NOTHING_PLAYING / RATE_LIMIT_MESSAGES). Import it into cogs/music.py alongside the existing
    responses imports.

    Add a `@app_commands.command(name="discover", description=...)` method `discover` to MusicCog. Flow:
    (1) guild-only guard (ephemeral "this only works in a server." like jam_add); (2) `await
    interaction.response.defer()`; (3) `anchor_rows = await get_user_top_artist(self.bot.pool,
    guild_id=str(guild.id), user_id=str(interaction.user.id), limit=1)`; if empty →
    `pick_random(DISCOVER_NO_HISTORY)` follow-up and return (D-05 cold-start, never error);
    (4) `since = datetime.now(timezone.utc) - timedelta(days=config.DISCOVER_COOCCURRENCE_WINDOW_DAYS)`;
    `adjacent_rows = await get_artist_cooccurrence(self.bot.pool, guild_id=str(guild.id),
    anchor_artist=anchor_rows[0]["artist"], since=since, limit=config.DISCOVER_ADJACENT_COUNT)`; if empty
    → `pick_random(DISCOVER_NO_HISTORY)` and return; (5) build `adjacent_artists =
    [r["artist"] for r in adjacent_rows]` — THESE are the picks (SQL-derived); (6) call
    `self.gemini.chat(build_discover_commentary_prompt(anchor_rows[0]["artist"], adjacent_artists), [],
    priority=2)` for VOICE ONLY — use the reply as plain text (NO parse_suggestions/JSON); if Gemini
    returns nothing, fall back to a plain non-Gemini sentence naming the anchor + adjacents so the command
    still works (firewall: the picks never depend on Gemini). Wrap the whole body in the same
    try/except-degrade shape MusicCog commands already use; on error, an in-character failure message, never
    a raw traceback to the user.

    Create tests/test_discover.py: source-assertion class (no live Discord) using `inspect.getsource` on
    the `discover` method asserting it calls `get_user_top_artist(`, `get_artist_cooccurrence(`,
    `build_discover_commentary_prompt(`, `pick_random(DISCOVER_NO_HISTORY)`, and that it does NOT call
    `parse_suggestions` (firewall: Gemini output is not parsed into picks). Assert `DISCOVER_NO_HISTORY`
    exists in personality.responses and is a non-empty list of str.
  </action>
  <verify>
    <automated>python -m pytest tests/test_discover.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cogs/music.py` contains an `@app_commands.command(name="discover"` method
    - `discover` source contains `get_user_top_artist(`, `get_artist_cooccurrence(`, `build_discover_commentary_prompt(`, and `pick_random(DISCOVER_NO_HISTORY)`; and does NOT contain `parse_suggestions` (D-04 firewall — picks are SQL-only)
    - `personality/responses.py` defines `DISCOVER_NO_HISTORY` as a non-empty `list[str]`
    - Two distinct empty guards exist (empty anchor rows, empty adjacency rows), each returning the cold-start message rather than raising (D-05)
    - `pytest tests/test_discover.py -q` exits 0
  </acceptance_criteria>
  <done>/discover surfaces SQL-derived adjacency with Gemini voice-only; cold-start returns in-character message; firewall enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Add the confirm-to-queue view for /discover (D-05)</name>
  <files>cogs/music.py, tests/test_discover.py</files>
  <read_first>
    - cogs/music.py (the `_play_track` + `get_queue` + `youtube_service`/`async_search`/`async_extract` flow; find how NowPlayingView is defined for the button/view idiom, but note this view is ONE-SHOT: `discord.ui.View(timeout=...)`, NOT `timeout=None`/setup_hook-registered)
    - cogs/ai.py lines 315-363 (`async_search` → `validate`/extract → duration cap → `Track(...)` → `queue.add(track)` — mirror the field shape for the queue action)
    - .planning/phases/14-smarter-music-brain/14-PATTERNS.md § "cogs/library.py — Propose-and-confirm (D-07)" (the one-shot confirm-view guidance; same pattern applies here)
  </read_first>
  <action>
    Add a one-shot `discord.ui.View` subclass (e.g. `DiscoverQueueView`) with a finite `timeout` (NOT
    `timeout=None`; NOT registered in setup_hook — it only needs to survive one interaction round-trip) and
    a single "queue it" button (plus optional dismiss). Attach it to the `/discover` follow-up when
    adjacency exists, seeded with the top adjacent artist name. On button press: `await
    interaction.response.defer()`, resolve the artist to a track via `self.bot.youtube_service.async_search(
    <artist>, count=1)` then `async_extract`, apply the `MAX_SONG_DURATION_SECONDS` duration cap
    (mirror try_auto_queue), build a `Track(...)` (requested_by = the pressing user), `queue.add(track)`,
    and start playback only if `should_start_playback`-equivalent guard says audio isn't already flowing
    (reuse MusicCog's existing playback-start path — do NOT introduce a `not queue.is_playing` gate,
    scar #2). Disable the button after use / on timeout. It must NEVER auto-queue without the button press
    (D-05 confirm-first).
    Extend tests/test_discover.py with source assertions that the view class exists, uses a finite
    `timeout=` (not `timeout=None`), and its callback calls `async_search`/`async_extract` + `queue.add(`
    and honors the duration cap.
  </action>
  <verify>
    <automated>python -m pytest tests/test_discover.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cogs/music.py` defines a one-shot confirm view class with a finite `timeout=` value (source does NOT set `timeout=None` for this view) and it is NOT registered via setup_hook
    - The button callback source contains `async_search`, `async_extract`, `queue.add(`, and a `MAX_SONG_DURATION_SECONDS` cap check
    - `/discover` attaches the view only when adjacency is non-empty (no view on the cold-start path)
    - Source contains no `not queue.is_playing` gate (scar #2)
    - `pytest tests/test_discover.py -q` exits 0
  </acceptance_criteria>
  <done>Confirm-first queueing via a one-shot view; duration-capped; no silent auto-queue; scar #2 respected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord invoker → get_user_top_artist / get_artist_cooccurrence SQL | user_id / guild_id / anchor artist cross into SQL |
| guild-wide co-occurrence rows → invoker's /discover output | aggregate adjacency (spanning many users' plays) surfaced to one invoker |
| Gemini commentary reply → user-facing message | model text wrapping fixed SQL picks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Information Disclosure | `/discover` co-occurrence adjacency | mitigate | `get_artist_cooccurrence` is a guild-WIDE aggregate whose result exposes only artist names + counts, never a `user_id` — one user's individual rows are never attributable in another user's output (Criterion 4). Anchor is the invoker's OWN guild-scoped data (`get_user_top_artist` WHERE user_id=$2). HIGH severity → covered by plan-14-01 live-DB regression |
| T-14-01 | Tampering | discovery SQL calls | mitigate | Both helpers bind `$N` params (plan 14-01); `anchor_artist`/`since` bound, never interpolated |
| T-14-03 | Spoofing (content) | Gemini commentary | mitigate | Picks are 100% SQL-derived; Gemini output is used as plain commentary text ONLY and never parsed into a recommendation — a hallucinated artist in commentary cannot become a queued track (`/discover` queues only the SQL-named adjacent artist via the confirm button) |
| T-14-04 | Tampering | commentary prompt | accept | Anchor/adjacent artist names (user-influenced metadata) into the prompt — existing surface class, unchanged |
| T-14-SC | Tampering | package installs | accept | Zero new package installs this phase |
</threat_model>

<verification>
- `python -m pytest tests/test_discover.py -q` green.
- `python -m pytest tests/ -q` remains green.
- Manual (deferred to parked live-Discord UAT): `/discover` on a seeded guild surfaces a real adjacent artist and queues it on confirm; cold-start guild shows the in-character message.
</verification>

<success_criteria>
- `/discover` picks are SQL-derived adjacency (BRAIN-02), Gemini supplies voice only (D-04 firewall).
- Cold-start returns an in-character message, never an error (D-05).
- Queueing is confirm-first via a one-shot view (D-05); co-occurrence stays a guild-wide aggregate (Criterion 4).
</success_criteria>

<output>
Create `.planning/phases/14-smarter-music-brain/14-04-SUMMARY.md` when done.
</output>
