---
phase: 14-smarter-music-brain
plan: 05
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - cogs/library.py
  - tests/test_jam_suggest.py
autonomous: true
requirements: [BRAIN-03]
must_haves:
  truths:
    - "/jam suggest <name> seeds Gemini with the named jam's existing tracks and requests additions (D-06)"
    - "every suggested addition is validated against real YouTube search via logic/autoqueue.py::validate_youtube_match before it is ever offered (D-06 hard requirement, BRAIN-03)"
    - "/jam suggest is propose-and-confirm — validated candidates are shown and the user confirms before anything is written to the shared jam snapshot (D-07)"
    - "if no suggestion survives validation, Dex says so in character and the jam snapshot is left untouched (D-07)"
    - "a non-existent jam name returns an in-character message, never a crash (D-06 seeds from EXISTING tracks)"
  artifacts:
    - path: "cogs/library.py"
      provides: "/jam suggest subcommand + one-shot propose-and-confirm view"
      contains: "name=\"suggest\""
    - path: "tests/test_jam_suggest.py"
      provides: "source-assertion + none-survive / snapshot-untouched regression"
      contains: "validate_youtube_match"
  key_links:
    - from: "cogs/library.py::jam_suggest"
      to: "logic.autoqueue.validate_youtube_match"
      via: "per-candidate hallucination gate before offer (reused verbatim)"
      pattern: "validate_youtube_match"
    - from: "cogs/library.py::jam_suggest"
      to: "database.save_jam"
      via: "append validated tracks to snapshot ONLY on confirm"
      pattern: "save_jam"
---

<objective>
Add the `/jam suggest <name>` subcommand (BRAIN-03) as a sibling of the existing
save/add/load/list/delete in `cogs/library.py` — generative jam assist that never commits garbage:

- **D-06 seed + validate:** load the named jam's EXISTING tracks via `database.get_jam` and seed
  `personality.prompts.build_jam_suggestion_prompt(existing_tracks, count=JAM_SUGGEST_CANDIDATE_COUNT)`;
  Gemini returns `{title, artist}` suggestions parsed by the reused `parse_suggestions`; EVERY suggestion
  is validated against real `youtube_service.async_search` results via `validate_youtube_match` (reused
  verbatim — NOT reimplemented, NOT difflib) before it is ever offered.
- **D-07 propose-and-confirm:** show the validated candidate additions in a one-shot confirm view; on
  confirm, APPEND them to the jam snapshot via `database.save_jam` (with an option to also queue them now);
  never silently mutate the shared server artifact. If none survive validation, an in-character
  "nothing landed" message and the snapshot is untouched.

Purpose: Dex reads the room's mixtape and offers genuinely on-vibe, validation-gated additions.
Output: `/jam suggest` subcommand + propose-and-confirm view + tests.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-smarter-music-brain/14-CONTEXT.md
@.planning/phases/14-smarter-music-brain/14-RESEARCH.md
@.planning/phases/14-smarter-music-brain/14-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (full Phase 14 symbol list — for the source-grounding review pass)

- `database.py`: `get_recently_skipped`, `get_user_top_artist`, `get_artist_cooccurrence`; `search_memories` `kind` param
- `services/memory.py`: `recall` `kind` param
- `logic/autoqueue.py`: `is_recently_skipped_artist`
- `logic/taste.py`: `select_positive_taste_context`
- `personality/prompts.py`: `build_recommendation_prompt` kwargs; `build_discover_commentary_prompt`; `build_jam_suggestion_prompt`
- `personality/responses.py`: `DISCOVER_NO_HISTORY`
- `cogs/music.py`: `/discover` command + confirm-to-queue View
- `cogs/library.py`: `/jam suggest` subcommand + propose-and-confirm View
- `config.py`: 6 Phase 14 knobs

**This plan (14-05) creates:** the `/jam suggest` subcommand (`jam_suggest` method) and a one-shot propose-and-confirm `discord.ui.View`. It consumes `build_jam_suggestion_prompt` (14-02), reuses `validate_youtube_match` (existing), `parse_suggestions` (existing, imported from cogs.ai), and `get_jam`/`save_jam` (existing).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the /jam suggest subcommand core (seed → generate → validate)</name>
  <files>cogs/library.py, tests/test_jam_suggest.py</files>
  <read_first>
    - cogs/library.py lines 692-855 (the `jam` app_commands.Group + `jam_save`/`jam_add` guard sequence, ephemeral responses, `get_jam`/`save_jam`/`count_jams` usage, `music_cog = self.bot.get_cog("MusicCog")` + `get_queue` access)
    - cogs/library.py lines 1-45 (imports — needs to add: `build_jam_suggestion_prompt` from personality.prompts, `validate_youtube_match` from logic.autoqueue, `parse_suggestions` from cogs.ai; confirm `get_jam`/`save_jam` already imported)
    - cogs/ai.py lines 43-83 (`parse_suggestions`) and lines 311-341 (the per-suggestion `async_search` → `validate_youtube_match` loop shape to mirror EXACTLY)
    - personality/prompts.py `build_jam_suggestion_prompt` (created in 14-02); personality/responses.py `pick_random` + NOTHING_PLAYING convention for the in-character branches
    - .planning/phases/14-smarter-music-brain/14-PATTERNS.md § "cogs/library.py — /jam suggest subcommand" (guard sequence, existing-jam load `existing is not None`, validation loop reuse)
  </read_first>
  <action>
    Import `build_jam_suggestion_prompt` (personality.prompts), `validate_youtube_match`
    (logic.autoqueue), and `parse_suggestions` (`from cogs.ai import parse_suggestions` — one-way import,
    cogs.ai does not import cogs.library so there is no cycle; if the executor finds this awkward, the
    RESEARCH fallback is to move parse_suggestions to a shared util, but the direct import is preferred to
    keep file ownership clean).

    Add a `@jam.command(name="suggest", description=...)` method `jam_suggest(self, interaction, name)`.
    Guard sequence copied verbatim from `jam_add` (guild-only → strip/empty name → over-long name →
    `music_cog` presence). Then `await interaction.response.defer(ephemeral=True)`.
    Load the EXISTING jam: `existing = await get_jam(self.bot.pool, guild_id=str(guild.id), name=name)`.
    Unlike jam_add's create-on-miss, if `existing is None` respond with an in-character "no jam by that
    name to riff on" message and return (D-06 seeds from existing tracks). Build
    `build_jam_suggestion_prompt(existing, count=config.JAM_SUGGEST_CANDIDATE_COUNT)`, call
    `self.bot.<gemini>.chat(prompt, [], priority=2)` (use the same GeminiService access the cog already
    has; if the cog lacks a gemini handle, reach it via the AICog like jam accesses MusicCog), then
    `parse_suggestions(response)`. For each parsed suggestion, `results = await
    self.bot.youtube_service.async_search(f"{s['title']} {s['artist']}",
    count=config.AUTO_QUEUE_SEARCH_CANDIDATES)`; take the first result where
    `validate_youtube_match(result.get("title",""), s["title"], s["artist"])` is True; collect it as a
    validated candidate (title/artist/url). Suggestions with no passing candidate are DROPPED (continue).
    If NO candidate survives, send an in-character "nothing landed" message and return WITHOUT calling
    save_jam (D-07 — snapshot untouched). Otherwise hand the validated candidates to the Task 2 confirm
    view.

    Create tests/test_jam_suggest.py (mirror test_jam_load.py / test_database_phase13.py static-assertion
    convention): source-assertion class asserting the `jam_suggest` method exists on the jam group, its
    source calls `build_jam_suggestion_prompt(`, `parse_suggestions(`, `validate_youtube_match(`, and
    `get_jam(`; asserts the non-existent-jam branch returns before any `save_jam`; asserts the none-survive
    branch returns before `save_jam` (snapshot untouched, D-07). Keep these poolless/source-level so they
    run without live Discord/Gemini/DB.
  </action>
  <verify>
    <automated>python -m pytest tests/test_jam_suggest.py tests/test_autoqueue_validate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cogs/library.py` contains a `@jam.command(name="suggest"` method `jam_suggest`
    - `jam_suggest` source contains `get_jam(`, `build_jam_suggestion_prompt(`, `parse_suggestions(`, and `validate_youtube_match(`
    - `cogs/library.py` imports `validate_youtube_match` from `logic.autoqueue` and `parse_suggestions` from `cogs.ai`; file contains NO `import difflib`
    - The non-existent-jam branch and the none-survive branch both `return` before any `save_jam(` call (source-order assertion)
    - `pytest tests/test_jam_suggest.py tests/test_autoqueue_validate.py -q` exits 0
  </acceptance_criteria>
  <done>/jam suggest seeds from existing tracks, generates, and validation-gates every candidate; garbage never reaches an offer.</done>
</task>

<task type="auto">
  <name>Task 2: Add the propose-and-confirm view + snapshot append (D-07)</name>
  <files>cogs/library.py, tests/test_jam_suggest.py</files>
  <read_first>
    - cogs/library.py lines 762-772 + 840-852 (`snapshot = [t.to_dict() for t in queue.tracks]` / `snapshot.append(track.to_dict())` + `save_jam(...)` append semantics to mirror)
    - .planning/phases/14-smarter-music-brain/14-PATTERNS.md § "Propose-and-confirm (D-07)" (one-shot `discord.ui.View(timeout=...)` with Confirm/Cancel, NOT timeout=None/setup_hook) and § "On confirm — mutation call"
    - cogs/library.py `jam_load` (for how a jam snapshot is turned into queued tracks, if wiring the optional "queue them now")
  </read_first>
  <action>
    Add a one-shot `discord.ui.View` subclass (e.g. `JamSuggestConfirmView`) with a finite `timeout`
    (NOT `timeout=None`, NOT setup_hook-registered — one interaction round-trip) carrying the validated
    candidate list and the jam name. Buttons: Confirm and Cancel. On Confirm: reload the current snapshot
    via `get_jam` (defensive against a concurrent edit), append the validated candidates as dicts in the
    same shape jam entries already use (title/artist/url/… — match `to_dict()`/existing jam-entry keys),
    and `await save_jam(self.bot.pool, guild_id=..., name=..., snapshot=<existing + additions>)` (append
    semantics identical to `/jam add`). Optionally also queue them now via the MusicCog queue (mirror
    jam_load) — gate this behind a second button or a note; queueing is optional per D-07. On Cancel or
    timeout: disable buttons, confirm nothing was written. The snapshot is mutated ONLY inside the Confirm
    callback (never before user confirmation, D-07).
    Extend tests/test_jam_suggest.py: assert the view class exists with a finite `timeout=` (not
    `timeout=None`), its Confirm callback source calls `save_jam(` and its Cancel path does not, and that
    `jam_suggest` attaches the view only when ≥1 validated candidate exists.
  </action>
  <verify>
    <automated>python -m pytest tests/test_jam_suggest.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cogs/library.py` defines a one-shot confirm view with a finite `timeout=` value (source does NOT set `timeout=None` for this view) and it is NOT registered via setup_hook
    - The Confirm callback source calls `save_jam(` and appends to the existing snapshot (append semantics); the Cancel callback does NOT call `save_jam(`
    - `jam_suggest` attaches the view only on the survivors path (not on the none-survive / no-jam paths)
    - `pytest tests/test_jam_suggest.py -q` exits 0
  </acceptance_criteria>
  <done>Propose-and-confirm view appends validated tracks to the jam snapshot only on explicit confirm; nothing written otherwise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord invoker → jam `name` param → get_jam/save_jam SQL | user-supplied name crosses into DB helpers |
| Gemini-suggested {title, artist} → YouTube validation → shared jam snapshot | untrusted model output that could persist into a shared server artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Spoofing (content) | `/jam suggest` candidate additions | mitigate | EVERY suggestion is re-validated against real `youtube_service.async_search` results via `validate_youtube_match` (token-set containment, reused verbatim) before it is offered; only validated candidates can reach the confirm view; the snapshot is mutated only on explicit user confirm — a hallucinated track can never be written to the shared jam (BRAIN-03 hard requirement, HIGH severity for shared-artifact integrity) |
| T-14-01 | Tampering | `get_jam` / `save_jam` calls | mitigate | Existing bound-`$N` helpers keyed on `guild_id`/`name` (T-12-01-01); name is a bound param, never interpolated |
| T-14-05 | Tampering (input validation) | jam `name` param | mitigate | Reuse `jam_add`'s empty-check + `PLAYLIST_NAME_MAX_LENGTH` guard before any DB call (V5) |
| T-14-04 | Tampering | jam-suggestion prompt | accept | Existing track titles/artists (user-influenced) seed the prompt — existing surface class, unchanged |
| T-14-SC | Tampering | package installs | accept | Zero new package installs this phase |
</threat_model>

<verification>
- `python -m pytest tests/test_jam_suggest.py tests/test_autoqueue_validate.py -q` green.
- `python -m pytest tests/ -q` remains green.
- Manual (deferred to parked live-Discord UAT): `/jam suggest <existing jam>` offers validation-passing additions, appends only on confirm; an all-hallucinated run shows "nothing landed" and leaves the jam unchanged.
</verification>

<success_criteria>
- `/jam suggest` seeds from the named jam's existing tracks and every candidate passes `validate_youtube_match` before being offered (BRAIN-03).
- Propose-and-confirm: the shared snapshot is written only on explicit confirm; none-survive leaves it untouched (D-07).
- Non-existent jam returns an in-character message, never a crash (D-06).
</success_criteria>

<output>
Create `.planning/phases/14-smarter-music-brain/14-05-SUMMARY.md` when done.
</output>
