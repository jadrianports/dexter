---
phase: 22-invite-plumbing
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - tests/test_invite_drift_guard.py
autonomous: true
requirements: [INVITE-02]
user_setup: []

must_haves:
  truths:
    - "Every invite URL in a git-tracked public doc literally equals build_invite_url()'s output — drift is structurally impossible, not merely discouraged (D-03 / SC-3)"
    - "The guard's currently-vacuous pass is provably NOT a false green — a positive-control test shows the scanner finds a URL when one exists"
    - "The .planning/ tree is excluded, so its legitimate <APP_ID> placeholder URLs never cause a permanent false-positive failure (D-10)"
    - "logic/invite.py is the ONLY module in the codebase that constructs an invite URL — a second constructor fails CI (D-03 / D-07)"
    - "The guard runs green in the zero-secret CI environment (it resolves the client ID from config.py, never from env)"
  artifacts:
    - path: "tests/test_invite_drift_guard.py"
      provides: "git-tracked-doc drift guard, positive control, .planning/ exclusion lock, single-constructor source guard"
      min_lines: 90
  key_links:
    - from: "tests/test_invite_drift_guard.py"
      to: "logic.invite.build_invite_url"
      via: "the canonical URL every scanned doc is compared against"
      pattern: "build_invite_url"
    - from: "tests/test_invite_drift_guard.py"
      to: "git ls-files"
      via: "subprocess enumeration of tracked docs (auto-covers whatever docs Phase 23 creates)"
      pattern: "ls-files"
---

<objective>
Ship INVITE-02's structural half: the CI drift-guard that makes a stale or over-privileged invite
link in a public doc impossible to merge.

Purpose: SC-3 — "The in-bot `/invite` link and the publicly-promoted link are the same URL — a single
source of truth, not two hand-maintained copies that can drift." This guard passes vacuously **today**
(verified: `git grep oauth2/authorize` over tracked non-`.planning/` files currently returns nothing)
and **starts enforcing automatically the moment Phase 23 pastes a link into the README or `/site`** —
Phase 23 needs to do nothing to opt in. This is the T-22-02 mitigation.

Output: `tests/test_invite_drift_guard.py` — a repo-introspection test file (new infrastructure for
this codebase; no existing test shells out to `git`).
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-invite-plumbing/22-CONTEXT.md
@.planning/phases/22-invite-plumbing/22-RESEARCH.md
@.planning/phases/22-invite-plumbing/22-PATTERNS.md
@.planning/phases/22-invite-plumbing/22-VALIDATION.md
@.planning/phases/22-invite-plumbing/22-01-SUMMARY.md
@.github/workflows/ci.yml
</context>

<interface_contract>
Provided by plan 22-01 (already on disk when this plan runs):

```
config.DISCORD_CLIENT_ID        -> int
config.INVITE_PERMISSIONS_VALUE -> int   (== 309240908864)
config.INVITE_SCOPES            -> tuple[str, ...]

logic.invite.build_invite_url(*, client_id: int, permissions_value: int,
                              scopes: tuple[str, ...] = ("bot", "applications.commands")) -> str
```

Canonical output (VERIFIED by executing against the installed discord.py 2.7.1):

    https://discord.com/oauth2/authorize?client_id=1492588698364018898&scope=bot+applications.commands&permissions=309240908864
</interface_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the git-tracked-doc drift guard + its positive control (D-03/D-10)</name>

  <files>tests/test_invite_drift_guard.py</files>

  <read_first>
    - `.planning/phases/22-invite-plumbing/22-RESEARCH.md` § "Drift-guard test skeleton" — a HIGH-confidence, already-worked-out code skeleton (`_repo_root()` via `git rev-parse --show-toplevel`, `_tracked_doc_files()` via `git ls-files`, the `URL_PATTERN` regex, the `tmp_path` positive control). Follow it; it was written against the real repo contents.
    - `.planning/phases/22-invite-plumbing/22-RESEARCH.md` § Pitfall 2 — WHY `.planning/` must be excluded: `.planning/research/STACK.md` (and its milestone-archive copies) contain a `<APP_ID>` **placeholder** URL that can never equal `build_invite_url()`'s output. Scanning it would be a permanent, unfixable red.
    - `.planning/phases/22-invite-plumbing/22-CONTEXT.md` — D-03 (one builder + a CI drift-guard), D-07 (the promoted link must be the literal OAuth2 URL — no shorteners, no vanity redirects; that is precisely what a literal-match test encodes), D-10 (the `.planning/` directory-prefix exclusion + the `.md`/`.html`/`.txt` extension allowlist, and the MANDATORY positive control).
    - `.planning/phases/22-invite-plumbing/22-PATTERNS.md` § `tests/test_invite_drift_guard.py` — **no close analog exists**; this is genuinely new test infrastructure. The nearest cousin in spirit is `tests/test_autoqueue_wiring.py` (a private `_helper()` produces the thing to inspect; multiple tests assert against it).
    - `.github/workflows/ci.yml` — confirms `actions/checkout@v4` (so `.git` IS present and `git ls-files` / `git rev-parse` work in CI) and that the job sets ONLY `TEST_DATABASE_URL` — **zero Discord secrets**. This is exactly why the canonical URL must resolve from `config.DISCORD_CLIENT_ID`'s committed default and never from an env var.
    - `.planning/phases/22-invite-plumbing/22-RESEARCH.md` § Assumptions Log — note that CONTEXT.md's original "passes vacuously" premise was once false. It is TRUE again now (the offending file was untracked + gitignored, and the leaked token rotated, before planning). Do **not** create, modify, or reference `dexter-architecture.md` — it is gone from the repo.
  </read_first>

  <behavior>
    Tests to write in `tests/test_invite_drift_guard.py`:
    - `test_no_doc_contains_a_drifted_invite_url` — THE guard. For every git-tracked, non-`.planning/`,
      `.md`/`.html`/`.txt` file, every OAuth2 URL found must literally equal
      `build_invite_url(client_id=config.DISCORD_CLIENT_ID, permissions_value=config.INVITE_PERMISSIONS_VALUE, scopes=config.INVITE_SCOPES)`.
      Passes vacuously today (zero URLs found); starts enforcing when Phase 23 adds one.
      The failure message must name the offending file AND the offending URL.
    - `test_drift_guard_actually_detects_a_mismatch` — **the mandatory positive control.** Write a
      `tmp_path` file containing a deliberately-wrong URL
      (`https://discord.com/oauth2/authorize?client_id=999&permissions=8&scope=bot` — note
      `permissions=8` is literally Administrator, the exact thing this guard exists to catch), run it
      through the SAME offender-collection function the real test uses, and assert the offender list is
      NON-empty. This proves today's vacuous pass is not a false green.
    - `test_drift_guard_accepts_the_canonical_url` — the negative control for the positive control: a
      `tmp_path` file containing exactly `build_invite_url()`'s output produces ZERO offenders. Proves
      the guard is a *literal-match* test, not a blanket "any invite URL is an error" test — Phase 23
      must be able to paste the real link and stay green.
    - `test_scanner_matches_urls_in_markdown_html_and_bare_forms` — the same canonical URL embedded as
      a Markdown link `[Add to Discord](URL)`, an HTML `href="URL"`, and bare-on-its-own-line is found
      in all three forms (Phase 23 will use all three). Assert the extracted match has no trailing
      `)` or `"` contamination.
    - `test_planning_tree_is_excluded_from_the_scan` — `_tracked_doc_files()` returns no path under
      `.planning/` (D-10). Assert positively that `.planning/` files exist and are tracked but are
      absent from the returned list — otherwise this test could pass for the wrong reason.
    - `test_only_text_extensions_are_scanned` — every path returned by `_tracked_doc_files()` has a
      suffix in `{".md", ".html", ".txt"}`.
    - `test_canonical_url_resolves_without_env_secrets` — `build_invite_url(...)` from the `config`
      constants yields a URL containing `permissions=309240908864` even with `DISCORD_CLIENT_ID`
      absent from the environment (monkeypatch `delenv` and re-derive from `config`). This is the
      CI-parity guarantee D-04 exists to provide.
  </behavior>

  <action>
Create `tests/test_invite_drift_guard.py`. New test infrastructure — no existing test in this repo
shells out to `git`, so build the helpers explicitly rather than importing from anywhere:

  - Module docstring: name the phase/requirement (Phase 22 / INVITE-02 / SC-3), the decisions (D-03,
    D-07, D-10), and the threat (T-22-02: a public doc promoting a stale or over-privileged link).
    State plainly that the main guard passes vacuously today and that
    `test_drift_guard_actually_detects_a_mismatch` is what proves that pass is real. List each test
    with a one-line purpose (the `tests/test_memory_command.py` docstring convention).

  - Module constants:
      `URL_PATTERN = re.compile(r"https://discord\.com/(?:api/)?oauth2/authorize\?[^\s)\"'<>]+")`
        — the `(?:api/)?` alternative catches the legacy `discord.com/api/oauth2/authorize` form; the
        negated character class terminates the match at a Markdown `)`, an HTML `"`/`'`, an angle
        bracket, or whitespace, so all three embedding forms extract cleanly.
      `TEXT_EXTENSIONS = frozenset({".md", ".html", ".txt"})`
      `PLANNING_PREFIX = ".planning/"`  — a directory-prefix denylist (D-10), NOT a per-file allowlist.

  - Helpers (private, underscore-prefixed):
      `_repo_root() -> Path` — `subprocess.run(["git", "rev-parse", "--show-toplevel"], capture_output=True, text=True, check=True)`.
      `_tracked_doc_files(root) -> list[Path]` — `git ls-files` from `root`; skip any path starting
        with `.planning/`; keep only paths whose suffix is in `TEXT_EXTENSIONS`. Return absolute paths.
      `_canonical_url() -> str` — `build_invite_url(client_id=config.DISCORD_CLIENT_ID, permissions_value=config.INVITE_PERMISSIONS_VALUE, scopes=config.INVITE_SCOPES)`.
      `_collect_offenders(paths, canonical) -> list[tuple[str, str]]` — **this is the seam the positive
        control depends on**: it takes an explicit list of paths so the `tmp_path` tests can feed it a
        fake file and exercise the exact same comparison logic the real guard uses. For each path, read
        with `encoding="utf-8", errors="ignore"`, find all `URL_PATTERN` matches, `rstrip(").,")` each
        match, and record `(path, match)` for any that != `canonical`.

  - Then the seven tests from `<behavior>`. The real guard is
    `_collect_offenders(_tracked_doc_files(_repo_root()), _canonical_url()) == []` with an assertion
    message that prints the offenders.

Do NOT hardcode the canonical URL string anywhere in this file — always derive it from
`build_invite_url()` + the `config` constants, or the `DISCORD_CLIENT_ID` env override would make the
guard lie. Do NOT add a permanently-committed fixture file containing a deliberately-wrong URL — that
would itself be a tracked doc needing yet another exclusion rule; use `tmp_path` (this is why
RESEARCH.md's skeleton uses a fixture, not a committed file).

Do NOT touch or reference `dexter-architecture.md`. Do NOT add any exclusion beyond `.planning/` +
the extension allowlist — every additional exclusion is a hole in the guarantee.
  </action>

  <verify>
    <automated>pytest tests/test_invite_drift_guard.py -q</automated>
    <automated>python -c "import subprocess; out = subprocess.run(['git','ls-files'], capture_output=True, text=True, check=True).stdout.splitlines(); docs = [f for f in out if not f.startswith('.planning/') and f.rsplit('.',1)[-1] in ('md','html','txt')]; print(f'{len(docs)} tracked docs in scan scope:', docs[:10])"</automated>
    <automated>ruff check tests/test_invite_drift_guard.py && ruff format --check tests/test_invite_drift_guard.py</automated>
  </verify>

  <acceptance_criteria>
    - `tests/test_invite_drift_guard.py` exists and defines `_repo_root`, `_tracked_doc_files`, `_canonical_url`, and `_collect_offenders`.
    - `_collect_offenders` accepts an explicit list of paths (so `tmp_path` tests exercise the real comparison logic, not a reimplementation of it).
    - `test_drift_guard_actually_detects_a_mismatch` feeds a `tmp_path` file containing `https://discord.com/oauth2/authorize?client_id=999&permissions=8&scope=bot` through `_collect_offenders` and asserts the result is NON-empty.
    - `test_drift_guard_accepts_the_canonical_url` feeds `_canonical_url()`'s exact output through `_collect_offenders` and asserts the result is EMPTY.
    - `_tracked_doc_files()` returns zero paths under `.planning/`, and `test_planning_tree_is_excluded_from_the_scan` proves `.planning/` docs are tracked-but-skipped (not merely absent).
    - `grep -c "dexter-architecture" tests/test_invite_drift_guard.py` returns 0.
    - The canonical URL is never hardcoded as a string literal in the test file (it is always derived from `build_invite_url()` + `config`).
    - `pytest tests/test_invite_drift_guard.py -q` exits 0 with 7 tests passing.
    - `ruff check tests/test_invite_drift_guard.py` and `ruff format --check tests/test_invite_drift_guard.py` both exit 0.
  </acceptance_criteria>

  <done>
Drift between the in-bot link and any publicly-promoted link is structurally impossible. Phase 23 can
paste `build_invite_url()`'s output into the README and `/site` and the guard picks it up with no
workflow change and no opt-in.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the single-constructor invariant — no second URL builder can be added (D-03/D-07)</name>

  <files>tests/test_invite_drift_guard.py</files>

  <read_first>
    - `tests/test_invite_drift_guard.py` — the helpers written in Task 1 (`_repo_root`, `_tracked_doc_files`); this task adds a `.py`-scanning sibling.
    - `tests/test_autoqueue_wiring.py` — the project's source-inspection test convention (assert wiring/structure from source text without a live Discord/DB path).
    - `.planning/phases/22-invite-plumbing/22-CONTEXT.md` § D-03 — "A single pure builder … is the **only** place an invite URL is ever constructed." The doc drift-guard (Task 1) only scans `.md`/`.html`/`.txt`; nothing yet stops a future phase from adding a second constructor *in Python*, which is the more likely regression.
    - `.planning/phases/22-invite-plumbing/22-RESEARCH.md` § Don't Hand-Roll — "The risk isn't reinventing a wheel — it's a second, informally-drifted copy of the same wheel."
    - `CLAUDE.md` § "Implementation Gotchas" (Phases 18–21) — the Phase 21 T-21-03 precedent: a hardcoded literal list IS the control; a clever dynamic form would eventually and silently sweep up the wrong thing.
  </read_first>

  <behavior>
    Tests to APPEND to `tests/test_invite_drift_guard.py`:
    - `test_logic_invite_is_the_only_url_constructor` — scan every git-tracked `.py` file EXCLUDING
      the `tests/` directory (test files legitimately contain fake/expected URLs and the regex source
      itself). Assert that the only file containing either `oauth_url(` or the literal
      `discord.com/oauth2/authorize` is `logic/invite.py`. The failure message must name the offending
      file. Docstring must explain: this is the D-03/D-07 single-source-of-truth invariant — if a
      future phase (e.g. Phase 23's site generator) needs a URL, it MUST import `build_invite_url`,
      not build its own.
    - `test_config_holds_no_url_literal` — `config.py` contains no `discord.com/oauth2` literal
      (the bitfield int and the client id live there; the URL does not).
  </behavior>

  <action>
Append to `tests/test_invite_drift_guard.py` a second scanning helper and the two tests above.

  - `_tracked_python_files(root) -> list[Path]` — `git ls-files`, keep only `.py` suffixes, EXCLUDE
    any path starting with `tests/` and any path starting with `.planning/`. (Test files legitimately
    contain fake URLs — the positive control from Task 1 is one — and this very file contains the
    `URL_PATTERN` regex source.)
  - `_CONSTRUCTOR_MARKERS = ("oauth_url(", "discord.com/oauth2/authorize")` — a small, hardcoded,
    reviewable literal tuple. Do NOT make this a clever dynamic heuristic; per the Phase 21 T-21-03
    precedent, the reviewability of the literal list IS the control.
  - `_ALLOWED_CONSTRUCTOR = "logic/invite.py"` — the single sanctioned module.

Then the two tests. The single-constructor test reads each file's text and records any path (relative
to the repo root, POSIX-normalized so it compares correctly on Windows) that contains any marker and is
not `logic/invite.py`; assert that list is empty.

Note for the executor: `logic/invite.py` legitimately contains `discord.utils.oauth_url(` — that is the
one allowed hit. `cogs/invite.py` must contain neither marker (22-02's own acceptance criteria already
assert this; this test makes it a repo-wide invariant rather than a per-file check).

Keep the path comparison OS-agnostic: use `PurePosixPath` / `str(path).replace("\\", "/")` on the
relative path, since `git ls-files` always emits forward slashes but `pathlib` on Windows does not.
  </action>

  <verify>
    <automated>pytest tests/test_invite_drift_guard.py -q</automated>
    <automated>pytest tests/test_invite_logic.py tests/test_invite_drift_guard.py tests/test_invite_cog.py -q</automated>
    <automated>pytest -q</automated>
    <automated>ruff check . && ruff format --check .</automated>
  </verify>

  <acceptance_criteria>
    - `tests/test_invite_drift_guard.py` defines `_tracked_python_files` (excluding `tests/` and `.planning/`) and a hardcoded `_CONSTRUCTOR_MARKERS` literal tuple containing `"oauth_url("` and `"discord.com/oauth2/authorize"`.
    - `test_logic_invite_is_the_only_url_constructor` passes today and would FAIL if `oauth_url(` were added to any non-test, non-`logic/invite.py` module (verify by reasoning through the assertion, not by actually adding one).
    - The test compares POSIX-normalized relative paths, so it passes on Windows and on ubuntu-latest CI alike.
    - `test_config_holds_no_url_literal` passes.
    - `pytest tests/test_invite_drift_guard.py -q` exits 0 with 9 tests passing (7 from Task 1 + 2 here).
    - `pytest -q` (full suite) exits 0.
    - `ruff check . && ruff format --check .` exits 0.
  </acceptance_criteria>

  <done>
D-03's "one and only one place a URL is constructed" is enforced by CI in Python, not just in docs.
Phase 23 physically cannot add a second constructor without turning the build red.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo docs (README, `/site`) → the public / a recruiter clicking "Add to Discord" | The promoted artifact is what people actually click. It is a *separate copy* of the URL from the in-bot one, and copies drift. This boundary is the entire reason SC-3 exists. |
| A future contributor / future phase → the invite URL | Phase 23 will paste this URL into new files. Nothing but a structural gate stops it from pasting a stale, hand-edited, or over-privileged variant. |
| CI (GitHub Actions, zero secrets, `permissions: contents: read`) → this guard | The guard must run with no `.env`, no Discord credentials, and read-only git access, or it does not run where it matters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02 | Tampering (of the promoted artifact, not the code) | Any git-tracked `.md`/`.html`/`.txt` doc promoting an invite link | mitigate | `test_no_doc_contains_a_drifted_invite_url`: `git ls-files` → drop `.planning/` (D-10) → keep text extensions → regex every OAuth2 URL → assert literal equality with `build_invite_url()`. Runs in the existing Phase 18 CI gate with **no workflow change** (it is an ordinary pytest) and **no secrets** (D-04's committed client-ID constant is what makes this possible). |
| T-22-02a | Tampering (guard is a no-op / false green) | The drift-guard itself | mitigate | The guard passes vacuously today (zero URLs in scope), which is indistinguishable from a broken scanner. `test_drift_guard_actually_detects_a_mismatch` (mandatory positive control, `tmp_path`) feeds a deliberately-wrong URL — `permissions=8`, i.e. literal Administrator — through the *same* `_collect_offenders` function and asserts it is caught. `test_drift_guard_accepts_the_canonical_url` is the paired negative control. |
| T-22-03 | Tampering / Spoofing | A second URL constructor in Python, or a shortener/vanity redirect | mitigate | `test_logic_invite_is_the_only_url_constructor` scans every tracked non-test `.py` and asserts `oauth_url(` / `discord.com/oauth2/authorize` appear ONLY in `logic/invite.py` (D-03). D-07's "literal OAuth2 URL, no shorteners" is enforced by the literal-match guard: a redirect URL would not match `build_invite_url()`'s output and would fail. |
| T-22-08 | Denial of Service (self-inflicted — a permanently-red CI gate) | `.planning/research/STACK.md`'s `<APP_ID>` placeholder URL | mitigate | D-10's `.planning/` directory-prefix exclusion + the `.md`/`.html`/`.txt` extension allowlist. Locked by `test_planning_tree_is_excluded_from_the_scan`, which asserts `.planning/` docs are tracked-but-skipped rather than merely absent (so it cannot pass for the wrong reason). |
| T-22-SC | Tampering | Supply chain (pip installs) | accept | **Zero new package installs this phase.** The guard uses only stdlib (`re`, `subprocess`, `pathlib`) plus the already-installed pytest. No `pip install` task exists in this plan. |
</threat_model>

<artifacts_this_phase_produces>
Symbols created by THIS plan (22-03):

| Symbol | File | Kind |
|--------|------|------|
| `tests/test_invite_drift_guard.py` | — | new test file (new repo-introspection test infrastructure) |
| `URL_PATTERN` | `tests/test_invite_drift_guard.py` | module constant (regex) |
| `TEXT_EXTENSIONS` | `tests/test_invite_drift_guard.py` | module constant |
| `PLANNING_PREFIX` | `tests/test_invite_drift_guard.py` | module constant (D-10 denylist) |
| `_CONSTRUCTOR_MARKERS` | `tests/test_invite_drift_guard.py` | module constant (hardcoded literal tuple) |
| `_ALLOWED_CONSTRUCTOR` | `tests/test_invite_drift_guard.py` | module constant |
| `_repo_root` | `tests/test_invite_drift_guard.py` | helper |
| `_tracked_doc_files` | `tests/test_invite_drift_guard.py` | helper |
| `_tracked_python_files` | `tests/test_invite_drift_guard.py` | helper |
| `_canonical_url` | `tests/test_invite_drift_guard.py` | helper |
| `_collect_offenders` | `tests/test_invite_drift_guard.py` | helper (the seam the positive control exercises) |
| `test_no_doc_contains_a_drifted_invite_url` | `tests/test_invite_drift_guard.py` | test (T-22-02) |
| `test_drift_guard_actually_detects_a_mismatch` | `tests/test_invite_drift_guard.py` | test (mandatory positive control) |
| `test_drift_guard_accepts_the_canonical_url` | `tests/test_invite_drift_guard.py` | test (negative control) |
| `test_scanner_matches_urls_in_markdown_html_and_bare_forms` | `tests/test_invite_drift_guard.py` | test |
| `test_planning_tree_is_excluded_from_the_scan` | `tests/test_invite_drift_guard.py` | test (D-10) |
| `test_only_text_extensions_are_scanned` | `tests/test_invite_drift_guard.py` | test (D-10) |
| `test_canonical_url_resolves_without_env_secrets` | `tests/test_invite_drift_guard.py` | test (D-04 CI parity) |
| `test_logic_invite_is_the_only_url_constructor` | `tests/test_invite_drift_guard.py` | test (D-03/D-07 / T-22-03) |
| `test_config_holds_no_url_literal` | `tests/test_invite_drift_guard.py` | test |
</artifacts_this_phase_produces>

<verification>
- `pytest tests/test_invite_drift_guard.py -q` — green, 9 tests.
- `pytest -q` — full suite green.
- `ruff check . && ruff format --check .` — green.
- The main guard passes **vacuously** (zero tracked non-`.planning/` docs currently carry an OAuth2 URL — confirmed by `git grep oauth2/authorize` at planning time), and the positive control proves that pass is real.

**Phase-23 hand-off note (record in the SUMMARY):** Phase 23 must paste the literal output of
`build_invite_url()` into the README / `/site` — no shortener, no vanity redirect, no second
constructor (D-07). The moment it does, `test_no_doc_contains_a_drifted_invite_url` stops being
vacuous and starts enforcing SC-3 with zero further work.
</verification>

<success_criteria>
- INVITE-02 / SC-3: the in-bot link and any publicly-promoted link cannot diverge — a divergence fails CI.
- The guard runs in the zero-secret Phase 18 CI gate with no workflow change.
- The guard is provably not a no-op (positive control) and provably not a blanket-reject (negative control).
- `logic/invite.py` is the only URL constructor in the repo, enforced structurally.
</success_criteria>

<output>
Create `.planning/phases/22-invite-plumbing/22-03-SUMMARY.md` when done.
</output>
</content>
