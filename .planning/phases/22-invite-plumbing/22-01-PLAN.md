---
phase: 22-invite-plumbing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - logic/__init__.py
  - logic/invite.py
  - tests/test_invite_logic.py
autonomous: true
requirements: [INVITE-01]
user_setup: []

must_haves:
  truths:
    - "The invite permission bitfield is exactly 309240908864 — the ten permissions Dexter's code provably uses (D-01 + D-09)"
    - "The bitfield grants NO administrator, manage_guild, manage_roles, manage_channels, ban_members, or kick_members — and a future silent escalation fails CI (D-02)"
    - "A single pure function builds the invite URL; it requests the `bot` + `applications.commands` scopes"
    - "The client ID resolves with zero secrets in CI (committed public constant + env override, D-04)"
  artifacts:
    - path: "config.py"
      provides: "DISCORD_CLIENT_ID, INVITE_PERMISSIONS_VALUE, INVITE_SCOPES"
      contains: "INVITE_PERMISSIONS_VALUE = 309240908864"
    - path: "logic/invite.py"
      provides: "build_invite_url() — the ONLY invite-URL constructor in the codebase"
      exports: ["build_invite_url"]
      min_lines: 25
    - path: "tests/test_invite_logic.py"
      provides: "Bitfield derivation lock, D-02 negative-assertion security lock, URL/scope shape tests"
      min_lines: 50
  key_links:
    - from: "logic/invite.py"
      to: "discord.utils.oauth_url"
      via: "the single wrapped library call (never a hand-built query string, D-07)"
      pattern: "discord\\.utils\\.oauth_url"
    - from: "tests/test_invite_logic.py"
      to: "config.INVITE_PERMISSIONS_VALUE"
      via: "exact-value + negative-flag assertions"
      pattern: "INVITE_PERMISSIONS_VALUE"
---

<objective>
Ship INVITE-01: the least-privilege OAuth2 permission bitfield and the one pure function that
builds Dexter's invite URL.

Purpose: This is the foundation both Wave 2 plans consume — `cogs/invite.py` (22-02) renders the
URL this builder returns, and the drift-guard (22-03) compares every public doc against it. Locking
the bitfield with a negative assertion (D-02) turns INVITE-01's "no Administrator" claim from a
comment into a CI-enforced contract (threat T-22-01).

Output: `config.py` constants (D-04/D-09), `logic/invite.py::build_invite_url()` (D-03), and
`tests/test_invite_logic.py` locking both.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-invite-plumbing/22-CONTEXT.md
@.planning/phases/22-invite-plumbing/22-RESEARCH.md
@.planning/phases/22-invite-plumbing/22-PATTERNS.md
@.planning/phases/22-invite-plumbing/22-VALIDATION.md
@CLAUDE.md
</context>

<interface_contract>
Consumed by plans 22-02 and 22-03. These names are FIXED — do not rename:

```
config.DISCORD_CLIENT_ID        -> int
config.INVITE_PERMISSIONS_VALUE -> int   (== 309240908864)
config.INVITE_SCOPES            -> tuple[str, ...]  (== ("bot", "applications.commands"))

logic.invite.build_invite_url(
    *,
    client_id: int,
    permissions_value: int,
    scopes: tuple[str, ...] = ("bot", "applications.commands"),
) -> str
```

Canonical output for the committed default client ID (VERIFIED by executing against the installed
discord.py 2.7.1 — note the param order discord.py emits: client_id, scope, permissions):

    https://discord.com/oauth2/authorize?client_id=1492588698364018898&scope=bot+applications.commands&permissions=309240908864
</interface_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the invite config constants + lock the bitfield with a negative assertion (D-02/D-04/D-09)</name>

  <files>config.py, tests/test_invite_logic.py</files>

  <read_first>
    - `config.py` — read the tail (the `# --- Phase 20: Owner Control Plane ... ---` block) and lines 55-90 for the `int(os.getenv("X") or "default")` idiom used by `DEXTER_CHANNEL_ID` and `OWNER_ID`. New knobs go in `config.py` and nowhere else.
    - `.planning/phases/22-invite-plumbing/22-CONTEXT.md` — D-01 (the 8-permission table with a code citation per permission), **D-09** (the amendment adding the two thread permissions; the locked value), D-02 (the negative-assertion test), D-04 (committed public constant + env override).
    - `.planning/phases/22-invite-plumbing/22-PATTERNS.md` § `config.py` (modify) — the exact `# --- Phase N: ... ---` block-header placement convention.
    - `tests/test_vision_logic.py` — the mock-free pure-unit test convention (file docstring, config-derived constants, no mocks/clocks/RNG).
  </read_first>

  <behavior>
    Tests to write in `tests/test_invite_logic.py` FIRST (they fail until `config.py` is edited):
    - `test_bitfield_matches_ten_permission_derivation` — constructing
      `discord.Permissions(view_channel=True, send_messages=True, embed_links=True, attach_files=True,
      add_reactions=True, read_message_history=True, connect=True, speak=True,
      create_public_threads=True, send_messages_in_threads=True)` yields `.value == 309240908864`,
      and that equals `config.INVITE_PERMISSIONS_VALUE`. This is the self-documenting derivation:
      the magic number is never trusted on its own.
    - `test_bitfield_excludes_dangerous_permissions` (the D-02 negative lock / T-22-01 mitigation) —
      `discord.Permissions(config.INVITE_PERMISSIONS_VALUE)` has `administrator is False`,
      `manage_guild is False`, `manage_roles is False`, `manage_channels is False`,
      `ban_members is False`, `kick_members is False`.
    - `test_bitfield_grants_every_required_permission` — the same `Permissions` object has all ten
      required flags `True` (so a future *reduction* that silently breaks `/imagine` attachments,
      reactions, voice, or `/autolyrics` threads also fails CI, not just an escalation).
    - `test_client_id_is_a_positive_int` — `config.DISCORD_CLIENT_ID` is an `int` and `> 0`
      (proves it resolves in CI with zero secrets — D-04's whole rationale).
    - `test_scopes_constant_is_the_expected_tuple` — `config.INVITE_SCOPES == ("bot", "applications.commands")`.
  </behavior>

  <action>
Append a new `# --- Phase 22: Invite Plumbing (INVITE-01/02) ---` block to the END of `config.py`,
placed BEFORE the `def sanitize_database_url(...)` function definition (constants stay above the
helper, matching the file's existing layout). Add exactly three constants:

  - `DISCORD_CLIENT_ID = int(os.getenv("DISCORD_CLIENT_ID") or "1492588698364018898")` — follows the
    established `int(os.getenv(...) or "default")` idiom. Comment it: a Discord client ID is PUBLIC
    by design (it is visible in every invite link ever handed out), so committing it leaks nothing;
    the committed default is load-bearing because the Phase 18 CI gate runs with **zero secrets and
    no `.env`** and the 22-03 drift-guard must still resolve a real client ID there (D-04). The env
    override keeps a fork pointed at its own app.
  - `INVITE_PERMISSIONS_VALUE = 309240908864` — the ten-permission least-privilege bitfield (D-01 as
    amended by D-09). Comment it with the ten permission names and WHY each is code-proven:
    `view_channel`, `send_messages`, `embed_links`, `attach_files` (cogs/imagine.py:69-74),
    `add_reactions` (cogs/events.py:345,355,379), `read_message_history` (cogs/music.py:833),
    `connect` (cogs/music.py:526), `speak`, `create_public_threads` (cogs/music.py:938 — `/autolyrics`),
    `send_messages_in_threads` (cogs/music.py:950,958,965). Also record that the 8-permission value
    `3263552` is **SUPERSEDED** by D-09 and must not reappear, and point at
    `tests/test_invite_logic.py::test_bitfield_excludes_dangerous_permissions` as the lock, so a
    future contributor reads the value as deliberate rather than "fixing" it.
  - `INVITE_SCOPES: tuple[str, ...] = ("bot", "applications.commands")` — a tuple, never a bare
    string (`oauth_url()` takes an `Iterable[str]`; a bare string would iterate character-by-character).

Then create `tests/test_invite_logic.py` with the five tests in `<behavior>`. Follow
`tests/test_vision_logic.py`'s discipline: a file docstring naming the phase/requirement/decisions,
`import config` + `import discord`, no mocks, no clocks, no RNG. Assert the ten-permission derivation
BEFORE asserting the literal — the derivation test is what makes the magic number auditable.

Do NOT touch `dexter-architecture.md` (it is untracked and gitignored — RESEARCH.md's Pitfall 0/1
are already resolved). Do NOT add an `INVITE_PERMISSIONS` `Permissions` object to config — config
holds the int; `logic/invite.py` reconstructs the `Permissions` object.
  </action>

  <verify>
    <automated>pytest tests/test_invite_logic.py -q</automated>
    <automated>python -c "import config; assert config.INVITE_PERMISSIONS_VALUE == 309240908864; assert config.INVITE_SCOPES == ('bot','applications.commands'); print('ok', config.DISCORD_CLIENT_ID)"</automated>
    <automated>ruff check config.py tests/test_invite_logic.py && ruff format --check config.py tests/test_invite_logic.py</automated>
  </verify>

  <acceptance_criteria>
    - `config.py` contains the literal `INVITE_PERMISSIONS_VALUE = 309240908864`.
    - `grep -c "3263552" config.py` returns 0 outside of a comment explicitly marking it SUPERSEDED; the superseded 8-permission value is never assigned to a constant.
    - `config.py` contains `DISCORD_CLIENT_ID = int(os.getenv("DISCORD_CLIENT_ID") or "1492588698364018898")`.
    - `config.py` contains `INVITE_SCOPES: tuple[str, ...] = ("bot", "applications.commands")`.
    - `tests/test_invite_logic.py` contains a test asserting `.administrator is False`, `.manage_guild is False`, `.manage_roles is False`, `.manage_channels is False`, `.ban_members is False`, `.kick_members is False` on `discord.Permissions(config.INVITE_PERMISSIONS_VALUE)`.
    - `tests/test_invite_logic.py` contains a test that derives `309240908864` from the ten `discord.Permissions(**kwargs)` keyword flags and asserts it equals `config.INVITE_PERMISSIONS_VALUE`.
    - `pytest tests/test_invite_logic.py -q` exits 0.
    - `ruff check config.py tests/test_invite_logic.py` and `ruff format --check config.py tests/test_invite_logic.py` both exit 0.
    - No file named `dexter-architecture.md` is created, modified, or referenced.
  </acceptance_criteria>

  <done>
The ten-permission least-privilege bitfield is a committed, self-documenting, CI-enforced constant.
Adding `manage_guild` (or dropping `attach_files`) in a future phase turns CI red.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create logic/invite.py::build_invite_url() — the single URL constructor (D-03/D-07)</name>

  <files>logic/invite.py, logic/__init__.py, tests/test_invite_logic.py</files>

  <read_first>
    - `logic/vision.py` — the pure-seam module docstring convention (Phase 10 D-01/D-02): what nondeterminism is excluded, which test file locks it.
    - `logic/proactive.py` — the keyword-only, config-defaulted signature convention.
    - `logic/__init__.py` — the two-line package comment. It currently claims modules "never [import] from discord, asyncio, or the DB" — this task makes that literally false and must amend it.
    - `.planning/phases/22-invite-plumbing/22-PATTERNS.md` § `logic/invite.py` — the flagged convention deviation and the recommended resolution (option 1: call it out explicitly).
    - `.planning/phases/22-invite-plumbing/22-RESEARCH.md` § Pattern 1 + § Don't Hand-Roll — the VERIFIED `discord.utils.oauth_url()` signature for discord.py 2.7.1 and why hand-building the query string is prohibited.
  </read_first>

  <behavior>
    Tests to APPEND to `tests/test_invite_logic.py`:
    - `test_url_contains_expected_scopes` — `build_invite_url(client_id=..., permissions_value=...)`
      output contains the substring `scope=bot+applications.commands`.
    - `test_url_contains_locked_permissions_value` — the output contains
      `permissions=309240908864` and the given `client_id=`.
    - `test_url_is_the_literal_oauth2_endpoint` — the output starts with
      `https://discord.com/oauth2/authorize?` (no shortener, no vanity redirect — D-07).
    - `test_scopes_are_overridable_but_default_to_the_config_tuple` — passing an explicit
      `scopes=("bot",)` changes the emitted `scope=` param, proving the parameter is honored and
      is not silently ignored.
    - `test_builder_is_deterministic` — two calls with identical args return an identical string
      (pure function, no clock/RNG/network).
  </behavior>

  <action>
Create `logic/invite.py` with this exact signature (keyword-only, per the Phase 10 seam convention):

    def build_invite_url(
        *,
        client_id: int,
        permissions_value: int,
        scopes: tuple[str, ...] = ("bot", "applications.commands"),
    ) -> str:

Its entire body is one call: `discord.utils.oauth_url(client_id, permissions=discord.Permissions(permissions_value), scopes=scopes)`.
Do NOT hand-concatenate or f-string a query string, and do NOT pass an `integration_type` param — no
such parameter exists on `oauth_url()` in discord.py 2.7.1 (VERIFIED against the installed package);
user-installable apps are a separate `app_commands.allowed_installs` mechanism, irrelevant to a bot
install. Do NOT pass `scopes` as a bare space-delimited string.

The module docstring MUST do three things:
  1. State that this is the ONLY place an invite URL is ever constructed (D-03/D-07) — `cogs/invite.py`
     calls it and never hand-builds a URL.
  2. **Explicitly call out the deliberate convention deviation**: every other `logic/` module's
     docstring promises "no `discord`", and this one imports `discord`. Explain WHY it is safe and
     necessary — `discord.utils.oauth_url()` and `discord.Permissions()` are offline, deterministic,
     no-I/O, no-network helpers, so the module still satisfies the *spirit* of the seam rule
     (deterministic, side-effect-free, no clock/RNG/network); and hand-rolling the URL to avoid the
     import would recreate exactly the second URL constructor D-07 prohibits. Do not silently break
     the convention — the docstring IS the mitigation.
  3. Name the lock: `tests/test_invite_logic.py`.

Amend `logic/__init__.py`'s package comment so it stays truthful: keep the "never from discord,
asyncio, or the DB" rule as the default, and add the single documented exception — `invite.py`
imports `discord` solely for the offline `oauth_url()`/`Permissions()` helpers (no I/O), see that
module's docstring. Do not delete the rule; qualify it.

Then append the five tests from `<behavior>` to `tests/test_invite_logic.py`, importing
`from logic.invite import build_invite_url`. Tests must derive expectations from `config` constants,
never from a hardcoded full URL string (a hardcoded URL would break under the `DISCORD_CLIENT_ID`
env override).
  </action>

  <verify>
    <automated>pytest tests/test_invite_logic.py -q</automated>
    <automated>python -c "import config; from logic.invite import build_invite_url; u = build_invite_url(client_id=config.DISCORD_CLIENT_ID, permissions_value=config.INVITE_PERMISSIONS_VALUE); assert u.startswith('https://discord.com/oauth2/authorize?'); assert 'scope=bot+applications.commands' in u; assert 'permissions=309240908864' in u; print(u)"</automated>
    <automated>ruff check logic/ tests/test_invite_logic.py && ruff format --check logic/ tests/test_invite_logic.py</automated>
  </verify>

  <acceptance_criteria>
    - `logic/invite.py` exists and defines `build_invite_url` with keyword-only params `client_id`, `permissions_value`, `scopes`.
    - `grep -c "discord.utils.oauth_url" logic/invite.py` returns 1 (exactly one call site).
    - `logic/invite.py` contains NO f-string or `+`-concatenation producing a `discord.com` URL, and no `integration_type`.
    - `logic/invite.py`'s module docstring explicitly names the `import discord` deviation and why it is safe (a grep for `discord` in the docstring returns a match, not a silent import).
    - `logic/__init__.py` still states the no-discord rule AND names `invite.py` as the single documented exception.
    - Running `build_invite_url(client_id=config.DISCORD_CLIENT_ID, permissions_value=config.INVITE_PERMISSIONS_VALUE)` returns exactly: `https://discord.com/oauth2/authorize?client_id=1492588698364018898&scope=bot+applications.commands&permissions=309240908864`
    - `pytest tests/test_invite_logic.py -q` exits 0 with at least 10 tests collected across both tasks.
    - `ruff check logic/ tests/test_invite_logic.py` and `ruff format --check ...` both exit 0.
  </acceptance_criteria>

  <done>
`logic/invite.py::build_invite_url()` is the single, pure, test-locked source of the invite URL.
Plans 22-02 and 22-03 can now import it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dexter → any guild that installs it | The OAuth2 permission bitfield IS the access-control artifact granted at this boundary. Over-requesting here is granted forever, silently, by every future installer. |
| Repo → CI (GitHub Actions, zero secrets) | The bitfield lock must be verifiable with no `.env` and no Discord credentials, or the guarantee is unenforced in the only place it runs automatically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Elevation of Privilege | `config.INVITE_PERMISSIONS_VALUE` / the invite URL's `permissions=` param | mitigate | Explicit ten-flag `discord.Permissions(**kwargs)` derivation (never hand-summed bits) + D-02's negative-assertion test asserting `administrator`/`manage_guild`/`manage_roles`/`manage_channels`/`ban_members`/`kick_members` are all `False` AND `.value == 309240908864`. A future phase adding `manage_guild` "for one feature" fails CI instead of shipping unnoticed (ASVS V4). |
| T-22-01b | Elevation of Privilege (inverse — silent capability loss) | same | mitigate | `test_bitfield_grants_every_required_permission` asserts all ten required flags are `True`, so a well-meaning "tighten the bitfield" change that silently breaks `/imagine` attachments, reactions, voice, or `/autolyrics` threads on fresh servers also fails CI. |
| T-22-04 | Information Disclosure | `config.DISCORD_CLIENT_ID` committed to the repo | accept | A Discord client/application ID is public by design — it is visible in every invite link ever handed out and in every OAuth2 consent screen. It is not a credential. The bot token remains env-only (`.env`, never committed). D-04. |
| T-22-SC | Tampering | Supply chain (pip installs) | accept | **Zero new package installs this phase.** `discord.py` 2.7.1 is already installed and pinned by `requirements.txt`. RESEARCH.md § Package Legitimacy Audit: not applicable. No `pip install` task exists in this plan. |
</threat_model>

<artifacts_this_phase_produces>
Symbols created by THIS plan (22-01):

| Symbol | File | Kind |
|--------|------|------|
| `DISCORD_CLIENT_ID` | `config.py` | constant (int, env-overridable) |
| `INVITE_PERMISSIONS_VALUE` | `config.py` | constant (int == 309240908864) |
| `INVITE_SCOPES` | `config.py` | constant (tuple[str, ...]) |
| `build_invite_url` | `logic/invite.py` | pure function |
| `logic/invite.py` | — | new module |
| `tests/test_invite_logic.py` | — | new test file |
| `test_bitfield_matches_ten_permission_derivation` | `tests/test_invite_logic.py` | test |
| `test_bitfield_excludes_dangerous_permissions` | `tests/test_invite_logic.py` | test (D-02 / T-22-01 lock) |
| `test_bitfield_grants_every_required_permission` | `tests/test_invite_logic.py` | test |
| `test_client_id_is_a_positive_int` | `tests/test_invite_logic.py` | test |
| `test_scopes_constant_is_the_expected_tuple` | `tests/test_invite_logic.py` | test |
| `test_url_contains_expected_scopes` | `tests/test_invite_logic.py` | test |
| `test_url_contains_locked_permissions_value` | `tests/test_invite_logic.py` | test |
| `test_url_is_the_literal_oauth2_endpoint` | `tests/test_invite_logic.py` | test |
| `test_scopes_are_overridable_but_default_to_the_config_tuple` | `tests/test_invite_logic.py` | test |
| `test_builder_is_deterministic` | `tests/test_invite_logic.py` | test |
</artifacts_this_phase_produces>

<verification>
- `pytest tests/test_invite_logic.py -q` — green.
- `pytest -q` — full suite green (no regression; this plan adds only new files + appended config constants).
- `ruff check . && ruff format --check .` — green (the Phase 18 CI gate runs both).
- The bitfield is derivable from ten named `discord.Permissions` keywords, not asserted as a bare magic number.
</verification>

<success_criteria>
- INVITE-01's "explicit `Permissions()` bitfield — no Administrator, no Manage Server/Roles" is a CI-enforced contract, not prose.
- `build_invite_url()` is the only invite-URL constructor in the codebase (22-03 will structurally enforce this).
- Both constants resolve in a zero-secret CI environment (D-04).
</success_criteria>

<output>
Create `.planning/phases/22-invite-plumbing/22-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
