---
phase: 07-player-ux-filters
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - database.py
  - cogs/library.py
  - bot.py
  - tests/test_database_phase7.py
autonomous: true
requirements: [PLAYER-05]
user_setup: []

must_haves:
  truths:
    - "A new user_favorites table (keyed by user_id + video_id) stores personal, server-agnostic favorites that follow a user across guilds (D-18, D-22, PLAYER-05)"
    - "/favorite saves the currently-playing song only; a 26th save is rejected with an ephemeral cap message (cap 25, D-19, D-21)"
    - "/favorites shows a pick-list select menu (SongSelect-style); choosing one queues it; the menu also lets the owner remove an entry (D-20)"
    - "favorites DB helpers follow the asyncpg pool + $N-parameterized convention and are covered by live-Postgres integration tests (D-22)"
    - "all favorites user-facing copy is ephemeral and in Dexter's templated voice (D-29, D-30)"
  artifacts:
    - path: "database.py"
      provides: "user_favorites schema + add_favorite/get_favorites/remove_favorite/count_favorites helpers"
      contains: "CREATE TABLE IF NOT EXISTS user_favorites"
    - path: "cogs/library.py"
      provides: "LibraryCog with /favorite and /favorites + FavoritesView select menu"
      contains: "class LibraryCog"
    - path: "tests/test_database_phase7.py"
      provides: "live-DB tests for the favorites helpers (insert, cap, dedupe, remove, list)"
      contains: "user_favorites"
    - path: "bot.py"
      provides: "cogs.library loaded at startup"
      contains: "cogs.library"
  key_links:
    - from: "cogs/library.py /favorite"
      to: "database.add_favorite + the playing track"
      via: "MusicCog queue lookup for the current track, then a capped insert"
      pattern: "add_favorite"
    - from: "FavoritesView select"
      to: "MusicCog queueing path"
      via: "reconstruct a Track from the favorite row and add it to the guild queue"
      pattern: "get_cog(\"MusicCog\")"
---

<objective>
Add personal favorites: a new `user_favorites` Postgres table with helpers, and a new `LibraryCog` exposing `/favorite` (save the current song) and `/favorites` (a pick-list menu to queue or remove saved songs). Favorites are per-user and global (D-18), capped at 25 (D-21), current-song-only (D-19).

Purpose: Give users a durable, cross-server personal shortlist — the first of the two "personal saves" features in this phase.

Output: `user_favorites` schema + `add_favorite`/`get_favorites`/`remove_favorite`/`count_favorites` in database.py (live-DB tested), `cogs/library.py` (`LibraryCog` + `FavoritesView`), and the cog wired into bot.py startup.
</objective>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-player-ux-filters/07-CONTEXT.md
@.planning/phases/07-player-ux-filters/07-01-SUMMARY.md
@.planning/phases/07-player-ux-filters/07-02-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this plan produces (Plan 03)

| Symbol / File | Kind | Decision |
|---------------|------|----------|
| `user_favorites` table (PK user_id+video_id) | new schema in SCHEMA_SQL | D-18, D-22 |
| `add_favorite` / `get_favorites` / `remove_favorite` / `count_favorites` | new asyncpg helpers | D-19, D-20, D-21 |
| `cogs/library.py::LibraryCog` | new cog | PLAYER-05 |
| `LibraryCog.favorite` (save current) + `LibraryCog.favorites` (pick-list) | new commands | D-19, D-20 |
| `cogs/library.py::FavoritesView` (SongSelect-style select + remove) | new view | D-20 |
| `bot.py` loads `cogs.library` | startup wiring | — |
| `tests/test_database_phase7.py` favorites cases | new tests | D-22 |
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: user_favorites schema + helpers (live-DB tested)</name>
  <files>database.py, tests/test_database_phase7.py</files>
  <read_first>
    - database.py — SCHEMA_SQL (66-124), helper convention (`async def x(pool: asyncpg.Pool, *, ...)`, `async with pool.acquire() as conn`, $N params); representative helpers log_image (296-309) / get_images_today (312-320)
    - tests/conftest.py — the `pool` fixture (live dexter_test DB; init_db; teardown DROPs tables). NOTE: extend the teardown DROP list to include user_favorites (and user_playlists from Plan 04) so test isolation holds
    - tests/test_database_phase4.py — live-DB test style (@pytest.mark.asyncio, pool fixture, fetchval assertions)
  </read_first>
  <behavior>
    - add_favorite inserts a row; a duplicate (same user_id+video_id) is a no-op (ON CONFLICT DO NOTHING) and does NOT consume a cap slot
    - count_favorites returns the user's current count
    - get_favorites returns rows newest-first (added_at DESC), each carrying enough to rebuild a Track (video_id, title, artist, url, duration_seconds)
    - remove_favorite deletes only the (user_id, video_id) row
  </behavior>
  <action>
    Add to SCHEMA_SQL: `CREATE TABLE IF NOT EXISTS user_favorites (user_id TEXT NOT NULL, video_id TEXT NOT NULL, title TEXT NOT NULL, artist TEXT, url TEXT NOT NULL, duration_seconds INTEGER, thumbnail TEXT, added_at TIMESTAMPTZ DEFAULT now(), PRIMARY KEY (user_id, video_id));` plus `CREATE INDEX IF NOT EXISTS idx_favorites_user ON user_favorites(user_id, added_at DESC);`. Add helpers mirroring the existing convention: `add_favorite(pool, *, user_id, video_id, title, artist, url, duration_seconds, thumbnail)` (INSERT ... ON CONFLICT DO NOTHING), `count_favorites(pool, *, user_id) -> int`, `get_favorites(pool, *, user_id, limit=25) -> list[dict]`, `remove_favorite(pool, *, user_id, video_id)`. Write tests/test_database_phase7.py FIRST with the pool fixture: insert/list/count/dedupe/remove cases. Update conftest.py teardown DROP to add `user_favorites` (and `user_playlists`).
  </action>
  <verify>
    <automated>pytest tests/test_database_phase7.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_database_phase7.py -x -q` exits 0 (requires local/test Postgres per conftest)
    - database.py contains `CREATE TABLE IF NOT EXISTS user_favorites` and the four helpers
    - conftest teardown drops user_favorites/user_playlists
  </acceptance_criteria>
  <done>Favorites persistence exists, deduped, capable of round-tripping a Track, and is live-DB tested.</done>
</task>

<task type="auto">
  <name>Task 2: LibraryCog with /favorite + /favorites pick-list</name>
  <files>cogs/library.py, bot.py</files>
  <read_first>
    - cogs/music.py — SongSelect/SongSelectView (41-89) for the select-menu pattern; how a Track is added to a guild queue + playback started (_queue_from_selection / queue.add / _play_track); get_queue(guild_id)
    - models/queue.py — Track.from_dict / Track fields (to rebuild a Track from a favorite row)
    - personality/responses.py — FAVORITE_SAVED/FAVORITE_DUPLICATE/FAVORITE_CAP_HIT/FAVORITES_EMPTY/NOTHING_PLAYING (Plan 01); pick_random
    - bot.py — _initialize_once cog-load loop (~312-318); add cogs.library to the always-on tuple
    - config.FAVORITES_MAX_PER_USER (Plan 01)
  </read_first>
  <behavior>
    - /favorite with a current track: if count_favorites >= FAVORITES_MAX_PER_USER → ephemeral FAVORITE_CAP_HIT; else add_favorite → ephemeral FAVORITE_SAVED (duplicate → ephemeral FAVORITE_DUPLICATE). No current track → ephemeral NOTHING_PLAYING.
    - /favorites with no rows → ephemeral FAVORITES_EMPTY; else an ephemeral FavoritesView select menu (up to 25 options) — choosing one rebuilds a Track and queues it via the MusicCog (joins voice if needed, same path as /play selection); a "remove" affordance lets the owner delete the chosen entry
    - all responses ephemeral; favorites are inherently the invoking user's, so no extra ownership check is needed beyond keying on interaction.user.id
  </behavior>
  <action>
    Create cogs/library.py with `class LibraryCog(commands.Cog)` and the standard `async def setup(bot): await bot.add_cog(LibraryCog(bot))`. Reach Postgres via `self.bot.pool` and music via `self.bot.get_cog("MusicCog")`. Implement `@app_commands.command(name="favorite")`: get current track from the guild queue; cap-check via count_favorites; call add_favorite (use str(user.id)); ephemeral response from the matching pool. Implement `@app_commands.command(name="favorites")`: get_favorites; empty → ephemeral; else build `FavoritesView` (a discord.ui.View holding a Select whose options are the favorites, value=video_id; a timed view like SongSelectView, timeout ~180s, ephemeral). On select: rebuild a Track (Track(video_id=..., title=..., artist=..., url=..., duration_seconds=..., requested_by=user.id, thumbnail=...)) and route through the MusicCog's existing "add to queue + start if idle" path; respond ephemerally. Provide removal (e.g. a second Select mode or a "🗑 remove" button) calling remove_favorite. Add `cogs.library` to the bot.py cog-load tuple in _initialize_once (idempotent `if _ext not in bot.extensions`).
  </action>
  <verify>
    <automated>python -c "import ast; m=ast.parse(open('cogs/library.py',encoding='utf-8').read()); assert any(isinstance(n,ast.ClassDef) and n.name=='LibraryCog' for n in ast.walk(m)); b=open('bot.py',encoding='utf-8').read(); assert 'cogs.library' in b; print('OK')"</automated>
    <human-check>/play a song, /favorite → ephemeral "saved"; /favorite again → ephemeral "already saved"; /favorites → ephemeral menu, pick one → it queues; remove one → gone; verify a favorite saved in server A appears in server B (global).</human-check>
  </verify>
  <acceptance_criteria>
    - cogs/library.py defines LibraryCog with `favorite` and `favorites` commands + FavoritesView
    - bot.py loads cogs.library
    - cap + duplicate + empty paths are ephemeral and on-brand
  </acceptance_criteria>
  <done>Users can save the current song and re-queue or remove favorites from a pick-list, globally across servers, capped at 25.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Discord user → user_favorites | a user writes/reads their own favorites keyed by their Discord id |
| favorite row → queue | a stored row is rebuilt into a Track and played |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering / Injection | favorites SQL | mitigate | All helpers use $N-parameterized asyncpg queries; no string interpolation. |
| T-07-03-02 | Elevation of Privilege | cross-user access | mitigate | Every read/write is keyed on `str(interaction.user.id)`; a user can only touch their own rows. |
| T-07-03-03 | DoS | unbounded saves | mitigate | FAVORITES_MAX_PER_USER (25) enforced before insert; dedupe via PK avoids count inflation. |
| T-07-03-04 | Tampering | stale/invalid favorite URL | accept | A dead YouTube URL fails through the existing AudioService skip-and-continue path; no new exposure. |
| T-07-03-SC | Tampering | dependencies | mitigate | No new pip deps. |
</threat_model>

<verification>
- `pytest tests/test_database_phase7.py -x -q` green (live/test Postgres)
- `pytest tests/ -x -q` green (no regression; conftest teardown updated)
- `python -c "import ast; ast.parse(open('cogs/library.py',encoding='utf-8').read()); ast.parse(open('database.py',encoding='utf-8').read())"` exits 0
- Clean local boot; /favorite + /favorites human-checks pass (cogs untested-by-design)
</verification>

<success_criteria>
- user_favorites persists per-user, global, deduped, capped at 25, live-DB tested.
- /favorite saves the current song; /favorites queues or removes via a pick-list; all ephemeral + on-brand.
- LibraryCog loads cleanly at startup.
- No new pip dependencies.
</success_criteria>

<output>
Create `.planning/phases/07-player-ux-filters/07-03-SUMMARY.md` when done.
</output>
