---
phase: 18-per-guild-config-foundation-ci-gate
plan: 05
type: execute
wave: 4
depends_on: [18-04]
files_modified:
  - bot.py
autonomous: true
requirements: [CONFIG-02, CONFIG-03, CONFIG-04, CONFIG-05]
user_setup: []

must_haves:
  truths:
    - "At boot, bot.guild_config is constructed, the cache is loaded once (fail-closed), and the home guild is seeded from config.DEXTER_CHANNEL_ID"
    - "An unset or unresolvable DEXTER_CHANNEL_ID is a silent INFO skip at boot — no error, no boot refusal"
    - "bot.py::_resolve_dexter_channel is deleted; the startup-message and idle-loneliness sites call bot.guild_config.resolve_ambient_channel"
    - "The startup message and idle message stay silent in a guild with no config row"
    - "No stopgap owner-only setter is added — the only write path this phase is the boot seed (D-13)"
  artifacts:
    - path: "bot.py"
      provides: "GuildConfigService boot wiring (construct + load_all + seed_home_guild) and repointed ambient call sites; _resolve_dexter_channel removed"
      contains: "GuildConfigService"
  key_links:
    - from: "bot.py _initialize_once"
      to: "bot.guild_config.load_all + seed_home_guild"
      via: "constructed after log_to_discord helper, before the cog-load loop"
      pattern: "guild_config"
    - from: "bot.py:_post_startup_messages / idle_check"
      to: "bot.guild_config.resolve_ambient_channel"
      via: "replacing _resolve_dexter_channel calls"
      pattern: "resolve_ambient_channel"
---

<objective>
Wire `GuildConfigService` into `bot.py`'s boot sequence (CONFIG-03/D-04) and consolidate bot.py's
two ambient call sites onto the strict resolver (CONFIG-02/04). Construct `bot.guild_config`,
load the cache once (D-06, fail-closed D-07), and run the idempotent home-guild seed (CONFIG-05 /
D-08/D-09) that keeps the owner's guild behaving exactly as before. Delete `_resolve_dexter_channel`
(its 4-step body already lives in the service's `resolve_announce_channel` from 18-04) and repoint the
startup-message (line 515) and idle-loneliness (line 739) sites at
`bot.guild_config.resolve_ambient_channel(guild)`.

Purpose: The home guild becomes an ordinary tenant with an ordinary row; a fresh clone / CI run with
no `DEXTER_CHANNEL_ID` boots fine and stays ambient-silent (D-10). The env var is demoted to a
one-time bootstrap input with no ongoing authority. Per D-13 this phase adds NO user-facing setter
and NO way to configure a second guild — the boot seed is the ONLY write path; `/setup` lands in
Phase 19.
Output: `bot.py` boot wiring + two repointed call sites; `_resolve_dexter_channel` gone.
</objective>

<artifacts>
## Artifacts this plan produces
- `bot.py::_initialize_once` — new block constructing `bot.guild_config = GuildConfigService(bot.pool, bot)`, `await bot.guild_config.load_all()`, and the home-guild seed.
- Deletion of `bot.py::_resolve_dexter_channel` (module-level function, lines 103-143).
- Repointed ambient call sites at `bot.py:515` (startup message) and `bot.py:739` (idle-loneliness).
</artifacts>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-per-guild-config-foundation-ci-gate/18-PATTERNS.md
@.planning/phases/18-per-guild-config-foundation-ci-gate/18-RESEARCH.md
@bot.py
@config.py
@services/guild_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boot wiring — construct service, load cache (fail-closed), seed home guild</name>
  <files>bot.py</files>
  <read_first>
    - bot.py (`_initialize_once` 356-448: pool creation ~382, `init_db` at 392, `bot.log_to_discord` helper defined at 432-434, the cog-load loop starts at 441; the `_ready_done`/`_ready_initializing` guard wrapping `_initialize_once`; `_post_startup_messages` at 505-522 for the try/except-log idiom)
    - config.py (line 57: `DEXTER_CHANNEL_ID = int(os.getenv("DEXTER_CHANNEL_ID") or "0") or None` — may be None)
    - services/guild_config.py (constructor `GuildConfigService(pool, bot)`; `load_all`, `seed_home_guild` signatures — from 18-04)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-PATTERNS.md (§"bot.py — deletion + call-site rewrites" — the boot-wiring insertion point; §"Cache-miss semantics" fail-closed)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-RESEARCH.md (§"Common Pitfalls" Pitfall 4 — do NOT add a second on_ready; Pitfall 5 — bot.get_channel returns None in setup_hook, must run in _initialize_once)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-CONTEXT.md (D-06/D-07/D-08/D-09/D-10; D-13 — no stopgap setter)
  </read_first>
  <action>
    Inside `_initialize_once` (NOT `setup_hook`, NOT a second `on_ready` — Pitfall 4/5), add a block
    AFTER `bot.log_to_discord` is defined (line 432-434, so fail-closed errors can reach the error
    channel) and BEFORE the cog-load loop (line 441): construct `bot.guild_config =
    GuildConfigService(bot.pool, bot)`; `await bot.guild_config.load_all()` (the service already
    handles fail-closed internally — do not wrap in a boot-aborting try). Then run the home-guild seed:
    read `config.DEXTER_CHANNEL_ID`; if it is falsy, log at INFO ("DEXTER_CHANNEL_ID unset — no home
    guild seeded; ambient-silent until /setup") and skip (D-10, expected for a fresh clone / CI). Else
    `ch = bot.get_channel(config.DEXTER_CHANNEL_ID)`; if `ch is None` OR it has no `.guild`, log at INFO
    (unresolvable — silent skip per D-10) and skip; else `await bot.guild_config.seed_home_guild(
    guild_id=str(ch.guild.id), ambient_channel_id=str(ch.id))`. Wrap the seed in a narrow try/except
    that logs a WARNING and continues (never abort boot). Add NO new module-level `on_ready`. Do NOT
    add any owner-only setter or second-guild config path (D-13 — the seed is the only writer this phase).
  </action>
  <verify>
    <automated>python -c "src=open('bot.py',encoding='utf-8').read(); [__import__('sys').exit('missing '+n) for n in ['GuildConfigService(bot.pool, bot)','guild_config.load_all()','seed_home_guild(','bot.get_channel(config.DEXTER_CHANNEL_ID)'] if n not in src]; assert src.count('async def on_ready')<=1, 'no second on_ready'; print('ok')"</automated>
  </verify>
  <done>
    `bot.guild_config` is constructed and its cache loaded in `_initialize_once`; the home guild is
    seeded when `DEXTER_CHANNEL_ID` resolves and silently skipped (INFO) when unset/unresolvable; no
    second `on_ready` was added.
  </done>
  <acceptance_criteria>
    - `bot.py` constructs `GuildConfigService(bot.pool, bot)` and calls `await bot.guild_config.load_all()` inside `_initialize_once`, after the `bot.log_to_discord` helper and before the cog-load loop.
    - The seed calls `bot.get_channel(config.DEXTER_CHANNEL_ID)` and calls `seed_home_guild` only when the channel resolves to a guild.
    - An unset/unresolvable `DEXTER_CHANNEL_ID` logs at INFO and does not raise (D-10).
    - No second `async def on_ready` is added (Pitfall 4); no owner-only setter is added (D-13).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delete _resolve_dexter_channel; repoint bot.py's two ambient sites</name>
  <files>bot.py</files>
  <read_first>
    - bot.py (lines 103-143 `_resolve_dexter_channel` to DELETE; line 515 startup-message call site inside `_post_startup_messages`'s try/except; line 739 idle-loneliness call site inside `idle_check`'s try/except)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-PATTERNS.md (§"Call site #1 — startup message" and "#2 — idle-loneliness" — both are inside try/except log.warning blocks that must be preserved)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-RESEARCH.md (§"Call-Site Inventory" rows bot.py:515 and bot.py:739 — both ambient, both must switch to the strict resolver; §"Common Pitfalls" Pitfall 2 — the startup message is an AMBIENT surface, do NOT give it a fallback)
  </read_first>
  <action>
    Delete the entire `_resolve_dexter_channel` function (bot.py lines 103-143) — its fallback-chain
    body now lives in `GuildConfigService.resolve_announce_channel` (18-04). At line 515
    (`_post_startup_messages`), replace `channel = _resolve_dexter_channel(guild)` with `channel =
    bot.guild_config.resolve_ambient_channel(guild)` — a SYNCHRONOUS call, no `await`. At line 739
    (`idle_check`), make the same replacement. Preserve the surrounding `try/except Exception:
    log.warning(...)` blocks and the `allowed_mentions=discord.AllowedMentions.none()` on the startup
    send unchanged. Do NOT route either site through `resolve_announce_channel` (Pitfall 2 — the
    startup message is an ambient surface that must go silent in an unconfigured guild, CONFIG-04).
    After the edits, confirm `_resolve_dexter_channel` is referenced nowhere in bot.py.
  </action>
  <verify>
    <automated>python -c "src=open('bot.py',encoding='utf-8').read(); assert 'def _resolve_dexter_channel' not in src, 'function must be deleted'; assert '_resolve_dexter_channel(' not in src, 'no residual calls'; assert src.count('guild_config.resolve_ambient_channel(') >= 2, 'both sites repointed'; print('ok')" && python -c "import ast; ast.parse(open('bot.py',encoding='utf-8').read()); print('parse ok')"</automated>
  </verify>
  <done>
    `_resolve_dexter_channel` is gone from bot.py; both ambient call sites use
    `bot.guild_config.resolve_ambient_channel(guild)` (no `await`); bot.py parses cleanly.
  </done>
  <acceptance_criteria>
    - `bot.py` contains neither `def _resolve_dexter_channel` nor any `_resolve_dexter_channel(` call.
    - Both the startup-message and idle-loneliness sites call `bot.guild_config.resolve_ambient_channel(guild)` synchronously (no `await`).
    - Neither site calls `resolve_announce_channel`.
    - `bot.py` parses (`ast.parse`) without error.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env `DEXTER_CHANNEL_ID` → boot seed | A config value (not user input) seeds the home-guild row |
| boot sequence → Discord posts | fail-closed errors surface to the owner ops channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-SEED | Tampering / Access Control | Home-guild boot seed | mitigate | `guild_id` derives from `bot.get_channel(config.DEXTER_CHANNEL_ID).guild.id` — a config value, never user input; seed uses the `DO NOTHING` helper so it cannot overwrite a later deliberate write (D-09) |
| T-18-FAILCLOSED | Denial of Service (soft) | `load_all` at boot | accept | Deliberate D-07 — a load failure leaves every guild unconfigured (ambient silent, `/play`/`/ask` live); error surfaces to `dexter.log` + `ERROR_LOG_CHANNEL_ID`; a restart fixes it |
| T-18-AMBIENT | Information Disclosure | startup / idle call sites | mitigate | Both repointed to the STRICT `resolve_ambient_channel`; a guild with no row gets `None` → no startup/idle message (CONFIG-04) — silence is structural, not a remembered guard |
| T-18-BOOT | Tampering | `on_ready` re-entrancy | mitigate | Wiring lives inside `_initialize_once` under the existing `_ready_done` guard; no second `on_ready` is added (Pitfall 4), so per-shard READY cannot double-run the seed unsafely |
</threat_model>

<verification>
- `bot.py` constructs `bot.guild_config`, loads the cache, and seeds the home guild inside `_initialize_once`.
- `_resolve_dexter_channel` deleted; both ambient sites use `resolve_ambient_channel` (sync).
- Unset `DEXTER_CHANNEL_ID` → INFO skip, no raise.
- `pytest -q` full suite: 0 fail (no regression).
</verification>

<success_criteria>
- CONFIG-03/CONFIG-05: boot constructs the service, loads the cache once, and idempotently seeds the home guild so the owner's guild is unchanged.
- CONFIG-02/CONFIG-04: bot.py's two ambient sites resolve through the single strict resolver; unconfigured guilds get no startup/idle message.
- D-10: a fresh clone / CI with no `DEXTER_CHANNEL_ID` boots fine and stays ambient-silent.
- D-13: no stopgap setter or second-guild config path ships — the boot seed is the only writer this phase.
</success_criteria>

<output>
Create `.planning/phases/18-per-guild-config-foundation-ci-gate/18-05-SUMMARY.md` when done.
</output>
