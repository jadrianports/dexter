---
phase: 18-per-guild-config-foundation-ci-gate
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - logic/guild_config.py
  - tests/test_guild_config_logic.py
autonomous: true
requirements: [CONFIG-02]
user_setup: []

must_haves:
  truths:
    - "decide_ambient_channel returns None for a missing row (no config) — silence is structural, not remembered"
    - "decide_ambient_channel returns None when configured is False even if ambient_channel_id is set"
    - "decide_ambient_channel returns the int channel id only when configured is True and ambient_channel_id is present"
    - "is_ambient_channel returns True only when the decided channel equals the given channel_id"
    - "logic/guild_config.py imports no discord, no asyncio, no datetime, no random"
  artifacts:
    - path: "logic/guild_config.py"
      provides: "pure decide_ambient_channel + is_ambient_channel decision seam (D-05)"
      contains: "def decide_ambient_channel"
      min_lines: 15
    - path: "tests/test_guild_config_logic.py"
      provides: "mock-free branch coverage of both pure functions"
      exports: []
  key_links:
    - from: "is_ambient_channel"
      to: "decide_ambient_channel"
      via: "delegates the row decision, compares to channel_id"
      pattern: "decide_ambient_channel\\(config_row="
---

<objective>
Extract the pure `logic/guild_config.py` decision seam (D-05) — two keyword-only, `discord`-free,
`datetime`-free, `random`-free functions the service and the two on_message gates dispatch on:
`decide_ambient_channel(*, config_row)` (D-01 strict decision) and `is_ambient_channel(*, config_row,
channel_id)` (the predicate that replaces both bare-equality `== config.DEXTER_CHANNEL_ID` gates).
Lock the silent-until-configured invariant under mock-free tests, exactly as `logic/proactive.py`
and `logic/vision.py` are locked.

Purpose: The safety property (an unconfigured guild is structurally silent) lives in one tested pure
function so no future ambient surface can forget to guard itself. The glue dispatches on the return
value and never re-derives the branch (Phase 10 D-02 convention).
Output: `logic/guild_config.py`, `tests/test_guild_config_logic.py`.
</objective>

<artifacts>
## Artifacts this plan produces
- `logic/guild_config.py::decide_ambient_channel(*, config_row: Mapping | None) -> int | None` (D-01, D-05).
- `logic/guild_config.py::is_ambient_channel(*, config_row: Mapping | None, channel_id: int) -> bool` (CONFIG-02 predicate).
- `tests/test_guild_config_logic.py` — NEW mock-free test file covering every branch.
</artifacts>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-per-guild-config-foundation-ci-gate/18-PATTERNS.md
@logic/proactive.py
@tests/test_roast_logic.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: logic/guild_config.py — pure decision seam</name>
  <files>logic/guild_config.py</files>
  <read_first>
    - logic/proactive.py (the exact template: module docstring stating "no random, no asyncio, no datetime, no discord", keyword-only params, one function per decision, boundary-condition comments)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-PATTERNS.md (§"logic/guild_config.py" — the verbatim target skeleton for both functions; note "no resolve_announce_channel logic belongs here" — the fallback chain does discord I/O and stays in the service tier)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-CONTEXT.md (D-01 strict, D-05 pure seam)
  </read_first>
  <behavior>
    - decide_ambient_channel(config_row=None) -> None (no row → silence)
    - decide_ambient_channel(config_row={"configured": False, "ambient_channel_id": "123"}) -> None (unconfigured → silence, even with a channel set)
    - decide_ambient_channel(config_row={"configured": True, "ambient_channel_id": None}) -> None (configured but no channel → None)
    - decide_ambient_channel(config_row={"configured": True, "ambient_channel_id": "123"}) -> 123 (int, coerced from TEXT storage)
    - is_ambient_channel(config_row={"configured": True, "ambient_channel_id": "123"}, channel_id=123) -> True
    - is_ambient_channel(config_row={"configured": True, "ambient_channel_id": "123"}, channel_id=999) -> False
    - is_ambient_channel(config_row=None, channel_id=123) -> False
  </behavior>
  <action>
    Create `logic/guild_config.py` with a `from __future__ import annotations` header and a module
    docstring stating the module is pure (no `random`, no `asyncio`, no `datetime`, no `discord`).
    Define `decide_ambient_channel(*, config_row: Mapping | None) -> int | None`: return `None` if
    `config_row is None`; return `None` if `not config_row.get("configured", False)`; else read
    `ambient_channel_id` and return `int(channel_id)` when it is not `None`, otherwise `None`. Because
    `ambient_channel_id` is stored as TEXT, coerce with `int(...)`. Define
    `is_ambient_channel(*, config_row: Mapping | None, channel_id: int) -> bool` that calls
    `decide_ambient_channel(config_row=config_row)` and returns `decided is not None and decided ==
    channel_id`. Both functions are keyword-only. Do NOT put any `guild.get_channel`, permission, or
    fallback-chain logic here — that is service-tier I/O.
  </action>
  <verify>
    <automated>python -c "import inspect; import logic.guild_config as m; src=inspect.getsource(m); assert 'import discord' not in src and 'import datetime' not in src and 'import random' not in src and 'import asyncio' not in src; assert m.decide_ambient_channel(config_row=None) is None; assert m.decide_ambient_channel(config_row={'configured':False,'ambient_channel_id':'1'}) is None; assert m.decide_ambient_channel(config_row={'configured':True,'ambient_channel_id':'123'})==123; assert m.is_ambient_channel(config_row={'configured':True,'ambient_channel_id':'123'},channel_id=123) is True; assert m.is_ambient_channel(config_row=None,channel_id=1) is False; print('ok')"</automated>
  </verify>
  <done>
    Both pure functions exist with the specified behavior; the module imports no discord/datetime/
    random/asyncio.
  </done>
  <acceptance_criteria>
    - `logic/guild_config.py` contains `def decide_ambient_channel(` and `def is_ambient_channel(`.
    - Source contains none of: `import discord`, `import datetime`, `import random`, `import asyncio`.
    - Both functions are keyword-only (`*,` in the signature).
    - All 7 behavior cases in `<behavior>` hold when called directly.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: tests/test_guild_config_logic.py — mock-free branch lock</name>
  <files>tests/test_guild_config_logic.py</files>
  <read_first>
    - tests/test_roast_logic.py (the mock-free test convention to mirror — plain assertions on pure-function returns, no patch/Mock)
    - logic/guild_config.py (the module under test, from Task 1)
  </read_first>
  <action>
    Create `tests/test_guild_config_logic.py` with mock-free tests (no `unittest.mock`, no fixtures,
    no DB) covering every branch listed in Task 1's `<behavior>`: the None row, the unconfigured row,
    the configured-but-null-channel row, the configured-with-channel row (asserting an `int` return),
    and both `is_ambient_channel` match/mismatch/None cases. Include an assertion that the module
    source contains none of `import discord`/`import datetime`/`import random` (mirrors the
    "pure seam" self-check other logic tests use). These tests need no DB and no async — plain
    synchronous test functions.
  </action>
  <verify>
    <automated>pytest -q tests/test_guild_config_logic.py</automated>
  </verify>
  <done>
    All branch tests pass with zero mocks and zero DB dependency.
  </done>
  <acceptance_criteria>
    - `pytest -q tests/test_guild_config_logic.py` exits 0.
    - The test file contains no `import` of `unittest.mock` / `Mock` / `patch`.
    - There is at least one assertion per branch (None row, unconfigured, configured-null-channel, configured-with-channel, is_ambient_channel true/false/None).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached config row → ambient dispatch decision | The pure functions decide whether a guild's ambient surfaces may fire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-AMBIENT | Information Disclosure / Spoofing | `decide_ambient_channel` / `is_ambient_channel` | mitigate | A missing or unconfigured row returns `None`/`False` structurally — an un-opted-in guild can never be spoofed into ambient output because the pure decision is the single gate, mock-free-tested for every branch (D-01/D-05) |
| T-18-PURITY | Tampering | `logic/guild_config.py` module surface | mitigate | The module imports no I/O or nondeterminism (no discord/datetime/random/asyncio) — asserted by test, so the decision cannot be perturbed by hidden state and the glue cannot re-derive the branch (Phase 10 D-02 convention) |
</threat_model>

<verification>
- Both pure functions return per the 7 behavior cases.
- Module imports no discord/datetime/random/asyncio.
- `pytest -q tests/test_guild_config_logic.py` exits 0 with no mocks.
</verification>

<success_criteria>
- CONFIG-02 (pure half): `decide_ambient_channel` + `is_ambient_channel` exist as the single decision
  seam, mock-free tested, with silence structural for unconfigured guilds.
</success_criteria>

<output>
Create `.planning/phases/18-per-guild-config-foundation-ci-gate/18-03-SUMMARY.md` when done.
</output>
