---
phase: 18-per-guild-config-foundation-ci-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements-dev.txt
  - "ALL tracked *.py (ruff check --fix + ruff format, mechanical whole-repo pass)"
autonomous: false
requirements: [CICD-01]
user_setup: []

must_haves:
  truths:
    - "ruff check . exits 0 across the whole repo"
    - "ruff format --check . exits 0 across the whole repo"
    - "The lint/format cleanup is a single atomic commit, separate from any config-seam code"
    - "The full pytest suite still collects 956 tests and passes 848 / skips 108 / fails 0 after the mechanical pass"
  artifacts:
    - path: "pyproject.toml"
      provides: "[tool.ruff] config — target py311, line-length 120, select E/F/W/I"
      contains: "[tool.ruff]"
    - path: "requirements-dev.txt"
      provides: "Ruff dev dependency pin"
      contains: "ruff"
  key_links:
    - from: "pyproject.toml"
      to: "ruff check / ruff format"
      via: "[tool.ruff] + [tool.ruff.lint] + [tool.ruff.format] tables"
      pattern: "line-length = 120"
---

<objective>
Adopt Ruff as the single lint+format tool (D-14) and bring the existing ~10k-LOC codebase clean
under a near-default ruleset, as ONE atomic mechanical commit kept fully separate from the
config-seam refactor (D-16). This is the FIRST plan in Phase 18 on purpose: it runs a repo-wide
`ruff check --fix` + `ruff format` pass so every subsequent config-seam commit lands on
already-clean files and its diff stays pure logic with zero formatting noise.

Purpose: Both pytest and Ruff become BLOCKING gates in CI (18-07). A gate that is red on `main`
is not a gate — so the repo must be clean before the CI workflow is wired.
Output: `pyproject.toml` `[tool.ruff]`, `requirements-dev.txt`, and a whole-repo formatted tree.
</objective>

<artifacts>
## Artifacts this plan produces
- `pyproject.toml` — NEW file, `[tool.ruff]` / `[tool.ruff.lint]` / `[tool.ruff.lint.per-file-ignores]` / `[tool.ruff.format]` tables (D-14).
- `requirements-dev.txt` — NEW file pinning `ruff>=0.15,<0.16` (dev-only, no runtime import).
- A repo-wide mechanical reformat (`ruff check --fix` import sorting + `ruff format`) as one atomic commit.
</artifacts>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-per-guild-config-foundation-ci-gate/18-RESEARCH.md
@.planning/phases/18-per-guild-config-foundation-ci-gate/18-CONTEXT.md
@requirements.txt
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — approve Ruff before install</name>
  <read_first>
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-RESEARCH.md (§"Package Legitimacy Audit" — Ruff is tagged [ASSUMED] because slopcheck was unavailable in the research sandbox; §"Standard Stack" for the `ruff>=0.15,<0.16` pin rationale)
  </read_first>
  <action>
    Do NOT run any `pip install` until this gate is approved. Present the Ruff legitimacy evidence to
    the user (pypi.org/project/ruff, Astral source repo github.com/astral-sh/ruff, the pinned
    `ruff>=0.15,<0.16`, dev-only surface) and wait for explicit approval before proceeding to Task 2.
    This is a blocking-human gate per the package-legitimacy protocol for `[ASSUMED]` packages and is
    never auto-approvable (`workflow.auto_advance` is ignored here).
  </action>
  <what-built>
    Nothing is installed yet. This gate precedes the `pip install ruff` step. Ruff is a dev-only
    lint/format tool (no runtime import, never touches production data or secrets), tagged [ASSUMED]
    per the package-legitimacy protocol because slopcheck could not run during research.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/ruff/ and confirm it is the Astral-maintained project
       (source repo github.com/astral-sh/ruff), actively released, millions of weekly downloads.
    2. Confirm the intended install is the pinned floor `ruff>=0.15,<0.16` (dev-only), NOT a
       floating `ruff@latest` or an unpinned major.
    3. Confirm Ruff is added to `requirements-dev.txt` only — NOT to runtime `requirements.txt`.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the Ruff install, or describe concerns.</resume-signal>
  <acceptance_criteria>
    - This is a blocking-human checkpoint and is NEVER auto-approvable (workflow.auto_advance is ignored for package legitimacy gates).
    - No `pip install` runs before this checkpoint is approved.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create Ruff config + dev-requirements</name>
  <files>pyproject.toml, requirements-dev.txt</files>
  <read_first>
    - requirements.txt (confirm current runtime pins; Ruff must NOT be added here)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-RESEARCH.md (§"Code Examples" → "Ruff config (pyproject.toml, new file)" — the concrete target table, sized against a live local run: line-length=88 gives 902 E501 findings, line-length=120 drops that to ~32 genuine long lines)
  </read_first>
  <action>
    Install the approved package with `pip install "ruff>=0.15,<0.16"`. Create `requirements-dev.txt`
    at repo root containing exactly the pin `ruff>=0.15,<0.16` (dev-only surface; runtime
    `requirements.txt` is untouched). Create `pyproject.toml` at repo root with a `[tool.ruff]` table
    setting `target-version = "py311"` (matches the project's Python 3.11+ floor) and
    `line-length = 120`; a `[tool.ruff.lint]` table with `select = ["E", "F", "W", "I"]`
    (pyflakes + pycodestyle + isort — near-default per D-14) and `ignore = []`; a
    `[tool.ruff.lint.per-file-ignores]` table mapping `"tests/*.py"` to `["F401"]` (test files import
    fixtures/mocks for side effects); and a `[tool.ruff.format]` table with default (black-compatible)
    settings. Do NOT add a `[project]` table — this pyproject exists for Ruff config only, not
    packaging. Do NOT tighten beyond the near-default ruleset (D-14 says start near default, tighten
    later).
  </action>
  <verify>
    <automated>ruff --version && ruff check pyproject.toml --select E,F,W,I --statistics; test -f requirements-dev.txt</automated>
  </verify>
  <done>
    `pyproject.toml` and `requirements-dev.txt` exist; `ruff --version` reports a 0.15.x build;
    `ruff` reads the config without a config-parse error.
  </done>
  <acceptance_criteria>
    - `pyproject.toml` contains the literal strings `[tool.ruff]`, `line-length = 120`, `target-version = "py311"`, and `select = ["E", "F", "W", "I"]`.
    - `pyproject.toml` contains NO `[project]` table.
    - `requirements-dev.txt` contains `ruff>=0.15,<0.16`; runtime `requirements.txt` does NOT mention ruff.
    - `ruff check .` runs without a configuration error (findings are expected at this point; they are fixed in Task 3).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Repo-wide lint/format cleanup — one atomic commit</name>
  <files>ALL tracked *.py (mechanical ruff check --fix + ruff format across the repo)</files>
  <read_first>
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-RESEARCH.md (§"Common Pitfalls" → Pitfall 6: the format pass must be its OWN commit, never entangled with config-seam logic; §"Ruff Adoption Sizing" numbers: ~58 source findings + ~124 test findings, mostly auto-fixable import sorting; `ruff format` wants 73/103 files)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-CONTEXT.md (D-16 — cleanup is its own atomic commit so the refactor stays reviewable)
  </read_first>
  <action>
    Run `ruff check . --fix` (auto-fixes import sorting and other safe fixes) then `ruff format .`
    across the entire repository. Manually resolve any residual `ruff check .` findings that `--fix`
    could not auto-repair (e.g. genuine unused-name or long-line cases) with the smallest correct
    edit — do NOT suppress with blanket `# noqa`. This whole pass is a SINGLE mechanical commit
    (message e.g. `style: adopt ruff, format repo`); it MUST NOT include any `guild_config`,
    `services/`, `logic/`, or CI-workflow code — those land in later plans. After the pass, run the
    full pytest suite to confirm the mechanical reformat regressed nothing (baseline: 956 collected,
    848 pass / 108 skip / 0 fail; the 108 live-DB skips stay skipped locally without TEST_DATABASE_URL).
  </action>
  <verify>
    <automated>ruff check . && ruff format --check . && pytest -q</automated>
  </verify>
  <done>
    `ruff check .` exits 0, `ruff format --check .` exits 0, and `pytest -q` reports 848 passed /
    108 skipped / 0 failed with no change in the collected count (956).
  </done>
  <acceptance_criteria>
    - `ruff check .` exits 0 with no findings.
    - `ruff format --check .` exits 0 (repo already formatted).
    - `pytest -q` shows 0 failed and 956 collected (848 pass / 108 skip), unchanged from the v1.3-close baseline.
    - The cleanup commit touches only formatting/import-order plus the two new config files — no config-seam source (no `services/guild_config.py`, `logic/guild_config.py`, `guild_config` DDL, or `.github/`).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → dev machine | An externally-fetched package (Ruff) is installed into the dev/CI environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-SC | Tampering | `pip install ruff` (npm/pip supply chain) | mitigate | Ruff is [ASSUMED] (slopcheck unavailable in research); Task 1 is a `blocking-human` legitimacy checkpoint verified against pypi.org/project/ruff BEFORE any install; pinned `ruff>=0.15,<0.16` (no floating latest); dev-only, no runtime import, never handles secrets or user data |
| T-18-SC2 | Elevation of Privilege | Ruff config auto-fix rewriting source | accept | `ruff check --fix` applies only safe fixes; the full pytest suite gate (Task 3) proves no behavioral regression; format is black-compatible and mechanical |
</threat_model>

<verification>
- `ruff check .` and `ruff format --check .` both exit 0 on the whole repo.
- `pytest -q` unchanged from baseline (848/108/0, 956 collected).
- `git log` shows the cleanup as one commit distinct from any config-seam commit.
</verification>

<success_criteria>
- Ruff is installed (post-approval), configured via `pyproject.toml`, pinned in `requirements-dev.txt`.
- The entire repo passes `ruff check` + `ruff format --check`.
- No config-seam or CI code is mixed into the cleanup commit (D-16).
</success_criteria>

<output>
Create `.planning/phases/18-per-guild-config-foundation-ci-gate/18-01-SUMMARY.md` when done.
</output>
