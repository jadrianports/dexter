---
phase: 18-per-guild-config-foundation-ci-gate
plan: 06
type: execute
wave: 4
depends_on: [18-04]
files_modified:
  - cogs/events.py
  - tests/test_proactive_events.py
autonomous: true
requirements: [CONFIG-02, CONFIG-04]
user_setup: []

must_haves:
  truths:
    - "cogs/events.py::_get_ambient_channel is deleted; the 3 ambient call sites call self.bot.guild_config.resolve_ambient_channel WITHOUT await"
    - "The proactive-callback and vision-roast on_message gates route through is_ambient_channel instead of message.channel.id == config.DEXTER_CHANNEL_ID"
    - "The vision-roast gate keeps its message.attachments condition and is NOT merged with the proactive gate"
    - "grep -rn DEXTER_CHANNEL_ID cogs/ returns nothing after this plan"
    - "The existing suite baseline (0 fail) is preserved; test_proactive_events.py patches the new seam, not config.DEXTER_CHANNEL_ID"
  artifacts:
    - path: "cogs/events.py"
      provides: "ambient call sites + on_message gates routed through the consolidated resolver/predicate; _get_ambient_channel removed; no DEXTER_CHANNEL_ID reference"
      contains: "resolve_ambient_channel"
    - path: "tests/test_proactive_events.py"
      provides: "patch targets updated to the new seam"
      exports: []
  key_links:
    - from: "cogs/events.py on_message gates"
      to: "logic.guild_config.is_ambient_channel"
      via: "predicate over self.bot.guild_config.get(message.guild.id)"
      pattern: "is_ambient_channel"
    - from: "cogs/events.py voice sites"
      to: "self.bot.guild_config.resolve_ambient_channel"
      via: "synchronous call, await dropped"
      pattern: "resolve_ambient_channel"
---

<objective>
Consolidate all four `cogs/events.py` ambient surfaces onto the Phase 18 seam (CONFIG-02/04). Delete
the duplicate `_get_ambient_channel` fallback function; repoint its three voice-event call sites at
`self.bot.guild_config.resolve_ambient_channel(...)` — dropping the `await`, because the new resolver
is SYNCHRONOUS (cache-only). Replace the two bare-equality `message.channel.id ==
config.DEXTER_CHANNEL_ID` gates in `on_message` (proactive-callback dispatch + vision-roast dispatch)
with the pure `is_ambient_channel(config_row=..., channel_id=...)` predicate. Update the regression
tests that patch `config.DEXTER_CHANNEL_ID` to patch the new seam, and prove `DEXTER_CHANNEL_ID` no
longer appears anywhere under `cogs/`.

Purpose: After this plan, an unconfigured guild is structurally silent on every events.py surface,
and the env var is fully demoted out of the hot path (a grep in `cogs/` returns nothing).
Output: consolidated `cogs/events.py`; updated `tests/test_proactive_events.py`.
</objective>

<artifacts>
## Artifacts this plan produces
- Deletion of `cogs/events.py::_get_ambient_channel` (lines 98-137).
- Repointed voice-event call sites at `cogs/events.py:266` (bot-moved complaint), `:310` (voice-join roast), `:356` (voice-leave roast) — `await` dropped.
- Rewritten `on_message` gates at `cogs/events.py:443-448` (proactive) and `:454-460` (vision) using `is_ambient_channel`.
- Updated patch targets in `tests/test_proactive_events.py:183` and `:202`.
</artifacts>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-per-guild-config-foundation-ci-gate/18-PATTERNS.md
@.planning/phases/18-per-guild-config-foundation-ci-gate/18-RESEARCH.md
@cogs/events.py
@logic/guild_config.py
@tests/test_proactive_events.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete _get_ambient_channel; repoint 3 voice sites; rewrite 2 on_message gates</name>
  <files>cogs/events.py</files>
  <read_first>
    - cogs/events.py (lines 98-137 `_get_ambient_channel` to DELETE; call sites at 266 / 310 / 356 each currently `await self._get_ambient_channel(...)`; the two `on_message` gates at 443-448 and 454-460; how `logic` imports are done at the top of the file)
    - logic/guild_config.py (`is_ambient_channel(*, config_row, channel_id)` — from 18-03)
    - services/guild_config.py (`resolve_ambient_channel` is synchronous; `get(guild_id)` cache accessor — from 18-04)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-PATTERNS.md (§"cogs/events.py — deletion + 4 call-site rewrites + 2 bare-equality-gate rewrites" — the exact before/after for each; NOTE the "drop await" instruction and the "do NOT merge the vision gate with the proactive gate" instruction)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-RESEARCH.md (§"Call-Site Inventory" — all 6 sites; §"Common Pitfalls" Pitfall 2)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-CONTEXT.md (D-01 strict, CONFIG-02 exactly-one-path, CONFIG-04 ambient default-off)
  </read_first>
  <action>
    Delete the entire `_get_ambient_channel` method (cogs/events.py lines 98-137) — its fallback body
    now lives once in `GuildConfigService.resolve_announce_channel` (18-04), NOT duplicated here. At
    lines 266, 310, and 356, replace `channel = await self._get_ambient_channel(<guild-expr>)` with
    `channel = self.bot.guild_config.resolve_ambient_channel(<guild-expr>)` — DROP the `await` (the new
    resolver is synchronous; keeping `await` on a non-coroutine would raise, and returning a coroutine
    into a truthiness check would fire ambient output in every guild — the exact opposite of CONFIG-04).
    Keep each site's surrounding structure otherwise unchanged (the `<guild-expr>` is `member.guild` at
    266, `guild` at 310 and 356 — preserve whichever the current code uses).

    Add `from logic.guild_config import is_ambient_channel` to the imports. Rewrite the proactive gate
    (443-448): replace the `and config.DEXTER_CHANNEL_ID and message.channel.id ==
    config.DEXTER_CHANNEL_ID` conditions with a single `and is_ambient_channel(config_row=
    self.bot.guild_config.get(message.guild.id), channel_id=message.channel.id)`, keeping `message.guild
    is not None` and the `await self._maybe_fire_proactive_callback(message)` body. Rewrite the vision
    gate (454-460) identically — substitute the same `is_ambient_channel(...)` predicate for the two
    `config.DEXTER_CHANNEL_ID` conditions, and PRESERVE the `and message.attachments` condition and the
    `await self._maybe_fire_vision_roast(message)` body. Do NOT merge the two gates (they are
    independent cadences per the Phase 17 comment). Remove the now-unused `config` import ONLY if no
    other `config.` reference remains in the file — otherwise leave the import. Confirm no
    `DEXTER_CHANNEL_ID` token remains anywhere in `cogs/events.py`.
  </action>
  <verify>
    <automated>python -c "src=open('cogs/events.py',encoding='utf-8').read(); assert 'DEXTER_CHANNEL_ID' not in src, 'env var must be gone from events.py'; assert 'def _get_ambient_channel' not in src, 'fallback fn must be deleted'; assert 'await self._get_ambient_channel' not in src, 'no residual awaited calls'; assert src.count('guild_config.resolve_ambient_channel(')>=3, '3 voice sites repointed'; assert 'is_ambient_channel(' in src, 'gates use predicate'; assert 'message.attachments' in src, 'vision attachment cond preserved'; print('ok')" && python -c "import ast; ast.parse(open('cogs/events.py',encoding='utf-8').read()); print('parse ok')"</automated>
  </verify>
  <done>
    `_get_ambient_channel` gone; 3 voice sites call the sync `resolve_ambient_channel` (no await); both
    on_message gates use `is_ambient_channel`; vision gate keeps `message.attachments` and stays
    separate; no `DEXTER_CHANNEL_ID` token remains in events.py; the file parses.
  </done>
  <acceptance_criteria>
    - `cogs/events.py` contains no `_get_ambient_channel` definition and no `await self._get_ambient_channel(` call.
    - `cogs/events.py` calls `self.bot.guild_config.resolve_ambient_channel(` at 3 voice sites, none with `await`.
    - Both `on_message` gates call `is_ambient_channel(config_row=self.bot.guild_config.get(...), channel_id=message.channel.id)`; the vision gate still includes `message.attachments`.
    - `grep -rn DEXTER_CHANNEL_ID cogs/` returns nothing.
    - `cogs/events.py` parses (`ast.parse`) without error.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update regression tests to patch the new seam; sweep test files</name>
  <files>tests/test_proactive_events.py</files>
  <read_first>
    - tests/test_proactive_events.py (lines 183 and 202: `patch("cogs.events.config.DEXTER_CHANNEL_ID", 500)` inside `with (...)` blocks around on_message tests; the `_make_bot`/`_make_message` fixtures at the top)
    - tests/test_vision_events.py (check for any DEXTER_CHANNEL_ID / _get_ambient_channel patch that must move to the new seam)
    - tests/test_ambient_recall_cadence.py (check for the same)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-PATTERNS.md (§"tests/test_proactive_events.py — patch-target update" — grep every test file for DEXTER_CHANNEL_ID / _get_ambient_channel / _resolve_dexter_channel before finalizing)
  </read_first>
  <action>
    In `tests/test_proactive_events.py`, replace both `patch("cogs.events.config.DEXTER_CHANNEL_ID",
    500)` uses (lines 183, 202) so the test drives the new seam: give the fake bot a `guild_config`
    whose `get(guild_id)` returns a config row `{"configured": True, "ambient_channel_id": "500"}` and
    set the fake message's `channel.id = 500` (so `is_ambient_channel` returns True), OR patch
    `cogs.events.is_ambient_channel` to return True/False per the test's intent. Whichever approach,
    the "in-channel fires / out-of-channel does not" assertions must still hold. Then grep ALL test
    files for `DEXTER_CHANNEL_ID`, `_get_ambient_channel`, and `_resolve_dexter_channel`; update any
    other occurrence to the new seam the same way. Also ensure the fake bot used across these on_message
    tests exposes a `guild_config` attribute with a `get` and a synchronous `resolve_ambient_channel`
    where those surfaces are exercised. Do NOT patch `cogs.ai.random` or reintroduce removed imports.
  </action>
  <verify>
    <automated>python -c "import glob; bad=[f for f in glob.glob('tests/*.py') if 'DEXTER_CHANNEL_ID' in open(f,encoding='utf-8').read() or '_get_ambient_channel' in open(f,encoding='utf-8').read() or '_resolve_dexter_channel' in open(f,encoding='utf-8').read()]; assert not bad, 'stale seam refs in: '+repr(bad); print('ok')" && pytest -q tests/test_proactive_events.py tests/test_vision_events.py tests/test_ambient_recall_cadence.py</automated>
  </verify>
  <done>
    No test file references `DEXTER_CHANNEL_ID` / `_get_ambient_channel` / `_resolve_dexter_channel`;
    the proactive, vision, and ambient-cadence tests pass against the new seam.
  </done>
  <acceptance_criteria>
    - No file under `tests/` contains `DEXTER_CHANNEL_ID`, `_get_ambient_channel`, or `_resolve_dexter_channel`.
    - `pytest -q tests/test_proactive_events.py tests/test_vision_events.py tests/test_ambient_recall_cadence.py` exits 0.
    - The in-channel-fires / out-of-channel-silent assertions still exercise real behavior (not trivially patched to always-True).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound Discord message → ambient dispatch | `on_message` decides whether proactive/vision cadences fire |
| voice-state event → ambient roast | voice sites decide where (or whether) to post |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-AMBIENT | Information Disclosure / Spoofing | on_message gates + 3 voice sites | mitigate | All four surfaces route through `is_ambient_channel` / strict `resolve_ambient_channel`; an unconfigured guild returns `None`/`False`, so no ambient/vision/proactive output can fire there (CONFIG-04) — silence is structural |
| T-18-COROUTINE | Tampering (correctness) | dropped-`await` at the 3 voice sites | mitigate | The new resolver is synchronous; leaving `await` (raises) or treating a returned coroutine as truthy (fires everywhere) are both prevented by the "drop await" edit + an `ast.parse` + full-suite gate |
| T-18-XGUILD | Information Disclosure | `self.bot.guild_config.get(message.guild.id)` | mitigate | The predicate consults only the triggering guild's cache row keyed by `guild_id` — never another guild's config |
</threat_model>

<verification>
- `_get_ambient_channel` deleted; 3 voice sites use sync `resolve_ambient_channel`; 2 gates use `is_ambient_channel`.
- `grep -rn DEXTER_CHANNEL_ID cogs/` returns nothing; no test file references the old seam.
- `pytest -q` full suite: 0 fail (baseline preserved).
</verification>

<success_criteria>
- CONFIG-02: every events.py ambient surface resolves through the single consolidated resolver/predicate; the duplicate fallback function and both bare-equality gates are gone.
- CONFIG-04: an unconfigured guild is silent on all four events.py surfaces.
- The env var is fully demoted: `DEXTER_CHANNEL_ID` appears nowhere under `cogs/`.
</success_criteria>

<output>
Create `.planning/phases/18-per-guild-config-foundation-ci-gate/18-06-SUMMARY.md` when done.
</output>
