---
phase: 18-per-guild-config-foundation-ci-gate
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - database.py
  - CLAUDE.md
  - tests/test_database_phase18.py
  - tests/conftest.py
autonomous: true
requirements: [CONFIG-01, CONFIG-05, CICD-01]
user_setup: []

must_haves:
  truths:
    - "A guild_config table with all 7 CONFIG-01 columns exists in SCHEMA_SQL and is created by init_db"
    - "seed_guild_config_if_absent inserts with ON CONFLICT (guild_id) DO NOTHING and never overwrites an existing row"
    - "load_all_guild_configs returns every guild_config row in one SELECT with no parameters"
    - "The conftest pool fixture registers the pgvector codec (extension-first) so live-DB memory tests pass rather than error"
  artifacts:
    - path: "database.py"
      provides: "guild_config DDL in SCHEMA_SQL + load_all_guild_configs + seed_guild_config_if_absent helpers"
      contains: "CREATE TABLE IF NOT EXISTS guild_config"
    - path: "tests/test_database_phase18.py"
      provides: "schema-shape + seed-idempotence coverage (CONFIG-01, CONFIG-05)"
      exports: []
    - path: "tests/conftest.py"
      provides: "pgvector-codec-registering pool fixture (CICD-01 prerequisite)"
      contains: "register_vector"
  key_links:
    - from: "seed_guild_config_if_absent"
      to: "guild_config table"
      via: "INSERT ... ON CONFLICT (guild_id) DO NOTHING"
      pattern: "ON CONFLICT \\(guild_id\\) DO NOTHING"
    - from: "tests/conftest.py pool fixture"
      to: "pgvector.asyncpg.register_vector"
      via: "create_pool(dsn, init=register_vector) after extension-first CREATE EXTENSION"
      pattern: "init=register_vector"
---

<objective>
Ship the `guild_config` table (CONFIG-01), its two boot helpers (`load_all_guild_configs`,
`seed_guild_config_if_absent` — the CONFIG-05 seed helper with the D-09 `DO NOTHING` clause), the
CLAUDE.md schema-narrative update, and the prerequisite `tests/conftest.py` pgvector-codec fix that
CICD-01's live-DB suite depends on. This is the data + test-infrastructure foundation the service
(18-04) and boot wiring (18-05) build on.

Purpose: Every later config-seam plan reads from this schema and these helpers; the conftest fix is
what makes the CI service container (18-07) unskip ~9 memory tests into PASSES instead of errors.
Output: `database.py` additions, `tests/test_database_phase18.py`, a fixed `tests/conftest.py`,
CLAUDE.md schema entry.
</objective>

<artifacts>
## Artifacts this plan produces
- `guild_config` table in `database.py::SCHEMA_SQL` — columns `guild_id` (TEXT PK), `ambient_channel_id` (TEXT), `configured` (BOOLEAN NOT NULL DEFAULT false), `silenced` (BOOLEAN NOT NULL DEFAULT false — D-11, no reader this phase), `is_blocked` (BOOLEAN NOT NULL DEFAULT false — D-11, no reader this phase), `joined_at` (TIMESTAMPTZ DEFAULT now()), `updated_at` (TIMESTAMPTZ DEFAULT now()).
- `database.py::load_all_guild_configs(pool) -> list[asyncpg.Record]` — D-06 boot load-all.
- `database.py::seed_guild_config_if_absent(pool, *, guild_id, ambient_channel_id) -> asyncpg.Record | None` — D-08/D-09 idempotent seed.
- `tests/test_database_phase18.py` — NEW test file (schema shape + seed idempotence).
- Fixed `tests/conftest.py` `pool` fixture (pgvector codec registration + teardown DROP additions).
- CLAUDE.md §"Database Schema" `guild_config` narrative entry.
</artifacts>

<execution_context>
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/James/Desktop/Projects/dexter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-per-guild-config-foundation-ci-gate/18-RESEARCH.md
@.planning/phases/18-per-guild-config-foundation-ci-gate/18-PATTERNS.md
@database.py
@tests/conftest.py
@tests/test_database_phase16.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: guild_config DDL + boot helpers in database.py</name>
  <files>database.py, CLAUDE.md</files>
  <read_first>
    - database.py (§SCHEMA_SQL — the `guild_jams` block at lines 188-197 and `resolution_cache` at 178-186 are the structural analogs; the string closes with `"""` at line 198; init_db runs SCHEMA_SQL in ONE conn.execute at 201-207; `get_proactive_opt_out`/`set_proactive_opt_out` at ~318-376 are the helper-shape analog)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-PATTERNS.md (§"database.py — guild_config DDL + helpers" — verbatim target DDL and both helper skeletons, plus the SCAR WARNING)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-CONTEXT.md (D-09 — DO NOTHING never DO UPDATE; D-11 — silenced/is_blocked ship now with no reader; D-12 — ship EXACTLY the CONFIG-01 columns, no ambient_roasts_enabled/vision_roasts_enabled)
    - CLAUDE.md (§"Database Schema (PostgreSQL)" — the narrative that must gain a guild_config entry; §"Implementation Gotchas" Pitfall 1 — SCHEMA_SQL is plain param-free DDL applied in one conn.execute)
  </read_first>
  <action>
    Append a `CREATE TABLE IF NOT EXISTS guild_config (...)` block to `SCHEMA_SQL` immediately before
    its closing `"""` (after the `guild_jams` index line). Columns exactly: `guild_id TEXT PRIMARY KEY`,
    `ambient_channel_id TEXT`, `configured BOOLEAN NOT NULL DEFAULT false`,
    `silenced BOOLEAN NOT NULL DEFAULT false`, `is_blocked BOOLEAN NOT NULL DEFAULT false`,
    `joined_at TIMESTAMPTZ DEFAULT now()`, `updated_at TIMESTAMPTZ DEFAULT now()`. `guild_id` is TEXT
    to match every other guild-keyed table (Discord snowflakes are TEXT throughout this codebase). No
    extra index — the PK serves the only two query patterns (SELECT * load-all + single-row upsert by
    guild_id). Keep it plain, param-free DDL (Pitfall 1). Add a comment on `silenced`/`is_blocked`
    noting "Phase 20 reader only (D-11)". Do NOT add `ambient_roasts_enabled`/`vision_roasts_enabled`
    (those are Phase 19's ALTER per D-12).

    Then add two async helpers near the `get_proactive_opt_out`/`set_proactive_opt_out` pair:
    `load_all_guild_configs(pool)` — a bare `conn.fetch("SELECT guild_id, ambient_channel_id,
    configured, silenced, is_blocked, joined_at, updated_at FROM guild_config")` returning the list
    (D-06, one round-trip, no params); and `seed_guild_config_if_absent(pool, *, guild_id,
    ambient_channel_id)` — an `INSERT INTO guild_config (guild_id, ambient_channel_id, configured)
    VALUES ($1, $2, true) ON CONFLICT (guild_id) DO NOTHING` followed by a `fetchrow` of that guild's
    row, returned to the caller. Use `$N` positional params only (T-18-SQLI). CRITICAL: the seed uses
    `ON CONFLICT (guild_id) DO NOTHING`, NOT the `DO UPDATE` idiom from `set_proactive_opt_out` — D-09
    forbids re-asserting the env value on every boot (it would make a later /setup silently self-revert).

    Finally, update CLAUDE.md §"Database Schema (PostgreSQL)": add the `guild_config` CREATE TABLE block
    to the schema listing and add a one-line "Phase 18 added the guild_config table" note in the
    narrative paragraph above the SQL.
  </action>
  <verify>
    <automated>python -c "import inspect, database; s=database.SCHEMA_SQL; assert 'CREATE TABLE IF NOT EXISTS guild_config' in s; assert all(c in s for c in ['ambient_channel_id','configured','silenced','is_blocked','joined_at','updated_at']); src=inspect.getsource(database.seed_guild_config_if_absent); assert 'ON CONFLICT (guild_id) DO NOTHING' in src; assert 'DO UPDATE' not in src; assert hasattr(database,'load_all_guild_configs'); print('ok')"</automated>
  </verify>
  <done>
    `guild_config` DDL is in SCHEMA_SQL with all 7 columns; `load_all_guild_configs` and
    `seed_guild_config_if_absent` exist; the seed uses `ON CONFLICT (guild_id) DO NOTHING` and never
    `DO UPDATE`; CLAUDE.md documents the new table.
  </done>
  <acceptance_criteria>
    - `database.SCHEMA_SQL` contains `CREATE TABLE IF NOT EXISTS guild_config` with all 7 columns named above.
    - SCHEMA_SQL does NOT contain `ambient_roasts_enabled` or `vision_roasts_enabled`.
    - `inspect.getsource(database.seed_guild_config_if_absent)` contains `ON CONFLICT (guild_id) DO NOTHING` and does NOT contain `DO UPDATE`.
    - `database.load_all_guild_configs` exists and issues a single `SELECT ... FROM guild_config` with no `$N` params.
    - All `guild_config` SQL uses `$N` positional parameters — no f-string / `%`-format SQL.
    - CLAUDE.md §"Database Schema" mentions `guild_config`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: tests/test_database_phase18.py — schema shape + seed idempotence</name>
  <files>tests/test_database_phase18.py</files>
  <read_first>
    - tests/test_database_phase16.py (the exact mirror: `_SKIP_LIVE` pattern + `@pytest.mark.skipif` live-DB round-trip style + schema introspection)
    - tests/conftest.py (the `pool` fixture these live-DB tests consume)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-VALIDATION.md (§"Per-Task Verification Map" rows CONFIG-01 and CONFIG-05)
  </read_first>
  <action>
    Create `tests/test_database_phase18.py` mirroring `test_database_phase16.py`'s structure. Include:
    (a) a NON-live structural test that does `inspect.getsource(database.seed_guild_config_if_absent)`
    and asserts `ON CONFLICT (guild_id) DO NOTHING` present and `DO UPDATE` absent, plus asserts
    `guild_config` and all 7 column names appear in `database.SCHEMA_SQL` (CONFIG-01 shape — runs
    without a DB). (b) A live-DB test (guarded by the same `_SKIP_LIVE`/`skipif` idiom as phase16) that
    calls `init_db(pool)`, introspects `information_schema.columns` for `guild_config`, and asserts the
    7 columns exist with the expected types (TEXT/BOOLEAN/TIMESTAMPTZ) and that `configured`/`silenced`/
    `is_blocked` default false. (c) A live-DB seed-idempotence test: call `seed_guild_config_if_absent`
    twice for the same `guild_id` with DIFFERENT `ambient_channel_id` values; assert the first
    `ambient_channel_id` survives (row unchanged, proving `DO NOTHING`), and both calls return the same
    row. Use `@pytest.mark.asyncio` on every async test (strict mode).
  </action>
  <verify>
    <automated>pytest -q tests/test_database_phase18.py</automated>
  </verify>
  <done>
    Structural assertions pass without a DB; live-DB tests pass with `TEST_DATABASE_URL` set and skip
    cleanly without it.
  </done>
  <acceptance_criteria>
    - `pytest -q tests/test_database_phase18.py` exits 0 (structural tests run; live-DB tests skip without `TEST_DATABASE_URL`).
    - The seed-idempotence test proves a second seed with a different `ambient_channel_id` does NOT change the stored row.
    - Every async test carries `@pytest.mark.asyncio`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fix tests/conftest.py pool fixture — pgvector codec registration</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (current `pool` fixture: `asyncpg.create_pool(dsn)` with NO `init=`, no extension-first connection; teardown DROP list at lines 50-57 currently includes `user_playlist_tracks` but NOT `guild_config`/`user_memories`)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-PATTERNS.md (§"tests/conftest.py — pool fixture fix" — the copy-ready fix mirroring bot.py::_initialize_once lines 362-391)
    - .planning/phases/18-per-guild-config-foundation-ci-gate/18-RESEARCH.md (§"Open Questions" Q2 — the codec gap turns ~9 skipped tests into CI ERRORS unless fixed; §"Common Pitfalls" Pitfall 1)
  </read_first>
  <action>
    Rewrite the `pool` fixture to mirror `bot.py::_initialize_once`'s extension-first ordering:
    import `register_vector` from `pgvector.asyncpg`; open a throwaway `asyncpg.connect(dsn=dsn)`,
    run `CREATE EXTENSION IF NOT EXISTS vector;` on it, close it in a `finally`; THEN create the pool
    with `asyncpg.create_pool(dsn, init=register_vector)`. Keep the existing `try/except Exception:
    pytest.skip(...)` wrapper around the connect+create so a missing Postgres still SKIPS (not errors).
    Keep `await init_db(p)` and the `yield p`. Extend the teardown `DROP TABLE IF EXISTS ...` list to
    ADD `guild_config` and `user_memories` (do NOT remove `user_playlist_tracks` or any existing
    table); keep `CASCADE`. This is additive — the throwaway-connection dance and `init=register_vector`
    are the load-bearing changes.
  </action>
  <verify>
    <automated>python -c "import inspect; import tests.conftest as c; src=inspect.getsource(c); assert 'register_vector' in src; assert 'CREATE EXTENSION IF NOT EXISTS vector' in src; assert 'init=register_vector' in src; assert 'guild_config' in src and 'user_memories' in src; assert 'user_playlist_tracks' in src; print('ok')" && pytest -q tests/test_database_phase16.py</automated>
  </verify>
  <done>
    The `pool` fixture registers the pgvector codec extension-first; teardown drops `guild_config` +
    `user_memories` in addition to the existing tables; Postgres-absent still skips.
  </done>
  <acceptance_criteria>
    - `tests/conftest.py` imports `register_vector` from `pgvector.asyncpg` and passes `init=register_vector` to `create_pool`.
    - A throwaway connection runs `CREATE EXTENSION IF NOT EXISTS vector;` BEFORE `create_pool`.
    - The teardown DROP list includes `guild_config` and `user_memories` and still includes `user_playlist_tracks`.
    - With no `TEST_DATABASE_URL`/Postgres, the fixture still `pytest.skip`s (does not error).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime code → Postgres/Neon | `guild_id` / `ambient_channel_id` values flow into SQL |
| test harness → ephemeral test DB | conftest constructs a live pool against `TEST_DATABASE_URL` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-SQLI | Tampering | `guild_config` reads/writes (`load_all_guild_configs`, `seed_guild_config_if_absent`) | mitigate | All SQL uses `$N` positional asyncpg params — no f-string / `%` interpolation anywhere in the DDL or helpers, matching every existing `database.py` helper |
| T-18-XGUILD | Information Disclosure | `seed_guild_config_if_absent` / `load_all_guild_configs` | mitigate | Seed is scoped by `guild_id` in its `VALUES`/`WHERE`; load-all returns rows keyed by `guild_id` for a caller that indexes by `guild_id` — no implicit cross-guild write |
| T-18-SEED | Tampering | Home-guild seed row | mitigate | `ON CONFLICT (guild_id) DO NOTHING` (D-09) — the bootstrap env value can never overwrite a deliberate later write; `guild_id` derives from a config value (Task belongs to 18-05), never user input |
| T-18-CICREDS | Information Disclosure | conftest test-DB DSN | accept | `TEST_DATABASE_URL` defaults to an ephemeral local/CI DSN (`dexter:dexter@localhost/dexter_test`), zero overlap with production Neon credentials by construction (D-15) |
</threat_model>

<verification>
- `guild_config` DDL present with all 7 columns; seed uses `DO NOTHING`; `load_all_guild_configs` param-free.
- `pytest -q tests/test_database_phase18.py` and `pytest -q tests/test_database_phase16.py` both exit 0.
- conftest registers the pgvector codec extension-first.
- `pytest -q` full suite unchanged (still 0 fail; live-DB tests skip locally).
</verification>

<success_criteria>
- CONFIG-01: the 7-column `guild_config` table exists in SCHEMA_SQL and is created by `init_db`.
- CONFIG-05 (data half): `seed_guild_config_if_absent` is idempotent with `ON CONFLICT DO NOTHING`.
- CICD-01 (prereq): the conftest pool fixture registers the pgvector codec so live-DB memory tests pass rather than error under a real DB.
</success_criteria>

<output>
Create `.planning/phases/18-per-guild-config-foundation-ci-gate/18-02-SUMMARY.md` when done.
</output>
