# Requirements (PRD Intel)

> Source type: PRD. One entry per requirement, derived `REQ-{slug}` IDs.

No PRDs were present in this ingest set. All 6 ingested documents classified as SPEC (3) or DOC (3).

The ingested documents are design specs and implementation plans, not requirements documents — they describe *how* to build, with behavior captured as technical constraints (`constraints.md`) and implementation context (`context.md`). No user-story / acceptance-criteria requirements were extracted.

Downstream note for `gsd-roadmapper`: requirement-shaped intent must be derived from the constraints and context files; there are no pre-formed `REQ-*` units and therefore no competing acceptance variants of the PRD kind.
