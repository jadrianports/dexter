# Decisions (ADR Intel)

> Source type: ADR. One entry per architectural decision record.

No ADRs were present in this ingest set. All 6 ingested documents classified as SPEC (3) or DOC (3).

Several SPECs contain "Decisions Made" / "Architecture Decisions" tables. Per the classifier, these are summaries of design choices *within* implementation specs, not standalone locked ADR records — so they were extracted as **constraints** (see `constraints.md`), not as locked decisions. None carry `locked: true`.

No locked decisions exist in this ingest. There are therefore zero LOCKED-vs-LOCKED contradictions.
